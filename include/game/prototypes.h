/* game/prototypes.h — shared game prototype home.
 *
 * Stub for the prototype-recovery lane. Add declarations only when the
 * signature is evidenced; do not place definitions in shared headers.
 */
#ifndef GAME_PROTOTYPES_H_
#define GAME_PROTOTYPES_H_

#include "types.h"

#endif /* GAME_PROTOTYPES_H_ */
