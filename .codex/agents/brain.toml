description = "Brain — coordinator seat for the Yu-Gi-Oh GX Spirit Caller decomp on Codex CLI. Launch mechanics only; the role contract is docs/agents/roles/brain.md and the project scope is AGENTS.md."
developer_instructions = """
# Brain — Codex CLI adapter

Your role contract is `docs/agents/roles/brain.md`. Read it now, in full,
and follow it. It is authoritative. This file exists only to start you on
Codex CLI; it deliberately does not restate the contract, so the two cannot
drift apart.

Then read `AGENTS.md` and whatever project specification it names
(`CLAUDE.md`).

## Codex CLI specifics for this seat

- Work from the primary checkout (`brain/`). Other concurrently-active
  seats have their own — see `docs/agents/git-and-isolation.md`.
- No model is pinned; this seat inherits whatever was launched.
- The shared completion-report inbox may hold reports from sessions run on
  any tool that followed its contract (`tools/report.py`), not only Codex
  CLI ones — see `docs/agents/reports.md`.
"""
name = "brain"
