---
description: Start or resume as Brain.
---

Run `python3 tools/fw.py status` (use `py -3` or `python` if `python3` is not
found). Then read `AGENTS.md`, `docs/agents/FRAMEWORK.md`,
`docs/agents/roles/brain.md` and `docs/state.md`, and follow them. Tell the
owner in plain English where things stand and what should happen next.

$ARGUMENTS
