DECOMPER — round 0906. Item: q-handoff-guard-repair. BUILD-FREE. Effort HIGH.

You are the Decomper. The other lane owns the compiler. Do NOT compile, do NOT
run ninja, do NOT run gate3.py. Everything below is text, tests and CI.

PREFLIGHT — STOP and report on any failure, do not work around it:

    cd /Users/leo/Dev/spirit-caller/claude-decomper-queue
    EXPECT=/Users/leo/Dev/spirit-caller/claude-decomper-queue
    [ "$(git rev-parse --show-toplevel)" = "$EXPECT" ] || { echo "WRONG WORKTREE"; exit 1; }
    git fetch origin || exit 1
    git switch -c decomper/q-handoff-guard-repair origin/main || exit 1
    python3.13 -c "import sys; assert sys.version_info[:2] >= (3,11)" || exit 1
    python3.13 tools/work_queue.py next decomper --claim || exit 1

Your branch is decomper/q-handoff-guard-repair, cut from origin/main, which is
6ab979c3c. Lane identity is the ROLE, not a provider or a model — branches are
<role>/<scope> and tests/test_role_lane_neutrality.py fails closed on anything
else.

EVIDENCE RULE, and it is the one rule that decides whether this round counts.
Every number, verdict or before/after claim you report must be pasted together
with the exact command that produced it, in the same block. Do not restate a
number from this kickoff as though you had measured it — re-run it. If you did
not run it, do not report it. A sentence with no command under it will be read
as not done.

THE WORK — both handoff guards are installed, documented and inert. The brain
re-derived this on main at 6ab979c3c. This is the third "documented, installed,
inert" finding in three rounds, so the pattern is the target, not just the two
instances.

GUARD 1 — tools/queue_state_drift.py measures anchor staleness with
git rev-list --count --merges <anchor>..<ref>. The repo has squash-merged
exclusively since 2026-08-25 (efb512d32 is the last real merge commit), so
merges_since is ALWAYS 0 and the _STALE_MERGE_TOLERANCE branch is unreachable.
Brain-measured on 6ab979c3c — reproduce all three before you change anything:

    git rev-list --count --merges efb512d32..HEAD      ->  0
    git rev-list --count efb512d32..HEAD               ->  38

    python3.13 -c "import sys; sys.path.insert(0,'tools'); from pathlib import Path; import queue_state_drift as q; c=q.main_anchor_checker(Path('.'),'HEAD'); print(c('0b2f8c630'), c('676fed454'), c('efb512d32'))"

That prints (True, 0) (True, 0) (True, 0) — "fresh, zero behind" for anchors 4,
13 and 38 commits back. 676fed454 is the anchor round 0903 left stale.

GUARD 2 — tools/check_dispatch_log.py requires a dispatch row only when the
"Last updated:" BLOCK changes (that line through the next blank line). A round
appending its narrative anywhere else in docs/state.md skips the requirement
silently, which is exactly what round 0903 did.

NEITHER IS WIRED INTO CI. .github/workflows/generated-files-drift.yml runs the
three index --checks plus validate_attempts.py and nothing else. A guard no
workflow invokes is not installed, whatever the docs say.

SCOPE, in this order. Four items. Do not add a fifth.

1. Fix both guards so they measure what they claim. Count squash-merge commits
   (or PR-numbered subjects), not --merges. For guard 2, key the requirement on
   the round narrative changing at all, not on one paragraph.

2. Wire both into CI, in the workflow where they belong. Name the workflow you
   chose and give one sentence for why that one.

3. Correct docs/guard-coverage-review.md. It lists test_install_git_hooks.py as
   FIRES-CORRECTLY while the pre-push hook that test installs was inert for its
   entire life — the test audited the installer, not the behaviour. Change that
   row's verdict to INSTALLER-ONLY, and add this note under the table VERBATIM:

       This table records whether a guard is INSTALLED, not whether it FIRES.
       A row here cannot be cited as evidence that a guard catches anything.
       Only a behavioural canary — the guard shown green on a known-bad input,
       then red on the same input after a fix — is that evidence.

   Write no additional prose in this file beyond that note and the changed
   verdict. This item is an edit, not an essay.

CANARY — control 7, non-negotiable, do this BEFORE the fix. For EACH guard,
reconstruct the round-0903 tree (anchor 676fed454, main at 050f06c0f, no 0903
dispatch row) and show the guard GREEN on that input — i.e. failing to catch a
handoff that was genuinely broken. Then show it RED on the same input after
your fix. If either guard is already RED before your fix, your model of the bug
is wrong: STOP and report that instead of proceeding. Paste both runs.

4. Two boundary defects, both confirmed still present on 6ab979c3c. Take them
   after the guards:
   (a) tools/pool_freshness.py hardcodes the string "python tools/pool_freshness.py ..."
       as its own REPRODUCER output at lines 91 and 157, regardless of host. A
       Mac-host kickoff therefore prints a reproducer the Mac cannot run,
       because macOS has no plain python. Make the emitted reproducer name an
       interpreter that exists on the host it is addressed to.
   (b) tools/make_kickoff.py's LANE_WORKTREES mac paths (lines 22-34) are
       ~/Dev/spirit-caller/decomper and ~/Dev/spirit-caller/scaffolder. Neither
       exists on this Mac — the real lane worktrees are claude-decomper-queue
       and claude-scaffolder-queue. A generated Mac kickoff would send a lane to
       a nonexistent directory. Either make the mac paths match reality, or make
       the generator refuse to emit a path that does not exist. State which you
       chose, in one sentence.

SUB-AGENTS PERMITTED, READ-ONLY ONLY. The worktree is shared: dispatch
investigation as read-only agents with no write access, and do every write,
commit and git operation yourself, sequentially.

VERIFY YOUR OWN CLAIMS. Before writing the PR body or any commit message,
reconcile every sentence against git diff --stat origin/main..HEAD. An omitted
edit is byte-identical to a correct no-op and no test can tell them apart —
that is how PR #1383 shipped a change that was never written.

A SHORT ROUND IS AN ACCEPTABLE ROUND. Four items done and pasted beats six
claimed. Do not add scope to fill time, and do not summarise instead of pasting.

GATE — paste all of these outputs verbatim, not a summary of them:

    python3.13 -m pytest -q tests
    python3.13 -m unittest discover -s tests
    python3.13 -m ruff check .
    git diff --stat origin/main..HEAD

Paste the pytest summary line, the "Ran N tests" + "OK" line from unittest, the
git diff --stat output, and the before/after canary runs for both guards. A
self-report that the guards now work is not evidence; the pasted red/green pair
is.

When done: python3.13 tools/work_queue.py done decomper q-handoff-guard-repair,
commit, push the branch, run gh pr create, and reply with the PR URL.
