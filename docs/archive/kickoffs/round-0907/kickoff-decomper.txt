DECOMPER — round 0907. Item: q-handoff-guard-repair (CONTINUATION). BUILD-FREE. Effort HIGH.

You are the Decomper. This continues YOUR OWN PR #1620 on YOUR OWN branch — do
not open a new branch and do not re-do round 0906's work. The other lane owns
the compiler. Do NOT compile, do NOT run ninja, do NOT run gate3.py.

PREFLIGHT — STOP and report on any failure, do not work around it:

    cd /Users/leo/Dev/spirit-caller/claude-decomper-queue
    EXPECT=/Users/leo/Dev/spirit-caller/claude-decomper-queue
    [ "$(git rev-parse --show-toplevel)" = "$EXPECT" ] || { echo "WRONG WORKTREE"; exit 1; }
    git fetch origin || exit 1
    git switch decomper/q-handoff-guard-repair || exit 1
    [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/decomper/q-handoff-guard-repair)" ] || { echo "BRANCH DIVERGED FROM PR #1620"; exit 1; }
    git merge --no-edit origin/main || exit 1
    python3.13 -c "import sys; assert sys.version_info[:2] >= (3,11)" || exit 1

origin/main is 7b8b6bd3f. Lane identity is the ROLE, not a provider or a model —
branches are <role>/<scope> and tests/test_role_lane_neutrality.py fails closed
on anything else.

EVIDENCE RULE. Every number, verdict or before/after claim you report must be
pasted with the exact command that produced it, in the same block. Do not
restate a number from this kickoff as though you had measured it — re-run it. A
sentence with no command under it will be read as not done.

WHAT ROUND 0906 GOT RIGHT, so you do not undo it. The brain re-ran both of your
canaries independently and they reproduce exactly: guard 1 old (True, 0) -> new
(True, 8) against tolerance 2, and guard 2 old ok=True -> new ok=False on the
real PR #1614 commit. The squash-subject counter is correct, failing closed on a
git error was a good call nobody asked for, and the temp-repo test is a real
behavioural test. All six CI checks are green. Three defects remain.

DEFECT 1 — GUARD 2 NOW BLOCKS LEGITIMATE WORK. This is the round's main item.
Your guard fires on ANY docs/state.md change. The brain ran it against real
merged history:

    PR #1610 housekeeping: archive state.md      -> BLOCKED
    PR #1611 housekeeping 2: state.md metric fix -> BLOCKED
    PR #1612 workflow review                     -> BLOCKED
    PR #1619 round 0905 (a real round)           -> passes

drift-check is a REQUIRED status check in the main-protection ruleset, so those
three would have been merge-deadlocked. The next housekeeping PR that touches
state.md deadlocks too.

DO NOT "FIX" THIS BY NARROWING THE TRIGGER. The brain already tested the obvious
narrowing — require a row only when a NEW round heading appears in state.md — and
it clears all three housekeeping PRs but ALSO clears PR #1614, the exact defect
this guard exists to catch, because round 0903's whole failure was that it never
opened a heading. It appended two paragraphs into the round-0902 section and left
main-sha stale. Reproduce that yourself before you design anything:

    git diff 050f06c0f^ 050f06c0f -- docs/state.md

A purely textual discriminator cannot separate "round narrative" from
"housekeeping prose" here. So keep your broad trigger, which is correct, and add
an EXPLICIT OPT-OUT: a PR that is deliberately not a round declares it, and the
guard honours the declaration. Choose the mechanism — commit-message trailer, PR
title token, or a marker file — and state in one sentence why you chose it over
the other two. Requirements: it must be visible in the git history or the PR
metadata that CI already has, it must not be settable by accident, and an
undeclared state.md change must still fail.

ACCEPTANCE TABLE, non-negotiable, paste the run. Your guard must produce exactly
this verdict for these five real commits — three that must pass WITH the opt-out
declared, one that must fail, one that must pass without needing it:

    19296d3fa  PR #1610 housekeeping     -> pass with opt-out, FAIL without
    41930febd  PR #1611 housekeeping     -> pass with opt-out, FAIL without
    55cada2d2  PR #1612 workflow review  -> pass with opt-out, FAIL without
    050f06c0f  PR #1614 round 0903       -> FAIL (no opt-out may rescue it;
                                            it IS a round and must be caught)
    6ab979c3c  PR #1619 round 0905       -> pass, opt-out not needed

DEFECT 2 — THE 0903 CANARY IS NOT IN THE DIFF. Your PR body says the PR
"Includes regression tests and round-0903 behavioral canaries". It does not:
grep tests/ for 0903, 676fed454 or 050f06c0f and you get nothing. You ran the
canaries live, which is what round 0906 asked for, but nothing was committed —
so the next audit of docs/guard-coverage-review.md hits exactly the gap the note
you added in that same PR warns about. Commit both canaries as real tests
pinned to those real commits, and correct that sentence in the PR body.

DEFECT 3 — ITEM 4(b) WAS HALF-DONE. You fixed the decomper and scaffolder mac
paths via _mac_worktree(), and left kb-map and kb-types pointing at
~/Dev/spirit-caller/kb-map and ~/Dev/spirit-caller/kb-types. Neither exists.
The brief offered two routes — make the paths match reality OR make the
generator refuse to emit a path that does not exist — and neither was applied to
these two lanes, so the generator can still send a lane to a nonexistent
directory. Close the DEFECT CLASS, not the two instances that were named by
hand. While you are in that file, the comment above LANE_WORKTREES still says
mac "remains explicitly UNVERIFIED until a Mac brain runs and confirms the
generated paths" — a Mac brain has, and the paths changed. Correct it.

SCOPE. Those three defects, in that order. Do not add a fourth.

SUB-AGENTS PERMITTED, READ-ONLY ONLY. Do every write, commit and git operation
yourself, sequentially.

VERIFY YOUR OWN CLAIMS. Before writing any commit message or updating the PR
body, reconcile every sentence against git diff --stat origin/main..HEAD. Defect
2 above is exactly this failure mode caught in your own last round: an omitted
artifact is byte-identical to a correct no-op, and only the diff can tell them
apart.

A SHORT ROUND IS AN ACCEPTABLE ROUND. Three defects closed and pasted beats five
claimed.

GATE — paste these outputs verbatim, not a summary: paste the pytest summary
line, the unittest "Ran N tests" + "OK" line, and the git diff --stat output.

    python3.13 -m pytest -q tests
    python3.13 -m unittest discover -s tests
    python3.13 -m ruff check .
    git diff --stat origin/main..HEAD

Also paste the acceptance-table run for all five commits, and the before/after
canary pair proving the opt-out does not rescue 050f06c0f.

When done: python3.13 tools/work_queue.py done decomper q-handoff-guard-repair,
commit, push the branch, update the PR #1620 body, and reply with the PR URL.
