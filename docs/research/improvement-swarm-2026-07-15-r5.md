# Improvement swarm — round 5 (2026-07-15)

**Method.** 18 lenses across 5 domains (docs, tools, workflow, research, strategy), each producing findings against the live tree at `main` (b3a5f3e9 / 208991da). Every finding was routed to an independent verifier operating refute-by-default: a single unsupported load-bearing claim kills the row. **86 findings raised, 48 survived** verification (55.8%). Findings were deduped against the r1/r2 (2026-07-09), r3 (2026-07-09), and r4 (2026-07-12) reports plus the adopted/deprioritized/dead ledger; a row survives only if it refutes the prior evidence or brings work the ledger never scoped.

This is round 5. It is the first round run entirely on the far side of byte-coverage, and it reads differently as a result.

## The honest read

- **Byte-coverage is done and the ledger that measures "what's left" measures the wrong thing.** Re-derived from committed delinks, the real 3-region code gap is 61,404 B (EUR 13,432 / USA 24,044 / JPN 23,928). `endgame-ledger.md` explains 11.6% of it. Its EUR row is wrong on all three terms — blobs closed by #1146, the 11 ov006 parks are phantom (that module's gap is literally 0), the 39 ITCM funcs are 11 EUR + 14 USA + 14 JPN. The largest single gap, ov004 at 20,110 B (33%), appears in **no row at all**. We have been steering the endgame with a hand transcription of brief prose.
- **The floor numbers underneath it are unproven, not merely noisy.** `batch_carve.whole_function()` never reads `returncode`, so asm_escape's exit-2 infrastructure errors ("run `ninja` first", "did not assemble") return the byte-identical verdict as a genuine mismatch: `verify-fail` — the bucket every residual row is built from. b577 already shipped real `ninja sha1` fixes for two functions the ledger lists as permanent floor. Cross-region reproducibility does not rescue the floor: deterministic tool bugs reproduce identically across regions.
- **Readable-C at ~8% has a ceiling nobody has tested.** Largest matched C TU in project history: 544 B. Exactly 8 matched C TUs exceed 512 B — 7 are one ov006 clone family, the 8th is an ov004 Thumb file. So there is ~one demonstrated large match that didn't ride a sibling. Meanwhile 869 .s TUs / 1,112,124 B (51% of residue, +46.6pp of C%) sit above that line, and 0.6% of them have a clone sibling. The ">544 B is a wall" verdict comes from a build-free triage with a documented 38% mis-binning rate on exactly that tier. The largest function ever *attempted* is 484 B — and it failed on comprehension, not register allocation.
- **The arithmetic is worse than the trajectory implies.** 78.3% of EUR's denominator is two modules (arm9 + ov002) sitting at 6.58% C. Driving *every other module* to 100% caps the headline at 26.89%. ov002 alone at 100% would put the project at 53.26% — and ov002 is excluded from the overlay C queue **by rule**, while no tool reports C% per module at all.
- **Nothing gates `main`, so the swarm's own output is unenforced.** No branch protection, no rulesets. 11 of 12 recent merges landed with ≥1 failing check. Lint has been red for 17 straight days on 3 ruff errors and 7,186 markdownlint errors that are 99.7% machine-generated. The invariants error channel is saturated at exactly 3, which means the `missing_tu_source` guard it exists for produces a byte-identical red X whether or not it fires. Red is the baseline; a real regression is invisible.
- **Highest-leverage next move: the 544 B ceiling probe.** One agent, hard-timeboxed, 3 singletons from the above-ceiling pool (2× ov002 + 1× arm9, never main-only — the repo already has precedent for overlay→main non-transfer), via m2c + the live post_edit objdiff hook. Binary verdict. If it lands, 1.11 MB of runway opens and rounds get redesigned around singletons with a size floor. If it doesn't, we state publicly that readable-C caps at ~11% under current method and stop selling a byte-% trajectory we cannot deliver. Everything else on this list is cheaper, and none of it changes what the campaign *is*.

## Top 10 improvements (ranked)

1. **Probe the 544 B C-match ceiling — the one question the whole campaign rests on.** 51% of residual mass sits above a line we have crossed once, non-clone. The wall verdict is a prediction from a build-free triage that mis-binned 38% of that same tier; b533 identified six >544 B functions and attempted zero. *Action:* 1 agent, timeboxed, 3 singletons (2× ov002 + 1× arm9), m2c-scaffolded, per-file `.o` + ELF `.text`-size guard before any full build. Binary verdict published either way. **Effort: days.**

2. **Re-derive the endgame ledger from bytes, not prose.** The instrument is wrong, not stale: 11.6% of the real gap explained, EUR row wrong on all three terms, ov004's 20,110 B invisible. *Action:* extend `tools/size_census.py` (do NOT greenfield — fix its `.text`-only scan, 71% `.init` false-positive rate on EUR, and its ITCM blindness), surface via `progress.py --json`, regenerate the ledger. Strike §6, refile §7, add an ov004 row. **Effort: hours.**

3. **Fix batch_carve's exit-code conflation, then re-census the floor.** `whole_function()` (:374) and `classify()` (:350) never read `returncode` while the same file reads it six times elsewhere. Infrastructure failure and genuine mismatch produce identical `verify-fail` verdicts. *Action:* capture returncode, add a `tool-error` verdict that is never parked and never counted as floor, require the "did NOT verify" marker before returning `verify-fail`, unit-test the three canned outputs (the `_run` mock seam exists), then re-derive ledger §1/§6 on a warm tree. **Effort: day.**

4. **Green main (~1 day, measured), then require 5 checks on a ruleset with no owner bypass.** `branches/main/protection` → 404; `rulesets` → `[]`. Every "blocks merge" claim is decoration. Ruff is 3 errors (2 auto-fixable); markdownlint `--fix` over 3 generated dirs was *executed* → 7,186 → 22; regenerating both indices flips drift-check **and** unittest green in one shot. *Action:* green it, then require `Python (ruff)`, `Markdown (markdownlint-cli2)`, `unittest`, `drift-check`, `pr-invariants (eur)`; exclude usa/jpn legs (continue-on-error does NOT green a check-run — requiring them would hard-block); pin `ruff-action`. **Effort: day + hours.**

5. **Add `progress.py --by-module` and re-scope the C lane against the 26.89% ceiling.** No tool, SVG, or doc reports C% per module. ~15-20 lines reusing `parse_delinks_file`, build-free, verified exact. Put arm9 and ov002 C% on the dashboard, and revisit b564's by-rule ov002 exclusion explicitly. **Effort: hours.**

6. **Fix AGENTS.md:18 and add a 6-line CLAUDE.md "Start here" block.** The roster still says scaffolder cannot run `ninja rom`/`dsd check`/`ninja objdiff` — in a worktree with 3 baseroms, dsd 0.11.0, objdiff 2.7.1, and 300+ `[auto, usa sha1 OK]` commits proving it self-gates. b561's fix touched 2 of 3 copies. Meanwhile CLAUDE.md links to none of AGENTS.md / state.md / docs/agents / gate3 and calls the canonical metric tool a "0% stub". Under an hour of pure doc editing for the capability model and the entry point every agent reads before every task. **Effort: hours.**

7. **Recover the `.thumb` routing tier in the objdiff/report lane, and close the family with a shared constant.** `objdiff_filter_panic_units.py:300-321` returns `None` for `.thumb.c`, so 36/36 EUR thumb units are dropped as "unmatched routing-tier" while shipping and complete. One-line fix, monkeypatch-verified (+36 kept, clears the downstream gate). This is the 4th site of the brief-569 suffix-strip family — one of which (`port_to_region.py:1152`) means no Thumb function can port at all. *Action:* fix the drop, then `tools/routing_suffixes.py` + conformance test, migrating live-buggy → correct → latent. **Effort: hours → day.**

8. **Bump objdiff v2.7.1 → v3.7.3 and retire panic-filter case (a).** Our own issue #352 is CLOSED (2026-07-10) by the maintainer, who merged PR #359 and closed #361/#365 as dupes; the fix is verified at the tag and v3.7.3 ships prebuilts for all three hosts. The DEAD row ("we own it forever") is refuted on all three clauses. *Caveats:* 251 commits / 186 files, v3.0.0 self-describes as a rewrite, 10 report.json consumers ride this, and the real untested gate is dsd v0.11.0's objdiff.json ↔ v3 config schema. Keep case (b). **Effort: hours → day.**

9. **Add `default rom sha1` to build.ninja and stop the bare-`ninja` hard-stop on Windows.** No `default` statement means bare `ninja` schedules 3,272 real m2ctx `.ctx.c` edges; `gcc` is absent on Windows and `m2ctx.py:62` catches only `CalledProcessError`, so the first raises an uncaught `FileNotFoundError` and `-k 1` aborts everything. `ninja -n` drops 3,284 → 5 with one line. CLAUDE.md:57 and BUILD.md:48 both prescribe the broken command; `optimal-match-loop.md:120` explains it wrongly on all three counts. **Effort: hours.**

10. **Widen `check_delink_dupes.py` to `libs/` and fix `configure.py`'s region-blind baserom errors.** The gate3/batch_carve merge preflight only matches `^src/`, so all 181 `libs/` delink blocks — interleaved directly into main's carve range — are invisible to the exact `dsd lcf` overlap abort it exists to catch; `sort_delinks.py:49` already does the right parse. Separately, `configure.py eur` tells cold-start users to source a **US** ROM (AYXE) when EUR is AYXP, on the first command a new machine runs, on the priority region. Both are one-regex/one-dict fixes with zero test exposure. **Effort: hours.**

## docs

### S — every agent, every session; verified twice each

**AGENTS.md's roster row still says scaffolder "Cannot run `ninja rom` / `dsd check modules` / `ninja objdiff`" — the ledger records b561 as having killed that text.** The stale claim sits at AGENTS.md:18, in the file :4/:124 makes rule #1 reading and :26-28 declares the SOURCE that `.claude/agents/*` are "derived from". Ground truth in the live scaffolder worktree: 3× baserom (134,217,728 B each), dsd 0.11.0, objdiff-cli 2.7.1, `ninja -t targets` resolves rom/sha1/check/objdiff/report, `tools/gate3.py --help` exits 0, and 300+ commits titled `[auto, usa sha1 OK]` prove the lane self-gates. b561's fix commit `0862afb9` touched exactly 2 files (`.claude/agents/{scaffolder,decomper}.md`) — migration was 1-of-3; AGENTS.md:18 and docs/decomp-workflow.md:52 still carry it. `git log -S "without the full build pipeline" -- AGENTS.md` = 1 commit (9d5da1e2, 2026-05-25), untouched ~7 weeks. Same row also lists `src/` + `config/**/symbols.txt` hands-off, which every batch_carve wave legitimately violates.
**Action:** rewrite AGENTS.md:18 to match `.claude/agents/scaffolder.md:15-21` (full toolchain + 3 baseroms, self-gates via gate3, no delegate-to-brain); move `src/**/*.s` + `config/<ver>/**/{symbols,delinks}.txt` (batch_carve mechanical only) to owns; same edit at docs/decomp-workflow.md:52; reconcile the "derived from" direction — the derived copies are currently the accurate artifact. Routes via PR. **Effort:** hours

**CLAUDE.md is the auto-loaded entry point, links to none of the orientation set, and calls the canonical metric tool a "0% stub".** `grep -c` over CLAUDE.md: AGENTS.md=0, docs/state.md=0, docs/agents=0, gate3=0, worktree=0, decomp-workflow=0, batch_carve=0. README.md and BUILD.md both point *into* CLAUDE.md and also carry 0 refs to AGENTS.md/state.md/gate3 — all three top-level docs funnel into a dead end. CLAUDE.md:79 teaches single-region `ninja sha1` as "the only test that actually matters" while gate3.py:26 calls itself "the canonical CLAUDE.md re-verify command" (gate3 mentions: brain.md=10, scaffolder.md=11, CLAUDE.md=0). :103-118 "Known placeholders" is 3-of-4 false: progress.py is 423 lines/3-tier and prints 99.44/88.23/100.00/8.03 (`print_stub`:301 never fires); "EUR has 23 overlays / USA/JPN TBD" vs 24 (ov000-ov023) + 27 delinks.txt in all 3 regions — contradicted by CLAUDE.md:146-150's own "All 27 modules × 3 regions check green". Last commit 2026-06-01, predating the entire b561-578 wave; no ledger row has ever touched it.
**Action:** add a ~6-line "Start here" block (AGENTS.md, docs/state.md, docs/agents/brain-onboarding.md, docs/decomp-workflow.md, `python tools/gate3.py`); delete placeholders 1/2/4 and fold the 3-revision SP routing into the :17-27 table. Do **not** delete item 3 wholesale — the NitroSDK version is genuinely still undocumented repo-wide; retitle to that alone, drop the overlay-count/dsd-init clauses. **Effort:** hours

### A

**The Windows worktree convention is documented backwards, its "no copy needed" baserom claim is structurally impossible, and no doc says which worktrees can build.** AGENTS.md:91-104 + docs/agents/worktree-mechanisms.md:34-35 assert Windows = Mechanism B (auto `.claude/worktrees/` sandboxes, "No manual `git worktree add` needed", "share the main checkout's `orig/` baseroms"). Live host: `git worktree list` = 5 manual siblings (brain, decomper, kb-map, kb-types, scaffolder) = Mechanism A, the documented *Mac* convention; zero `.claude/worktrees/` anywhere. Sharing is false on its own terms — `.gitignore:22 *.nds` + 0 tracked `.nds` means a git worktree cannot inherit `orig/`, and AGENTS.md:70-76 prescribes `cp orig/baserom_*.nds`. Provisioning is heterogeneous and undocumented (brain 3/no objdiff, decomper 3/full, scaffolder 3/full, kb-types 1 eur-only, kb-map 0/no dsd), and gate3.py:56 `REGIONS=[eur,usa,jpn]` + :67-68 CONFIGURE-FAIL-no-skip makes default `--scope all` unrunnable in kb-types/kb-map — neither of which is rostered anywhere despite holding live brief branches (576/578). The same diet also tore the section mid-sentence: AGENTS.md:97 ends on a dangling comma, worktree-mechanisms.md:5 opens on the stranded predicate; `9c746435^` shows one sentence cut in half (other 3 extractions clean).
**Action:** replace the Mac=A/Windows=B mapping with the live layout; heal the seam by BOTH deleting AGENTS.md:93-97 and restoring worktree-mechanisms.md:5 to the full pre-diet sentence (it is split, not duplicated — deleting alone destroys content); fix the ":18-20 no copy needed" sentence with the real seeding step; add a worktree → baseroms → runnable-gate3-scopes table into worktree-mechanisms.md, **not** AGENTS.md (32,420 B vs the 32,768 cap = 348 B headroom). **Effort:** hours

**The research index carries no outcome field — which is why each swarm round hand-rebuilds the NEGATIVE/DEAD ledger from four prior reports.** `generate_research_index.py:226` globs `RESEARCH_DIR.glob("*.md")` — top level only (418 rows / 419 notes), leaving 2,502 nested research .md unindexed (85.6%). Columns are File|Title|Summary; 0 of 420 notes carry front-matter; README has ZERO occurrences of NEGATIVE/DEAD/NO-GO/WITHDRAWN/REFUTED. So "was this tried, did it fail?" is unqueryable. Supersession too: safe-queue-v3.md (45,231 B, 11 referring briefs) has 0 supersede markers though v4:87 shows 99 of its 107 rows resolved (92% stale). A convention already exists and is useless: 52 of 419 notes carry a free-form `**Status:**` prose line ("triaged", "scouted", "sized + de-risked"). (The finding's "LOCKED 137" is a BLOCKED substring artifact — real ≈ 9 files — which *shrinks* the backfill to the ~20 the action already scopes.)
**Action:** add `status:` front-matter (open|adopted|negative|deprioritized|superseded + `supersedes:`/`superseded_by:`), parse it, emit a Status column + a NEGATIVE/DEAD section; `--check` is already CI-gated. Backfill only the ~20 list-3 rows + safe-queue-v2/v3 + path-to-100-coverage. Widen the glob to campaign-analytics/ + retriage/ (skip dossiers/ — only 145/1268 and 95/1068 map to a src .c); widening needs the link cell switched from `path.name` to a relative path. Audit the gate first: `--check` exits 1 on main *right now* (brief-575-ov002-mopup.md missing) — it already let a note slip. **Effort:** days

**docs/briefs/CLOSED-LOG.md has one commit in its life and is 29 briefs stale — relocating it out of AGENTS.md killed the update reflex.** `git log -- docs/briefs/CLOSED-LOG.md` = exactly 1 commit (778b6d4b, 2026-07-08). Before the move the history lived in AGENTS.md, which took 44 commits in the prior 30 days, 42 of them appends (143 entries). Since: AGENTS.md 4, CLOSED-LOG 0. Decisive: post-move merge commit `3d298b64` ("merge 552…, queue 554/555") touched AGENTS.md + docs/state.md and *not* CLOSED-LOG — the ritual survived the move, the append did not follow the file. The log stops at 549; 550-578 absent, i.e. every item the r5 ledger lists as adopted-since-r4. 3,963 lines, 1 heading, no TOC, no generator, no gate. Not a deliberate freeze — 4 live pointers (AGENTS.md:487/:528, state.md:34, brain.md:123) still call it the current history.
**Action:** do NOT build the proposed generator — false premise: the `**Brief:** NNN` line is absent from 12 of the 15 briefs 550-578 that have notes, 14 of 29 have no research file at all, and regeneration would DELETE ~43 pre-convention entries (178-197 etc.). Take the fallback: add "append the merged brief to CLOSED-LOG.md" to the merge ritual in `.codex/agents/brain.toml` + `.claude/agents/brain.md`, and backfill 550-578 (15 from notes, 14 from PRs). **Effort:** day

### B

**506 of 1,663 relative links in docs/ (30.4%) are broken; 97.6% from one root cause, and 183 were created by the CLOSED-LOG relocation itself.** Independently re-scanned: 12 files, mass concentrated in CLOSED-LOG.md 183, safe-queue-v2 150, safe-queue-v3 150, state.md 8, contained-reshape-catalog 6 — all writing repo-root-relative paths as document-relative links. Archaeology proves it: `778b6d4b^:AGENTS.md:2309` holds the identical link string, correct from the root; the relocation's own boast "Nothing below was edited, only relocated" (CLOSED-LOG.md:7) *is* the defect. safe-queue-v2/v3 were authored under campaign-analytics/ and never worked. No CI validates targets (lychee|markdown-link-check|linkcheck = 0 hits; MD051 is fragments-only). Prototype test written and run: 50 lines, stdlib, 3.0s, no baserom; fix verified — 494/494 resolve under `../`×depth.
**Action:** add `tests/test_docs_links.py` as a `unittest.TestCase` (a bare pytest file is silently skipped by `unittest discover`), exempting build/ + extract/ (2 gitignored targets fail in clean CI); host it in **lint.yml**, not tests.yml — that workflow's `paths:` excludes docs/**, leaving it dormant on exactly the PRs it guards. Then apply `../../` (CLOSED-LOG) / `../../../` (safe-queue). Residue needs human calls: 2 refs dead from anywhere, 6 catalog refs point at the sibling `brain/` worktree. Land with the CLOSED-LOG item above — same file. **Effort:** hours

**docs/state.md's "no-build source of truth" reports USA/JPN code at 89.83% — 9.16pp stale — ~90 lines below its own header announcing the merges that closed the gap.** :109-115 (dated 07-12, unmarked as Prior though the file marks superseded blocks at :128/:144) vs live `tools/progress.py` on the same committed-delinks tier: EUR 97.41→99.44 / C 8.03; USA 89.83→98.99 / 7.11; JPN 89.83→99.00 / 7.11. `git log -L 109,116` shows the block last touched by 04a9c051 (07-12) while b563's merge dcff5d57 (07-15) rewrote `config/{usa,jpn}/**/delinks.txt` — the exact tier progress.py reads — without refreshing it, under a ":11 Last updated: 2026-07-15" header that announces that merge. A brain reading cold scopes ~2 more drain waves for a finished drain. It has rotted twice (prior fix 43134d63 = "fix state.md's 25pp-stale numbers"). Queue line :22-24 also lists b575/b576/b578 pending though all merged (#1145/#1144/#1146).
**Action:** refresh from progress.py, redate, cite b563+b572; demote the :53 duplicate under a `---` "**Prior**" header; fix the Queue line. Durable fix: drop the hardcode for a pointer, or add progress.py to the brain's end-of-session step. **Effort:** hours

**docs/briefs/ froze at 179 eight weeks ago, AGENTS.md refutes itself about it inside one section, and `.codex/agents/brain.toml` still routes every brief there.** Last numbered brief 2026-05-21 (3d6ac0ac); live briefs are `docs/research/brief-NNN-*.md` (336 files, highest 578) ≈ 399 briefs of drift. AGENTS.md:413 "it goes into `docs/briefs/NNN-<slug>.md`" vs :428 "Since brief 180, briefs are inline-spec in the kickoff message" — same §Task briefs, so a top-down reader obeys the dead rule. The correction reached no prompt (`grep -rlinE 'inline-spec|brief 180' .claude/agents/ .codex/agents/` = 0) while brain.toml:57 instructs briefs into the dead dir and :70-71 to regenerate its index; decomper.{md:60,toml:44} route readers there too. `generate_briefs_index.py --check` runs green on every PR over a frozen 172-file corpus. Wider pattern: brain.toml:49,78 *also* still carries the 24/27 gate baseline the ledger marks b561-fixed — the `.codex` copies are a systematic straggler across ≥2 ADOPTED doc-fix rows.
**Action:** rewrite §Task briefs to lead with inline-spec, demote the file format to history (reclaims AGENTS.md bytes); strike brain.toml:57,70-71 and decomper.{md:60,toml:44}. Then `git mv docs/briefs/[01]*.md docs/briefs/archive/` and drop `--check` from the drift gate — the generator hard-fails on an empty dir (:167-169), and the banner alternative would itself drift-FAIL unless templated into `render_index()`. **Effort:** hours

**configure.py's baserom errors hardcode US/AYXE for every region — the first cold-start command misdirects EUR users to the wrong dump.** :289/:292 (missing-file) and :318 (hash-mismatch) are static, no conditional on `version`. Reproduced: `verify_baserom('eur')` prints "Drop a clean US-region … ROM at: orig\baserom_eur.nds / (Game code: AYXE…)" — EUR is AYXP. The mismatch path is worse: it prints EUR's *correct* expected hash `1da50df7…` while asserting "we require AYXE / US". Correct codes already sit at :115-117 and CLAUDE.md:6-10. `main()` calls verify_baserom at :402 before the build.ninja write at :406 — literally the first thing a cold start hits, on the priority region, on the one un-automatable step. `git log -S "US-region"` = 1 commit (06bb8c66, day-one scaffold): a US-template artifact predating the EUR-first pivot; 0 test references, so nothing breaks.
**Action:** promote :115-117 to a `GAME_CODE` dict keyed by version, interpolate into both messages, print the expected SHA-1 on the missing-file path, and fix the 4th instance at `orig/README.md:3`. Drop the finding's BUILD.md:20-21 rider — that promise is scoped to hash-check failures, which do print the hash. **Effort:** hours

**Biggest win:** the two S entries — AGENTS.md:18 plus a 6-line CLAUDE.md "Start here" block — under an hour of pure doc editing, and they fix the capability model and the entry point that every agent reads before every task.

## tools

**S — `objdiff_filter_panic_units.py` silently drops the entire `.thumb` routing tier (36/36 EUR units) from the objdiff/report pipeline**
`_legacy_suffix_from_source()` (tools/objdiff_filter_panic_units.py:300-321) recovers `.legacy.o`/`.legacy_sp3.o` but returns `None` for `.thumb.c`, so every `.thumb.c` unit is dropped before `objdiff_resolve_relocs.py` and `ninja report` see it — labelled "target_path missing — unmatched routing-tier unit" even though it is matched and shipping (units carry `"complete": true`; EUR `ninja sha1` is byte-identical with them linked). Verified by running the real filter on live `objdiff.json`: 10680→9707 kept, thumb 36→0, legacy 296/296 and legacy_sp3 107/107 survive — the asymmetry isolates the helper. Resolved-sidecar coverage: `.thumb` 0/36, `.legacy` 256/305, plain 9292/12055. Applying the proposed one-liner by monkeypatch recovered all 36 (kept +36) and they clear the downstream gate too. Root cause is chronological: the legacy fix landed 2026-05-23 (brief 199), the thumb tier 2026-06-09 (brief 393) — never propagated; the test file has zero `thumb` references and drop reasons print to stdout only, so 36 drops/build went unnoticed for a month. Combined with brief-569's still-open `post_edit.py` gap, the ov004 Thumb lane has **no** automated match feedback from either channel. **Action:** add `if source_path.endswith('.thumb.c'): return '.thumb.o'` mirroring the two existing branches; add a `.thumb.c` case beside `test_legacy_o` (tests/test_objdiff_filter_panic_units.py:191); verify `*.thumb.o.resolved` goes 0→36. *(hours)*

**S — `emit_data_blob.py --overlay` is dead on arrival: wrong overlay bin filename**
Every `--overlay ovNNN` invocation fails in all 3 regions for every overlay: tools/emit_data_blob.py:149 builds `overlay{id:04d}.bin` and the :154 fallback glob repeats the same wrong prefix+padding, while dsd extracts `ov{id:03d}.bin`. Live: `--overlay ov006 --addr 0x021b5c28 --dry-run` → `REFUSE: no overlay bin for ov006`, exit 1; `module_binary_and_base('eur','ov002'|'ov004'|'ov006')` all raise, while `_read_overlay_base` returns correct bases — purely the filename. `overlays.yaml` itself carries `file_name: ov006.bin`, and containment_check.py:277 / cross_region_cluster_apply.py:199 / predict_walls.py:267 all use the right convention — emit_data_blob is the sole outlier. Untested by construction (`extract/` is gitignored, absent from CI), so 23 tests pass green over a 100% dead branch, and the error text misdirects ("your extract is incomplete") while `ov006.bin` sits on disk. Load-bearing: endgame-ledger §6's 11 EUR ov006 verify-fail parks (021b5c28…021cb02c) are the only remaining EUR contingent residual and this is their designated tool; bytes at that offset verified to decode as real ARM once the name is fixed. **Action:** extend `_read_overlay_base` into `_read_overlay_entry` returning base+`file_name` (or minimally `f"ov{id:03d}.bin"` at :149/:154); add a synthetic temp-extract-tree + ROOT-monkeypatch regression test per brief 578's own IO pattern. *(hours)*

**S — `build.ninja` has no `default` statement: bare `ninja` schedules 3,272 m2ctx edges that hard-stop the build on Windows**
`tools/configure.py` never emits a `default`, so bare `ninja` targets every root node — `ninja -n` = 3,284 actions, of which 3,272 are real (non-phony) `m2ctx` `.ctx.c` edges that exist as outputs on no edge's input side. `gcc` is absent on Windows and m2ctx.py:62 catches only `CalledProcessError`, so the first raises an uncaught `FileNotFoundError: [WinError 2]` and, under ninja's `-k 1` default, aborts everything (reproduced, exit 1). No `.ctx.c` has ever been built on any region. CLAUDE.md:57 and BUILD.md:48 both prescribe bare `ninja` as THE matching-loop step. Proven fix: appending `default rom` to a copy drops `ninja -n` 3,284→5. `docs/research/tooling/optimal-match-loop.md:120` explains it as "the m2ctx phony fails silently but wastes time" — wrong on all three counts. **Action:** add `n.default(...)` after tools/configure.py:1077; prefer `default sha1` or `default rom sha1` over bare `rom`, since `rom` alone would silently drop `dsd check modules/symbols --fail`, report, progress and sha1 from bare `ninja` on macOS/Linux where gcc exists. Then fix CLAUDE.md:57, BUILD.md:48, and optimal-match-loop.md:120. *(hours)*

**A — `check_delink_dupes.py`, the gate3/batch_carve merge preflight, is blind to all 181 `libs/` delink blocks**
`_FILE_RE = re.compile(r"^(src/\S+):\s*$")` (:39) only matches `src/...` TU headers, so a `libs/...:` header never sets `cur` and the block's `.text` line is dropped — a duplicate address claimed by a libs file is reported as clean, i.e. exactly the `dsd lcf` "overlaps with previous file" merge abort the tool exists to catch (docstring cites PR #948/#198). Reproduced on the shipped `find_dupes()`: libs-vs-libs and src-vs-libs at 0x02000b60 both return `{}`; src-vs-src fires. Surface: 181 libs blocks (178 with `.text`) across 12 delinks.txt in all 3 regions, interleaved directly into main's carve range (config/eur/arm9/delinks.txt:379-386). Oversight, not scope: sort_delinks.py:49 does the identical parse with `^\S.*:$` and its comment names the `libs/nitro/..legacy.c:` shape explicitly; the docstring's stated rationale (ignore section headers) is satisfied by any `^\S` anchor, so the `src/`-only narrowing buys nothing. Fix measured safe: `^((?:src|libs)/\S+):\s*$` over all 81 delinks.txt → 0 dupes, 0 false positives, existing 5 tests green. Amplifier: zero CI references — it runs only at gate3.py:154 and batch_carve.py:830, on the merged integration tree, the one place batch_carve's in-run `audit()` cannot help. **Action:** widen the regex, fix the :20-21 docstring, add libs-vs-libs and src-vs-libs regression cases (both fail today). *(hours)*

**A — `post_edit.py`'s test gate is permanently RED on Windows, and one "separator bug" is a live production defect**
`unittest discover -s tests` exits 1 with 12 pre-existing failures, and .claude/hooks/post_edit.py:316-325 gates unconditionally on that exit code, so every `.py` edit under `tools/` or `tests/` fires `[post-edit-hook] unittest discover failed` regardless of what changed — reproduced by invoking the real hook on an *unmodified* file (exit 1, 2115 B stderr). The 2000-char tail holds only 3 of 12 failures, truncates mid-word, and ends in stdout junk, so an alphabetically-early regression is invisible, not merely ambiguous. Both hook copies are byte-identical and separately registered. Critically, the "10 POSIX-separator assertion bugs" framing is wrong for 4 of them: tools/m2c_feed.py:125 matches `r"/(ov\d+|arm9)/symbols\.txt$"` against a Windows `str(Path)`, so `module` is always `"?"`, `:128` skips every symbols file, and **`--module ovNNN` disambiguation is 100% non-functional on Windows** (live: `resolve_symbol('eur','func_020c0000')` → FeedError). Normalizing those assertions would cement the bug. One more (`test_ov002_context...`) is a `WinError 2` toolchain gap, and test_objdiff_resolve_relocs asserts on backslashes the *tool* writes into objdiff.json — a tool-side question. Cost is 13.7s/edit, not 21s (the ledger's ~13s was right). **Action:** fix m2c_feed.py:125 via `sf.as_posix()`; normalize the 6 genuine test-side assertions (test_cluster_c_pattern3_gen:255,259, test_cross_region_cluster_apply:548,557, test_m2c_feed:167, test_patch_ov004_veneers:2756); regenerate the 2 index-drift files; adjudicate objdiff_resolve_relocs separately. Per-module scoping is complementary, not blocked — it independently greens ~90/95 tools. *(hours)*

**B — No shared routing-suffix constant: 11 hand-rolled sites, 4 live-buggy**
The three routing suffixes are copy-pasted across 11 sites; 5 correct (seed: tools/sig_census.py:80 `('.legacy_sp3','.legacy','.thumb')`), 4 live-buggy (objdiff_filter_panic_units.py:300-321 — the S-tier item above; both post_edit.py:66 copies; port_to_region.py:1152, which rejects `.thumb.c` at the gate even though :817-825 handles it), 2 latent (find_region_siblings.py:600, saved only by `search()` vs `fullmatch()`; batch_carve.py:71/73 hardcoding `kind:function\(arm,`). Every consumer predating the thumb tier (2026-06-09) is silently wrong and nothing detects disagreement. **Novelty caveat:** brief-569 §5 already names this exact root-cause family and flagged 3 of the 4 live sites as spawned follow-ups; the genuinely new contribution is the 4th site (which 569's "three files" census missed, proving it incomplete) plus the shared-constant closure. Migration safety verified: no `tools/__init__.py`, no glob over `tools/*.py`, 42 files already use the sibling-import idiom, no ninja edge perturbed. **Action:** add `tools/routing_suffixes.py` (`ROUTING_SUFFIXES` + `strip_routing_suffix`) promoted verbatim from sig_census.py:80 plus a parametrised conformance test; migrate live-buggy → correct → latent, each step shippable; add the module to configure.py:1479-1482 implicit deps; do **not** touch package layout. *(day)*

**B — PR #1074's mwasm fan-out fix landed only half the win: all 27 `--delinks` still passed at runtime on every one of 6,945 edges**
Brief 545 scoped `mwasm_implicit` (the rebuild-trigger axis) per-module but left `delinks_args` baked into the shared `rule mwasm` *command* (configure.py:573-580), so every edge re-parses up to 979,747 B of delinks.txt via `patch_section_align._lookup_delinks_slot_size` (in-order, first-match-wins — ov002's 2,854 TUs sit at scan position 6). Measured waste ~36-48s CPU/region (~107-143s per 3-region gate) — **not** the claimed 63.3s, which did not reproduce. **Novelty caveat:** docs/research/speed/buildcost-analysis.md:136-140 already names this follow-up, but deferred it as "only command-line length"; that rationale is what the measurement refutes. The memory index's "CLOSED — no action needed" is a half-truth. Safety proven: index and runtime use *different* matching rules, so divergence was structurally possible — swept all 20,260 `.s` edges across 3 regions, **0 divergences** (eur's 29 index-misses correctly take the full-list fallback); `rule mwasm` has exactly one consumer and `$asm_flags` already proves the per-edge-variable mechanism. **Action:** replace the literal `{delinks_args}` with `$delinks_args`; set it per-edge in `add_mwasm_builds` (:1207-1256) from the `scoped_delinks` it already computes, full-list on index miss. ~5 lines. *(hours)*

**B — Three build-and-abandon tools (1,457 lines + 3 CI test files) are cited by no workflow**
`bulk_data_candidates.py` (475L), `list_named_tier_callees.py` (393L) and `tier_classifier.py` (589L) are referenced by no build rule, no CI workflow, no sibling import, and no hand-written doc/brief/runbook — an independent repo-wide `git grep -F` over all tracked files returns only `docs/tools-index.md`, their own test, and generate_tool_index.py's prefix map. Each has exactly 1 commit (the creating one: 2026-04-26, 2026-05-14, 2026-04-30) vs batch_carve's 9 / port_to_region's 6, yet each ships a test that runs on every CI job. All three still execute cleanly (`--help` exit 0), so they are functional-but-unreachable — "cite" beats "delete". **Framing correction:** tools-index.md is greppable, so the honest claim is "catalogued but never cited from any workflow," not "invisible" — though the generated entries are mangled docstring fragments with no motivating brief or invocation. Precedent exists both ways (briefs/003 namechecks three then-orphans, all now passing; ov004-thunk-section-fix.md documents a deliberately-unwired utility). A prototype of the proposed lint ran over 90+ tools with **zero** false positives. **Action:** cite each from its motivating runbook or delete tool+test; add the zero-external-reference lint to the existing generated-files-drift workflow. *(hours)*

**Biggest win in this domain:** the `.thumb` routing tier is invisible to the objdiff/report lane (36/36 EUR units, proven, one-line fix) — and it is the 4th instance of one root-cause family that a shared `routing_suffixes.py` constant would close permanently.

## workflow

### S — do these first

**Nothing gates `main`; 11 of 12 recent merges landed red.** `gh api .../branches/main/protection` → 404 "Branch not protected"; `.../rulesets` → `[]`; `.../branches/main --jq .protected` → `false`. No check is required, so every "blocks merge" claim in the workflows is decoration — `match-invariants.yml:12` concedes it ("errors block merge iff this check is required in branch protection") while `tools/ci_format_invariants.py:125` posts "Errors block merge (build would fail)." to PRs, which is false. Audit of #1129–#1146: 11 of 12 merged with ≥1 failing check (#1146=7 failures, #1143=6, #1140=4; only #1134 clean); `Python (ruff)` + `Markdown (markdownlint-cli2)` failed on all 11, and even `pr-invariants (eur)` — the leg the ledger calls "blocking" — was red on #1146 and merged anyway. Red is the baseline, so genuine regressions are invisible. **Verifier corrections:** (a) work lands via local merge + direct owner push (`merged_by: cntrl-alt-lenny`, `allow_auto_merge:false`), so the ruleset must NOT grant owner/admin bypass or it reproduces the same decoration; (b) `continue-on-error` does NOT make a matrix leg green at check-run level — `pr-invariants (usa)`/`(jpn)` report `failure` — so requiring them would hard-block, making the "exclude usa/jpn" prescription correct but for the opposite reason; (c) prior art exists and is parked: `docs/state.md:2014` "match-invariants not yet a required branch-protection check" (since 2026-05-18). **Action:** green main first (below), then `gh api -X POST repos/:owner/:repo/rulesets` requiring `Python (ruff)`, `Markdown (markdownlint-cli2)`, `unittest`, `drift-check`, `pr-invariants (eur)`; exclude usa/jpn legs; no owner bypass. Pin `astral-sh/ruff-action` (`lint.yml:20` is `version: latest`) or a new ruff release spontaneously re-reds the gate. **Effort: hours** (ruleset) after **~1 day** (green).

**Green main — the whole red baseline is ~1 day of mechanical work, measured not estimated.** Lint is 40/40 failure on main (2026-07-03→07-15) from exactly 3 ruff errors: F541 ×2 at `tools/containment_check.py:733-734`, F401 unused `ResolveRelocsError` at `tools/fastmatch.py:96` (2 auto-fixable). Match-invariants has 25 consecutive failures since 2026-07-01 from exactly 3 permanent `cross_file_name_drift` errors on `func_02086800_dummy`. Markdownlint's 7,186 errors are 99.7% machine-generated: dossiers/ 6,217 + retriage/ 913 + map/ 36 = 7,166, of which MD022+MD031 = 7,148 (99.75%) — `markdownlint-cli2 --fix` over those 3 dirs was **executed** → "Summary: 22 error(s)", 1,268 files, docs-only, zero `config/`/`src/`/`tools/` paths. Drift: `generate_research_index.py && generate_tool_index.py` was **executed** → both `--check` flip to current AND `unittest` goes FAILED(2) → "Ran 35 tests OK" in one shot. **Action:** `ruff check --fix .` + delete the F401; run the markdownlint `--fix` and commit, then hand-fix the ~42 residual (22 generated + 20 hand-written) — prefer `--fix` over `.markdownlintignore`, since `docs/research/dossiers-high/` (1,068 .md, 0 errors) proves compliant generation is achievable, and also fix the dossiers generator or it re-reddens; regenerate both indices. **Effort: day.**

**The invariants error channel is saturated at exactly 3 — the guard it exists for is dead.** `check_missing_tu_sources` emits `severity="error"` (`check_match_invariants.py:298`) into the same channel the workflow gates with `if errors > 0: exit 1`. Since the count is permanently 3 on main *and* on every PR (the errors live in the committed tree), a real `missing_tu_source` moves 3→4 and produces a byte-identical red X. The workflow's own comment states the purpose the saturation kills: "This guards against a PR sneaking through with a missing_tu_source." The ledger's "harmless to sha1" defence is true and irrelevant — harmless-to-sha1 ≠ harmless-to-gate. **Action:** baseline the floor rather than chase the dummy — add `--expect-errors N` or an allowlist keyed on check+path (`cross_file_name_drift`+`func_02086800_dummy`), fail only on errors *outside* it, so error #4 is genuinely new. **Effort: hours.**

### A

**`gate3.py` — the canonical 3-region gate — can never return PASS on Windows, the primary dev platform.** `gate3.py:102` `tests_ok = run([PY,"-m","pytest","-q","tests"]) == 0` and `:165` `passed = not failed and tests_ok`; the suite is 12-red on Windows, so `--scope all` cannot reach PASS regardless of ROM state. Live: `12 failed, 2564 passed` / `[pytest] FAIL` / `GATE FAIL` / exit 1. Its docstring calls pytest "a hard gate" and claims "gate3 NEVER prints a PASS it didn't read … its stdout IS the paste-able proof AGENTS.md's integrity controls require" — on Windows it never prints a PASS it *did* earn, so the integrity control normalises pasting a FAIL. Confirmed by behaviour: 6 of 7 recorded gate3 invocations across briefs use `--no-tests` (b558, b562 ×2, b563, b572, b575) — the pytest gate has effectively never fired in anger. **Action:** fix the test-side assertions (below); until then give `run_tests()` a `--deselect` allowlist from one list and print the deselected count so the exemption is visible in the pasted proof — do NOT document `--no-tests` as the workaround. **Implementer caveat:** do not source the allowlist from "what is red today" — 2 of the 12 are live index drift (`tools/emit_data_blob.py` shipped in b578 without regenerating `docs/tools-index.md`), which go green on regen. **Effort: hours.**

**The "10 known-Windows-env test artifacts" label is false — it hides a real product bug at `m2c_feed.py:125`.** `mod_m = re.search(r"/(ov\d+|arm9)/symbols\.txt$", str(sf))` is a POSIX-only regex over `str(Path)`. Measured against the real glob: **27/27** EUR symbols files resolve to `"?"`, then `if module_hint and module != module_hint: continue` filters out every file → `FeedError: '0x021aa4a0' not found as a function … module ov002`. Fix applied → `TestResolveSymbol` 2-failed → 5 passed (reverted). Scanned all 95 `tools/*.py`: this is the **sole** product-side offender (`patch_objects_legacy.py:160`, `port_external_source.py:893`, `scaffold_batch.py:167` already use `.as_posix()`). True split of the 12: 2 index drift + 2 m2c_feed product bug + 1 m2ctx CreateProcess + 7 test-side. This class shipped once already — `tests.yml:105-112` cites brief 059 / PR #395: dsd emits backslash paths, the patcher "silently no-op'd for every legacy-routed TU" — and `tests.yml:99` calls today's failures "unrelated path-separator assumptions", which is demonstrably false for 2. **Action:** `[\\/](ov\d+|arm9)[\\/]symbols\.txt$` (or `sf.as_posix()` — verified equivalent, 25/27, no regression); convert the 7 test-side assertions to `.as_posix()`; then widen the existing `configure-windows` job (`tests.yml:46`, currently 2 narrow classes) to full `unittest discover` so the class cannot silently return. **Effort: hours.**

**b561's "you have NO toolchain" fix landed in 1 of 3 role-doc copies; the stale text still reaches every agent.** `.codex/agents/scaffolder.toml:7-9` still says "You DO NOT have a local toolchain — you cannot run `ninja`, `dsd`, `objdiff-cli`", and `AGENTS.md:18` repeats it ("Cannot run `ninja rom` / `dsd check modules` / `ninja objdiff`, so delegates **final ROM** verification to brain") — while `.claude/agents/scaffolder.md:17-18` says the opposite and the filesystem settles it (3 baseroms, `dsd.exe`, `objdiff-cli.exe`, `build.ninja`, 3 built ROMs). Staleness: `.codex/agents/` last touched 607c3b90 (2026-06-22, PR #1007) vs `.claude/agents/` 0862afb9 (b561, 2026-07-12); `grep -l gate3` → 3/3 `.claude/agents/*.md`, **0/3** `.codex/agents/*.toml` — the Codex agents have never been told the canonical gate driver exists. A second ADOPTED row has the same defect: `.codex/agents/brain.toml:36,77,102` still drives the dead `ninja rom` gate and never mentions `ninja sha1`. Per `docs/state.md:11-12`, Codex agents are live on the mechanical/drain lanes — i.e. told they cannot run the drain they are assigned. **Prioritisation (verifier):** fix `AGENTS.md:18` first — it is proven-read ("Every agent reads this before starting work", `AGENTS.md:3-5`), whereas nothing in `.codex/` registers `agents/*.toml`, so that load path is unproven. `AGENTS.md` is hands-off for scaffolder → route via PR for brain to merge. **Action:** rewrite `AGENTS.md:18`'s Where-it-runs/Role cells; port the `.claude/agents/*.md` bodies into `.codex/agents/*.toml`; add a sync test (normalised body text, not byte-identity — formats differ) mirroring the obligation the repo already accepts for the 2 hook copies. **Effort: hours.**

### B

**789 live, byte-matching `src/` .c files advertise themselves as unverified drafts with guessed offsets.** `git grep -l CAMPAIGN-PREP -- 'src/**/*.c'` = 789 (identical for UNVERIFIED): eur 309, usa 240, jpn 240. Cross-checked against all 27 delinks.txt/region (real path is `config/<r>/arm9/overlays/ovNNN/` — a naive `config/<r>/overlays/*` glob fabricates orphans): **789/789 live, 0 orphans, all `complete`**, and 0/789 carry GLOBAL_ASM. Verifier built 18 random EUR flagged TUs + the named example → **19/19 at 100.0% match**, so the headers are provably false, not merely stale by inference. `src/main/func_02086800.c` still reads "UNVERIFIED: built-free swarm draft … risk: offsets guessed … confidence: med" on a complete matching TU — misleading every agent that opens it, in the exact phase whose deliverable is readable C. Same population carries the dead extern, so both fixes share a sweep. **Action:** add the rule to `tools/check_match_invariants.py` first (it already runs build-free in CI and already has `check_orphan_externs`/`_collect_complete_ranges`) so the class cannot regrow — note the `complete` qualifier is vacuous (30591/30591 entries are complete), so it reduces to "no TU in delinks may carry the header"; then a deterministic LLM-free sweep rewrites the 789 headers to a factual one-liner, restricted to the span between the first `/*` and the first non-comment token, refusing any file whose preprocessed output changes. Comment-only ⇒ byte-neutral, confirmed by a live header rewrite (7 lines → 1, still 100.0%) and per-region `ninja sha1`. Schedule against a clean tree — a concurrent ov020 carve was live in the scaffolder worktree during verification. **Effort: day.**

**Branch slugs encode the LLM provider, not the role — 28 of 30 recent PRs violate AGENTS.md's own rule.** `AGENTS.md:172-179` mandates `<agent-slug>/<scope>` because "The slug left of `/` identifies which role owns pushes to that branch", and `AGENTS.md:36` is titled "Slugs are roles, not LLM providers" — yet #1117–#1146 are 28/30 `claude/<scope>-NNN` (only #1121 `scaffolder/prune-worktrees`, #1120 `brain/trim-agents-md-closed-briefs` comply). Even the provider half is unreliable: `AGENTS.md:504` prescribes `codex/ov002-mopup-575`, but #1145 shipped as `claude/ov002-mopup-575`. Every PR's author is `cntrl-alt-lenny`, so authorship carries no identity either, and worktree/role/slug are three disagreeing labels (kb-types → `claude/eur-datablob-578`, a scaffolder brief; kb-map → `claude/endgame-ledger-576`, a decomper brief). Live cost: b577 (decomper) and b578 (scaffolder) are two concurrent Claude lanes in different roles, both slugged `claude/`. **Verifier corrections:** the count is 28/30, not 25/27 (the ~93% is right); the violation is hardcoded in **tooling**, not just prose — `tools/batch_carve.py:251` emits `(git switch -C claude/<brief>-NNN origin/main)`, so any fix must touch it alongside `AGENTS.md:504/516/522`; and `.githooks/pre-push` is dormant (`core.hooksPath` unset), so enforcement should ride the live `.claude/hooks/pre_bash.py` + `.codex/hooks/pre_bash.py` pair. **Action:** pick one — enforce the rule (briefs + `batch_carve.py` prescribe role slugs, hook rejects non-role slugs) or amend `AGENTS.md` §Branch naming and §Slugs-are-roles to match practice. Either way the slug must again answer "which lane owns this branch". **Effort: hours.**

**Biggest win in this domain: spend the ~1 day to green main, then require the 5 checks on a `main` ruleset with no owner bypass — it converts eleven decorative workflows and a dead `missing_tu_source` guard into real enforcement, and every other item here is either its prerequisite or rides the same machinery.**

## research

**S — Lint CI has been red for 200/200 consecutive runs (17 days); 99.2% of the noise is two machine-generated dirs**
`.github/workflows/lint.yml` is the repo's only always-on code-quality gate and both its jobs fail on every run since 2026-06-30 (job-level sweep: last green run 28431978887 @ 2026-06-30T08:47:02Z; failure from 28437525593 onward, every run through 2026-07-15). Red is the baseline, so a genuine new F821/undefined-name or B-rule violation would land on main invisibly. Markdown job: 7,186 errors, of which docs/research/dossiers = 6,217 and docs/research/retriage = 913 (7,130 = 99.2%); all human-authored docs = 56. `docs/research/dossiers-high` (1,068 files) emits zero, so only the older templated batches are malformed. Ruff job: exactly 3 cosmetic errors (2× F541 at tools/containment_check.py:733-734 from brief 534; 1× F401 `ResolveRelocsError as _rr_Error` at tools/fastmatch.py:96 — a true positive, safely deletable since fastmatch.py:146 catches via broad `except Exception`). Verifier reproduced the fix empirically: adding the two dir exclusions yields exactly 56 errors, and `ruff check --fix` + the one deletion gives exit 0. **Two corrections must ride with this:** (1) "red in all recorded history" is wrong — the workflow has 1,732 successes lifetime; it is red *continuously* for 17 days, and it was born green (7aa2f884) and rotted. (2) `.markdownlintignore` is **inert** — markdownlint-cli2 v0.23.0 does not read it (proved by controlled A/B); the only live mechanism is lint.yml's negated `globs:`. **Action:** one PR — add `!docs/research/dossiers/**` + `!docs/research/retriage/**` to lint.yml's globs; fix the 56 human-doc errors (MD022 34, MD031 12, MD018 3, rest misc); `ruff check --fix` + delete the F401; pin `astral-sh/ruff-action` to ruff 0.15.13 instead of `version: latest` so a stabilized rule can't re-red the gate with zero code change. Honors brief 021's "fix the content, not the rule" norm — excludes only machine-generated trees, fixes human ones. **Effort: hours.** *(Absorbs the separate lower-scope "ruff is red + unpinned" finding, which is the same 3 errors and the same pin; that finding's guess that a recent ruff release caused the redness is refuted — the flip lands exactly on the 2026-06-30 code landing.)*

**S — batch_carve maps asm_escape's exit-2 infrastructure errors to `verify-fail`, the bucket every endgame-ledger residual is made of**
`tools/batch_carve.py:352-374 whole_function()` never reads `r.returncode`; it tests only for `"byte-identical"`/`"✅"` and falls through to `return "verify-fail"`. asm_escape.py reserves exit 2 for infrastructure failure at 7 sites (875, 880, 903, 934, 959, 963, 978 — including "not found in build/{version}/delinks (already matched, or run `ninja` first?)", "no configured function size", "did not assemble") vs exit 1 for a genuine mismatch (987). Verifier drove the real `Ops.whole_function` with canned returns: exit 1 genuine-mismatch and all four exit-2 infra errors produce the **byte-identical verdict**. Damning: the same file reads `.returncode` six times elsewhere (324, 424, 450, 460, 478, 489) — everywhere except the two methods that produce census verdicts. This is the ledger's documented classify()→REFUSE defect ("same tree censused 16→6→0 as the env degraded") replayed in the bucket endgame-ledger §1 (USA/JPN main floor) and §6 (EUR ov006 parks) are built from. The cross-region-reproducibility defense of that floor is falsified: b577 shipped real `ninja sha1` fixes for func_020b40cc and func_020b40e0, both listed in §1 as permanent verify-fail floor — deterministic tool bugs reproduce identically across regions. **Action:** capture `returncode` in `whole_function()` (:374) and `classify()` (:350); add a `tool-error` verdict that is never parked and never counted as floor; require the negative marker ("did NOT verify") before returning `verify-fail`; unit-test the three canned outputs (the `_run` mock seam already exists at tests/test_batch_carve.py:273/320/323); then re-derive endgame-ledger §1 and §6 from split buckets on a warm tree. Contamination by exit-2 specifically is not yet proven — the re-census is the proof. **Effort: day.**

**A — S2 compile gate is a 2-step edit to an existing CI job; r4's changed-file scoping is both unnecessary and silently wrong**
r4's recipe (new `compile-check.yml`, `git diff` → `.o` mapping, "keeps it seconds") fails on every count. A full cold sweep of all 3,993 EUR objects at `-j4` measured **137s** independently (finder: 2m43s; USA 3,273 objects / 2m56s) — 3,993/3,993 `.o`, 0 errors, so the gate is **green on main today** and needs no cleanup wave. The scoping is defective: a PR touching only `src/overlay002/ov002_core.h` (included by 125 EUR TUs) maps to zero `.o` targets and reports green having compiled nothing — a hand-rolled reimplementation of what ninja already does correctly via `-MD` (configure.py:468) + `depfile=` (:477/:495/:516/:539). And it isn't a new workflow: `tests.yml:46 configure-windows` already does checkout + setup-python + `configure.py eur --skip-sha1` on windows-latest. A `build/` cache can't help (actions/checkout resets mtimes). **Action:** extend `configure-windows` in place — add `src/**`, `libs/**`, `include/**`, `config/**` to its `paths:` (currently tests/**, tools/**.py only, so src PRs don't trigger it); cache `tools/mwccarm`; run `configure.py --skip-sha1 --no-extract eur`; enumerate the 4 mwcc rules via `ninja -t targets rule <rule>` (no `default`, no all-objects phony exists) and `ninja -k 0` them. **Three corrections are mandatory or it cannot run:** (1) **drop `--compiler tools/mwccarm`** — it trips configure.py:893's `if args.compiler is None:` guard that emits the download_tool edge, deleting the only rule producing the compiler (reproduced: `ninja: error: 'tools/mwccarm/.../mwccarm.exe' ... missing and no known rule to make it`); this also removes the dependency on a not-yet-existing cache-key constant. (2) 3,993 targets = 172,941 chars vs Windows' 32,767 CreateProcess limit — batch (`xargs -n 400` proved 3993/3993, 0 errors). (3) `ninja -t targets` emits CRLF; `tr -d '\r'` or every batch dies on an invisible `\r`. EUR-only v1 per phased-EUR-first; USA/JPN later as `continue-on-error` matrix legs mirroring b571. **Effort: hours.**

**A — objdiff #352 is fixed upstream and shipped in v3.7.3; the ledger's "we own it forever" DEAD row is refuted on all three clauses**
The DEAD row ("still unfixed upstream, 3 duplicate PRs, 0 maintainer engagement. We own it; the panic filter is ours to carry") was already false 5 days before the ledger was written. Issue #352 (filed by us) is CLOSED 2026-07-10T05:30:04Z **by maintainer `encounter`**, who merged PR #359 (LagoLunatic, "ARM: Fix index out of bounds error when there are no mapping symbols") and closed #361/#365 as dupes. v3.7.3 published 2026-07-10T06:47:36Z; fix verified **at the tag**: the empty-slice guard is exactly the `saturating_sub(1)` → `usize::MAX` underflow in our recorded panic ("the len is 0 but the index is 18446744073709551615", docs/research/objdiff-arm-crash-workaround.md:11,43). We are pinned at `OBJDIFF_VERSION = 'v2.7.1'` (configure.py:74); download_tool.py:33 already fetches by tag and all three v3.7.3 prebuilt assets exist — no source build, no cherry-pick. **Action:** bump to v3.7.3, re-run `ninja objdiff && ninja report` with the filter's case (a) (`has_addressable_text_code`, objdiff_filter_panic_units.py:127) disabled; if green, delete case (a) and its ELF machinery — **keep case (b)** (missing `.o` for unmatched `.legacy`/`.legacy_sp3`), which is our own routing design. **Caveats:** v2.7.1→v3.7.3 is 251 commits / 186 files and v3.0.0 self-describes as "a ground-up rewrite"; 10 report.json consumers ride this, so "hours" is optimistic. Two kills were attempted and failed (the `diff --format json` CLI mode survives at v3.7.3 diff.rs:71-74/220; the `left/sections/match_percent` schema is intact at bindings/diff.rs:24-60), but the **real untested gate is dsd v0.11.0's generated objdiff.json ↔ objdiff v3 config schema**, not the panic fix. Also: #359 is a defensive guard, not a root-cause fix (its author suspects dsd emits invalid objects); our filter's predicate ("no STT_FUNC with st_size>0") is a conservative proxy for #359's ("no mapping symbols"), so case-(a) retirement must be empirically confirmed; and `ninja report` alone is an insufficient test — post_edit.py swallows errors (`if proc.returncode != 0 or not proc.stdout.strip(): return None`), so a hook regression would be silent (test both hook copies per b566). Check brief-309:67's recorded dependence on v2.7.1 bl/pool-reloc semantics survives before trusting post-bump percentages. **Effort: hours→day.**

**B — 54 libs/nitro ports carry provably false provenance (all claim `FX_Init`); ~34 real SDK identifications never reached symbols.txt**
libs/nitro holds 100 ported `.c` files; 54 claim `function: FX_Init`, all `size 0x4`, all `void func_X(){return;}`, spread across main (25) + ov002 (11), ov004 (9), ov010 (4), ov011 (2), ov015 (2), ov003 (1). One SDK function cannot live at 53+ addresses across main and six overlays — these are generic empty stubs that byte-twin with any `bx lr`, so the sha1 gate is untroubled while the headers assert something false. The 54th is `OS_DefaultIrqHandler.legacy.c`, whose target is a **real named symbol** (config/eur/arm9/symbols.txt:3353) yet claims FX_Init — false directly, not by pigeonhole. `func_02007218.legacy.c` still carries verbatim the exact mis-attribution that cross-project-pipeline-lessons.md lesson 1 (L81-85) documents as wrong: the lesson was written, the artifacts were never cleaned (origin: 76ccda2e, "brief 071 wave 1 — 64 byte-identical ports"). This is live build content, not scratch (configure.py:183/198/1099/1123 walks libs/; 71 entries in config/eur/arm9/delinks.txt). Second half: config/eur has 9,867 `kind:function` symbols — 9,747 `func_*` (98.8%), 51 `__sinit_*`, and just **13** NitroSDK-prefixed — so the surviving identifications the port arc paid for were never applied as names and the readable-C frontier gets nothing. **Corrections:** "46 distinct upstream names" is the *file* count, not the name count (actual: 36 distinct, 35 non-FX_Init); 10 of the 46 non-FX_Init files carry no `function:` header at all, so the real applicable pool is **~34, not ~45**. The "brief 080" citation is wrong — the ~71%/`OS_IrqDummy`→138 stats are at docs/briefs/082-pokeheartgold-bulk-port-wave-1.md:24. **Action:** two byte-neutral passes — (1) strip or quarantine the false `upstream:`/`function:` attribution from the 54 stubs and add a guard to port_external_source.py so size≤8 relocation-free bodies can never carry an attribution (well-calibrated: it flags exactly 5 non-FX_Init files; the other 29 are 0xc–0xb4, above lesson 1's ≤0x10 twin band; the existing CalleeResolution guard at :428-470 acts on callees, not target attribution, so this is genuinely additive); (2) apply the ~34 names via the proven brief-492 recipe (`git mv` .c + `\bfunc_X\b` across config/eur and src `.c` **and** `.s`) — headroom confirmed, OS_Halt/MI_Copy64B/MTX_Identity44_/FS_Init/GX_SendFifo48B/OS_SPrintf/CTRDG_IsOptionCartridge all return 0 hits in symbols.txt. Refresh libs/nitro/README.md (stale on both counts: claims "no `.c` sources yet" with 100 present, and surveys 0 NitroSDK names with 13 present). Verify metric-neutral via `tools/progress.py` + `ninja sha1`. **Effort: day.**

**B — sig_census's `no_prediction` bucket fuses env breakage with semantic refusal — in the tool whose GO verdict depends on it**
`tools/sig_census.py:296-300` runs port_to_region.py with `capture_output=True` and never reads `returncode` or `stderr`. port_to_region.py emits nothing on stdout when it fails (reproduced: missing file → exit 1, 0 bytes stdout, stderr-only), so crash/missing-file/bad-args/missing-config/timeout all land in `json.loads("")` → JSONDecodeError → `return None` → the single `no_prediction` counter (:460-461, printed :466-468). The docstring at :291-294 claims errors are "reported, not silently dropped, by the caller" — true only in the sense that a *count* prints; the cause is destroyed at the capture boundary. Damning in-file evidence: the same file's other two wrappers **do** check (`sig_new:188` tests `proc.returncode != 0`; `sig_apply_dry:249` passes returncode+stderr through) — :296 violates its own file's convention, and :249 is a working precedent for the fix. **Action:** on non-zero, return a sentinel carrying `stderr`; split `no_prediction` into `refused` vs `tool_error`; make non-zero `tool_error` fail loudly. **Implementer caveats:** the collision-refusal path emits *valid* JSON `{"status":"refused"}` with no `main_func_resolution` key → `data.get()` → None → same bucket via a **different** route, so returncode alone won't classify it (needs a third state); the confidence-floor refusal at port_to_region.py:1245 is dead code from this caller (always `--dry-run`). Also: "the 16-case gap is unattributable" is overstated — non-`applied_rename` kinds are attributed via the per-kind breakdown at :463-464; only the `no_prediction` subset is opaque. Distinct from the already-fixed b569 addr-vs-target_name bug, and from the `.thumb` gap (closed at port_to_region.py:823). **Effort: hours.**

**B — `.claude/hooks` ↔ `.codex/hooks` is a named sync requirement with zero enforcement**
Brief 566 shipped the in-loop C feedback into two separately-registered hook copies (.claude/settings.json vs .codex/hooks.json, each with its own registrations) and the ledger records that they "stay in sync." Nothing enforces it: repo-wide, `codex` appears in exactly one non-.codex file, and that's a prose line in `.codex/agents/scaffolder.toml:27`. tests/test_claude_hooks.py hardcodes `_HOOKS_DIR = ... / ".claude" / "hooks"` — the .codex copies are *provably* unreachable by all 2,580 tests. generated-files-drift.yml gates only the three index files. The copies are byte-identical today and have never diverged (the .codex tree was created wholesale in dbaaf91c and every subsequent touch hit both), so this is prospective risk, not demonstrated drift — but the failure mode is an *absence* of stderr output that nobody would attribute to hook drift. **Action:** add tests/test_hook_copies_in_sync.py asserting sha256 equality for each filename present in both dirs, failure message naming both paths; encode the `save_agent_reply.py` omission as an explicit allowlist (it is almost certainly intentional — it's registered on Claude's "Stop" event, and .codex/hooks.json implements only PreToolUse/PostToolUse, so this resolves to a one-line comment). Auto-collected by tests.yml:44's `unittest discover`, zero wiring, fits its "zero baserom / toolchain dependency" charter. Blast radius is Codex-agent ergonomics only — impact C, don't inflate. **Effort: hours.**

**B — NEGATIVE RESULT: mypy/pyright is not worth it — record it so it stops resurfacing**
The attractive pitch ("51k lines, 96% annotated, no type checker") does not survive measurement. tools/ carries 1,171 functions at 96.1% return-annotation / 94.9% full-param coverage; an AST contradiction scan over all 94 non-vendored tools + hooks yields 2 hits, both false positives (find_callsites.py:80, find_pattern_clusters.py:359 — plain tuple *type aliases*, **not NamedTuples as the finding claimed**; the correction matters, since real NamedTuples returning bare tuples would have argued *for* mypy). The one high-value thing a checker buys — undefined names — is already CI-gated for free: ruff.toml selects `F`, tools/ is not excluded, lint.yml runs on every PR+push with no continue-on-error, and the verifier confirmed by *planting* an undefined name and watching F821 fire (not by trusting a passive zero). Every real defect this round was a dropped subprocess exit code — a runtime-contract bug mypy structurally cannot see (`subprocess.run` returns a well-typed `CompletedProcess` whether or not you read `.returncode`; there is no must-check concept). Type checking would have caught none of the findings above. **Action:** record in the r5 ledger as DEPRIORITIZED-WITH-MEASUREMENT citing 96.1%/94.9%, 0 real contradictions, F821-clean-and-planted-verified, and note that the 138 `sys.path.insert` sites across 130 files (the package-boundary item, currently filed B-tier at improvement-swarm-2026-07-12-r4.md:43, not deprioritized) **gate** any checker adoption — mypy inherits that cost without proportional benefit. Standing caveat to record honestly: what is established is "no return-literal contradictions," not global annotation accuracy. **Effort: hours.**

**B — ds-decomp isn't quiet: 100% of its 3-month delta is parked in PR #60, whose axis is our own open issue #58's axis**
"Upstream is quiet — main delta 4 commits" is true of main (HEAD fffc342, exactly 4 past the v0.11.0 bump 4136cef) but undercounts the real delta ~12x. PR #60 "Improve game compatibility" (author **AetiasHax = the maintainer**; open, draft=false, mergeable_state=clean, 48 commits / 35 files / +1,434 −553) targets `dsd init` — the same command our issue #58 reports broken on this game. Verified exhaustively: two other branches exist but both predate main HEAD; zero commits repo-wide after 2026-04-12 outside `game-compatibility`. Issue #58 is open, its only comment is ours ("If a fix is in the works I'm happy to test against this game"); the maintainer never replied and opened #60 eight days later. **Honest limit (the finding's own):** #60's body names no overlay/overlay-swap/#58/shared-base — hypothesis, not claim. The ledger evaluated #60 on exactly one axis ("not delink-tolerance work") — correct but orthogonal; it never checked it against our own filed issue. **Action:** build dsd from #60's branch and run `dsd init --rom-config extract/eur/config.yaml` **without** `--allow-unknown-function-calls` (the hidden flag documented at CLAUDE.md:124-137, bypassing "Local function call from 0x021aaed4 in overlay 0 to 0x021b3810 leads to no function"; overlays 0 and 2 share base 0x021aaee0). Use `-d/--dry` — cheaper than throwaway paths, and never point it at config/eur (hand-curated, thousands of symbols). If clean, post on #58; that publish step needs explicit user approval at execution time. **Unbudgeted prerequisite: no Rust toolchain is installed** (no cargo/rustc/rustup anywhere) — "hours" is tight. Impact honestly C-to-B: we are byte-complete on all 3 regions and would never re-init; upside is dropping the hidden-flag caveat plus upstream goodwill. **Strike the finding's m2c line** — "only ONE post-pin ARM commit" is false (compare `ce05217...master` = 42 ahead, 5 `arm:` commits, 4 functional: vstm/vldm→vpush/vpop, vpush/vpop, vldr/vstr, low float registers). The conclusion survives on different grounds: all 4 are VFP work and ARM946E-S has no FPU, so the pin genuinely has every NDS-relevant change. ds-rom's ~75% speedup is confirmed still unreleased (latest release v0.7.0, 2026-02-20) and triple-blocked. **Effort: hours (plus toolchain install).**

**Biggest win in this domain:** the two S-tier items are both *silent-gate* failures — a CI linter that has been red so long it can't report a real bug, and a census classifier that reports "the tool is broken" as "this function is a permanent wall" — and the second one means the endgame ledger's floor numbers are unproven, not just noisy.

## strategy

### S — must act

**The endgame ledger measures parked functions, not uncovered bytes — it explains 11.6% of the real gap and its EUR row is wrong on all three counts**
`docs/research/campaign-analytics/endgame-ledger.md` is a hand transcription of brief prose, so it structurally cannot measure distance-to-100%. Re-deriving from committed delinks (via `tools/progress.py::parse_delinks_file` + `CODE_SECTIONS`) gives a 3-region code gap of 61,404 B (EUR 13,432 / USA 24,044 / JPN 23,928). The only correct residual row (USA/JPN main floor, 14 addrs = 7,144 B) is 11.6% of it. The EUR row is wrong on all three terms: blobs CLOSED (PR #1146, commit 227e7d2c), the 11 ov006 parks are PHANTOM (ov006 delinks: total=102972 matched=102972 gap=0; every park addr sits inside a `complete` C TU starting 4 B earlier), and the 39 ITCM funcs are mis-filed as EUR (really 11 EUR + 14 USA + 14 JPN). The largest single gap — ov004 at 20,110 B, 33% — appears in **no ledger row at all** (`grep -i ov004` exits 1). Verifier reproduced every byte independently.
**Action**: extend `tools/size_census.py` (do NOT greenfield — it already has the same data source and a `--json` per-module table, but its `parse_claimed_text` scans `.text` only → 71% `.init` false-positive rate on EUR, and its `_module_paths` never yields `itcm`) to emit per-region/per-module gap bytes + address ranges; surface via `progress.py --json`; regenerate the ledger from it. Strike §6 outright, refile §7, add an ov004 row. **[hours]**

**C% has no per-module breakdown anywhere — 78.3% of the denominator is 2 modules at 6.58% C, capping every other lane at 26.89%**
No tool, SVG, or doc reports C% per module: `progress.py` emits one global number, `calcrom.py` buckets per-module with no C column, the heatmap colours by `matched_code`. Computed from progress.py's own parsers: EUR's 2,385,948 B denominator is 78.26% just arm9 (738,080 B) + ov002 (1,129,252 B), sitting at 6.58% C combined. The other 24 modules are 21.7% of the denominator at 13.29% C — driving **every one to 100% C caps the headline at 26.89%**. ov002 alone at 100% would put the project at 53.26%. Sharpened by verification: `cmatch-sweep-queue-overlays.md:24` excludes ov002 from the overlay C queue **by rule**, so the single largest C module (47.3% of the denominator, 4.46% C) is formally out of scope while no tool reports its C%. (Novelty narrower than claimed — `docs/research/data-tier-scoping.md:64-76` already publishes the code-byte column; what is new is joining the C numerator to it plus the ceiling arithmetic.)
**Action**: add `--by-module` to `tools/progress.py` (~15-20 lines, reuses `parse_delinks_file`, build-free; verifier implemented and cross-checked it exactly). Put arm9 and ov002 C% on the dashboard. Re-scope the C lane against the 26.89% ceiling and revisit b564's ov002 exclusion explicitly. **[hours]**

### A — high value

**The demonstrated C-match ceiling is 544 B with n≈1 outside one clone family, and 51% of residual mass sits above it, never once attempted**
Largest matched C TU in project history: 544 B. Exactly 8 matched C TUs >512 B exist — 7 are one ov006 clone family (all exactly 544 B) and the 8th is an ov004 Thumb file, so there is ~ONE demonstrated large match that did not ride a sibling. Meanwhile 869 .s TUs / 1,112,124 B (51.0% of residue, +46.6pp of C%) sit above 544 B, and only 5/847 (0.6%) worklist funcs there have a clone sibling. Matched C averages 48 B/TU vs residue's 344 B/TU (7.2x). The ">544 B is a wall" verdict is a **prediction, never a test**: it comes from a build-free 294-agent triage with a documented 38% mis-binning rate on exactly that E/F tier. b533 identified six >544 B functions and attempted **zero** ("larger than anything this session successfully matched"); the largest ever attempted is 484 B — and it failed on *comprehension* (32 B short, "materially incomplete reconstruction"), the exact failure mode m2c addresses, unlike the small-function reg-alloc walls that killed m2c in b379/381/383. The entire parked near-miss ledger tops out at 224 B.
**Action**: one agent, hard-timeboxed, 3 singletons from the >544 B pool via m2c (needs `python tools/m2c_bootstrap.py` — not vendored; `tools/m2c_feed.py` is the ready feeder) + the live `post_edit.py` objdiff hook. Binary verdict. If it lands → 1.11 MB of runway, redesign rounds around singletons with a size floor. If not → state publicly that readable-C caps at ~11% and stop selling a byte-% trajectory. **Scope it 2× ov002 + 1× arm9, never main-only**: ov002 holds 390 TUs / 568,468 B = 51.1% of above-ceiling mass (+23.8pp of C% alone) vs arm9's 244/260,372, so a main-proof method generalizes to at most 23% of the prize — and the repo already has precedent for exactly this non-transfer (`VtableDispatchPatterns.md` 89% on overlays, 0/11 on main, b532). Beachhead on ov002's 129-256 B tail (1,051 TUs) before the 1,457 B average; use b533's per-file `ninja build/<ver>/.../<f>.o` + ELF `.text`-size guard before any full build (b533 saw a 567 KB avalanche from a main-ARM9 size mismatch). Add a per-module "mass above 544 B" column to the ledger. **[days]**

**generate_heatmap.py's units metric is wrong on both paths — the public SVG is pinned at 99.75% forever, and the local path renders bytes as a unit count**
Two defects. (a) `total_units = len(units)` (:392) counts 26 synthetic `_dsd_gap@*` cells hardcoding `matched_code:"0"` (:377), so they can never complete: the committed SVG reads `units 10333/10359` (99.75%) while `progress.py` reads 10333/10333 (100.00%). 17 of the 26 have `total_code==0` and are render-filtered (:158) — invisible on the map yet inflating a visible denominator. (b) :391 writes `complete_units` (a COUNT) into key `complete_code` and :173 reads it back — self-consistent only on the delinks path; on the documented `ninja report` path objdiff's real `complete_code` is BYTES, reproduced live as subtitle `units 2374740/10333`. The two tools emit exact inverse conventions, refuting `progress.py:26-28`'s "same top-level shape" contract. More novel than self-claimed: r4 never mentions the heatmap at all.
**Action**: emit objdiff's real schema (`complete_units` count + `complete_code` bytes), read `complete_units` at :173, exclude gap cells from `total_units`; update `tests/test_heatmap_delinks.py:144` and `tests/test_generate_heatmap.py:195`, which enshrine the wrong semantics. Structural fix: source root `measures` from `progress.summarize_delinks` so the two tools cannot diverge again (no circular-import risk — progress is the chain root). **[hours]**

**The flagship heatmap resolves to 2 colours across 9,785 cells — it renders the finished metric and structurally cannot render the frontier**
Measured on the committed EUR SVG: 9,785 renderable cells, exactly 2 distinct fills (`#49ca49` ×9,776, `#be3737` ×9); parsed `<title>` pcts are `['0.00','100.00']` and nothing else. The whole red→green gradient is unreachable, because delinks synthesis sets each unit's `matched_code` to `total_code` or 0 (:346-347). The map's entire information content equals one number already in its own header (99.44%) — and the 9 red cells are exactly the 9 gap cells, so every carved TU is green. Cost: 5.6 MB committed, 176 blobs = 7.9 MiB ≈ 9.3% of the 85 MB `.git`, regenerated on every push touching `src/**` or `config/**/delinks.txt`. Its own docstring (:11-16) justifies code-only colouring by "USA/JPN at ~1% code" — dead since b563 took them to ~99% — and by consistency with `update_progress_badge.py`, which is itself dead.
**Action**: re-point `match_pct` at a C-vs-.s ratio using the extension already on `tu['source']` (`.s` 6,338 / `.c` 3,995 in EUR) and emit it as a per-unit measure; `unit['name']` carries the same extension on the report.json path, so one rule serves both. Simulated: 8.17% green by byte area, and **ov002's 1.13 MB block dominates the map as the frontier**. Retire code-coverage colouring. Note for the implementer: the new metric is also per-cell binary — the value is re-pointing at the frontier, not reviving the gradient. **[hours]**

### B — worth doing

**docs/state.md's self-declared "no-build source of truth" metric block is 9.16 points stale three days after b565 hand-wrote it**
`state.md:53-54` and `:109-113` both report USA/JPN code 89.83% / C 7.08%; live `progress.py` gives USA 98.99%/7.11%, JPN 99.00%/7.11%, EUR 99.44%/8.04%. The block asserts the committed delinks tier "is the no-build source of truth" but is hand-copied prose, so it rots the instant a wave lands — b563's 1,024 ships did exactly that, and `progress-badge.yml` tracks only 4 SVGs, never state.md. This is a **path, not an instance**: 43134d63 fixed a 25pp-stale block → 04a9c051 hand-refreshed (07-12) → 9.16pp stale (07-15). Third cycle.
**Action**: wrap in `<!-- METRICS:BEGIN/END -->` (a NEW convention — no markers exist repo-wide; existing generated docs are wholly generator-owned, which state.md's hand-written prose forbids), add a writer + `--check` to `progress.py`, gate in `generated-files-drift.yml`. Reconcile with `calcrom.py:369` `print_summary()` — it claims this job but is build-gated and reads the forbidden report.json tier. Cheaper alternative: delete the numbers, cite the one-line command. **[hours]**

**The .claude/.codex config mirror drifts silently with no guard — agent definitions are 20 days apart**
All three `.codex/agents/*.toml` are frozen at 2026-06-22 (607c3b90) while their `.claude` twins were rewritten 07-09/07-12; `git show --name-only 0862afb9 | grep -i codex` → no hits, so the ledger-ADOPTED b561 rewrite propagated one-sidedly (~19-38% content divergence). `.codex` is live (state.md:11-24: 2× Codex workers on b575/b576). Zero tests/CI compare them. No live misconfiguration proven — the .toml twins don't carry b561's fatal defect — so this is hygiene; impact is honestly C-to-B.
**Action**: ship byte-equality of `post_edit.py` + `pre_bash.py` (md5-identical today) in `tests/test_claude_hooks.py`. **Drop the finding's own settings.json/hooks.json set-equality alternative — it fails on arrival** (`.claude` registers {PostToolUse, PreToolUse, Stop}; `.codex` has no Stop/`save_agent_reply.py` twin) unless you first reconcile or allowlist that asymmetry. **Drop the commit-date staleness check** — CI checkout is depth-1, so `git log -1 -- <path>` returns empty; use a content marker. **Add `.claude/**` + `.codex/**` to `tests.yml` paths** or the guard never fires on the very commits that cause drift. **[hours]**

### C — cleanup

**tools/update_progress_badge.py is dead and hard-broken with 28 green tests, still cited as live in three places**
`--check` exits 1 with `could not find EUR progress badge URL in README.md`; README:8-10 went static (sha1/modules/license) when 792b4ce4's revamp orphaned the tool. Zero call sites in any workflow or ninja edge. Its 28 tests pass because they feed synthetic README strings via `_readme_with()` and never read the real file — green suite, 100% broken tool. Both modes fail, not just `--check`. b561:118 saw this exact failure and dismissed it in a parenthetical as "a pre-existing unrelated README badge-URL lookup issue" — misdiagnosed, never actioned. It is an active trap: `generate_heatmap.py:16` anchors the heatmap's colouring rationale to it.
**Action**: delete the tool + its tests; remove the hardcoded row at `tools/generate_tool_index.py:93` and **regenerate** `docs/tools-index.md` (it is auto-generated — hand-editing won't hold; count 94→93); fix `progress.py:27`, `generate_heatmap.py:16`, and the fourth stale ref the finding missed, `tests/test_progress.py:574` ("CI pipes summarize_delinks output into update_progress_badge.py" — a CI pipe that does not exist). Leave the historical brief refs alone. **[hours]**

**Biggest win in this domain**: the 544 B ceiling probe — 3 ov002/arm9 singletons, one timeboxed agent, and either 1.11 MB of runway opens or the campaign stops selling a byte-% trajectory it cannot deliver.

## Negative / confirmatory results worth recording

- **mypy/pyright: DEPRIORITIZED-WITH-MEASUREMENT, not deferred.** 1,171 tools/ functions at 96.1% return / 94.9% param annotation; AST contradiction scan = 0 real hits (2 false positives, both plain tuple aliases). F821 is already CI-gated free via ruff's `F` selector — verified by *planting* an undefined name, not by trusting a zero. Every real defect this round was a dropped subprocess exit code, which mypy structurally cannot see. Gated behind the 138 `sys.path.insert` / 130-file package boundary regardless. Stop re-surfacing it.
- **`.markdownlintignore` is inert.** markdownlint-cli2 v0.23.0 does not read it (controlled A/B). Only lint.yml's negated `globs:` is live. Any future "just ignore the generated dirs" fix must edit the workflow, not the ignore file.
- **`continue-on-error` does not green a check-run.** It suppresses the workflow-run conclusion only; `pr-invariants (usa)`/`(jpn)` report `failure` at check-run level. So "requiring them would be a decorative no-op" is backwards — it would hard-block. The exclude-usa/jpn prescription is right for the opposite reason.
- **The r4 changed-file compile-gate scoping is refuted, not merely suboptimal.** A `.h`-only PR maps to zero `.o` targets and reports green having compiled nothing. Ninja already does this correctly via `-MD` + `depfile=`. Also: a `build/` cache cannot help — actions/checkout resets mtimes.
- **`--compiler tools/mwccarm` is a trap.** It trips `configure.py:893`'s `if args.compiler is None:` guard, deleting the only rule that produces the compiler. Reproduced: `ninja: error: ... missing and no known rule to make it`.
- **The `run_wave.py` proposal is dead on arrival.** Units are 100.00% in all 3 regions (10333/10333, 10129/10129, 10129/10129) — batch_carve has zero waves left to run, corroborating b575's 0-candidate result.
- **The m2c pin needs no bump — but not for the stated reason.** `ce05217...master` is 42 commits ahead with 5 `arm:` commits (4 functional), not "one test". All four are VFP work; ARM946E-S has no FPU. Right conclusion, wrong evidence.
- **objdiff #352's DEAD row is refuted on all three clauses** (closed 2026-07-10 by the maintainer, PR #359 merged, v3.7.3 shipped with prebuilts). Two attempted kills of the upgrade failed (`diff --format json` survives; the `left/sections/match_percent` schema is intact). The real untested gate is dsd 0.11.0's objdiff.json ↔ v3 config schema.
- **Cross-region reproducibility does not prove a structural floor.** Deterministic tool bugs reproduce identically across regions — b577 shipped real sha1 fixes for two functions the ledger lists as permanent verify-fail floor.
- **The CLOSED-LOG generator is a false premise.** The `**Brief:** NNN` line is absent from 12 of 15 briefs 550-578 with notes; 14 of 29 have no research file; regeneration would delete ~43 pre-convention entries. Take the ritual-edit fallback.
- **The `research-index --check` gate already let a note slip.** It exits 1 on main right now (brief-575-ov002-mopup.md missing). Audit the gate before relying on it to hold a new Status column true.
- **`.claude/hooks` ↔ `.codex/hooks` has never actually drifted** (created wholesale in dbaaf91c; every subsequent touch hit both). The proposed sha256 test locks in a good state rather than forcing a merge — prospective risk, impact C, don't inflate it.