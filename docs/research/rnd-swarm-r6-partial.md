[//]: # (markdownlint-disable MD013 MD041)

# R&D swarm r6 — PARTIAL salvage (2026-07-16)

> ⚠️ **SUPERSEDED by [rnd-swarm-r6-verified.md](rnd-swarm-r6-verified.md) (brief 590, 2026-07-17).**
> All 29 findings below have now been adversarially verified: 22 CONFIRMED / 7 PLAUSIBLE / 0
> REFUTED. Four are already fixed and merged (PRs #1153, #1157). One (`-ipa file`, codegen-levers)
> directly contradicts a prior CLOSED verdict from r4 and must not be actioned as written. Read the
> verified doc, not this one, for anything beyond historical interest.

⚠️ **STATUS: UNVERIFIED (historical).** The r6 swarm (8 lanes × dual refute-by-default verifiers) hit the
Claude session limit mid-run: 5 of 8 scout lanes completed (29 findings below), **zero
verifier passes ran**, no critic, no synthesis. Three lanes never scouted (retriever-fewshot,
endgame-roadmap, agent-workflow-rnd). The codegen-levers scout additionally ran without its
safety-classifier review — treat with extra care. **Resume plan:** re-invoke the workflow with
`resumeFromRunId: wf_31fae408-7a1` after the limit resets (12:10pm Europe/London) — completed
scouts replay from cache; verifiers/critic/synthesis run live. The verified report will
supersede this file.

## Raw scout findings (NOT acted on until verified, except where a live agent independently re-derives them)

### big-function-methodology

- **[strategic/hours] The 544 B ceiling is already broken (560 B singleton + 484 B from-scratch crack) — reframe b582 from existence-proof to throughput-proof on the 600-900 B band, with a feature-screened shortlist**
  - what: Brief 579 matched func_ov010_021b32ac at 560 B from scratch (no byte-matched anchor), and brief 535 cracked the 484 B func_ov010/ov015_021b2924 pair that brief 533 had failed on comprehension — so the r5 'never crossed non-clone' framing is already stale. Re-derived from EUR delinks: 868 .s TUs / 1,111,564 B above 544 B (ov002 390 TUs/568,468 B, main 244/260,372 B), median 880 B ~220 insns / 15 bl / 9 distinct callees / 28% switch. The 600-900 B probe band holds 147 ov002 + 112 arm9 TUs. Screening by matched-callee coverage yields named candidates: func_ov002_0222c674 (616 B, 14 of 17 callees already matched C, 0 switch-jump idiom, 4 pool words, no stack frame, all 3 data refs already typed in ov002_core.h), func_ov002_0222df4c (624 B, 10/14 matched callees, frame 0), func_0206a44c (main, 728 B, 8/10 matched callees, 0 pool words, frame 0x14). Probe verdict should be 'X/3 matched, N hours each, diff-type on any failure' — a rate, not a bit; ov002 first (overlay size-miss is contained; a main size-miss avalanched 567 KB in b533).
  - evidence (unverified): src/overlay010/func_ov010_021b32ac.c (560 B per config/eur/arm9/overlays/ov010/delinks.txt); docs/research/brief-579-cmatch-ov.md:23-29; docs/research/brief-535-hard-re.md:14-16,87-118; docs/research/brief-533-pattern-b-sweep.md:108-120,140-149; pool stats re-derived live from config/eur/**/delinks.txt + src/overlay002/func_ov002_0222c674.s (state-ladder over data+0x5a8, callees/externs listed at :6-25); docs/state.md:20 (b582 queued)
  - first step: Amend the b582 kickoff: target band 600-900 B, name the 3 screened candidates above (2x ov002 + 1x arm9), require per-candidate timebox (~3-4 h), max 2 independent drafts before parking with diff-type, and publish attempts/hours/final-% per candidate so the verdict is a throughput number.
- **[high/hours] Evidence-prep protocol: raw .s is ground truth, dossiers are hypotheses, pool literals come from the reloc table — and m2c must be bootstrapped first (it is not on this Mac)**
  - what: Codify a fixed evidence-prep step before any C is written for a >544 B function: (1) read the raw src/**/func_*.s directly — brief 579 found material errors in 3 of 4 prep dossiers it used (r3 was an address not a scalar; an assumed PackRGB555 helper did not exist; a callee had 2 args not 3), and brief 533's 484 B failure was a 32 B comprehension gap; (2) resolve every _LITn pool word against the delinked .o's reloc table (objdump -s + -r) before trusting any symbolic name — brief 405 proved m2c AND m2c_feed mis-resolve a raw constant as &data_xxx, which passes dcheck and fails sha1; (3) generate the structure draft with m2c via the wired feeder: python tools/m2c_feed.py --version eur <func> --m2c --context (brief 555 wiring, pulls ov002_core.h's proven types into m2c --context); (4) harvest signatures for every already-matched callee from src/ (the screening in finding 1 guarantees 8-14 of them). Blocker: tools/_vendor/m2c is absent on this machine and .venv_permuter lacks pycparser — python tools/m2c_bootstrap.py (pin ce05217) is the probe's first mechanical action.
  - evidence (unverified): docs/research/brief-579-cmatch-ov.md:36-52,80-96,133-141 (three dossier errors); docs/research/brief-405-swarm-pilot.md:60-64 (pool-literal mis-resolution); tools/m2c_feed.py:33-47 (--m2c --context wiring, brief 555) and :66 (M2C_VENDOR path); tools/m2c_bootstrap.py:28 (pin); verified live: tools/_vendor/ has no m2c dir and .venv_permuter/bin/python lacks pycparser; docs/research/brief-272-track2-coldre-accelerator-pilot.md:15-25,162-169 (m2c validated, 0x200-0x600 sweet spot)
  - first step: Run python tools/m2c_bootstrap.py, then smoke-test the full feeder on the finding-1 shortlist: python tools/m2c_feed.py --version eur func_ov002_0222c674 --m2c --context, and check every emitted symbol against objdump -r of the delinked .o.
- **[high/day] Codify the large-function crack loop as a pass-ordered runbook: structure -> data types -> locals discipline -> coloring, with the per-file .o size gate between every pass**
  - what: Every lever that cracked the 484 B and 560 B functions already exists in-repo but is scattered across 2 briefs, 2 recipe catalogs, and 26 gotchas. Write docs/research/big-function-runbook.md ordering them: PASS 1 structure (do/while bottom-test, early-return vs predication, && short-circuit = Recipe 5, statement order = store order = Recipe 6, switch ladder shape); PASS 2 data (typed struct + field access over cast-arithmetic — b579 f1 took 59%->100% on this alone; real bitfields not shift-masks; % not re-multiply — b579 f3 took 25%->100%; textually-duplicated MAX3/MIN3 ternaries — b579 f2); PASS 3 locals (declaration order = stack-slot AND callee-saved coloring — gotcha 11 + SM-13, function-scope not nested decls — b535 lever 1, phantom-zero copy idiom int ret=0; int fired=ret; + two-statement accumulate — b535 lever 2, selective hoisting per .s — Recipe 4, asymmetric pool binding — Recipe 2); PASS 4 coloring residue only (register keyword probe SM-14, then park). Between passes: ninja build/<ver>/<tu>.o + objdump -h .text size check (b533/b535 discipline) — never a full ROM build on a size-mismatched draft. Always fix the FIRST divergence in the diff (sm64ds prior: 'don't just reshuffle').
  - evidence (unverified): docs/research/brief-535-hard-re.md:87-118 (three levers, verbatim); docs/research/brief-579-cmatch-ov.md:44-96 (four levers with before/after match %); docs/research/reshape-recipes/contained-reshape-catalog.md:39-52 (recipes 1-6); docs/research/recipe-gotchas.md (gotcha 11/32); docs/research/reshape-recipes/imported-sm64ds.md:884-895 (structure-first prior) and SM-13/SM-14/SM-15 entries; docs/research/brief-533-pattern-b-sweep.md:133-149 (per-file .o + ELF size gate, main-vs-overlay blast radius)
  - first step: Draft the runbook by lifting the cited sections verbatim into the 4-pass skeleton, and paste its checklist into the b582 kickoff (the probe then doubles as the runbook's first validation).
- **[high/day] Peer-validated loop economics: cap attempts (P(match) drops ~4x after attempt 3), retrieve similar MATCHED functions as few-shot, keep permuter demoted**
  - what: Two independent LLM-loop projects published concrete numbers in the last year that calibrate the >544 B tier. Macabeus's 60-function benchmark (GBA/N64): 74% matched, ~half on the first agent attempt, and 'if a function isn't matched by the 3rd attempt the conditional probability drops to roughly one-quarter' — with 12-attempt caps and context = up to 5 similar matched functions (embedding retrieval) + caller/callee signatures + types. Chris Lewis's Snowboard Kids 2 loop (~75% matched): LLMs handle mid-size fine and only 'give up immediately' above ~1,000 instructions (~4 KB) — 4.5x our above-ceiling median of 880 B; his permuter integration was disabled entirely for producing overfitted junk (do{}while(0), illogical reuse) and doom loops, converging with our own permuter record (P-11 plateaus b198, ~0 recovery) and b403's demotion. Both used similar-function retrieval as the highest-value context; ours can be built cheaply as call-graph adjacency (share >=N callees with a matched TU) over the 3,378 matched C TUs — the finding-1 screening already computes the raw signal — with RushRE (bit-identical mwcc 2.0/sp1p5) as a secondary corpus for genre-typical C shapes.
  - evidence (unverified): https://macabeus.medium.com/can-llms-really-do-matching-decompilation-i-tested-60-functions-to-find-out-4e39b0ae4288 ; https://blog.chrislewis.au/the-long-tail-of-llm-assisted-decompilation/ ; docs/research/reshape-recipes/imported-sm64ds.md:884-960 (two-stage retry, Fable cracked 5/12 Sonnet-exhausted, refine-head economics); docs/research/brief-403-route-w1.md (permuter demoted); local: p90 of above-544 pool = 2,400 B (re-derived from config/eur/**/delinks.txt)
  - first step: Encode the stopping rule in the b582 kickoff (max 2 drafts / ~3 h per function, then park with diff-type and move on); if the probe lands, spend the day building the call-graph-adjacency retriever (reuse the finding-1 screening script) before scaling to waves.
- **[medium/hours] Inline-helper decomposition is the one peer-standard big-function lever our tree has never used — A/B it on a matched TU, then deploy for readability and as a match lever**
  - what: Vendored pokediamond (mwccarm, -inline on,noauto — same inline semantics as our -inline noauto) ships static inline helpers inside byte-matched C (field_move.c, gf_rtc.c, pokedex.c...), proving marked-inline expansion byte-matches on this compiler family. Our src/ contains ZERO inline function definitions (verified by grep across all .c/.h). For 600-900 B functions (median 15 calls + repeated field-extract idioms), factoring repeated accessor/idiom code into static inline helpers (a) may be REQUIRED for some matches — inline expansion has distinctive codegen (per-site re-evaluation, per-site register coloring) that a hand-flattened form can miss, and (b) is the difference between 'matched C' and the campaign's actual deliverable, READABLE C, at this size. Validation is cheap and byte-gated: take one already-matched mid-size TU with a repeated idiom, factor it into a static inline, confirm the .o is byte-identical, then allow the probe to use helpers freely.
  - evidence (unverified): tools/_vendor/pokediamond/arm9/src/field_move.c:101,109,117 + arm9/src/gf_rtc.c:85 (static inline in matched code); tools/_vendor/pokediamond/arm9/Makefile:128 (-inline on,noauto); tools/configure.py:128 (-inline noauto, 'Inline only functions marked with inline'); verified live: grep for 'static inline|__inline|^inline' across src/**/*.{c,h} returns zero definitions
  - first step: Pick a matched TU with a repeated 2-3-line idiom (e.g. one of the b579 family-1 quad), factor the idiom into a static inline helper in the same file, rebuild the single .o, and objdiff it — byte-identical means the lever is free to use in b582.
- **[high/day] Pre-classify the two proven >472 B walls with cheap gates: a frame-size first-diff check for stack-slot-reuse, and the C-1/gotcha-3 family for predication — plus a testable shared-temp/union lever for the former**
  - what: The only structural walls actually hit above 472 B are now precisely characterized by brief 579: family 4 = mwcc not reusing stack slots across disjoint scopes (draft frame 0x24 vs orig 0x10 — 'that one gap cascades into nearly every downstream register choice') and family 5 = predicate-vs-branch selection on a cheap guarded body (5 C shapes -> byte-identical wrong codegen; reshape-recipes Recipe 5 covers only the opposite direction). Protocol for the probe: after the first compile, diff the PROLOGUE FIRST (push set + sub sp,#N) — a frame-size mismatch is structural, so either apply the stack-slot levers immediately (b535 lever 1: function-scope declaration order; fewer named locals; and the untested-but-mechanism-grounded lever: merge disjoint-scope locals into one shared temp set or an explicit union so both scopes claim the same slots) or park within the timebox; a predicated-vs-branch diff routes to codegen-walls C-1/C-1r + gotcha 3, and if 4 variants fail, park as near-miss (it stays a safe .s). Note the P-11 permanent reg-alloc plateau is documented at 0x5c-0x74 (92-116 B) — there is no evidence it scales to this tier, so do not pre-emptively park big functions as P-11.
  - evidence (unverified): docs/research/brief-579-cmatch-ov.md:107-131 (family 4 frame 0x24-vs-0x10 diagnosis), :144-172 (family 5, five falsified variants, Recipe-5 direction gap); docs/research/brief-535-hard-re.md:92-96 (scope nesting drove stack-slot assignment — the mechanism the union lever inverts); docs/research/codegen-walls.md:6617-6800 (P-11 size range + falsification protocol precedent); docs/research/reshape-recipes/contained-reshape-catalog.md:316-368 (Recipe 5 covers only naive-too-unconditional)
  - first step: Add the prologue-first diff to the runbook (finding 3) and run the shared-temp/union experiment against brief 579's own parked exemplar ov009:021aaaec — its draft 2 and full diagnosis are in the brief, so it is the cheapest possible test subject and a crack would un-park a 472 B pair.

### ov002-arm9-strategy

- **[strategic/hours] Drop the b564 ov002 C-queue exclusion — it was drain-lane contention, the drain is finished, and every tractability signal on record is pro-ov002**
  - what: The rule lives at docs/research/campaign-analytics/cmatch-sweep-queue-overlays.md:23-24 ('filtered to module ov000-ov023, excluding ov002'; :33-34 'Pattern A3 is in excluded ov002') with NO stated rationale anywhere in the tree. Timeline supplies it: the census merged 2026-07-13 (commit 2c29a94d) while the USA/JPN ov002 .s drain was mid-wave-9 (b552/b554) and the wine mop-up (b575) didn't merge until 2026-07-15 — ov002 was the ONE contended wine-lane module. That condition has expired: brief-558-drain.md:8 says 'ov002 is DRAINED: yes' and b575 (#1145) is merged. The empirical record against a hardness reading: brief-498 wave 1 shipped 26 ov002 HIGH C-matches in one wave (brief-498-c-match-high-wave1.md:26-28); brief 536's single ship from safe-queue-v4's final 8 was an ov002 function, first-draft byte-match (brief-536-cmatch-final8.md); retriage R4-R10 confirmed ~395 ov002 E/F functions tractable (retriage/INDEX.md:79-95). The only anti-signal ever recorded is brief-522 (Codex nano-tier wave, 0 ships from ~6 attempted rows, 3 object-level near-miss parks — brief-522-cmatch-ov002.md:11-15).
  - evidence (unverified): docs/research/campaign-analytics/cmatch-sweep-queue-overlays.md:23-24,33-34; docs/research/brief-558-drain.md:8; docs/research/brief-498-c-match-high-wave1.md:26-28; docs/research/brief-522-cmatch-ov002.md:11-15; docs/research/retriage/INDEX.md:79-95
  - first step: Amend cmatch-sweep-queue-overlays.md's census rules to strike 'excluding ov002', and record the provenance (contention with the b538-575 drain, expired) so the rule can't silently resurrect in the next census brief.
- **[high/hours] The C-match queue is EMPTY today and its construction method (exact-canonical clone families) is structurally exhausted — families cover only 2.5% of ov002 and 3.0% of main bytes**
  - what: The just-merged unified queue (docs/research/campaign-analytics/cmatch-queue.md:91-96, brief 580) counts 0 open anchored + 0 open anchorless families; its 7 in-flight rows were consumed by brief 579 the same day (7 ships, 2 parks — brief-579-cmatch-ov.md:14-28). Nothing is queued. Root cause is method, not runway: the b559/b564 censuses queue only exact-canonical clone families (shared cluster_id + equal size), and in the two modules that hold 78.6% of the denominator that structure covers almost nothing — c-match-worklist.json clusters with >=2 in-module members cover 204/2,827 ov002 funcs (26,808 B = 2.5%) and 196/2,802 main funcs (19,668 B = 3.0%). Independently verified on the live tree: a mnemonic-sequence-identical census over src/overlay002/*.s and src/main/*.s (registers/immediates/symbols stripped) finds families covering 6.5%/4.8% of files but only 2.3%/2.1% of instructions. Family-first queueing mathematically cannot reach the 78% denominator; the drained overlays it worked on are structurally different (small, sibling-rich).
  - evidence (unverified): docs/research/campaign-analytics/cmatch-queue.md:91-96; docs/research/brief-579-cmatch-ov.md:14-28; docs/research/c-match-worklist.json (cluster_id analysis: ov002 70 clusters/204 members/26,808B of 1,081,424B; main 76/196/19,668B of 655,482B); docs/research/campaign-analytics/cmatch-sweep-queue.md:1-19 ('catalogs exact-canonical clone/sibling groups')
  - first step: Declare the family-census method DONE in the b580 queue doc's header and re-scope the next census brief to singleton queueing from the prep INDEX (finding 3), keeping family propagation as an opportunistic bonus per ship.
- **[strategic/days] The singleton re-queue is turnkey: c-match-prep INDEX still holds 1,720 ov002 + 1,459 main live candidates (87% of all tractable-tier bytes) with per-candidate recipes, and the wave format that ships them is already proven**
  - what: docs/research/c-match-prep/INDEX.json (3,990 candidates, each with recipe + risk + tier) cross-checked against tracked src/**/*.s TODAY: ov002 has 321 HIGH (34,400 B) + 1,399 MED (267,396 B) still-.s; main has 596 HIGH (49,588 B) + 863 MED (147,472 B). Together 498,856 of 573,992 still-.s HIGH+MED bytes = 86.9%; the other 23 modules hold only 75,136 B of tractable runway (+3.2pp of headline, max). Attempt-provenance split: 319/321 ov002 HIGH candidates sit outside brief-498's two attempted address bands — never attempted by any C wave; 473/596 main HIGH sit above 0x02044724, the last address the INDEX-order waves (w1-w8) reached. Size profile matches the proven cheap tier: 238 ov002 + 426 main HIGH candidates are <=128 B (tool-scout swarm verdict: LLM-assisted matching is real for <=~0x100). Proven wave format on this exact population: briefs 498/504/508/510 shipped 53/39/40/40 EUR accepts per wave at 22-37% yield, 15.0 funcs/h peak (brief-504:110), with 3-region ports — and wave 1 was ov002-heavy. Proposed queue: size-ascending HIGH-first, alternating one ov002 wave / one main wave (per the ~1-module-per-wave session cap), MED next gated on finding-4 KB anchors. Honest arithmetic: HIGH-only ships ~240 funcs but only ~+1pp (HIGH is small-byte); landing MED at the retriage-informed ~50% adds ~+8.7pp (~20% headline); full HIGH+MED of the two modules = 29.2% vs the 26.6% ceiling of driving all other modules to 100%.
  - evidence (unverified): docs/research/c-match-prep/INDEX.json (count:3990; per-module still-.s computed against src/overlay002/*.s + src/main/*.s); docs/research/brief-504-cmatch-high-w4.md:12,16,110; docs/research/brief-498-c-match-high-wave1.md:26-28; docs/research/campaign-analytics/prep-superseded-list.md:8-9 (473 superseded — already excluded from these counts); docs/research/tool-scout-swarm-2026-06-19.md (<=0x100 tier verdict)
  - first step: Ship a ~30-line build-free generator (filter INDEX.json to module in {ov002,main} AND tracked .s exists AND addr not in attempted-provenance set; sort tier then size) emitting cmatch-queue-v2.md, then kick one ov002 HIGH wave (~100 attempts, expected ~25-40 ships) as the validation wave.
- **[high/day] ov002's MED/E-F mass is KB-anchored, not comprehension-dark — and the retriage's 25/25-tractable 400-1200 B tier is the right candidate list for r5's 544 B ceiling probe**
  - what: The struct KB (docs/research/types/DuelStateSingleton.md, DuelQueueState.md, plus PerPlayerRowTable/id_lo13 documented in retriage docs) is what flipped ov002's 'intractable' tier: R4 re-triaged 297/413 examined <400 B funcs tractable (72%); R8 35/35; R9 38/50 (76%); and Ov002EFRetriagedLarge.md judged 25/25 (100%) of KB-struct-touching 400-1200 B functions tractable with named field-level reasons (e.g. 0x022AE718 576B DSS+0xD48; 0x02216530 620B DQS+0x48C). 301 of the 1,067 dossiers-high reference ov002 functions. Dispatch structure exists as a booster: the shipped CardHandlerTable dispatch (brief 536, func_ov002_0225368c), retriage's 'DSS fn-ptr dispatch via f_d2c (~10 funcs)', and the mnemonic census shows the biggest near-clone families concentrate in the 0x02248xxx-0x02252xxx handler band (14+8+6+5+5-member families, ~44 funcs). r5 #1's ceiling probe prescribes '2x ov002 singletons, never main-only' but names no candidates and flags that the largest-ever attempt (484 B) died on comprehension — drawing the probe's candidates from the 25/25 list neutralizes exactly that failure mode, because comprehension (struct layout, field semantics) is pre-cleared per candidate.
  - evidence (unverified): docs/research/retriage/Ov002EFRetriagedLarge.md:1-45 (25/25 table with per-candidate KB anchors); docs/research/retriage/INDEX.md:79-95,164; docs/research/brief-536-cmatch-final8.md:14-25; docs/research/types/DuelStateSingleton.md (exists, KB source); mnemonic census on src/overlay002/*.s (14-member family at 0x022483ac...)
  - first step: Amend the r5 #1 probe kickoff to pick its 2 ov002 singletons from Ov002EFRetriagedLarge.md (suggest 0x022AE718 @576B and 0x02216530 @620B — smallest, both on documented DSS/DQS fields), keeping the per-file .o + ELF .text-size guard it already prescribes.
- **[high/hours] The re-queue MUST carry an attempt-provenance column: fresh candidates yield 22-37% while old-fail retries ran 0/119, and the census's 'appears in any brief' ATTEMPTED heuristic is documented mis-marking**
  - what: Brief 512 is the controlled experiment: 119 previously-failed HIGH candidates retried with dossiers yielded 0 EUR accepts — including 112 that showed objdiff/per-unit-report 100% yet failed EUR ROM sha1 (brief-512-cmatch-high-w8.md:11-14) — versus 22-37% accept rates on fresh candidates in the same lane (briefs 504/508/510). So queue value is dominated by whether a candidate was ever attempted, yet the b559/b564 censuses classify ATTEMPTED as 'a word-boundary address hit in any brief' (cmatch-sweep-queue.md:31-33), and b559's own brain note (:48-60) documents the heuristic mis-marking two of the swarm's highest-value families as attempted when they only appear in brief-498 as DEFERRED. For ov002 the correct provenance is cheap and precise: only two briefs ever attempted ov002 C (522: ~6 rows; 536: 1 row), so the never-attempted set is derivable from those two docs plus the parked-and-floor ledger, not from brief-mention greps. Secondary guardrail the b512 data re-proves: per-accept ninja sha1 is the only gate (112 objdiff-100%-but-sha1-fail), consistent with the metric canon.
  - evidence (unverified): docs/research/brief-512-cmatch-high-w8.md:11-24; docs/research/campaign-analytics/cmatch-sweep-queue.md:31-33,48-60; docs/research/brief-522-cmatch-ov002.md:41 (the 15 never-attempted rows enumerated); docs/research/brief-504-cmatch-high-w4.md:86 (37.1% fresh yield)
  - first step: In the finding-3 queue generator, mark ATTEMPTED from explicit sources only (briefs 498-536 attempt tables + campaign-analytics/cmatch-parked-and-floor.md), default UNKNOWN->fresh-last; sort fresh-first.
- **[medium/hours] Per-module C% ground truth (the table no tool prints): ov002 4.5%, arm9 9.9%, drained overlays only 13.9% — plus the three ov002-specific wave gotchas already on record**
  - what: Computed live from config/eur/**/delinks.txt (15-line parse, the exact method r5 #5 proposes for progress.py --by-module): EUR total 2,372,300 code bytes, 8.15% C; ov002 = 1,129,252 B (47.6% of denominator) at 4.5% C (983 C TUs / 2,789 .s TUs); arm9 = 735,810 B (31.0%) at 9.9%; combined 78.6% at 6.59%; all-other-modules-to-100% ceiling = 26.56%. The same parse shows the drained overlays average only 13.9% C — i.e. even the 'successful' modules are far from cap, but their remaining tractable runway is just 75,136 B, so future rounds parked outside ov002+main can move the headline at most ~3pp. Operational gotchas for ov002 waves, all already documented but never joined in one kickoff: (1) port_to_region BSS-symbol gap — brief 498 needed 4 ov002 BSS names manually mapped per region (brief-498:55-62); (2) containment_check is only module-aware since b534 — any pre-534 ov002 containment verdict (incl. parts of the b518/b522 era) is unreliable (safe-queue-v4.md Part 1 documents an 805,331-byte miss on ov002); (3) ov002 shares its RAM window with the {0,2,5,8} overlay-swap group, so bare-address tooling can resolve into up to 9 overlapping overlays (retriage/INDEX.md R10 note — fixed in containment_check, but any NEW queue tooling must key on module+addr, never bare addr).
  - evidence (unverified): config/eur/arm9/overlays/ov002/delinks.txt + config/eur/arm9/delinks.txt (parse reproduced in-session; ov002 50,316 C / 1,078,936 .s bytes); docs/research/brief-498-c-match-high-wave1.md:55-62; docs/research/campaign-analytics/safe-queue-v4.md:24-60; docs/research/retriage/INDEX.md (R10 containment ambiguity note)
  - first step: Land r5 #5's progress.py --by-module emitting exactly this table (numbers above are the expected EUR output), then paste the ov002/arm9 rows into the wave kickoff as the before-metric.

### r5-claims-audit

- **[strategic/hours] AUDIT: r5 endgame-ledger claims (61,404 B gap, 11.6% explained, ov004 invisible) CONFIRMED byte-exact — and the live tree makes the ledger MORE wrong: gap is now 55,540 B, ledger paragraph 1 is 12/14 stale post-b577, ov004 is 36.2% of the live gap and still has zero rows**
  - what: Independently re-derived the per-module code gap from committed delinks (progress.py parse_delinks_file + CODE_SECTIONS, .text+.init) at r5's snapshot b3a5f3e9: EUR 13,432 / USA 24,044 / JPN 23,928 = 61,404 B, byte-exact match. Ledger-14 main-floor funcs sum to 7,144 B (3,572 B/region via symbols.txt sizes) = 11.63%. All 11 ov006 'parks' are phantom: EUR ov006 gap=0 and every park address sits inside a complete C TU starting exactly 4 B earlier (11/11, e.g. 0x021b5c28 inside src/overlay006/ov006_stubs_b5c24.c 0x021b5c24-0x021b5c2c). ITCM = 11 EUR + 14 USA + 14 JPN = 39, mis-filed as all-EUR. ov004 = exactly 20,110 B (5,902+7,104+7,104) with zero ledger mentions (grep -ci ov004 = 0, exit 1). LIVE DELTA: total gap is now 55,540 B (b577 shipped 12 of the 14 paragraph-1 rows — only func_02099834 remains uncovered per region, confirmed via interval subtraction; b578 closed both EUR blobs — 0x020b2d2c and 0x020b3c78 are complete .s TUs), yet the ledger still lists all 14 as parked and the blobs as 'pending Brief 578'. ov004's share rose to 36.2%.
  - evidence (unverified): docs/research/campaign-analytics/endgame-ledger.md:23-54,113-141 (stale rows) vs config/usa/arm9/delinks.txt (src/usa/main/func_020b40cc.s marked complete) and config/eur/arm9/delinks.txt (func_020b2d2c/func_020b3c78 complete); re-derivation script reproduced 61,404 B at b3a5f3e9 and 55,540 B live; tools/progress.py:42,79,241
  - first step: Execute r5 item 2 as written (extend tools/size_census.py — fix its .text-only scan and ITCM blindness — surface via progress.py --json, regenerate the ledger) but seed it with the live numbers: 55,540 B total, ov004 20,110 B/36.2%, main floor = func_02099834 x2 regions only, strike paragraph 6, refile paragraph 7 as 11/14/14.
- **[high/day] AUDIT: batch_carve returncode conflation CONFIRMED and still live post-b577 — classify() :350 and whole_function() :374 read only stdout strings while six other sites in the same file read returncode; asm_escape's 7 exit-2 infra sites (line numbers shifted) all map to 'verify-fail'**
  - what: Read the live code: tools/batch_carve.py Ops.classify (def :329, fallthrough return 'refuse' :350) and Ops.whole_function (def :352, return 'verify-fail' :374) test only r.stdout+r.stderr for 'byte-identical'/'REFUSE'/'clean' markers and never read r.returncode; returncode IS read at :324, :424, :450, :460, :478, :489 — exactly the 6 sites r5 named. asm_escape.py reserves exit 2 for infrastructure failure at 7 sites on the live tree (:938 did-not-compile, :943 not-found-in-delinks/'run ninja first', :966 did-not-assemble in generate(); :997, :1022 no-configured-size, :1026 no-disassembly, :1042 did-not-assemble in generate_whole()) vs exit 1 for genuine mismatch (:1051, with the 'did NOT verify' marker). None of the exit-2 outputs contain 'byte-identical' or the check-mark, so all 7 produce the identical 'verify-fail' verdict. No 'tool-error' verdict exists anywhere in batch_carve.py. b577's merge fixed 3 asm_escape bugs (commit 1ea3d12a) — proving 12/14 'floor' entries were tool artifacts — but left the batch_carve conflation untouched.
  - evidence (unverified): tools/batch_carve.py:329-350,352-374 (grep returncode = only :324,:424,:450,:460,:478,:489); tools/asm_escape.py:938,943,966,997,1022,1026,1042 (return 2) vs :1051 (return 1 after 'did NOT verify'); git log: 1ea3d12a fix(asm_escape) touched asm_escape only
  - first step: Execute r5 item 3 as written: capture returncode in classify()/whole_function(), add a tool-error verdict never parked and never counted as floor, require the 'did NOT verify' marker (asm_escape.py:1050) before returning verify-fail, unit-test the canned outputs via the existing _run mock seam (tests/test_batch_carve.py:273+), then re-census the remaining floor (now just func_02099834 x2 + ov004/small-overlay ranges) on a warm tree.
- **[strategic/days] AUDIT: the 544 B C-match ceiling numbers CONFIRMED byte-exact at r5's snapshot — but the live tree has ALREADY crossed it: brief 579 shipped a 560 B match (ov010), so the probe should be scoped against 560 B / 9 TUs / 868-TU 1.11 MB pool; the 0.6% sibling figure could not be independently re-derived**
  - what: At snapshot b3a5f3e9: largest matched C TU 544 B; exactly 8 TUs >512 B (7 x 544 B ov006 clone family + 528 B src/overlay004/func_ov004_021dd374.thumb.c); 869 .s TUs / 1,112,124 B above 544 B = 51.0% of .s residue; ov002 390 TUs/568,468 B and arm9 244/260,372 above the line — every figure byte-exact. The '48 B/TU' average reconciles as 193,780 B / 3,992 complete C TUs including zero-code TUs (48.5). LIVE DELTA: commit eebd36f6 (brief 579 family 6, merged) shipped src/overlay010/func_ov010_021b32ac.c at 560 B — the new record, which itself rode the sibling-family lane ('ov010/ov015 double-switch', ported to USA/JPN with a hand-fixed Task_Invoke resolution), so the core premise ('a >544 B SINGLETON has never been attempted; the wall verdict is a prediction from a 38% mis-binning triage') is intact. Pool above the old line is now 868 TUs / 1,111,564 B (the delta is exactly the one converted 560 B TU). The '5/847 = 0.6% have a clone sibling' figure has no re-derivable doc source in the tree; treat it as plausible-unverified.
  - evidence (unverified): Ceiling census re-run at b3a5f3e9 (via git archive) and live: docs/research/brief-579-cmatch-ov.md:23,221; commit eebd36f6; src/overlay010/func_ov010_021b32ac.c (560 B .text in config/eur/arm9/overlays/ov010/delinks.txt); snapshot numbers 869/1,112,124/51.0%/390-568,468/244-260,372 all reproduced
  - first step: Run r5 item 1's probe with corrected framing: 3 singletons from the >544 B pool (2x ov002 + 1x arm9), stating the demonstrated ceiling as 560 B (family-ridden) and the singleton record as 484 B failed-on-comprehension; have the probe agent first re-derive the sibling fraction over the 868-TU pool with find_region_siblings.py so the 0.6% claim gets its missing provenance.
- **[medium/hours] AUDIT: the .thumb routing-tier drop in objdiff_filter_panic_units.py CONFIRMED (exactly 36 EUR units, mechanism proven in source and on-disk artifacts) including the 4th family site in port_to_region.py — full pipeline re-run not repeated (needs ninja objdiff), but no rebuild is required to act**
  - what: tools/objdiff_filter_panic_units.py:298-321 _legacy_suffix_from_source() returns '.legacy.o'/'.legacy_sp3.o' for those suffixes and None for '.thumb.c'; the caller (:377-407) only rewrites target/base paths when the helper returns non-None, else drops the unit as 'target_path missing — unmatched routing-tier unit'. Ground truth: exactly 36 .thumb.c TUs in EUR delinks (all overlay004); build/eur holds exactly 36 compiled src/overlay004/*.thumb.o AND 36 delinked build/eur/delinks/src/overlay004/*.thumb.o, with NO bare <name>.o on either side — so dsd's bare-.o advertisement can never resolve and all 36 must drop. The 4th family site is live: tools/port_to_region.py:1152-1156 strips only .legacy_sp3/.legacy from the filename stem before parse_filename_stem(), so any .thumb.c source fails the gate with 'filename doesn't match any accepted pattern'. Not re-run: the 10680->9707 filter execution (requires ninja objdiff; the repo-root objdiff.json.raw copy is stale, 2026-05-23).
  - evidence (unverified): tools/objdiff_filter_panic_units.py:317-321 (returns None for .thumb.c),:377-407 (drop path); tools/port_to_region.py:1152-1156; counts: 36 .thumb.c in config/eur delinks, 36+36 .thumb.o under build/eur/{src,delinks}/src/overlay004 with zero bare .o counterparts
  - first step: Land r5 item 7's one-liner (if source_path.endswith('.thumb.c'): return '.thumb.o') plus a .thumb.c test case beside tests/test_objdiff_filter_panic_units.py:191, then verify on the next warm ninja objdiff that *.thumb.o.resolved goes 0->36; fold port_to_region.py:1152 into the same routing_suffixes.py migration.
- **[high/day] AUDIT: 'nothing gates main' CONFIRMED live and slightly UNDERSTATED by r5 — protection 404 / rulesets [] / protected false as of 2026-07-16, and 17 of 18 merged PRs #1129-#1146 landed with at least one failing check (r5 said 11 of 12); r5's spot values match exactly**
  - what: Live gh api today: repos/cntrl-alt-lenny/gx-spirit-caller/branches/main/protection -> 404 'Branch not protected'; /rulesets -> []; branches/main --jq .protected -> false. Independent check-run audit over every merged PR #1129-#1146 (18 PRs, all merged): 17 of 18 had >=1 failing check-run on the head SHA; only #1134 was clean. r5's named values reproduce exactly: #1146 = 7 failures, #1143 = 6, #1140 = 4, #1134 = 0. The redness composition (3 baseline failures on nearly every PR — ruff, markdownlint, invariants — rising to 4-7 when drift/unittest join) matches r5's green-main decomposition.
  - evidence (unverified): gh api repos/cntrl-alt-lenny/gx-spirit-caller/branches/main/protection (404), /rulesets ([]), per-PR check-runs for #1129-#1146 (17/18 with failures; #1146=7/16, #1143=6/14, #1140=4/6, #1134=0/2)
  - first step: Execute r5 item 4's two-step: the ~1-day green-main PR (ruff --fix + F401 delete, markdownlint --fix over the 3 generated dirs, regenerate both indices) then the ruleset requiring Python (ruff), Markdown (markdownlint-cli2), unittest, drift-check, pr-invariants (eur) with no owner bypass — r5's caveat that owner direct-push must not be exempted is confirmed relevant since all merges land via owner push.
- **[medium/hours] AUDIT: all four mechanical top-10 claims (build.ninja no-default, check_delink_dupes libs-blindness, configure.py AYXE errors, AGENTS.md:18 stale roster) CONFIRMED on the live tree with exact-count matches — safe to batch as one mechanical fix wave**
  - what: (7) build.ninja contains zero 'default' statements; live bare 'ninja -n' on the brain tree schedules 5,866 actions of which 4,000 are m2ctx .ctx.c edges (r5's 3,272 was its snapshot config — live is worse); tools/m2ctx.py:57-61 invokes gcc catching only subprocess.CalledProcessError, so a missing gcc raises uncaught FileNotFoundError; CLAUDE.md's Matching-workflow block and BUILD.md:48 both prescribe bare ninja. (8) tools/check_delink_dupes.py:39 _FILE_RE matches only ^(src/...): — synthetic repro: libs-vs-libs and src-vs-libs duplicates at 0x02000b60 return {} while src-vs-src fires; exactly 181 libs/ blocks (178 with .text) across 12 delinks.txt, interleaved into main's carve range (config/eur/arm9/delinks.txt:383+). (9) tools/configure.py verify_baserom hardcodes 'US-region'/'AYXE' in the missing-file message (~:286-292) and 'we require AYXE / US' in the mismatch message (~:311-321) with no version conditional, while printing the region-correct expected hash; correct codes sit at :114-118; called from main() at :402 before the build.ninja write at :406; 4th instance at orig/README.md:3. (10) AGENTS.md:18 still carries verbatim 'without the full build pipeline (baserom / dsd / objdiff)' + 'Cannot run ninja rom / dsd check modules / ninja objdiff'; docs/decomp-workflow.md:52 repeats it; b561's fix commit 0862afb9 touched exactly 2 files (.claude/agents/{scaffolder,decomper}.md); ground truth: the scaffolder worktree has 3 baseroms + dsd + objdiff-cli and .claude/agents/scaffolder.md:15-21 asserts the opposite of AGENTS.md:18.
  - evidence (unverified): build.ninja (grep -c ^default = 0; ninja -n = 5,866/4,000 m2ctx); tools/m2ctx.py:57-61; tools/check_delink_dupes.py:39 + synthetic find_dupes() repro + 181/178/12 count; tools/configure.py:286-292,311-321,114-118,402,406 + orig/README.md:3; AGENTS.md:18 + docs/decomp-workflow.md:52 + git show 0862afb9 (2 files) + .claude/agents/scaffolder.md:15-21
  - first step: Queue one mechanical wave with four independent commits: (a) n.default('rom sha1') after configure.py's edge emission + fix CLAUDE.md/BUILD.md:48/optimal-match-loop.md; (b) widen check_delink_dupes regex to ^((?:src|libs)/\S+):\s*$ + two failing-today regression cases; (c) GAME_CODE dict keyed by version interpolated into both verify_baserom messages + orig/README.md; (d) rewrite AGENTS.md:18 and decomp-workflow.md:52 from .claude/agents/scaffolder.md:15-21.

### toolchain-frontier

- **[high/day] objdiff v2.7.1→v3.7.3: the r5 'untested gate' (dsd objdiff.json ↔ v3 config schema) is now CLOSED by schema-level evidence — ship the bump as a two-phase side-by-side test that never touches the sha1 gate**
  - what: Every seam r5 left unverified checks out compatible: (a) dsd v0.11.0 emits objdiff.json with min_version 2.3.2 (objdiff.json.raw/objdiff.json) and v3.7.3's validate_min_version is a plain `version >= min_version` semver compare (objdiff-core/src/config/mod.rs:302-314 at tag v3.7.3) — accepted; (b) the config schema diff v2.7.1→v3.7.3 is ADDITIVE-ONLY (new: ignore_patterns, top-level+per-unit options, extra watch defaults; unit keys are a strict superset — verified by diffing config.schema.json fetched at both tags); every key dsd writes exists in both; (c) report.proto is field-for-field identical for Report/Measures/ReportUnit/ReportItem with one additive `optional uint64 address = 5`, and REPORT_VERSION = 2 at BOTH tags (objdiff-core/src/bindings/report.rs:12 @v2.7.1, :18 @v3.7.3) — so all 8 tools/*.py report.json consumers (update_progress_badge, diff_reasons, calcrom, configure, generate_heatmap, progress, ci_format_diff_reasons, objdiff_filter_panic_units) keep parsing unchanged fields; (d) `report generate -p/-o/-d` args unchanged at v3.7.3 (objdiff-cli/src/cmd/report.rs:42-54; new optional `-f` format); (e) v3.7.3 release assets keep the exact `objdiff-cli-{system}-{machine}` naming (macos-arm64 present), so tools/download_tool.py:33 fetches it unmodified. Residual risk is only METRIC DRIFT from the rewritten differ (e.g. v3.0.0 'ARM: Ignore trailing zeroed padding in inferred function sizes' #237) — and our headline metric complete_units rides dsd's metadata complete flag, not the differ, so the canon number is bump-stable; fuzzy_match_percent/matched_code may move. Smallest safe test: PHASE A (zero repo change): `python3.13 tools/download_tool.py objdiff v3.7.3 --path /tmp/objdiff-cli-v373`; on an already-built EUR tree run `/tmp/objdiff-cli-v373 report generate -o /tmp/report.v3.json` against the existing filtered objdiff.json, then `python3.13 tools/ci_format_diff_reasons.py` (it already compares two report.json snapshots) old-vs-new to quantify drift; also rerun report generate on the RAW pre-filter config (objdiff.json.raw) — if no panic, PR #359's fix holds on our objects and panic-filter case (a) (`has_addressable_text_code`, tools/objdiff_filter_panic_units.py:127) can be retired (keep case (b), our routing design). PHASE B: bump configure.py:74 + docker ARG, reconfigure, 3-region `ninja report`, confirm complete_units unchanged, then `ninja sha1` (objdiff-cli appears in NO sha1-path edge in build.ninja — the ship gate is immune by construction).
  - evidence (unverified): objdiff.json.raw/objdiff.json (min_version 2.3.2, unit keys name/target_path/metadata); tools/configure.py:74 (OBJDIFF_VERSION='v2.7.1') and :1476-1487 (report edge); https://github.com/encounter/objdiff/blob/v3.7.3/objdiff-core/src/config/mod.rs (validate_min_version); https://github.com/encounter/objdiff/blob/v3.7.3/objdiff-core/src/bindings/report.rs vs v2.7.1 (REPORT_VERSION=2 both); config.schema.json + objdiff-core/protos/report.proto diffed at tags v2.7.1/v3.7.3 via GitHub API; v3.7.3 release assets list (objdiff-cli-macos-arm64)
  - first step: Run Phase A on the brain Mac: `python3.13 tools/download_tool.py objdiff v3.7.3 --path /tmp/objdiff-cli-v373 && /tmp/objdiff-cli-v373 report generate -o /tmp/report.v3.json` in a tree where `ninja report` last succeeded, then diff /tmp/report.v3.json vs build/eur/report.json with tools/ci_format_diff_reasons.py and eyeball complete_units parity.
- **[medium/hours] The only real v3 breakage surface is the two `diff --format json` consumers (post_edit hook ×2 copies, suggest_coercion) — v3.0.0 REMOVED that mode and v3.7.0 restored it with a breaking schema; quarantine both with a golden one-unit probe before the bump**
  - what: v3.0.0 release notes: 'Removed: objdiff-cli diff JSON output mode (bd95faa) — remained unimplemented after v3 internal architecture changes'; v3.7.0 release notes: 'Breaking Change: Add symbol kind and structured flags to one-shot diff output... adjusts the CLI JSON/Protobuf output in a backwards-incompatible way'. Our consumers: (1) .claude/hooks/post_edit.py:236-253 runs `objdiff-cli diff -1 <target.o> -2 <base.o> <sym> -o - --format json` and parses exactly `data["left"]["sections"][0]["match_percent"]` — r5 verified that shallow path intact at v3.7.3 bindings/diff.rs:24-60, BUT the hook swallows every error into `return None` (post_edit.py:247-253), so if v3 output shifted the hook would silently return no feedback forever — it must be probed explicitly, its silence proves nothing; (2) tools/suggest_coercion.py:29,849 parses much DEEPER into the diff JSON (symbols/instructions/instruction/mnemonic/arguments/argument/relocation rows — grep of its key accesses) and that instruction-row schema is precisely what v3.7.0 restructured ('structured flags', 'symbol kind'); nobody has verified it. The 8 report.json consumers are NOT at risk (finding 1). This also resolves the campaign's '10 report.json consumers' count: 8 tools/*.py + the 2 byte-identical post_edit.py hook copies (b566) that ride the same binary.
  - evidence (unverified): .claude/hooks/post_edit.py:236-253 (exact invocation + `left.sections[0].match_percent` parse + error-swallowing); tools/suggest_coercion.py:29,849 and its parsed keys (symbols, instructions, mnemonic, arguments, match_percent); https://github.com/encounter/objdiff/releases/tag/v3.0.0 ('Removed: objdiff-cli diff JSON output mode'); v3.7.0 release note ('backwards-incompatible' one-shot diff output change)
  - first step: Pick one shipped unit with both .o present (e.g. any build/eur/delinks/<unit>.o + build/eur/<unit>.o pair), run the v2.7.1 and v3.7.3 binaries with identical `diff -1 ... -2 ... <sym> -o - --format json` args, save both JSONs, and assert (a) post_edit's exact key path still resolves, (b) suggest_coercion --json - parses the v3 file without exception; wire the two saved files in as golden fixtures in tests/.
- **[high/day] metroskrew: upstream frozen at 0.1.3 (2025-05-31, zero commits in 13.5 months); ALL FIVE binaries our build graph routes through hash-match its live support table (incl. mwldarm + mwasmarm); no macOS target exists — the Mac play is amd64-container compile lanes to lift the 1-wine-lane cap, and the 0.1.3 depfile fix is load-bearing**
  - what: Verified today via GitHub API: mid-kid/metroskrew pushed_at=2025-05-31, latest release 0.1.3 (assets: metroskrew-linux.tar.xz, metroskrew-windows.7z, metroskrew-wine.tar.xz — NO macOS asset; repo description: 'Port of the proprietary Windows Metrowerks ARM compiler to Linux by relinking', i.e. output is x86 Linux ELF that macOS cannot exec natively). Its support table (subprojects/packagefiles/mw-executables/meson.build) lists ds/2.0/sp1p5 mwccarm=6153c610...0ee6, mwasmarm=448cb0c7...be83, mwldarm=ccb5234d...88c9, ds/1.2/sp2p3 mwccarm=45404f28..., ds/1.2/sp3 mwccarm=ef95fb24... — I sha1'd our local tools/mwccarm tree and all five match exactly, so every compile/assemble/link tier in our routing (2.0/sp1p5 + both legacy tiers + Thumb harness assembler) is covered. Production evidence: pret/pokeplatinum CI runs 'Install Metroskrew: sudo make skrew' on plain ubuntu-24.04 (.github/workflows/build.yml:30-31) for a sha1-gated matching decomp. Mac adoption shape: run the released linux binaries inside a linux/amd64 container under Rosetta 2 (Docker Desktop/colima/OrbStack) — unlike wine, there is NO wineserver singleton, so N parallel compile lanes are independent processes; this attacks the recorded 'Mac = 1 wine lane max' cap and the wineserver-deadlock failure mode directly. Two cautions: (1) pin EXACTLY 0.1.3 — its only change is fixing `-M` depfile output that 'previously only output half of the dependencies', and every mwcc rule in our build.ninja uses depfiles (build.ninja:23-24 transform_dep.py + `depfile = $basefile.d`), so 0.1.2 would cause silent stale-rebuild bugs; (2) pokeplatinum ships on sp1p2, not our sp1p5 — byte-identity of OUR outputs still needs one local A/B before trust.
  - evidence (unverified): https://github.com/mid-kid/metroskrew (pushed_at 2025-05-31, release 0.1.3) and subprojects/packagefiles/mw-executables/meson.build version table; local `shasum -a 1` of tools/mwccarm/2.0/sp1p5/{mwccarm,mwasmarm,mwldarm}.exe and 1.2/{sp2p3,sp3}/mwccarm.exe (all five match the table); pret/pokeplatinum .github/workflows/build.yml:30-31; /Users/leo/Dev/spirit-caller/brain/build.ninja:23-24 (depfile usage); pret/pokeplatinum tools/devtools/get_metroskrew.sh (release-download recipe, SKREW_SYS=linux)
  - first step: On the Mac: `docker run --platform linux/amd64 -v $PWD:/work -it debian:bookworm-slim` (Rosetta), download metroskrew 0.1.3 linux tarball per pokeplatinum's get_metroskrew.sh URL scheme, compile ONE already-shipped TU with our exact mwcc_30_131 flag line from build.ninja, and `cmp` the .o against the wine-built build/eur/src/... .o; if identical, time 8 parallel single-TU compiles vs the wine lane.
- **[high/hours] decomp.dev unpark blocker found by graph audit: PR #1020's bare `ninja report` NEVER runs `dsd delink`, so a fresh CI workspace uploads a green-but-near-empty report — one-word workflow fix or one-line configure.py fix**
  - what: The objdiff_report edge's implicit deps are OBJDIFF + the 3 filter scripts + project.source_object_files() ONLY (tools/configure.py:1476-1487; confirmed in generated build.ninja: `build build/eur/report.json: objdiff_report objdiff.json | ./objdiff-cli tools/... build/eur/src/...o`). delink.yaml (`./dsd delink`, which produces every build/<ver>/delinks/*.o TARGET object) is a dependency only of the ROM LINK edge (build.ninja:86567) and the phony `delink` target (:271). Locally nobody notices because full builds always ran; but PR #1020's workflow (its whole point is a fresh workspace: checkout + staged orig/+extract/ + `configure.py --no-extract` + bare `ninja report`) never delinks, so every unit's target_path is missing → tools/objdiff_filter_panic_units.py DROPS each unit ('target_path missing — unmatched routing-tier unit') or patch_arm_mapping_symbols errors → the uploaded <ver>_report is empty-or-red. The PR's own comment mis-states the graph ('`ninja report` builds: ... dsd delink -> compile every src .o -> ...') and its deliberate optimization — dropping the `arm9` target that zeldaret/ph runs first ('ph runs ninja arm9 report check; report alone is sufficient') — is exactly what severed the delink dependency: in ph's flow arm9 forces delink; in ours nothing does. Fix option A (workflow): `ninja delink report`. Fix option B (better, fixes every fresh clone): add `str(project.game_build / 'delinks' / 'delink.yaml')`-equivalent to the objdiff_report implicit list in configure.py:1478. Everything else in the PR survived 3 weeks of repo churn: all five configure.py flags it uses still exist (-w :57, --compiler :59, --no-extract :60, --dsd :61, --skip-sha1 :62), PYTHON=sys.executable (:241) is container-safe, tools/requirements.txt exists, report edge python is not hardcoded to homebrew in the container case.
  - evidence (unverified): tools/configure.py:1476-1487 (objdiff_report implicit deps — no delink); /Users/leo/Dev/spirit-caller/brain/build.ninja:188,271 (delink edge + phony) and :86567 (delink.yaml consumed only by the link edge); gh pr diff 1020 (.github/workflows/decomp-report.yml 'ninja report' step + its comment block claiming delink runs); r5 §S documents the filter's target_path-missing drop behavior (tools/objdiff_filter_panic_units.py)
  - first step: Prove it locally without docker: in a scratch worktree, `python3.13 tools/configure.py eur --no-extract ...` then `rm -rf build/eur/delinks && ninja report` and inspect build/eur/report.json's total_units; then push the one-line configure.py fix (delink.yaml into the report edge's implicit deps) + change the PR #1020 step to `ninja delink report` as belt-and-suspenders.
- **[medium/hours] decomp.dev onboarding is fully DECOUPLED from the objdiff bump and cheap to run: ingestion contract verified in decomp.dev source (artifact regex + Report::parse JSON + migrate; REPORT_VERSION=2 both sides), refresh is a 30-min poll without the GitHub App, and the recurring owner cost is pin-sync only**
  - what: Read from encounter/decomp.dev source (pushed 2026-05-24): crates/github/src/lib.rs:481 matches artifact names against `^(?P<version>[A-z0-9_.\-]+)[_-]report(?:[_-].*)?$` (so `eur_report` exactly as PR #1020 uploads); :626-648 accepts, inside the artifact zip, any file with stem `report` or `<version>_report` regardless of extension, then calls objdiff-core `Report::parse` (first byte `{` → JSON path — bindings/report.rs:22-38 at v3.7.3) followed by `report.migrate()` which upgrades v0/v1 and accepts version==2. Since v2.7.1 emits REPORT_VERSION=2 and decomp.dev's objdiff-core v3 still defines REPORT_VERSION=2, our CURRENT pinned toolchain's report.json ingests with zero migration — so onboarding does not wait on finding 1's bump, and the bump doesn't invalidate onboarding (no ordering constraint in either direction). Refresh mechanics (crates/web/src/cron.rs:18-40): projects WITHOUT the decomp.dev GitHub App are polled every 30 minutes (partial) + every 12h (full); the App only adds webhook-driven instant ingestion and PR comments — genuinely optional, matching issue #1022. Remaining unpark steps are exactly issue #1022's four (build+push private GHCR image with `--platform linux/amd64`; set package private + grant repo pull; merge #1020 WITH finding-4's delink fix and with the Dockerfile ARGs (DSD_VERSION/OBJDIFF_VERSION) synced to configure.py:72-74; register at decomp.dev/manage/new) — about one owner-hour plus one docker build on the Mac where orig/+extract/ exist. Steady-state maintainer cost: zero-touch (CI runs per push to main; image rebuilds ONLY when a toolchain pin bumps, since delinks.txt lives in the repo and extract/ is ROM-static).
  - evidence (unverified): https://github.com/encounter/decomp.dev crates/github/src/lib.rs:481,626-648 (artifact regex, Report::parse + migrate) and crates/web/src/cron.rs:18-40 (30-min/12-h refresh); https://github.com/encounter/objdiff/blob/v3.7.3/objdiff-core/src/bindings/report.rs:22-38,64-75 (JSON parse + migrate, REPORT_VERSION=2); tools/configure.py:72-74 (pins the Dockerfile ARGs must track); gh issue view 1022 + gh pr view 1020 (files, still-draft, MERGEABLE, authored 2026-06-24)
  - first step: Bundle finding 4's fix into the PR #1020 branch, then run the docker build from docs/decomp-dev-setup.md on the Mac (`docker build --platform linux/amd64 -f docker/decomp-dev-build.Dockerfile -t ghcr.io/cntrl-alt-lenny/gx-spirit-caller-build:main .`) and execute the doc's local self-test (container run of configure+`ninja delink report` for eur) BEFORE pushing the image — that one local run exercises every failure point issue #1022 lists.

### codegen-levers

- **[high/day] Opt-level is a proven per-function/per-TU routing axis on our exact compiler — and we have never pointed it at a parked wall**
  - what: SonicRushAdventure-Decomp (bit-identical mwccarm 2.0/sp1p5, byte-matching CI) ships 7 game TUs at non-default opt levels (-O4,s, -O2,s, -O2,p vs default -O4,p) via per-file OPTFLAGS overrides, AND wraps individual functions in `#pragma push` / `#pragma optimization_level 2` / `#pragma pop` inside otherwise -O4,p TUs (5 sites in wirelessManager.c, plus wfs.c and wfs_thread.c). Add the opt-level axis to the direct-mwcc variant matrix and probe the parked near-miss ledger: guard-chain stack-layout coin-flip trio (02089df8/70/e8, 12 B), DISPCNT/BG3CNT scheduling park (0208ddec), Park-Miller mask lowering (02054b44), immediate decomposition (0202e5ac), byte-swap chain (0201d620), plus the P-6 predication-threshold class. Pragma form needs zero build-graph changes (works inside existing carve TUs); only if a whole-TU `,s` flag is needed does it become a configure.py suffix tier mirroring the shipped .legacy.c plumbing.
  - evidence (unverified): https://github.com/RushRE/SonicRushAdventure-Decomp/blob/94de7ce8e95c1136f8d14e93f1dc3c07d10f20b9/common.mk#L176-L182 (per-TU OPTFLAGS) and /src/network/wirelessManager.c#L328-L331 (#pragma optimization_level 2); ours: tools/configure.py:121 single '-O4,p', no ,s/-O2 token anywhere in docs/ or tools/ (grep verified); walls: docs/research/codegen-walls.md:6320-6326 (P-8 untested opt-level hunch); parked targets: docs/research/campaign-analytics/cmatch-parked-and-floor.md:15-25; pragma-accepted-by-our-binary: docs/research/style-a-epilogue.md:78-83
  - first step: In the direct-mwcc harness, recompile the guard-chain trio (func_02089df8 family) and 0208ddec under {-O2,p | -O4,s | -O2,s | pragma optimization_level 1/2/3} x {2.0/sp1p5, 1.2/sp2p3, 1.2/sp3}, objdump-diff vs orig; any flip ships via #pragma inside the existing carve TU and gates on ninja sha1.
- **[high/hours] Our CC_FLAGS is missing `-ipa file` — the scene-standard flag for matched NDS game C on 2.0-series mwcc — and the readable-C endgame is exactly where it bites**
  - what: Both independent matching corpora on 2.0-series mwccarm compile ALL game C with `-ipa file`: SonicRushAdventure (bit-identical sp1p5; common.mk IPA_FLAG, cleared only for SDK libs; also baked into their permuter_settings.toml compiler_command) and pret/pokeplatinum (meson.build global shared_args, with per-lib '-wrap:noipa' opt-outs). Our tools/configure.py CC_FLAGS has no -ipa token at all. `-ipa file` changes codegen only when a TU has intra-TU call structure (statics, visible callees) — I verified our biggest matched multi-function TUs (24-function ov006 files) have zero static functions and ~zero intra-TU calls, so our existing matches DON'T contradict the hypothesis that the original ROM was built with it.
  - evidence (unverified): https://github.com/RushRE/SonicRushAdventure-Decomp/blob/94de7ce8e95c1136f8d14e93f1dc3c07d10f20b9/common.mk#L162-L173 and permuter_settings.toml:7; https://github.com/pret/pokeplatinum/blob/main/meson.build#L28 ('-ipa','file') + lib/crypto/meson.build ('-wrap:noipa'); ours: tools/configure.py:121-133 (no -ipa); non-contradiction: src/usa/overlay006/func_ov006_021b4fb0.c (24 funcs, 0 static, verified via grep); brief-036 tested -ipa file only on a single-function TU (docs/research/style-a-epilogue.md:70-77) where it is structurally a no-op
  - first step: A/B a synthetic two-function TU (static callee writing a global, caller re-reading it around the call) under 2.0/sp1p5 with/without -ipa file to confirm a codegen delta; then rebuild one parked cross-call near-miss together with its address-adjacent original-TU callee in one TU with the flag, and diff.
- **[medium/day] decomp.me mwcc_30_131 census: ~1,400+ public preset scratches, API-harvestable from a browser context — refutes the 'unverifiable and blocked' verdict**
  - what: Verified via decomp.me's own API (driven through the browser pane; bare curl/WebFetch get Cloudflare-403'd): the nds_arm9 preset listing reports Sonic Rush Adventure (preset 148, compiler mwcc_30_131 = OURS) with 1,352 scratches and Phantom Hourglass (preset 128, ALSO mwcc_30_131) with 65 — plus custom-flag scratches on the same compiler id (cursor pagination shows ~500 created since 2026-01-24 alone, ~85/month current rate). Unauthenticated GET /api/scratch/<slug> returns full source_code, compiler_flags, score/max_score, parent (fork lineage) and context. Harvest protocol: (a) paginate /api/scratch?compiler=mwcc_30_131 (~15-20 pages) through the browser-pane fetch loop; (b) higher-signal curated subset needing NO API: SonicRushAdventure embeds 146 unique decomp.me scratch links in source comments with match % — where the repo function later reached 100%, diffing the scratch source vs repo HEAD isolates the exact edit that closed the gap.
  - evidence (unverified): decomp.me API responses captured in-session: /api/preset?platform=nds_arm9 → preset 148 'Sonic Rush Adventure' compiler mwcc_30_131 n=1352; preset 128 'Phantom Hourglass' mwcc_30_131 n=65; /api/scratch/3UGCj → 200 with source_code+flags+score (verify at https://decomp.me/preset/148); scratch-link corpus: grep -rc 'decomp.me/scratch' over SonicRushAdventure-Decomp src/ = 146 unique (e.g. src/game/stage/mapFarSys.c:539); prior verdict being extended: docs/research/speedup-research-swarm-2026-07-03.md:55-56,142
  - first step: Run the browser-pane pagination loop to dump all mwcc_30_131 scratch metadata to JSON in the repo (id, score, max, family, flags), then join SRA's 146 in-repo links against repo HEAD to extract the first batch of solved near-miss diffs.
- **[medium/hours] Two reg-alloc steering levers from the bit-identical corpus that our taxonomy lacks: dead-local ballast and same-scalars role-reuse**
  - what: (a) Dead-local ballast: SRA's SailSea_DrawSeaSegment declares `s32 v = 0; u16 v_u16 = 0;` with the comment 'not sure what they did here, but both of these are required to match' — initialized locals whose only effect is perturbing allocation/frame layout. (b) Same-scalars role-reuse: ringManager.c reuses ONE pair of locals (left/right) across three successive axis computations ('we have to use the same two variables to get the code to match!'), pinning one register pair across statement groups instead of letting fresh names re-color. Neither shape appears in recipe-gotchas.md (1-34), the contained-reshape-catalog, nor any codegen-walls falsification list (P-4/P-8/P-11/C-39 matrices tried renames, reorders, temps, volatile — never dead locals, never cross-group reuse).
  - evidence (unverified): https://github.com/RushRE/SonicRushAdventure-Decomp/blob/94de7ce8e95c1136f8d14e93f1dc3c07d10f20b9/src/sail/sailSea.c#L335-L338 and /src/stage/core/ringManager.c#L610-L626; absence in ours: grep over docs/research/recipe-gotchas.md + codegen-walls.md + reshape-recipes/ for dead-local/ballast/variable-reuse = 0 hits; target class: docs/research/campaign-analytics/cmatch-parked-and-floor.md:22-31 (register-role swap rows)
  - first step: Take the three cheapest register-swap parks (021ae218 6 B; 0200dd58 20 B/11 runs; 02078ebc 1 B) and run both levers through the direct-mwcc harness: add one dead initialized local (u16 then s32 variants), and rewrite fresh per-group temps as one reused pair; objdump-diff each.
- **[medium/hours] `(s32)(u16)` cast-chain at call-argument position emits the lsl#16/lsr#16 'bloat pair' on our exact compiler — attacks the P-1 halfword sub-family**
  - what: SRA could not stop mwcc 2.0/sp1p5 from optimizing away redundant halfword extends until they forced them with an explicit double cast at the ARGUMENT position: `#define FX_SINCOSCAST (s32)(u16)` used as `SinFX(FX_SINCOSCAST(angle))` — documented in-source as ''Necessary' in order to match the bloat LSL16/LSR16 pairs (which probably stem from implicit conversions across inlined functions)'. Same pattern deployed at a second site (gameObject.c). This is the EMIT direction of our P-1 wall (target preserves the shift pair, mwcc collapses/omits): brief 049 wave 14 parked 7 ov002 `lsl 16; lsr 16` candidates as P-1-permanent after C-15 routing misfired, and P-1's falsification only proved the 8-bit literal-shift form collapses.
  - evidence (unverified): https://github.com/RushRE/SonicRushAdventure-Decomp/blob/94de7ce8e95c1136f8d14e93f1dc3c07d10f20b9/src/stage/stageTask.c#L923-L928 (macro + comment) and /src/stage/gameObject.c#L782; our wall: docs/research/codegen-walls.md:5971-5994 (P-1 wave-14 members func_ov002_0226af78 etc.) and gotcha 16 at docs/research/recipe-gotchas.md:1082
  - first step: Direct-mwcc compile the natural C of one wave-14 member (func_ov002_0226af78) with the extract rewritten as `(s32)(u16)expr` in value and argument positions (plus a u16-typed intermediate variant); objdump-diff for the lsl/lsr pair.
- **[medium/hours] Add a 2.0 sub-SP probe (sp1p2 first) to the wall-declaration protocol — SRA routes a game TU through 2.0/sp1p2 for a scheduling divergence our protocol can't see**
  - what: SRA compiles src/stage/stageTask.c with MWCCVER := 2.0/sp1p2 while everything else is 2.0/sp1p5, with in-source comments pinning the reason: 'MW < 2.0sp1p5 seems necessary in order to match the early computing of the sin/cos array indices' and a decomp.me scratch (9f4ui) recorded as '100% with MW < 2.0sp5, but only close to 100% otherwise'. Instruction-SCHEDULING differences between 2.0 sub-SPs are therefore real and TU-granular in shipped titles. Our wall protocol tri-compiles 2.0/sp1p5 + 1.2/sp2p3 + 1.2/sp3 only; the 10-version 2.0 SP ladder (base..sp2p4) already sits in tools/mwccarm (brief 036 enumerated it) but is consulted by no recipe. pret/pokeplatinum corroborates the pattern at library granularity (subproject compilers 2.0/sp1p2, 2.0/sp2, 2.0/sp2p2 in meson.build).
  - evidence (unverified): https://github.com/RushRE/SonicRushAdventure-Decomp/blob/94de7ce8e95c1136f8d14e93f1dc3c07d10f20b9/common.mk#L43 and /src/stage/stageTask.c#L1152; pret corroboration: https://github.com/pret/pokeplatinum/blob/main/meson.build (mwrap_sdk '2.0/sp1p2'/'2.0/sp2'/'2.0/sp2p2'); our gap: docs/research/codegen-walls.md Recommendation §3 routes only 3 tiers; vendored ladder: docs/research/style-a-epilogue.md:61-66; targets: docs/research/campaign-analytics/cmatch-parked-and-floor.md:19-24
  - first step: Direct-mwcc compile 0208ddec and 0201d620 best-known C under 2.0/sp1p2 (then sp1, base) with unchanged flags; objdump-diff. If either flips, file the T-3-pattern routing-tier brief before shipping.
