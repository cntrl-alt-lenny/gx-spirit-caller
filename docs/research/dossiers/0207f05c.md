# 0207f05c — func_0207f05c (main, class C, 220B)

## Recipe + Risk
**Recipe:** reuse func2 wrap; unsigned count>=n (movcs) materialised; mla base bound once from initial h8; store h0,h2,h4 in order; src post-inc, dst[i]
**Risk:** inner stores split addressing — first via [r4,i*8] scaled-reg, h2/h4 via precomputed ip=r4+i*8; this scheduling/addressing of dst[i].field is permuter-class, unlikely exact first build.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0207f05c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     reuse func2 wrap; unsigned count>=n (movcs) materialised; mla base bound once from initial h8; store h0,h2,h4 in order; src post-inc, dst[i]
 *   risk:       inner stores split addressing — first via [r4,i*8] scaled-reg, h2/h4 via precomputed ip=r4+i*8; this scheduling/addressing of dst[i].field is permuter-class, unlikely exact first build.
 *   confidence: low
 */
/* func_0207f05c: range-gate then struct-copy fill into record array. */
typedef struct {
    unsigned short h0;   /* 0x0 */
    unsigned short h2;   /* 0x2 */
    unsigned short h4;   /* 0x4 */
    unsigned short h6;   /* 0x6 */
} elem_t;                /* 8 bytes */

typedef struct {
    int            f0;          /* 0x0  record index */
    unsigned short h4;          /* 0x4 */
    unsigned short h6;          /* 0x6 */
    unsigned short h8;          /* 0x8 */
} obj_t;

extern unsigned char data_021a08f4[];

int func_0207f05c(obj_t *p, elem_t *src, int n) {
    unsigned short h6 = p->h6;
    unsigned short h8 = p->h8;
    int flag = (h8 <= h6 + 1) && ((unsigned)p->h4 <= h6);
    unsigned short count = flag ? (unsigned short)(h6 - h8 + 1) : 0;
    if (count >= (unsigned)n) {
        elem_t *dst = (elem_t *)((char *)data_021a08f4 + p->f0 * 0x540 + 0x100) + p->h8;
        int i;
        for (i = 0; i < n; i++) {
            dst[i].h0 = src->h0;
            dst[i].h2 = src->h2;
            dst[i].h4 = src->h4;
            src++;
            p->h8++;
        }
        return 1;
    }
    return 0;
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| +0x00 | f0 | int | record index; `f0 * 0x540` is mla base (same formula as ec68/ef90) |
| +0x04 | h4 | unsigned short | lower bound; compared unsigned against h6 |
| +0x06 | h6 | unsigned short | upper bound reference |
| +0x08 | h8 | unsigned short | start index into dst array; post-incremented each iteration |
| elem+0x00 | h0 | unsigned short | first halfword of 8-byte record; store order: h0 first |
| elem+0x02 | h2 | unsigned short | second halfword; store order: h2 second |
| elem+0x04 | h4 | unsigned short | third halfword; store order: h4 third |

## Closest matched example
**Path:** src/main/func_0207ec68.c (prep draft — same Cell-family, same mla base)
**Key lesson:** The mla base formula `data_021a08f4 + f0 * 0x540 + 0x100` and the mixed signed/unsigned range test (`h8 <= h6+1` signed, `(unsigned)h4 <= h6` unsigned) are shared across ec68, ef90, and this function. The store order `h0` then `h2` then `h4` inside the loop is fixed. The scheduler divergence is in how mwcc addresses `dst[i].h0` vs `dst[i].h2`/`dst[i].h4`: orig uses scaled-reg `[r4, i*8]` for `h0` but a precomputed pointer `ip = r4 + i*8` for `h2` and `h4`.

## Try this
1. Write the three stores as `dst[i].h0 = src->h0; dst[i].h2 = src->h2; dst[i].h4 = src->h4;` in exactly this order (not via a temporary `elem_t` copy or struct assign). Keeping `src++` and `p->h8++` after all three stores forces the post-inc to appear at the end of the loop body.
2. Check objdiff: verify that `dst[i].h0` uses a scaled-register addressing mode (`[r4, rx, lsl#3]`) while `dst[i].h2` and `dst[i].h4` use a base+offset addressing mode (precomputed base plus 2/4). If all three emit the same addressing form, try introducing an explicit intermediate pointer `elem_t *e = &dst[i]` to force the base+offset pattern for h2/h4.
3. Park as permuter-class; the split `[r4,i*8]` vs `ip=r4+i*8` addressing scheduling for consecutive halfword stores is un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021a08f4` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

