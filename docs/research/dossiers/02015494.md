# 02015494 — func_02015494 (main, class D, 420B)

## Recipe + Risk

**Recipe:** 0x8d8 :1 bitfield guard; 0x920 jump-table(0..4) + state-3 nested 0x924 jump-table; unsigned ldmcc; orr #0x1000000; (x&~1)|1
**Risk:** struct-guessed: asm TRUNCATED inside .L_1b1c — state-3 case2/3, state-4 arm and _LIT consts reconstructed; verify vs full .s. Two stacked addls jump-tables (0x920 then 0x924) may flip layout under mwcc (permuter-class fallback to .s).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02015494 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     0x8d8 :1 bitfield guard; 0x920 jump-table(0..4) + state-3 nested 0x924 jump-table; unsigned ldmcc; orr #0x1000000; (x&~1)|1
 *   risk:       struct-guessed: asm TRUNCATED inside .L_1b1c — state-3 case2/3, state-4 arm and _LIT consts reconstructed; verify vs full .s. Two stacked addls jump-tables (0x920 then 0x924) may flip layout under mwcc (permuter-class fallback to .s).
 *   confidence: low
 */
/* CAMPAIGN-PREP candidate for func_02015494 (main, class D, MED tier) — prep batch p_0147.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * Drop into src/main/, run ninja + objdiff, then apply the risk note below.
 *   recipe: :1 bitfield guard (0x8d8 bit0) + 0x920 addls jump-table (0..4); state-2 nested 0x924
 *           addls jump-table; unsigned ldmcc early-returns; orr #0x1000000 RMW; (x&~1)|1; o[8] stores.
 * NOTE: JSON asm TRUNCATED inside .L_1b1c (state-2 case after the `orr r0,r0,#0x1` of the 0x8e0 RMW).
 * The remaining 0x924 arms and literal pool are reconstructed in-idiom — VERIFY against full .s.
 */
extern char *GetSystemWork(void);
extern int  func_020139b4(void);
extern int  func_020139c4(void);
extern void func_02018a38(int);
extern void func_02018a64(void);
extern int  func_02019034(void);
extern int  func_020190ec(void);
extern void func_020195ec(void);
extern int  func_02019664(void);
extern void func_0201b708(int);
extern void func_0201b7b4(void);
extern int  func_0201b7e0(void *);

struct Flag { unsigned int b0 : 1; };

void func_02015494(void *o) {
    char *w = GetSystemWork();
    if (((struct Flag *)(w + 0x8d8))->b0 == 0) return;
    switch (*(int *)(w + 0x920)) {
    case 1:
        if (func_020190ec() == 0) return;
        if (func_020139c4() != 5) return;
        if ((unsigned int)func_02019034() < (unsigned int)(*(int *)(w + 0x8dc) + 1)) return;
        *(int *)((char *)o + 8) = 0x000e0100;
        *(int *)(w + 0x900) |= 0x1000000;
        return;
    case 2:
        if (func_020139b4() != 2) return;
        if (func_0201b7e0(o) != 4) return;
        *(int *)((char *)o + 8) = 0x000e0101;
        func_0201b708(4);
        func_02018a38(0x13);
        return;
    case 3:
        switch (*(int *)(w + 0x924)) {
        case 1:
            *(int *)(w + 0x924) = 4;
            /* fallthrough */
        case 4:
            if (func_02019664() != 0x69) return;
            *(int *)((char *)o + 8) = 0x000e0102;
            func_02018a64();
            *(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;
            return;
        case 3: /* reconstructed */
            if (((struct Flag *)(w + 0x8e0))->b0) return;
            func_020195ec();
            return;
        case 2: /* reconstructed */
            *(int *)((char *)o + 8) = 0x000e0103;
            func_0201b7b4();
            return;
        }
        return;
    case 4: /* reconstructed: state-4 = .L_1b90 */
        func_02019034();
        return;
    }
}
```

## Struct context

All offsets are from `GetSystemWork()` (SystemWork, BSS `data_02104f58`, size 0x92c).
The `o` parameter is the caller-supplied output buffer; `o+8` is the status-word write target (not a SystemWork field).

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x8d8 | enable bit | s32 `:1` bit0 | master gate — early return if 0 |
| +0x900 | flag word | s32 | OR'd with 0x1000000 in case-1 (set bit 24) |
| +0x8dc | bound word | s32 | lower-bound for unsigned compare in case-1 (state[0x8dc]+1) |
| +0x8e0 | bitfield word | s32 | bic|orr set in case-3/4: `(x&~1)|1` sets bit 0; bit0 guard check in case-3/3 |
| +0x8e8 | bit-range field | s32 | written via func_02018a38(0x13) / func_02018a64() |
| +0x920 | mode/state | s32 | outer jump-table dispatch (cases 1..4) |
| +0x924 | sub-state | s32 | inner jump-table dispatch (cases 1,2,3,4; case1 self-sets to 4 then falls through) |

## Closest matched example

**Path:** src/main/func_02014bf4.c
**Why similar:** Same pattern — `GetSystemWork()` + struct-Bit0 guard on `+0x8d8` + `+0x920` dispatch + `o+8` status write.
**Key lesson:** The `:1` bitfield struct idiom (`struct Bit0 { unsigned int b : 1; }`) is required to emit `lsl#31;lsr#31` instead of `and #1`. The two nested `addls` jump-tables (0x920 outer, 0x924 inner) may cause register/scheduling divergence; if so, try reordering case arms to match the jump-table order in the .s, or escalate to .s fallback.

## Try this

1. Drop the prep draft as-is; confirm the `struct Flag { unsigned int b0 : 1; }` guard on `+0x8d8` emits `lsl#31;lsr#31` in objdiff. If it emits `and #1`, restructure as a named bitfield.
2. If the outer or inner jump-table layout flips (cases reordered vs .s), try numbering the switch cases to match the assembly order exactly and check whether adding/removing the `case 4` reconstructed arm changes the `addls` offset.
3. If both tables remain mismatched after reshaping, this is a permuter-class double-table problem — fall back to `.s` and ship as `func_02015494.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00110100` | MMIO/const |
| `_LIT1` | `0x00110200` | MMIO/const |
| `_LIT2` | `0x00110300` | MMIO/const |
| `_LIT3` | `0x00110301` | MMIO/const |
| `_LIT4` | `0x00110302` | MMIO/const |
| `_LIT5` | `0x00110303` | MMIO/const |
| `_LIT6` | `0x00110400` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190ec` | main TU call |
| `bl` | `func_020139c4` | main TU call |
| `bl` | `func_02019034` | main TU call |
| `bl` | `func_020139b4` | main TU call |
| `bl` | `func_0201b7e0` | main TU call |
| `bl` | `func_0201b708` | main TU call |
| `bl` | `func_02018a38` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_02018a64` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

