# 0205be60 — func_0205be60 (main, class C, 184B)

## Recipe + Risk

**Recipe:** exact store-order ([14] before refcount, [c]/[10] after [1c]); bind owner=*holder across alloc
**Risk:** reshape-able: the unusual store interleave ([0x0c]/[0x10] emitted after [0x1c]) only matches if the C assignments stay in that source order; mwcc reordering struct stores would diverge. Owner offsets 0x20c/0x424 struct-guessed.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0205be60 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     exact store-order ([14] before refcount, [c]/[10] after [1c]); bind owner=*holder across alloc
 *   risk:       reshape-able: the unusual store interleave ([0x0c]/[0x10] emitted after [0x1c]) only matches if the C assignments stay in that source order; mwcc reordering struct stores would diverge. Owner offsets 0x20c/0x424 struct-guessed.
 *   confidence: med
 */
/* func_0205be60: allocate a 0x24 node, populate it, manage a refcount on
 * the owner, then push it onto owner[0x424] and return *out = node. r4 =
 * *holder (the owner) cached across the alloc. On alloc failure log+return 1.
 * STORE ORDER is load-bearing: [0],[4],[8],[14] then the refcount field
 * [18], then [1c],[c],[10],[20] (note [c]/[10] are written LATE, after
 * [1c]). When the first field (a==0) the node is a 'root' (refcount=1, no
 * owner bump); else node[0x18]=owner[0x20c]++ clamped to >=2. */

typedef struct Owner { char _pad[0x20c]; int refcount; char _pad2[0x424 - 0x210]; void *list; } Owner;
typedef struct Node {
    int   f00;   /* +0x00 a */
    int   f04;   /* +0x04 b */
    int   f08;   /* +0x08 s0 */
    int   f0c;   /* +0x0c s1 */
    int   f10;   /* +0x10 s2 */
    int   f14;   /* +0x14 = 0 */
    int   f18;   /* +0x18 refcount slot */
    int   f1c;   /* +0x1c = 0 */
    void *next;  /* +0x20 list link */
} Node;

extern void *func_020453e8(int size);
extern void  func_0205ffc0(void *holder, void *tag);
extern char  data_02100b08[];

int func_0205be60(void *holder, int a, int b, void **out, int s0, int s1, int s2) {
    Owner *owner = *(Owner **)holder;
    Node *n = (Node *)func_020453e8(0x24);

    if (n == 0) {
        func_0205ffc0(holder, data_02100b08);
        return 1;
    }
    n->f00 = a;
    n->f04 = b;
    n->f08 = s0;
    n->f14 = 0;
    if (a == 0) {
        n->f18 = 1;
    } else {
        int rc = owner->refcount;
        owner->refcount = rc + 1;
        n->f18 = rc;
        if (owner->refcount < 2) owner->refcount = 2;
    }
    n->f1c = 0;
    n->f0c = s1;
    n->f10 = s2;
    n->next = owner->list;
    owner->list = n;
    *out = n;
    return 0;
}
```

## Struct context

No SystemWork or GameSingleton access. Operates on an `Owner` struct (pointer-indirected) and a newly allocated `Node`.

| offset | field | type | use |
|--------|-------|------|-----|
| Owner+0x20c | refcount | int | read-modify-write: incremented, clamped to >=2; source for n->f18 in non-root path |
| Owner+0x424 | list | void* | linked-list head; n->next receives old head; head updated to n |
| Node+0x00 | f00 | int | receives a |
| Node+0x04 | f04 | int | receives b |
| Node+0x08 | f08 | int | receives s0 |
| Node+0x0c | f0c | int | receives s1; written LATE (after f1c) |
| Node+0x10 | f10 | int | receives s2; written LATE (after f1c) |
| Node+0x14 | f14 | int | always 0; written before refcount block |
| Node+0x18 | f18 | int | refcount slot: 1 if a==0 (root), else owner->refcount pre-increment |
| Node+0x1c | f1c | int | always 0; written before f0c/f10 |
| Node+0x20 | next | void* | receives owner->list (old head) |

**Owner struct offsets are guessed**: `refcount` at +0x20c and `list` at +0x424 are inferred. The pad between them is `0x424 - 0x210 = 0x214` bytes.

## Closest matched example

**Path:** src/main/func_0201d07c.c
**Why similar:** alloc + null-guard + fill-fields-in-unusual-order + list-push pattern.
**Key lesson:** The store interleave is the sole matching constraint: `f14=0` must appear BEFORE the refcount if-else block, and `f0c`/`f10` must appear AFTER `f1c=0`. This is the exact order in the prep draft — do not reorder based on field offset. MWCC respects the C source order for struct stores, so the C must mirror the asm store sequence precisely. Also: `owner` must be bound once from `*(Owner**)holder` BEFORE the alloc call (r4 in the recipe), so it survives across the `func_020453e8` call.

## Try this

1. Build as-is. In objdiff verify that `str n->f14, #0` appears before the `cmp r0, #0` branch (the `a==0` test), and that `str n->f0c` and `str n->f10` appear after `str n->f1c`. If any store is out of order, reorder the corresponding C assignment to match the asm sequence exactly.
2. If the refcount clamp `if (owner->refcount < 2) owner->refcount = 2;` generates an unexpected branch shape (bge vs blt), rewrite as `n->f18 = rc; owner->refcount = rc + 1; if (owner->refcount < 2) owner->refcount = 2;` to separate the pre-increment snapshot from the clamp.
3. Park as `.s` if two reshape attempts cannot align the store order — the risk says reshape-able but the interleave is unusual enough that MWCC may not honor the C ordering under optimization. Accept as `.s` rather than permuting.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02100b08` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020453e8` | main TU call |
| `bl` | `func_0205ffc0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

