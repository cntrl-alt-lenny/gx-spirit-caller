# 020778c4 — func_020778c4 (main, class D, 144B)

## Recipe + Risk

**Recipe:** x*10+y => mla (keep 10 in a reg); unsigned cmp for cs/cc; accumulator-first adds for the pack
**Risk:** permuter-class: 'mla fusion' (note) — mwcc may lower hi*10+lo to add r,lo,hi,lsl#3;add r,r,hi,lsl#1 (shift pair) instead of mov #10;mla, diverging every digit pair.

## Prep draft

```c
/* func_020778c4: parse an ASCII-decimal date/time field into a packed int.
 * Each 2-byte pair is hi*10+lo-0x210 (ASCII '0'*11 bias). arg1==0x17 selects
 * a year branch (+0x76c if pair>=0x32 else +0x7d0); else high=pair0*100+pair1.
 * Result packs high<<16 | nextpair<<8 | lastpair (built with add). r0=p. */
int func_020778c4(unsigned char *p, int arg1) {
    int a, b, c, d;
    a = p[0] * 10 + p[1] - 0x210;
    p += 2;
    if (arg1 == 0x17) {
        if ((unsigned int)a >= 0x32) d = a + 0x76c;
        else d = a + 0x7d0;
    } else {
        b = p[0] * 10 + p[1] - 0x210;
        p += 2;
        d = a * 100 + b;
    }
    b = p[0] * 10 + p[1] - 0x210;
    c = p[2] * 10 + p[3] - 0x210;
    return (d << 0x10) + (b << 8) + c;
}
```

## Struct context

No struct fields accessed. Pure arithmetic on a `unsigned char *` byte stream; no globals.

| offset | field | type | use |
|--------|-------|------|-----|
| — | `p[0]` / `p[1]` | `unsigned char` | BCD digit pair: high*10 + low − 0x210 |
| — | `arg1` | `int` | selector: 0x17 → year branch (unsigned >= 0x32 cs/cc), else month/day |
| — | `d` | `int` | accumulated high word (year+offset or pair0*100+pair1) |

## Closest matched example

**Path:** src/main/func_0207e3bc.c
**Key lesson:** unsigned comparisons (`>= 0x32`) must use an unsigned cast on the left operand — `(unsigned int)a >= 0x32` — to force the `bcs`/`bcc` encoding rather than `bge`/`blt`. Without the cast, mwcc treats `a` as signed and emits the wrong branch polarity.

## Try this

1. The `mla` wall is the primary risk: `p[0] * 10 + p[1]` is the canonical C form — do NOT rewrite as `(p[0] << 3) + (p[0] << 1) + p[1]`, which guarantees the shift-pair divergence. Submit the prep draft first and check objdiff; if mwcc emits the shift-pair anyway the divergence is permuter-class.
2. Confirm in objdiff that the unsigned year branch emits `cmp rN, #0x32; bcs .label` (not `bge`). If signed branches appear, ensure the cast `(unsigned int)a >= 0x32` is present on the correct operand.
3. Park as permuter-class; the `mla` vs `add+add lsl` coin-flip is not forceable by C reshaping — the compiler decides based on internal cost model, not source form. Route to macOS permuter if available.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x0000076c` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

