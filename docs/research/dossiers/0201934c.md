# 0201934c — func_0201934c (main, class C, 224B)

## Recipe + Risk

**Recipe:** byte-table index then switch; cmp/movge override arms as if-assigns
**Risk:** reshape-able: arms use signed movge/movlt (so g0/g1 must be `int`), and the dispatch is two-tier (`bgt`/`bge` for the 0x3b boundary + `addls pc` for 0..7) — mwcc may pick a different jump-table/branch split; case-body order must follow source to lay the table ascending.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0201934c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     byte-table index then switch; cmp/movge override arms as if-assigns
 *   risk:       reshape-able: arms use signed movge/movlt (so g0/g1 must be `int`), and the dispatch is two-tier (`bgt`/`bge` for the 0x3b boundary + `addls pc` for 0..7) — mwcc may pick a different jump-table/branch split; case-body order must follow source to lay the table ascending.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0201934c (main, class C) — prep batch p_0004.
 * UNVERIFIED build-free draft. Drop into src/, run ninja+objdiff, tweak per risk.
 *   recipe: byte-table index then switch(key); each arm cmp/movge as if-assign returning override else unchanged key
 */
/* func_0201934c: in=r0. key = data_020be6c4[in] (ldrb). g0 = *(int*)(data_02104f4c+0x928),
 * g1 = *(int*)(data_02104f4c+0x92c) — both loaded up front and reused across arms. Returns key
 * UNCHANGED unless an arm overrides r0. Dispatch: switch(key) with a 0..7 jump table, plus
 * key==0x3b and key==0x49. Each override is `if (g0 >= K) r0 = V;` (movge = signed if-assign),
 * some chained (a second cmp g0,K2; cmpge g1,K3). 0x49 arm uses movlt (g0<0xa). 0x3b arm uses
 * cmpeq chains. Default arms (key 0,1,2,5,7 and miss) return key as-is. */
extern unsigned char data_020be6c4[];
extern char data_02104f4c[];

int func_0201934c(int in) {
    int r0 = data_020be6c4[in];
    int g0 = *(int *)(data_02104f4c + 0x928);
    int g1 = *(int *)(data_02104f4c + 0x92c);
    switch (r0) {
    case 3:
        if (g0 >= 0x24) r0 = 0x4a;
        break;
    case 4:
        if (g0 >= 0x29) r0 = 0x5e;
        break;
    case 6:
        if (g0 >= 0x18) r0 = 0x4b;
        if (g0 >= 0x17 && g1 >= 0x4) r0 = 0x4b;
        break;
    case 7:
        if (g0 >= 0x2c) r0 = 0x3f;
        if (g0 >= 0x2b && g1 >= 0x3) r0 = 0x3f;
        break;
    case 0x49:
        if (g0 < 0xa) r0 = 0x2d;
        if (g0 == 0xa) { if (g1 < 0x7) r0 = 0x2d; }
        break;
    case 0x3b:
        if (g0 == 0x21 && g1 == 0x3) r0 = 0x3c;
        if (g0 == 0x21 && g1 == 0x4) r0 = 0x3d;
        break;
    }
    return r0;
}
```

## Struct context

This function reads from a separate global `data_02104f4c` (not the main SystemWork singleton). The two fields accessed are:

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| `data_02104f4c+0x928` | g0 (progress/level counter?) | `int` | primary threshold guard across all switch arms (signed comparisons: `>=`, `<`, `==`) |
| `data_02104f4c+0x92c` | g1 (sub-counter?) | `int` | secondary threshold in chained cmp arms (cases 6, 7, 0x49, 0x3b) |

Note: the struct-context-main.md documents `+0x928` as "read as `data_02104f4c+0x928` by `func_0201934c.c` (a separate global, not SystemWork itself)." The pointer base `data_02104f4c` is distinct from the `GetSystemWork()` return value (`data_02104f58`).

## Closest matched example

**Path:** src/main/func_020196b0.c
**Why similar:** small function using a discarded `GetSystemWork()` call plus signed predicated returns — same genre of conditional-override pattern.
**Key lesson:** mwcc keeps an `extern` call even when the result is discarded; write the call as a plain statement. For the signed `movge`/`movlt` predicates, the variable holding the tested value must be `int`, not `unsigned int` — otherwise mwcc emits `bhs`/`blo` (unsigned) instead of `bge`/`blt` (signed), and the instruction encoding differs.

## Try this

1. Drop the prep draft as-is; verify that `g0` and `g1` are declared `int` (not `unsigned int`) — the movge/movlt predicated override arms require signed comparisons. Also verify that `r0` is declared `int` so the ldrb result is zero-extended then stored into a signed int register (the final `return r0` emits as-is).
2. If objdiff shows a different jump-table split (e.g. all cases in one `addls pc` block vs. the two-tier `bgt 0x3b / addls 0..7` shape), split the switch: handle cases 0..7 first with a dense switch, then test `0x3b` and `0x49` with explicit `if` chains below it; or reverse: test `>= 0x3b` first with `if (r0 > 0x49) ...` before the dense switch.
3. If the chained `cmpge` arms (case 6 double-if, case 7 double-if) still don't match after reordering, escalate to permuter — the two-step `cmp g0,K1; movge r0,V; cmp g0,K2; cmpge g1,K3; movge r0,V` sequence may need a specific variable scoping that only the permuter can find.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020be6c4` | data symbol |
| `_LIT1` | `data_02104f4c` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

