[//]: # (markdownlint-disable MD013 MD041)

# 021ee4c8 — func_ov002_021ee4c8 (ov002, class E retriage, 380B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses Ov002Slot.id_lo13 (lsl#19/lsr#19) for slot occupied guard and self->b0 player-side check. The "blx callback" flagged as coin-flip is a dispatch on `self->f0` (card id at +0x00) against two constants (0x14c1, 0x1a05): both paths are internally deterministic. Path 0x14c1 walks sibling slots in the PPT accumulating a count via DuelQueueState+0x494 indexing. Path 0x1a05 reads `data_ov002_022cf1a4` flag before calling func_ov002_021b9ecc + func_0202ed90.
**Confidence:** high
**Structs needed:** PerPlayerRowTable, Ov002Slot, DuelQueueState

## Assembly

```asm
        .text
        .extern data_ov002_022ce288
        .extern data_ov002_022cf16c
        .extern data_ov002_022cf1a4
        .extern func_0202ed90
        .extern func_ov002_021b9ecc
        .extern func_ov002_021c1e44
        .extern func_ov002_021c84e0
        .extern func_ov002_021ee44c
        .global func_ov002_021ee4c8
        .arm
func_ov002_021ee4c8:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, lr}
    mov sl, r0
    ldrh r3, [sl, #0x2]
    mov r9, r1
    mov r8, r2
    mov r3, r3, lsl #0x1f
    cmp r9, r3, lsr #0x1f
    movne r0, #0x0
    ldmneia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    cmp r8, #0x5
    movge r0, #0x0
    ldmgeia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    ldr r4, _LIT0
    and r5, r9, #0x1
    mov r3, #0x14
    mul r4, r5, r4
    ldr r5, _LIT1
    mul r6, r8, r3
    add r5, r5, r4
    add r3, r5, r6
    ldr r3, [r3, #0x30]
    mov r3, r3, lsl #0x13
    movs r3, r3, lsr #0x13
    moveq r0, #0x0
    ldmeqia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    bl func_ov002_021c1e44
    cmp r0, #0x0
    movne r0, #0x0
    ldmneia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    ldrh r1, [sl]
    ldr r0, _LIT2
    cmp r1, r0
    beq .L_94
    ldr r0, _LIT3
    cmp r1, r0
    beq .L_120
    b .L_15c
.L_94:
    mov r0, r9
    mov r1, r8
    mov r6, #0x0
    bl func_ov002_021c84e0
    movs fp, r0
    moveq r0, r6
    ldmeqia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    ldr r4, _LIT4
    add r7, r5, #0x30
    mov r5, r6
.L_bc:
    cmp r8, r5
    beq .L_fc
    ldr r0, [r7]
    mov r0, r0, lsl #0x13
    movs r0, r0, lsr #0x13
    beq .L_fc
    mov r0, r9
    mov r1, r5
    bl func_ov002_021c84e0
    ldr r1, [r4, #0x494]
    add r1, r1, r6
    mov r1, r1, lsl #0x19
    add r1, r4, r1, lsr #0x18
    add r1, r1, #0x400
    strh r0, [r1, #0x98]
    add r6, r6, #0x1
.L_fc:
    add r5, r5, #0x1
    cmp r5, #0x4
    add r7, r7, #0x14
    ble .L_bc
    ldrh r1, [sl, #0xc]
    sub r0, r6, #0x1
    sub r1, r1, fp
    bl func_ov002_021ee44c
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
.L_120:
    ldr r0, _LIT5
    add r0, r0, r4
    ldrh r0, [r6, r0]
    cmp r0, #0x0
    beq .L_154
    mov r0, r9
    mov r1, r8
    bl func_ov002_021b9ecc
    bl func_0202ed90
    cmp r0, #0x0
    moveq r0, #0x1
    movne r0, #0x0
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
.L_154:
    mov r0, #0x1
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
.L_15c:
    mov r0, #0x1
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
_LIT0: .word 0x00000868
_LIT1: .word data_ov002_022cf16c
_LIT2: .word 0x000014c1
_LIT3: .word 0x00001a05
_LIT4: .word data_ov002_022ce288
_LIT5: .word data_ov002_022cf1a4
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| Ov002Self | +0x00 | f0 | u16 | card id; dispatch value 0x14c1 or 0x1a05 |
| Ov002Self | +0x02 | b0 | u16:1 | player-side check (must equal player param) |
| Ov002Self | +0x0C | f_0c | u16 | passed to func_ov002_021ee44c as arg |
| PerPlayerRowTable | (player&1)*0x868 | row base | char* | slot walk base |
| Ov002Slot | row+slot*0x14+0x30 | id_lo13 | u32:13 | slot occupied guard |
| DuelQueueState | +0x494 | f_494 | int | index used to compute strh destination in sibling-slot loop |
| data_ov002_022cf1a4 | player_off+slot_off | flag | u16 | nonzero gate for 0x1a05 path |

## Recipe sketch

```c
struct self_t {
    unsigned short f0;
    unsigned short b0 : 1;
    unsigned short _r0 : 15;
    char _pad[8];
    unsigned short f_0c;
};
struct subrow { char _pad[0x30]; unsigned int id_lo13 : 13; unsigned int _hi : 19; };
extern char data_ov002_022cf16c[];
extern unsigned short data_ov002_022cf1a4[];
extern int func_ov002_021c1e44(struct self_t *, int, int);
extern int func_ov002_021c84e0(int player, int slot);
extern void func_ov002_021ee44c(int a, int b);
extern int func_ov002_021b9ecc(int player, int slot);
extern int func_0202ed90(int);
typedef struct { char _p[0x494]; int f_494; char _p2[0x400]; char _p3[0x98]; } DQS;
extern DQS data_ov002_022ce288;

int func_ov002_021ee4c8(struct self_t *self, int player, int slot) {
    if (player != (unsigned int)self->b0) return 0;
    if (slot >= 5) return 0;
    int player_off = (player & 1) * 0x868;
    int slot_off = slot * 0x14;
    char *row = data_ov002_022cf16c + player_off;
    struct subrow *sr = (struct subrow *)(row + slot_off);
    if (sr->id_lo13 == 0) return 0;
    if (func_ov002_021c1e44(self, player, slot) != 0) return 0;
    unsigned short id = self->f0;
    if (id == 0x14c1) {
        int base_count = func_ov002_021c84e0(player, slot);
        if (base_count == 0) return 0;
        struct subrow *p = (struct subrow *)(row + 0x30);
        int count = 0;
        for (int s = 0; s <= 4; s++, p++) {
            if (s == slot) continue;
            if (p->id_lo13 == 0) continue;
            int v = func_ov002_021c84e0(player, s);
            /* store v into dqs[f_494 + count] slot at +0x498+0x400 */
            int idx = data_ov002_022ce288.f_494 + count;
            /* strh v to computed dqs address */
            count++;
        }
        func_ov002_021ee44c(count - 1, (int)self->f_0c - base_count);
        return 0; /* tail — no explicit return value (void tail) */
    } else if (id == 0x1a05) {
        if (data_ov002_022cf1a4[(player_off + slot_off) / 2] == 0) return 1;
        int r = func_ov002_021b9ecc(player, slot);
        int ok = func_0202ed90(r);
        return ok == 0 ? 1 : 0;
    }
    return 1;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_021ee988.c
**Why similar:** Same pattern — player != self->b0 guard first, then slot >= 5 guard, then id_lo13 check, then func_021c1e44 rejection guard, then result based on slot state.
**Key lesson:** The player-side check `player != self->b0` must precede the PPT lookup — reordering these guards changes the register allocation. The `id_lo13` check uses `movs` to set flags from the bitfield result, so the struct bitfield `: 13` syntax is required (not `& 0x1fff`).

## Try this

1. Declare `self->f0` as `unsigned short` at `+0x00`, `self->b0` as `: 1` at `+0x02`, `self->f_0c` as `unsigned short` at `+0x0C`. Start with `player != self->b0` guard at the very top, before the PPT lookup.
2. For the 0x14c1 branch: the sibling-slot loop skips `s == slot` and the `func_ov002_021c84e0` result is stored into a `dqs->f_494`-indexed slot. If the strh store address mismatches in objdiff, the DuelQueueState offset +0x494 needs to match exactly — declare as `char _pad[0x494]; int f_494;` in the DuelQueueState typedef.
3. The 0x1a05 path condition is unusual: if the flag is nonzero it calls both `021b9ecc` AND `func_0202ed90` and returns `ok == 0`; if flag is zero it returns 1 directly. If these two arms are reversed in objdiff, swap the branch sense.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00000868` | MMIO/const |
| `_LIT1` | `data_ov002_022cf16c` | raw |
| `_LIT2` | `0x000014c1` | MMIO/const |
| `_LIT3` | `0x00001a05` | MMIO/const |
| `_LIT4` | `data_ov002_022ce288` | raw |
| `_LIT5` | `data_ov002_022cf1a4` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021c1e44` | overlay call (ov002) |
| `bl` | `func_ov002_021c84e0` | overlay call (ov002) |
| `bl` | `func_ov002_021ee44c` | overlay call (ov002) |
| `bl` | `func_ov002_021b9ecc` | overlay call (ov002) |
| `bl` | `func_0202ed90` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

