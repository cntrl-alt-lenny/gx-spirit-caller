# 0208b1e0 — func_0208b1e0 (main, class D, 196B)

## Recipe + Risk
**Recipe:** D: 9 unrolled (s64)in[i]*scale>>12; 3 scale groups r2/r3/5th-arg
**Risk:** 5th arg (s2) must land via [sp,#8] not a 5th reg; mwcc's >>12 is smull;lsr#0xc;orr lsl#0x14 - solid. permuter-class only if scratch reg rotation (lr/r4) reorders. reshape-able.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208b1e0 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: 9 unrolled (s64)in[i]*scale>>12; 3 scale groups r2/r3/5th-arg
 *   risk:       5th arg (s2) must land via [sp,#8] not a 5th reg; mwcc's >>12 is smull;lsr#0xc;orr lsl#0x14 - solid. permuter-class only if scratch reg rotation (lr/r4) reorders. reshape-able.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0208b1e0 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. recipe: D divmod (fixed-point >>12 via
 * (s64)x*s) over 9 unrolled elements; 3 scale groups (r2/r3/[sp+8]).
 * func(in=r0, out=r1, s0=r2, s1=r3, s2=5th-arg). out[i]=(int)(((s64)in[i]*s)>>12).
 * Elems 0..2 use s0, 3..5 use s1, 6..8 use s2. Likely a 3x3 / 3-vec3 scale.
 */

void func_0208b1e0(int *in, int *out, int s0, int s1, int s2)
{
    out[0] = (int)(((long long)in[0] * s0) >> 12);
    out[1] = (int)(((long long)in[1] * s0) >> 12);
    out[2] = (int)(((long long)in[2] * s0) >> 12);
    out[3] = (int)(((long long)in[3] * s1) >> 12);
    out[4] = (int)(((long long)in[4] * s1) >> 12);
    out[5] = (int)(((long long)in[5] * s1) >> 12);
    out[6] = (int)(((long long)in[6] * s2) >> 12);
    out[7] = (int)(((long long)in[7] * s2) >> 12);
    out[8] = (int)(((long long)in[8] * s2) >> 12);
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| in[0..2] | r0+0..+8 | int[] | group 0, multiplied by s0 (r2) |
| in[3..5] | r0+0xc..+0x14 | int[] | group 1, multiplied by s1 (r3) |
| in[6..8] | r0+0x18..+0x20 | int[] | group 2, multiplied by s2 (5th arg, [sp,#8]) |
| out[0..8] | r1+0..+0x20 | int[] | destination, written in ascending order |

The 5th argument `s2` must be declared as a normal C parameter (not a register variable) so that mwcc reads it from `[sp,#8]` — on ARM AAPCS, 5 int-size args place arg5 at `sp+0` in the callee's view; mwcc with 4 register-allocated args puts the 5th at `[sp,#8]` in the caller's frame. Declaring `s2` as a plain 5th parameter is sufficient.

## Closest matched example
**Path:** src/main/func_0208b108.c (sibling in this batch)
**Key lesson:** The `(int)(((long long)in[i] * s) >> 12)` expression reliably emits `smull lo,hi,in[i],s; lsr lo,#0xc; orr lo,lo,hi,lsl#0x14` — do not rearrange as `(int)(((long long)s * in[i]) >> 12)` because operand order in smull tracks source order. The 9-element unrolled body extends the 4-lane sibling pattern across three scale-group boundaries; no loop is needed or desired.

## Try this
1. Declare `s2` as the 5th named parameter (no special annotation needed); if objdiff shows `s2` loaded from `[sp,#0]` instead of `[sp,#8]`, add a dummy `int _unused` 5th param before `s2` to shift its stack slot, then revert — the issue is likely a missing parameter in the caller frame.
2. If the scratch register used for the smull hi-word (lr vs r4) rotates between groups in objdiff, add explicit `int tmp0 = ..., tmp1 = ..., tmp2 = ...` group-local temps to give the compiler three separate smull targets.
3. Park as permuter-class if the scratch-register rotation between the three groups (lr/r4/ip assignments) does not stabilize after the temp-binding reshape — this is a scheduling coin-flip that cannot be forced by C alone.
