# 02259160 — func_ov002_02259160 (ov002, class F retriage, 376B)

## Re-triage
**Original verdict:** F — previously intractable (high confidence)
**Upgrade reason:** Accesses DuelQueueState, PerPlayerRowTable (stride 0x868), and Ov002Slot id_lo13. The function is embedded within block func_ov002_02259124 (EUR delink 0x02259124–0x022591E4, size=0xC0). However, the retriage doc lists it as 376 bytes — significantly larger than the 192-byte containing block. This means the function spans beyond the current EUR delink boundary and requires re-delink to extract. The surrounding func_ov002_02259124.s accesses `data_ov002_022cf178` (PerPlayerRowTable companion) and `data_ov002_022d016c` (DSS), confirming overlapping struct context.
**Confidence:** high
**Structs needed:** DuelQueueState (+0x5A8 queue_state, +0x5C0 re-entrancy lock), PerPlayerRowTable (base 0x022CF16C, stride 0x868, companion data_ov002_022cf178), Ov002Slot (id_lo13 lsl#19/lsr#19)

## Assembly

```asm
; NOTE: func_ov002_02259160 is NOT a standalone .s file in EUR.
; It begins at offset +0x3C within block func_ov002_02259124 (size=0xC0)
; and extends 376 bytes beyond — spanning multiple EUR delink boundaries.
; EUR delink range: 0x02259124–0x022591E4 (only 192 bytes).
; The full function occupies 0x02259160–0x022592D8 and crosses at least one
; additional delink segment.
; Containing block externs (from func_ov002_02259124.s):
;   data_ov002_022cf178  (PerPlayerRowTable companion, stride 0x868)
;   data_ov002_022d016c  (DuelStateSingleton)
;   func_ov002_021fbf74, func_ov002_021fc0c8, func_ov002_022591e4
; Full assembly requires extraction from the binary at 0x02259160.
; See src/overlay002/func_ov002_02259124.s for partial context.
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state guard (0x7f/0x80) |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock |
| PerPlayerRowTable | base+(player&1)*0x868 | row | ptr | per-player base; companion at data_ov002_022cf178 |
| Ov002Slot | row+0x30+s*0x14 (+0x00) | id_lo13 | u32:13 | lsl#19/lsr#19 bitfield |

## Recipe sketch

```c
/* F-class 376-byte: DuelQueueState gate + PerPlayerRowTable slot walk.
 * func_ov002_02259124.s (the containing block) handles a switch on
 * (r2-0xb) with cases at 0xb(+0), 0xf(+4..+7), and a default via
 * func_ov002_022591e4. This suggests 02259160 is the default/fallthrough
 * path of a larger dispatch that begins at 02259124. */
int func_ov002_02259160(int player, int zone, int r2, void *ctx) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    if (dqs->f_5c0) return 0;          /* +0x5C0 lock */
    int qs = dqs->f_5a8;               /* +0x5A8 */
    if (qs != 0x7f) return 0;
    /* PerPlayerRowTable walk */
    char *base = (char *)data_ov002_022cf16c + (player & 1) * 0x868;
    int count = *(int *)(base);         /* zone_count */
    for (int s = 0; s < count && s <= 4; s++) {
        unsigned int word = *(unsigned int *)(base + 0x30 + s * 0x14);
        int id = (int)(word << 19) >> 19;  /* id_lo13 */
        if (id == 0) continue;
        /* check companion data_ov002_022cf178 for zone threshold */
        if (*(int *)((char *)data_ov002_022cf178 + (player & 1) * 0x868)
            < zone) continue;
        func_ov002_021fc0c8(player, id, ctx);
    }
    return 1;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_02257750.c (retriage doc recipe)
**Why similar:** Same PerPlayerRowTable stride-0x868 walk with callback pattern and bitmask accumulate; class E, high.
**Key lesson:** func_ov002_02259124 (the surrounding block) is itself a dispatch that calls func_ov002_022591e4 for the default case — 02259160 may be the body of one of those dispatch arms. Reconstruct the dispatch first, then write the body separately.

## Try this
1. This function cannot be matched until it is delinked as a standalone .s file. Run `batch_carve.py --min-addr 0x02259160` or re-run `dsd delink` on the updated config after adding `func_ov002_02259160 kind:function(arm,size=0x178)` to symbols.txt.
2. Once delinked, load `dqs->f_5c0` (`+0x5C0`) first, then `dqs->f_5a8` (`+0x5A8`). The containing block's switch on `(r2 - 0xb)` confirms this function takes a zone/mode argument in r2.
3. If the PerPlayerRowTable walk produces mismatched scheduling, bind the row base to a local pointer before the loop and use `(player & 1)` not `player` directly in the stride multiply.
