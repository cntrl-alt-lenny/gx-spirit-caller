# 0203953c — func_0203953c (main, class C, 340B)

## Recipe + Risk
**Recipe:** flag-bit setup r7/r6 via nested if; struct-base bind node(r5); ordered byte/word stores 14,8,1c,18,1f,1e,c,1d
**Risk:** struct-guessed and TRUNCATED asm (body ends mid-store at .L_9c0). func_02038c84 2nd arg is r4+0x20 (sub-obj base) — placeholder wrong; offsets and tail-call unknown. confirm against real struct + full asm

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0203953c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     flag-bit setup r7/r6 via nested if; struct-base bind node(r5); ordered byte/word stores 14,8,1c,18,1f,1e,c,1d
 *   risk:       struct-guessed and TRUNCATED asm (body ends mid-store at .L_9c0). func_02038c84 2nd arg is r4+0x20 (sub-obj base) — placeholder wrong; offsets and tail-call unknown. confirm against real struct + full asm
 *   confidence: low
 */
#include <stdio.h>

typedef unsigned char u8;
typedef unsigned short u16;
typedef unsigned int u32;
typedef int s32;

struct Node_953c {
    u8 _pad0[0x8];
    u32 field_8;       /* +0x8 = 0 */
    u8 _padc[0xc - 0xc];
    u32 field_c;       /* +0xc <- r4 (sub-obj) */
    u8 _pad10[0x14 - 0x10];
    void *field_14;    /* +0x14 <- sl */
    u32 field_18;      /* +0x18 = 0 */
    u8 _pad1c0;        /* +0x1c */
    u8 field_1d;       /* +0x1d */
    u8 field_1e;       /* +0x1e <- r9 */
    u8 field_1f;       /* +0x1f <- r7 */
    u8 sub20[0x20];    /* +0x20 */
};

struct Cfg_9c480 { u8 _pad0[4]; u16 field_4; };
extern struct Cfg_9c480 data_0219c480;
extern u32 data_0219b330;
extern u32 data_0219c494;

extern int func_02089024(int a0);
extern struct Node_953c *func_0203a84c(int a0);
extern int func_020890e4(int a0);
extern struct Node_953c *func_02038c84(u32 g, void *sub, int sz);
extern int func_02038e58(int z0, int a0, void *sub, int r4);

int func_0203953c(int sl, int r9, int r8) {
    int r7, r6;
    struct Node_953c *node;
    int sub;

    if (data_0219c480.field_4 != 0) {
        return func_02089024(sl);
    }

    if (r8 & 0x400) {
        r7 = 2;
        r6 = -1;
        if (r8 & 0x200) r7 = 1;
    } else {
        r7 = 0;
        r6 = 1;
    }

    node = func_0203a84c(sl);
    if (node != 0) {
        ((u8 *)node)[0x1c]++;
        return 1;
    }

    sub = func_020890e4(sl);
    if (sub == 0) return 0;

    node = func_02038c84(data_0219b330, (u8 *)0 + 0 + ((char *)0 - (char *)0), r6 << 5);
    /* func_02038c84(global, sub+0x20-ish, r6<<5) */
    if (node == 0) return 0;

    node->field_14 = (void *)(long)sl;
    node->field_8 = 0;
    ((u8 *)node)[0x1c] = 1;
    node->field_18 = 0;
    node->field_1f = (u8)r7;
    node->field_1e = (u8)r9;
    node->field_c = (u32)sub;
    node->field_1d = 2;

    if (r8 & 0x8000) {
        if (sub != func_02038e58(0, sl, &node->sub20, sub)) return 0;
        node->field_1d = 1;
    }
    return 0;
}
```

## Struct context
This function accesses `data_0219c480` (shared global manager, field_4 as u16 guard) and `data_0219b330` (global handle passed to func_02038c84). No GameSingleton or SystemWork access. The `Node_953c` struct layout is inferred:

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x8 | field_8 | u32 | written 0 after node allocation |
| +0xc | field_c | u32 | written with `sub` value |
| +0x14 | field_14 | void* | written with cast of `sl` (int) |
| +0x18 | field_18 | u32 | written 0 |
| +0x1c | (byte) | u8 | incremented in fast-path; written 1 in slow-path |
| +0x1d | field_1d | u8 | written 2 normally, 1 in tail branch |
| +0x1e | field_1e | u8 | written with r9 (second arg) |
| +0x1f | field_1f | u8 | written with r7 (flag from nested-if) |
| +0x20 | sub20[0x20] | u8[0x20] | address &node->sub20 passed to func_02038e58 |

**WARNING:** The prep draft has a placeholder for the second arg to func_02038c84 (`(u8 *)0 + ...`). The real arg is `(void *)(sub + 0x20)` or similar — this MUST be fixed against the real asm before attempting to match.

**Near-sibling:** func_02039690 (same batch) uses the same store pattern (14,8,1c,18,1f,1e,c,1d) with an mla stride-0xc index instead of the nested-if flag setup. Both are truncated-asm low-confidence candidates.

## Closest matched example
**Path:** src/main/Task_InvokeLocked.c
**Why similar:** Both set up flag values via nested conditionals before binding a node pointer and writing multiple fields in a specific order.
**Key lesson:** The ordered byte/word store sequence (14, 8, 1c, 18, 1f, 1e, c, 1d) is the primary matching constraint — mwcc preserves source-declaration order for struct field stores when the pointer is pinned in a callee-saved register. Declare the stores in exactly that source order and bind `node` to a named local pointer (r5 in orig). The nested-if for r7/r6 must be written as nested `if` statements (not a ternary chain) to emit the `movne/moveq` or `movgt` sequence for the two flag values.

## Try this
1. Fix the placeholder second arg to func_02038c84 — read the full disassembly to determine the correct sub+0x20 expression; the current draft emits an invalid zero pointer.
2. Verify the store order in objdiff matches 14, 8, 1c, 18, 1f, 1e, c, 1d; if any pair is swapped, reorder the corresponding source statements to match.
3. If the func_02038c84 arg or the truncated tail cannot be reconstructed from the available asm fragment, park as low-confidence / needs-full-asm and escalate to a human reviewer.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219c480` | data symbol |
| `_LIT1` | `data_0219b330` | data symbol |
| `_LIT2` | `data_0219c494` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02089024` | main TU call |
| `bl` | `func_0203a84c` | main TU call |
| `bl` | `func_020890e4` | main TU call |
| `bl` | `func_02038c84` | main TU call |
| `bl` | `func_02038e58` | main TU call |
| `bl` | `func_02089008` | main TU call |
| `bl` | `func_0207d12c` | main TU call |
| `bl` | `func_02038fb0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

