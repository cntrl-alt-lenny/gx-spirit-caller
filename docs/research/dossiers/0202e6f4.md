# 0202e6f4 — func_0202e6f4 (main, class D, 168B)

## Recipe + Risk
**Recipe:** sparse membership switch->0/1; all-points binary-search compare tree
**Risk:** 9 sparse points; mwcc must reproduce the exact pivot ordering (0x1656 then 0x1531) and adjacent-pair equality folds (0x143f/0x1440, 0x1685/0x1686) — compiler-internal tree shape (permuter-class)

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0202e6f4 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     sparse membership switch->0/1; all-points binary-search compare tree
 *   risk:       9 sparse points; mwcc must reproduce the exact pivot ordering (0x1656 then 0x1531) and adjacent-pair equality folds (0x143f/0x1440, 0x1685/0x1686) — compiler-internal tree shape (permuter-class)
 *   confidence: med
 */
/* func_0202e6f4: sparse membership test on r0, returns 0/1. mwcc lowers to a
 * binary-search compare tree (pivots 0x1656, 0x1531). All members are isolated
 * points / tiny adjacent pairs (no jump-table run). Member set decoded:
 *   {0x143f,0x1440, 0x1531, 0x161e, 0x1656, 0x1685,0x1686, 0x169e, 0x16a3}. */
int func_0202e6f4(int id) {
    switch (id) {
    case 0x143f:
    case 0x1440:
    case 0x1531:
    case 0x161e:
    case 0x1656:
    case 0x1685:
    case 0x1686:
    case 0x169e:
    case 0x16a3:
        return 1;
    default:
        return 0;
    }
}
```

## Struct context
Pure card-ID membership predicate — no struct field accesses.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| — | — | — | no struct accesses |

## Closest matched example
**Path:** src/main/func_0202effc.c
**Why similar:** Same class-D sparse switch->membership pattern with pure binary-search compare tree and no jump-table run.
**Key lesson:** The adjacent pairs (0x143f/0x1440 and 0x1685/0x1686) are folded by mwcc into `addne r1,rN,#1; cmpne r0,r1` sequences rather than two separate literal loads. The tree root pivots (0x1656 then 0x1531) are the specific binary-search midpoints mwcc's balancer chose; if it picks a different root the entire cmp sequence differs. This is firmly permuter-class — no C-level rewrite changes which pivot the compiler selects.

## Try this
1. Drop the prep draft as-is. Verify objdiff for the first pivot: it should open with `ldr r1,=0x1656; cmp r0,r1; bgt .Lupper` or similar. The adjacent-pair fold for 0x143f/0x1440 should appear as `addne r1,rN,#1; cmpne r0,r1`.
2. If the tree root differs (e.g., mwcc picks 0x161e instead of 0x1656): there is no C reshape that controls pivot selection. Check whether reordering case labels source-wise changes anything (it should not for a switch — cases are unordered by spec).
3. Park as permuter-class; binary-search tree pivot selection is compiler-internal and un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001656` | MMIO/const |
| `_LIT1` | `0x00001531` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

