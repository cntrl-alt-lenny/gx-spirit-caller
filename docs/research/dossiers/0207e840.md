# 0207e840 — func_0207e840 (main, class C, 96B)

## Recipe + Risk
**Recipe:** decl s inside each branch (avoid hoisting f18); eor operand-order (f10>0)^f4; accumulator-first add c+(h<<3)
**Risk:** orig reloads p->f18 once per branch into a reused reg; if mwcc hoists the load above the if, two ldr diverge. reshape-able: keep the local scoped inside each branch (done).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0207e840 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     decl s inside each branch (avoid hoisting f18); eor operand-order (f10>0)^f4; accumulator-first add c+(h<<3)
 *   risk:       orig reloads p->f18 once per branch into a reused reg; if mwcc hoists the load above the if, two ldr diverge. reshape-able: keep the local scoped inside each branch (done).
 *   confidence: med
 */
/* func_0207e840: eor-predicate select of element base, tail-call. */
typedef struct {
    unsigned short h0;   /* 0x0 */
    unsigned short h2;   /* 0x2 */
    char _pad04[0x8];    /* 0x4..0xb */
    int c;               /* 0xc */
} sub_t;

typedef struct {
    int   f0;            /* 0x0 */
    int   f4;            /* 0x4 */
    char  _pad08[0x4];   /* 0x8 */
    int   fc;            /* 0xc */
    int   f10;           /* 0x10 */
    char  _pad14[0x4];   /* 0x14 */
    sub_t *f18;          /* 0x18 */
} obj_t;

extern int func_0207e92c(obj_t *p);

int func_0207e840(obj_t *p) {
    sub_t *s;
    if ((p->f10 > 0) ^ p->f4) {
        s = p->f18;
        p->f0 = s->c + (s->h2 << 3);
    } else {
        s = p->f18;
        p->f0 = s->c + (s->h0 << 3) - 8;
    }
    p->fc = 0;
    return func_0207e92c(p);
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| +0x00 | f0 | int | written with computed value c+(h<<3) or c+(h<<3)-8 |
| +0x04 | f4 | int | second operand of `eor`; read as boolean flag |
| +0x0c | fc | int | zeroed before tail-call |
| +0x10 | f10 | int | first operand of `eor`; tested as `> 0` (signed) |
| +0x18 | f18 | sub_t* | pointer to sub-struct; loaded once per branch arm |
| sub+0x00 | h0 | unsigned short | used in else arm: `c + (h0<<3) - 8` |
| sub+0x02 | h2 | unsigned short | used in if arm: `c + (h2<<3)` |
| sub+0x0c | c | int | base accumulator for both arms |

## Closest matched example
**Path:** src/main/func_0207e8f4.c
**Key lesson:** `func_0207e8f4` is a matched sibling that accesses `p->f_18` (same offset) as a pointer and computes a shift-pair on the loaded value. It confirms that f18 holds a sub-struct pointer at offset +0x18 of obj_t, and that mwcc keeps the deref in a dedicated register per use. The recipe's "decl s inside each branch" idiom forces mwcc to emit a fresh `ldr` of f18 in each arm rather than hoisting it above the `eor` test.

## Try this
1. Keep `sub_t *s` declared as a local variable inside each branch arm (as shown in the prep draft) — do NOT hoist it above the `if`. This forces mwcc to emit one `ldr p->f18` per branch arm, matching the orig's two separate loads.
2. Verify that the `eor` condition is written as `(p->f10 > 0) ^ p->f4` (not `!= 0` for either operand) — mwcc's EOR codegen requires this exact boolean-xor form so it emits `cmp f10, #0; movgt r_, #1; eor r_, r_, f4`.
3. Check objdiff: if mwcc hoists the `ldr p->f18` above the `if`, restructure with a dummy conditional guard or move the declaration textually deeper into each arm via braces.
