# 0208e890 — func_0208e890 (main, class C, 136B)

## Recipe + Risk
**Recipe:** volatile spin-wait (32b) then 16b RMW pair; store-order verbatim; 0x1f0080 from literal
**Risk:** store-order/scheduling: the three tail stores must stay in a4,a8,ac order (mwcc shares r2=0 for a8/ac). reshape-able via source order; the sub-sp,#4 frame around the call is mwcc default.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208e890 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     volatile spin-wait (32b) then 16b RMW pair; store-order verbatim; 0x1f0080 from literal
 *   risk:       store-order/scheduling: the three tail stores must stay in a4,a8,ac order (mwcc shares r2=0 for a8/ac). reshape-able via source order; the sub-sp,#4 frame around the call is mwcc default.
 *   confidence: med
 */
/* func_0208e890 (main, class C) — VBLANK-ish spin-wait + MMIO writes + call + ordered stores.
 * while (*0x4000600 & 0x8000000) ;   (32-bit read)
 * *0x4000600 |= 0x8000;
 * *(u16*)0x4000060 |= 0x2000;  *(u16*)0x4000060 |= 0x1000;   (two 16-bit RMW)
 * func_0208e700();
 * 0x40004a4 = 0x001f0080; 0x40004a8 = 0; 0x40004ac = 0;   (ORDER load-bearing). */
typedef unsigned short u16;
typedef unsigned int   u32;

extern void func_0208e700(void);

void func_0208e890(void)
{
    while (*(volatile u32 *)0x04000600 & 0x08000000)
        ;

    *(volatile u32 *)0x04000600 |= 0x8000;
    *(volatile u16 *)0x04000060 |= 0x2000;
    *(volatile u16 *)0x04000060 |= 0x1000;

    func_0208e700();

    *(volatile u32 *)0x040004a4 = 0x001f0080;
    *(volatile u32 *)0x040004a8 = 0;
    *(volatile u32 *)0x040004ac = 0;
}
```

## Struct context
No struct globals; all access is direct MMIO.

| Address | Register | Access |
|---------|----------|--------|
| `0x04000600` | GXSTAT/state register | spin-wait on bit 27, then RMW or-in 0x8000 |
| `0x04000060` | Sound/PWM control register | two sequential 16-bit RMW: or-in 0x2000, then or-in 0x1000 |
| `0x040004a4` | Register a4 | store literal 0x001f0080 (first of three tail stores) |
| `0x040004a8` | Register a8 | store 0 (second tail store) |
| `0x040004ac` | Register ac | store 0 (third tail store) |

## Closest matched example
**Path:** src/main/func_0208e700.c (prep draft — callee)
**Key lesson:** `func_0208e700()` is the direct callee here. The lesson from that function is that store order is load-bearing and all writes must be `volatile`. For this function, the spin-wait uses a 32-bit read testing bit 27 (`0x08000000`) — not bit 15 (`0x8000`) — which is a different mask from the write that follows. The two 16-bit RMW to `0x04000060` must be sequential (each is a separate ldrh+orr+strh) in the declared order: 0x2000 first, then 0x1000.

## Try this
1. Confirm the spin-wait mask: `*(volatile u32 *)0x04000600 & 0x08000000` (bit 27). If objdiff shows `& 0x8000` (bit 15) in the loop condition, the mask is wrong — orig tests bit 27 in the poll, not bit 15 which is the write mask used after the loop.
2. Confirm the two 16-bit RMW sequence: `*(volatile u16 *)0x04000060 |= 0x2000` then `*(volatile u16 *)0x04000060 |= 0x1000` — both must be separate statements so mwcc emits two independent ldrh+orr+strh pairs. If objdiff combines them into `|= 0x3000`, split them.
3. Confirm the three tail stores in order: `0x40004a4 = 0x001f0080`, then `0x40004a8 = 0`, then `0x40004ac = 0`. The constant 0x001f0080 must come from a pool literal (ldr from .pool), not constructed by mov+add. If the constant is computed at runtime, declare it as `(u32)0x001f0080` explicitly.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x04000600` | MMIO/const |
| `_LIT1` | `0x04000060` | MMIO/const |
| `_LIT2` | `0x001f0080` | MMIO/const |
| `_LIT3` | `0x040004a4` | MMIO/const |
| `_LIT4` | `0x040004a8` | MMIO/const |
| `_LIT5` | `0x040004ac` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208e700` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

