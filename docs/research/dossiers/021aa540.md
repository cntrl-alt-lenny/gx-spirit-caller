# 021aa540 — func_ov022_021aa540 (ov020, class D, 320B)

## Recipe + Risk
**Recipe:** store-order cmd[0x14]/[0x18]; bic|orr mask-insert; switch case-pairs; mla stride-0x20
**Risk:** reshape-able: orig zero-extends state (lsl#16;lsr#16) before `and #0xf` at cmd[0x14] insert; declare `state` as `unsigned short` param to emit the pair. Tail (enable arm) struct-guessed: 02006e1c/020944a4 order inferred from .extern set.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021aa540 (ov022, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store-order cmd[0x14]/[0x18]; bic|orr mask-insert; switch case-pairs; if-not ternary fields
 *   risk:       reshape-able: orig zero-extends state (lsl#16;lsr#16) before `and #0xf` at cmd[0x14] insert; declare `state` as `unsigned short` param to emit the pair. Tail (enable arm) struct-guessed: 02006e1c/020944a4 order inferred from .extern set.
 *   confidence: low
 */
/* func_ov022_021aa540: build+commit a BgCfg-style command record for `state`,
 * then conditionally query/release the handle when `enable`. 0201d47c/0201e5b8
 * command-record family (cf. JPN twin 021c11f0). Tail after 0201d760 inferred
 * from the .extern set (02006e1c, 020944a4 must be called in the enable arm). */
extern int  data_ov022_021ab9a0[];
extern int  func_02006c0c(int id, int b, int c);
extern void func_0201d47c(void *cmd);
extern void func_0201e5b8(void *cmd);
extern int  func_0201d6d4(int h);
extern void func_0201d760(int h);
extern int  func_02006e1c(int h);
extern void func_020944a4(int a);

int func_ov022_021aa540(int index, int state, int flag, int enable) {
    char cmd[40];
    int h;
    h = func_02006c0c(data_ov022_021ab9a0[index], 4, 0);
    func_0201d47c(cmd);
    *(unsigned short *)(cmd + 0x14) =
        (*(unsigned short *)(cmd + 0x14) & ~0xf) | ((unsigned short)state & 0xf);
    switch (state) {
    case 1:
    case 6:
        *(int *)(cmd + 0x18) =
            (*(int *)(cmd + 0x18) & ~0x1c000) | ((flag ? 2 : 0) << 14);
        break;
    case 2:
    case 7:
        *(int *)(cmd + 0x18) =
            (*(int *)(cmd + 0x18) & ~0x1c000) | ((flag ? 3 : 1) << 14);
        break;
    case 3:
    case 8:
        *(int *)(cmd + 0x18) = (*(int *)(cmd + 0x18) & ~0x1c000) | 0x8000;
        break;
    case 4:
        *(int *)(cmd + 0x18) = (*(int *)(cmd + 0x18) & ~0x1c000) | 0xc000;
        break;
    }
    *(int *)(cmd + 0) = h;
    *(unsigned short *)(cmd + 0x14) |= 0x10;
    func_0201e5b8(cmd);
    if (enable) {
        flag = func_0201d6d4(h);
        func_0201d760(h);
        func_020944a4(func_02006e1c(h));
    }
    return h;
}
```

## Struct context
`cmd` is a 40-byte (`0x28`) stack buffer matching the `BgCfg` layout from `docs/research/types/BgCfg.md`.

| field | offset | type | purpose |
|-------|--------|------|---------|
| cmd+0x00 | f0 | int | handle h written after switch |
| cmd+0x14 | f14 | unsigned short | priority nibble + 0x10 enable bit |
| cmd+0x18 | f18 | int | tile-base bits [16:14] via bic/orr |

`data_ov022_021ab9a0` is the archive-entry index array; `index` selects which entry to open via `func_02006c0c`. The matched sibling `func_ov020_021aab7c` uses the identical `func_02006c0c` + `func_0201d47c` + `func_0201e5b8` call sequence.

## Closest matched example
**Path:** `src/overlay020/func_ov020_021aab7c.c`
**Why similar:** exact same BgCfg command-record pattern: `func_02006c0c` → `func_0201d47c` → field RMW at f14/f18 → `f0 = h` → `f14 |= 0x10` → `func_0201e5b8`.
**Key lesson from that file:** The `*(unsigned short *)(cmd + 0x14)` zero-extends the 4-bit field; the store-order is f14 bic/orr FIRST, then f18 bic/orr in the switch, then `cmd+0 = h`, then `f14 |= 0x10`, then `func_0201e5b8` — do not reorder f0 before f14.

## Try this
1. The state zero-extend at cmd+0x14: write `(unsigned short)state & 0xf` — this forces MWCC to emit `lsl#10/lsr#10` on the `state` parameter before the `and #0xf`; without the `(unsigned short)` cast mwcc may skip the zero-extend pair.
2. In objdiff, if case-pairs (1/6, 2/7, 3/8) fall through incorrectly: ensure `case 1: case 6:` share a single body with `break` — MWCC should emit a branch-table that routes both to the same arm; if they're separated, combine them.
3. If the enable arm order (`func_0201d6d4` → `func_0201d760` → `func_020944a4(func_02006e1c(h))`) diverges, the tail is struct-guessed — swap `func_0201d6d4` result capture vs discard and check whether `flag` is reused; escalate to permuter if still wrong.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov022_021ab9a0` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02006c0c` | main TU call |
| `bl` | `func_0201d47c` | main TU call |
| `bl` | `func_0201e5b8` | main TU call |
| `bl` | `func_0201d6d4` | main TU call |
| `bl` | `func_0201d760` | main TU call |
| `bl` | `func_020944a4` | main TU call |
| `bl` | `Task_Invoke` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

