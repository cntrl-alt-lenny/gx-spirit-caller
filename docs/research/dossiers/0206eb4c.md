# 0206eb4c — func_0206eb4c (main, class D, 156B)

## Recipe + Risk

**Recipe:** (u16) cast => lsl#16;lsr#16; bswap16 of v + bswap32 of r computed together; byte/half/word store-order
**Risk:** Orig computes the v-bswap16 and r-bswap32 INTERLEAVED across r5/lr/r0 (two swaps live at once) and pins the (u16) lsl;lsr — register allocation here is a coin-flip. permuter-class. Also the redundant 'a!=0' re-guard (cmp r5,#0) after the early null-return is intentional; keep both guards.

## Prep draft

```c
/* func_0206eb4c (main, class D) — (u16) lsl;lsr truncation + bswap16/bswap32, store-order. */
typedef unsigned char  u8;
typedef unsigned short u16;
typedef unsigned long  u32;

extern u32 func_0206e174(void *a);

int func_0206eb4c(void *a, void *dst)
{
    char *p = (char *)a;
    char *d = (char *)dst;
    u32   r;
    u16   v;

    if (a == 0) {
        return -0x27;
    }
    r = func_0206e174(p);
    v = (u16)((a != 0 && r != 0) ? *(u16 *)(p + 0x74) : 0);

    *(u8 *)(d + 0x0) = 0x8;
    *(u8 *)(d + 0x1) = 0x2;
    *(u16 *)(d + 0x2) = (u16)(((v >> 8) & 0xff) | ((v << 8) & 0xff00));
    *(u32 *)(d + 0x4) = ((r >> 24) & 0xff) | ((r >> 8) & 0xff00) |
                        ((r << 8) & 0xff0000) | (r << 24);
    return 0;
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `p + 0x74` | u16 payload | `u16 *` | source for bswap16 into `v`; loaded conditionally when a!=0 && r!=0 |
| `d + 0x0` | tag byte | `u8` | header byte 0 = 0x8 |
| `d + 0x1` | len byte | `u8` | header byte 1 = 0x2 |
| `d + 0x2` | bswapped u16 | `u16` | bswap16 of `v` = `(v>>8)|(v<<8)` |
| `d + 0x4` | bswapped u32 | `u32` | bswap32 of `r` |

## Closest matched example

**Path:** src/main/Task_PostLocked.c
**Key lesson:** The `(u16)` cast in the store forces `lsl#16;lsr#16` in the caller (same as Task_PostLocked's `arg2` zero-extension pattern). Two separate local variables holding the two bswap results must remain live simultaneously — do not collapse them into one expression, as the interleaved register use is exactly what forces the permuter-class allocation. The redundant double-guard `(a != 0 && r != 0)` after the initial `if (a == 0) return` is intentional and must be preserved verbatim.

## Try this

1. Preserve both guards as written: the outer `if (a == 0) return -0x27;` AND the inner `(a != 0 && r != 0)` ternary condition — removing the second guard eliminates the `cmp r5,#0` reload the orig emits.
2. Run objdiff and check whether the interleaved bswap16/bswap32 register live-ranges match: orig emits v-swap across r5/lr and r-swap in r0 simultaneously; if your build collapses one swap before starting the other, the scheduling differs.
3. Park as permuter-class; the two-swap live-range interleave is a register-allocation coin-flip that C reshaping cannot force without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206e174` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

