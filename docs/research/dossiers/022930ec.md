[//]: # (markdownlint-disable MD013 MD041)

# 022930ec — func_ov002_022930ec (ov002, class E retriage, 264B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Iterates a fixed 5-slot loop calling func_ov002_022575c8 / func_ov002_021c1e44 / func_ov002_021c1a2c / func_ov002_021b9ecc / func_ov002_021c8470. The "coin-flip" blx calls were through named functions — func_ov002_021b9ecc is a documented Ov002Slot predicate. The DuelQueueState tag in the retriage table reflects the 0x708 constant that is a DuelQueueState-related threshold. All calls and constants are now named.
**Confidence:** high
**Structs needed:** Ov002Slot (id_lo13 via lsl#19/lsr#19 inside func_ov002_021b9ecc), Ov002SelfContext (self->b0 :1 player bit, self->f0 card id), constants 0x1596 / 0x13c3 / 0x708

## Assembly

```asm
; func_ov002_022930ec — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern func_ov002_021b9ecc
        .extern func_ov002_021c1a2c
        .extern func_ov002_021c1e44
        .extern func_ov002_021c8470
        .extern func_ov002_022575c8
        .extern func_ov002_0228c460
        .global func_ov002_022930ec
        .arm
func_ov002_022930ec:
    stmdb sp!, {r4, r5, r6, r7, r8, lr}
    mov r5, r0
    ldrh r0, [r5, #0x2]
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_0228c460
    cmp r0, #0x0
    movne r0, #0x1
    ldmneia sp!, {r4, r5, r6, r7, r8, pc}
    ldr r7, _LIT0
    ldr r8, _LIT1
    ldr r6, _LIT2
    mov r4, #0x0
.L_34:
    ldrh r1, [r5, #0x2]
    mov r0, r5
    mov r2, r4
    mov r1, r1, lsl #0x1f
    mov r1, r1, lsr #0x1f
    rsb r1, r1, #0x1
    bl func_ov002_022575c8
    cmp r0, #0x0
    beq .L_e8
    ldrh r1, [r5, #0x2]
    mov r0, r5
    mov r2, r4
    mov r1, r1, lsl #0x1f
    mov r1, r1, lsr #0x1f
    rsb r1, r1, #0x1
    bl func_ov002_021c1e44
    cmp r0, #0x0
    bne .L_e8
    ldrh r0, [r5, #0x2]
    ldrh r2, [r5]
    mov r1, r4
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    rsb r0, r0, #0x1
    bl func_ov002_021c1a2c
    cmp r0, #0x0
    beq .L_e8
    ldrh r0, [r5, #0x2]
    mov r1, r4
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    rsb r0, r0, #0x1
    bl func_ov002_021b9ecc
    cmp r0, r8
    cmpne r0, r7
    beq .L_e8
    ldrh r0, [r5, #0x2]
    mov r1, r4
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    rsb r0, r0, #0x1
    bl func_ov002_021c8470
    cmp r0, r6
    movge r0, #0x1
    ldmgeia sp!, {r4, r5, r6, r7, r8, pc}
.L_e8:
    add r4, r4, #0x1
    cmp r4, #0x5
    blt .L_34
    mov r0, #0x0
    ldmia sp!, {r4, r5, r6, r7, r8, pc}
_LIT0: .word 0x00001596
_LIT1: .word 0x000013c3
_LIT2: .word 0x00000708
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| Ov002Self | +0x00 | f0 | u16 | card id — passed to func_ov002_021c1a2c as r2 |
| Ov002Self | +0x02 | b0 | u16 b0:1 | player bit; lsl#1f/lsr#1f → player; `rsb r0,r0,#1` = opponent (1-player) |
| (constant) | — | 0x1596 | int | compared to func_ov002_021b9ecc result (r7) |
| (constant) | — | 0x13c3 | int | compared to func_ov002_021b9ecc result (r8) |
| (constant) | — | 0x708 | int | threshold for func_ov002_021c8470 return value (r6 = 0x708) |

## Recipe sketch

```c
typedef unsigned short u16;
struct Ov002Self { u16 f0; u16 b0 : 1; u16 rest : 15; };
extern int func_ov002_0228c460(int player);
extern int func_ov002_022575c8(struct Ov002Self *self, int opp, int slot);
extern int func_ov002_021c1e44(struct Ov002Self *self, int opp, int slot);
extern int func_ov002_021c1a2c(int opp, int slot, u16 card_id);
extern int func_ov002_021b9ecc(int opp, int slot);
extern int func_ov002_021c8470(int opp, int slot);

int func_ov002_022930ec(struct Ov002Self *self) {
    int player = (int)self->b0;
    if (func_ov002_0228c460(player) != 0)
        return 1;
    for (int s = 0; s < 5; s++) {
        int opp = 1 - player;
        if (func_ov002_022575c8(self, opp, s) == 0) continue;
        if (func_ov002_021c1e44(self, opp, s) != 0) continue;
        if (func_ov002_021c1a2c(opp, s, self->f0) == 0) continue;
        int kind = func_ov002_021b9ecc(opp, s);
        if (kind == 0x13c3 || kind == 0x1596) continue;
        if (func_ov002_021c8470(opp, s) >= 0x708)
            return 1;
    }
    return 0;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_021b3ecc.c
**Why similar:** Same opponent-player derivation pattern (`rsb r0,r0,#1` = `1 - player`) passed across a 5-slot loop body.
**Key lesson:** The double `cmpne r0,r7` after `cmp r0,r8` is a two-value skip — write it as `if (kind == 0x13c3 || kind == 0x1596) continue;` in C; mwcc emits `cmp/cmpne` for the OR-of-two-equals pattern. The `movge r0,#1` / early return requires `if (result >= 0x708) return 1;` inside the loop, not after it.

## Try this

1. Declare `int opp = 1 - (int)self->b0;` inside the loop body (recompute each iteration from `ldrh r1,[r5,#0x2]` per iteration) rather than caching it — the assembly re-reads `[r5,#0x2]` at each call site.
2. If the `cmpne r0,r7` two-value skip mismatches, ensure both constants are in the condition: `kind == 0x13c3 || kind == 0x1596` (arm order: r8=0x13c3 first, r7=0x1596 second from `cmpne`).
3. This is an Ov002Self 5-slot loop, not a PPT scan or DSS dispatch — escalate only if func_ov002_021b9ecc caller-sequence (player vs opp arg order) mismatches.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001596` | MMIO/const |
| `_LIT1` | `0x000013c3` | MMIO/const |
| `_LIT2` | `0x00000708` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_0228c460` | overlay call (ov002) |
| `bl` | `func_ov002_022575c8` | overlay call (ov002) |
| `bl` | `func_ov002_021c1e44` | overlay call (ov002) |
| `bl` | `func_ov002_021c1a2c` | overlay call (ov002) |
| `bl` | `func_ov002_021b9ecc` | overlay call (ov002) |
| `bl` | `func_ov002_021c8470` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

