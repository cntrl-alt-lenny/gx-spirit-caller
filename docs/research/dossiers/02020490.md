# 02020490 — func_02020490 (main, class C, 192B)

## Recipe + Risk
**Recipe:** LCG seed add-chain, mul by const, store-order halfwords, tail switch
**Risk:** orig add-chain is ((u16a + *0x027ffc3c) folded into r0, then +u16b, then u16c+acc) with operand-order add r1,r2,r1 (c first). mwcc may reassociate the three adds / pick a different accumulator reg -> add operand order flips. Also 0x10dcd*x+0x3039 split as +0x39 then +0x3000. reshape-able (operand-order) + struct-guessed

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02020490 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     LCG seed add-chain, mul by const, store-order halfwords, tail switch
 *   risk:       orig add-chain is ((u16a + *0x027ffc3c) folded into r0, then +u16b, then u16c+acc) with operand-order add r1,r2,r1 (c first). mwcc may reassociate the three adds / pick a different accumulator reg -> add operand order flips. Also 0x10dcd*x+0x3039 split as +0x39 then +0x3000. reshape-able (operand-order) + struct-guessed
 *   confidence: low
 */
/* func_02020490: reseed RNG field and kick a transition.
 * func_02093a20(&tmp) fills three u16 (tmp0,tmp2,tmp4). seed =
 * ((u16)tmp0 + *(int*)0x027ffc3c) + (u16)tmp2 + (u16)tmp4;
 * f34 = seed*0x10dcd + 0x3039; fc=0; f8=0x65; func_0201f138(3);
 * branch on func_02020550(1): 0x18 -> teardown ret0; 2 -> ret1; else ret0.
 */
struct Tmp { unsigned short f0, f2, f4; };
struct Ctl { char pad0[8]; unsigned short f8; char padA[0xc-0xa]; unsigned short fc; char padE[0x34-0xe]; int f34; };
extern struct Ctl data_02191f40;
extern void func_02093a20(void *p);
extern void func_0201f138(int);
extern void func_0201f19c(int);
extern int  func_02020550(int);

int func_02020490(void)
{
    struct Tmp t;
    int seed;
    int r;
    func_02093a20(&t);
    seed = ((int)t.f0 + *(int *)0x027ffc3c) + (int)t.f2;
    seed = (int)t.f4 + seed;
    data_02191f40.f34 = seed * 0x10dcd + 0x3039;
    data_02191f40.fc = 0;
    data_02191f40.f8 = 0x65;
    func_0201f138(3);
    r = func_02020550(1);
    if (r == 0x18) {
        func_0201f19c(0x18);
        func_0201f138(9);
        return 0;
    }
    if (r == 2) return 1;
    func_0201f19c(r);
    func_0201f138(9);
    return 0;
}
```

## Struct context
Accesses `data_02191f40` (Ctl struct). No GameSingleton fields.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x08 | f8 | unsigned short | store: set to 0x65 after seed computation |
| +0x0c | fc | unsigned short | store: cleared to 0 after seed computation |
| +0x34 | f34 | int | store: LCG result `seed*0x10dcd+0x3039` |

Also reads `*(int*)0x027ffc3c` (literal hardware/RNG seed address).

## Closest matched example
**Path:** src/main/Rand_Next.c
**Why similar:** Same LCG multiply-and-add pattern (`seed * const + const`) against a seeded state variable; Rand_Next uses `0x343fd` and `0x269ec3` but the form is identical.
**Key lesson:** For LCG seed chains, mwcc's constant-folding of `seed * 0x10dcd + 0x3039` may split the addend as two immediates (`+0x39` then `+0x3000`) rather than a single `add r?,#0x3039`. The prep draft already captures this split in `seed = (int)t.f4 + seed` on a separate line to control the accumulator register. If the add operand order is wrong (e.g. `add r1,r2,r1` vs `add r0,r1,r0`), swap which temporary appears on the left vs right side of each addition.

## Try this
1. Build as-is. In objdiff, inspect the add-chain after `func_02093a20`: the orig emits `add r0,r0,r1` (t.f0 + *0x027ffc3c in r0) then `add r0,r0,r2` (+t.f2) then `add r1,r2,r1` (t.f4 first in r1, added to accumulated r1). If the operand order of any add is flipped, restructure the individual addition lines: swap `(int)t.f4 + seed` to `seed + (int)t.f4` or vice versa.
2. If `seed*0x10dcd+0x3039` emits as a single `add r?,#0x3039` (one immediate), split the constant: `data_02191f40.f34 = seed*0x10dcd + 0x39 + 0x3000;` to reproduce the two-instruction immediate addition.
3. If the tail switch result handling (0x18 vs 2 vs else) emits in a different comparison order, swap the two `if (r==...)` checks in source order.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x027ffc3c` | MMIO/const |
| `_LIT1` | `0x00010dcd` | MMIO/const |
| `_LIT2` | `data_02191f40` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02093a20` | main TU call |
| `bl` | `func_0201f138` | main TU call |
| `bl` | `func_02020550` | main TU call |
| `bl` | `func_0201f19c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

