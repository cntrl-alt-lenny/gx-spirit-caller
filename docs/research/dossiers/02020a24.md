# 02020a24 — func_02020a24 (main, class C, 256B)

## Recipe + Risk
**Recipe:** CLASS C: reload base each block (orig re-ldr _LIT0); if-chain ||; store +0x10 then +0x3c order
**Risk:** variadic fn-ptr (+0x38) calls + per-block reload-vs-bind of data_02191f40 base; if mwcc binds it in one reg, the repeated `ldr _LIT0` collapse and diverge. permuter-class

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02020a24 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS C: reload base each block (orig re-ldr _LIT0); if-chain ||; store +0x10 then +0x3c order
 *   risk:       variadic fn-ptr (+0x38) calls + per-block reload-vs-bind of data_02191f40 base; if mwcc binds it in one reg, the repeated `ldr _LIT0` collapse and diverge. permuter-class
 *   confidence: low
 */
/* func_02020a24: field stores + (1||3||5) guard chain + dual tail calls.
 * Global struct at data_02191f40; +0x38 is a fn-ptr called 3 ways.
 * Base is RELOADED each block in the orig (ldr _LIT0 repeats), not bound. */

extern char data_020c65b4[];
extern char data_020c65cc[];
extern char data_020c6810[];
extern char data_02191f40[];
extern char data_02192020[];
extern int  func_0201f138(int x);
extern void func_0201fc6c(void);
extern void func_0201fd24(void);
extern void func_02092904(void *p, int n);
extern void func_02092928(void);
extern void func_02093bfc(void);
extern void func_02094688(void *dst, void *src, int n);

typedef struct {
    char pad00[0x10];
    int  f10;      /* 0x10 */
    char pad14[0x04];
    int  f18;      /* 0x18 */
    char pad1c[0x14];
    int  f30;      /* 0x30 */
    char pad34[0x04];
    void (*f38)();  /* 0x38 fn-ptr */
    int  f3c;      /* 0x3c */
    int  f40;      /* 0x40 */
} S02191f40;

int func_02020a24(int a0, void *a1) {
    S02191f40 *s = (S02191f40 *)data_02191f40;
    if (s->f30 != 1) {
        func_02093bfc();
    }
    s = (S02191f40 *)data_02191f40;
    s->f10 = 0xc0;
    s->f3c = 0x20;
    if (s->f38 != 0) {
        s->f38(data_020c65b4);
    }
    s = (S02191f40 *)data_02191f40;
    if (s->f38 != 0) {
        s->f38(data_020c65cc, s->f3c);
    }
    s = (S02191f40 *)data_02191f40;
    s->f40 = a0;
    func_0201f138(3);
    if (a0 == 1 || a0 == 3 || a0 == 5) {
        func_02094688(a1, data_02192020, 0xc0);
        func_02092904(data_02192020, 0xc0);
        func_02092928();
        s = (S02191f40 *)data_02191f40;
        if (s->f18 != 0) {
            func_0201fc6c();
        } else {
            func_0201fd24();
        }
        return 0;
    }
    s = (S02191f40 *)data_02191f40;
    if (s->f38 != 0) {
        s->f38(data_020c6810, a0);
    }
    return 0;
}
```

## Struct context
This function accesses `data_02191f40`, a local module struct (not GameSingleton/SystemWork). All offsets are relative to that global.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x10 | f10 | int | written 0xc0 (screen mode?) |
| +0x18 | f18 | int | tested != 0 to choose fc6c vs fd24 tail |
| +0x30 | f30 | int | guard: if != 1 call func_02093bfc |
| +0x38 | f38 | void(*)() | fn-ptr, called 1–3 arg variadic; null-guarded each call |
| +0x3c | f3c | int | written 0x20; then passed as arg2 to f38 call |
| +0x40 | f40 | int | written a0 (caller arg) |

## Closest matched example
**Path:** src/main/func_02014bf4.c
**Why similar:** both re-reload a global base pointer each logical block rather than binding once, and both contain guarded fn-ptr / callback dispatch with multiple early-return paths.
**Key lesson:** when the orig emits repeated `ldr _LIT0` for the same global, the C must re-cast the pointer each block with a fresh local declaration (`s = (Type *)data_...;`) — do not hoist to a single binding before the first `if`. mwcc binds a local if it survives across the if-body; re-declaring forces a reload. The `s->f3c` read on line 52 must be done after the `s->f3c = 0x20` store (same block), so the second call receives the stored value, not a stale reg.

## Try this
1. Ensure each occurrence of `s = (S02191f40 *)data_02191f40;` is a fresh assignment at the START of each block as written — do not merge them. If objdiff shows a missing `ldr` for the literal before a `blx`, split that block's `s` re-assignment earlier.
2. If the `a0==1||a0==3||a0==5` test does not emit `cmp r0,#1; cmpne r0,#3; cmpne r0,#5`, try rewriting as three separate `if` + `goto done` guards to force cmpne fusion.
3. The fn-ptr `f38` is variadic-style (called with 1 or 2 args); if the call sites don't match, add explicit prototypes. Park as permuter-class if base-reload count still diverges after reshape — the repeated `ldr _LIT0` pattern is un-forceable without the permuter on Windows.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02191f40` | data symbol |
| `_LIT1` | `data_020c65b4` | data symbol |
| `_LIT2` | `data_020c65cc` | data symbol |
| `_LIT3` | `data_02192020` | data symbol |
| `_LIT4` | `data_020c6810` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02093bfc` | main TU call |
| `blx` | `r2` |  |
| `bl` | `func_0201f138` | main TU call |
| `bl` | `func_02094688` | main TU call |
| `bl` | `func_02092904` | main TU call |
| `bl` | `func_02092928` | main TU call |
| `bl` | `func_0201fc6c` | main TU call |
| `bl` | `func_0201fd24` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

