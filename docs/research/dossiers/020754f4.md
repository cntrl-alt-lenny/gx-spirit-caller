# 020754f4 — func_020754f4 (main, class C, 632B)

## Recipe + Risk

**Recipe:** faithful full TU (now have complete .s): bind b/raw/h, big-endian byte stores in offset order, signed asr shifts (int h), reload b->_32
**Risk:** permuter-class: already a confirmed .s wall (brief 302, no C match). ~80 instrs of reg-alloc/scheduling coin-flips (q cursor vs pkt+const, callee-saved h/raw, merged early-returns). Escape path is .s. Also struct-guessed offsets.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020754f4 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     faithful full TU (now have complete .s): bind b/raw/h, big-endian byte stores in offset order, signed asr shifts (int h), reload b->_32
 *   risk:       permuter-class: already a confirmed .s wall (brief 302, no C match). ~80 instrs of reg-alloc/scheduling coin-flips (q cursor vs pkt+const, callee-saved h/raw, merged early-returns). Escape path is .s. Also struct-guessed offsets.
 *   confidence: low
 */
/* func_020754f4: packet serializer. CONFIRMED reg-alloc wall (currently
 * ships as src/main/func_020754f4.s, brief 302). Full faithful C below for
 * a permuter target. b=p->_c; raw=b->_820; h=raw?*(int*)raw:0.
 * Builds a packet `pkt` from data_0219eee0 (alloc fn-ptr, _LIT0), writes
 * big-endian fields, frees via data_0219ef0c (_LIT2); data_021a0704 is an
 * int counter (_LIT1) bumped on the _30==0 path. Returns b->_31. */
typedef struct B B;
typedef struct P P;
extern void *data_0219eee0;   /* void* (*)(int)  [_LIT0 alloc] */
extern int   data_021a0704;   /* int counter     [_LIT1] */
extern void *data_0219ef0c;   /* void (*)(void*) [_LIT2 free] */
extern int   func_02077b5c(P *p);
extern void  func_020757e0(void *dst, int n);
extern void  func_02094688(void *a, void *b, int n);
extern void  func_02075d44(B *b, void *p, int n);
extern void  func_020705d4(void *pkt, int len, int z0, int z1, P *p);

int func_020754f4(P *p) {
    char *b = *(char **)((char *)p + 0xc);
    void *raw = *(void **)(b + 0x820);
    int h = (raw != 0) ? *(int *)raw : 0;
    unsigned char *pkt, *q;
    int v, w, len;

    v = func_02077b5c(p);
    *(b + 0x54) = (char)(v >> 24);
    *(b + 0x55) = (char)(v >> 16);
    *(b + 0x56) = (char)(v >> 8);
    *(b + 0x57) = (char)v;
    func_020757e0(b + 0x58, 0x1c);

    pkt = (unsigned char *)((void *(*)(int))data_0219eee0)(h + 0x9d);
    if (pkt == 0) { *(b + 0x455) = 9; return 1; }

    pkt[5] = 2;
    q = pkt + 5;
    q[1] = 0; q[2] = 0; q[3] = 0x46; q[4] = 3; q[5] = 0;
    func_02094688(b + 0x54, q + 6, 0x20);
    q[0x26] = 0x20;

    if (*(unsigned char *)(b + 0x30) != 0) {
        func_02094688(b + 0x74, q + 0x27, 0x20);
        *(b + 0x31) = 1;
        q += 0x47;
    } else {
        func_020757e0(q + 0x27, 0x1c);
        w = data_021a0704;
        q[0x43] = (char)(w >> 24);
        q[0x44] = (char)(w >> 16);
        q[0x45] = (char)(w >> 8);
        q[0x46] = (char)w;
        func_02094688(q + 0x27, b + 0x74, 0x20);
        q += 0x47;
        data_021a0704 = data_021a0704 + 1;
        *(b + 0x31) = 0;
    }

    q[0] = (char)(*(unsigned short *)(b + 0x32) >> 8);
    q[1] = (char)*(unsigned short *)(b + 0x32);
    q[2] = 0;
    q += 3;

    if (*(unsigned char *)(b + 0x31) == 0) {
        if (h != 0) {
            q[0] = 0xb;
            q[1] = (char)((h + 6) >> 16);
            q[2] = (char)((h + 6) >> 8);
            q[3] = (char)(h + 6);
            q[4] = (char)((h + 3) >> 16);
            q[5] = (char)((h + 3) >> 8);
            q[6] = (char)(h + 3);
            q[7] = (char)(h >> 16);
            q[8] = (char)(h >> 8);
            q[9] = (char)h;
            q += 0xa;
            func_02094688(*(void **)((char *)raw + 4), q, h);
            q += h;
        }
        q[0] = 0xe; q[1] = 0; q[2] = 0; q[3] = 0;
        q += 4;
    }

    len = (int)(q - pkt) - 5;
    pkt[0] = 0x16;
    pkt[1] = 3;
    pkt[2] = 0;
    pkt[3] = (char)(len >> 8);
    pkt[4] = (char)len;
    func_02075d44((B *)b, pkt + 5, len);
    func_020705d4(pkt, len + 5, 0, 0, p);
    ((void (*)(void *))data_0219ef0c)(pkt);
    return *(unsigned char *)(b + 0x31);
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| p+0x0c | _c | `char *` | Inner struct pointer `b` |
| b+0x30 | _30 | `unsigned char` | Branch selector: non-zero = copy from b+0x74 path |
| b+0x31 | _31 | `unsigned char` | Return value; set 0 or 1; reloaded at return |
| b+0x32 | _32 | `unsigned short` | Big-endian halfword: written to q[0]/q[1] |
| b+0x54 | _54..+0x57 | `char[4]` | Big-endian word from func_02077b5c result |
| b+0x74 | _74 | `char[0x20]` | Source for conditional func_02094688 copy |
| b+0x455 | _455 | `unsigned char` | Error status (9 = alloc fail) |
| b+0x820 | _820 | `void *` | Pointer to raw TLS record; may be null |
| data_0219eee0 | — | `void*(*)(int)` | Alloc function pointer |
| data_0219ef0c | — | `void(*)(void*)` | Free function pointer |
| data_021a0704 | — | `int` | Counter, incremented on _30==0 path |

## Closest matched example

**Path:** src/main/func_02074720.legacy.c
**Key lesson:** The `b = p->_c` bind pattern and `data_0219ef0c` free-function-pointer call appear in both functions. The critical issue for 020754f4 is that it already ships as `.s` (brief 302): the ~80-instruction body has confirmed register assignment coin-flips between `q` cursor arithmetic and `pkt+const` pointer expressions, and the callee-saved allocation of `h` (the TLS record length) vs `raw` (the raw pointer) does not stabilize under any single C reshape. This is the largest function in this batch.

## Try this

1. Park as permuter-class; this is a confirmed .s wall from brief 302. Do not attempt C reshape for the register-allocation divergences — ~80 instructions of scheduling and callee-saved set selection are not closeable from source.
2. If attempting permuter anyway: ensure `h` is declared `int` (not `unsigned`) so the `(h + 6) >> 16` byte extractions emit `asr` (signed shift), matching orig. The key permuter lever is the `q` cursor register vs the `pkt + constant` form of addressing.
3. Accept the existing `.s` file as the match; do not discard it.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219eee0` | data symbol |
| `_LIT1` | `data_021a0704` | data symbol |
| `_LIT2` | `data_0219ef0c` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02077b5c` | main TU call |
| `bl` | `func_020757e0` | main TU call |
| `blx` | `r1` |  |
| `bl` | `func_02094688` | main TU call |
| `bl` | `func_02075d44` | main TU call |
| `bl` | `func_020705d4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

