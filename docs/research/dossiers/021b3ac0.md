[//]: # (markdownlint-disable MD013 MD041)

# 021b3ac0 — func_ov002_021b3ac0 (ov002, class F retriage, 336B)

## Re-triage
**Original verdict:** F — previously intractable (reg-alloc walled, shipped as .s)
**Upgrade reason:** Uses PerPlayerRowTable (LIT1=data_ov002_022cf16c, LIT2=0x868 stride) to locate a slot, calls func_ov002_021b9ecc and func_ov002_021b3618, then calls func_ov002_021e797c with a local buffer built from slot fields. The register juggling that was flagged as scheduling-coin-flip is now explained: it packs slot data into a local struct on the stack (six fields over 0x18 bytes), using the id_lo13 bitfield (`lsl#19/lsr#19`), the player-parity bit, and the slot index. The `bic r0, ip, #0x3e; orr` sequence packs flag bits from the slot's f_038/f_040 into the stack buffer. All struct fields are now documented.
**Confidence:** high
**Structs needed:** PerPlayerRowTable, Ov002Slot (id_lo13 bitfield)

## Assembly

```asm
; func_ov002_021b3ac0 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022cf16c
        .extern func_ov002_021b34f4
        .extern func_ov002_021b3618
        .extern func_ov002_021b9ecc
        .extern func_ov002_021e797c
        .global func_ov002_021b3ac0
        .arm
func_ov002_021b3ac0:
    stmdb sp!, {r4, r5, r6, r7, r8, r9, sl, lr}
    sub sp, sp, #0x18
    mov r8, r0
    mov r7, r1
    mov r6, r2
    mov r5, r3
    bl func_ov002_021b3618
    mov r4, r0
    add r3, sp, #0x0
    mov r2, #0x0
    mov r0, r8
    mov r1, r7
    str r2, [r3]
    str r2, [r3, #0x4]
    str r2, [r3, #0x8]
    str r2, [r3, #0xc]
    str r2, [r3, #0x10]
    str r2, [r3, #0x14]
    bl func_ov002_021b9ecc
    ldrh r9, [sp, #0x2]
    mov r2, r8, lsl #0x10
    mov lr, r2, lsr #0x10
    bic sl, r9, #0x1
    and r9, lr, #0x1
    orr r9, sl, r9
    strh r9, [sp, #0x2]
    ldrh lr, [sp, #0x2]
    mov r2, r7, lsl #0x10
    mov ip, r2, lsr #0x10
    strh r0, [sp]
    ldr r2, _LIT0
    ldrh r3, [sp, #0x4]
    ldr r0, _LIT1
    ldr r9, _LIT2
    mov ip, ip, lsl #0x1b
    bic lr, lr, #0x3e
    orr lr, lr, ip, lsr #0x1a
    and ip, r8, #0x1
    mov r1, #0x14
    mla r9, ip, r9, r0
    mul r0, r7, r1
    add r1, r9, #0x30
    ldr r9, [r1, r0]
    strh lr, [sp, #0x2]
    mov r1, r9, lsl #0x2
    mov r1, r1, lsr #0x18
    ldrh ip, [sp, #0x2]
    mov r9, r9, lsl #0x12
    mov r1, r1, lsl #0x1
    add r1, r1, r9, lsr #0x1f
    mov r1, r1, lsl #0x10
    mov r1, r1, lsr #0x10
    bic r0, ip, #0x3000
    and r3, r3, r2
    mov r1, r1, lsl #0x17
    orr r1, r3, r1, lsr #0x11
    strh r0, [sp, #0x2]
    strh r1, [sp, #0x4]
    cmp r4, r2, lsr #0x10
    beq .L_120
    mov r1, r4, asr #0x8
    mov r2, r8
    mov r3, r7
    and r0, r4, #0xff
    and r1, r1, #0xff
    bl func_ov002_021b34f4
    cmp r0, #0xc
    cmpne r0, #0xd
    bne .L_120
    add sp, sp, #0x18
    mov r0, #0x0
    ldmia sp!, {r4, r5, r6, r7, r8, r9, sl, pc}
.L_120:
    add r0, sp, #0x0
    mov r1, r6
    mov r2, r5
    bl func_ov002_021e797c
    cmp r0, #0x1
    moveq r0, #0x1
    movne r0, #0x0
    add sp, sp, #0x18
    ldmia sp!, {r4, r5, r6, r7, r8, r9, sl, pc}
_LIT0: .word 0xffff803f
_LIT1: .word data_ov002_022cf16c
_LIT2: .word 0x00000868
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| PerPlayerRowTable | base + (player&1)*0x868 | row base | Ov002FieldZone* | `mla r9, ip(player&1), 0x868, cf16c` |
| Ov002Slot | +0x030 + slot*0x14 (word) | id_lo13 | unsigned int : 13 | `lsl#0x12 / lsr#0x1f` (different shift for bit 18 sign field); also bit extraction `lsl#2/lsr#18` |
| Stack buffer | sp+0x0 (u16 lo) | card_token | u16 | `strh r0, [sp]` — stores func_ov002_021b9ecc result (r0) |
| Stack buffer | sp+0x2 (u16) | flags_a | u16 | bit 0 = player parity; bits 2..5 cleared by `bic; orr` from slot index |
| Stack buffer | sp+0x4 (u16) | flags_b | u16 | packed from f_040 word; bits derived from `lsl#2/lsr#18/lsl#1` |
| LIT0 mask | 0xffff803f | bitmask | u32 | `and r3, r3, r2` — applied to flags_b sp+4 before orr |
| func_ov002_021b3618 | (player, slot) → r4 | — | int | result r4 compared against 0xffff0000 mask; if match, dispatches 021b34f4 |
| func_ov002_021b9ecc | (player, slot) → token | — | int | card token stored at sp+0 |
| func_ov002_021b34f4 | (player_lo, slot_lo, player, slot) → int | — | int | returns 0xc or 0xd → early return 0 |
| func_ov002_021e797c | (&buf, r6, r5) → int | — | int | final dispatch; return 1 if result==1 |

## Recipe sketch

```c
/* Takes (player, slot, arg_r6, arg_r5).
 * Builds a 0x18-byte local struct from slot data and dispatches
 * through 021e797c (returns 1 on success). Early-returns 0 if
 * 021b34f4 yields 0xc or 0xd. */
int func_ov002_021b3ac0(int player, int slot, int arg_r6, int arg_r5) {
    unsigned short buf[0xc];  /* 0x18 bytes on stack */
    int r4 = func_ov002_021b3618(/* implicit args */);

    memset(buf, 0, 0x18);

    int tok = func_ov002_021b9ecc(player, slot);
    buf[0] = (unsigned short)tok;  /* sp+0x0: strh token */

    /* sp+0x2 word: bic bit0, set bit0 = player parity */
    unsigned short f2 = buf[1];            /* sp+0x2 */
    f2 = (f2 & ~1u) | (player & 1);
    buf[1] = f2;

    /* pack slot index bits 4:1 into flags_a (sp+0x2) bits 5:2 */
    unsigned short slot_u = (unsigned short)slot;
    unsigned short lr_val = buf[1];
    lr_val = (lr_val & ~0x3e) | ((slot_u << 27) >> 26);
    buf[1] = lr_val;

    /* read slot word from PPT */
    Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (player & 1) * 0x868);
    unsigned int slot_word = row->slots[slot].id_lo13;  /* full word at +0x30 */

    /* extract packed fields from slot_word into sp+0x4 */
    unsigned short f4 = buf[2];
    int field_a = (int)((slot_word << 2) >> 24) << 1;
    field_a    += (slot_word << 18) >> 31;
    f4 = (unsigned short)((f4 & 0x803f) | (((unsigned short)field_a) << 7));
    buf[2] = f4;

    /* check if r4 indicates a pre-existing owner */
    if (r4 != (int)(0xffff0000u >> 16)) {
        int r4_player = r4 & 0xff;
        int r4_slot   = (r4 >> 8) & 0xff;
        int res = func_ov002_021b34f4(r4_player, r4_slot, player, slot);
        if (res == 0xc || res == 0xd) return 0;
    }

    int result = func_ov002_021e797c(buf, arg_r6, arg_r5);
    return (result == 1) ? 1 : 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_021b3014.c
**Why similar:** Uses `(player&1)*0x868` mla to get player row base from data_ov002_022cf16c; demonstrates the standard PPT slot offset computation.
**Key lesson:** The `bic/orr` bit-packing into a stack half-word buffer is mwcc's codegen for building a local struct from individual bit fields; the 0xffff803f mask (`LIT0`) is a pre-computed clear-mask for flags_b bits 13:6. Express these as explicit bit-field struct members on the stack buffer to avoid register pressure from manual shift arithmetic.

## Try this
1. Declare `Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (player & 1) * 0x868);` and load the slot word as `unsigned int sw = *(unsigned int *)((char *)&row->slots + slot * 0x14 + 0x30);` — the `mla r9, ip, 0x868, cf16c; mul r0, r7, 0x14; ldr r9, [r1+r0+0x30]` pattern requires the flat multiply approach.
2. If the `bic lr, lr, #0x3e; orr lr, lr, ip, lsr #0x1a` pack block does not match, check that slot bits are shifted by 27 then right-shifted by 26 to land in bits 5:2 of the u16 — this is `(slot << 27) >> 26 = slot * 2` effectively placing the slot index as a 5-bit field starting at bit 1.
3. If objdiff shows the `cmp r4, r2, lsr #0x10` instruction — this compares r4 against 0xffff — rewrite as `if (r4 != 0xffff)` rather than using the shifted-mask form; mwcc emits the `lsr #0x10` form from `r2 = 0xffff0000; r4 != r2>>16`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0xffff803f` | MMIO/const |
| `_LIT1` | `data_ov002_022cf16c` | raw |
| `_LIT2` | `0x00000868` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021b3618` | overlay call (ov002) |
| `bl` | `func_ov002_021b9ecc` | overlay call (ov002) |
| `bl` | `func_ov002_021b34f4` | overlay call (ov002) |
| `bl` | `func_ov002_021e797c` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

