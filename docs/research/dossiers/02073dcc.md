# 02073dcc — func_02073dcc (main, class C, 136B)

## Recipe + Risk

**Recipe:** bind global once (single ldr); 4-group || returning bool, climbing accumulator regs
**Risk:** mwcc may lower || as branch-to-common-return instead of the default-1/conditional-clear (movne r0,#0) idiom the asm uses. reshape-able (keep bool-returning ||).

## Prep draft

```c
extern int data_0219ef1c;
extern int func_02073e54(int param);
extern int func_02073e68(int param);

int func_02073dcc(int param) {
    int g = data_0219ef1c;
    return (g == 0 || param == g) ||
           param == 0x7f000001 ||
           func_02073e68(param) != 0 ||
           func_02073e54(param) != 0;
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `data_0219ef1c` | global IP | `int` | bound to local `g` once; tested `==0` then `param==g` |
| `0x7f000001` | loopback literal | `u32` | 127.0.0.1 — literal constant comparison |
| `func_02073e68` | range predicate | `int(int)` | called third in OR chain |
| `func_02073e54` | nibble predicate | `int(int)` | called fourth; tests top nibble == 0xe |

## Closest matched example

**Path:** src/main/func_02073e54.c
**Key lesson:** func_02073e54 is the confirmed sibling — it returns `(arg & 0xf0000000u) == 0xe0000000u` as a bool via `moveq r0,#1; movne r0,#0`. The "climbing accumulator regs" pattern means mwcc pipelines the OR chain through successive registers (r0→r2→r4→r5) with each group defaulting to 1 and cleared on failure; this is the `default-1/conditional-clear` idiom. Binding `data_0219ef1c` to `int g` before the return forces a single `ldr` and prevents re-loading.

## Try this

1. Bind `int g = data_0219ef1c;` as the very first statement in the function body — this must be before any other computation; mwcc emits a single `ldr` into a callee-saved register for `g` and reuses it in both the `g==0` and `param==g` tests without a second load.
2. Write the full short-circuit expression as a single `return` statement spanning all four OR groups — do NOT split into if/else or intermediate booleans; the single-return form is what drives the `movne r0,#0` conditional-clear idiom rather than branch-to-return.
3. If mwcc emits `branch-to-common-return` for any || group (e.g. `bne .Lreturn1; cmp; bne .Lreturn1`) instead of `movne r0,#0; cmp` in sequence, try wrapping each group as `(int)(!!(expr))` to force explicit bool-compare emission; escalate to permuter-class only if the four-group climbing pattern cannot be reproduced.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219ef1c` | data symbol |
| `_LIT1` | `0x7f000001` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02073e68` | main TU call |
| `bl` | `func_02073e54` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

