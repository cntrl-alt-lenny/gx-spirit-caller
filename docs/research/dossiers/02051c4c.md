# 02051c4c — func_02051c4c (main, class C, 312B)

## Recipe + Risk
**Recipe:** clone of 02051b10 recipe; f1=0 store-value delta; same mul-index base + guard chain
**Risk:** Source .s TRUNCATED past .L_294 (no pool/epilogue); tail inferred. The strb f1 store-value (0 vs 1) and the two mov #imm orderings are the only visible clone deltas. struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02051c4c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 *   recipe:     clone of 02051b10 recipe; f1=0 store-value delta; same mul-index base + guard chain
 *   risk:       Source .s TRUNCATED past .L_294 (no pool/epilogue); tail inferred. The strb f1 store-value (0 vs 1) and the two mov #imm orderings are the only visible clone deltas. struct-guessed.
 *   confidence: low
 */
/* func_02051c4c — main, cls C. Near-clone of func_02051b10; same 12-byte Slot.
   Visible delta vs 02051b10: f1 is set to 0 here (mov r2,#0 before strb) where
   02051b10 set it to 1; and the order of the two 'mov #imm' setups differs.
   NOTE: source .s is TRUNCATED (size=312; visible body ends at 'mov r0,r8'). */
typedef struct {
    unsigned char  id;      /* +0  data_0219dcb4 */
    unsigned char  f1;      /* +1  data_0219dcb5 */
    unsigned char  f2;      /* +2  data_0219dcb6 */
    unsigned char  _pad3;
    unsigned short hslot;   /* +4  data_0219dcb8 */
    unsigned char  _pad6[2];
    void          *ptr;     /* +8  data_0219dcbc */
} Slot;                     /* sizeof == 0x0c */

extern Slot gSlotTab[];     /* base data_0219dcb4 */

extern int  func_0204b3d0(void);
extern int  func_020aaf40(void);
extern int  func_020aaddc(int b);
extern void *func_020454cc(int a, int n);
extern void func_0204548c(int a);
extern void func_02050054(int a, int b);
extern void func_020aadf8(void *p, int b);

int func_02051c4c(int id, int b, int c)
{
    if (b == 0 || c == 0)
        return 0;

    if (id >= 0x64 && gSlotTab[id - 0x64].id != 0) {
        if (gSlotTab[id - 0x64].ptr != 0 && func_020aaf40() != 0)
            return 0;
    } else {
        id = func_0204b3d0();
        if (id == 0)
            return 0;
    }

    /* .L_218: */
    gSlotTab[id - 0x64].id = (unsigned char)id;
    gSlotTab[id - 0x64].f1 = 0;          /* clone delta: 0 here, 1 in 02051b10 */
    gSlotTab[id - 0x64].hslot = 0;
    if (gSlotTab[id - 0x64].ptr != 0)
        func_0204548c(4);

    {
        int n = func_020aaddc(b);
        gSlotTab[id - 0x64].ptr = func_020454cc(4, n + 1);
    }
    if (gSlotTab[id - 0x64].ptr == 0) {
        func_02050054(9, (int)0xfffe82ac);
        return 0;
    }

    /* .L_294: (TRUNCATED — tail inferred) */
    func_020aadf8(gSlotTab[id - 0x64].ptr, b);
    return id;
}
```

## Struct context
Near-clone of 02051b10. `gSlotTab` (base `data_0219dcb4`) stride-0x0c array; same Slot layout. No SystemWork or GameSingleton.

Near-clone of 02051b10.

| offset | field | type | use |
|--------|-------|------|-----|
| Slot+0x00 | id | u8 | slot identity byte; guard and set |
| Slot+0x01 | f1 | u8 | set to **0** (clone delta: 02051b10 sets 1) |
| Slot+0x04 | hslot | u16 | zeroed |
| Slot+0x08 | ptr | void* | guard/free/alloc target |

## Closest matched example
**Path:** src/main/func_02051b10.c (prep draft — sibling)
**Why similar:** Near-clone of 02051b10 — identical structure, guard logic, and tail; only the `f1` store value (0 vs 1) and `mov #imm` ordering differ.
**Key lesson:** The only confirmed clone deltas are: (1) `gSlotTab[id-0x64].f1 = 0` (not 1), and (2) the order of the two `mov #imm` instructions inside the re-init block differs. If 02051b10 matches first, use its exact C layout here and flip `f1 = 0`. MWCC is sensitive to the order in which the immediate constants appear in source — if `f1=0` and `hslot=0` appear in different relative order than in 02051b10, swap them.

## Try this
1. Start from the matched 02051b10 source (once it matches) and change only `f1 = 1` → `f1 = 0`. In objdiff confirm the `mov r2,#0; strb r2,[r_slot,#1]` sequence where 02051b10 had `mov r2,#1`.
2. Check the `mov #imm` ordering in objdiff — if the two immediate constants (`4` for `func_0204548c` and `9` for `func_02050054`) appear in a different relative order than in 02051b10, reorder the C statements to match.
3. When to park: same as 02051b10 — if the truncated tail (`.L_294` onward) cannot be forced by any reshape, accept as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dcb4` | data symbol |
| `_LIT1` | `data_0219dcb8` | data symbol |
| `_LIT2` | `data_0219dcb5` | data symbol |
| `_LIT3` | `data_0219dcb6` | data symbol |
| `_LIT4` | `0xfffec77f` | MMIO/const |
| `_LIT5` | `data_0219dcbc` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020aaf40` | main TU call |
| `bl` | `func_0204b3d0` | main TU call |
| `bl` | `func_0204548c` | main TU call |
| `bl` | `func_020aaddc` | main TU call |
| `bl` | `func_020454cc` | main TU call |
| `bl` | `func_02050054` | main TU call |
| `bl` | `func_020aadf8` | main TU call |
| `bl` | `func_02067850` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

