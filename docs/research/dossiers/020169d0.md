# 020169d0 — func_020169d0 (main, class D, 400B)

## Recipe + Risk
**Recipe:** Outer w[0x920] (cmp#6) jump-table, nested w[0x924] table in case5; case1 uses ldrcs/strcs cond store (>= bound); bic|orr
**Risk:** permuter-class: inner bit0 `and #1` wall + two jump tables. Reshape-able: case1 `>= bound` must stay strcs (write as if(cmp>=)store), and the two case-0=return numberings on cmp#6/cmp#4

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020169d0 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     Outer w[0x920] (cmp#6) jump-table, nested w[0x924] table in case5; case1 uses ldrcs/strcs cond store (>= bound); bic|orr
 *   risk:       permuter-class: inner bit0 `and #1` wall + two jump tables. Reshape-able: case1 `>= bound` must stay strcs (write as if(cmp>=)store), and the two case-0=return numberings on cmp#6/cmp#4
 *   confidence: low
 */
/* func_020169d0 (main, class D, MED) — GetSystemWork dispatcher.
 * w[0x8d8] NOT guarded; w[0x920] (0..6 jump table, case0=return) selects handler;
 * case 4 has a NESTED w[0x924] jump-table (0..4, case0=return); case 6 is a tail
 * call. o[8] words 0x1d0100..0x1d0600. Modeled on src/main/func_020169d0.s. */
extern char *GetSystemWork(void);
extern int  func_020139b4(void);
extern int  func_020139c4(void);
extern int  func_02019034(void);
extern int  func_020190ec(void);
extern int  func_020195ec(void);
extern int  func_02019664(void);
extern void func_0201aabc(int);
extern void func_0201b7b4(void);

void func_020169d0(void *o) {
    char *w = GetSystemWork();
    switch (*(int *)(w + 0x920)) {
    case 1:
        if (func_020190ec() == 0) return;
        if (func_020139c4() != 5) return;
        if ((unsigned int)func_02019034() >= (unsigned int)(*(int *)(w + 0x8dc) + 1))
            *(int *)((char *)o + 8) = 0x001d0100;
        return;
    case 2:
        if (func_02019664() != 0x6a) return;
        if (func_020139b4() != 2) return;
        *(int *)((char *)o + 8) = 0x001d0200;
        *(int *)(w + 0x900) |= 0x1000000;
        return;
    case 3:
        *(int *)((char *)o + 8) = 0x001d0300;
        return;
    case 4:
        *(int *)((char *)o + 8) = 0x001d0400;
        return;
    case 5:
        switch (*(int *)(w + 0x924)) {
        case 1:
            *(int *)(w + 0x924) = 4;
            /* fallthrough */
        case 4:
            *(int *)((char *)o + 8) = 0x001d0500;
            *(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;
            return;
        case 3:
            if (((*(unsigned int *)(w + 0x8e0) << 0x1f) >> 0x1f)) return;
            if (func_020195ec() == 0)
                *(int *)((char *)o + 8) = 0x001d0501;
            else
                *(int *)((char *)o + 8) = 0x001d0502;
            return;
        case 2:
            *(int *)((char *)o + 8) = 0x001d0503;
            *(int *)(w + 0x900) &= ~0x1000000;
            func_0201b7b4();
            return;
        }
        return;
    case 6:
        *(int *)((char *)o + 8) = 0x001d0600;
        func_0201aabc(0x7a);
        return;
    }
}
```

## Struct context

All offsets are from `GetSystemWork()` (SystemWork, BSS `data_02104f58`, size 0x92c).
Note: `+0x8d8` is NOT checked in this function — the outer `switch` on `+0x920` starts directly.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x8dc | bound word | s32 | lower-bound for unsigned `>=` compare in case-1 (`w[0x8dc]+1`) |
| +0x8e0 | bitfield word | s32 | bit0 `(v<<0x1f)>>0x1f` guard in inner case-3; bic\|orr `(x&~1)\|1` sets bit0 in inner case-4 |
| +0x900 | flag word | s32 | OR'd with 0x1000000 in case-2; AND'd with ~0x1000000 in inner case-2 |
| +0x920 | mode/state | s32 | outer `addls` jump-table (cmp#6, entries 0..6; case0=implicit return) |
| +0x924 | sub-state | s32 | nested `addls` jump-table inside case-5 (cmp#4, entries 1,2,3,4; case1 self-sets to 4) |

## Closest matched example
**Path:** src/main/func_0201660c.c (near-sibling)
**Why similar:** Near-clone of 0201660c — same double jump-table structure (`w[0x920]` outer + `w[0x924]` inner), no `+0x8d8` guard, same `bic|orr` idiom in the inner table. Differences: outer table is cmp#6 (7 entries vs 5), nested table is in case-5 (vs case-4), and case-1 uses `>=` (strcs) instead of `<` (bcc).
**Key lesson:** The case-1 `>=` bound condition must be written as `if (func_02019034() >= (w[0x8dc]+1)) store` (positive guard, not negated with early return) to emit `ldrcs/strcs` predicated store rather than a conditional branch. The inner bit0 `and #1` wall in case-5/3 is the same permuter-class problem as in 0201660c.

## Try this
1. Drop the prep draft; verify in objdiff that case-1 emits a `strcs` or `movcs` predicated store (not a branch) for the `if (>= bound)` write to `o[8]`. The prep draft uses `>=` polarity which should be correct.
2. Check the outer `addls` table is `cmp #6` (7-entry, cases 0..6) and the inner is `cmp #4` (cases 1..4 effectively). If case ordering diverges, try reordering switch arms to match the `.s` table order.
3. If the inner bit0 `and #1` wall or either `addls` table layout diverges and reshaping fails, park as permuter-class and ship as `.s` — same outcome expected as for func_0201660c.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x001d0100` | MMIO/const |
| `_LIT1` | `0x001d0200` | MMIO/const |
| `_LIT2` | `0x001d0300` | MMIO/const |
| `_LIT3` | `0x001d0400` | MMIO/const |
| `_LIT4` | `0x001d0500` | MMIO/const |
| `_LIT5` | `0x001d0501` | MMIO/const |
| `_LIT6` | `0x001d0502` | MMIO/const |
| `_LIT7` | `0x001d0503` | MMIO/const |
| `_LIT8` | `0x001d0600` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190ec` | main TU call |
| `bl` | `func_020139c4` | main TU call |
| `bl` | `func_02019034` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_020139b4` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |
| `bl` | `func_0201aabc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

