# 0208e700 — func_0208e700 (main, class C, 172B)

## Recipe + Risk
**Recipe:** store-order verbatim; e518 local before e4ec local; volatile prevents store coalesce/reorder
**Risk:** store-order: must keep the 0x4000454=0 ONLY at tail (sibling e7ac adds a mid one). reshape-able via volatile+exact source order; residual reg pick for the two zero-stores is scheduling.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208e700 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store-order verbatim; e518 local before e4ec local; volatile prevents store coalesce/reorder
 *   risk:       store-order: must keep the 0x4000454=0 ONLY at tail (sibling e7ac adds a mid one). reshape-able via volatile+exact source order; residual reg pick for the two zero-stores is scheduling.
 *   confidence: med
 */
/* func_0208e700 (main, class C/D) — two poll loops fill two locals, then ordered MMIO stores.
 * *0x4000600 |= 0x8000; poll func_0208e518(&a) until 0; poll func_0208e4ec(&b) until 0;
 * store sequence (ORDER IS LOAD-BEARING):
 *   0x4000440=3; 0x4000454=0; 0x4000440=0; if(b) 0x4000448=b; 0x4000440=2; 0x4000448=a; 0x4000454=0.
 * a = local from e518 (sp+0), b = local from e4ec (sp+4); e518 polled before e4ec. */
typedef unsigned int u32;

extern int func_0208e518(u32 *out);
extern int func_0208e4ec(u32 *out);

void func_0208e700(void)
{
    u32 a;
    u32 b;

    *(volatile u32 *)0x04000600 |= 0x8000;

    while (func_0208e518(&a) != 0)
        ;
    while (func_0208e4ec(&b) != 0)
        ;

    *(volatile u32 *)0x04000440 = 3;
    *(volatile u32 *)0x04000454 = 0;
    *(volatile u32 *)0x04000440 = 0;
    if (b != 0)
        *(volatile u32 *)0x04000448 = b;
    *(volatile u32 *)0x04000440 = 2;
    *(volatile u32 *)0x04000448 = a;
    *(volatile u32 *)0x04000454 = 0;
}
```

## Struct context
No struct globals; all access is direct MMIO.

| Address | Register | Access |
|---------|----------|--------|
| `0x04000600` | GXSTAT/state register | RMW or-in 0x8000 at start |
| `0x04000440` | Control register | written 3, then 0, then 2 (in order) |
| `0x04000448` | Data register | conditional write of `b`, then unconditional write of `a` |
| `0x04000454` | Status/sync register | written 0 twice: once early (after 440=3), once at tail |

## Closest matched example
**Path:** src/main/func_0208e518.legacy.c
**Key lesson:** `func_0208e518` is the poll helper that fills `a` — it reads GXSTAT at 0x04000600 and extracts bits 8-12 into `*a0`. The poll loop `while (func_0208e518(&a) != 0)` must come BEFORE `while (func_0208e4ec(&b) != 0)` to match the stack layout (`a` at sp+0, `b` at sp+4). Swapping the poll order swaps the stack offsets and changes the ldr scheduling.

## Try this
1. Confirm the exact store sequence in objdiff: `440=3 → 454=0 → 440=0 → (cond)448=b → 440=2 → 448=a → 454=0`. If any store is missing or reordered, the volatile qualifier on each write is the fix — ensure ALL six stores are `*(volatile u32 *)addr = value;` not pointer-cached writes.
2. Confirm `func_0208e518` is polled BEFORE `func_0208e4ec` so `a` lives at sp+0 and `b` at sp+4. Swapping them changes stack offsets and the ldr order after the polls.
3. The single `*(volatile u32 *)0x04000454 = 0` at the tail (NOT mid-sequence) is the key difference from sibling `func_0208e7ac` — if objdiff shows two 454=0 stores, you have the wrong sibling; the mid-store belongs only in 0208e7ac.
