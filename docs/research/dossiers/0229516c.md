[//]: # (markdownlint-disable MD013 MD041)

# 0229516c — func_ov002_0229516c (ov002, class F retriage, 352B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses DuelQueueState (data_ov002_022ce288) and Ov002Slot id_lo13. At 352B this is a larger function but follows the standard duel-dispatch pattern — DuelQueueState state fields gate a PerPlayerRowTable slot scan with Ov002Slot id_lo13 extraction. Both struct layouts are now fully documented.
**Confidence:** high
**Structs needed:** DuelQueueState (base 0x022CE288, f_5a8 queue_state, f_5c0 re-entrancy lock, f_5cc tick counter, f_5d0 edge bump), Ov002Slot (id_lo13 :13 via lsl#19/lsr#19), PerPlayerRowTable (stride 0x868)

## Assembly

```asm
; Source file not present — func_ov002_0229516c has no .s file in the working tree.
; This function was either already matched and its .s removed, or was never emitted
; as a standalone unit. The retriage entry is preserved for campaign tracking.
; Size: 352B, Conf: high, KB hits: DuelQueueState + Ov002Slot
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state 0x7f / 0x80 |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock |
| DuelQueueState | +0x5CC | f_5cc | int | tick counter (incremented) |
| DuelQueueState | +0x5D0 | f_5d0 | int | edge-bump counter |
| DuelQueueState | +0x5D4 | f_5d4 | int | cleared on 0->1 edge |
| DuelQueueState | +0x5D8 | f_5d8 | int | guard flag |
| Ov002Slot | +0x008 | id_lo13 | u32 :13 | card id via lsl#19/lsr#19 |
| PerPlayerRowTable | base | data_ov002_022cf16c | extern | player slot array, stride 0x868 |

## Recipe sketch

```c
typedef unsigned short u16;
struct Ov002Self { u16 f0; u16 b0 : 1; u16 rest : 15; };
struct DuelQueueState {
    char _pad5a8[0x5a8]; int f_5a8;
    void *f_5ac;
    char _pad5b4[4]; int f_5b4;
    char _pad5c0[8]; int f_5c0;
    int f_5c8;
    int f_5cc;
    int f_5d0;
    int f_5d4;
    int f_5d8;
    char _pad688[0x688 - 0x5dc]; int f_688;
};
extern struct DuelQueueState data_ov002_022ce288;
extern char data_ov002_022cf16c[];

/* 352B: DuelQueueState tick-counter gated + two-player Ov002Slot scan */
int func_ov002_0229516c(struct Ov002Self *self) {
    if (data_ov002_022ce288.f_5c0) return 0;
    int qs = data_ov002_022ce288.f_5a8;
    if (qs == 0x80) {
        /* active: walk both players, scan slots */
        for (int p = 0; p < 2; p++) {
            char *row = data_ov002_022cf16c + (p & 1) * 0x868;
            for (int s = 0; s < 5; s++) {
                unsigned int w = *(unsigned int *)(row + 0x30 + s * 0x14);
                unsigned int id = (w << 19) >> 19;
                if (id == 0) continue;
                /* card-specific handler call(self, p, s) */
            }
        }
        return 0x7f;
    }
    if (qs == 0x7f) {
        /* done: update tick counter cluster */
        if (!data_ov002_022ce288.f_5d8) {
            data_ov002_022ce288.f_5cc++;
            /* edge logic: bump f_5d0, clear f_5d4 / f_688 on threshold */
        }
    }
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_0223483c.c
**Why similar:** Same DuelQueueState f_5a8 dispatch (0x80 active / 0x7f done) with slot-scan in the 0x80 arm and state-update in the 0x7f arm; tick counter cluster update pattern from DuelQueueState.md.
**Key lesson:** The tick counter cluster update (`f_5cc++`, `f_5d0++`, `f_5d4=0`, `f_688=0`) is a documented pattern from func_ov002_021e2b3c — copy that cluster exactly. The `f_5d8` guard must be checked before the increment (`if (!f_5d8) { f_5cc++; ... }`). The 0x80 arm returns 0x7f (done marker), not 0 or 1.

## Try this
1. Load `data_ov002_022ce288.f_5a8` and write `if (qs == 0x80)` before `if (qs == 0x7f)` — verify `cmp r0,#0x80` is first in objdiff.
2. In the 0x7f arm, write `if (!data_ov002_022ce288.f_5d8) { data_ov002_022ce288.f_5cc++; }` and check that the `ldr / add / str` for f_5cc at offset +0x5CC matches the assembly.
3. If the tick-counter cluster (f_5d0, f_5d4, f_688) doesn't appear in this function, this may only update f_5cc without the full cluster — check the actual assembly once the .s file is recovered; if only f_5cc increments, the 0x7f arm is simpler than the full template.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

