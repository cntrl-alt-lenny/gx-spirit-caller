# 021b718c — func_ov010_021b718c (ov010, class D, 196B)

## Recipe + Risk

**Recipe:** D: two-pass copy; stride-6 or stride-8; sign-extend s8 field
**Risk:** two-pass: first copy then process. stride may be 6 or 8 (check disasm). s8: cast to (s8) or signed char.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021b718c (ov010, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     subs-flag reuse for if(t<0) cond store; signed /2; *0x97 literal mul; bind MMIO ptr; store-order
 *   risk:       reshape-able: orig reuses the `subs` flags (movmi/strmi) for the negative clamp; if mwcc emits a separate cmp, keep `t=A[6]-delta; A[6]=t; if(t<0)` adjacent (already done) so the sub sets the condition.
 *   confidence: med
 */
/* func_ov010_021b718c (ov010, class D): decrement a countdown A[0x18] by delta;
 * if it goes negative, clamp B[0xc] to 0 (the timer keeps its negative value).
 * Then compute q = func_020b3870(B[0xc]*0x97, B[0x8]) / 2 (signed), and write
 * two MMIO halfwords at 0x04001040 (const 0x4e4) and 0x04001044 (packed). If
 * B[0xc]==0 clear B[0x8] and return 1, else return 0. Store order preserved.
 *
 * A=021b9884, B=021b9890. *0x97 is a literal multiply (not magic-divide). The
 * 0x04001040 base is held across both stores -> bind one volatile pointer. */

extern int data_ov010_021b9884[];   /* A */
extern int data_ov010_021b9890[];   /* B */
extern int func_020b3870(int a, int b);

int func_ov010_021b718c(int delta) {
    volatile unsigned short *reg = (volatile unsigned short *)0x04001040;
    int t;
    int q;
    int lo, hi;

    t = data_ov010_021b9884[6] - delta;        /* A[0x18] */
    data_ov010_021b9884[6] = t;
    if (t < 0)
        data_ov010_021b9890[3] = 0;            /* B[0xc] */

    q = func_020b3870(data_ov010_021b9890[3] * 0x97,
                      data_ov010_021b9890[2]) / 2;   /* B[0x8] = B[2] */
    lo = (0x6f - q) << 8;
    hi = q + 0x6f;

    reg[0] = 0x4e4;                                   /* *0x04001040 */
    reg[2] = (unsigned short)((lo & 0xff00) | (hi & 0xff)); /* *0x04001044 */

    if (data_ov010_021b9890[3] == 0) {
        data_ov010_021b9890[2] = 0;
        return 1;
    }
    return 0;
}
```

## Struct context

Two global int arrays used as structs:
- `data_ov010_021b9884` (A): `A[6]` = index 6 = byte offset 0x18 (countdown timer)
- `data_ov010_021b9890` (B): `B[2]` = byte offset 0x8 (f8 field); `B[3]` = byte offset 0xc (fc field)

MMIO: `0x04001040` = BLDCNT (GBA/NDS blend control); `0x04001044` = BLDALPHA.
The `volatile unsigned short *reg` pointer covers both halfwords via `reg[0]`
and `reg[2]` (byte offsets 0 and 4).

| field | offset | type | purpose |
|-------|--------|------|---------|
| A[6] | 0x18 | int | countdown; decremented by delta |
| B[3] | 0x0c | int | blend factor; clamped to 0 if timer overflows |
| B[2] | 0x08 | int | secondary blend param; cleared on B[3]==0 |

## Closest matched example

**Path:** src/overlay010/func_ov010_021b7218.c (near-clone in this same batch)
**Why similar:** near-identical body — same globals (A=021b9884, B=021b9890), same MMIO pair (0x04001040/44), same func_020b3870 call with *0x97, same subs-flag reuse shape. Differs in that 021b7218 has a second MMIO write pair in the else branch and uses `f8 - fc` as the multiply argument rather than `B[0xc]` alone.
**Key lesson from that file:** the `t = A - delta; store t; if (t < 0) clamp;` pattern must stay as three consecutive statements so mwcc reuses the `subs` flags for the `movmi`/`strmi` conditional store; separating the store from the subtraction inserts a `cmp` and breaks the match.

## Try this

1. Keep `t = data_ov010_021b9884[6] - delta; data_ov010_021b9884[6] = t; if (t < 0) ...` as three consecutive statements with no intervening code — this lets mwcc fuse the subtract and conditional store via `subs + strmi`.
2. In objdiff: if you see `subs r0,...` then `movmi`/`strmi` in orig but a separate `cmp r0,#0; strlt` in your output, the subtract and the store of `t` are not adjacent in your C. Reorder to fix.
3. Fallback (near-clone note): if register allocation diverges vs 021b7218, check whether `lo` and `hi` need to be declared as separate locals vs computed inline; 021b7218 uses `f8 - fc` as the multiply arg, so if this function uses just `B[0xc]` directly that changes one `ldr` in the call — verify in disasm before spending permuter budget.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov010_021b9884` | raw |
| `_LIT1` | `data_ov010_021b9890` | raw |
| `_LIT2` | `0x000004e4` | MMIO/const |
| `_LIT3` | `0x04001040` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020b3870` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

