# 02018be8 — func_02018be8 (main, class C, 244B)

## Recipe + Risk
**Recipe:** Fill32 clear; low16=0xffff RMW loop; 14 ordered field stores
**Risk:** reshape-able: 14 RMW stores must keep asm order (store-order); the constant folds (0x50000-0x60000 mask, 0x178-1 reuse of r2) and second GetSystemWork base must not be CSE'd with the first — split as written.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02018be8 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     Fill32 clear; low16=0xffff RMW loop; 14 ordered field stores
 *   risk:       reshape-able: 14 RMW stores must keep asm order (store-order); the constant folds (0x50000-0x60000 mask, 0x178-1 reuse of r2) and second GetSystemWork base must not be CSE'd with the first — split as written.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_02018be8 (main, class C/D) — prep batch p_0004.
 * UNVERIFIED build-free draft. Drop into src/, run ninja+objdiff, tweak per risk.
 *   recipe: Fill32 clear; loop sets low-16 of [i].f0c to 0xffff (and r0=-0x10000; orr r0,lsr#16); 14 RMW field stores in order
 */
/* func_02018be8: w = GetSystemWork(); Fill32(0, w, 0x92c). For 0x56 entries (stride 0x18)
 * w[i].field0c = (field0c & 0xffff0000) | 0xffff  (and r0=-0x10000; orr r0,lsr#16). Then a long
 * block of fixed RMW stores to w+0x908.. in EXACT order (store-order sensitive). Reads
 * GetSystemWork() AGAIN near the end (separate base) to clear bit0x1000 of [0x8f8]. Then
 * three func_0201aabc(0x75/0x76/0x77) tail calls. */
extern char *GetSystemWork(void);
extern void Fill32(int val, void *dst, int size);
extern void func_0201aabc(int id);

void func_02018be8(void) {
    char *w = GetSystemWork();
    char *p;
    int i;
    Fill32(0, w, 0x92c);
    p = w;
    for (i = 0; i < 0x56; i++) {
        *(unsigned int *)(p + 0xc) = (*(unsigned int *)(p + 0xc) & 0xffff0000) | 0xffff;
        p += 0x18;
    }
    *(int *)(w + 0x908) = 0x50000;
    *(int *)(w + 0x90c) = 0x50000 + 0xee000;
    *(int *)(w + 0x8d4) = (*(int *)(w + 0x8d4) & (0x50000 - 0x60000)) | 1;
    *(int *)(w + 0x8d0) = (*(int *)(w + 0x8d0) & ~0xf) | 2;
    *(int *)(w + 0x91c) = 1;
    *(int *)(w + 0x920) = 1;
    *(int *)(w + 0x924) = 1;
    *(int *)(w + 0x928) = 0;
    *(int *)(w + 0x8d8) = (*(int *)(w + 0x8d8) & ~1) | 1;
    *(int *)(w + 0x900) = (((*(int *)(w + 0x900) | 0x800) & ~0x600) | 0x600);
    *(int *)(w + 0x8f4) = 0x178 - 1;
    {
        char *w2 = GetSystemWork();
        *(int *)(w2 + 0x8f8) = *(int *)(w2 + 0x8f8) & ~0x1000;
    }
    func_0201aabc(0x75);
    func_0201aabc(0x76);
    func_0201aabc(0x77);
}
```

## Struct context
This function is the SystemWork initialiser. It touches the entire struct including
the stride-0x18 sub-array at base+0 (loop over 0x56 entries), and eleven individual
fields in the bitfield/mode cluster.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| `+0x0..+0x92b` | (whole struct) | char[0x92c] | Fill32(0, w, 0x92c) clears the entire struct |
| `+0x0..+0x17` × 0x56 | stride-0x18 sub-array entry field 0xc | `unsigned int` | loop: clear low16 then OR 0xffff |
| `+0x908` | `fx32` X-coord point 0 | `int` | set to 0x50000 |
| `+0x90c` | `fx32` Y-coord point 0 | `int` | set to 0x50000 + 0xee000 = 0x13e000 |
| `+0x8d4` | dense bitfield word | `int` | RMW: AND mask (0x50000-0x60000 = -0x10000 = 0xffff0000), OR 1 |
| `+0x8d0` | dense bitfield word | `int` | RMW: AND ~0xf, OR 2 (low 4 bits = 2) |
| `+0x91c` | phase/round counter | `int` | write 1 |
| `+0x920` | dispatch selector | `int` | write 1 |
| `+0x924` | sub-state selector | `int` | write 1 |
| `+0x928` | (adjacent word) | `int` | write 0 |
| `+0x8d8` | enable/gate bit 0 | `int` | RMW: AND ~1, OR 1 → set bit 0 |
| `+0x900` | multi-bit flag word | `int` | RMW: OR 0x800, AND ~0x600, OR 0x600 |
| `+0x8f4` | bitfield flag word | `int` | write 0x178 - 1 = 0x177 |
| `+0x8f8` | dense bitfield word | `int` | RMW: AND ~0x1000 (clear bit 12) via second GetSystemWork() call |

## Closest matched example
**Path:** src/main/func_02018f80.c
**Why similar:** Both call `GetSystemWork()` and write directly to named offsets in the struct; func_02018be8 is a superset that also runs a stride loop and calls sub-functions.
**Key lesson:** The second `GetSystemWork()` call must be a separate local (not CSE'd with the first). The constant `0x178 - 1` must be written as the subtraction expression, not the folded literal `0x177`, so r2 is reused from the loop stride constant as noted in the risk. Store ordering of the 14 writes is strict — rearranging any pair will diverge in objdiff.

## Try this
1. Drop the prep draft as-is into src/main/func_02018be8.c; verify `Fill32` and `func_0201aabc` externs match the project's existing declarations (check include/). Check that the loop walk via `p` (char * increment by 0x18) rather than index arithmetic matches the stride — the prep file uses `p = w; p += 0x18` which should force the loop body to use a pointer walk and emit `add p,p,#0x18` in the loop tail.
2. If objdiff shows the RMW at `w+0x8d4` with the wrong mask constant, ensure `(0x50000 - 0x60000)` stays as the subtraction expression rather than `-0x10000`; mwcc must emit the literal `rsb` / subtraction, not a plain `mvn`. Also check the `w+0x900` triple-RMW order (OR 0x800, AND ~0x600, OR 0x600 — all in one expression).
3. If the second `GetSystemWork()` call is CSE'd into the first (single base pointer), split it as shown with a separate `char *w2` local scope block; if the loop walk still mismatches frame layout, escalate to permuter for store scheduling.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x0000092c` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `Fill32` |  |
| `bl` | `func_0201aabc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

