# 021ab484 — func_ov008_021ab484 (ov008, class C, 132B)

## Recipe + Risk
**Recipe:** stack stores in asm order; field<<k inline; base+e2*0x40+e3*4 index
**Risk:** stack stores: spill order = asm order. field<<k: inline shift not extracted. stride: 0x40 then *4.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021ab484 (ov008, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     stack stores in asm order; field<<k inline; base+e2*0x40+e3*4 = int* index e2*0x10+e3
 *   risk:       struct-guessed + reshape-able: the goto/label is illegal as written — must drop it; store ORDER (block[2]=0 split around the q load) is the real lever and likely needs the q computation interleaved exactly there. Confirm [r0,#8]->[+0x2c] chain.
 *   confidence: low
 */
/* func_ov008_021ab484: build a 0x28 stack arg-block (shifted fields,
   indexed table base) then one call. cls C (store-order / shifts). */

extern int data_ov008_021b2d34;   /* _LIT0 base, +r2*0x40 + r3*4 */

extern void func_0201e964(int a0, void *p, void *block, int zero);

/* incoming: r0,r1 (<<12), r2 idx0, r3 idx1; stack params at +0x30.. */
int func_ov008_021ab484(int a0, int a1, int e2, int e3,
                        int s30, int s34, int s38, int s3c)
{
    int block[10];   /* sp..sp+0x1c, 0x28 bytes; block[8..9]=a0,a1<<12 */
    int *q;

    block[8] = a0 << 12;   /* [sp,#0x20] */
    block[9] = a1 << 12;   /* [sp,#0x24] */
    block[0] = 0;
    block[1] = 0;
    q = (int *)(&data_ov008_021b2d34 + e2 * 0x10 + e3);
    block[2] = 0;
    block[3] = s3c;
    block[4] = s34 << 7;
    block[5] = s38;
    block[6] = 0;
    block[7] = 0;
    {
        int *node = (int *)q[2];          /* [r0,#0x8] */
        return_helper:
        func_0201e964(2,
                      (void *)(node[0xb] + s30 * 8), /* [r1,#0x2c] + s30<<3 */
                      &block[8],
                      0);
    }
    (void)return_helper;
    return 0;
}
```

## Struct context
No named struct from `docs/research/types/` covers this function. The `data_ov008_021b2d34` global is also used in `021ab3ac` where it's treated as a `Gfx_021ab3ac` base. Here it's indexed as `int*` at stride `e2*0x40 + e3*4` (i.e., row `e2` × 64 ints, column `e3`). The pointer arithmetic `&data_ov008_021b2d34 + e2*0x10 + e3` accounts for `sizeof(int)=4` so the byte offset is `e2*0x40 + e3*4`.

| field | offset (bytes) | type | purpose |
|-------|---------------|------|---------|
| block[8] | sp+0x20 | int | a0 << 12 |
| block[9] | sp+0x24 | int | a1 << 12 |
| block[0..1] | sp+0x00..0x04 | int | zeroed |
| block[2] | sp+0x08 | int | zeroed (after q load) |
| block[3] | sp+0x0c | int | s3c |
| block[4] | sp+0x10 | int | s34 << 7 |
| block[5] | sp+0x14 | int | s38 |
| block[6..7] | sp+0x18..0x1c | int | zeroed |
| q[2] | *q + 0x08 | int* | node pointer |
| node[0xb] | *node + 0x2c | int | base for func_0201e964 arg1 |

## Closest matched example
**Path:** src/overlay008/func_ov008_021ab120.c
**Why similar:** Both are ov008 functions that build a stack-resident data block then pass a pointer into it to a helper, with the block slots filled in a fixed asm-determined order.
**Key lesson from that file:** mwcc stores block slots in declaration order within the function body. The interleaving of `q` load between `block[1]=0` and `block[2]=0` is deliberate — it reflects the asm store sequence. Moving `q` before or after changes which stores surround it and causes store-order divergence.

## Try this
1. Remove the illegal `goto`/`return_helper` label and `(void)return_helper` — these are m2c artifacts. Inline the node dereference directly: `func_0201e964(2, (void *)((int *)q[2])[0xb] + s30 * 8), &block[8], 0);`. Keep `block[8]` as the pointer arg (not `block[0]`).
2. The store sequence `block[8], block[9], block[0], block[1], q=..., block[2], block[3]...` must be respected in source order. If objdiff shows `str block[2]` before the q load, move `block[2]=0` above the `q=` line. Watch for `a0<<12` and `a1<<12` — confirm they are stored as `block[8]` and `block[9]` (near the top of sp) not `block[0]`/`block[1]`.
3. Fallback: the index expression `&data_ov008_021b2d34 + e2 * 0x10 + e3` must emit `ldr base; add e2*0x40; add e3*4`. If mwcc constant-folds or reorders, bind each factor as `int *q = (int *)(&data_ov008_021b2d34); q += e2 * 0x10; q += e3;`. If store order still won't settle after 3 tries, route to .s.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov008_021b2d34` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0201e964` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

