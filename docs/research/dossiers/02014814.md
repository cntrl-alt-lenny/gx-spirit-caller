# 02014814 — func_02014814 (main, class D, 268B)

## Recipe + Risk
**Recipe:** 0x8d8 :1 bitfield guard; 0x920==1 gate; 0x924 jump-table; func_020195ec ldreq/ldrne o[8] pair; (x&~1)|1
**Risk:** reshape-able: the func_020195ec eq/ne result must stay predicated (ldreq/streq + ldrne/strne) — keep it as if-else over the same o[8] slot, not a branch; else stores diverge. Jump-table layout may still flip (permuter-class fallback).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02014814 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     0x8d8 :1 bitfield guard; 0x920==1 gate; 0x924 jump-table; func_020195ec ldreq/ldrne o[8] pair; (x&~1)|1
 *   risk:       reshape-able: the func_020195ec eq/ne result must stay predicated (ldreq/streq + ldrne/strne) — keep it as if-else over the same o[8] slot, not a branch; else stores diverge. Jump-table layout may still flip (permuter-class fallback).
 *   confidence: med
 */
extern char *GetSystemWork(void);
extern int  func_02019034(void);
extern int  func_020195ec(void);
extern int  func_02019664(void);
extern int  func_ov000_021ae4ac(void);

struct Flag { unsigned int b0 : 1; };

void func_02014814(void *o) {
    char *w = GetSystemWork();
    if (((struct Flag *)(w + 0x8d8))->b0 == 0) return;
    if (*(int *)(w + 0x920) != 1) return;
    switch (*(int *)(w + 0x924)) {
    case 1:
        *(int *)(w + 0x924) = 4;
        /* fallthrough */
    case 4:
        if ((unsigned int)func_02019034() < (unsigned int)(*(int *)(w + 0x8dc) + 1)) return;
        if (func_02019664() != 0x6c) return;
        *(int *)((char *)o + 8) = 0x00090100;
        *(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;
        return;
    case 3:
        if (func_ov000_021ae4ac() != 2) {
            if (((struct Flag *)(w + 0x8e0))->b0) return;
            if (func_020195ec() == 0)
                *(int *)((char *)o + 8) = 0x00090101;
            else
                *(int *)((char *)o + 8) = 0x00090102;
            return;
        }
        *(int *)(w + 0x91c) = 0xa;
        *(int *)(w + 0x920) = 1;
        *(int *)(w + 0x924) = 1;
        *(int *)(w + 0x8e0) |= 4;
        return;
    case 2:
        *(int *)((char *)o + 8) = 0x00090103;
        return;
    }
}
```

## Struct context

This is a member of the SystemWork dispatch family with a COMPLETE asm (no truncation). The 0x924 switch is a `addls pc` jump-table.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| SystemWork+0x8d8 | enable gate | s32 / bit 0 | `:1` bitfield — master enable; early return if b0==0 |
| SystemWork+0x920 | dispatch selector | s32 | equality gate == 1 only |
| SystemWork+0x924 | sub-state | s32 | jump-table: cases 1/2/3/4 (case1 falls to case4) |
| SystemWork+0x8dc | timer/ceiling | s32 | read as `+1` ceiling for unsigned bcc |
| SystemWork+0x8e0 | two-bit flag | s32 / bit 0 | b0 tested as `:1` guard; `(x&~1)|1` sets bit0; `|= 4` sets bit2 |
| SystemWork+0x91c | round counter | s32 | written to 0xa in the func_ov000_021ae4ac==2 arm |
| o+8 | output param | int | packed status codes 0x00090100..0x00090103 |

## Closest matched example

**Path:** src/main/func_02014bf4.c
**Why similar:** Same `GetSystemWork()` + 0x8d8 `:1` bitfield guard, same 0x920 equality gate, same `o+8` output-param pattern; this function is a longer member of the same dispatch family.
**Key lesson:** The critical codegen constraint is the `func_020195ec() == 0` branch in case3: both stores to `o+8` (0x00090101 and 0x00090102) must be predicated (`ldreq`/`streq` + `ldrne`/`strne`), not implemented as a branch. This means the if-else must assign to the SAME variable in both arms with the assignment to `o+8` outside — or keep as `if/else` directly on `*(int*)(o+8)` with no intermediate. The `(x&~1)|1` RMW on 0x8e0 stays as `ldr; bic; orr; str`.

## Try this

1. The bit0 test at `sw+0x8d8` must use `:1` bitfield to get `lsl#31; lsrs#31`. The bit0 test at `sw+0x8e0` inside case3 also needs the same struct — reuse `struct Flag` for both.
2. If the `func_020195ec() == 0` if-else in case3 generates a branch instead of predicated stores: try assigning to a local first — `int msg = (func_020195ec() == 0) ? 0x00090101 : 0x00090102; *(int*)(o+8) = msg;` — which may stay predicated as a conditional `mov` + single `str`.
3. If the jump-table case order (1/4/3/2 with case1 falling to case4) flips: park as permuter-class fallback — the `addls pc` entry ordering depends on mwcc's case sort, which can vary.
