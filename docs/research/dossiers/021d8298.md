# 021d8298 — func_ov002_021d8298 (ov002, class E retriage, 380B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Takes `(player, slot_ptr, flag)` args. Computes PerPlayerRowTable offset via `mul ip,r3,r0` (stride 0x868). Reads `data_ov002_022cf17c` (0x260 list) at `(ppt_base+ip+0x10)` count and `(ppt_base+ip+0x260)` list. Reads `data_ov002_022cf178` at `(ppt_base+ip)` as a "from" address. The `lsl#19/lsr#19` at .L_f8 reads the slot word's id_lo13 and compares to 0x17cc. All offsets now named from the PerPlayerRowTable doc — the count/list access at `+0x10`/`+0x1c` and companion arrays `cf178`/`cf17c` are documented.
**Confidence:** high
**Structs needed:** PerPlayerRowTable (stride 0x868, +0x10 count, +0x260 list at cf17c, +0x1c count2), data_ov002_022cf178 (event-count companion), data_ov002_022cf17c (0x260 list)

## Assembly

```asm
; func_ov002_021d8298 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022cf16c
        .extern data_ov002_022cf178
        .extern data_ov002_022cf17c
        .extern func_ov002_021d479c
        .extern func_ov002_021e278c
        .extern func_ov002_021e30b4
        .extern func_ov002_02244fe4
        .global func_ov002_021d8298
        .arm
func_ov002_021d8298:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, lr}
    sub sp, sp, #0x4
    mov r6, r0
    ldr r0, _LIT0
    and r3, r6, #0x1
    mul ip, r3, r0
    mov r5, r1
    ldr r4, [r5]
    ldr r0, _LIT1
    mov r1, r4, lsl #0x2
    mov r1, r1, lsr #0x18
    ldr r0, [r0, ip]
    mov r7, r4, lsl #0x12
    mov r1, r1, lsl #0x1
    mov r4, r2
    cmp r0, #0x0
    add r7, r1, r7, lsr #0x1f
    mov r0, #0x0
    bls .L_15c
    ldr r1, _LIT2
    add r2, r1, ip
    add r1, r2, #0x260
    ldr r2, [r2, #0x10]
.L_5c:
    ldr r8, [r1]
    mov lr, r8, lsl #0x2
    mov lr, lr, lsr #0x18
    mov r8, r8, lsl #0x12
    mov lr, lr, lsl #0x1
    add r8, lr, r8, lsr #0x1f
    cmp r7, r8
    bne .L_14c
    ldr r0, _LIT3
    ldr r1, [sp]
    ldr ip, [r0, ip]
    bic r1, r1, #0x200
    mov r0, r6, lsl #0x1f
    orr r0, r1, r0, lsr #0x16
    mov r1, #0x200
    rsb r1, r1, #0x0
    orr r0, r0, #0x400
    mov lr, r7, lsl #0x16
    orr r2, r3, #0x1a
    orr r2, r2, lr, lsr #0x10
    mov r2, r2, lsl #0x10
    mov ip, ip, lsl #0x16
    orr r3, r3, #0x16
    orr r3, r3, ip, lsr #0x10
    mov r3, r3, lsl #0x10
    and ip, r0, r1
    and r0, r7, r1, lsr #0x17
    orr ip, ip, r0
    mov r1, r2, lsr #0x10
    mov r2, r3, lsr #0x10
    mov r0, #0x3f
    mov r3, #0x0
    str ip, [sp]
    bl func_ov002_021d479c
    cmp r4, #0x0
    beq .L_f8
    mov r0, r6
    mov r1, r7
    bl func_ov002_021e278c
.L_f8:
    ldr r1, [r5]
    ldr r0, _LIT4
    mov r1, r1, lsl #0x13
    mov r3, r1, lsr #0x13
    cmp r3, r0
    bne .L_134
    mov r0, r6, lsl #0x1f
    and r0, r0, #-2147483648
    mov r1, r7, lsl #0x10
    orr r2, r0, #0x500000
    mov r0, r3, lsl #0x10
    orr r0, r2, r0, lsr #0x10
    mov r1, r1, lsr #0x10
    mov r2, #0x0
    bl func_ov002_021e30b4
.L_134:
    ldr r1, [sp]
    mov r0, #0x1a
    bl func_ov002_02244fe4
    add sp, sp, #0x4
    mov r0, #0x1
    ldmia sp!, {r3, r4, r5, r6, r7, r8, pc}
.L_14c:
    add r0, r0, #0x1
    cmp r0, r2
    add r1, r1, #0x4
    bcc .L_5c
.L_15c:
    mov r0, #0x0
    add sp, sp, #0x4
    ldmia sp!, {r3, r4, r5, r6, r7, r8, pc}
_LIT0: .word 0x00000868
_LIT1: .word data_ov002_022cf17c
_LIT2: .word data_ov002_022cf16c
_LIT3: .word data_ov002_022cf178
_LIT4: .word 0x000017cc
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| PerPlayerRowTable | (player&1)*0x868 | row base | ptr | ip = player*0x868 |
| data_ov002_022cf17c | [ip] (int) | 0x260 list count | int | r0 = list count; loop bound |
| data_ov002_022cf16c | ip+0x260 | 0x260 list ptr | int* | r1 = base of 0x260 list |
| data_ov002_022cf16c | ip+0x10 | f_010 (count) | int | r2 = [r2+0x10] for list entry limit |
| data_ov002_022cf178 | [ip] (int) | event count companion | int | r0 = ip-th event count |
| Ov002Slot | [r5]+word | slot word | u32 | lsl#19/lsr#19 vs 0x17cc |

## Recipe sketch

```c
extern int   data_ov002_022cf17c[];  /* 0x260-list count, indexed by player*0x868 words */
extern int   data_ov002_022cf16c[];  /* PerPlayerRowTable */
extern int   data_ov002_022cf178[];  /* event-count companion */
extern int func_ov002_021d479c(int kind, int a, int b, int c);
extern int func_ov002_021e278c(int player, int packed);
extern int func_ov002_021e30b4(int a, int b, int c);
extern int func_ov002_02244fe4(int kind, int a);

int func_ov002_021d8298(int player, int *slot_ptr, int flag) {
    int stride  = 0x868;
    int ip      = (player & 1) * stride;          /* byte offset */
    int word    = *slot_ptr;
    int packed  = (u16)(((word << 2) >> 24 << 1) + ((word << 18) >> 31));  /* r7 */

    /* load 0x260-list count via cf17c companion */
    int count17c = *(int *)((char *)data_ov002_022cf17c + ip);
    if (count17c == 0) return 0;

    char *ppt  = (char *)data_ov002_022cf16c;
    int  *list = (int *)(ppt + ip + 0x260);
    int   lim  = *(int *)(ppt + ip + 0x10);    /* f_010 */
    int   sp_slot = 0;

    for (int i = 0; i < count17c; i++, list++) {
        int entry  = *list;
        int epacked = (u16)(((entry << 2) >> 24 << 1) + ((entry << 18) >> 31));
        if (packed != epacked) continue;

        int from = *(int *)((char *)data_ov002_022cf178 + ip);
        /* build arg-packed call to func_021d479c(0x3f, ...) */
        int a1 = (u16)((word & ~0x200) | ((player << 31) >> 22) | 0x400);
        a1 = (int)(a1 & (-0x200)) | ((packed >> 0) & (0x1ff >> 7));
        int a2 = (u16)((player & 1) | 0x1a | ((packed << 22) >> 16));
        int a3 = (u16)((player & 1) | 0x16 | ((from << 22) >> 16));
        sp_slot = (int)(a1 & (-0x200)) | ((packed) & 0xff);
        func_ov002_021d479c(0x3f, a2, a3, 0);

        if (flag != 0)
            func_ov002_021e278c(player, packed);

        u32 slotid = ((u32)(*slot_ptr) << 19) >> 19;   /* id_lo13 */
        if (slotid == 0x17cc) {
            int a = (u16)(((player << 31) >> 31 << 31) | 0x500000 | (slotid << 16 >> 16));
            func_ov002_021e30b4(a, (u16)packed, 0);
        }

        func_ov002_02244fe4(0x1a, sp_slot);
        return 1;
    }
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_021d8414.c
**Why similar:** Same func_ov002_021d479c 4-arg call with bit-packed u16 args built from slot-word fields using `lsl/lsr` chains.
**Key lesson:** The `data_ov002_022cf17c` and `data_ov002_022cf178` companions use the same `(player & 1) * 0x868` byte offset as the PPT — they are parallel arrays, not flat int arrays indexed by a single value. Access them as `*(int *)((char *)array + ip)` where `ip = (player & 1) * 0x868`.

## Try this
1. Load the 0x260-list count as `*(int *)((char *)data_ov002_022cf17c + (player & 1) * 0x868)` — this is `data_ov002_022cf17c[player * 0x868 / 4]` in int-array terms. If that count is 0, return 0 immediately (the `bls .L_15c` early exit).
2. If objdiff shows the loop's `cmp r0,r2` / `bcc` in wrong register, note that r0 is the loop counter and r2 is the count from `[r2+0x10]` (PPT f_010) — the loop bound is from a DIFFERENT companion field than the list-count, so declare both separately.
3. The `id_lo13 == 0x17cc` check at `.L_f8` compares the original `*slot_ptr` word's id_lo13 (not the list entry) — read `*slot_ptr` again for this check rather than reusing the packed value.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00000868` | MMIO/const |
| `_LIT1` | `data_ov002_022cf17c` | raw |
| `_LIT2` | `data_ov002_022cf16c` | raw |
| `_LIT3` | `data_ov002_022cf178` | raw |
| `_LIT4` | `0x000017cc` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021d479c` | overlay call (ov002) |
| `bl` | `func_ov002_021e278c` | overlay call (ov002) |
| `bl` | `func_ov002_021e30b4` | overlay call (ov002) |
| `bl` | `func_ov002_02244fe4` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

