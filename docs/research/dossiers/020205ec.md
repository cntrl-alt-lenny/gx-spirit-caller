# 020205ec — func_020205ec (main, class C, 204B)

## Recipe + Risk

**Recipe:** early guard, 3-arg callback, >/== branch with 1<<(n-1), store-order
**Risk:** the >/== split: orig does cmp f8,fa; bls then bne so the (f8>fa) path stores f8&f6 while (f8==fa) path ORs into f6 -- three-way via two compares. mwcc if/else-if on > then == should reproduce, but the shared ip=(u16)p->f8 and shift 1<<(ip-1) reg reuse may rotate. reshape-able (explicit > then == ladder) + struct-guessed

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020205ec (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     early guard, 3-arg callback, >/== branch with 1<<(n-1), store-order
 *   risk:       the >/== split: orig does cmp f8,fa; bls then bne so the (f8>fa) path stores f8&f6 while (f8==fa) path ORs into f6 -- three-way via two compares. mwcc if/else-if on > then == should reproduce, but the shared ip=(u16)p->f8 and shift 1<<(ip-1) reg reuse may rotate. reshape-able (explicit > then == ladder) + struct-guessed
 *   confidence: low
 */
/* func_020205ec: register a depth p into the ctl bitmask state.
 * if (u16)p->f2 set -> abort path. else optional f38 callback. Then by
 * comparing ctl.f8 to p->fa: deeper(>) resets f8=fa,f6=bit; equal ORs bit
 * into f6 (bit = 1<<(p->f8-1)). Then drive func_02020550((u16)(p->f8+1)).
 */
struct P  { char pad0[2]; unsigned short f2; char pad4[0x8-0x4]; unsigned short f8; unsigned short fa; };
struct Ctl{ char pad0[6]; unsigned short f6; unsigned short f8; void (*f38)(void *, int, int); };
extern struct Ctl data_02191f40;
extern unsigned char data_020c67dc[];
extern void func_0201f138(int);
extern void func_0201f19c(void);
extern int  func_02020550(int);

void func_020205ec(struct P *p)
{
    int n;
    if (p->f2 != 0) {
        func_0201f19c();
        func_0201f138(9);
        return;
    }
    if (data_02191f40.f38 != 0)
        data_02191f40.f38(data_020c67dc, p->f8, p->fa);

    n = p->f8;
    if (data_02191f40.f8 > p->fa) {
        data_02191f40.f8 = p->fa;
        data_02191f40.f6 = (unsigned short)(1 << (n - 1));
    } else if (data_02191f40.f8 == p->fa) {
        data_02191f40.f6 = (unsigned short)(data_02191f40.f6 | (1 << (n - 1)));
    }
    {
        int r = func_02020550((unsigned short)(n + 1));
        if (r == 0x18) { func_0201f138(7); return; }
        if (r == 2) return;
        func_0201f138(9);
    }
}
```

## Struct context

Accesses `data_02191f40` (Ctl struct) and `struct P *p` (argument).

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| p+0x02 | f2 | unsigned short | early guard: non-zero triggers error path |
| p+0x08 | f8 | unsigned short | depth value: used as n for shift `1<<(n-1)` and comparison |
| p+0x0a | fa | unsigned short | target depth: compared against ctl.f8 for >/== dispatch |
| ctl+0x06 | f6 | unsigned short | bitmask field: reset (>-path) or OR-updated (==-path) |
| ctl+0x08 | f8 | unsigned short | current depth: compared to p->fa; reset to p->fa on >-path |
| ctl+0x?? | f38 | void(*)(void*,int,int) | optional 3-arg callback before comparison block |

Note: `f38` offset in `struct Ctl` is guessed (not verified); the `pad0[6]` + `f6` + `f8` layout is likely but struct-guessed.

## Closest matched example

**Path:** src/main/func_02020550.c (same batch)
**Why similar:** Both functions share the `1<<(n-1)` shift-by-register pattern and the `(unsigned short)(n+1)` u16-widen call to `func_02020550`; func_020205ec is the callback registered with func_020206b8 in func_02020550.
**Key lesson:** The three-way comparison (`ctl.f8 > p->fa` then `ctl.f8 == p->fa`) must be an if/else-if ladder in source to produce `cmp f8,fa; bls .skip_gt; ... bne .skip_eq; ...` — two compares on the same two operands. Using `data_02191f40.f8` vs `p->fa` must stay as direct field reads (not cached in a local) so the compiler re-evaluates the comparison from struct memory. If the two-compare sequence collapses to one compare with a branch, add explicit `else if` rather than sequential `if`.

## Try this

1. Build as-is. Check objdiff for the three-way cmp block: must see `cmp r_ctl_f8, r_p_fa; bls .Lskip_gt` then `bne .Lskip_eq` (two branches from the same two regs). If only one compare appears, add a comment between the `if` and `else if` to prevent CSE, or split into `if (...) { ... } else { if (...) { ... } }` nested form.
2. If the `(unsigned short)(1 << (n-1))` cast emits `and r?,#0xffff` instead of `lsl#16;lsr#16`, wrap the result: `unsigned short bit = (unsigned short)(1 << (n-1));` and assign `data_02191f40.f6 = bit;` to force the two-instruction zero-extend on the store.
3. If the f38 3-arg callback (`void (*)(void*,int,int)`) emits wrong argument types (e.g. u16 zero-extends on p->f8 and p->fa), declare the prototype with `int` args — the ldrh-loaded u16 fields naturally zero-extend to int when passed as `int` arguments without needing explicit widen instructions.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02191f40` | data symbol |
| `_LIT1` | `data_020c67dc` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0201f19c` | main TU call |
| `bl` | `func_0201f138` | main TU call |
| `blx` | `r3` |  |
| `bl` | `func_02020550` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

