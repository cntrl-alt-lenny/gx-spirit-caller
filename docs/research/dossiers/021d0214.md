# 021d0214 — func_ov011_021d0214 (ov011, class C, 188B)

## Recipe + Risk
**Recipe:** CLASS C: loop-copy with guard; copy only if field nonzero; stride-12
**Risk:** copy guard: if(src.f != 0) copy. stride-12: three fields per element.

## Prep draft

```c
/* func_ov011_021d0214 (ov011, cls C, 188B): loop over a stride-12 array;
 * for each element, if field[0] is nonzero, copy all three fields (offsets
 * 0, 4, 8) from src to dst. Sits inside the func_ov011_021cffe4.s span
 * (021cffe4–021d02a4); prep file not yet split. */

/* Adjust extern names once delinks.txt splits this out. */
extern char data_ov011_021d4660[];   /* or another stride-12 array */

/* Prototype: args inferred from recipe — src array ptr, dst array ptr, count */
void func_ov011_021d0214(void *src, void *dst, int count) {
    int i;
    int *s = (int *)src;
    int *d = (int *)dst;
    for (i = 0; i < count; i++) {
        if (s[0] != 0) {
            d[0] = s[0];
            d[1] = s[1];
            d[2] = s[2];
        }
        s += 3;   /* stride 12 = 3 ints */
        d += 3;
    }
}
```

## Struct context

This function operates on a stride-12 element array (12 bytes = 3×int per element).
This does NOT match the primary ov011 arrays (stride 0x28 = 40). Likely a local
struct or a different data region. No matching type in `docs/research/types/`.

| field | offset | type | purpose |
|-------|--------|------|---------|
| elem.f0 | 0 | int | guard field — tested for != 0 before copy |
| elem.f4 | 4 | int | second field — copied if guard passes |
| elem.f8 | 8 | int | third field — copied if guard passes |

Stride = 12 (0xC) = three 4-byte fields per element.

## Closest matched example
**Path:** src/overlay011/func_ov011_021d1884.c
**Why similar:** Both are counted loops with a per-element operation at stride, touching ov011 data arrays.
**Key lesson from that file:** Declare `char *p = data_…; unsigned int i = 0;` in that order so `i` lands in r5 and `p` in r4 (or vice versa), then use `p += stride` at the end of the loop body. The counted-loop pattern `for (; i < N; i++) { …; p += stride; }` reliably matches the orig's post-increment form.

## Try this
1. Run as-is with the exact struct of three int fields and the `if (s[0] != 0)` guard. If the prototype is wrong (e.g., a global array instead of pointer args), replace `src`/`dst` with `char *` pointers into known global arrays and a `count` from a data load — look in the parent stub `func_ov011_021cffe4.s` at the call site setting up r0/r1/r2 just before `bl func_ov011_021d0214`.
2. In objdiff, if the guard branch direction is inverted (`bne` vs `beq`), flip to `if (s[0] == 0) { s+=3; d+=3; continue; }` (skip the copy). Check that the loop counter register matches (cmp against count) and that the base pointer stays callee-saved across iterations.
3. If the 3-int copy emits separate stores instead of `stm`, try `typedef struct { int f0,f1,f2; } E12; *(E12*)d = *(E12*)s;` — that may trigger an ldm/stm pair. If register allocation is still off, escalate to permuter.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

