# 0200fb18 — func_0200fb18 (main, class C, 188B)

## Recipe + Risk

**Recipe:** C: base+0x1000 hoisted; signed (fc<<29)>>31 guard; bucket i*4 inline
**Risk:** guard mov;lsl#0x1d;lsrs#0x1f sets flags via movs and predicates the key cmp (ldreq/cmpeq/moveq); if mwcc materialises the bit to a reg + branch instead of the eq-predicate chain, the whole compare block diverges. reshape-able via bitfield

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0200fb18 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     C: base+0x1000 hoisted; signed (fc<<29)>>31 guard; bucket i*4 inline
 *   risk:       guard mov;lsl#0x1d;lsrs#0x1f sets flags via movs and predicates the key cmp (ldreq/cmpeq/moveq); if mwcc materialises the bit to a reg + branch instead of the eq-predicate chain, the whole compare block diverges. reshape-able via bitfield
 *   confidence: med
 */
/* func_0200fb18 (main, class C/D) — keyed lookup, type-5 single list vs bucket array.
 * base = data_0218cbc8 + 0x1000. arg0: type byte (signed) @0.
 *  type==5: walk list @+0x90c; else iterate [+0x914] buckets @+0x900+i*4.
 * node guard: (node->fc<<0x1d)>>0x1f == 0 (bit2 clear), then key(node->f8)==arg->f8. */

typedef struct Node {
    struct Node *next;  /* 0x0 */
    int          pad4;  /* 0x4 */
    int          key;   /* 0x8 */
    unsigned int fc;    /* 0xc : bitfield, bit2 tested */
} Node;

extern char data_0218cbc8;

Node *func_0200fb18(char *arg)
{
    char *base = &data_0218cbc8 + 0x1000;
    if (*(signed char *)arg == 5) {
        Node *n = *(Node **)(base + 0x90c);
        if (n != 0) {
            do {
                if (((n->fc << 0x1d) >> 0x1f) == 0) {
                    if (n->key == *(int *)(arg + 8)) return n;
                }
                n = n->next;
            } while (n != 0);
        }
        return 0;
    } else {
        unsigned int cnt = *(unsigned int *)(base + 0x914);
        unsigned int i = 0;
        if (cnt == 0) return 0;
        do {
            Node *n = *(Node **)(base + i * 4 + 0x900);
            if (n != 0) {
                do {
                    if (((n->fc << 0x1d) >> 0x1f) == 0) {
                        if (n->key == *(int *)(arg + 8)) return n;
                    }
                    n = n->next;
                } while (n != 0);
            }
            i++;
        } while (i < cnt);
        return 0;
    }
}
```

## Struct context

This function accesses `data_0218cbc8` (an unnamed BSS global) at `+0x1000` offset as its effective base. No SystemWork. The critical struct layout is the Node chain accessed through the base:

| offset | field | type | use |
|--------|-------|------|-----|
| base+0x90c | list head | `Node *` | type-5 path: start of single linked list |
| base+0x900+i*4 | bucket[i] | `Node *` | non-5 path: hash bucket array, cnt entries |
| base+0x914 | cnt | `unsigned int` | number of buckets to iterate |
| node+0x0 | next | `Node *` | linked-list forward pointer |
| node+0x8 | key | `int` | match key compared to `*(int*)(arg+8)` |
| node+0xc | fc | `unsigned int` | bit 2 tested via `(fc<<0x1d)>>0x1f == 0`; must use shift-pair, NOT `& 4` |

Note: `data_0218cbc8` is distinct from `data_0218dbc8` used by `func_0200fd1c`.

## Closest matched example

**Path:** src/main/func_0200eef0.c
**Why similar:** Similar size (~188 B), operates on a pointer offset from a named BSS global, uses a do-while walk over a linked list checking a guard field then a key field.
**Key lesson:** The node guard `(n->fc << 0x1d) >> 0x1f == 0` must emit `lsl#0x1d; lsrs#0x1f` where the `lsrs` sets the `Z` flag, allowing `ldreq/cmpeq/moveq` predication of the key comparison. If MWCC emits `lsl; lsr; cmp #0; bne` (materialising the bit to a register and branching), switch the guard to a `:1 bitfield` at bit 2: `unsigned int bit2 : 1` at position bit2 within `fc`, so MWCC sees a bitfield extract and may emit the `movs` idiom. Alternatively, try `if (!(n->fc & 4))` — which compiles to `tst #4; bne` — and accept that approach if the predication chain cannot be forced.

## Try this

1. Build as-is. In objdiff check the node guard: confirm `lsl r,fc,#0x1d` followed by `lsrs r,r,#0x1f` sets the `Z` flag and that the subsequent key comparison is predicated `cmpeq / moveq` (not a branch). If a `bne` appears instead, declare `fc` as a packed struct with `: 2` (bits 0..1) then `unsigned int bit2 : 1` (bit 2) and rewrite the guard as `if (n->bit2 == 0)`.
2. If the `base = &data_0218cbc8 + 0x1000` hoist diverges (objdiff shows the add inside the loop): declare `base` as a pointer computed once before the `if`; the draft already does this. Confirm `r3` or `ip` holds `base` unchanged across both the if and else branches.
3. Park as `.s` if the predication chain cannot be forced via bitfield or shift-pair and objdiff shows a branching form — this is reshape-able, not permuter-class, but if all C forms produce branches, accept as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0218cbc8` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

