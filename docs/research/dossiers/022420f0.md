# 022420f0 — func_ov002_022420f0 (ov002, class F retriage, 316B)

## Re-triage
**Original verdict:** F — previously intractable
**Upgrade reason:** DuelQueueState+0x5B8, PerPlayerRowTable stride 0x868, and Ov002Slot id_lo13 are
all documented. The reloc at 0x022420f8 calls func_ov002_022575c8 (slot-scan predicate), and the
surrounding block (0x022420dc loads data_ov002_022ce288, data_ov002_022cd744, data_ov002_022d016c;
0x022421d0 calls func_ov002_02203330) places this squarely in the DuelQueueState+Ov002Slot dispatch
family. The 316-byte size and the call to func_022575c8 at 0x022420f8 (8 bytes into the function)
suggest a short prologue switch followed by a slot scan.
**Confidence:** high
**Structs needed:** DuelQueueState (data_ov002_022ce288 +0x5B8), DuelStateSingleton (data_ov002_022d016c), PerPlayerRowTable (stride 0x868), Ov002Slot (id_lo13 lsl#19/lsr#19), data_ov002_022cd744 (player flag array)

## Assembly
```asm
; func_ov002_022420f0 — NOT YET EXTRACTED
; This function exists in the binary at 0x022420f0, between func_ov002_022420ec.s and func_ov002_0224215c.s.
; It has not been disassembled into a .s file yet (absent from src/overlay002/ and symbols.txt).
; Size: 316 bytes (0x13C). Confirmed via retriage doc + reloc coverage (lines 13192-13196).
;
; Key relocs at this address range:
;   0x022420dc  load    -> 0x022ce288   (data_ov002_022ce288 = DuelQueueState)
;   0x022420e4  load    -> 0x022cd744   (data_ov002_022cd744 = player flag array)
;   0x022420e8  load    -> 0x022d016c   (data_ov002_022d016c = DuelStateSingleton)
;   0x022420f8  arm_call -> 0x022575c8  (func_ov002_022575c8 — slot-scan predicate/action)
;   0x022421d0  arm_call -> 0x02203330  (func_ov002_02203330)
;   NOTE: 0x022420ec is a separate 1-instruction-stub func (func_ov002_022420ec.s = 68B);
;         0x022420f0 is the entry of THIS function (4 bytes after 022420ec stub end? likely gap).
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5B8 | sub_state | int | dispatch ladder index |
| data_ov002_022cd744 | +0x0 | player_flag | int[] | word-indexed by player bit: `ldr r0,[r0,r1,lsl#2]` |
| DuelStateSingleton | various | f_d44 / f_d70-78 | int | trigger guards and action args |
| PerPlayerRowTable | base+(player&1)*0x868 | row | ptr | mla stride |
| Ov002Slot | +0x008 | id_lo13 | :13 bitfield | lsl#19/lsr#19 occupancy test |
| func_022575c8 | — | slot-scan action | fn | called with (card, slot_idx) per occupied slot |
| func_02203330 | — | state setter | fn | called on transition |

## Recipe sketch

Based on func_ov002_0224003c (same 6-arm DuelQueueState pattern, same struct set):

```c
int func_ov002_022420f0(CardObj *card) {
    DuelQueueState     *dqs = &data_ov002_022ce288;
    DuelStateSingleton *dss = &data_ov002_022d016c;

    switch (dqs->sub_state) {  /* +0x5B8 */
    default: return 0;
    case 0: {
        /* player flag check via data_ov002_022cd744 */
        int player_bit = (card->f2 >> 0) & 1;
        int player_flag = data_ov002_022cd744[player_bit];
        if (!player_flag) { return 1; }
        /* slot scan via func_022575c8 */
        Ov002FieldZone *row = (Ov002FieldZone *)((u8 *)&data_ov002_022cf16c
                                + (player_bit & 1) * 0x868);
        for (int s = 0; s < 5; s++) {
            if (row->slots[s].id_lo13 == 0) continue;
            func_ov002_022575c8(card, s);
        }
        dqs->sub_state++;
        return 0;
    }
    case 1:
        func_ov002_02203330(card);
        dqs->sub_state++;
        return 0;
    case 2:
        if (!dss->f_d44) return 1;
        {
            int player_bit = (card->f2 >> 0) & 1;
            func_ov002_0225935c(player_bit, card->f0, &func_ov002_0223fd74);
            dqs->sub_state++;
        }
        return 0;
    case 3:
        if (!func_ov002_022593f4()) return 0;
        func_ov002_0223dcc0(card, dss->f_d70, dss->f_d74 + dss->f_d78);
        dqs->sub_state++;
        return 0;
    }
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_0224003c.s
**Why similar:** Immediately preceding function in the same sub-state dispatch family; identical load
of data_ov002_022cd744, data_ov002_022ce288, data_ov002_022d016c; same func_022575c8 slot-scan call.
**Key lesson:** data_ov002_022cd744 is a word array indexed by player bit via `ldr r0,[r0,r1,lsl#2]`
— declare as `extern int data_ov002_022cd744[]` and index by `player_bit` (not byte-indexed). The
func_022575c8 call at 0x022420f8 (very early in the function) suggests it may be the case-0 body
before the sub_state switch — if the switch is 0-based and case 0 is the slot scan, the initial
`bl func_022575c8` is the guard or primary action.

## Try this
1. Declare `int sub_state = dqs->sub_state` at +0x5B8; case 0: load `data_ov002_022cd744[player_bit]` (word array, exact extern name `data_ov002_022cd744`) then call `func_ov002_022575c8(card, slot)` for each occupied slot where `row->slots[s].id_lo13 != 0`.
2. If objdiff shows sub_state dispatch at wrong index: try case 0 = func_022575c8 guard only (no loop), with the loop body in case 1 — the reloc at +0x08 (022420f8) may be the case-0 single call.
3. If func_02203330 call timing is wrong: it may be a post-loop state setter called unconditionally at the end of case 0, not a separate case — escalate if rearranging doesn't help.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

