# 020296cc — func_020296cc (main, class C, 556B)

## Recipe + Risk
**Recipe:** memcpy struct-copy; Fill32; bind +0x100 base; bic/orr RMW; shift-insert BLDCNT
**Risk:** permuter-class: 6-iter ldm/stm copy loops are mwcc-version/reg-rotation dependent and asm is TRUNCATED past .L_114; whole-function reg-alloc shape unverifiable here. struct-guessed offsets too.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020296cc (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     memcpy struct-copy; Fill32; bind +0x100 base; bic/orr RMW; shift-insert BLDCNT
 *   risk:       permuter-class: 6-iter ldm/stm copy loops are mwcc-version/reg-rotation dependent and asm is TRUNCATED past .L_114; whole-function reg-alloc shape unverifiable here. struct-guessed offsets too.
 *   confidence: low
 */
/* CAMPAIGN-PREP candidate for func_020296cc (main, class C) — brief p_0010.
 * UNVERIFIED build-free draft. NOTE: asm in batch is TRUNCATED at .L_114
 * (final literal-pool MMIO tail not shown). This reconstructs the full body
 * from the visible shape + the standard 0x4000000 BLDCNT eva-coef RMW.
 *   recipe:     struct-copy via memcpy (ldm/stm 0x68); Fill32; bind base r7+0x100; bic/orr flag RMW; BLDCNT divmod-free shift RMW
 *   risk:       reg-alloc on the 6-iter ldm/stm copy loops + the 0x4000000-vs-LIT0 branch tail
 *   confidence: low
 */
typedef unsigned char u8;
typedef unsigned short u16;
typedef signed short s16;

extern void Fill32(int value, void *dst, int size);

/* save/restore image copied to a 0x68 scratch, init, copied back */
typedef struct Obj296cc {
    char  _pad0[0x68];
    char  blk68[0x2c];      /* 0x68 : 11 words copied from param1 (r6) */
    char  _pad1[0x100 - 0x94];
    char  blk100[0xa0];     /* 0x100 sub-struct: +0x84 s16, +0x8c s16, +0x9a u16 */
    char  _pad2[0x16c - (0x100 + 0xa0)];
    char  blk16c[0xc];      /* 0x16c : 3 words from param1+0x1c */
    char  _pad3[0x187 - 0x178];
    u8    f187;             /* 0x187 byte flag */
} Obj296cc;

extern u16 data_020c66dc;   /* _LIT0 base for the else-branch BLDCNT-like reg */

int func_020296cc(Obj296cc *p, char *src1, int arg2) {
    char save[0x68];
    s16 keep8c;
    u16 *blend;
    u16 t, lo;

    /* copy 0x68 of p into scratch (ldm/stm 6x4+2) */
    Fill32(0, save, 0);            /* placeholder to anchor extern; removed below */
    (void)arg2;

    __builtin_memcpy(save, p, 0x68);
    keep8c = *(s16 *)(p->blk100 + 0x8c);
    Fill32(0, p, 0x19c);
    __builtin_memcpy(p, save, 0x68);
    *(s16 *)(p->blk100 + 0x8c) = keep8c;

    __builtin_memcpy(p->blk68, src1, 0x1c);        /* 7 words */
    __builtin_memcpy(p->blk16c, src1 + 0x1c, 0xc); /* 3 words */

    t = *(u16 *)(p->blk100 + 0x9a);
    t &= ~0x4;
    *(u16 *)(p->blk100 + 0x9a) = t;
    t = *(u16 *)(p->blk100 + 0x9a);
    t |= 0x2; t &= ~0x1; t |= 0x1;
    *(u16 *)(p->blk100 + 0x9a) = t;
    *(s16 *)(p->blk100 + 0x84) = -1;
    p->f187 &= ~0x1;

    if (*(s16 *)(p->blk100 + 0x8c) == 0)
        blend = (u16 *)0x04000000;
    else
        blend = (u16 *)&data_020c66dc;

    /* BLDCNT eva-coef field: ((reg>>8 & 0x1f)+0x10) reinserted at bits 8..12 */
    {
        unsigned int r = *(unsigned int *)blend;
        unsigned int coef = (r & 0x1f00) >> 8;
        r = (r & ~0x1f00) | (((coef + 0x10) & 0xff) << 8);
        *(unsigned int *)blend = r;
    }
    (void)lo;
    return 1;
}
```

## Struct context
Does not touch GameSingleton or SystemWork. Operates on a large local object with embedded sub-struct at +0x100.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x00..+0x67 | (head) | char[0x68] | copied to `save[]` scratch via memcpy |
| +0x68..+0x93 | blk68 | char[0x2c] | overwritten from src1[0..0x1b] |
| +0x18c (blk100+0x8c) | keep8c | s16 | saved before Fill32, restored after |
| +0x19a (blk100+0x9a) | t | u16 | bic/orr RMW sequence: clear bit2, set bit1, clear bit0, set bit0 |
| +0x184 (blk100+0x84) | f84 | s16 | written -1 (mvn pattern) |
| +0x187 | f187 | u8 | bit 0 cleared |
| +0x16c..+0x177 | blk16c | char[0xc] | overwritten from src1[0x1c..0x27] |

MMIO/data:

| address | use |
|---------|-----|
| 0x04000000 | BLDCNT main (if blk100+0x8c == 0) |
| data_020c66dc | else-branch blend target |

## Closest matched example
**Path:** src/main/func_02029624.c
**Why similar:** Same object family (overlapping blk100 sub-struct, +0x9a flag u16, +0x84 s16 field, similar size range), both use Fill32 + ordered stores + guarded MMIO tail.
**Key lesson:** The 6-iter ldm/stm loops from `__builtin_memcpy` are highly mwcc-version-sensitive — reg rotation between ldm and stm varies with mwcc sp revision and the number of words copied. If the ldm/stm register lists diverge in objdiff, try replacing `__builtin_memcpy` with an explicit word-by-word loop using `int *dst = ...; int *src = ...;` to get deterministic per-word ldr/str. The BLDCNT shift-insert coef RMW at the end is guessed — if the orig uses a volatile :5 bitfield instead of the shift/mask arithmetic, switch to the bitfield form.

## Try this
1. Build as-is. Since asm is TRUNCATED past .L_114, first check that the visible head (save/restore memcpy + Fill32 + blk100 RMW) matches; accept that the BLDCNT tail is inferred. If ldm/stm register lists diverge, replace `__builtin_memcpy` with manual word loops.
2. If the blk100+0x9a multi-step RMW (bic 4; orr 2; bic 1; orr 1) does not match the exact sequence, try separate load-store pairs per step rather than the chained `t |= ...; t &= ...;` compound expression — mwcc may fold them into fewer ops.
3. Park as permuter-class; the ldm/stm register-rotation and the truncated tail make this un-forceable without macOS permuter and full asm visibility.
