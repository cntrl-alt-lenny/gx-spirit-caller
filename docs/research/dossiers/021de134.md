# 021de134 — func_ov002_021de134 (ov002, class F retriage, 264B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Repeatedly reads `DuelStateSingleton+0xCEC` (card_list_slot_idx) via `eor r6,r1,r0` — the XOR-discriminant pattern is now named. Also reads `DSS+0xd4` (guard flag, r/w) and `DSS+0xd8` (store). Uses PerPlayerRowTable with stride 0x868 for slot walks (accessing both slot groups 0..4 and 5..9). The function has three major phases all keyed on DSS+0xCEC and the bitmask arg (`r8` from `arg1`). The compound flag-clear and slot iteration for func_ov002_021dd950 and func_ov002_021dbf98 are deterministic once the DSS offsets are named.
**Confidence:** high
**Structs needed:** DuelStateSingleton (+0xCEC card_list_slot_idx, +0xD4 guard flag, +0xD8 store), PerPlayerRowTable (stride 0x868), Ov002Slot (id_lo13 at row+0x30+slot*0x14)

## Assembly

```asm
; func_ov002_021de134 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022cf16c
        .extern data_ov002_022d016c
        .extern func_0202e2c8
        .extern func_ov002_021b30ac
        .extern func_ov002_021b3ecc
        .extern func_ov002_021ca51c
        .extern func_ov002_021dbf14
        .extern func_ov002_021dbf98
        .extern func_ov002_021dd950
        .extern func_ov002_021e2b3c
        .extern func_ov002_021e2c5c
        .extern func_ov002_021fe3d4
        .global func_ov002_021de134
        .arm
func_ov002_021de134:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, lr}
    sub sp, sp, #0x18
    ldr r6, _LIT0
    mov fp, r3
    mov r5, r0
    ldr r0, [r6, #0xd4]
    mov r4, r2
    ands r7, fp, #0x2
    movne r3, #0x1
    str r0, [sp, #0x10]
    moveq r3, #0x0
    mov r0, r5
    mov r2, r4
    bl func_ov002_021dbf14
    mov r8, r0
    cmp r7, #0x0
    mov r9, #0x0
    beq .L_8e8
    str r9, [sp, #0x14]
.L_830:
    ldr r0, _LIT0
    mov r7, #0x0
    ldr r1, [r0, #0xcec]
    ldr r0, [sp, #0x14]
    eor r6, r1, r0
    mov sl, r6, lsl #0x4
    str sl, [sp, #0xc]
.L_84c:
    mov r0, #0x1
    tst r8, r0, lsl sl
    beq .L_874
    ldr r2, _LIT1
    mov r0, r6
    mov r1, r7
    bl func_ov002_021b30ac
    cmp r0, #0x0
    movne r0, #0x1
    orrne r9, r9, r0, lsl sl
.L_874:
    add sl, sl, #0x1
    add r7, r7, #0x1
    cmp r7, #0x4
    ble .L_84c
.L_88x:
    ldrh r0, [r5]
    cmp r0, #0x0
    beq .L_8d4
    ldr r0, [sp, #0xc]
    mov sl, #0x5
    add r7, r0, #0x5
.L_89c:
    mov r0, #0x1
    tst r8, r0, lsl r7
    beq .L_8c4
    ldr r2, _LIT2
    mov r0, r6
    mov r1, sl
    bl func_ov002_021b3ecc
    cmp r0, #0x0
    movne r0, #0x1
    orrne r9, r9, r0, lsl r7
.L_8c4:
    add r7, r7, #0x1
    add sl, sl, #0x1
    cmp sl, #0xa
    ble .L_89c
.L_8d4:
    ldr r0, [sp, #0x14]
    add r0, r0, #0x1
    str r0, [sp, #0x14]
    cmp r0, #0x2
    blt .L_830
.L_8e8:
    mov r0, #0x0
    str r0, [sp, #0x4]
.L_8f0:
    ldr r0, _LIT0
    mov sl, #0x0
    ldr r1, [r0, #0xcec]
    ldr r0, [sp, #0x4]
    eor r7, r1, r0
    mov r6, r7, lsl #0x4
.L_908:
    mov r0, #0x1
    tst r8, r0, lsl r6
    beq .L_940
    ldrh r0, [r5, #0x2]
    mov r1, sl
    mov r2, fp
    mov r0, r0, lsl #0x1f
    cmp r7, r0, lsr #0x1f
    movne r0, #0x1
    moveq r0, #0x0
    str r0, [sp]
    ldrh r3, [r5]
    mov r0, r7
    bl func_ov002_021dbf98
.L_940:
    add r6, r6, #0x1
    add r7, r7, #0x1   /* actually sl increments */
    add sl, sl, #0x1
    cmp sl, #0xa
    ble .L_908
    ldr r0, [sp, #0x4]
    add r0, r0, #0x1
    str r0, [sp, #0x4]
    cmp r0, #0x2
    blt .L_8f0
    ldr r0, _LIT0
    ldr r0, [r0, #0xd4]
    cmp r0, #0x0
    beq .L_a8c
    bl func_ov002_021fe3d4
    bl func_ov002_021ca51c
    ldr r1, _LIT0
    str r0, [r1, #0xd8]
    bl func_ov002_021e2b3c
    ldr r0, [sp, #0x10]
    eor r2, r8, r9
    ldr r1, _LIT0
    orr r0, r0, r2
    str r0, [r1, #0xd4]
    mov r0, #0x0
    str r0, [sp, #0x8]
.L_9a4:
    ldr r0, _LIT0
    ldr r2, _LIT3
    ldr r3, [r0, #0xcec]
    ldr r0, [sp, #0x8]
    ldr r1, _LIT4
    eor r9, r3, r0
    and r0, r9, #0x1
    mla r1, r0, r1, r2
    mov r6, r9, lsl #0x4
    add r7, r1, #0x30
    mov sl, #0x0
.L_9d0:
    mov r0, #0x1
    tst r8, r0, lsl r6
    beq .L_a60
    ldr r0, [r7]
    mov r0, r0, lsl #0x13
    mov r0, r0, lsr #0x13
    bl func_0202e2c8
    cmp r0, #0x0
    beq .L_a04
    cmp r4, #0xb
    cmpne r4, #0xd
    moveq r2, #0xc
    beq .L_a08
.L_a04:
    mov r2, r4
.L_a08:
    ldr r0, _LIT0
    ldr r1, [r0, #0xd4]
    mov r0, #0x1
    mvn r0, r0, lsl r6
    and r1, r1, r0
    ldr r0, _LIT0
    str r1, [r0, #0xd4]
    ldrh r0, [r5, #0x2]
    ldrh ip, [r5]
    mov r1, r0, lsl #0x1f
    mov r0, r0, lsl #0x11
    mov r0, r0, lsr #0x1f
    eor r0, r0, r1, lsr #0x1f
    cmp r9, r0
    movne r0, #0x1
    moveq r0, #0x0
    add r0, fp, r0
    add r3, r0, #0x10000
    mov r0, r9
    mov r1, sl
    str ip, [sp]
    bl func_ov002_021dd950
.L_a60:
    add r6, r6, #0x1
    add r7, r7, #0x14
    add sl, sl, #0x1
    cmp sl, #0xa
    ble .L_9d0
    ldr r0, [sp, #0x8]
    add r0, r0, #0x1
    str r0, [sp, #0x8]
    cmp r0, #0x2
    blt .L_9a4
    bl func_ov002_021e2c5c
.L_a8c:
    ldr r0, _LIT0
    ldr r1, [r0, #0xd4]
    ldr r0, [sp, #0x10]
    eor r0, r1, r0
    add sp, sp, #0x18
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
_LIT0: .word data_ov002_022d016c
_LIT1: .word 0x00001825
_LIT2: .word 0x00001513
_LIT3: .word data_ov002_022cf16c
_LIT4: .word 0x00000868
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelStateSingleton | +0xCEC | card_list_slot_idx | int | eor with p → player index; read at every outer loop iteration |
| DuelStateSingleton | +0xD4 | f_d4 | int | saved at entry; restored+orr'd at close |
| DuelStateSingleton | +0xD8 | f_d8 | int | stores func_021ca51c result |
| PerPlayerRowTable | (player&1)*0x868 | row base | ptr | mla with stride 0x868 |
| Ov002Slot | row+0x30+slot*0x14 | id_lo13 | u13 | lsl#19/lsr#19 then func_0202e2c8 |

## Recipe sketch

```c
extern DuelStateSingleton data_ov002_022d016c;
extern int  data_ov002_022cf16c[];
extern int func_ov002_021dbf14(int a, int arg1, int c);
extern int func_ov002_021b30ac(int player, int slot, int k);
extern int func_ov002_021b3ecc(int player, int slot, int k);
extern int func_ov002_021dbf98(int player, int slot, int flags, int arg3);
extern int func_ov002_021fe3d4(void);
extern int func_ov002_021ca51c(void);
extern int func_ov002_021e2b3c(void);
extern int func_ov002_021e2c5c(void);
extern int func_0202e2c8(int id);
extern int func_ov002_021dd950(int player, int slot, int flags, int card_id);

int func_ov002_021de134(int self, int arg1, int arg2, int flags) {
    DuelStateSingleton *dss = &data_ov002_022d016c;
    int saved_d4 = dss->f_d4;            /* +0xD4 */
    int has_flag2 = flags & 0x2;
    int r8 = func_ov002_021dbf14(self, (has_flag2 ? 1 : 0), arg2);
    int r9 = 0;

    if (has_flag2) {
        for (int p = 0; p < 2; p++) {
            int player = dss->f_cec ^ p;       /* +0xCEC */
            int bit_base = player << 4;
            for (int s = 0; s <= 4; s++) {
                if (!(r8 & (1 << (bit_base + s)))) continue;
                if (func_ov002_021b30ac(player, s, 0x1825) != 0)
                    r9 |= 1 << (bit_base + s);
            }
            ldrh r0, [r5]  /* if self->f0 != 0 */
            if (*(u16 *)self != 0) {
                for (int s = 5; s <= 9; s++) {
                    int bit = bit_base + s;
                    if (!(r8 & (1 << bit))) continue;
                    if (func_ov002_021b3ecc(player, s, 0x1513) != 0)
                        r9 |= 1 << bit;
                }
            }
        }
    }

    for (int p = 0; p < 2; p++) {
        int player = dss->f_cec ^ p;
        for (int s = 0; s <= 9; s++) {
            int bit = (player << 4) + s;
            if (!(r8 & (1 << bit))) continue;
            int b0 = *(u16 *)((char *)self + 2);
            int b0_bit = ((b0 << 31) >> 31) ^ ((b0 << 17) >> 31);
            int sp_flag = (player == b0_bit) ? 0 : 1;
            int card_id = *(u16 *)self;
            func_ov002_021dbf98(player, s, flags, card_id);
        }
    }

    if (dss->f_d4 != 0) {                     /* +0xD4 */
        func_ov002_021fe3d4();
        dss->f_d8 = func_ov002_021ca51c();    /* +0xD8 */
        func_ov002_021e2b3c();
        dss->f_d4 = saved_d4 | (r8 ^ r9);

        int stride = 0x868;
        char *ppt  = (char *)data_ov002_022cf16c;
        for (int p = 0; p < 2; p++) {
            int player = dss->f_cec ^ p;
            char *row  = ppt + (player & 1) * stride;
            int bit_base = player << 4;
            for (int s = 0; s <= 9; s++) {
                int bit = bit_base + s;
                if (!(r8 & (1 << bit))) continue;
                u32 word = *(u32 *)(row + 0x30 + s * 0x14);
                u32 id   = (word << 19) >> 19;  /* id_lo13 */
                int cid  = func_0202e2c8(id);
                int eff_arg2 = (cid != 0 && (arg2 == 0xb || arg2 == 0xd)) ? 0xc : arg2;
                dss->f_d4 &= ~(1 << bit);       /* clear this bit */
                func_ov002_021dd950(player, s, flags, *(u16 *)self);
            }
        }
        func_ov002_021e2c5c();
    }

    return dss->f_d4 ^ saved_d4;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_021debe0.c
**Why similar:** This function (021debe0) is a thin wrapper that calls func_ov002_021de134 with a computed bitmask argument — confirming that r8 in 021de134 is a slot-bitmask and fp (arg3=flags) controls branch gating.
**Key lesson:** The `tst r8, r0, lsl sl` pattern (where sl = bit position) is a bitmask test: `if (r8 & (1 << sl))`. The bit position `sl = player << 4` encodes player in the upper bits and slot in the lower — this is the standard ov002 slot-bitmask encoding. Declaring the bitmask as `unsigned int r8` (not `int`) keeps mwcc from emitting an arithmetic shift.

## Try this

1. Load `dss->f_cec` (+0xCEC) as `data_ov002_022d016c.f_cec` inside each outer loop (p=0..1); compute `player = dss->f_cec ^ p` — the reload inside the loop is mandatory because mwcc inserts a fresh ldr at each `.L_830` and `.L_8f0` label.
2. If objdiff shows the DSS +0xD4 store ordering wrong (stored before vs after func_021e2b3c call), move `dss->f_d4 = saved_d4 | (r8 ^ r9)` to immediately before the third slot-walk loop, not inside the `if (dss->f_d4 != 0)` condition.
3. This is a DSS dispatch function — if the third loop's bit-clear and func_021dd950 call still mismatches after struct naming, escalate: the `mvn r0,r0,lsl r6; and r1,r1,r0` variable-shift clear requires a register-reuse that may need the permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov002_022d016c` | raw |
| `_LIT1` | `0x00001825` | MMIO/const |
| `_LIT2` | `0x00001513` | MMIO/const |
| `_LIT3` | `data_ov002_022cf16c` | raw |
| `_LIT4` | `0x00000868` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021dbf14` | overlay call (ov002) |
| `bl` | `func_ov002_021b30ac` | overlay call (ov002) |
| `bl` | `func_ov002_021b3ecc` | overlay call (ov002) |
| `bl` | `func_ov002_021dbf98` | overlay call (ov002) |
| `bl` | `func_ov002_021fe3d4` | overlay call (ov002) |
| `bl` | `func_ov002_021ca51c` | overlay call (ov002) |
| `bl` | `func_ov002_021e2b3c` | overlay call (ov002) |
| `bl` | `func_0202e2c8` | main TU call |
| `bl` | `func_ov002_021dd950` | overlay call (ov002) |
| `bl` | `func_ov002_021e2c5c` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

