# 021cab6c — func_ov006_021cab6c (ov006, class C, 156B)

## Recipe + Risk

**Recipe:** decl-order = first-use; signed >>1 via (x<<k)>>1; if-assign clamp
**Risk:** decl-order critical: first-use order = register assignment. >>1 signed: (x<<k)>>k gives asr-based bitfield extract.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021cab6c (ov006, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     decl-order = first-use; signed >>1 via (x<<k)>>1; if-assign nest for cmpge/cmple chains
 *   risk:       permuter-class: loop body rotates r4/r5/r8 (center,scratch,idx); mwcc will likely pick a different scratch reg for ((k<<6)>>1), diverging the loop-interior allocation.
 *   confidence: low
 */
/* func_ov006_021cab6c: per-column point-in-box hit test.
   r0=obj*, r1=px, r2=py, r3=out{flag,index}. cls C (loop reg-alloc). */

typedef struct {
    char _pad58[0x58];
    int  f58;   /* 0x58 width-ish */
    int  f5c;   /* 0x5c */
    int  f60;   /* 0x60 count */
    char _pad64[0x78 - 0x64];
    int  f78;   /* 0x78 center x base */
    int  f7c;   /* 0x7c center y base */
} Obj_021cab6c;

typedef struct {
    int flag;
    int index;
} HitOut_021cab6c;

int func_ov006_021cab6c(Obj_021cab6c *o, int px, int py, HitOut_021cab6c *out)
{
    int count;
    int cy;
    int cx;
    int top, bottom;
    int idx;
    int k;

    out->flag = 0;
    out->index = 0;
    count = o->f60;
    if (count > 0) {
        cy = o->f7c + (o->f5c << 3) - 0x16;
        cx = o->f78 + ((o->f58 << 3) >> 1);
        bottom = cy + 0xc;
        top    = cy - 0xc;
        k = 1 - count;
        idx = 0;
        do {
            int center = cx + ((k << 6) >> 1);
            if (px >= center - 0x1c && py >= top) {
                if (px <= center + 0x1c && py <= bottom) {
                    out->flag = 1;
                    out->index = idx;
                    return 1;
                }
            }
            idx++;
            k += 2;
        } while (idx < count);
    }
    out->flag = 0;
    return 0;
}
```

## Struct context

`Obj_021cab6c` — inline struct in prep. Accessed fields:

| field | offset | type | purpose |
|-------|--------|------|---------|
| `f58` | +0x58 | int | width parameter; `(f58 << 3) >> 1` = half-width in pixels |
| `f5c` | +0x5c | int | Y offset factor; `f5c << 3` added to f7c for cy |
| `f60` | +0x60 | int | column count (loop bound) |
| `f78` | +0x78 | int | center X base coordinate |
| `f7c` | +0x7c | int | center Y base coordinate |

`HitOut_021cab6c` is a 2-int output: `flag` at +0 (1 on hit, 0 on miss), `index` at +4 (matched column index).

The `(x << k) >> 1` pattern throughout encodes signed arithmetic right-shift-based half-values: `(o->f58 << 3) >> 1` is `f58 * 4` (signed), and `(k << 6) >> 1` is `k * 32` (signed). The recipe note says `>>1 signed: (x<<k)>>1` — these must stay as shift expressions, not as multiplications, to produce `asr #1` in the output.

## Closest matched example

**Path:** src/overlay006/func_ov006_021c9bc8.c (candidate in this batch — see its dossier)
**Why similar:** both are point-in-box hit tests iterating over the same grid structure with outer/inner bounds from `base + 0x78/0x7c`; 021cab6c adds signed shift arithmetic and a different cell-shape (column-based rather than grid-based).
**Key lesson from that file:** decl-order controls register allocation for all local variables — the order in which variables are declared (NOT the order they are used) determines the prologue stmdb register list. The prep declares `count, cy, cx, top, bottom, idx, k` in that order; `center` is inside the loop body and gets a temporary register.

## Try this

1. Build as-is. The decl-order is the primary risk — if objdiff shows `center` mapped to a different scratch register (r4/r5/r8), try moving `int center` out of the do-while body and declaring it BEFORE `idx` and `k` in the outer scope to shift its register assignment. If the risk note says this is permuter-class, move directly to step 3 if step 1 doesn't match.
2. If the signed shift `(k << 6) >> 1` generates `lsl #6 / asr #1` but objdiff expects a plain `mul`, try `k * 32` instead — but only if the asm shows a `mul` not an `asl/asr` pair. The `(o->f58 << 3) >> 1` expression should similarly produce `lsl #3 / asr #1`; if it shows `mov r, r lsl #2` (net result is *4), try `o->f58 * 4` instead.
3. Low-confidence + permuter-class: if step 1-2 give a loop body that matches but the interior register rotation (r4/r5/r8 cycling) is off, submit to permuter. Park as .s if permuter plateaus.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

