# 0208e7ac — func_0208e7ac (main, class C, 176B)

## Recipe + Risk
**Recipe:** store-order verbatim incl. extra mid 454=0; e518 before e4ec; volatile pins order
**Risk:** store-order: the extra 0x4000454=0 (vs e700) is the whole delta; if omitted/merged the store stream diverges. reshape-able (source order). zero-reg sharing (r3) is scheduling, permuter-class residual.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208e7ac (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store-order verbatim incl. extra mid 454=0; e518 before e4ec; volatile pins order
 *   risk:       store-order: the extra 0x4000454=0 (vs e700) is the whole delta; if omitted/merged the store stream diverges. reshape-able (source order). zero-reg sharing (r3) is scheduling, permuter-class residual.
 *   confidence: med
 */
/* func_0208e7ac (main, class C/D) — sibling of e700; ONE EXTRA 0x4000454=0 store.
 * *0x4000600 |= 0x8000; poll func_0208e518(&a) until 0; poll func_0208e4ec(&b) until 0;
 * store sequence (note the extra 454=0 before 440=2):
 *   0x4000440=3; 0x4000454=0; 0x4000440=0; if(b) 0x4000448=b; 0x4000454=0; 0x4000440=2; 0x4000448=a; 0x4000454=0.
 * The doubled adjacent 454=0 lets mwcc CSE the constant 0 (orig keeps it in r3). */
typedef unsigned int u32;

extern int func_0208e518(u32 *out);
extern int func_0208e4ec(u32 *out);

void func_0208e7ac(void)
{
    u32 a;
    u32 b;

    *(volatile u32 *)0x04000600 |= 0x8000;

    while (func_0208e518(&a) != 0)
        ;
    while (func_0208e4ec(&b) != 0)
        ;

    *(volatile u32 *)0x04000440 = 3;
    *(volatile u32 *)0x04000454 = 0;
    *(volatile u32 *)0x04000440 = 0;
    if (b != 0)
        *(volatile u32 *)0x04000448 = b;
    *(volatile u32 *)0x04000454 = 0;
    *(volatile u32 *)0x04000440 = 2;
    *(volatile u32 *)0x04000448 = a;
    *(volatile u32 *)0x04000454 = 0;
}
```

## Struct context
No struct globals; all access is direct MMIO.

| Address | Register | Access |
|---------|----------|--------|
| `0x04000600` | GXSTAT/state register | RMW or-in 0x8000 at start |
| `0x04000440` | Control register | written 3, then 0, then 2 (in order) |
| `0x04000448` | Data register | conditional write of `b`, then unconditional write of `a` |
| `0x04000454` | Status/sync register | written 0 THREE times: after 440=3, after cond-write-b, and at tail |

## Closest matched example
**Path:** src/main/func_0208e700.c (prep draft — direct sibling)
**Key lesson:** This function is byte-for-byte identical to `func_0208e700` except for one extra `*(volatile u32 *)0x04000454 = 0;` inserted after the conditional `if (b != 0) 0x40004448=b` block and before `0x04000440=2`. That single extra store is the entire structural delta. The orig keeps the constant 0 in r3 across all three stores to 0x04000454, so mwcc can share the zero register — if the compiler re-materialises 0 each time the scheduling diverges.

## Try this
1. Count the `*(volatile u32 *)0x04000454 = 0;` stores in the assembled output — there must be exactly three: one after `440=3`, one after the `if(b)` block, and one at the tail. If objdiff shows only two (matching sibling func_0208e700), the mid-store is missing — add `*(volatile u32 *)0x04000454 = 0;` between the `if(b)` block and `*(volatile u32 *)0x04000440 = 2;`.
2. Verify the full 8-store sequence: `440=3 → 454=0 → 440=0 → (cond)448=b → 454=0 → 440=2 → 448=a → 454=0`. All stores must be `volatile` to prevent coalescing.
3. If the zero-register assignment (r3 shared across multiple 454=0 stores) diverges from orig after the above fixes, escalate the residual as a scheduling/permuter-class issue — the register-sharing is not forceable by C reshape.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x04000600` | MMIO/const |
| `_LIT1` | `0x04000440` | MMIO/const |
| `_LIT2` | `0x04000454` | MMIO/const |
| `_LIT3` | `0x04000448` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208e518` | main TU call |
| `bl` | `func_0208e4ec` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

