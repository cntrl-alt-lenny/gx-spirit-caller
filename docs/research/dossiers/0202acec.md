# 0202acec — func_0202acec (main, class D, 268B)

## Recipe + Risk
**Recipe:** explicit per-case inserts; masked-reg first then payload (A|(B<<8)); store-order preserved
**Risk:** reshape-able: orig keeps reg base in r3 and reloads payload de/e0 from the struct each store; if mwcc binds the second store's reg via r[1]=base+2 differently or reorders the bic/orr operands, halfword stores diverge.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0202acec (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     explicit per-case inserts; masked-reg first then payload (A|(B<<8)); store-order preserved
 *   risk:       reshape-able: orig keeps reg base in r3 and reloads payload de/e0 from the struct each store; if mwcc binds the second store's reg via r[1]=base+2 differently or reorders the bic/orr operands, halfword stores diverge.
 *   confidence: med
 */
/* func_0202acec (main, class D): 4-way switch on signed-short field 0xdc; each
 * case does TWO MMIO bitfield-insert halfword stores (no sharing). reg pair =
 * 0x04000048/0x0400004a (cases 0/1) or 0x04001048/0x0400104a (cases 2/3).
 * Cases 0/2: low-6 field = de|0x20 ; second reg low-6 = e0|0x20.
 * Cases 1/3: bits[13:8] field = (de<<8)|0x2000 ; second reg low-6 = e0|0x20.
 * struct-guessed: 0xdc sel, 0xde/0xe0 unsigned-short payloads. */
typedef volatile unsigned short vu16;

struct Obj0202acec {
    char _p[0xdc];
    short sel;            /* +0xdc, ldrsh */
    unsigned short de;    /* +0xde, ldrh */
    unsigned short e0;    /* +0xe0, ldrh */
};

int func_0202acec(struct Obj0202acec *p) {
    vu16 *r;

    switch (p->sel) {
    case 0:
        r = (vu16 *)0x04000048;
        r[0] = (r[0] & ~0x3f) | p->de | 0x20;
        r[1] = (r[1] & ~0x3f) | p->e0 | 0x20;
        break;
    case 1:
        r = (vu16 *)0x04000048;
        r[0] = (r[0] & ~0x3f00) | (p->de << 8) | 0x2000;
        r[1] = (r[1] & ~0x3f) | p->e0 | 0x20;
        break;
    case 2:
        r = (vu16 *)0x04001048;
        r[0] = (r[0] & ~0x3f) | p->de | 0x20;
        r[1] = (r[1] & ~0x3f) | p->e0 | 0x20;
        break;
    case 3:
        r = (vu16 *)0x04001048;
        r[0] = (r[0] & ~0x3f00) | (p->de << 8) | 0x2000;
        r[1] = (r[1] & ~0x3f) | p->e0 | 0x20;
        break;
    }
    return 1;
}
```

## Struct context
No GameSingleton or SystemWork fields. Struct layout inferred from asm offsets:

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| `p + 0xdc` | sel | `short` (ldrsh) | 4-way switch selector (cases 0..3) |
| `p + 0xde` | de | `unsigned short` | payload for first MMIO store (low-6 or bits[13:8] depending on case) |
| `p + 0xe0` | e0 | `unsigned short` | payload for second MMIO store (always low-6) |
| `0x04000048` / `0x04001048` | r[0] | `volatile u16` | first halfword: masked insert of de |
| `0x0400004a` / `0x0400104a` | r[1] | `volatile u16` | second halfword: masked insert of e0 |

Note: this function is a Near-companion of `func_0202abdc` (0202abdc.md) — same switch shape on `sel` at 0xdc, same MMIO reg pair, but different payload source and no `goto`-sharing.

## Closest matched example
**Path:** src/main/func_0202a998.c (prep, same batch)
**Why similar:** Both are MMIO halfword-insert functions that bind a register pair address once per switch case and do masked RMW stores in exact order; `func_0202a998` is the same-batch analog.
**Key lesson:** The orig keeps the struct base in r3 and reloads `p->de` and `p->e0` from memory before each store (no CSE). To reproduce this, ensure `de` and `e0` are `unsigned short` fields (not byte-promoted) and are read directly inside each store expression rather than cached in locals. The `r[0]` / `r[1]` indexing via `vu16 *` forces mwcc to emit `ldrh [r3]` / `ldrh [r3,#2]` pair with the struct pointer base in r3.

## Try this
1. Drop the prep draft as-is. The key check is that `r[1]` is accessed as `[r3,#2]` (not a pre-computed second pointer) — if objdiff shows `add r?,r3,#2` before the second ldrh, add `vu16 *r1 = r + 1;` and use `*r1` instead.
2. If mwcc CSEs `p->de` into a register and reuses it across both the `r[0]` and `r[1]` stores in a case, restructure each store to reload `p->de`/`p->e0` inline (no intermediate variable for the payload) to force the reload-per-store pattern.
3. If the `bic/orr` operand order differs (orig does `(old & ~mask) | payload` but mwcc emits `payload | (old & ~mask)`), try swapping the OR operand order in the C expression — for commutative OR, mwcc may prefer one canonical form.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x04000048` | MMIO/const |
| `_LIT1` | `0x04001048` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

