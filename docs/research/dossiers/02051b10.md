# 02051b10 — func_02051b10 (main, class C, 316B)

## Recipe + Risk
**Recipe:** mul-index struct array base[id-0x64]; guard chain; bind ptr field reload per orig
**Risk:** Source .s TRUNCATED past .L_15c (no pool/epilogue) so tail+_LIT4/5 are inferred; whole-function byte-match unverifiable. struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02051b10 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 *   recipe:     mul-index struct array base[id-0x64]; guard chain; bind ptr field reload per orig
 *   risk:       Source .s TRUNCATED past .L_15c (no pool/epilogue) so tail+_LIT4/5 are inferred; whole-function byte-match unverifiable. struct-guessed.
 *   confidence: low
 */
/* func_02051b10 — main, cls C. mul-index (stride 12) table writes + guards.
   NOTE: source .s is TRUNCATED (size=316/79 insns; visible body ends at
   'ldr r1,_LIT5' with no epilogue and no literal pool). Tail + pool inferred. */
typedef struct {
    unsigned char  id;      /* +0  data_0219dcb4 */
    unsigned char  f1;      /* +1  data_0219dcb5 */
    unsigned char  f2;      /* +2  data_0219dcb6 */
    unsigned char  _pad3;
    unsigned short hslot;   /* +4  data_0219dcb8 */
    unsigned char  _pad6[2];
    void          *ptr;     /* +8  data_0219dcbc */
} Slot;                     /* sizeof == 0x0c */

extern Slot gSlotTab[];     /* base data_0219dcb4 */

extern int  func_0204b3d0(void);
extern int  func_020aaf40(void);
extern int  func_020aaddc(int b);
extern void *func_020454cc(int a, int n);
extern void func_0204548c(int a);
extern void func_02050054(int a, int b);
extern void func_020aadf8(void *p, int b);

int func_02051b10(int id, int b, int c)
{
    if (b == 0 || c == 0)
        return 0;

    if (id >= 0x64 && gSlotTab[id - 0x64].id != 0) {
        if (gSlotTab[id - 0x64].ptr != 0 && func_020aaf40() != 0)
            return 0;
        /* fall through to reinit (.L_dc) */
    } else {
        id = func_0204b3d0();
        if (id == 0)
            return 0;
    }

    /* .L_dc: (re)initialise the slot */
    gSlotTab[id - 0x64].id = (unsigned char)id;
    gSlotTab[id - 0x64].f1 = 1;
    gSlotTab[id - 0x64].hslot = 0;
    if (gSlotTab[id - 0x64].ptr != 0)
        func_0204548c(4);

    {
        int n = func_020aaddc(b);
        gSlotTab[id - 0x64].ptr = func_020454cc(4, n + 1);
    }
    if (gSlotTab[id - 0x64].ptr == 0) {
        func_02050054(9, (int)0xfffe82ac); /* _LIT4 placeholder */
        return 0;
    }

    /* .L_15c: (TRUNCATED in source — tail inferred) */
    func_020aadf8(gSlotTab[id - 0x64].ptr, b);
    return id;
}
```

## Struct context
`gSlotTab` (base `data_0219dcb4`) is a 12-byte-stride array of `Slot` records, accessed by `id - 0x64`. No SystemWork or GameSingleton accessed.

| offset | field | type | use |
|--------|-------|------|-----|
| Slot+0x00 | id | u8 | slot identity byte; guard (!=0) and set |
| Slot+0x01 | f1 | u8 | set to 1 (delta vs 02051c4c which sets 0) |
| Slot+0x04 | hslot | u16 | zeroed |
| Slot+0x08 | ptr | void* | guard/free/alloc target |

## Closest matched example
**Path:** src/main/func_0201ac1c.c
**Why similar:** Guard chain + linear slot scan + conditional early returns; stride-indexed struct array pattern.
**Key lesson:** The `id - 0x64` offset into `gSlotTab` is computed as a multiply-add (`mla id,12,base`) once, then used repeatedly without re-deriving. Bind `id` in a callee-saved register; do NOT reassign it after `func_0204b3d0()` changes it — the original `id = func_0204b3d0()` path saves the new value and falls through to use it as the array index.

## Try this
1. Source `.s` is TRUNCATED — only build to verify the visible body (up to `func_020aadf8` call). If the first ~72 instructions match, the inferred tail is plausible. Run objdiff on just the matched portion before worrying about the epilogue.
2. The guard `id >= 0x64 && gSlotTab[id-0x64].id != 0` controls whether we re-init or call `func_0204b3d0()`. In objdiff confirm the `cmp r0,#0x64; blo` branch goes directly to the `else` path calling `func_0204b3d0`. If the branch direction is inverted, swap the condition.
3. When to park: if the literal pool (`_LIT4=0xfffe82ac`, `_LIT5`) cannot be forced to the expected position due to the truncation, or if the callee-saved register layout diverges (mwcc uses r5 instead of r4 for id), this is a struct-guessed/incomplete-source wall — accept as `.s`.
