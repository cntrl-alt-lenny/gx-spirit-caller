# 0202ef38 — func_0202ef38 (main, class D, 196B)

## Recipe + Risk

**Recipe:** class-D switch->membership; dense window -> pl jump-table, ascending order
**Risk:** orig spans the dense 0x1961-0x1973 cluster as a 19-entry pl table; mwcc's density heuristic must agree to table vs chain that run. Tail pivot 0x1b3e split also structural. permuter-class (switch shape).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0202ef38 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     class-D switch->membership; dense window -> pl jump-table, ascending order
 *   risk:       orig spans the dense 0x1961-0x1973 cluster as a 19-entry pl table; mwcc's density heuristic must agree to table vs chain that run. Tail pivot 0x1b3e split also structural. permuter-class (switch shape).
 *   confidence: med
 */
/* func_0202ef38: card-id membership test (switch -> 1, default 0).
 * 19-entry pl jump-table for the dense 0x1961..0x1973 window plus a
 * compare tree around pivot 0x1b3e for the 0x1b3d..0x1b5a tail. */
int func_0202ef38(int a0) {
    switch (a0) {
    case 0x1961:
    case 0x1965:
    case 0x1966:
    case 0x1967:
    case 0x1968:
    case 0x1969:
    case 0x196a:
    case 0x1970:
    case 0x1973:
    case 0x1b3d:
    case 0x1b3e:
    case 0x1b3f:
    case 0x1b5a:
        return 1;
    }
    return 0;
}
```

## Struct context

Pure card-ID membership predicate — no struct field accesses.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| — | — | — | no struct accesses |

## Closest matched example

**Path:** src/main/func_0202effc.c
**Why similar:** Same class-D switch->membership with a mixed table+tree lowering, near-sibling in the same address range.
**Key lesson:** The 0x1961..0x1973 window is 19 entries wide (0x13 slots) with 9 true members; mwcc emits a `pl` table bounded by `cmp r1,#0x12; addls pc,...`. The non-member slots in the window dispatch to the false branch. The tail cluster 0x1b3d..0x1b5a is handled by a short compare tree pivoting at 0x1b3e. Both the table width decision (mwcc must see 19 slots as worth tabling) and the tail pivot are compiler-internal structural choices.

## Try this

1. Drop the prep draft as-is. In objdiff confirm the `pl` table: `sub r1,a0,#0x1961; cmp r1,#0x12; addls pc,r1,lsl#2` with 19 entries (0x1961..0x1973). Then verify the tail tree shows `cmp a0,#0x1b3e; beq .Ltrue; ...` branches for 0x1b3d, 0x1b3f, and 0x1b5a.
2. If mwcc emits a compare chain instead of the 19-entry table: the density heuristic disagreed. There is no C reshape that forces table emission — the threshold is mwcc-internal. Check if the two absent slots at 0x1962 and 0x196b..0x196f (holes in the window) push it below the table-density threshold.
3. Park as permuter-class; the `pl` table emission decision and tail pivot are compiler-internal and un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001b3e` | MMIO/const |
| `_LIT1` | `0x00001973` | MMIO/const |
| `_LIT2` | `0x00001b3d` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

