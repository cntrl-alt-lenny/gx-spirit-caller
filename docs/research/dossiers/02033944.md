# 02033944 — func_02033944 (main, class D, 144B)

## Recipe + Risk
**Recipe:** Class D: reload count in cond; `1<<i` shift-test; bit-clear on path
**Risk:** i declared u32 so `1<<i`+`tst lsl r5` match, but mwcc may hoist `func_02046ac4()` out of loop or hold count in a reg (orig reloads 0xea0). Hoist/reload divergence reshape-able via re-read; deeper reg-alloc permuter-class. count offset struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02033944 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     Class D: reload count in cond; `1<<i` shift-test; bit-clear on path
 *   risk:       i declared u32 so `1<<i`+`tst lsl r5` match, but mwcc may hoist `func_02046ac4()` out of loop or hold count in a reg (orig reloads 0xea0). Hoist/reload divergence reshape-able via re-read; deeper reg-alloc permuter-class. count offset struct-guessed.
 *   confidence: med
 */
/* func_02033944: scan set bits of `mask` over [0, this->count). For each set
 * bit i (i != func_02046ac4()): if func_02052bc4(i)==0 and func_020469d0(i)!=0
 * return 0; if func_020469d0(i)==0 clear bit i and continue. After the loop,
 * return func_02052974(mask, b, c). count (this->count at 0xea0) is RELOADED
 * each iteration in the orig, so it is read fresh in the loop condition.
 */
extern int func_020469d0(int i);
extern int func_02046ac4(void);
extern int func_02052974(unsigned int mask, int b, int c);
extern int func_02052bc4(int i);

struct Obj_2033944 {
    char pad[0xea0];
    unsigned int count;
};

int func_02033944(struct Obj_2033944 *self, unsigned int mask, int b, int c) {
    unsigned int i;
    if (self->count != 0) {
        for (i = 0; i < self->count; i++) {
            if (i == (unsigned int)func_02046ac4()) continue;
            if (!(mask & (1u << i))) continue;
            if (func_02052bc4(i & 0xff) != 0) continue;
            if (func_020469d0(i & 0xff) == 0) {
                mask &= ~(1u << i);
                continue;
            }
            return 0;
        }
    }
    return func_02052974(mask, b, c);
}
```

## Struct context
This function accesses a large object at guessed offset 0xea0 for the `count` field. No GameSingleton or SystemWork fields are touched directly; the struct is specific to this function.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0xea0 | count | unsigned int | loop upper bound; reloaded every iteration in orig |

## Closest matched example
**Path:** src/main/func_0201abb0.c
**Why similar:** Both are Class D bit-manipulation loops using `1<<i` shift expressions and bit-clear via `~` on a field.
**Key lesson:** Declare the loop variable as `unsigned int` so `1u << i` emits `lsl r5` (not a sign-extended shift). The orig reloads the count field from memory each iteration — if mwcc caches it in a register, add a volatile read or force a re-read via the struct pointer each iteration. The `mask &= ~(1u << i)` bit-clear uses `mvn`/`bic` in the orig; keeping it as a single-expression `&= ~(...)` is the right shape.

## Try this
1. Drop as written and check objdiff. If `func_02046ac4()` is hoisted out of the loop, force a re-read each iteration by binding it to a local inside the loop body: `unsigned int skip = (unsigned int)func_02046ac4();` inside the for loop before the comparison.
2. If count is held in a register rather than reloaded from `[r4, #0xea0]` each iteration, add a volatile cast on the condition: `while (i < *(volatile unsigned int *)&self->count)` — or restructure the loop to re-dereference `self->count` in the condition explicitly.
3. If reg-alloc diverges (register assignments for i/mask/b/c differ), escalate to permuter-class; the `1<<i` shift + multi-branch structure is un-forceable without macOS permuter.
