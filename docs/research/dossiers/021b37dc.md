# 021b37dc — func_ov015_021b37dc (ov015, class D, 212B)

## Recipe + Risk
**Recipe:** D: stride-0x30 Matrix loop; load then store each element
**Risk:** Matrix stride 0x30. loop: load then store each element in row-major order.

## Prep draft

No prep file exists at `docs/research/c-match-prep/021b37dc.c`. EUR relocs show this
address in ov002 context (from:0x021b37dc kind:arm_call to:0x021b9ecc module:overlay(2)),
meaning ov002 calls into this address. Draft from recipe — stride-0x30 with load-then-store
is a matrix copy or transform over an array of Matrix3x4 objects:

```c
/* func_ov015_021b37dc: copy/transform Matrix3x4 array — stride 0x30 between matrices.
 * Loop: for each element in the source, load then store into dst.
 * Size 212B = 53 insns. A loop over 12 elements (load+store pairs) + prologue fits. */

typedef struct Matrix3x4 {
    int m00; int m01; int m02; int m03;
    int m10; int m11; int m12; int m13;
    int m20; int m21; int m22; int m23;
} Matrix3x4; /* sizeof = 0x30 */

void func_ov015_021b37dc(Matrix3x4 *dst, const Matrix3x4 *src, int count)
{
    int i;
    for (i = 0; i < count; i++) {
        dst[i].m00 = src[i].m00;
        dst[i].m01 = src[i].m01;
        dst[i].m02 = src[i].m02;
        dst[i].m03 = src[i].m03;
        dst[i].m10 = src[i].m10;
        dst[i].m11 = src[i].m11;
        dst[i].m12 = src[i].m12;
        dst[i].m13 = src[i].m13;
        dst[i].m20 = src[i].m20;
        dst[i].m21 = src[i].m21;
        dst[i].m22 = src[i].m22;
        dst[i].m23 = src[i].m23;
    }
}
```

Alternate: pointer-IV form (MWCC prefers incrementing pointers over indexed access for loops):

```c
void func_ov015_021b37dc(Matrix3x4 *dst, const Matrix3x4 *src, int count)
{
    while (count-- > 0) {
        dst->m00 = src->m00;
        dst->m01 = src->m01;
        dst->m02 = src->m02;
        dst->m03 = src->m03;
        dst->m10 = src->m10;
        dst->m11 = src->m11;
        dst->m12 = src->m12;
        dst->m13 = src->m13;
        dst->m20 = src->m20;
        dst->m21 = src->m21;
        dst->m22 = src->m22;
        dst->m23 = src->m23;
        dst++;
        src++;
    }
}
```

## Struct context

Touches `Matrix3x4` (HIGH confidence). Stride 0x30 = sizeof(Matrix3x4) exactly.

| field | offset | type | purpose |
|-------|--------|------|---------|
| m00–m03 | 0x00–0x0C | s32 | row 0 |
| m10–m13 | 0x10–0x1C | s32 | row 1 |
| m20–m23 | 0x20–0x2C | s32 | row 2 |

## Closest matched example
**Path:** src/overlay015/func_ov015_021b3240.c
**Why similar:** Same overlay (ov015), similar size (~45 insns), operates on a struct with
stride-derived pointer arithmetic and a function-call inside the loop body.
**Key lesson from that file:** MWCC uses pointer-advance style (increment base pointer, use
`[ptr]` or named field accesses) rather than indexed `ptr[i].field`. Declaration order of
loop variables controls register assignment — declare the loop counter before any struct
pointer locals.

## Try this
1. Try the pointer-IV form first (`dst++; src++;` at the bottom of the while loop). The
   "load then store each element" risk note means there is no interleaving between rows —
   each `src->mNN` load appears immediately before its corresponding `dst->mNN = ...` store,
   in row-major order. Do not hoist all 12 loads before the stores.
2. In objdiff: if register r4/r5 hold `src`/`dst` and the loop uses `ldr rN,[r5],#4` +
   `str rN,[r4],#4` (post-increment), replace the `dst->mNN = src->mNN; dst++; src++;` form
   with direct `int *d = (int*)dst, *s = (int*)src` and `*d++ = *s++;` ×12 in a while loop.
   Class D means iteration is expected.
3. Fallback: if register allocation for the loop counter or pointer pair can't be steered,
   escalate to permuter. Park as .s only if permuter also fails (212B = 53 insns, small enough
   to ship as a whole-function .s).
