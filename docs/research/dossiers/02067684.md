# 02067684 — func_02067684 (main, class D, 460B)

## Recipe + Risk
**Recipe:** ordered struct stores; divmod-by-const => '%'; strcpy fields; alloc-or-static branch
**Risk:** ASM TRUNCATED (460B, body cut at mvn r1,#0); param count/order, all byte offsets, the divmod constant (shown 100=placeholder) and the static-fallback symbol are struct-guessed; whole tail missing.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02067684 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     ordered struct stores; divmod-by-const => '%'; strcpy fields; alloc-or-static branch
 *   risk:       ASM TRUNCATED (460B, body cut at mvn r1,#0); param count/order, all byte offsets, the divmod constant (shown 100=placeholder) and the static-fallback symbol are struct-guessed; whole tail missing.
 *   confidence: low
 */
/* func_02067684 @ 0x02067684  (cls D, main) — large struct constructor (~14 params,
   ASM TRUNCATED in batch). Optional alloc(0x110) when handle==0, two strcpy fields,
   long ordered store block, then 4x divmod-by-constant byte writes at +0x84.
   func_020aadf8==strcpy; func_020a991c returns int; tail (after loop) not in provided asm. */
typedef unsigned char  u8;
typedef unsigned short u16;

extern u8   *func_020453e8(int size);
extern void  func_02055330(void);
extern void  func_020a9950(void);
extern void  func_020aadf8(char *dst, const char *src); /* strcpy */
extern int   func_020a991c(void);

struct C; /* opaque, offsets accessed by byte index below */

u8 *func_02067684(u8 **handle, int a1, int a2, const char *name,
                  const char *a5, int a6, int a7,
                  int a8, int a9, int a10, int a11, int a12, int a13, int a14)
{
    u8 *o;
    int i;

    if (handle == 0) {
        /* movs r5,r0; ldreq r5,_LIT0 — when arg0 null, use a static object */
        extern u8 _gx_static_2067684[];
        o = _gx_static_2067684;
    } else {
        o = func_020453e8(0x110);
        *(u8 **)handle = o;
        o = *(u8 **)handle;
    }

    func_02055330();
    func_020a9950();
    func_020aadf8((char *)(o + 0x4),  name);
    func_020aadf8((char *)(o + 0x44), a5);

    *(int *)(o + 0xC0) = a2;
    *(int *)(o + 0xAC) = 0;
    *(int *)(o + 0xB0) = 0;
    *(int *)(o + 0x00) = a1;
    *(int *)(o + 0xB8) = 1;
    *(int *)(o + 0x10C) = a14;
    *(int *)(o + 0x88) = a8;
    *(int *)(o + 0x8C) = a9;
    *(int *)(o + 0x90) = a10;
    *(int *)(o + 0x94) = a11;
    *(int *)(o + 0x98) = a12;
    *(int *)(o + 0x9C) = a13;
    *(int *)(o + 0xA0) = 0;
    *(int *)(o + 0xA4) = 0;
    *(int *)(o + 0xD4) = 0;
    *(int *)(o + 0xBC) = a6;
    *(int *)(o + 0xC4) = 0;
    *(int *)(o + 0xC8) = a7;
    *(int *)(o + 0x104) = 0;
    *(u16 *)(o + 0x108) = 0;
    *(int *)(o + 0xA8) = 0;
    *(int *)(o + 0xB4) = 0;

    for (i = 0; i < 4; i++) {
        *(u8 *)(o + 0x84 + i) = (u8)(func_020a991c() % 100);
    }
    /* TRUNCATED: original continues after the loop (mvn r1,#0x0 ...) */
    return o;
}
```

## Struct context
This function constructs a large heap-allocated object (0x110 bytes) with many ordered fields. No SystemWork or GameSingleton is accessed. All offsets are internal to the allocated object `o`.

| offset | field | type | use |
|--------|-------|------|-----|
| `+0x00` | `f_00` | `int` | `a1` (first int param) |
| `+0x04` | `name[]` | `char[0x40]` | strcpy from `name` param |
| `+0x44` | `a5str[]` | `char[?]` | strcpy from `a5` param |
| `+0x84..+0x87` | `rng[4]` | `u8[4]` | 4 bytes from `func_020a991c() % 100` |
| `+0x88..+0x9C` | `a8..a13` | `int[6]` | params stored in ascending order |
| `+0xA0..+0xA8` | zeros | `int[3]` | zeroed fields |
| `+0xAC..+0xB4` | zeros | `int[3]` | zeroed fields |
| `+0xB8` | `f_B8` | `int` | constant `1` |
| `+0xBC` | `f_BC` | `int` | `a6` |
| `+0xC0` | `f_C0` | `int` | `a2` |
| `+0xC4` | `f_C4` | `int` | `0` |
| `+0xC8` | `f_C8` | `int` | `a7` |
| `+0xD4` | `f_D4` | `int` | `0` |
| `+0x104` | `f_104` | `int` | `0` |
| `+0x108` | `f_108` | `u16` | `0` |
| `+0x10C` | `f_10C` | `int` | `a14` |

The `alloc-or-static` branch: when `handle == 0`, the function uses a file-scope static object `_gx_static_2067684` instead of heap-allocating.

## Closest matched example
**Path:** src/main/func_020680cc.c
**Why similar:** Both are object initializers/teardown functions that chain multiple field writes on a pointer; the store order is critical and structurally determined by the source.
**Key lesson:** Store order in the long initializer block must be preserved exactly as written. MWCC emits stores in source order when the object has no alias; reordering any of the `*(int *)(o + 0xNN)` lines will shift the codegen. The `% 100` divmod constant must be verified against the actual asm's magic number — the prep draft uses 100 as a placeholder, so first-build will likely show a mismatched multiply constant in objdiff.

## Try this
1. Verify the divmod constant: in objdiff, find the `smull` in the loop body and reverse-engineer the multiplier; if not `100`, substitute the correct divisor in the `% N` expression. Common pattern: if smull shows `0x51eb851f` the divisor is 100; if `0xa3d70a4` it is 200; etc.
2. Verify the `alloc-or-static` branch: `_gx_static_2067684` is a placeholder name — objdiff will show the real static symbol. Check that `handle == 0` is the correct null-check (not `*handle == 0`); if the asm does `movs r5,r0; ldreq …` the check is on `handle` itself.
3. The tail is **missing** from the prep draft (truncated at `mvn r1,#0`). Do not attempt a final match until the tail is reconstructed from a fresh disassembly. Park the partial as `.s` or use `GLOBAL_ASM` for the truncated portion until the tail is decoded.
