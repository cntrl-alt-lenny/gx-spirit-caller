# 021b5948 — func_ov019_021b5948 (ov019, class D, 208B)

## Recipe + Risk
**Recipe:** signed /2 (sign>>31+asr); nested bounds && for lazy ldrge; store-order cmd pack; bind base
**Risk:** permuter-class: the lazy ldrge/cond-exec bounds chain (nested-&& vs flat 4-term &&) drives branch shape and may not match; stack-slot order for a/b also struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021b5948 (ov019, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     signed /2 (sign>>31+asr); nested bounds && for lazy ldrge; store-order cmd pack; bind base
 *   risk:       permuter-class: the lazy ldrge/cond-exec bounds chain (nested-&& vs flat 4-term &&) drives branch shape and may not match; stack-slot order for a/b also struct-guessed.
 *   confidence: low
 */
/* func_ov019_021b5948 (ov019, class D): y = (0xc0 - obj[0x14])/2 - 8 (signed
 * div-by-2 via add sign>>31; asr#1). base = obj[0x18] + y. If obj[4]==1 &&
 * obj[0]==1: probe a hit window (coords via func_02006110), then pack a 6-byte
 * command block and dispatch (func_02005dac handle -> func_020944a4).
 *
 * Stack frame 0x10 = 6-byte cmd (lowest) + two coord ints. coord a=[sp+0xc],
 * b=[sp+0x8]; func_02006110(&a,&b). cmd.w = ((base-2)&0xff)|0xc0614000;
 * cmd.h = (sel|0x1400) with sel 0x338 or (probe!=0 && hit) 0x340. */

extern int  func_02005dac(int a, int b, void *blk);
extern void func_02006110(int *outA, int *outB);
extern int  func_020061ac(void);
extern void func_020944a4(void *blk, int handle, int n);

struct cmd_021b5948 {
    int w;
    unsigned short h;
};

int func_ov019_021b5948(int *obj) {
    struct cmd_021b5948 cmd;
    int a, b;
    int y;
    int base;
    int hit;
    int sel;
    int handle;

    y = (0xc0 - obj[5]) / 2 - 8;     /* obj[0x14] = obj[5] */
    base = obj[6] + y;               /* obj[0x18] = obj[6] */

    if (obj[1] == 1 && obj[0] == 1) {
        hit = 0;
        func_02006110(&a, &b);
        if (a >= 0x68 && b >= base + 5) {
            if (a < 0x98 && b < base + 5 + 0x12)
                hit = 1;
        }
        sel = 0x338;
        if (func_020061ac() != 0 && hit != 0)
            sel = 0x340;

        cmd.w = ((base - 2) & 0xff) | 0xc0614000;
        cmd.h = (unsigned short)(sel | 0x1400);
        handle = func_02005dac(2, 0, &cmd);
        func_020944a4(&cmd, handle, 6);
    }
    return 1;
}
```

## Struct context
`obj` is treated as a plain `int *` array with offsets decoded by index:

| field | offset | index | type | purpose |
|-------|--------|-------|------|---------|
| obj[0] | +0x00 | [0] | int | outer enable flag (must be 1) |
| obj[1] | +0x04 | [1] | int | secondary enable flag (must be 1) |
| obj[5] | +0x14 | [5] | int | height used for signed /2 |
| obj[6] | +0x18 | [6] | int | y-base offset |

`cmd_021b5948` is a 6-byte local struct: `int w` (4B) + `unsigned short h` (2B). The `func_020944a4` call passes size=6, confirming the 6-byte extent.

## Closest matched example
**Path:** `src/overlay019/func_ov019_021b2728.c`
**Why similar:** same ov019 touch-input probe pattern (`func_02006110` + `func_020061ac`) with a nested bounds check building up a boolean.
**Key lesson from that file:** The signed /2 for `y` must use `(x + (x >> 31)) >> 1` or plain `/ 2` on a signed int — MWCC emits `add r,r,r,asr#31 / asr r,r,#1`; writing `>> 1` directly on a signed int produces `asr#1` without the sign-fixup and diverges. Plain `/ 2` in C on a signed value is the correct form.

## Try this
1. The signed divide-by-2: write `(0xc0 - obj[5]) / 2` literally (not `>> 1`) — MWCC emits the `add r,r,r,asr#31; asr#1` pair automatically for signed `/2`.
2. The nested bounds check `if (a >= 0x68 && b >= base+5) { if (a < 0x98 && b < base+5+0x12) hit=1; }` must stay as two separate `if` blocks, not a flat 4-term `&&` — the outer test produces a `ldrge` (lazy second load), collapsing to one `if` changes the branch structure.
3. If hit-window branch shape is still wrong in objdiff after step 2, try swapping `a` and `b` stack positions (declare `int b, a;` instead of `int a, b;`) — stack-slot order is struct-guessed; then escalate to permuter if frame is still off.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0xc0614000` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02006110` | main TU call |
| `bl` | `func_020061ac` | main TU call |
| `bl` | `func_02005dac` | main TU call |
| `bl` | `func_020944a4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

