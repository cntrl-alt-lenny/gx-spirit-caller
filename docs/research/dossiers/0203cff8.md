# 0203cff8 — func_0203cff8 (main, class D, 128B)

## Recipe + Risk
**Recipe:** int v=u16 => (v>>2) ASR; predicated if/else dup shift; mla base B; store f2,f3 order
**Risk:** (v>>2) must stay ASR: arg1[1] is unsigned short (promotes to int) so signed >> => asr; if mwcc treats it unsigned it emits lsr. reshape-able (keep int v). base B mla vs add+mul is the secondary risk. Offsets struct-guessed.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0203cff8 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     int v=u16 => (v>>2) ASR; predicated if/else dup shift; mla base B; store f2,f3 order
 *   risk:       (v>>2) must stay ASR: arg1[1] is unsigned short (promotes to int) so signed >> => asr; if mwcc treats it unsigned it emits lsr. reshape-able (keep int v). base B mla vs add+mul is the secondary risk. Offsets struct-guessed.
 *   confidence: med
 */
/* func_0203cff8 (cls D): arith-shift bitfield compute + dual indexed bases + clamp
 * store + Copy32. arg1->f2 is u16 loaded into int so (v>>2) is ASR (signed int).
 * if/else duplicates (v>>2) under predication. base A = arg3+0x444 + i*4 (indexed);
 * base B = arg3+0x470 + i*0xc0 (mla). 0x444 pooled (not rotatable). Store f2 then f3. */

extern void Copy32(void *src, void *dst, int size);

void func_0203cff8(int i, unsigned short *arg1, int arg2, char *arg3) {
    int v = arg1[1];                                   /* arg1->f2 (u16 -> int) */
    unsigned char *a = (unsigned char*)(arg3 + 0x444) + i * 4;
    void          *b = (arg3 + 0x470) + i * 0xc0;
    unsigned n;
    if (v & 2) {
        n = (v >> 2) & 0xff;
    } else {
        n = ((v >> 2) + 0x19) & 0xff;
    }
    if ((n & 0xff) > a[2]) {
        a[2] = (unsigned char)n;
        a[3] = (unsigned char)((a[3] & ~0x7f) | (arg2 & 0x7f));
    }
    Copy32(arg1, b, 0xc0);
}
```

## Struct context

This function operates on a flat `char *arg3` base (struct-guessed) using two derived pointer arrays; offsets are inferred, not confirmed. No GameSingleton or SystemWork fields are accessed.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| arg3+0x444+i*4+2 | a[2] (f_cnt) | unsigned char | read for clamp guard; conditionally written |
| arg3+0x444+i*4+3 | a[3] (f_flags) | unsigned char | 7-bit merge RMW (bic+orr 0x7f) |
| arg3+0x470+i*0xc0 | b (cell start) | void* | Copy32 destination (mla base B) |
| arg1[1] | f2 | unsigned short promoted to int | arith-shift source |

## Closest matched example
**Path:** src/main/func_0201d0c.c
**Why similar:** Fill32 + multi-field indexed struct write with bitfield clamp — same shaped arith-shift + bic/orr pack-store pattern at known offsets.
**Key lesson:** Declare the intermediate `v` as `int` (not `unsigned`) so that `v >> 2` lowers to `asr` not `lsr`. The MLA for base B (`i * 0xc0`) requires mwcc to see an integer multiply with a non-rotatable constant — keep the multiplication inline in the pointer expression (not a pre-computed offset).

## Try this
1. Build as-is; verify `v >> 2` emits `asr #2` in objdiff. If `lsr #2` appears instead, add an explicit `int v = (int)arg1[1];` cast (already present — confirm mwcc sees it as signed).
2. If base B dispatches as `add + mul` instead of `mla`, try caching `b` after computing `a` so that the multiplier `i * 0xc0` falls in the standard `mla r,r,r,r` slot; alternatively separate the `i * 0xc0` into its own `int` temporary.
3. If the bic/orr for `a[3]` still diverges after ASR and MLA fix, park as permuter-class (7-bit mask bic+orr with stored byte is occasionally un-forceable without macOS permuter).
