# 0200b2f4 — func_0200b2f4 (main, class D, 104B)

## Recipe + Risk
**Recipe:** counted loop; Fill32(0,dst,0xc); f0=arg2+s0, then f4=s4 & f8=-1 fold to stmib
**Risk:** stmib requires d4/d8 stores land in consecutive regs (src.s4 then -1); a reg rotation breaks the merge into two str. struct-guessed (0xc dst / 0x8 src strides). reshape-able/permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0200b2f4 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     counted loop; Fill32(0,dst,0xc); f0=arg2+s0, then f4=s4 & f8=-1 fold to stmib
 *   risk:       stmib requires d4/d8 stores land in consecutive regs (src.s4 then -1); a reg rotation breaks the merge into two str. struct-guessed (0xc dst / 0x8 src strides). reshape-able/permuter-class.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0200b2f4 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED.
 *   recipe:     Fill32(0,dst,0xc) then f0=arg2+src.s0, f4=src.s4, f8=-1 in order (f4/f8 fold to stmib)
 *   risk:       see risk note
 */
/* func_0200b2f4: for i in [0,count): zero a 0xc-byte dst slot then write 3 words;
   return 1. */

typedef struct {                /* dst slot, size 0xc */
    int d0;                     /* arg2 + src.s0 */
    int d4;                     /* src.s4 */
    int d8;                     /* = -1 */
} Dst_b2f4;

typedef struct {                /* src slot, stride 0x8 */
    int s0;
    int s4;
} Src_b2f4;

extern void Fill32(int val, void *dst, int n);

int func_0200b2f4(Dst_b2f4 *dst, Src_b2f4 *src, int arg2, int count)
{
    int i;
    for (i = 0; i < count; i++) {
        Dst_b2f4 *d = &dst[i];
        Src_b2f4 *s = &src[i];
        Fill32(0, d, 0xc);
        d->d0 = arg2 + s->s0;
        d->d4 = s->s4;
        d->d8 = -1;
    }
    return 1;
}
```

## Struct context
No SystemWork or well-known globals. All data comes through the function arguments.

| offset | field | type | use |
|--------|-------|------|-----|
| Dst_b2f4+0x00 | d0 | int | arg2 + s->s0 |
| Dst_b2f4+0x04 | d4 | int | s->s4 (must land in a register adjacent to d8's -1 for stmib) |
| Dst_b2f4+0x08 | d8 | int | -1 (mvn); must be in the register immediately after d4's register for stmib merge |
| Src_b2f4+0x00 | s0 | int | read once; added to arg2 |
| Src_b2f4+0x04 | s4 | int | read once; copied to d4 |

## Closest matched example
**Path:** src/main/func_0200b35c.c
**Why similar:** Same structure — counted loop, fill then per-field stores, return 1.
**Key lesson:** The `stmib` for `d->d4` and `d->d8` requires MWCC to schedule `s->s4` into one register and `-1` into the immediately following register before the store pair. The statement order `d->d4 = s->s4; d->d8 = -1;` is the correct order in C. If objdiff shows two separate `str` instructions instead of `stmib`, a register rotation occurred — try moving the `-1` computation earlier: `int neg1 = -1; d->d4 = s->s4; d->d8 = neg1;` to give the compiler more freedom to allocate adjacent registers.

## Try this
1. Build as-is. In objdiff look at the stores for `d->d4` and `d->d8`: if `stmib` appears and the offsets are `[d, #4]` and `[d, #8]`, it matched. If two separate `str` appear instead, the register pairing broke.
2. To recover the `stmib`: introduce `int s4val = s->s4;` before `Fill32`, so the value is already in a register when MWCC assigns the `-1` to the next register. Alternatively: declare a temp `int m1 = -1;` before the loop so the MVN is hoisted out and the register is free to pair with s4.
3. When to park: if no reshape forces the `stmib` merge, this is **permuter-class**. Accept as `.s` and do not iterate further.
