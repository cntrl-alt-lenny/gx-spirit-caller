# 02085f48 — func_02085f48 (main, class D, 452B)

## Recipe + Risk

**Recipe:** flag-branch store-order; smull>>8 = (s64)a*b>>8; -(x<<4)*y; bind sp-struct as int[]
**Risk:** batch asm truncated before .L_7f0; struct offsets + the s[] index->sp-offset mapping are inferred; store-order across branches likely diverges — struct-guessed + permuter-class.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02085f48 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     flag-branch store-order; smull>>8 = (s64)a*b>>8; -(x<<4)*y; bind sp-struct as int[]
 *   risk:       batch asm truncated before .L_7f0; struct offsets + the s[] index->sp-offset mapping are inferred; store-order across branches likely diverges — struct-guessed + permuter-class.
 *   confidence: low
 */
/* func_02085f48: builds a 0x3c stack-struct from obj flags then FX-scales fields,
 * passes &struct+4 to func_02084e0c(0x0e? ...).  obj @ r0.  Flag bits in obj[0]: 8,4,1.
 * smull r3,rH,a,b; lsr#8|lsl#0x18 = (s64)a*b>>8 ; the .L_7f0 scaler = (s64)*>>12. */
typedef signed int s32;
typedef unsigned short u16;
typedef signed long long s64;
typedef s32 fx32;

typedef struct {
    u32 f0;
    char _04[0x14];
    fx32 a18;
    fx32 a1c;
    fx32 a24;
    fx32 a28;
    char _2c[4];
    fx32 a30;
    fx32 a34;
    char _38[2];
    u16 a2c;
    u16 a2e;
} Obj;
typedef unsigned int u32;

extern void func_02084e0c(int, int *, int);

void func_02085f48(Obj *o) {
    int s[15];                  /* 0x3c bytes */
    s[13] = 0;                  /* sp+0x34 */
    s[14] = 2;                  /* sp+0x38 */
    s[0] = (o->f0 & 8) ? 0x00101710 : 0x00101910; /* literal pool placeholders */
    s[1] = 3;
    s[10] = 0; s[9] = 0; s[8] = 0; s[7] = 0;
    s[5] = 0; s[4] = 0; s[3] = 0;
    if (o->f0 & 4) {
        s[11] = 0; s[12] = 0;
        if (o->f0 & 1) { s[2] = 0x1000; s[6] = 0x1000; }
        else { s[2] = o->a18; s[6] = o->a1c; }
    } else if (o->f0 & 1) {
        s[11] = -(o->a24 << 4) * (int)o->a2c;
        s[12] = -(o->a28 << 4) * (int)o->a2e;
        s[2] = 0x1000; s[6] = 0x1000;
    } else {
        s[11] = -(int)((((s64)o->a18 * o->a24) >> 8)) * (int)o->a2c;
        s[12] = -(int)((((s64)o->a1c * o->a28) >> 8)) * (int)o->a2e;
        s[2] = o->a18;
        s[6] = o->a1c;
    }
    /* .L_7f0: scale by obj[0x30]/0x34 if != 0x1000 (Q12) — body continues past batch */
    func_02084e0c(s[0], &s[1], 0x0e);
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| o->f0 | flags | u32 | bit3=0x8 (literal selector), bit2=0x4 (zero-scale branch), bit0=0x1 (identity-scale branch) |
| o->a18 | | fx32 | Q12 scale component X — stored to s[2] in non-identity branch |
| o->a1c | | fx32 | Q12 scale component Y — stored to s[6] |
| o->a24 | | fx32 | smull multiplier for s[11] in the `(f0&1)` branch |
| o->a28 | | fx32 | smull multiplier for s[12] in the `(f0&1)` branch |
| o->a2c | u16 | | scale multiplier post-smull for s[11] |
| o->a2e | u16 | | scale multiplier post-smull for s[12] |
| s[0]  | sp+0x00 | int | tag literal (0x00101710 or 0x00101910) |
| s[1..14] | sp+0x04..0x38 | int[] | the 0x38-byte struct body passed as &s[1] |

## Closest matched example

**Path:** src/main/func_020808b8.c
**Key lesson:** func_020808b8 (store-order reshape candidate, 192B) shows that the exact C statement order for successive struct-field stores must match the assembler's interleaved `str` sequence. Here, the `s[N]=0` zero-init stores at the top of the function (`s[13],s[14],s[10],s[9],...`) must appear before the flag-branch dispatch; if mwcc hoists or reorders any of them into the branches the store sequence diverges.

## Try this

1. Verify that the `s[]` index-to-sp-offset mapping is correct: s[13] must be at sp+0x34, s[14] at sp+0x38, s[11]/s[12] at sp+0x2c/0x30. If objdiff shows a different offset, adjust by counting 4 bytes per int slot from sp+0x00 (s[0]).
2. The `-(o->a24 << 4) * (int)o->a2c` expression in the `f0&1` branch must emit `lsl#4` followed by `rsb` (negate) and then `mul` — do not fold `-(x<<4)*y` into `-x*y<<4` as that may reorder the `lsl` and `rsb`. Keep the shift-then-negate-then-multiply form exactly.
3. Park as permuter-class; the batch asm is truncated before .L_7f0 and the store-order across all three flag-branches is inferred, not confirmed. The `smull>>8` in the else-branch is also a dense coin-flip. If any store diverges after steps 1–2, route to macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00101710` | MMIO/const |
| `_LIT1` | `0x00101910` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02084e0c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

