# 02024024 — func_02024024 (main, class C, 124B)

## Recipe + Risk
**Recipe:** C: clone of 020234f8; bind base across Fill32+stores; count once; do-while scan
**Risk:** reshape-able: same as 020234f8 — orig reuses the `mul r5,r3,0x88` byte offset for all three post-call addresses (add r0,r6,r5 / str r4,[r6,r5]). If mwcc re-multiplies or drops the offset-reuse, ~1-2 insn; hoist `off=i*0x88`.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02024024 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     C: clone of 020234f8; bind base across Fill32+stores; count once; do-while scan
 *   risk:       reshape-able: same as 020234f8 — orig reuses the `mul r5,r3,0x88` byte offset for all three post-call addresses (add r0,r6,r5 / str r4,[r6,r5]). If mwcc re-multiplies or drops the offset-reuse, ~1-2 insn; hoist `off=i*0x88`.
 *   confidence: med
 */
/* func_02024024: clone of func_020234f8 with data_0219a8e4 (slot stride 0x88)
 * and init helper func_02024368. Scan for first empty slot bounded by count
 * at +4; if found, zero (Fill32 0x88), store arg0 at slot+0, init, return
 * slot; else return 0.
 *
 * Manager { void *array; int count; }. Base bound in r6 across Fill32 and
 * reused for the stores; count read once into r5.
 */

typedef struct { unsigned char b[0x88]; } slot_0219a8e4_t;
typedef struct { slot_0219a8e4_t *array; int count; } mgr2_0219a8e4_t;

extern mgr2_0219a8e4_t data_0219a8e4;
extern void Fill32(int v, void *dst, int size);
extern int func_02024368(void *slot);

void *func_02024024(int arg0) {
    int count = data_0219a8e4.count;
    int i = 0;
    slot_0219a8e4_t *base;
    slot_0219a8e4_t *slot;

    if (count > 0) {
        slot_0219a8e4_t *p = data_0219a8e4.array;
        do {
            if (*(int *)p == 0) break;
            i++;
            p += 1;
        } while (i < count);
    }
    if (i >= count) return 0;

    base = data_0219a8e4.array;
    slot = (slot_0219a8e4_t *)((char *)base + i * 0x88);
    Fill32(0, slot, 0x88);
    *(int *)slot = arg0;
    func_02024368(slot);
    return slot;
}
```

## Struct context

This function uses a pool-manager layout at data_0219a8e4 (array pointer + count), iterates slots of stride 0x88, and calls Fill32 on the chosen slot. Does not touch GameSingleton or SystemWork.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| data_0219a8e4 + 0x0 | array | slot_0219a8e4_t* | loop base pointer; reloaded after loop for post-call stores |
| data_0219a8e4 + 0x4 | count | int | read once before loop into count variable |
| slot + 0x0 | first word | int | tested == 0 for empty; written with arg0 after Fill32 |

## Closest matched example

**Path:** src/main/func_0200ad0c.c
**Why similar:** Both bind a global struct pointer across Fill32 and reuse the mul-derived byte offset for multiple subsequent stores/args.
**Key lesson:** The key codegen issue is that the original emits `mul r5,r3,0x88` (computing `i*0x88`) once and reuses `r5` for all three post-Fill32 address calculations (`add r0,r6,r5` for the Fill32 dst arg, `str r4,[r6,r5]` for the arg0 write, and `add r0,r6,r5` for the func_02024368 arg). If mwcc does not hoist the multiply or re-emits it for each use, introduce an explicit `int off = i * 0x88;` variable before the `base` reload and reference it for all three address expressions — this forces a single mul into r5 with three subsequent register uses.

## Try this

1. Hoist `int off = i * 0x88;` immediately after the loop exits (before the `base` reload): write `slot = (slot_0219a8e4_t *)((char *)base + off);`, then `Fill32(0, slot, 0x88);`, `*(int *)((char *)base + off) = arg0;`, `func_02024368((char *)base + off);` — all three reusing `off` so mwcc keeps the single mul.
2. If objdiff shows count read twice (before loop and again at `if (i >= count)`), bind with `int count = data_0219a8e4.count;` hoisted before the if-guard, and reference only the local in the post-loop test.
3. If the do-while scan body diverges (mwcc converts to while), check whether the `if (count > 0)` guard is needed: if count==0 skip is already correct, the do-while body must be guarded. Escalate to permuter or .s if loop structure cannot be forced.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_02024098` | `data_0219a8e4` | data symbol |
| `.L_0202409c` | `0x0219a8e4` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Fill32` |  |
| `bl` | `func_02024368` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

