# 0226366c — func_ov002_0226366c (ov002, class F retriage, 340B)

## Re-triage
**Original verdict:** F — previously intractable (high confidence)
**Upgrade reason:** Accesses DuelQueueState and Ov002Slot. Embedded at offset +0x24 (36 bytes) within block func_ov002_02263648.s (EUR delink 0x02263648–0x022636CC, size=0x84=132 bytes). However the retriage doc lists 340 bytes — far larger than the 132-byte EUR block. In USA/JPN the function is separately delinked at the same address with `size=0x104` (260 bytes, a different count). The EUR block 02263648 is a different function (scans both players' slots for a match using `func_ov002_021b4120` + `func_ov002_021d5b80` + `func_ov002_021e30b4`). The full 0226366c function spans into subsequent EUR segments and must be carved from the binary. EUR reloc confirms: `from:0x0226366c kind:arm_call to:0x021b4120`.
**Confidence:** high
**Structs needed:** DuelQueueState (+0x5A8 queue_state, +0x5D4 cleared_on_edge), Ov002Slot (id_lo13 lsl#19/lsr#19, +0x40 flag word), PerPlayerRowTable (base 0x022CF16C, stride 0x868)

## Assembly

```asm
; NOTE: func_ov002_0226366c is NOT a standalone EUR .s file.
; It is embedded at offset +0x24 within func_ov002_02263648.s
; (EUR delink block 0x02263648–0x022636CC, size=0x84=132 bytes).
; The full function is 340 bytes (retriage) or 260 bytes (USA/JPN size=0x104),
; and spans beyond the EUR block boundary into 0x022636CC+.
; EUR reloc: from:0x0226366c kind:arm_call to:0x021b4120 confirms the address.
; USA/JPN have this as a standalone .s (func_ov002_0226366c size=0x104).
;
; The containing EUR block func_ov002_02263648.s shows the surrounding context:
;   func_ov002_02263648:
;     stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, lr}
;     ldr r5, _LIT0         ; 0x000015b8
;     mov r9, r0
;     mov r7, #0
;     mov r6, #0xb          ; slot count
;   .L_14c:
;     eor r8, r9, r7
;     mov r0, r8
;     mov r1, r6            ; 0xb
;     mov r2, r5            ; 0x15b8
;     bl func_ov002_021b4120
;     movs r4, r0           ; sign test
;     bmi .L_1a0
;     [func_ov002_021d5b80 + func_ov002_021e30b4 calls + return 0]
;   .L_1a0:
;     add r7, r7, #1; cmp r7, #2; blt .L_14c
;     mov r0, #1; ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, pc}
; -- func_ov002_0226366c begins at offset +0x24 from 02263648 --
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state gate at function entry |
| DuelQueueState | +0x5D4 | f_5d4 | int | cleared_on_edge = loop bound for secondary scan |
| Ov002Slot | row+0x30+s*0x14 (+0x00) | id_lo13 | u32:13 | lsl#19/lsr#19 extraction in slot dispatch |
| PerPlayerRowTable | base+(player&1)*0x868 | row | ptr | stride 0x868 via mla |

## Recipe sketch

```c
/* F-class 340-byte: DuelQueueState gate + eor-player loop calling 021b4120
 * (slot id query with sign test) and 021d5b80 (slot state update).
 * Pattern closely mirrors func_ov002_022636cc (same callee set, same eor). */
int func_ov002_0226366c(int player) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    if (dqs->f_5a8 != 0x7f) return 0;            /* +0x5A8 queue_state */
    int p;
    for (p = 0; p < 2; p++) {
        int xi = player ^ p;
        int r = func_ov002_021b4120(xi, 0xb, 0x16b9); /* or 0x15b8 */
        if (r < 0) continue;                      /* movs sign test */
        unsigned topbit = ((unsigned)xi << 31) & 0x80000000u;
        func_ov002_021d5b80(xi, 0xb, r, 0);
        func_ov002_021e30b4((int)(topbit | 0x0a4f16b9u), r);
        return 0;
    }
    return 1;
}
```

## Closest matched example
**Path:** docs/research/c-match-prep/022636cc.c
**Why similar:** func_ov002_022636cc is the immediate predecessor (starts at 02263648, size=0x84). Same callee set (021b4120, 021d5b80, 021e30b4), same eor-player loop, same movs+bmi sign test. The only difference is the literal constant (_LIT0 differs: 0x15b8 vs 0x16b9) and the packed word argument to 021e30b4.
**Key lesson:** The `movs r4, r0 / bmi` sign test must be written as `r < 0` not `r == 0`. The `orr r0, r3, r0` packed-word argument to 021e30b4 packs player-parity bit into the high bit. The USA/JPN size=0x104 (260 bytes) suggests the EUR version may be slightly larger — use the EUR binary byte count when it becomes available.

## Try this
1. Delink first: add `func_ov002_0226366c kind:function(arm,size=0x154)` to EUR symbols.txt (use 340 bytes from retriage doc until the delinker confirms the real size). EUR reloc at 0x0226366c confirms the address.
2. Once delinked, start with `if (dqs->f_5a8 != 0x7f) return 0;` — the DuelQueueState gate is confirmed by the "DuelQueueState, Ov002Slot" retriage entry.
3. The `func_ov002_021d5b80` and `func_ov002_021e30b4` argument packing may differ from 022636cc — compare the literal in `_LIT0` and `_LIT1` between the two. If they use the same literals, the body may be byte-identical and this is a pure argument-constant variant.
