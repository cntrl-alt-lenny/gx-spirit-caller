[//]: # (markdownlint-disable MD013 MD041)

# 021fa77c — func_ov002_021fa77c (ov002, class F retriage, 344B)

## Re-triage

**Original verdict:** F — previously intractable
**Upgrade reason:** Reads `DuelQueueState+0x5B4` (pending-action flag) as the switch discriminant via `addls pc, pc, r2, lsl #2` (8-entry computed-jump table). Also reads `DuelStateSingleton+0xD44` (an unconfirmed count field at `data_ov002_022d016c+0xd44`) as a loop bound. Both are deterministic once named.
**Confidence:** high
**Structs needed:** DuelQueueState, DuelStateSingleton

## Assembly

```asm
; func_ov002_021fa77c — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022ce288
        .extern data_ov002_022d016c
        .extern func_ov002_021ae400
        .extern func_ov002_021af9d0
        .extern func_ov002_021b004c
        .extern func_ov002_021d8128
        .extern func_ov002_021e2b3c
        .extern func_ov002_021e2c5c
        .extern func_ov002_021f4a84
        .extern func_ov002_022577dc
        .global func_ov002_021fa77c
        .arm
func_ov002_021fa77c:
    stmdb sp!, {r3, r4, r5, r6, r7, lr}
    ldr r2, _LIT0
    mov r5, r0
    ldr r2, [r2, #0x5b4]
    cmp r2, #0x7
    addls pc, pc, r2, lsl #0x2
    b .L_2a4
    b .L_198
    b .L_198
    b .L_198
    b .L_2a4
    b .L_2a4
    b .L_1b8
    b .L_1e4
    b .L_240
.L_198:
    bl func_ov002_021f4a84
    cmp r0, #0x1
    ldmneia sp!, {r3, r4, r5, r6, r7, pc}
    ldr r0, _LIT0
    mov r1, #0x5
    str r1, [r0, #0x5b4]
    mov r0, #0x0
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_1b8:
    ldrh r0, [r5, #0x2]
    mov r1, #0x56
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_021ae400
    ldr r1, _LIT0
    mov r0, #0x0
    ldr r2, [r1, #0x5b4]
    add r2, r2, #0x1
    str r2, [r1, #0x5b4]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_1e4:
    bl func_ov002_022577dc
    cmp r0, #0x5
    movlt r1, r0
    movge r1, #0x5
    cmp r1, #0x20
    bge .L_208
    cmp r0, #0x5
    movge r0, #0x5
    b .L_20c
.L_208:
    mov r0, #0x20
.L_20c:
    ldrh r3, [r5, #0x2]
    add r1, r0, #0x27
    ldrh r2, [r5]
    mov r0, r3, lsl #0x1f
    mov r0, r0, lsr #0x1f
    mov r3, #0x0
    bl func_ov002_021af9d0
    ldr r1, _LIT0
    mov r0, #0x0
    ldr r2, [r1, #0x5b4]
    add r2, r2, #0x1
    str r2, [r1, #0x5b4]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_240:
    bl func_ov002_021e2b3c
    ldr r6, _LIT1
    mov r4, #0x0
    ldr r0, [r6, #0xd44]
    cmp r0, #0x0
    bls .L_28c
    mov r7, r4
.L_25c:
    add r0, r4, #0x1
    bl func_ov002_021b004c
    ldrh r3, [r5, #0x2]
    mov r1, r0
    mov r2, r7
    mov r0, r3, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_021d8128
    ldr r0, [r6, #0xd44]
    add r4, r4, #0x1
    cmp r4, r0
    bcc .L_25c
.L_28c:
    bl func_ov002_021e2c5c
    ldr r1, _LIT1
    mov r0, #0x1
    ldr r1, [r1, #0xd44]
    strh r1, [r5, #0xc]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_2a4:
    mov r0, #0x1
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
_LIT0: .word data_ov002_022ce288
_LIT1: .word data_ov002_022d016c
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|------------|------|----------------------|
| DuelQueueState | +0x5B4 | f_5b4 | int | 8-entry computed-jump dispatch (`addls pc, pc, r2, lsl #2`); arms 0/1/2 → same handler; arms 3/4/5 → return 1; arm 6 → ae400 + increment; arm 7 → 022577dc + 021af9d0 + increment |
| DuelStateSingleton | +0xD44 | f_d44 | int | loop bound in arm 8 (`ldr r0, [r6, #0xd44]`) and stored to `self->fc` at end |

## Recipe sketch

```c
int func_ov002_021fa77c(CardObj *self) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    DuelStateSingleton *dss = &data_ov002_022d016c;
    int state = dqs->f_5b4;   /* +0x5B4 */
    /* computed jump: states 0,1,2 → .L_198; 3,4,5 → return 1; 6 → .L_1b8; 7 → .L_1e4 */
    /* state 0/1/2: */
    if (state == 0 || state == 1 || state == 2) {
        if (func_ov002_021f4a84(self) != 1)
            return 1;
        dqs->f_5b4 = 5;
        return 0;
    }
    /* state 3/4/5: return 1 immediately */
    if (state == 3 || state == 4 || state == 5)
        return 1;
    /* state 6: */
    if (state == 6) {
        int bit0 = (self->f2 << 31) >> 31;
        func_ov002_021ae400(bit0, 0x56);
        dqs->f_5b4++;
        return 0;
    }
    /* state 7: clamp helper result and call 021af9d0 */
    if (state == 7) {
        int n = func_ov002_022577dc();
        int r1 = n < 5 ? n : 5;
        int r0 = r1 < 0x20 ? (n < 5 ? n : 5) : 0x20;
        int bit0 = (self->f2 << 31) >> 31;
        func_ov002_021af9d0(bit0, r0 + 0x27, self->f0, 0);
        dqs->f_5b4++;
        return 0;
    }
    /* state 8: counted loop then store dss->f_d44 to self->fc */
    {
        func_ov002_021e2b3c();
        int count = dss->f_d44;   /* +0xD44 */
        for (int i = 0; i < count; i++) {
            void *p = func_ov002_021b004c(i + 1);
            int bit0 = (self->f2 << 31) >> 31;
            func_ov002_021d8128(bit0, p, 0);
        }
        func_ov002_021e2c5c();
        self->fc = (short)dss->f_d44;
        return 1;
    }
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_021f85f8.c
**Why similar:** DuelQueueState `f_5b4` dispatch pattern with direct assignment and increment arms.
**Key lesson:** The `addls pc, pc, r2, lsl #2` computed jump (ARM switch table) is generated by mwcc for contiguous integer switch ranges. Expressing it as a chain of `if (state == N)` blocks or a `switch(state)` statement both work; the key constraint is that arms 0/1/2 share the same body (so a `case 0: case 1: case 2:` fall-through or equivalent nested `||` ifs is needed).

## Try this

1. Load `DuelQueueState *dqs = &data_ov002_022ce288;` and read `int state = dqs->f_5b4;` (+0x5B4). Use a `switch(state)` with `case 0: case 1: case 2:` sharing one body that calls `func_ov002_021f4a84` and assigns `dqs->f_5b4 = 5`.
2. For state 7, the double-clamp `min(n,5)` then `min(result, 0x20)` uses two conditional moves. Try `int clamp = (n < 5) ? n : 5; int r0 = (clamp < 0x20) ? clamp : 0x20;` — if objdiff shows extra moves, collapse into `int r0 = (n < 5 ? n : 5) < 0x20 ? (n < 5 ? n : 5) : 0x20;` to force re-evaluation.
3. If the `addls pc, pc` table shape does not appear, ensure the switch is a contiguous `0..7` range with no gaps — adding the `case 8:` body below the table range as a separate fall-through block after a `default: return 1;` gives mwcc the range-guard it needs.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov002_022ce288` | raw |
| `_LIT1` | `data_ov002_022d016c` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021f4a84` | overlay call (ov002) |
| `bl` | `func_ov002_021ae400` | overlay call (ov002) |
| `bl` | `func_ov002_022577dc` | overlay call (ov002) |
| `bl` | `func_ov002_021af9d0` | overlay call (ov002) |
| `bl` | `func_ov002_021e2b3c` | overlay call (ov002) |
| `bl` | `func_ov002_021b004c` | overlay call (ov002) |
| `bl` | `func_ov002_021d8128` | overlay call (ov002) |
| `bl` | `func_ov002_021e2c5c` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

