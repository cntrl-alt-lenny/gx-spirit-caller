# 021cfda8 — func_ov002_021cfda8 (ov002, class F retriage, 368B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Walks PerPlayerRowTable with stride 0x868 (`mla r1,r2,r0,r1`) and reads `Ov002Slot.id_lo13` via the documented `lsl#19 / lsr#19` pair at the .L_2b4 slot guard. Also reads `data_ov002_022ce950+0x810` (phase counter) and `data_ov002_022ce950+0x80c` (clear-field), both now named. The dispatch arm (0 vs 1) routes through `func_ov002_0229ade0` (event 0x21) or `func_ov002_0229c7f8` — entirely deterministic once the struct layout is known.
**Confidence:** high
**Structs needed:** PerPlayerRowTable (stride 0x868), Ov002Slot (id_lo13 bitfield), data_ov002_022ce950 (+0x810 phase, +0x80c clear)

## Assembly

```asm
; func_ov002_021cfda8 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022ce950
        .extern data_ov002_022cf16c
        .extern data_ov002_022d0250
        .extern func_ov002_021b947c
        .extern func_ov002_021b9b64
        .extern func_ov002_0229ade0
        .extern func_ov002_0229c7f8
        .global func_ov002_021cfda8
        .arm
func_ov002_021cfda8:
    stmdb sp!, {r3, r4, r5, r6, r7, lr}
    ldr r0, _LIT0
    ldrh r0, [r0]
    tst r0, #0x8000
    ldr r0, _LIT0
    movne r7, #0x1
    ldrh r5, [r0, #0x4]
    ldrh r4, [r0, #0x2]
    moveq r7, #0x0
    mov r0, r5
    bl func_ov002_021b947c
    ldr r1, _LIT0
    mov r6, #0x0
    ldr r1, [r1, #0x810]
    cmp r1, #0x0
    beq .L_260
    cmp r1, #0x1
    beq .L_354
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_260:
    cmp r4, #0xb
    beq .L_270
    cmp r4, #0x10
    blt .L_280
.L_270:
    ldr r0, _LIT0
    mov r1, #0x0
    str r1, [r0, #0x80c]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_280:
    and r1, r0, #0xff
    cmp r7, r1
    bne .L_2b4
    mov r1, r0, lsl #0x10
    mov r1, r1, lsr #0x10
    mov r1, r1, asr #0x8
    and r1, r1, #0xff
    cmp r4, r1
    bne .L_2b4
    mov r0, r0, lsr #0x10
    mov r0, r0, lsl #0x10
    mov r6, r0, lsr #0x10
    b .L_2f4
.L_2b4:
    cmp r4, #0xa
    bgt .L_2f4
    ldr r0, _LIT1
    ldr r1, _LIT2
    and r2, r7, #0x1
    mla r1, r2, r0, r1
    mov r0, #0x14
    mla r0, r4, r0, r1
    ldr r0, [r0, #0x30]
    mov r0, r0, lsl #0x13
    movs r0, r0, lsr #0x13
    bne .L_2f4
    ldr r0, _LIT0
    mov r1, #0x0
    str r1, [r0, #0x80c]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_2f4:
    cmp r4, #0xe
    bne .L_32c
    ldr r0, _LIT3
    mov r1, r5, lsl #0x2
    ldrh r1, [r0, r1]
    ldr r0, _LIT4
    mov r1, r1, lsl #0x13
    cmp r0, r1, lsr #0x13
    bne .L_32c
    mov r0, r5
    bl func_ov002_021b9b64
    ldr r1, [r0]
    bic r1, r1, #0x20000
    str r1, [r0]
.L_32c:
    mov r1, r7
    mov r2, r4
    mov r3, r6
    mov r0, #0x21
    bl func_ov002_0229ade0
    ldr r0, _LIT0
    ldr r1, [r0, #0x810]
    add r1, r1, #0x1
    str r1, [r0, #0x810]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
.L_354:
    mov r0, #0x21
    bl func_ov002_0229c7f8
    cmp r0, #0x0
    ldreq r0, _LIT0
    moveq r1, r6
    streq r1, [r0, #0x80c]
    ldmia sp!, {r3, r4, r5, r6, r7, pc}
_LIT0: .word data_ov002_022ce950
_LIT1: .word 0x00000868
_LIT2: .word data_ov002_022cf16c
_LIT3: .word data_ov002_022d0250
_LIT4: .word 0x00001571
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| data_ov002_022ce950 | +0x000 | f0 (u16) | u16 | bit 15 → r7 (player flag) |
| data_ov002_022ce950 | +0x002 | f2 (u16) | u16 | slot index → r4 |
| data_ov002_022ce950 | +0x004 | f4 (u16) | u16 | zone/row index → r5 |
| data_ov002_022ce950 | +0x810 | phase counter | int | 0 = first arm, 1 = confirm arm |
| data_ov002_022ce950 | +0x80c | clear field | int | zeroed on bail/confirm |
| PerPlayerRowTable | +0x000 | base (player&1)*0x868 | ptr | player row base |
| Ov002Slot | +0x030+slot*0x14 | id_lo13 | u13 bitfield | lsl#19/lsr#19; empty guard |
| data_ov002_022d0250 | +0x00  | u16 array | u16[] | node lookup by r5*4 |

## Recipe sketch

```c
struct Ce950 {
    u16 f0;       /* +0x000  bit15 = player flag */
    u16 f2;       /* +0x002  slot index */
    u16 f4;       /* +0x004  row/zone index */
    char _pad[0x80a];
    int  f80c;    /* +0x80C  clear-field */
    int  f810;    /* +0x810  phase counter */
};
extern struct Ce950 data_ov002_022ce950;
extern u32 data_ov002_022cf16c[];  /* PerPlayerRowTable */
extern u16 data_ov002_022d0250[];  /* node lookup table */
extern int func_ov002_021b947c(int a);
extern int func_ov002_021b9b64(int a);
extern int func_ov002_0229ade0(int kind, int a, int b, int c);
extern int func_ov002_0229c7f8(int kind);

void func_ov002_021cfda8(void) {
    int player = (data_ov002_022ce950.f0 & 0x8000) ? 1 : 0;
    int slot   = data_ov002_022ce950.f2;
    int zone   = data_ov002_022ce950.f4;
    int result = 0;
    func_ov002_021b947c(zone);

    int phase = data_ov002_022ce950.f810;
    if (phase == 0) {
        if (slot == 0xb || slot >= 0x10) {
            data_ov002_022ce950.f80c = 0;
            return;
        }
        /* check ce950 packed fields match player/slot */
        /* (field at r0 after ldrh — see .L_280 word decode) */
        /* ... if match: set result from upper bits ... */
        /* else if slot <= 0xa: check PPT slot id_lo13 == 0 → bail */
        if (slot <= 0xa) {
            char *row = (char *)data_ov002_022cf16c + (player & 1) * 0x868;
            u32 word  = *(u32 *)(row + 0x30 + slot * 0x14);
            u32 id    = (word << 19) >> 19;   /* id_lo13 bitfield */
            if (id == 0) {
                data_ov002_022ce950.f80c = 0;
                return;
            }
        }
        if (slot == 0xe) {
            u16 node = data_ov002_022d0250[zone * 2];
            u32 nodeid = (node << 19) >> 19;
            if (nodeid == 0x1571) {
                int *p = (int *)func_ov002_021b9b64(zone);
                *p &= ~0x20000;
            }
        }
        func_ov002_0229ade0(0x21, player, slot, result);
        data_ov002_022ce950.f810 = phase + 1;
    } else if (phase == 1) {
        if (func_ov002_0229c7f8(0x21) == 0)
            data_ov002_022ce950.f80c = result;
    }
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_021d1b2c.c
**Why similar:** Same data_ov002_022ce950 struct field pattern (f2 read, f2060 cleared) with func_ov002_0229ade0 event dispatch.
**Key lesson:** The g950 struct typedef with named offsets is what unlocks the field reads. Declare `f2`, `f4`, `f810`, `f80c` as explicit struct fields rather than raw offsets — mwcc's field access ordering then matches the ldrh/str sequence exactly.

## Try this
1. Declare `data_ov002_022ce950` as a struct with `u16 f0, f2, f4` at offsets 0/2/4 and `int f80c, f810` at 0x80C/0x810; read `f0 & 0x8000` for player flag; read `f2` for slot; read `f4` for zone.
2. If objdiff shows ldrh vs ldr mismatch on the slot/zone loads, confirm the fields are `u16` not `int` — the bitfield extraction at +0x002/+0x004 uses `ldrh` not `ldr`.
3. The slot check at .L_2b4 is a PerPlayerRowTable walk: use `(char *)data_ov002_022cf16c + (player & 1) * 0x868` then `slot.id_lo13` via `: 13` bitfield — this is not a PPT scan, no stride loop.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov002_022ce950` | raw |
| `_LIT1` | `0x00000868` | MMIO/const |
| `_LIT2` | `data_ov002_022cf16c` | raw |
| `_LIT3` | `data_ov002_022d0250` | raw |
| `_LIT4` | `0x00001571` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021b947c` | overlay call (ov002) |
| `bl` | `func_ov002_021b9b64` | overlay call (ov002) |
| `bl` | `func_ov002_0229ade0` | overlay call (ov002) |
| `bl` | `func_ov002_0229c7f8` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

