# 021ace68 — func_ov018_021ace68 (ov018, class C, 280B)

## Recipe + Risk
**Recipe:** CLASS C: divmod-by-name for /var; min/max via chained ?: to mirror movgt/movle
**Risk:** permuter-class: nested max-of-3/min-of-3 emit movgt/movle whose incumbent reg (r0 vs r4/r5/r6) is a coin-flip; the duplicated `if(mx<=b)else max(r,g)` branch-tail may cross-jump differently. ?: vs if-assign may flip cc sense.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021ace68 (ov018, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS C: divmod-by-name for /var; min/max via chained ?: to mirror movgt/movle; decl r,g,b first.
 *   risk:       permuter-class: nested max-of-3/min-of-3 emit movgt/movle whose incumbent reg (r0 vs r4/r5/r6) is a coin-flip; the duplicated `if(mx<=b)else max(r,g)` branch-tail may cross-jump differently. ?: vs if-assign may flip cc sense.
 *   confidence: low
 */
// func_ov018_021ace68 : BGR555 -> HSV (H 0-360, S 0-255, V=max)
// args: r0 packed colour, r1=outHue, r2=outSat, r3=outMax(value)
extern int func_020b3870(int num, int den); // mwcc signed divmod runtime, quotient in r0

void func_ov018_021ace68(int c, int *outHue, int *outSat, int *outMax)
{
    int r = (c & 0x1f) << 3;          // r4: red,   0..248
    int g = (c & 0x3e0) >> 2;         // r5: green, 0..248
    int b = (c & 0x7c00) >> 7;        // r6: blue,  0..248
    int mx, mn, delta, sat, hue;

    // value = max(r,g,b) via max(r,g) then vs b
    mx = (r > g) ? r : g;
    if (mx <= b) mx = b; else mx = (r > g) ? r : g;
    *outMax = mx;

    // min(r,g,b)
    mn = (r > g) ? g : r;
    if (mn > b) { mn = b; } else { mn = (r > g) ? g : r; }

    delta = mx - mn;
    if (mx == 0) sat = 0;
    else sat = func_020b3870(delta * 255, mx);   // (delta*255)/max
    *outSat = sat;

    if (sat == 0) { hue = 0; }
    else {
        if (r == mx)      hue = func_020b3870((g - b) << 8, delta);
        else if (g == mx) hue = func_020b3870((b - r) << 8, delta) + 0x200;
        else if (b == mx) hue = func_020b3870((r - g) << 8, delta) + 0x400;
        else              hue = 0;
        hue = (hue * 0x3c) >> 8;
        if (hue < 0) hue += 0x168;   // +360 wrap
    }
    *outHue = hue;
}
```

## Struct context
This function takes no global data — all inputs come from the argument `c` (packed BGR555) and the three output pointers. No struct from docs/research/types/ is involved. The inline field decomposition is:

| field | bits | formula | purpose |
|-------|------|---------|---------|
| r | [4:0] | (c & 0x1f) << 3 | red 0-248 |
| g | [9:5] | (c & 0x3e0) >> 2 | green 0-248 |
| b | [14:10] | (c & 0x7c00) >> 7 | blue 0-248 |

The `func_020b3870` call is the mwcc signed division runtime (variable divisor). The pattern `func_020b3870(num, den)` returning quotient in r0 is the standard variable-divisor divmod seen across ov018.

## Closest matched example
**Path:** src/overlay018/func_ov018_021accc8.c
**Why similar:** same ov018 file, uses `func_020b3870` for a signed division, and has a ternary-vs-branch pattern for the clamp conditional.
**Key lesson from that file:** `func_020b3870` must receive its arguments as `(numerator, denominator)` with no intermediate binding; mwcc schedules the `neg`/shift of the numerator inline before the `bl`. The key structural trick for the min/max chains here is that mwcc emits `movgt`/`movle` for `?:` on a `>` compare, so the ternary form `(r > g) ? r : g` is correct — do NOT convert to if-assign.

## Try this
1. Build as-is. If the `mx` computation block diverges, check which register holds `mx` going into the `*outMax = mx` store. If it is in r0 instead of r4/r5/r6, try declaring `mx` before `mn` and after `r,g,b` — declaration order controls which callee-saved register mwcc allocates for each local.
2. For the `min(r,g,b)` block: if `mn` diverges, try swapping the two forms — write the duplicated else arm as an if-assign: `if (mn > b) mn = b; else { mn = (r > g) ? g : r; }` — the duplicated `else` reconstruction is the most fragile part since the tail is RECONSTRUCTED in the disasm.
3. Fallback: confidence is LOW and the risk note says permuter-class for the max-of-3 register coin-flip. After two reshape rounds, submit to the permuter focusing on the `mx = (r > g) ? r : g; if (mx <= b) ...` block, or escalate to .s for the entire max/min section.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020b3870` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

