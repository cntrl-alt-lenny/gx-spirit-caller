# 0207dca8 — func_0207dca8 (main, class C, 80B)

## Recipe + Risk

**Recipe:** 0x4c stack struct &buf passed to 3 calls; result(r4) zero-init; r4 = *(f8+0x64) + f24 between calls
**Risk:** Struct-guessed: 0x4c frame and offsets f8(+0x8)/f24(+0x24)/inner +0x64 inferred from ldr pattern. The add r4,r2,r1 operand order (f64+f24) is reshape-able; frame size is the gate — confirm against real struct.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0207dca8 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     0x4c stack struct &buf passed to 3 calls; result(r4) zero-init; r4 = *(f8+0x64) + f24 between calls
 *   risk:       Struct-guessed: 0x4c frame and offsets f8(+0x8)/f24(+0x24)/inner +0x64 inferred from ldr pattern. The add r4,r2,r1 operand order (f64+f24) is reshape-able; frame size is the gate — confirm against real struct.
 *   confidence: med
 */
extern void func_02098388(void *buf);
extern int  func_02098038(void *buf, int arg);
extern void func_02097ff0(void *buf);

struct Buf_207dca8 {
    char _pad8[8];
    int *f8;     /* +0x8 -> ptr */
    char _pad24[0x24 - 0x8 - 4];
    int  f24;    /* +0x24 */
    char _tail[0x4c - 0x24 - 4];
};

int func_0207dca8(int arg)
{
    struct Buf_207dca8 buf;
    int result;

    result = 0;
    func_02098388(&buf);
    if (func_02098038(&buf, arg) != 0) {
        result = buf.f8[0x64 / 4] + buf.f24;
        func_02097ff0(&buf);
    }
    return result;
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `buf+0x8` | f8 | `int*` | pointer; dereferenced at `f8[0x64/4]` = `*(f8+0x64)` |
| `buf+0x24` | f24 | `int` | scalar added to `*(f8+0x64)` to form result |
| `buf` total | stack frame | 0x4c bytes | the struct occupies exactly 0x4c bytes on stack |
| `result` | r4 | `int` | zero-initialised before func_02098038 call |

## Closest matched example

**Path:** src/main/func_0207dcf8.c
**Key lesson:** func_0207dcf8 (matched, 0x2c bytes) is a smaller sibling in the same module cluster — same pattern of `if (func_A(self) == 0) return 0; func_B(self); return 1;`. The key codegen lesson from dcf8 is that `if (h() == 0) return 0` (not `int r = h(); if (r == 0) return r`) is needed to emit the `moveq r0,#0` before the predicated pop. For dca8, the equivalent is that `result = 0` before the call and `result = ...` inside the `if` body ensures mwcc keeps r4 as the return register across both paths.

## Try this

1. Write `result = buf.f8[0x64 / 4] + buf.f24` as `result = buf.f8[25] + buf.f24` (0x64/4 = 25) so the index is a compile-time constant — verify objdiff emits `ldr r2,[f8,#0x64]; add r4,r2,r1` where f24 is in r1; if the operand order is `add r4,r1,r2` instead, swap to `result = buf.f24 + buf.f8[25]`.
2. Confirm the stack frame is 0x4c bytes by checking the objdiff prologue for `sub sp,sp,#0x4c` (minus the standard push size) — if the frame is a different size, adjust `_tail` padding in the struct to match.
3. If the frame size matches but the `add` operand order still diverges, this is reshape-able — try both `f8[25]+f24` and `f24+f8[25]` orderings and accept whichever objdiff confirms.

## GROUND TRUTH (from .s)

**N/A — already matched.** This function has a shipped C source at `src/main/func_0207dca8.c` and no longer has a `.s` ground-truth stub in `brain/src` (the .s is replaced once a function matches). No further GROUND TRUTH extraction is needed or possible here; do not re-attempt this function.

