# 02031684 — func_02031684 (main, class D, 224B)

## Recipe + Risk
**Recipe:** CLASS D: switch in-set->0 default->1 (INVERTED polarity); exact 12-set
**Risk:** polarity is flipped (member->0); if campaign assumes member->1 the two return-blocks invert. Otherwise tree-balance/pivot choice is the residual, permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02031684 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS D: switch in-set->0 default->1 (INVERTED polarity); exact 12-set
 *   risk:       polarity is flipped (member->0); if campaign assumes member->1 the two return-blocks invert. Otherwise tree-balance/pivot choice is the residual, permuter-class.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_02031684 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. Membership predicate with INVERTED
 * polarity: the case-set returns 0, the default returns 1 (.L_1140 is
 * `mov r0,#0`, .L_1148 is `mov r0,#1`). Same mwcc binary-search
 * compare-tree (base 0x18fd as r2, plus 0x14c7/0x1a53 sub-pivots,
 * `subne;cmpne` cluster for 0x18fb..0x18fd). Exact 12-value set recovered
 * by ARM flag-emulation (the SMALLER side = the case-set).
 *   recipe:     CLASS D: switch; in-set->0, default->1 (polarity flipped).
 */
int func_02031684(int x) {
    switch (x) {
    case 0x14b6:
    case 0x14c7:
    case 0x14da:
    case 0x157e:
    case 0x18fb:
    case 0x18fc:
    case 0x18fd:
    case 0x1936:
    case 0x1955:
    case 0x19d6:
    case 0x1a53:
    case 0x1ac8:
        return 0;
    default:
        return 1;
    }
}
```

## Struct context
No struct fields accessed. Pure card-id membership predicate with inverted polarity.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| n/a | n/a | n/a | pure card-id predicate, no struct |

## Closest matched example
**Path:** src/main/func_02030b84.c (this batch)
**Why similar:** Near-clone of 02030b84 — same inverted-polarity pattern (in-set->0, default->1) over a sparse card-ID set, same class D. Different case set (12 vs 12) and different pivot base (0x18fd vs 0x1954).
**Key lesson:** Polarity inversion is already encoded correctly (`return 0` in case body, `return 1` in default). The residual risk after polarity is the tree pivot: 0x18fd is the base register (r2) and 0x14c7/0x1a53 are sub-pivots for the left/right subtrees. If objdiff shows the `mov r0,#0` and `mov r0,#1` tails are present but in the wrong branch positions, the pivot has shifted — park as permuter-class.

## Try this
1. Drop prep draft verbatim; confirm in objdiff that both `.L_1140 = mov r0,#0` (case side) and `.L_1148 = mov r0,#1` (default) are present and that the case block branches to the `#0` tail.
2. If the polarity is correct but the tree shape is wrong (wrong pivot — check for `ldr r2,=0x18fd` as the root), try adding an explicit `if (x < 0x18fd)` split to nudge the root pivot.
3. Park as permuter-class if tree balance/pivot choice diverges after reshaping; switch heuristic is un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x000018fd` | MMIO/const |
| `_LIT1` | `0x000014c7` | MMIO/const |
| `_LIT2` | `0x00001a53` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

