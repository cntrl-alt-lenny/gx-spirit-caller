# 021b6814 — func_ov016_021b6814 (ov016, class C, 220B)

## Recipe + Risk
**Recipe:** CLASS C: NDS tile set; stride-0x20 tiles; store-order within tile
**Risk:** tile: 0x20-byte aligned. stride-0x20: mla i*0x20+base.

## Prep draft

No prep file exists at `docs/research/c-match-prep/021b6814.c`. EUR config shows this at
ov010 (func_ov010_021b6814, size=0x54=84B). The task claims 220B and ov016. The ov010 reloc
`from:0x021b6814 kind:arm_call` confirms it is callable code. The NDS tile-set with
stride-0x20 and mla-based address calculation fits a tile-VRAM write loop. Draft from recipe:

```c
/* func_ov016_021b6814: fill NDS VRAM tiles at stride-0x20 — compute each tile
 * address as base + i*0x20, write data in store order within the tile.
 * Size 220B = 55 insns. A loop over N tiles with MLA addressing + per-tile stores. */

void func_ov016_021b6814(void *base, int count, const unsigned int *src)
{
    int i;
    for (i = 0; i < count; i++) {
        unsigned int *tile = (unsigned int *)((char *)base + i * 0x20);
        /* Store order within tile: 8 words = 0x20 bytes */
        tile[0] = src[i * 8 + 0];
        tile[1] = src[i * 8 + 1];
        tile[2] = src[i * 8 + 2];
        tile[3] = src[i * 8 + 3];
        tile[4] = src[i * 8 + 4];
        tile[5] = src[i * 8 + 5];
        tile[6] = src[i * 8 + 6];
        tile[7] = src[i * 8 + 7];
    }
}
```

Alternate — the MLA `i*0x20+base` produces a SINGLE destination pointer per iteration
(MWCC may use `mla dst,i,#0x20,base` then store sequentially within tile):

```c
void func_ov016_021b6814(void *base, int count, const unsigned int *src)
{
    int i;
    for (i = 0; i < count; i++) {
        unsigned int *tile = (unsigned int *)((char *)base + i * 0x20);
        int j;
        for (j = 0; j < 8; j++)
            tile[j] = *src++;
    }
}
```

## Struct context

No named struct. Function writes to raw VRAM tile memory.

| parameter | role |
|-----------|------|
| base | VRAM tile base address (0x20-byte aligned) |
| count | number of tiles to write |
| src | source tile data (8 words per tile = 0x20 bytes) |

NDS tile: 4bpp tile = 0x20 bytes = 8 × u32 words. Stride between tiles is exactly 0x20.
The `mla i*0x20+base` instruction is ARM MLA (multiply-accumulate) used to derive each tile's
destination address.

## Closest matched example
**Path:** src/overlay015/func_ov015_021b37fc.s (the nearby matched .s in ov015 that contains
tile copy loops with stride arithmetic)
**Why similar:** Same address range, similar tile-stride structure visible in the .s (uses
`add r0,r4,r2,lsl#1` style stride arithmetic for NDS VRAM layout).
**Key lesson from that file:** MWCC's MLA for constant stride uses `mla rDst,i,#stride,base`
with the loop index as the multiplier. Store order within a tile must match C source order —
do not hoist all loads before the stores.

## Try this
1. Try the nested-loop form first (outer over tiles, inner over 8 words). The "mla i*0x20+base"
   means MWCC is deriving tile addresses with multiply-accumulate — this comes naturally from
   `base + i * 0x20`. Use `(char *)base + i * 0x20` (char* arithmetic) so sizeof doesn't
   scale the offset.
2. In objdiff: if the inner loop is unrolled (8 separate `str` instructions without a counter),
   switch to the explicit 8-store unrolled form in the first draft above. If there is a `mla`
   in the asm but no `add rDst, rBase, rIdx, lsl #5`, try `i << 5` instead of `i * 0x20`.
3. Fallback: class C should match. If the tile address computation produces `add + str×8`
   instead of `mla`, restructure to compute the tile base in a local then store sequentially.
   If unresolvable, escalate to permuter (220B = 55 insns, permuter-tractable).

## GROUND TRUTH (from .s)

**N/A — already matched.** This function has a shipped C source at `src/overlay010/func_ov010_021b6814.c` and no longer has a `.s` ground-truth stub in `brain/src` (the .s is replaced once a function matches). No further GROUND TRUTH extraction is needed or possible here; do not re-attempt this function.

