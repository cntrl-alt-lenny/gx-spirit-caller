# 0208610c — func_0208610c (main, class D, 552B)

## Recipe + Risk
**Recipe:** bitset RMW; mla idx*0x18; 6x FX12 smull>>12; Copy32 records; store-order
**Risk:** batch asm truncated before .L_9b0 6x-smull tail; ip=idx*0x18 byte-offset is held across all 6 scales while r3=idx*0x18 word-stride aliases — mwcc reg-alloc of the two parallel indices is permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208610c (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     bitset RMW; mla idx*0x18; 6x FX12 smull>>12; Copy32 records; store-order
 *   risk:       batch asm truncated before .L_9b0 6x-smull tail; ip=idx*0x18 byte-offset is held across all 6 scales while r3=idx*0x18 word-stride aliases — mwcc reg-alloc of the two parallel indices is permuter-class.
 *   confidence: low
 */
/* func_0208610c: flag dispatch (r3 bit2) — bitset set/clear in *data_021a1b1c[0xc4..]
 * (idx=arg[1],arg[2]) plus Copy32 of 0x18-byte stride records data_021a2920..2934[idx*0x18],
 * with a 6x FX12 scale (smull>>12) in the .L_9b0 tail.  r0=obj, r1=src, r2=arg, r3=mask. */
typedef unsigned char u8;
typedef unsigned int u32;
typedef signed int s32;
typedef signed long long s64;

extern void Copy32(void *dst, void *src, int n);
extern u32 *data_021a1b1c;
extern u32  data_021a2920[], data_021a2924[], data_021a2928[];
extern u32  data_021a292c[], data_021a2930[], data_021a2934[];

void func_0208610c(u32 *obj, u32 *src, u8 *arg, int mask) {
    u8 a1 = arg[1];
    u8 a2 = arg[2];
    if (mask & 4) {
        u32 *base = data_021a1b1c;
        obj[0] |= 1;
        if ((1u << (a2 & 0x1f)) & base[(0xc4 / 4) + (a2 >> 5)]) {
            u32 *bs = base + (0xc4 / 4);
            bs[a1 >> 5] |= 1u << (a1 & 0x1f);
            obj[0] |= 0x18;
        } else {
            Copy32((char *)data_021a2920 + a2 * 0x18, (char *)data_021a2920 + a1 * 0x18, 0x18);
            Copy32((char *)data_021a2920 + a2 * 0x18, obj + 4, 0x18);
        }
    } else {
        u32 *base = data_021a1b1c;
        obj[1] = src[0];
        obj[2] = src[1];
        obj[3] = src[2];
        if ((1u << (a2 & 0x1f)) & base[(0xc4 / 4) + (a2 >> 5)]) {
            Copy32(src, (char *)data_021a2920 + a1 * 0x18, 0x18);
            base = data_021a1b1c;
            { u32 *bs = base + (0xc4 / 4); bs[a1 >> 5] &= ~(1u << (a1 & 0x1f)); }
            obj[0] |= 0x18;
        } else {
            u32 *bs = base + (0xc4 / 4);
            int w = a1 * 6;
            int o2 = a2 * 6;
            bs[a1 >> 5] &= ~(1u << (a1 & 0x1f));
            data_021a2920[w] = (s32)(((s64)src[0] * data_021a2920[o2]) >> 12);
            data_021a2924[w] = (s32)(((s64)src[1] * data_021a2924[o2]) >> 12);
            data_021a2928[w] = (s32)(((s64)src[2] * data_021a2928[o2]) >> 12);
            data_021a292c[w] = (s32)(((s64)src[3] * data_021a292c[o2]) >> 12);
            data_021a2930[w] = (s32)(((s64)src[4] * data_021a2930[o2]) >> 12);
            data_021a2934[w] = (s32)(((s64)src[5] * data_021a2934[o2]) >> 12);
            Copy32(data_021a2920 + w, obj + 4, 0x18);
        }
    }
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| arg[1] | a1 | u8 | destination bitset index; `a1*0x18` byte stride into data_021a2920..2934; `a1*6` word stride |
| arg[2] | a2 | u8 | source bitset index for conditional test and Copy32 source offset |
| data_021a1b1c | bitset_base | u32* | dereferenced bitset; 0xc4/4 = 0x31 word offset to active region |
| data_021a2920..2934 | scale_arrays | u32[] | 6 parallel stride-6-word arrays; FX12 scale factor by element |
| obj[0] | flags | u32 | OR-accumulated with 1, 0x18, 0x20/8 depending on branch |
| obj[1..3] | | u32 | = src[0..2] in else branch |

## Closest matched example
**Path:** src/main/func_020822a0.c
**Key lesson:** func_020822a0 demonstrates that a 12-byte struct copy via `data_triple = *src` produces `ldmia/stmia` naturally. The Copy32 calls here use byte arithmetic (`(char *)data_021a2920 + a1 * 0x18`) to compute the 0x18-byte stride; the `mla` instruction (`ip = a1 * 0x18`) holding the byte offset while `r3 = a1 * 6` holds the word stride is the two-index liveness that makes this permuter-class.

## Try this
1. In the 6x-smull else-branch, declare `int w = a1 * 6` and `int o2 = a2 * 6` as the first two locals in that block scope — this gives mwcc both word-stride indices before the first smull and may keep `w` in a callee-saved register across all 6 `smull>>12` stores. Do not fold `a1 * 6` inline into the array index.
2. Each `(s32)(((s64)src[N] * data_021a292cN[o2]) >> 12)` must use a `(long long)` or `(s64)` cast to force `smull`; do not let the multiply be 32-bit. The `>> 12` shift on the 64-bit result produces the `asr#12` on the high word idiom — verify via objdiff that each of the 6 stores emits `smull; asr`.
3. Park as permuter-class; the batch asm is truncated before .L_9b0 and the parallel ip/r3 dual-index for byte vs word stride across 6 smull calls is a register-allocation coin-flip that cannot be forced via C reshape without the macOS permuter.
