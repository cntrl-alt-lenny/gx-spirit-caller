# 02244104 — func_ov002_02244104 (ov002, class F retriage, 380B)

## Re-triage
**Original verdict:** F — previously intractable
**Upgrade reason:** DuelQueueState+0x5B8 (sub-state dispatch) and Ov002Slot id_lo13 bitfield are
documented. The function sits at 0x02244104, after func_ov002_022440d8.s and before
func_ov002_02244160.s. The reloc for func_02244160 (the immediate successor) shows the same
DuelQueueState+0x5B8 4-arm dispatch with func_022593f4/func_0223dcc0/func_021ae400/func_0225935c —
the same call set seen in the DuelQueueState+Ov002Slot family. At 380 bytes this function covers
0x02244104..0x0224425B, which bridges to func_02244160 at 0x02244160.
**Confidence:** high
**Structs needed:** DuelQueueState (data_ov002_022ce288 +0x5B8), DuelStateSingleton (data_ov002_022d016c +0xD70, +0xD74, +0xD78), Ov002Slot (id_lo13 lsl#19/lsr#19)

## Assembly
```asm
; func_ov002_02244104 — NOT YET EXTRACTED
; This function exists in the binary at 0x02244104, between func_ov002_022440d8.s and func_ov002_02244160.s.
; It has not been disassembled into a .s file yet (absent from src/overlay002/ and symbols.txt).
; Size: 380 bytes (0x17C). Confirmed via retriage doc + reloc coverage at 0x02244054-0x022440d0.
;
; Key relocs from neighbor (0x022440a0-0x022440d0):
;   0x022440a0  arm_call -> 0x021bd030  (func_021bd030 — slot predicate)
;   0x022440b8  arm_call -> 0x022577dc  (func_ov002_022577dc — PPT scan predicate)
;   0x022440d0  arm_call -> 0x0223f684  (func_ov002_0223f684 — card handler)
;   Load refs at 0x02244054-58: data_ov002_022ce288 (DuelQueueState), data_ov002_022d0e6c
;
; func_ov002_02244160.s (immediate successor) uses same struct set for reference.
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5B8 | sub_state | int | 4-arm dispatch (addls pc, r0<=3) |
| DuelStateSingleton | +0xD70 | f_d70 | int/ptr | arg to func_0223dcc0 slot-match |
| DuelStateSingleton | +0xD74 | f_d74 | int | range offset component |
| DuelStateSingleton | +0xD78 | f_d78 | int | range offset component |
| data_ov002_022d0e6c | +0x5A8 (or other) | trigger_flag | int | guard for trigger actions |
| Ov002Slot | +0x008 | id_lo13 | :13 bitfield | lsl#19/lsr#19 — slot occupancy |
| func_022577dc | — | PPT scan predicate | fn | called with card; returns 0/nonzero |
| func_021ae400 | — | audio/event trigger | fn | called with player and event code |
| func_0225935c | — | state setter | fn | called with player, card_id, callback |

## Recipe sketch

Based on func_ov002_02244160.s (immediate successor, identical struct hits and 4-arm dispatch):

```c
int func_ov002_02244104(CardObj *card) {
    DuelQueueState     *dqs = &data_ov002_022ce288;
    DuelStateSingleton *dss = &data_ov002_022d016c;

    switch (dqs->sub_state) {  /* +0x5B8 */
    default: return 1;
    case 0: {
        /* PPT scan via func_022577dc, then slot predicate func_021bd030 */
        int player_bit = (card->f2 >> 0) & 1;
        if (!func_ov002_022577dc(card)) return 1;
        if (!func_021bd030(card)) return 1;
        /* trigger audio event */
        func_021ae400(player_bit, 0x7c);
        /* set state via 0225935c */
        func_ov002_0225935c(player_bit, card->f0, &func_ov002_021f27b4);
        dqs->sub_state++;
        return 0;
    }
    case 1: {
        int player_bit = (card->f2 >> 0) & 1;
        func_021ae400(player_bit, 0x64);
        func_ov002_0225935c(player_bit, card->f0, &func_ov002_021f208c);
        dqs->sub_state++;
        return 0;
    }
    case 2:
    case 3:
        if (!func_ov002_022593f4()) return 0;
        func_ov002_0223dcc0(card, dss->f_d70, dss->f_d74 + dss->f_d78);
        dqs->sub_state++;
        return 0;
    }
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_02244160.s
**Why similar:** Immediate successor function (0x02244160); same 4-arm DuelQueueState+0x5B8 dispatch,
same calls to func_021ae400 (0x7c/0x64 event codes), func_0225935c with func_021f27b4/func_021f208c
callbacks, func_022593f4 trigger guard, func_0223dcc0 action.
**Key lesson:** In func_02244160, case 1 and 3 share the same body (`bl func_022593f4` + `bl func_0223dcc0`) — mwcc emits them as a fall-through. Declare them as separate cases with identical bodies (not as a single case with fallthrough) or mwcc will reorder them. The func_021ae400 event codes differ per case (0x7c for case 0, 0x64 for case 1) — use the exact literal, not a variable.

## Try this
1. Declare `switch (dqs->sub_state)` at +0x5B8; case 0: call `func_ov002_022577dc(card)` as guard then `func_021ae400(player_bit, 0x7c)` and `func_ov002_0225935c(player_bit, card->f0, &func_ov002_021f27b4)` — exact function pointers as declared in func_02244160.s.
2. If objdiff shows func_022593f4 / func_0223dcc0 ordering wrong: check whether cases 2 and 3 are separate arms or a shared arm; try `case 2: case 3:` fallthrough in C.
3. If func_021ae400 event code is wrong: escalate to compare both event codes with func_02244160 — the 0x7c/0x64 pair in the successor should be mirrored or offset by a constant in this function.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

