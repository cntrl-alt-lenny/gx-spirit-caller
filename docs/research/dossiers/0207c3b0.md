# 0207c3b0 — func_0207c3b0 (main, class C, 212B)

## Recipe + Risk

**Recipe:** CLASS C: decl-order node/head fields; bind head base once; cond-store splice via if/else; ldreq/streq guarded tail
**Risk:** struct-guessed/permuter-class: the r3 walk-var (p) must stay live to gate the final ldreq/streq block (cmp r3,#0); my dual node->f4 assignment is malformed C - the f0 read+inc must compile to ldr/add/str. Field offsets (f4=tail,f8=head) inferred from store directions; confirm Head/Node layout.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0207c3b0 (main, class C, MED tier) — brief 496.
 *   recipe:     CLASS C: decl-order node/head fields; bind head base once; cond-store splice via if/else; ldreq/streq guarded tail
 *   risk:       struct-guessed/permuter-class: the r3 walk-var (p) must stay live to gate the final ldreq/streq block (cmp r3,#0); my dual node->f4 assignment is malformed C - the f0 read+inc must compile to ldr/add/str. Field offsets (f4=tail,f8=head) inferred from store directions; confirm Head/Node layout.
 *   confidence: low
 */
typedef struct Node {
    int          f0;
    struct Node *f4;   /* +0x4 */
    struct Node *f8;   /* +0x8 */
    struct Node *fc;   /* +0xc */
} Node;

typedef struct {
    int  f0;
    Node *f4;          /* head->+0x4 list tail/seed */
    Node *f8;          /* head->+0x8 list head     */
    int  fc;
} Head;

extern void *func_0207b538(void);

void func_0207c3b0(Node *node) {
    char *raw = (char *)func_0207b538() + 0x2000;
    Head *head = *(Head **)(raw + 0x270);
    int  count;
    Node *p;
    if (node == 0) return;
    if (head == 0) return;
    count = *(int *)(raw + 0x274);
    if ((unsigned)count <= 0xc) return;
    p = head->f4;
    if (p != 0) {
        for (;;) {
            if (p == node) {
                Node *nx = node->f8;
                if (nx != 0) nx->fc = node->fc;
                else head->f4 = node->fc;
                nx = node->fc;
                if (nx != 0) nx->f8 = node->f8;
                else head->f8 = node->f8;
                break;
            }
            p = p->fc;
            if (p == 0) break;
        }
    }
    node->fc = 0;
    node->f8 = head->f8;
    head->f8 = node;
    if (node->f8 != 0) node->f8->fc = node;
    else head->f4 = node;
    if (p == 0) {
        node->f4 = head->f0 ? (Node *)0 : 0; /* placeholder, replaced below */
        node->f4 = (Node *)(long)((Head *)head)->f0;
        ((Head *)head)->f0++;
    }
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `func_0207b538()+0x2270` | head ptr | `Head *` | base of the list head object |
| `func_0207b538()+0x2274` | count | `int` | threshold: `<= 0xc` → early return |
| `Head.f0` | count/f0 | `int` | read as seed value for `node->f4`, then incremented |
| `Head.f4` | tail | `Node *` | walk start; updated on unlink |
| `Head.f8` | front | `Node *` | updated on insert |
| `Node.f4` | next | `Node *` | walk direction |
| `Node.f8` | prev | `Node *` | back-link |
| `Node.fc` | next-walk | `Node *` | walk-forward link in loop |

The `.s` confirms: the early-return guards for `node==0` and `head==0` use `ldmeqia;bxeq` (predicated return). The count guard uses `ldmlsia;bxls` (unsigned <=). The walk uses r3 as the walker, which must be preserved to gate the final `cmp r3,#0; ldreq/streq` tail block.

## Closest matched example

**Path:** `src/main/List_Unlink.c`

**Key lesson:** The doubly-linked-list splice in the `.s` uses predicated stores (`strne`/`streq`) for the neighbor-fixup paths, same as `List_Unlink`. The critical difference is the r3 walk-variable: it must stay live from the loop exit all the way to the final `cmp r3,#0` tail check. In C, `p` must be declared outside the `if(p!=0)` block and must not be reassigned before the tail check. The `head->f0++` is a separate ldr+add+str triple (not a pre/post-increment on an int pointer).

## Try this

1. Already shipped as `src/main/func_0207c3b0.s`. The `.s` confirms field offsets: `Head.f4` = tail (walk start), `Head.f8` = front insert point, `Node.fc` = forward walk link.
2. If attempting C: remove the malformed double-assignment to `node->f4` in the draft. Replace with: `node->f4 = (Node *)head->f0; head->f0++;` as two separate statements. Declare `p` before the `if (node==0)` check so it stays in scope for the tail `if (p==0)` test.
3. Park as permuter-class if the predicated `ldreq`/`streq` tail block does not reproduce — the `cmp r3,#0` gating depends on r3 staying live across the insert sequence, which is a register-allocation scheduling decision.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0207b538` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

