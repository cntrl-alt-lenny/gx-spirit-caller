# 0200fd1c — func_0200fd1c (main, class C, 104B)

## Recipe + Risk

**Recipe:** D: load fc once, derive bit via <<29>>31, two guarded eq-returns
**Risk:** orig loads fc into r3, derives bit into ip via movs, reuses BOTH r3(>>12) and ip(flag) downstream; if mwcc reloads fc for the >>12 path instead of keeping r3, an extra ldr diverges. reshape-able via single fc local

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0200fd1c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: load fc once, derive bit via <<29>>31, two guarded eq-returns
 *   risk:       orig loads fc into r3, derives bit into ip via movs, reuses BOTH r3(>>12) and ip(flag) downstream; if mwcc reloads fc for the >>12 path instead of keeping r3, an extra ldr diverges. reshape-able via single fc local
 *   confidence: med
 */
/* func_0200fd1c (main, class C/D) — single list walk @data_0218dbc8+0x910.
 * node guard bit2 = (node->fc<<0x1d)>>0x1f. If clear: match node->f8==arg[0];
 * else if bit set: match arg[2]==(node->fc>>12u). next@0, f8@8, fc@0xc. */

typedef struct Node {
    struct Node *next;  /* 0x0 */
    int          pad4;  /* 0x4 */
    int          f8;    /* 0x8 */
    unsigned int fc;    /* 0xc */
} Node;

extern char data_0218dbc8;

Node *func_0200fd1c(int *arg)
{
    Node *n = *(Node **)(&data_0218dbc8 + 0x910);
    if (n != 0) {
        do {
            unsigned int fc = n->fc;
            unsigned int bit = (fc << 0x1d) >> 0x1f;
            if (bit == 0) {
                if (n->f8 == arg[0]) return n;
            } else {
                if ((unsigned int)arg[2] == (fc >> 12)) return n;
            }
            n = n->next;
        } while (n != 0);
    }
    return 0;
}
```

## Struct context

This function accesses `data_0218dbc8` at `+0x910` for the list head. This is the same BSS block used by `func_0200fd84` and other nearby lookup functions, but at a different offset (+0x910 not +0x90c). No SystemWork.

| offset | field | type | use |
|--------|-------|------|-----|
| data_0218dbc8+0x910 | list head | `Node *` | start of the walked linked list |
| node+0x0 | next | `Node *` | forward pointer |
| node+0x8 | f8 | `int` | match key for the bit==0 path; compared to `arg[0]` |
| node+0xc | fc | `unsigned int` | loaded ONCE into a local; bit 2 derived via `<<0x1d; >>0x1f`; high bits `>>12` used on the bit==1 path |

The critical register reuse: `fc` is loaded into `r3`, then `bit = (fc<<29)>>31` is placed into `ip` via `movs ip,r3,lsl#29; lsr ip,ip,#31`. Both `r3` (for `fc>>12`) and `ip` (for the flag test) are reused downstream without reloading `n->fc`.

Near-clone of the guard pattern in 0200fb18, but this function is smaller (104 B), single-list only, and the bit==1 path uses `fc>>12` vs `arg[2]` (not a key match).

## Closest matched example

**Path:** src/main/func_0200eef0.c
**Why similar:** Both are compact (~100 B) linked-list walkers that load a node field once and reuse it across two different conditions inside the loop body.
**Key lesson:** The `unsigned int fc = n->fc;` local is essential — it must be the only load of `n->fc`, binding the value to a single register (`r3`) that is used for BOTH the `<<29;>>31` bit extraction AND the `>>12` key computation. If MWCC reloads `n->fc` for the `>>12` path (adding an extra `ldr`), the divergence is confirmed — fix by ensuring `fc` is an `unsigned int` local (not declared `volatile`) and that both uses reference `fc` not `n->fc` directly. The prep draft already does this.

## Try this

1. Build as-is. In objdiff check that there is only ONE `ldr` of `[n, #0xc]` per loop iteration. If a second `ldr` appears for the `fc >> 12` expression, the CSE is broken — confirm both `bit == 0` and `bit != 0` branches reference the same `fc` local rather than re-reading `n->fc`.
2. If the guard bit extraction diverges: the `(fc << 0x1d) >> 0x1f` form should produce `lsl#0x1d; lsr#0x1f`. If it emits `(fc >> 2) & 1` instead, try adding a `movs` hint by declaring `unsigned int bit = (unsigned int)(fc << 0x1d) >> 0x1f;` with explicit casts to preserve the shift-pair.
3. Park as `.s` if an extra `ldr` of `n->fc` persists after confirming the local variable is in scope across both branches — this would indicate a register-pressure spill that is permuter-class. Given confidence MED, attempt at most 2 reshapes before parking.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0218dbc8` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

