# 02011620 — func_02011620 (main, class D, 104B)

## Recipe + Risk

**Recipe:** angle>>4*2 shared index; ldrsh s,c; rsb=-s; 12-int store-order; 0x1000=1.0
**Risk:** reshape-able/permuter-class: the two ldrsh loads are interleaved among the early zero-stores in asm; mwcc -O4 scheduling of the 2 loads + 8 zero stores is the only likely diverger (a coin-flip, values/order otherwise exact)

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02011620 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     angle>>4*2 shared index; ldrsh s,c; rsb=-s; 12-int store-order; 0x1000=1.0
 *   risk:       reshape-able/permuter-class: the two ldrsh loads are interleaved among the early zero-stores in asm; mwcc -O4 scheduling of the 2 loads + 8 zero stores is the only likely diverger (a coin-flip, values/order otherwise exact)
 *   confidence: med
 */
/* func_02011620 @ 0x02011620 (main, class D)
 * sincos table lookup + 3x4 (0x30-byte) matrix store fill. X-axis rotation.
 * r0 = dst matrix (12 ints), r1 = angle.
 *
 *   j = (angle >> 4) * 2;          // signed asr#4, then idx*2 (shared)
 *   s = tbl[j];                    // ldrsh, sin
 *   c = tbl[j + 1];                // ldrsh, cos
 *   m[0]=0x1000; m[1]=0;   m[2]=0; m[3]=0;
 *   m[4]=c;      m[5]=s;   m[6]=0; m[7]=-s;
 *   m[8]=c;      m[9]=0;   m[10]=0; m[11]=0;
 *
 * STORE-ORDER per asm; rsb => unary minus on s; asr#4 => signed >>4;
 * j and j+1 share the idx*2 product.
 */

extern short data_020bef80[];

void func_02011620(int *m, int angle)
{
    int j;
    int s;
    int c;

    j = (angle >> 4) * 2;
    s = data_020bef80[j];
    c = data_020bef80[j + 1];

    m[0]  = 0x1000;
    m[1]  = 0;
    m[2]  = 0;
    m[3]  = 0;
    m[4]  = c;
    m[5]  = s;
    m[6]  = 0;
    m[7]  = -s;
    m[8]  = c;
    m[9]  = 0;
    m[10] = 0;
    m[11] = 0;
}
```

## Struct context

No global struct accessed beyond the sin/cos lookup table.

| offset | field | type | use |
|--------|-------|------|-----|
| data_020bef80[j] | s | short (ldrsh) | sin value at angle index j |
| data_020bef80[j+1] | c | short (ldrsh) | cos value at angle index j+1 |

`data_020bef80` is the confirmed sin/cos table (matched in `src/main/data_020bef80.c`). The index `j = (angle >> 4) * 2` uses a signed arithmetic right shift (`asr#4`) — `angle` must be `int` (not `unsigned`) for MWCC to emit `asr`.

Near-clone sibling: **02011688** (same recipe but different rotation axis, loads `c` before `s`, and is a leaf function). **02011620** loads `s` first; **02011688** loads `c` first.

## Closest matched example

**Path:** src/main/func_020115e0.c
**Why similar:** 12-int fixed-pattern matrix fill using `0x1000` as the fixed-point 1.0 identity constant; store-order-sensitive sequential word writes to a flat `int*` output.
**Key lesson:** `data_020bef80` table uses `ldrsh` (signed halfword); declare it `short[]` not `unsigned short[]` — the sign extension into `int s` is implicit and required for the `-s` store at `m[7]` to emit `rsb` (not a separate negation instruction). The `asr#4` shift requires `angle` to be a signed `int`; if declared `unsigned int` the compiler emits `lsr#4` instead. The load/store interleaving of `s` and `c` among the zero-stores is the permuter-class risk — if it does not match after two attempts, accept as `.s`.

## Try this

1. Build as-is; in objdiff verify that `m[0]=0x1000` appears first (before the `ldrsh` loads) or after them, matching the ASM interleaving. If the zero-stores appear before both loads but ASM interleaves them, try declaring `s` and `c` after the first few stores: move `int s; int c;` declarations immediately before the `j =` expression but after `m[0] = 0x1000;`.
2. If `-s` at `m[7]` emits `neg` instead of `rsb`: verify `s` is `int` (not `short`) — the `rsb` requires the negation to fold into a load-sign-extend followed by `rsb r_out, r_s, #0`. Do not use `(int)(-s)` cast; keep unary `-s` directly.
3. When to park: if the load/store interleaving across the two `ldrsh` and eight zero-stores cannot be forced after two reshape attempts, this is permuter-class scheduling — accept as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020bef80` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

