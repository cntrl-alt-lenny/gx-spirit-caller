# 021ac208 — func_ov008_021ac208 (ov008, class D, 164B)

## Recipe + Risk

**Recipe:** C: byte-combine index via shifts; bind row base; nested-if guard chain; if-assign ok=1
**Risk:** byte-combine: (a<<8)|b = index; bind row=base+idx before guard.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021ac208 (ov008, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     C: byte-combine index via shifts; bind row base; nested-if guard chain; if-assign ok=1
 *   risk:       permuter-class: mwcc peepholes (hw<<24)>>24 to and#0xff so it can't fuse into the index add (orig keeps lsr#24 in add operand-2); brief-403 permuter-probed, no match -> shipped .s.
 *   confidence: low
 */
/* func_ov008_021ac208 — guarded dispatch keyed by a stride-52 row whose index
 * is the sum of the two bytes packed in data_ov008_021b2dc0.u16@+4. */
extern unsigned char data_02104bac[];      /* MMIO-ish; u16 read at +0x54 */
extern unsigned char data_ov008_021b2790[];/* stride-52 rows: u16@+0, int@+4 */
extern unsigned char data_ov008_021b2dc0[];
extern void func_02006164(void);
extern int  func_ov008_021aa578(int, int, int, int);
extern void func_ov008_021b1e0c(int);
extern void func_ov008_021b1e20(int, int);
extern int  func_ov008_021b1ee0(void);
extern void func_ov008_021b2028(int);

void func_ov008_021ac208(void) {
    unsigned hw = *(unsigned short *)(data_ov008_021b2dc0 + 4);
    int idx = (hw & 0xff) + ((hw << 16) >> 24);   /* byte0 + byte1 */
    unsigned char *row = data_ov008_021b2790 + idx * 52;
    int ok = 0;
    func_ov008_021b1e0c(*(int *)(row + 4));
    func_ov008_021b1e20(*(unsigned short *)row, 0);
    if (func_ov008_021b1ee0() != 0) {
        if (func_ov008_021aa578(0xe3, 0, 0x100, 0x14) != 0) {
            if (func_02006164() != 0) ok = 1;
        } else if (*(unsigned short *)(data_02104bac + 0x54) & 0x800) {
            ok = 1;
        }
    }
    func_ov008_021b2028(ok);
}
```

## Struct context

`data_ov008_021b2790` is the stride-52 row array confirmed in `ov008_core.h` and used in `func_ov008_021aa900`. The row layout at +0 is a `u16` (passed to `func_ov008_021b1e20`), and at +4 is an `int` (passed to `func_ov008_021b1e0c`).

`data_02104bac` at +0x54 is the `GlobalData02104bac.flags` field (confirmed HIGH confidence, 3 files). The bit tested is 0x800 (bit 11).

| expr | offset | type | purpose |
|------|--------|------|---------|
| data_ov008_021b2dc0 + 4 | +4 | unsigned short | packed byte-pair for index |
| row + 0 | +0 | unsigned short | row key for func_ov008_021b1e20 |
| row + 4 | +4 | int | row value for func_ov008_021b1e0c |
| data_02104bac + 0x54 | +0x54 | unsigned short | GlobalData02104bac.flags; bit 0x800 |

## Closest matched example

**Path:** src/overlay008/func_ov008_021b2028.c
**Why similar:** Both are ov008 dispatch functions with a nested `if (func_ov008_021b1ee0() != 0 && ...)` guard chain that ends with `func_ov008_021b2028(ok)`, feeding an ok/0 flag.
**Key lesson from that file:** `ov008_core.h` (brief 403) explicitly catalogues `021ac208` as a **byte-combine builder** that was permuter-probed (~7k iters, no match) and shipped as whole-function `.s`. The root cause: mwcc peepholes `(hw << 16) >> 24` (extract byte1) into `and #0xff` which prevents it from fusing into the `add idx` as `operand-2 lsr#N`. The siblings `021aceac` and `021adbbc` share the same root cause.

## Try this

1. Per `ov008_core.h` brief-403: this function is a confirmed permuter failure. The peephole that converts the shift-extract into `and #0xff` is mwcc-internal and cannot be defeated from C. Do not attempt further direct-mwcc reshapes.
2. Check whether the function is `kind:data-clean` (no kind:data symbol refs — only code). If so, ship immediately as whole-function `.s`. The data refs here (`data_02104bac`, `data_ov008_021b2790`, `data_ov008_021b2dc0`) are kind:data; confirm they are link-compatible with `.s` before routing.
3. If a data-ref blocks the `.s` route, the variable-shift workaround from the `021acfa0` permuter probe (`b0 = 24; b0 = hw << b0;`) may survive the peephole — but `ov008_core.h` notes the residual load-hoist/store-sink cascade didn't close for `021acfa0`. Route to `.s` or mark as parked.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov008_021b2dc0` | raw |
| `_LIT1` | `data_ov008_021b2790` | raw |
| `_LIT2` | `data_02104bac` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov008_021b1e0c` | overlay call (ov008) |
| `bl` | `func_ov008_021b1e20` | overlay call (ov008) |
| `bl` | `func_ov008_021b1ee0` | overlay call (ov008) |
| `bl` | `func_ov008_021aa578` | overlay call (ov008) |
| `bl` | `func_02006164` | main TU call |
| `bl` | `func_ov008_021b2028` | overlay call (ov008) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

