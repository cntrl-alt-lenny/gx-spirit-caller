# 02031470 — func_02031470 (main, class D, 284B)

## Recipe + Risk
**Recipe:** CLASS D: switch over exact 15-value set; in-set->1 default->0
**Risk:** tightly-packed runs 0x1a24/25 and 0x1b41-44 use `subne;cmpne;cmpne` equality chains; mwcc may instead emit a tiny jump-table or reorder pivots. tree-balance is permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02031470 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS D: switch over exact 15-value set; in-set->1 default->0
 *   risk:       tightly-packed runs 0x1a24/25 and 0x1b41-44 use `subne;cmpne;cmpne` equality chains; mwcc may instead emit a tiny jump-table or reorder pivots. tree-balance is permuter-class.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_02031470 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. Larger membership predicate; PURE
 * binary-search compare-tree (NO jump-table despite the triage note),
 * reusing two literal bases (0x168f as r3, plus per-subtree r2 pivots) via
 * sub/add #imm and `subne/cmpne` equality clusters for the
 * 0x1a24/0x1a25 and 0x1b41..0x1b44 runs. Exact 15-value set recovered by
 * ARM flag-emulation.
 *   recipe:     CLASS D: switch over exact case-set; in-set->1, default->0.
 */
int func_02031470(int x) {
    switch (x) {
    case 0xfe0:
    case 0xfee:
    case 0x114f:
    case 0x158a:
    case 0x1594:
    case 0x168f:
    case 0x1774:
    case 0x179c:
    case 0x1a24:
    case 0x1a25:
    case 0x1a60:
    case 0x1b41:
    case 0x1b42:
    case 0x1b43:
    case 0x1b44:
        return 1;
    default:
        return 0;
    }
}
```

## Struct context
No struct fields accessed. Pure card-id membership predicate.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| n/a | n/a | n/a | pure card-id predicate, no struct |

## Closest matched example
**Path:** src/main/func_0203158c.c
**Why similar:** Same class D sparse-set predicate (in-set->1, default->0) with pure binary-search tree lowering and base-CSE pivot reuse; verified byte-exact.
**Key lesson:** The prep confirms this uses a PURE compare-tree (no jump-table) despite having two tightly-packed pairs/quads (0x1a24/25, 0x1b41-44). The original uses `subne;cmpne;cmpne` chains for those runs, NOT jump-tables. The 0x168f literal is the tree's primary base register (r3); mwcc reuses it via sub/add offsets throughout. If objdiff shows mwcc chose a jump-table for 0x1b41-44, the tree/table cut is permuter-class.

## Try this
1. Drop prep draft verbatim; in objdiff confirm the 0x1a24/0x1a25 pair appears as `subne;cmpne` (two equality checks, NOT a 2-slot table) and similarly for 0x1b41..0x1b44 as a `subne;cmpne;cmpne;cmpne` chain.
2. If mwcc emits a mini jump-table for the 0x1b41-44 quad instead, the run is above the density threshold; try separating them by inserting an intermediate compare to break the contiguous block: `case 0x1b41: case 0x1b43: case 0x1b44: return 1; case 0x1b42: return 1;` (interleaving with an explicit jump to break linearity).
3. Park as permuter-class if tree balance or the chain/table cut diverges after reshaping; mwcc switch heuristic pivot choice is un-forceable without macOS permuter.
