# 02037fe4 — func_02037fe4 (main, class C, 248B)

## Recipe + Risk
**Recipe:** nibble switch (w>>0x14)&0xf; mla id*0xc base; movs/movmi clamp to 0; signed b24->b25
**Risk:** struct-guessed/reshape-able: the else-branch addr is `data_0219b2e0->w34 + (u16w)*0xc` via mla; if mwcc binds the *0xc product to a base or reorders the +0x4 pointer it diverges. The movs r2,lsl#1/movmi #0 must stay if-assign not max. Confirm 0xc stride and w34 offsets.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02037fe4 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     nibble switch (w>>0x14)&0xf; mla id*0xc base; movs/movmi clamp to 0; signed b24->b25
 *   risk:       struct-guessed/reshape-able: the else-branch addr is `data_0219b2e0->w34 + (u16w)*0xc` via mla; if mwcc binds the *0xc product to a base or reorders the +0x4 pointer it diverges. The movs r2,lsl#1/movmi #0 must stay if-assign not max. Confirm 0xc stride and w34 offsets.
 *   confidence: low
 */
extern unsigned char data_0219b2e0[];
extern void *func_02037b58(int a);
extern void func_0203c5a4(int a, int b, int c);
extern void func_02087528(int a, int b);
extern void func_02087da4(void *p, int kind, int val);

typedef struct {
    char pad4[0x4];
    int w4;             /* +0x4 */
    char pad24[0x24 - 0x8];
    signed char b24;    /* +0x24 */
    signed char b25;    /* +0x25 */
    char pad34[0x34 - 0x26];
    unsigned int w34;   /* +0x34 */
    char pad68[0x68 - 0x38];
    unsigned short h68; /* +0x68 */
} S37f;

typedef struct {
    char pad7[0x7];
    unsigned char b7;   /* +0x7 */
} Tbl37;

int func_02037fe4(int a, int b, int c) {
    S37f *o = (S37f *)func_02037b58(a);
    unsigned int w;
    int sel;
    if (o == 0) return 0;
    o->b24 = (signed char)b;
    if (o->h68 & 0x6000) return 1;
    w = o->w34;
    sel = (w >> 0x14) & 0xf;
    if (sel == 4) {
        func_0203c5a4(o->w4, b, c);
    } else if (sel == 5) {
        func_02087528(o->w4, b);
    } else {
        unsigned int id = (unsigned short)w;
        Tbl37 *t = (Tbl37 *)((char *)((S37f *)data_0219b2e0)->w34 + id * 0xc);
        if (t->b7 & 0x40) {
            int v0 = (b - 0x40) << 1;
            if (v0 < 0) v0 = 0;
            func_02087da4((char *)o + 4, 1, v0);
            {
                int v1 = b << 1;
                if (v1 > 0x7f) v1 = 0x7f;
                func_02087da4((char *)o + 4, 2, v1 - 0x7f);
            }
        } else {
            func_02087da4((char *)o + 4, (int)(unsigned short)0xffff, b - 0x40);
        }
    }
    o->b25 = o->b24;
    return 1;
}
```

## Struct context
No GameSingleton or SystemWork fields. The function uses `data_0219b2e0` as a base object (same global used by many main functions) to derive a table pointer via `w34`.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x04 (S37f) | w4 | int | passed as first arg to sel==4/5 callee |
| +0x24 (S37f) | b24 | signed char | set from `b` arg; mirrored to b25 at end |
| +0x25 (S37f) | b25 | signed char | written from b24 at epilogue |
| +0x34 (S37f) | w34 | unsigned int | nibble selector (bits 20..23) + u16 id (bits 0..15) |
| +0x68 (S37f) | h68 | unsigned short | 0x6000 early-return guard |
| +0x34 (data_0219b2e0 as S37f) | w34 | unsigned int | table base pointer for 0xc-stride lookup |
| +0x07 (Tbl37) | b7 | unsigned char | 0x40 flag selects clamp path vs flat path |

## Closest matched example
**Path:** src/main/func_02038270.c
**Why similar:** Both dispatch on a field of a struct resolved via `func_02037b58`, use a (u16) cast to re-narrow a bitfield result, and have an early-return `h68 & 0x6000` guard identical to the one in `func_02038270`.
**Key lesson:** The `movs/movmi` clamp-to-zero (for `v0 = (b-0x40)<<1; if(v0<0) v0=0`) must come from a plain `if(v0 < 0) v0 = 0;` assignment, not a ternary or `max()`. mwcc emits `movs r2,r2,lsl#1` (which sets flags) followed by `movmi r2,#0` — this only happens when the shift and the conditional zero-assign share the same register and branch sequence. Also, the mla encoding for `id*0xc` relies on the computed base (`data_0219b2e0->w34`) being in a separate register from the id; do not fold them into a single expression.

## Try this
1. Verify the `0xc` stride: `id * 0xc` is an mla, not a shift. The table address must be `(char *)((S37f *)data_0219b2e0)->w34 + id * 0xc` exactly — if objdiff shows add+lsl instead of mla, the stride is wrong. Also confirm `(unsigned short)w` (not a full-word mask) for the id extraction.
2. If the v0 clamp path emits movcc instead of movmi, switch from `v0 = v0 < 0 ? 0 : v0` to `if (v0 < 0) v0 = 0;`. For v1 clamp to 0x7f, keep `if (v1 > 0x7f) v1 = 0x7f;` — both clamps must be if-assign form.
3. If struct offsets (b24/b25/w34/h68) produce wrong offsets in objdiff, adjust the pad arrays; the struct layout is inferred and the risk note calls it "struct-guessed". Escalate to permuter only if clamp shape and mla are confirmed correct but reg assignment still diverges.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219b2e0` | data symbol |
| `_LIT1` | `0x0000ffff` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02037b58` | main TU call |
| `bl` | `func_0203c5a4` | main TU call |
| `bl` | `func_02087528` | main TU call |
| `bl` | `func_02087da4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

