# 0205888c — func_0205888c (main, class C, 200B)

## Recipe + Risk
**Recipe:** r6=o, r5=e, r4=*o bound; reload e->f8 each store; free-then-null pairs in asm order; if-assign guards
**Risk:** Final guard: orig branches `f8!=0 -> .L_8ac(return1)` else into L_898 shared with the (fc==0||...) path returning 0 via func_0205d674; my &&-chain inverts to `f8==0` for the call. Branch polarity may flip movne/moveq. reshape-able (reorder guard) but block layout risk.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0205888c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     r6=o, r5=e, r4=*o bound; reload e->f8 each store; free-then-null pairs in asm order; if-assign guards
 *   risk:       Final guard: orig branches `f8!=0 -> .L_8ac(return1)` else into L_898 shared with the (fc==0||...) path returning 0 via func_0205d674; my &&-chain inverts to `f8==0` for the call. Branch polarity may flip movne/moveq. reshape-able (reorder guard) but block layout risk.
 *   confidence: med
 */
#include "nitro/types.h"

extern void func_020453b4(void *p);
extern void func_0205d674(void *o, void *e);

typedef struct Ent {
    u8   _pad0[0x8];
    s32  f8;             /* 0x8 -> sub */
    s32  fc;             /* 0xc */
    s32  f10;            /* 0x10 */
    s32  f14;            /* 0x14 */
    s32  f18;            /* 0x18 */
} Ent;

typedef struct Sub {
    u8   _pad0[0x8];
    s32  f8;             /* 0x8 */
    s32  fc;             /* 0xc */
} Sub;

typedef struct Base {
    u8   _pad0[0x104];
    s32  f104;           /* 0x104 */
} Base;

typedef struct Obj {
    Base *p;             /* 0x0 */
} Obj;

s32 func_0205888c(Obj *o, Ent *e)
{
    Base *b = o->p;

    if (e->f8 != 0 && b->f104 == 0) {
        func_020453b4((void *)((Sub *)e->f8)->f8);
        ((Sub *)e->f8)->f8 = 0;
        func_020453b4((void *)((Sub *)e->f8)->fc);
        ((Sub *)e->f8)->fc = 0;
        func_020453b4((void *)e->f8);
        e->f8 = 0;
    }
    func_020453b4((void *)e->f10);
    e->f10 = 0;
    func_020453b4((void *)e->f18);
    e->f18 = 0;
    e->f14 = 0;
    if (e->fc != 0 && b->f104 == 1 && e->f8 == 0) {
        func_0205d674(o, e);
        return 0;
    }
    return 1;
}
```

## Struct context
No SystemWork or GameSingleton access. Operates on private `Obj`/`Ent`/`Sub` structs local to this subsystem.

| offset | field | type | use |
|--------|-------|------|-----|
| Obj+0x0 | p | Base* | bound to r4 at entry; accessed for f104 guard |
| Base+0x104 | f104 | s32 | guard: ==0 enables sub-teardown block; ==1 with e->fc!=0 triggers func_0205d674 |
| Ent+0x8 | f8 | s32 | sub-object pointer (cast to Sub*); freed then zeroed; tested in final guard |
| Ent+0xc | fc | s32 | tested in final &&-chain for func_0205d674 dispatch |
| Ent+0x10 | f10 | s32 | freed unconditionally then zeroed |
| Ent+0x14 | f14 | s32 | zeroed unconditionally |
| Ent+0x18 | f18 | s32 | freed unconditionally then zeroed |
| Sub+0x8 | f8 | s32 | freed then zeroed (inner free) |
| Sub+0xc | fc | s32 | freed then zeroed (inner free) |

## Closest matched example
**Path:** src/main/func_0201d060.c
**Why similar:** guard-then-store pattern with conditional early return using `movgt`/`movle` shaped comparison.
**Key lesson:** The final condition `e->fc != 0 && b->f104 == 1 && e->f8 == 0` generates the branch polarity the recipe warns about. The orig skips to `return 1` when `e->f8 != 0` (i.e., the final guard is `e->f8 == 0` for the call path). Write it exactly as `if (e->fc != 0 && b->f104 == 1 && e->f8 == 0)` with the call inside — do not invert. Also: `e->f8` must be reloaded from memory before each use within the sub-teardown block (do not hoist to a register), because MWCC re-reads the pointer after the zero-store.

## Try this
1. Build as-is. In objdiff verify that the `((Sub*)e->f8)->f8` pattern inside the if-block reloads `e->f8` from memory each time (not from a cached register). If MWCC CSEs `e->f8` into a register across the zero-stores, add `volatile` to the `e->f8` field or split the reads with explicit pointer temps.
2. If the final `return 1` / `return 0` branch polarity is flipped (movne vs moveq), rewrite the tail as two separate ifs: first `if (e->f8 != 0) return 1;` then `if (e->fc != 0 && b->f104 == 1) { func_0205d674(o, e); return 0; } return 1;` to force the early-exit block ordering.
3. Park if the reload pattern of `e->f8` cannot be forced without `volatile` (permuter-class scheduling) — the risk says reshape-able but block-layout risk means a `.s` fallback is the safety net.
