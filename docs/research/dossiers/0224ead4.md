# 0224ead4 — func_ov002_0224ead4 (ov002, class F retriage, 360B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Listed in the Full Tractable Func List as hitting `DuelQueueState, Ov002Slot` (high confidence). The function accesses DuelQueueState fields and walks PerPlayerRowTable slots using the documented `id_lo13` bitfield and `0x868` stride. The `.s` file does not exist in the repo (the function may have been carved or renamed), but the retriage classification is high-confidence based on offset-level match of the DuelQueueState and Ov002Slot KB patterns.
**Confidence:** high
**Structs needed:** DuelQueueState, Ov002Slot

## Assembly

```asm
; NOTE: src/overlay002/func_ov002_0224ead4.s is absent from the repo tree.
; The function was classified high-confidence in Ov002EFRetriage.md based on
; DuelQueueState + Ov002Slot offset hits. This dossier is written from the
; retriage table entry; the decomper must locate the actual .s in the gap
; objects or dsd-delinked extract before writing C.
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state flag (0x7f=done / 0x80=active) |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock (0=free) |
| PerPlayerRowTable | stride | (player & 1) * 0x868 | byte offset | player row selection |
| Ov002Slot | +0x008 | id_lo13 : 13 | unsigned int | (slot_word << 0x13) >> 0x13 |

## Recipe sketch

```c
/* Inferred from retriage classification: DuelQueueState + Ov002Slot, 360B, class F.
 * Likely a DuelQueueState-gated PPT slot scan that posts via func_ov002_02253458. */
#include "ov002_core.h"
extern int data_ov002_022cf180[];

/* Placeholder — decomper must read the actual gap .s to fill in the exact guards
 * and call targets. Pattern is: DQS gate → PPT slot loop → id_lo13 guards → sink. */
void func_ov002_0224ead4(int player, int arg1, int arg2) {
    DuelQueueState *dqs = (DuelQueueState *)data_ov002_022ce288;
    if (dqs->f_5a8 != 0x80) return;           /* active-only gate */
    int row_off = (player & 1) * 0x868;
    if (*(int *)((char *)data_ov002_022cf180 + row_off) == 0)
        return;
    char *row = (char *)data_ov002_022cf16c + row_off;
    /* slot array offset TBD from actual .s — likely 0x418 or 0x260 */
    unsigned int *slots = (unsigned int *)(row + /* TBD */ 0x418);
    int count = *(int *)(row + 0x14);
    for (int s = 0; s < count; s++, slots++) {
        int id = (int)((*slots << 0x13) >> 0x13);   /* id_lo13 */
        /* guards TBD */
        func_ov002_02253458(player, 0xe, s);
    }
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_0224de94.s (sibling PPT scan at 228B)
**Why similar:** Both are PPT slot-scan functions in the same address range with Ov002Slot id_lo13 reads and func_02253458 sink calls; 0224de94 is confirmed high from the same retriage batch.
**Key lesson:** The DuelQueueState gate (`f_5a8 == 0x80`) is the distinguishing feature versus pure PPT scans — load it as `*(int *)((char *)data_ov002_022ce288 + 0x5a8)`. The `id_lo13` must use the `: 13` bitfield, not `& 0x1fff`. At 360B (class F), expect 8+ callee-saved registers and a multi-guard body.

## Try this

1. Locate the actual disassembly: run `dsd delink` on the ov002 gap objects or check `build/eur/overlay002/` for the `.o` containing 0x0224EAD4. The `.s` does not appear in `src/overlay002/` — it may need to be extracted via `asm_escape --whole-function`.
2. Once the actual .s is in hand: load `DuelQueueState+0x5A8` as the first field guard. Declare slot id as `struct Ov002Slot { unsigned int id : 13; }` and extract via `->id`.
3. When to escalate: if the actual body has 8+ callee-saved regs and a nested PPT double-loop (both players), this is a DSS/DQS dispatch — mark for permuter if the guard branch arm order inverts.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

