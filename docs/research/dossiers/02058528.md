# 02058528 — func_02058528 (main, class D, 172B)

## Recipe + Risk
**Recipe:** store-order f0,f4,f8,f10(cond),fc,f14; homed args a1=sp+0x14,a2,a4=sp+0x20,a5=sp+0x24
**Risk:** incoming a1/a2/a4/a5 read from homed stack +0x14/+0x18/+0x20/+0x24 (a3 stays r3); f10 store before fc must hold. struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02058528 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store-order f0,f4,f8,f10(cond),fc,f14; homed args a1=sp+0x14,a2,a4=sp+0x20,a5=sp+0x24
 *   risk:       incoming a1/a2/a4/a5 read from homed stack +0x14/+0x18/+0x20/+0x24 (a3 stays r3); f10 store before fc must hold. struct-guessed.
 *   confidence: med
 */
/* func_02058528 (D): alloc 0x18 node, fill fields, append to list. */
extern void *func_020453e8(int sz);
extern void  func_0205ffc0(int ctx, void *lit);
extern unsigned char data_021002a4[];
typedef struct Node { int f0; int f4; int f8; int fc; void *f10; int f14; } Node;
typedef struct Host { int f0; char _4[0x434]; Node *head; Node *tail; } Host;
int func_02058528(Host *h, int a1, int a2, int a3, void *a4, int a5) {
    Host *root = (Host *)*(void **)h;
    Node *n = (Node *)func_020453e8(0x18);
    if (n == 0) {
        func_0205ffc0((int)h, data_021002a4);
        return 1;
    }
    n->f0 = a1;
    n->f4 = a2;
    n->f8 = a3;
    if (a4 != 0)
        n->f10 = *(void **)((char *)a4 + 0x18);
    else
        n->f10 = 0;
    n->fc = a5;
    n->f14 = 0;
    if (root->head == 0)
        root->head = n;
    if (root->tail != 0)
        root->tail->f14 = (int)n;
    root->tail = n;
    return 0;
}
```

## Struct context
Function operates on a `Host` struct (pointer-indirected: `root = *(Host**)h`) and allocates a `Node` of size 0x18. Neither touches SystemWork or GameSingleton.

| offset | field | type | use |
|--------|-------|------|-----|
| Host+0x438 | head | Node* | list head pointer; written if null |
| Host+0x43c | tail | Node* | list tail pointer; tail->f14 and root->tail updated |
| Node+0x0 | f0 | int | receives a1 |
| Node+0x4 | f4 | int | receives a2 |
| Node+0x8 | f8 | int | receives a3 (stays in r3) |
| Node+0xc | fc | int | receives a5; stored AFTER f10 |
| Node+0x10 | f10 | void* | receives *(a4+0x18) if a4!=0, else 0; stored BEFORE fc |
| Node+0x14 | f14 | int | 0 always (next pointer / end-marker) |

**Host struct is guessed**: `head` at Host+0x438 and `tail` at Host+0x43c are inferred. The `_4[0x434]` pad implies head is at `0x438`. If objdiff shows bad ldr/str offsets, adjust pad size.

## Closest matched example
**Path:** src/main/func_0200fd84.c
**Why similar:** singly-linked list walk/append with conditional null-guard and direct struct field stores.
**Key lesson:** The store order of `f10` before `fc` is load-bearing — mwcc schedules conditional stores (the `a4!=0` branch) before the unconditional `n->fc` store. Swapping them diverges. Also: args a1/a2 are homed to the stack because the function has six parameters — the first four go to r0–r3 and a4/a5 are passed on the stack at sp+0x14 up. The prep draft reads them directly from the homed-stack positions.

## Try this
1. Build the prep draft as-is. Verify `n->f10 = ... / n->f10 = 0` stores at offset 0x10 and that the `str n->fc` at 0xc follows immediately after. If objdiff shows `n->fc` before `n->f10`, swap lines 28–29 so the if-assign for `f10` precedes the `n->fc = a5` line.
2. If `f10` and `fc` are correct but the tail-append block diverges (wrong register for `root->tail->f14`), inspect which register holds `n` at the append — it must be the same r that held the alloc result; do not rebind to a fresh local.
3. Park as permuter-class only if the homed-arg spill/reload pattern (`ldmia sp+0x14` block) cannot be reproduced; in that case ship as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021002a4` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020453e8` | main TU call |
| `bl` | `func_0205ffc0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

