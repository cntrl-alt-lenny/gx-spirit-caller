# 0201a84c — func_0201a84c (main, class C, 212B)

## Recipe + Risk

**Recipe:** GetSystemWork bound r5; acc r4=0 early; bit-loops orr<<(i+k) inline shift; bit10=(v<<0x15)>>31
**Risk:** reshape-able: the else-branch order is inverted in asm (bit-set/acc|=0x800 sits in the bne=true block as the fall-through `.L_2fc`); if the if/else bodies emit in source order the branch polarity flips — swap the two arms to follow the asm.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0201a84c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     GetSystemWork bound r5; acc r4=0 early; bit-loops orr<<(i+k) inline shift; bit10=(v<<0x15)>>31
 *   risk:       reshape-able: the else-branch order is inverted in asm (bit-set/acc|=0x800 sits in the bne=true block as the fall-through `.L_2fc`); if the if/else bodies emit in source order the branch polarity flips — swap the two arms to follow the asm.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0201a84c (main, class C) — UNVERIFIED build-free draft.
 * switch(mode r0) with two small bit-accumulate loops. sys=GetSystemWork() bound (r5), acc r4=0.
 *   case 0: if (!func_0201a824()) acc |= 0x400;
 *           else { v=*(int*)(sys+0x8e8);
 *                  if (((v<<0x15)>>0x1f)==0) { *(int*)(sys+0x8e8)=v|0x400; acc|=0x800; }
 *                  else { if(func_0200a704()) acc|=0x1000; if(func_0200a928()) acc|=0x2000; }
 *                  if (acc==0) acc|=0x4000; }
 *   case 1: if (func_0201a824()) {
 *               for(i=0;i<3;i++) acc |= func_0200a68c(i) << (i+0xf);
 *               for(i=0;i<3;i++) acc |= func_0200a8b0(i) << (i+0x12);
 *               acc |= 0x200000; }
 *   return acc;
 * NOTE bit10 of +0x8e8 is (v<<0x15)>>0x1f. Loop shift uses i+const so DON'T bind i*S; inline (i+0xf). */
extern char *GetSystemWork(void);
extern int func_0201a824(void);
extern int func_0200a704(void);
extern int func_0200a928(void);
extern int func_0200a68c(int);
extern int func_0200a8b0(int);

int func_0201a84c(int mode) {
    char *r5 = GetSystemWork();
    int acc = 0;
    int i;
    if (mode == 0) {
        if (func_0201a824() == 0) {
            acc |= 0x400;
        } else {
            int v = *(int *)(r5 + 0x8e8);
            if ((unsigned int)((unsigned int)v << 0x15) >> 0x1f) {
                if (func_0200a704() != 0)
                    acc |= 0x1000;
                if (func_0200a928() != 0)
                    acc |= 0x2000;
            } else {
                *(int *)(r5 + 0x8e8) = v | 0x400;
                acc |= 0x800;
            }
            if (acc == 0)
                acc |= 0x4000;
        }
    } else if (mode == 1) {
        if (func_0201a824() != 0) {
            for (i = 0; i < 3; i++)
                acc |= func_0200a68c(i) << (i + 0xf);
            for (i = 0; i < 3; i++)
                acc |= func_0200a8b0(i) << (i + 0x12);
            acc |= 0x200000;
        }
    }
    return acc;
}
```

## Struct context

This function touches SystemWork via `GetSystemWork()` at offset `+0x8e8`.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| sys+0x8e8 | bitfield word | s32 | Read as `v`; bit 10 tested via `(v<<0x15)>>0x1f`; RMW `v|0x400` written back when bit10==0 |

The `+0x8e8` field per `struct-context-main.md` is: 8-bit field at bits 11..18, cleared/set by `func_02018a64`/`func_02018a38`. Here the access is a separate bitfield — bit 10 tested as `(v<<0x15)>>0x1f`, which is `(v >> 10) & 1`.

## Closest matched example

**Path:** src/main/func_0201a4dc.c
**Why similar:** Same GetSystemWork-bound r4/r5 pattern; same bit-extract with `(v<<N)>>31` to test a single bit from a syswork field; same guard-chain + multiple return paths.
**Key lesson:** The `(v << 7) >> 31` form for extracting bit 24 is the canonical mwcc pattern — keep the exact unsigned shift pair. In `func_0201a84c` the bit-10 extract is `(v << 0x15) >> 0x1f` (shift left by 21, then logical right 31). Use exactly this shift pair — do NOT rewrite as `(v >> 10) & 1` which emits `lsr;and` instead of `lsl;lsr`.

## Try this

1. Build the prep draft as-is. If the case-0 else-branch body is wrong, swap the inner `if/else` arms: put `{v|0x400 store; acc|=0x800;}` in the `if` body (true branch) and the `func_0200a704/func_0200a928` calls in the `else` body. The risk note says the asm fall-through is the bit-set path, meaning the `bne` (bit10 non-zero) goes to the other arm — so the polarity in C should be `if ((v<<0x15)>>0x1f == 0)` pointing to the store-and-0x800 arm.
2. If loops emit wrong: verify the shift expression is `(i + 0xf)` inline (not a bound `k = i + 0xf`); mwcc should keep the add in the shift operand. If reg-alloc on `acc` is wrong across loops, try binding `acc` explicitly as a named variable initialized to 0 before the if/else.
3. If inner branch ordering is still mismatched after swapping arms, escalate to permuter — the scheduling around `acc==0 -> |=0x4000` tail may be permuter-class.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201a824` | main TU call |
| `bl` | `func_0200a704` | main TU call |
| `bl` | `func_0200a928` | main TU call |
| `bl` | `func_0200a68c` | main TU call |
| `bl` | `func_0200a8b0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

