# 0200c284 — func_0200c284 (main, class D, 116B)

## Recipe + Risk

**Recipe:** comparator clone; idx*0xc -> mla index, field +8; fixed-order flag stores in tail
**Risk:** orig uses a {r3,lr} stack frame with ldm{eq,hi}ia conditional returns (unlike c078's bx lr); whether mwcc picks frame-return vs bx-lr is a routing/leaf coin-flip. base +0x1c / stride 0xc struct-guessed. permuter-class/struct-guessed.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0200c284 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     comparator clone; idx*0xc -> mla index, field +8; fixed-order flag stores in tail
 *   risk:       orig uses a {r3,lr} stack frame with ldm{eq,hi}ia conditional returns (unlike c078's bx lr); whether mwcc picks frame-return vs bx-lr is a routing/leaf coin-flip. base +0x1c / stride 0xc struct-guessed. permuter-class/struct-guessed.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0200c284 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED.
 *   recipe:     same comparator as c078 but stride 0xc (mla) field +8; offsets 0x264/0x268, bit 0x8000000
 *   risk:       see risk note
 */
/* func_0200c284: compare element field against val by mode; on no-match-condition
   record val/idx, set a status bit, raise a global flag; return 1. */

extern unsigned char data_021040ac[];

int func_0200c284(void *a, int idx, unsigned int val, int mode)
{
    unsigned int v = *(unsigned int *)(*(char **)((char *)a + 0x1c) + idx * 0xc + 0x8);
    if (v == val) return 1;
    if (mode == 1) {
        if (v < val) return 1;
    } else if (mode == 2) {
        if (v > val) return 1;
    }
    *(unsigned int *)((char *)a + 0x264) = val;
    *(int *)((char *)a + 0x268) = idx;
    *(unsigned int *)((char *)a + 0x7c) |= 0x8000000;
    *(int *)(data_021040ac + 0x20) = 1;
    return 1;
}
```

## Struct context

This function accesses `data_021040ac` (GlobalAudioState, ≥0xC4C B — see [GlobalAudioState.md](../types/GlobalAudioState.md)). All other fields are passed through the `void *a` argument.

**Near-clone of 0200c078** — the differences are: element-ptr base offset `0x1c` (vs `0x18`), stride `0xc` using `mla` (vs `0x20` lsl#5), field offset `+0x8` (vs `+0x18`), tail stores at `0x264`/`0x268` (vs `0x25c`/`0x260`), and bit mask `0x8000000` (vs `0x4000000`).

| offset | field | type | use |
|--------|-------|------|-----|
| a+0x1c | element-list ptr | char** | dereferenced to get array base; struct-guessed |
| element base + idx*0xc + 0x8 | v | unsigned int | compared against `val`; stride 0xc via mla |
| a+0x264 | val-store | unsigned int | written in the tail |
| a+0x268 | idx-store | int | written in the tail |
| a+0x7c | flag word | unsigned int | ORed with 0x8000000 in the tail |
| data_021040ac+0x20 | global flag | int | written 1 in the tail |

## Closest matched example

**Path:** src/main/func_0200c558.c
**Why similar:** Same pattern — guarded early return, then a tail that writes several fields in fixed order and returns 1.
**Key lesson:** Unlike c078 (which is frameless), this function uses a `{r3,lr}` stack frame with `ldm{eq,hi}ia` conditional returns (the `mla` instruction for `idx * 0xc` forces a non-leaf body, which triggers the frame). MWCC needs the `mla` to be visibly non-trivial (i.e. `idx * 0xc` not strength-reduced) to keep the frame. The expression `idx * 0xc` is the right C spelling; do NOT rewrite as `(idx << 3) + (idx << 2)` or MWCC will optimize differently. Confirm in objdiff that the prologue is `stmdb sp!, {r3, lr}` and the conditional returns are `ldmeqia sp!, {r3, pc}` / `ldmhsia sp!, {r3, pc}`.

## Try this

1. Build as-is. Confirm in objdiff: prologue is `stmdb sp!, {r3, lr}` (unlike c078 which is frameless). If the prologue is absent (MWCC chose a frameless leaf), the `mla` did not force a non-leaf body — try adding a local variable: `int prod = idx * 0xc; unsigned int v = *(unsigned int *)(*(char **)((char *)a + 0x1c) + prod + 0x8);`.
2. If the conditional-return predicates are wrong (e.g. `bxeq lr` instead of `ldmeqia sp!, {r3, pc}`), the function is being treated as a leaf despite the frame — try removing the `{r3,lr}` frame by converting the function back to frameless form (no local variables at all).
3. When to park: this is **permuter-class/struct-guessed**. If the frame vs. frameless routing cannot be forced after one or two reshape attempts, accept as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021040ac` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

