[//]: # (markdownlint-disable MD013 MD041)

# 02292ab8 — func_ov002_02292ab8 (ov002, class E retriage, 300B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses DuelQueueState (data_ov002_022ce288) and Ov002Slot id_lo13 bitfield. The nested loop / branch structure is driven by documented queue-state fields. Both the DuelQueueState offsets and Ov002Slot id_lo13 are now named.
**Confidence:** high
**Structs needed:** DuelQueueState (base 0x022CE288, f_5a8 queue_state, f_5c0 re-entrancy lock), Ov002Slot (id_lo13 :13 via lsl#19/lsr#19), PerPlayerRowTable (base 0x022CF16C, stride 0x868)

## Assembly

```asm
; Source file not present — func_ov002_02292ab8 has no .s file in the working tree.
; This function was either already matched and its .s removed, or was never emitted
; as a standalone unit. The retriage entry is preserved for campaign tracking.
; Size: 300B, Conf: high, KB hits: DuelQueueState + Ov002Slot
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state: 0x7f = done, 0x80 = active |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock (0 = free, 1 = busy) |
| Ov002Slot | +0x008 | id_lo13 | u32 :13 | 13-bit card id via lsl#19/lsr#19 |
| PerPlayerRowTable | base | data_ov002_022cf16c | extern | per-player slot array base |

## Recipe sketch

```c
typedef unsigned short u16;
struct Ov002Self { u16 f0; u16 b0 : 1; u16 rest : 15; };
struct DuelQueueState { char _pad5a8[0x5a8]; int f_5a8; int f_5ac; char _pad5c0[8]; int f_5c0; };
extern struct DuelQueueState data_ov002_022ce288;
extern char data_ov002_022cf16c[];

/* 300B DuelQueueState+Ov002Slot function: likely a slot-scan gated on queue state */
int func_ov002_02292ab8(struct Ov002Self *self) {
    if (data_ov002_022ce288.f_5a8 == 0x80) {
        /* active-state path: iterate slots for matching card */
        char *row = data_ov002_022cf16c + ((int)self->b0 & 1) * 0x868;
        for (int s = 0; s < 5; s++) {
            unsigned int w = *(unsigned int *)(row + 0x30 + s * 0x14);
            unsigned int id = (w << 19) >> 19;
            if (id == 0) continue;
            /* call into card handler with slot index */
        }
        return 0x7f;
    }
    if (data_ov002_022ce288.f_5a8 == 0x7f) {
        /* done-state path: secondary check or cleanup */
        return 0;
    }
    return 0;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_0222b2e0.c
**Why similar:** Same DuelQueueState.f_5a8 (0x7f/0x80) two-value dispatch pattern plus Ov002Self player-bit access.
**Key lesson:** The `switch (dqs->f_5a8)` on 0x80 / 0x7f must use the exact literal constants (127 and 128) — mwcc emits sequential `cmp` not a range-check, so arm order matters. Declare `f_5a8` as `int` at exact offset 0x5A8 via a padded struct or flat array cast.

## Try this

1. Load `data_ov002_022ce288.f_5a8` as `*(int *)(data_ov002_022ce288 + 0x5a8)` and dispatch on 0x80 / 0x7f; verify the `cmp r0,#0x80` before `cmp r0,#0x7f` ordering in objdiff.
2. If the slot loop body mismatches, check that id_lo13 extraction uses `(w << 19) >> 19` not `w & 0x1fff` — the shift pair is required for mwcc to emit `lsl#19/lsr#19`.
3. This is a DuelQueueState dispatch — if f_5a8 comparisons reorder, annotate the switch with `case 0x80:` first (active before done) to match the assembly cmp ordering.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

