# 02023188 — func_02023188 (main, class C, 140B)

## Recipe + Risk

**Recipe:** CLASS D/C: short idx so idx*0x3c => smlabb; aliased bases bound once; strh-then-reload f74
**Risk:** smlabb needs idx kept `short` and fused as mla; orig also RELOADS f74 after the >=0x90 store for the loop test. mwcc may use mul+add, drop the reload, or rotate the 3 aliased base regs (r4/r6/r7). struct-guessed offsets + permuter-class fusion

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02023188 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS D/C: short idx so idx*0x3c => smlabb; aliased bases bound once; strh-then-reload f74
 *   risk:       smlabb needs idx kept `short` and fused as mla; orig also RELOADS f74 after the >=0x90 store for the loop test. mwcc may use mul+add, drop the reload, or rotate the 3 aliased base regs (r4/r6/r7). struct-guessed offsets + permuter-class fusion
 *   confidence: low
 */
/* func_02023188: while head(0x3476)!=tail(0x3474) walk slots (stride 0x3c,
 * smlabb idx*0x3c+base@0x12b4), teardown f38, bump+wrap a 0x3474 counter. */

extern char data_02197434[];
extern char data_0219a834[];
extern void func_02006e1c(int x);

int func_02023188(void) {
    short idx = *(short *)(data_0219a834 + 0x76);
    if (idx == *(short *)(data_0219a834 + 0x74)) return 1;
    {
        char *cnt  = data_02197434 + 0x74 + 0x3400;   /* 0x3474 */
        char *slotbase = data_02197434 + 0x2b4 + 0x1000; /* 0x12b4 */
        char *blk  = data_02197434 + 0x3400;
        short head;
        do {
            char *sl = slotbase + idx * 0x3c;
            if (*(int *)(sl + 0x38) != 0) {
                func_02006e1c(*(int *)(sl + 0x38));
                *(int *)(sl + 0x38) = 0;
            }
            *(short *)cnt = (short)(*(short *)cnt + 1);
            head = *(short *)(blk + 0x76);
            if (*(short *)(blk + 0x74) >= 0x90) {
                *(short *)(blk + 0x74) = 0;
            }
        } while (head != *(short *)(blk + 0x74));
    }
    return 1;
}
```

## Struct context

Touches two globals: `data_0219a834` (head/tail read before the loop) and `data_02197434` at two offsets (slot array at +0x12b4 stride 0x3c, and a count/wrap block at +0x3400). No GameSingleton or SystemWork fields are involved.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| `data_0219a834 + 0x76` | idx / head (init) | `short` | initial `idx` and loop head comparison |
| `data_0219a834 + 0x74` | tail (init) | `short` | early-exit guard |
| `data_02197434 + 0x3474` (`cnt`) | slot counter | `short` | incremented each iteration |
| `data_02197434 + 0x3476` (`blk + 0x76`) | loop head | `short` | re-loaded after cnt bump for loop test |
| `data_02197434 + 0x3474` (`blk + 0x74`) | tail | `short` | wrapped to 0 if `>= 0x90`; loop exit test |
| `slotbase + idx*0x3c + 0x38` | slot f38 | `int` | non-zero → call `func_02006e1c` then clear |

## Closest matched example

**Path:** src/main/func_02023214.c
**Why similar:** Struct-copy / fixed-stride block operations on `data_02197434`; same 0x3400 region offsets; matching do-while with counter-bump and wrap.
**Key lesson:** The `idx` variable must be declared `short` (not `int`) so the compiler uses `smlabb` (signed halfword multiply-accumulate bottom×bottom) for the `idx * 0x3c` address computation. A plain `int` idx will generate a full `mul` instead. The reload of `blk+0x74` after the wrap store (for the loop `while` test) must be an explicit re-read — do not CSE it with the write.

## Try this

1. Ensure `idx` is `short` throughout; the `char *sl = slotbase + idx * 0x3c` must keep `idx` as a halfword so mwcc fuses into `smlabb`. If a `mul` appears instead, try `(short)(idx) * 0x3c` coercion.
2. If the three aliased base-pointer registers `cnt`/`slotbase`/`blk` get assigned in a different callee-saved order than orig's `r4/r6/r7`, try reordering their declarations (first declared = first callee-saved). The reload of `blk+0x74` after the store must not be optimized away — add a `volatile` cast if mwcc CSEs it.
3. If the `smlabb` fusion is still absent after `short` coercion, park as permuter-class; the halfword-multiply fusion is not forceable on Windows without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219a834` | data symbol |
| `_LIT1` | `data_02197434` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Task_Invoke` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

