# 021ab43c — func_ov020_021ab43c (ov020, class D, 100B)

## Recipe + Risk
**Recipe:** identical clone recipe to 021ab364, palette nibble 0xe000; mla index; inner counter from shared j
**Risk:** reshape-able: same as 021ab364 — `int j=0; int k=j;` reproduces the orig's `mov r1,ip; mov r4,r1` inner-init; only divergence risk is that inner-start mov if collapsed to `int k=0`. Pointer math and row gap exact; high confidence given the clone.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021ab43c (ov020, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     identical clone recipe to 021ab364, palette nibble 0xe000; mla index; inner counter from shared j
 *   risk:       reshape-able: same as 021ab364 — `int j=0; int k=j;` reproduces the orig's `mov r1,ip; mov r4,r1` inner-init; only divergence risk is that inner-start mov if collapsed to `int k=0`. Pointer math and row gap exact; high confidence given the clone.
 *   confidence: med
 */
/* func_ov020_021ab43c: clone of 021ab364 differing only in the palette nibble
 * (0xe000 vs 0xf000). 4 rows x 25 cols, tile index = row*100+1 then ++. */
extern unsigned short *func_0208e1cc(void);

void func_ov020_021ab43c(int unused, int row) {
    unsigned short *p = func_0208e1cc();
    int idx = row * 100 + 1;
    int i;
    int j = 0;
    p += ((row * 5 + 4) * 32 + 5);
    for (i = 0; i < 4; i++) {
        int k = j;
        for (; k < 25; k++) {
            *p++ = idx | 0xe000;
            idx++;
        }
        p += 7;
    }
}
```

## Struct context
Identical to 021ab364. No named struct. `func_0208e1cc()` returns the BG tilemap base. Pointer math `(row * 5 + 4) * 32 + 5` is shared with 021ab364. Only difference: palette nibble `0xe000` instead of `0xf000`.

| field | offset | type | purpose |
|-------|--------|------|---------|
| BG tilemap | returned by func_0208e1cc | unsigned short* | VRAM tilemap base |
| tile index | idx = row*100+1 | int | MLA-computed tile index, ORed with palette nibble 0xe000 |

## Closest matched example
**Path:** src/overlay020/func_ov020_021ab3c8.c (relative to C:/Users/leona/Dev/gx-spirit-caller/kb-map)
**Why similar:** Same ov020 tilemap-fill family (4-row loop, func_0208e1cc base, same pointer formula). This candidate is a near-clone of 021ab364 in this batch — crack 021ab364 first, then swap `0xf000` to `0xe000`.

**Key lesson from that file:** Near-clone of 021ab364 — apply the same `int j = 0; int k = j;` reshape. The only byte difference between 021ab364 and 021ab43c should be the immediate constant `0xf000` vs `0xe000` in the `orr` instruction. Match 021ab364 first, then derive this one by symbol rename + constant change.

**WALL WARNING:** Same `§WALL` classification as 021ab364 in ov020_core.h — permuter-class inline tilemap-fill nested loop. The register-rotation issue is identical.

## Try this
1. Match 021ab364 first (see its dossier). Once matched, copy the `.c`, rename the symbol to `func_ov020_021ab43c`, and change `0xf000` to `0xe000`. Build immediately — no other reshape expected.
2. If 021ab364 is not yet matched: build this draft as-is with the same reshape logic (`int j = 0; int k = j;`). The objdiff mismatch pattern will be identical to 021ab364.
3. Escalate to Mac permuter together with 021ab364. Same root cause, same fix applies to both.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208e1cc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

