# 02045954 — func_02045954 (main, class C, 96B)

## Recipe + Risk
**Recipe:** callee-saved decl r6/r5/r4=r0/r1/r3; store-order h8,ha,h4,h6 then store global
**Risk:** store-order [p+8],[p+a],[p+4],[p+6] with shared imm #1 in r0 reused for ha and h4 -- if mwcc rematerializes the 1 or reorders the h6=0 store the movs diverge; reshape-able. struct-guessed.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02045954 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     callee-saved decl r6/r5/r4=r0/r1/r3; store-order h8,ha,h4,h6 then store global
 *   risk:       store-order [p+8],[p+a],[p+4],[p+6] with shared imm #1 in r0 reused for ha and h4 -- if mwcc rematerializes the 1 or reorders the h6=0 store the movs diverge; reshape-able. struct-guessed.
 *   confidence: med
 */
typedef unsigned char u8;
typedef unsigned short u16;
typedef unsigned int u32;

extern u8 *data_0219dad0;
extern void func_020458d8(void);
extern void func_02077b4c(u32 a);
extern void func_020945f4(void *dst, int val, int n);

void func_02045954(u8 *p, u16 a1, u32 a2, u32 a3) {
    if (data_0219dad0 != 0) {
        return;
    }
    func_020945f4(p, 0, 0xc);
    *(u16 *)(p + 0x8) = a1;
    *(u16 *)(p + 0xa) = 1;
    *(u16 *)(p + 0x4) = 1;
    *(u16 *)(p + 0x6) = 0;
    data_0219dad0 = p;
    func_020458d8();
    func_02077b4c(a3);
}
```

## Struct context
This function initialises a struct at pointer `p` and stores it in
`data_0219dad0`. The sibling `func_02045828` reads this same global and
uses the `p+0x4` halfword field. Does not touch GameSingleton or SystemWork.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| p+0x08 | h8 | u16 | set to a1 (caller's first arg after p) |
| p+0x0a | ha | u16 | set to 1 (shared imm in r0) |
| p+0x04 | h4 | u16 | set to 1 (same imm reused) |
| p+0x06 | h6 | u16 | set to 0 |
| data_0219dad0 | (global) | u8* | set to p after all field stores |

## Closest matched example
**Path:** src/main/func_02045570.c
**Why similar:** both guard on `data_0219dad0 != 0` as an early-return sentinel and operate on the same global pointer.
**Key lesson:** The asm stores halfwords in the order `[p+8],[p+a],[p+4],[p+6]` — not the natural layout order. MWCC reuses the `#1` immediate: r0 holds 1 after `*(u16*)(p+0xa) = 1` and is then used directly for `*(u16*)(p+0x4) = 1` without rematerialization. Writing the two `= 1` stores consecutively in source (`p+0xa` then `p+0x4`) before the `= 0` store forces this reuse. The `a2` parameter appears to be unused (not stored to p) — keep it in the signature to preserve the register frame but do not reference it in the body.

## Try this
1. Write the four stores in exactly this order: `p+0x8 = a1; p+0xa = 1; p+0x4 = 1; p+0x6 = 0;` — the `= 1` pair must be adjacent so MWCC keeps the immediate in a register between them.
2. In objdiff, if `p+0x6 = 0` appears before the `= 1` pair (mwcc moved the zero store up), add `(void)a2;` between the `func_020945f4` call and the first store to prevent the compiler from moving the `h6=0` write into the cleared region.
3. If the callee-saved assignment `r6/r5/r4 = r0/r1/r3` (skipping a2 in r2) is wrong, check that the parameter is declared as `u32 a2` in position 3 (r2) even though it is unused — some MWCC versions still allocate a callee-saved slot for it. Escalate to permuter if store order is confirmed but the immediate reuse pattern still diverges.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dad0` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020945f4` | main TU call |
| `bl` | `func_020458d8` | main TU call |
| `bl` | `func_02077b4c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

