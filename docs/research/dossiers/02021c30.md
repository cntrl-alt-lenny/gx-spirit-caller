# 02021c30 — func_02021c30 (main, class C, 140B)

## Recipe + Risk

**Recipe:** CLASS C: hoist count; bind b6=base+0x1048; b7=b6+i*0x68; store a0 via [b6,off] but +4 via b7
**Risk:** orig stores a0 via indexed [r6,r5] yet stores +4 via precomputed r7=r6+off; if mwcc folds both onto one base (or recomputes), the str pair diverges. reshape-able (split the two base exprs as written) else permuter-class

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02021c30 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS C: hoist count; bind b6=base+0x1048; b7=b6+i*0x68; store a0 via [b6,off] but +4 via b7
 *   risk:       orig stores a0 via indexed [r6,r5] yet stores +4 via precomputed r7=r6+off; if mwcc folds both onto one base (or recomputes), the str pair diverges. reshape-able (split the two base exprs as written) else permuter-class
 *   confidence: med
 */
/* func_02021c30: scan up to data_02198434->fb0 entries (stride 0x68) for
 * a free f48, Fill32 it, store a0 at +0 and 0 at +4, return the slot. */

extern void Fill32(int value, void *dest, int size);
extern char data_02197434[];
extern char data_02198434[];

void *func_02021c30(int a0) {
    int count = *(int *)(data_02198434 + 0xb0);
    char *base = data_02197434;
    int i = 0;
    if (count > 0) {
        char *cur = base;
        do {
            if (*(int *)(cur + 0x1000 + 0x48) == 0) break;
            cur += 0x68;
        } while (++i < count);
    }
    if (i >= count) return 0;
    {
        char *b6 = base + 0x48 + 0x1000;
        char *b7 = b6 + i * 0x68;
        Fill32(0, b7, 0x68);
        *(int *)(b6 + i * 0x68) = a0;
        *(int *)(b7 + 0x4) = 0;
        return b7;
    }
}
```

## Struct context

Accesses `data_02197434` (same Slot16 array global as func_02021b38 in this batch) and `data_02198434` (SlotB). Not GameSingleton/SystemWork.

| global / offset | field | type | use in this function |
|----------------|-------|------|---------------------|
| data_02198434+0xb0 | count | int | hoisted before scan loop; upper bound |
| data_02197434+0x1048+i*0x68 | slot free-check | int | scan: == 0 means free slot |
| b7 = data_02197434+0x1048+i*0x68 | slot base | char* | Fill32 target; return value |
| b6 + i*0x68 | slot+0x00 | int | written = a0 (two-expr form: b6+i*stride) |
| b7 + 0x04 | slot+0x04 | int | written = 0 (via b7 direct pointer) |

## Closest matched example

**Path:** src/main/func_0201b60c.c
**Why similar:** both require a specific pattern where two adjacent stores use DIFFERENT base expressions for the same address — one uses indexed [base + offset], the other uses a precomputed pointer — and mwcc must be prevented from collapsing them.
**Key lesson:** The critical asymmetry is `*(int *)(b6 + i * 0x68) = a0` (indexed from b6) vs `*(int *)(b7 + 0x4) = 0` (offset from b7). Both b6 and b7 must be declared as SEPARATE `char*` locals in the same inner block — do not alias them. If mwcc folds both onto b7, try: change `*(int *)(b6 + i * 0x68) = a0` to `*(int *)(b7 + 0) = a0` — but if the orig truly uses [r6,r5], the indexed form must be kept. The offset 0x1048 = 0x1000 + 0x48 should be split as `base + 0x48 + 0x1000` (two-constant form) to match two adds in the orig.

## Try this

1. Ensure `b6 = base + 0x48 + 0x1000` and `b7 = b6 + i * 0x68` are both declared as separate `char*` locals in the inner block — if objdiff shows only one base reg, add `volatile char *b6_v = b6;` to prevent CSE.
2. If the `*(int *)(b6 + i * 0x68) = a0` store emits using b7's register instead of b6's (losing the indexed form), try rewriting as `*(int *)b7 = a0` to see if objdiff accepts it — then check if the base-register binding changes.
3. Park as permuter-class if the two-base-expr pair cannot be separated after reshape; the [r6,r5] vs [r7,#4] split is un-forceable without macOS permuter on Windows.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02198434` | data symbol |
| `_LIT1` | `data_02197434` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Fill32` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

