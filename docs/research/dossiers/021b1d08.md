# 021b1d08 — func_ov008_021b1d08 (ov008, class D, 260B)

## Recipe + Risk
**Recipe:** D: memcpy 0x50; /4 then /10 magic for %20 index; 3x5 OAM fill
**Risk:** struct-guessed + permuter-class: the umull divmod (v/4 bias then *0xcccccccd>>4, *20 remainder) is the exact %20 idiom but the two nested fill loops use ip/r3/r4 in a specific order and `add ip,#0x36` row stride; mwcc loop reg-alloc + the strh post-inc won't reshape cleanly.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021b1d08 (ov008, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: memcpy 0x50; /4 then /10 magic for %20 index; 3x5 OAM fill
 *   risk:       struct-guessed + permuter-class: the umull divmod (v/4 bias then *0xcccccccd>>4, *20 remainder) is the exact %20 idiom but the two nested fill loops use ip/r3/r4 in a specific order and `add ip,#0x36` row stride; mwcc loop reg-alloc + the strh post-inc won't reshape cleanly.
 *   confidence: low
 */
/* func_ov008_021b1d08 — 0x50-byte struct-copy + umull divmod index + dual fill loops.
 * data_ov008_021b23e0 = 0x50-byte source table copied to a stack buffer;
 * (*data_ov008_021b270c)->f0x24 drives a /4 then /10 magic (0xcccccccd) to
 * index the local buffer; func_0208e1cc()+0xf6+0x400 gives the OAM dest base. */
#include "ov008_core.h"
#include <string.h>

extern int func_0208e1cc(void);

void func_ov008_021b1d08(int a0) {
    int buf[20];                                   /* 0x50 bytes */
    int *o = *(int **)data_ov008_021b270c;
    int v, q, sel, i, j;
    unsigned short *p;
    memcpy(buf, data_ov008_021b23e0, 0x50);        /* ldmia/stmia x5 loop */
    v = o[0x24 / 4];                               /* [r0,#0x24] */
    q = (v / 4) / 10;                              /* asr/umull 0xcccccccd */
    sel = buf[(v / 4) - 20 * q];                   /* index = (v/4) % 20 */
    p = (unsigned short *)(func_0208e1cc() + 0xf6 + 0x400);
    if (a0 == 0 || sel < 0) {
        for (i = 0; i < 3; i++) {                  /* fill 0 */
            for (j = 0; j < 5; j++) *p++ = 0;
            p = (unsigned short *)((char *)p + 0x36);
        }
    } else {
        int base = sel * 15 + 0x290;              /* rsb r5,r5,r5 lsl#4 = *15 */
        for (i = 0; i < 3; i++) {
            for (j = 0; j < 5; j++) { *p++ = (unsigned short)(base | 0x4000); base++; }
            p = (unsigned short *)((char *)p + 0x36);
        }
    }
}
```

## Struct context
Uses `ov008_core.h` globals:
- `data_ov008_021b270c`: scene object; field at +0x24 (`o[9]`) is the v-value driving the index computation.
- `data_ov008_021b23e0`: 0x50-byte (20-int) const table — already matched in `data_ov008_021b23e0.c`; values 0..5, then 0xffffffff x2, then 5..0 (symmetric ramp with sentinel).

| field | offset | type | purpose |
|-------|--------|------|---------|
| f24 | +0x24 | int | raw index value; divided by 4, then %20 into buf |

Divmod recipe:
- `v / 4`: `asr#2` (signed right shift).
- `(v/4) / 10` → quotient `q`: `umull` with magic `0xcccccccd` then `lsr#4` — write `(v/4) / 10`.
- `(v/4) % 20` = `(v/4) - 20*q`: write `buf[(v/4) - 20*q]` as array index.
- `sel * 15`: `rsb r,r,r lsl#4` = `r*16 - r` = `r*15` — write `* 15` directly; mwcc will emit the rsb-lsl pattern.
- Row stride `0x36` bytes: the `p` pointer advances by 5 u16s (10 bytes) per inner loop, then `+0x36` skips to the next OAM row start. The `+0x36` is added as a byte offset after casting back to `char*`.

## Closest matched example
**Path:** src/overlay008/data_ov008_021b23e0.c
**Why similar:** the source table for the `memcpy` is this already-matched data file; its layout (0..5, sentinel pair, 5..0) confirms the 20-element int array shape and the index semantics.
**Key lesson from that file:** the table contains `0xffffffff` (-1) sentinels at indices 6 and 7; `sel < 0` in the guard catches these (since `buf[index]` returns -1 = `0xffffffff` as a signed int, which is `< 0`). The `memcpy` of exactly 0x50 bytes maps to five `ldmia`/`stmia` pairs in the asm — write `memcpy(buf, data_ov008_021b23e0, 0x50)` and let mwcc emit the block-copy.

## Try this
1. Class D + permuter-class: the two nested loops with `strh *p++ = 0` and `p = (char*)p + 0x36` are the scheduling wall. Before going to permuter, try writing the inner loop as `for (j = 0; j < 5; j++, p++) *p = 0;` (using `p++` in the for increment) vs the current `*p++ = 0` form — different loop-increment positions can change which register holds `p` at loop end.
2. The `base | 0x4000` followed by `base++` is a post-increment that also or's a constant. If objdiff shows the `orr` and the `add #1` fused or reordered, try `*p++ = (unsigned short)((base++) | 0x4000)` — post-increment inside the expression may alter the instruction schedule.
3. If after two reshape attempts the loops still diverge (ip/r3/r4 reg assignment wrong), this is confirmed permuter-class. Run a short permuter seeded with the loop body, then route to `.s` if no convergence after 500 iterations.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov008_021b23e0` | raw |
| `_LIT1` | `data_ov008_021b270c` | raw |
| `_LIT2` | `0xcccccccd` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208e1cc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

