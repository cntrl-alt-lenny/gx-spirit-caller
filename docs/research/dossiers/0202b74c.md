# 0202b74c — func_0202b74c (main, class C, 128B)

## Recipe + Risk
**Recipe:** if(id==0) early -1; for(i=1;i<n;i++) so guard=cmp/ble, body cmp+bne; pass i to callee
**Risk:** reshape-able (decl-order/loop-shape): func_0202b0e0 must receive the live loop index i (asm leaves r0=i at the bl). If mwcc spills i or reorders ret/list decls, the index reg or the i=1 start diverges -- keep i as the controlling var, declare list before ret.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0202b74c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     if(id==0) early -1; for(i=1;i<n;i++) so guard=cmp/ble, body cmp+bne; pass i to callee
 *   risk:       reshape-able (decl-order/loop-shape): func_0202b0e0 must receive the live loop index i (asm leaves r0=i at the bl). If mwcc spills i or reorders ret/list decls, the index reg or the i=1 start diverges -- keep i as the controlling var, declare list before ret.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0202b74c (main, class C) -- batch p_0011.
 * UNVERIFIED build-free draft. Lookup: if id==0 return -1. Acquire list via func_02006c0c,
 * scan entries[1..count) for a matching id, on hit ret = func_0202b0e0(i). Then if list!=0
 * Task_InvokeLocked(list). Return ret.  r6=id, r4=ret(-1 init), r5=list.
 */
extern void Task_InvokeLocked(void *list);
extern int  data_020b4768[];
extern char data_020c6a88[];
extern int  func_02006c0c(void *a, int b, int c);
extern int  func_0202b0e0(int idx);

int func_0202b74c(int id) {
    int *list;
    int ret;
    int n;
    int i;
    if (id == 0)
        return -1;
    ret = -1;
    list = (int *)func_02006c0c(data_020c6a88, 4, 0);
    n = data_020b4768[0];
    for (i = 1; i < n; i++) {
        if (id == list[i]) {
            ret = func_0202b0e0(i);
            break;
        }
    }
    if (list != 0)
        Task_InvokeLocked(list);
    return ret;
}
```

## Struct context

No GameSingleton or SystemWork fields accessed directly. The function touches:
- `data_020b4768[0]` — an int count (the loop bound `n`).
- `list[i]` — the acquired list buffer, accessed as `int*` with stride 4.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| data_020b4768+0 | count | int | loop bound n |
| list[i] | entry id | int | compared against `id` arg |

## Closest matched example
**Path:** src/main/func_0202b410.c
**Why similar:** Also calls `func_02006c0c(data_..., 4, 0)` to acquire a list pointer, then uses it; same acquire-then-release (Task_InvokeLocked) idiom around the work.
**Key lesson:** The declaration order `list` before `ret` controls which callee-saved register mwcc assigns first (r5=list, r4=ret matches the asm). If the order inverts, swap the declarations. The loop index `i` must be the controlling variable (not a separate copy) so it lives in a callee-saved register and is passed directly as r0 to `func_0202b0e0` at the `bl`.

## Try this
1. Swap declaration order if objdiff shows r4/r5 assignment inverted: move `int ret;` before `int *list;` or vice-versa until r5=list, r4=ret matches asm prologue.
2. If `func_0202b0e0(i)` receives the wrong value (spilled `i` rather than live reg), pull `i` out of the for-init into a separate declaration and ensure it is not declared between `list` and `ret`.
3. If the post-loop `Task_InvokeLocked` conditional diverges (predicated vs branch), try removing the `!= 0` spelling and using a bare `if (list)` — mwcc sometimes emits `cmp r5,#0; blne` for the pointer guard; escalate to permuter if neither form matches.
