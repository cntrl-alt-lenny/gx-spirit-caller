# 0208d3fc — func_0208d3fc (main, class C, 244B)

## Recipe + Risk
**Recipe:** switch (int); case-order 0x40/0x60->0x20/0x70->0x30->0x10 to drive fallthrough block layout
**Risk:** permuter-class: 6-case signed binary-search tree + 3 fallthrough chains; both the comparison tree AND block ordering are mwcc-specific. Most likely arm in batch to need permuter or .s escape.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208d3fc (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     switch (int); case-order 0x40/0x60->0x20/0x70->0x30->0x10 to drive fallthrough block layout
 *   risk:       permuter-class: 6-case signed binary-search tree + 3 fallthrough chains; both the comparison tree AND block ordering are mwcc-specific. Most likely arm in batch to need permuter or .s escape.
 *   confidence: low
 */
/* func_0208d3fc (main, class C) — field idx5 (=0xa); DEEP SWITCH on int
 * (signed tree), MANY fallthroughs — the brittle one. Cases 0x10,0x20,0x30,0x40,0x60,0x70.
 * Fallthrough chains observed: 0x70->0x30->0x10 and 0x60->0x20; 0x40 standalone.
 *   case 0x40 -> sound[0x246]=0x83
 *   case 0x60 -> sound[0x246]=0x8b ; (ft 0x20) sound[0x245]=0x83
 *   case 0x70 -> sound[0x246]=0x9b ; (ft 0x30) sound[0x245]=0x93 ; (ft 0x10) sound[0x244]=0x83
 *   case 0x20 -> sound[0x245]=0x83
 *   case 0x30 -> sound[0x245]=0x93 ; (ft 0x10) sound[0x244]=0x83
 *   case 0x10 -> sound[0x244]=0x83
 * Case ORDER below is chosen so the fallthrough block layout matches orig
 * (.L_284->.L_290 and .L_2a0->.L_2ac->.L_2b8).
 */
extern unsigned short data_021a6304[13];
extern void func_0208dce4(int bits);

void func_0208d3fc(int arg) {
    unsigned short old = data_021a6304[5];
    data_021a6304[5] = (unsigned short)arg;
    data_021a6304[0] = (data_021a6304[0] | old) & ~arg;
    switch (arg) {
    case 0x40:
        *(volatile unsigned char *)0x04000246 = 0x83;
        break;
    case 0x60:
        *(volatile unsigned char *)0x04000246 = 0x8b;
        /* fallthrough */
    case 0x20:
        *(volatile unsigned char *)0x04000245 = 0x83;
        break;
    case 0x70:
        *(volatile unsigned char *)0x04000246 = 0x9b;
        /* fallthrough */
    case 0x30:
        *(volatile unsigned char *)0x04000245 = 0x93;
        /* fallthrough */
    case 0x10:
        *(volatile unsigned char *)0x04000244 = 0x83;
        break;
    }
    func_0208dce4(data_021a6304[0]);
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| `data_021a6304[0]` | mask word | `unsigned short` | running bitmask OR'd/AND-NOT per dispatch |
| `data_021a6304[5]` | current-state slot (idx5, byte offset 0xa) | `unsigned short` | stores incoming arg; old read before write |
| `0x04000244` | sound channel register | `volatile unsigned char` | written 0x83 (cases 0x10, 0x30 ft, 0x70 ft-ft) |
| `0x04000245` | sound channel register | `volatile unsigned char` | written 0x83 (cases 0x20, 0x60 ft) or 0x93 (cases 0x30, 0x70 ft) |
| `0x04000246` | sound channel register | `volatile unsigned char` | written 0x83 (case 0x40), 0x8b (case 0x60), 0x9b (case 0x70) |

## Closest matched example
**Path:** src/main/func_0208cc18.c
**Key lesson:** The 6-case deep-switch with three interlocking fallthrough chains (0x70->0x30->0x10 and 0x60->0x20) is the most complex dispatch in this family. The C case ordering in the prep file is specifically chosen to produce the `.L_284`/`.L_290` and `.L_2a0`/`.L_2ac`/`.L_2b8` block layout — changing case order will break the fallthrough pairing. mwcc's binary-search pivot for 6 cases spanning 0x10..0x70 is the single most brittle part.

## Try this
1. The C case order in the prep file — `0x40`, `0x60->0x20`, `0x70->0x30->0x10` — is already the result of working backward from the orig block layout. Do not reorder cases; build exactly as written and check objdiff first.
2. Objdiff check: look for the two fallthrough chains in the output. If block ordering differs (e.g. the 0x70-chain blocks appear before the 0x60-chain blocks), try physically reordering the two chain groups in the switch while keeping each chain's internal order intact.
3. Park as permuter-class; the binary-search tree over 6 non-contiguous multiples-of-0x10 values and the triple-chain fallthrough structure are the most fragile combination in this batch. Accept as `.s` if tree shape diverges after one reshape attempt.
