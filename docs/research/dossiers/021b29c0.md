[//]: # (markdownlint-disable MD013 MD041)

# 021b29c0 — func_ov002_021b29c0 (ov002, class E retriage, 244B)

## Re-triage

**Original verdict:** E — previously intractable (reg-alloc walled, shipped as .s)
**Upgrade reason:** Uses PerPlayerRowTable with documented stride 0x868 (LIT1 = 0x868, LIT3 = data_ov002_022cf16c) and reads Ov002Slot.id_lo13 via the `lsl#19 / lsr#19` bitfield pair. The function iterates a secondary table (data_ov002_022d0658) and compares slot id_lo13 against four documented magic card-id constants. Once the struct layout is named, the comparisons become deterministic range checks, not scheduling coin-flips.
**Confidence:** high
**Structs needed:** PerPlayerRowTable, Ov002Slot (id_lo13 bitfield)

## Assembly

```asm
; func_ov002_021b29c0 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022cf16c
        .extern data_ov002_022d0658
        .extern func_ov002_021b1d84
        .global func_ov002_021b29c0
        .arm
func_ov002_021b29c0:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, lr}
    ldr r6, _LIT0
    ldr r0, _LIT1
    ldr sl, _LIT2
    ldr fp, _LIT3
    ldr r8, _LIT4
    ldr r7, _LIT5
    mov r9, #0x1
    add r4, r6, #0x2b
    rsb r5, r0, #0x21c0
.L_1f0:
    ldrh r0, [sl, #0x2]
    mov r0, r0, lsl #0x1c
    mov r0, r0, lsr #0x1c
    cmp r0, #0x8
    bne .L_28c
    ldrh r3, [sl]
    and r0, r3, #0xff
    and r1, r0, #0x1
    ldr r0, _LIT1
    mla r2, r1, r0, fp
    mov r0, r3, asr #0x8
    and r1, r0, #0xff
    mov r0, #0x14
    mla r0, r1, r0, r2
    ldr r0, [r0, #0x30]
    mov r0, r0, lsl #0x13
    cmp r8, r0, lsr #0x13
    mov r1, r0, lsr #0x13
    bcc .L_264
    cmp r1, r8
    bcs .L_284
    cmp r1, r7
    bhi .L_254
    beq .L_284
    b .L_28c
.L_254:
    ldr r0, _LIT6
    cmp r1, r0
    beq .L_284
    b .L_28c
.L_264:
    cmp r1, r6
    bhi .L_27c
    bcs .L_284
    cmp r1, r5
    beq .L_284
    b .L_28c
.L_27c:
    cmp r1, r4
    bne .L_28c
.L_284:
    mov r0, r9
    bl func_ov002_021b1d84
.L_28c:
    add r9, r9, #0x1
    cmp r9, #0x100
    add sl, sl, #0x8
    blt .L_1f0
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
_LIT0: .word 0x00001a4e
_LIT1: .word 0x00000868
_LIT2: .word data_ov002_022d0658
_LIT3: .word data_ov002_022cf16c
_LIT4: .word 0x00001843
_LIT5: .word 0x00001505
_LIT6: .word 0x00001644
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| PerPlayerRowTable | base + (player&1)*0x868 | row base | Ov002FieldZone* | mla base, player&1, 0x868, cf16c to find player row |
| Ov002Slot | +0x030 + slot*0x14 | id_lo13 | u32 : 13 | read via `lsl #0x13 / lsr #0x13`; compared against 4 card-id constants |
| data_ov002_022d0658 | +0x0 (u16 word0), +0x2 (u16 word2) | entry word | u16 | iterated: word2[3:0] = type field == 8; word0 encodes player(lo byte)&slot(hi byte) |

The four magic card-id constants from the literal pool:
- 0x1843 (LIT4 = r8), 0x1505 (LIT5 = r7), 0x1644 (LIT6), 0x1a4e (LIT0 = r6)
- Derived: 0x1a79 = r6+0x2b, 0x19b8 = 0x21c0 - 0x868 = rsb from stride

## Recipe sketch

```c
/* Walks data_ov002_022d0658 (stride 8, count 0xFF) looking for type-8 entries
 * whose target slot holds a card-id in one of four known sets.
 * Calls func_ov002_021b1d84(entry_index) on each match. */
void func_ov002_021b29c0(void) {
    /* LIT0=0x1a4e LIT4=0x1843 LIT5=0x1505 LIT6=0x1644
     * r4 = 0x1a4e+0x2b = 0x1a79
     * r5 = 0x21c0 - 0x868 = 0x1958  (rsb stride from 0x21c0) */
    unsigned short *tbl = data_ov002_022d0658;
    for (int i = 1; i < 0x100; i++, tbl += 4 /* stride 8 bytes = 4 u16s */) {
        unsigned short word2 = tbl[1];          /* +0x2 */
        if ((word2 << 28) >> 28 != 8) continue; /* type field != 8 */
        unsigned short word0 = tbl[0];          /* +0x0 */
        int player = word0 & 0xff;
        int slot   = (word0 >> 8) & 0xff;
        Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (player & 1) * 0x868);
        unsigned int id = row->slots[slot].id_lo13; /* lsl#19 / lsr#19 */
        /* match if id in {0x1843, 0x1505, 0x1644, 0x1a4e, 0x1a79, 0x1958} */
        if (id == 0x1843 || id == 0x1505 || id == 0x1644 ||
            id == 0x1a4e || id == 0x1a79 || id == 0x1958) {
            func_ov002_021b1d84(i);
        }
    }
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_021b3014.c
**Why similar:** Also reads a u16 from `(player&1)*0x868` stride table and dispatches, illustrating the PPT player-row computation pattern.
**Key lesson:** The `mla r2, r1, r0, fp` idiom where r0=0x868 and fp=cf16c base computes player row; slot offset is then `slot * 0x14 + 0x30` into the row. Declaring `Ov002FieldZone *row` typed rather than bare char arithmetic lets mwcc emit the mla as a single instruction.

## Try this

1. Declare `Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (player & 1) * 0x868);` and read `row->slots[slot].id_lo13` — this must be a `unsigned int id_lo13 : 13` bitfield so mwcc emits the `lsl #0x13 / lsr #0x13` pair.
2. If the comparison branch order differs in objdiff, reorder the `if` chain to match: the assembly tests `r8 (0x1843)` first as a lower-bound against `r1`, branches bcc/bcs, so put the `id >= 0x1843` range arm before the `id < 0x1843` arm.
3. Escalate if the four-constant comparison block does not reduce to a clean if/else cascade — this is a multi-range check that may need intermediate temporaries to avoid CSE reordering.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001a4e` | MMIO/const |
| `_LIT1` | `0x00000868` | MMIO/const |
| `_LIT2` | `data_ov002_022d0658` | raw |
| `_LIT3` | `data_ov002_022cf16c` | raw |
| `_LIT4` | `0x00001843` | MMIO/const |
| `_LIT5` | `0x00001505` | MMIO/const |
| `_LIT6` | `0x00001644` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021b1d84` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

