# 021cc664 — func_ov011_021cc664 (ov011, class C, 316B)

## Recipe + Risk
**Recipe:** CLASS C: two-phase init; first arm zeroes, second arm fills from table
**Risk:** two-phase: conditional on flag. zero arm: memset or loop. fill arm: table copy.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021cc664 (ov011, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     max as if-assign (movge); *0xa8>>8; span>>3 + &7; stack-ptr 5th arg reused.
 *   risk:       permuter-class: nine callee-saved regs hold loop invariants (r5 left, r6 count, r8 base, r9 cursor, fp=6); mwcc's induction-var (i*8 vs running r9+=8) and which constants live in regs is a scheduling/reg-rotation coin-flip.
 *   confidence: low
 */
/* func_ov011_021cc664: render a horizontal run of 8-wide cells plus a
 * remainder cell and two cap cells. Inputs lo,hi are .8 fixed; max(lo,hi)
 * sets the right edge. The span [left+0x38, right+0x38] is split into
 * (count) full 8px groups (asr#3) and a (&7) remainder; each group and the
 * caps is drawn via 021cc5c4 with a 5-arg (4 reg + 1 stack ptr) call.
 *
 *   r2=(lo*0xa8); if(hi>=lo)x=hi; r1=(x*0xa8); r2>>=8; r0=(r1>>8)+0x38;
 *   r5=r2>>8... wait: r5=(lo*0xa8>>8)+0x38 (left); right=[sp+8]=(max*0xa8>>8)+0x38
 *   span=right-left; count=span>>3 (r6); rem=span&7 ([sp+4]);
 *   base=021ca094(); loop count: 021cc5c4(6,7,0,0x10, &{ (left+8*i)<<12, base+0x8000 })
 *   if rem: 021cc5c4(6, rem-1, 0,0x10, &{ (left+8*count)<<12, base+0x8000 })
 *   021cc5c4(5,-1,0,0x10,&{ (right-3)<<12, base }); 021cc5c4(0,-1,0,0x10,&{ (left-3)<<12, base })
 */

extern int  func_ov011_021ca094(void);
extern void func_ov011_021cc5c4(int a, int b, int c, int d, void *e);

void func_ov011_021cc664(int lo, int hi) {
    int sp_arg[2];          /* {coord, handle} passed by &, at sp+0xc/0x10 */
    int right, left, span, count, rem, base, i, x;

    x = lo;
    if (hi >= lo)
        x = hi;

    right = ((x  * 0xa8) >> 8) + 0x38;
    left  = ((lo * 0xa8) >> 8) + 0x38;
    span  = right - left;
    count = span >> 3;
    rem   = span & 7;

    base = func_ov011_021ca094();

    if (count > 0) {
        i = 0;
        do {
            sp_arg[0] = (left + i * 8) << 0xc;
            sp_arg[1] = base + 0x8000;
            func_ov011_021cc5c4(6, 7, 0, 0x10, sp_arg);
            i++;
        } while (i < count);
    }

    if (rem != 0) {
        sp_arg[0] = (left + count * 8) << 0xc;
        sp_arg[1] = base + 0x8000;
        func_ov011_021cc5c4(6, rem - 1, 0, 0x10, sp_arg);
    }

    sp_arg[0] = (right - 3) << 0xc;
    sp_arg[1] = base;
    func_ov011_021cc5c4(5, -1, 0, 0x10, sp_arg);

    sp_arg[0] = (left - 3) << 0xc;
    sp_arg[1] = base;
    func_ov011_021cc5c4(0, -1, 0, 0x10, sp_arg);
}
```

## Struct context

This function does not directly access a named struct. It uses:
- Locals only: `left`, `right`, `span`, `count`, `rem`, `base`, `i`, `x`.
- A 2-element `int sp_arg[2]` on the stack as the 5th-argument pointer to `021cc5c4`.
- `func_ov011_021ca094()` returns a base handle/address (no struct fields accessed).

| field   | offset | type | purpose |
|---------|--------|------|---------|
| sp_arg[0] | sp+0 | int | packed coord shifted left 12 |
| sp_arg[1] | sp+4 | int | base handle ± 0x8000 |

## Closest matched example
**Path:** src/overlay011/func_ov011_021cc5c4.c
**Why similar:** same 5-arg call signature with a stack pointer as arg5; same handle-table idiom; size is 160B (smaller but same family).
**Key lesson from that file:** The 5th argument must be a local stack array (`int sp_arg[2]`) whose address is taken with `&sp_arg[0]` (or just `sp_arg`). mwcc passes it as `add rN, sp, #off`. Declaring the 2-element array before the scalar locals (right, left…) may shift which stack offset mwcc assigns it — experiment with declaration order if the `add sp, #off` offset differs.

## Try this
1. The big risk is nine callee-saved regs. Check objdiff for the `stmdb sp!,{...}` list first — if mwcc uses r4–fp (10 regs) vs. orig's count, the induction var form (`i*8` vs. `base+=8` running pointer) is probably wrong. Try replacing the do-while with a running-pointer loop: `int *p = &sp_arg[0]; do { p[0] = (left + i*8)<<12; ... }` vs. a pointer-advance `cursor += 8`.
2. If the loop matches but `rem/count` register assignments differ: move the declaration of `rem` before `count` (or vice versa) to flip which callee-saved reg each gets.
3. If still stuck (9-reg rotation is a permuter-class wall per the risk note): convert to .s and ship.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov011_021ca094` | overlay call (ov011) |
| `bl` | `func_ov011_021cc5c4` | overlay call (ov011) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

