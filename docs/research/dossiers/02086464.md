# 02086464 — func_02086464 (main, class D, 328B)

## Recipe + Risk

**Recipe:** stack-struct store-order; blx via fnptr-table[flags&7]; smull>>12 reload obj[0x30]
**Risk:** struct-guessed: s[] index->sp mapping (0x8/0x38/0x3c/0x44/0x48) inferred; orig RELOADS obj[0x30] each of 3 smull (r3=[r4,#0x30] thrice) — if mwcc binds it once, 2 ldr diverge (reshape: separate reads).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02086464 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     stack-struct store-order; blx via fnptr-table[flags&7]; smull>>12 reload obj[0x30]
 *   risk:       struct-guessed: s[] index->sp mapping (0x8/0x38/0x3c/0x44/0x48) inferred; orig RELOADS obj[0x30] each of 3 smull (r3=[r4,#0x30] thrice) — if mwcc binds it once, 2 ldr diverge (reshape: separate reads).
 *   confidence: med
 */
/* func_02086464: build 0x50 stack-struct, blx a dispatch fn via data_02102380[flags&7],
 * then conditionally FX12-scale 3 fields by obj[0x30] and 3 by obj[0x34], then func_02084e0c. */
typedef signed int s32;
typedef signed long long s64;
typedef s32 fx32;

extern void (*data_02102380[])(int *, void *);
extern void func_02084e0c(int, int *, int);

void func_02086464(int *o) {
    int s[20];                  /* 0x50 bytes */
    s[0] = (o[0] & 8) ? 0x00101610 : 0x00101810;
    s[17] = 0x1000;             /* sp+0x44 */
    s[1] = 3;
    s[18] = 2;                  /* sp+0x48 */
    s[16] = 0;                  /* sp+0x40 */
    s[13] = 0; s[12] = 0; s[11] = 0; s[10] = 0; s[9] = 0; s[8] = 0;
    s[5] = 0; s[4] = 0;
    data_02102380[o[0] & 7](&s[2], o);   /* blx, dst = sp+0x8 */
    if (o[0x0c] != 0x1000) {             /* obj[0x30] */
        s[2]  = (s32)(((s64)o[0x0c] * s[2])  >> 12);
        s[3]  = (s32)(((s64)o[0x0c] * s[3])  >> 12);
        s[14] = (s32)(((s64)o[0x0c] * s[14]) >> 12);   /* sp+0x38 */
    }
    if (o[0x0d] != 0x1000) {             /* obj[0x34] */
        s[6]  = (s32)(((s64)o[0x0d] * s[6])  >> 12);
        s[7]  = (s32)(((s64)o[0x0d] * s[7])  >> 12);
        s[15] = (s32)(((s64)o[0x0d] * s[15]) >> 12);   /* sp+0x3c */
    }
    func_02084e0c(s[0], &s[1], 0x12);
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| o[0] | flags | u32 | bit3=0x8 literal selector; bits[2:0] = dispatch table index (& 7) |
| o[0x0c] (obj+0x30) | scale_x | fx32 | Q12 scale factor; reloaded 3x in `if` block — do NOT bind to local |
| o[0x0d] (obj+0x34) | scale_y | fx32 | Q12 scale factor; reloaded 3x in second `if` block |
| data_02102380[] | fnptr_table | fn*[] | indexed by `o[0] & 7`; called via blx; fills &s[2] as out-arg |
| s[2..3] (sp+0x08..0x0c) | | fx32 | first two scaled outputs; also blx dst ptr entry |
| s[14] (sp+0x38) | | fx32 | third x-scale output |
| s[6..7] (sp+0x18..0x1c) | | fx32 | first two y-scale outputs |
| s[15] (sp+0x3c) | | fx32 | third y-scale output |

## Closest matched example

**Path:** src/main/func_020808b8.c
**Key lesson:** func_020808b8 (192B, store-order reshape) shows that the exact declaration order of stack-struct stores controls the `str` sequence mwcc emits. Here the zero-init batch (`s[13]=0; s[12]=0; ...`) at the top of the function must appear in exactly the written descending index order, not hoisted or reordered, since the original emits them in a specific sequence before the `blx` dispatch.

## Try this

1. In both `if (o[0x0c] != 0x1000)` and `if (o[0x0d] != 0x1000)` blocks, write `o[0x0c]` (resp. `o[0x0d]`) as a direct array dereference at every use — do NOT introduce a local `int sc = o[0x0c]`. The risk note explicitly states the orig reloads `[r4,#0x30]` three times; mwcc must emit 3 separate `ldr r3,[r4,#0x30]` instructions. Binding to a local collapses them to 1.
2. Verify the s[] index-to-sp-offset mapping via objdiff: s[2] must be at sp+0x08, s[14] at sp+0x38, s[15] at sp+0x3c, s[16] at sp+0x40, s[17] at sp+0x44, s[18] at sp+0x48. If any offset differs by 4, adjust by shifting the index (e.g. if s[14] lands at sp+0x3c, use s[15] for the third scale target).
3. If objdiff shows `o[0x0c]` still bound to one register after step 1 (mwcc CSE'ing it anyway), add a `volatile` read: `*(volatile int *)(o + 0x0c)` — though this may shift other regs. If that still diverges, escalate to permuter-class.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00101610` | MMIO/const |
| `_LIT1` | `0x00101810` | MMIO/const |
| `_LIT2` | `data_02102380` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `blx` | `r2` |  |
| `bl` | `func_02084e0c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

