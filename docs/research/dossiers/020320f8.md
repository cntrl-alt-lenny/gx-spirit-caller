# 020320f8 — func_020320f8 (main, class D, 184B)

## Recipe + Risk
**Recipe:** exact store-order incl field0=0 mid-block; stack-arg d/e/f via [sp]; mvn=-1 wrap guard
**Risk:** struct-guessed: 0x24 alloc size + field map inferred; args a,b are strh (short). Risk store-order: orig writes field0=0 AFTER field20; if mwcc sinks the zero-init it reorders str. Confirm Obj/Node layout.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_020320f8 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     exact store-order incl field0=0 mid-block; stack-arg d/e/f via [sp]; mvn=-1 wrap guard
 *   risk:       struct-guessed: 0x24 alloc size + field map inferred; args a,b are strh (short). Risk store-order: orig writes field0=0 AFTER field20; if mwcc sinks the zero-init it reorders str. Confirm Obj/Node layout.
 *   confidence: low
 */
typedef struct Obj {
    /* 0x00 */ unsigned int field0;
    /* 0x04 */ struct Node *field4;
    /* 0x08 */ struct Node *field8;
    /* 0x0c */ struct Node *fieldc;
} Obj;

typedef struct Node {
    /* 0x00 */ struct Node *field0;
    /* 0x04 */ struct Node *field4;
    /* 0x08 */ unsigned int field8;
    /* 0x0c */ short fieldc;
    /* 0x0e */ short fielde;
    /* 0x10 */ int field10;
    /* 0x14 */ int field14;
    /* 0x18 */ int field18;
    /* 0x1c */ int field1c;
    /* 0x20 */ int field20;
} Node;

extern void *func_02032028(Obj *obj, int size);

int func_020320f8(Obj *obj, short a, short b, int c, int d, int e, int f)
{
    Node *n;
    int prev;

    if (obj == 0)
        return -1;
    n = (Node *)func_02032028(obj, 0x24);
    if (n == 0)
        return -1;
    n->field8 = obj->field0;
    n->fieldc = a;
    n->fielde = b;
    n->field10 = c;
    n->field14 = 0;
    n->field18 = d;
    n->field1c = e;
    n->field20 = f;
    n->field0 = 0;
    n->field4 = (Node *)obj->field8;
    if (obj->field8 != 0)
        obj->field8->field0 = n;
    obj->field8 = n;
    if ((Node **)&obj->fieldc != 0) {
        if (obj->fieldc == 0)
            obj->fieldc = n;
    }
    prev = (int)obj->field0;
    prev = prev + 1;
    obj->field0 = (unsigned int)prev;
    if (prev == -1)
        obj->field0 = (unsigned int)(prev + 1);
    return (int)n->field8;
}
```

## Struct context
No GameSingleton/SystemWork offsets. This is an allocate-and-insert function for a pool-managed linked list. The `mvn=-1` wrap guard (check if counter wrapped to -1 after increment) is the distinguishing idiom.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| obj+0x00 | field0 (counter) | unsigned int | read as `prev`; incremented; wrap-guarded; written back |
| obj+0x04 | field4 | Node* | (unused in function body — part of Obj layout) |
| obj+0x08 | field8 (head) | Node* | read for n->field4; old head->field0 patched; updated to n |
| obj+0x0c | fieldc (tail?) | Node* | address taken (`&obj->fieldc`) as null-guard; written to n if zero |
| n+0x08 | field8 | unsigned int | written from obj->field0; returned at end |
| n+0x0c | fieldc | short | written from arg a (strh) |
| n+0x0e | fielde | short | written from arg b (strh) |
| n+0x10 | field10 | int | written from arg c |
| n+0x14 | field14 | int | zeroed mid-block |
| n+0x18 | field18 | int | written from arg d (stack) |
| n+0x1c | field1c | int | written from arg e (stack) |
| n+0x20 | field20 | int | written from arg f (stack) |
| n+0x00 | field0 | Node* | zeroed after field20 store |
| n+0x04 | field4 | Node* | written with old obj->field8 |

## Closest matched example
**Path:** src/main/Task_PostLocked.c
**Why similar:** Both are IRQ-free allocate+insert functions with a counter, double null-guard, and exact store-order sensitivity; similar complexity (184B vs 0x74B).
**Key lesson:** The store order `field8, fieldc, fielde, field10, field14=0, field18, field1c, field20, field0=0, field4` must be preserved exactly. The `field0=0` store after `field20` (not at the top) is what distinguishes this from a simple memset pattern and is where mwcc is most likely to sink the zero to the top. Placing `n->field14 = 0` and `n->field0 = 0` exactly where they appear in the sequence (not hoisted to a declaration-time init) is the primary reshape lever. The `(Node **)&obj->fieldc != 0` guard follows the same always-true address pattern as func_02031ebc's headc guard.

## Try this
1. Keep `n->field14 = 0` and `n->field0 = 0` at their exact sequence positions (after field10 and after field20 respectively) — do NOT initialize them at declaration or as part of a memset. If mwcc hoists either zero to the alloc-return point, add a dummy use between them: `(void)n->field10` after the field10 store.
2. Verify that args `a` and `b` are typed `short` (not `int`) at the call site — `strh` emission depends on the argument type being short; if they arrive as `int` parameters and are stored to `short` fields, mwcc emits `strh` correctly, but the intermediate register truncation must not disappear.
3. If the `prev == -1` wrap guard emits `cmn` + `bne` (compare-negative) rather than `cmp r0,#0xffffffff`, restructure as `if ((int)obj->field0 == -1) obj->field0 = 0;` — the `mvn` pattern is a coin-flip between `cmn` and `mvn`+`cmp`; escalate to permuter if neither matches.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02032028` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

