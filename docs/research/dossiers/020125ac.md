# 020125ac — func_020125ac (main, class D, 112B)

## Recipe + Risk

**Recipe:** const-fold sub (0x26-0x27=-1) kept as expr; store-order=source; reuse confirmed struct
**Risk:** const materialization: orig derives bc4 as `sub r3,r0,#0x27` from the 0x26 in r0 and reuses r0=1 for c40/c44/b84. If mwcc remats constants instead of reusing, mov/str count diverges. reshape-able (keep 0x26-0x27 form, share the `1`).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020125ac (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     const-fold sub (0x26-0x27=-1) kept as expr; store-order=source; reuse confirmed struct
 *   risk:       const materialization: orig derives bc4 as `sub r3,r0,#0x27` from the 0x26 in r0 and reuses r0=1 for c40/c44/b84. If mwcc remats constants instead of reusing, mov/str count diverges. reshape-able (keep 0x26-0x27 form, share the `1`).
 *   confidence: med
 */
/* func_020125ac(): like 020124f4 but all-constant init with fold tells:
 * f_bbc=1, f_bc0=0x26, f_bc4=(0x26-0x27)=-1, f_b84=1. Same data_021040ac
 * layout (confirmed via func_02012560.c). Stores in exact asm order. */
typedef struct {
    char _pad_b84[0xb84];
    int  f_b84;   /* 0xb84 */
    int  f_b88;   /* 0xb88 */
    char _pad_b90[0x14];
    int  f_ba0;   /* 0xba0 */
    char _pad_bb0[0xc];
    int  f_bb0;   /* 0xbb0 */
    int  f_bb4;   /* 0xbb4 */
    char _pad_bbc[0x4];
    int  f_bbc;   /* 0xbbc */
    int  f_bc0;   /* 0xbc0 */
    int  f_bc4;   /* 0xbc4 */
    int  f_bc8;   /* 0xbc8 */
    char _pad_c38[0x6c];
    int  f_c38;   /* 0xc38 */
    int  f_c3c;   /* 0xc3c */
    int  f_c40;   /* 0xc40 */
    int  f_c44;   /* 0xc44 */
    int  f_c48;   /* 0xc48 */
} state_021040ac_t;

extern state_021040ac_t data_021040ac;
extern int func_02011aec(void);

void func_020125ac(void)
{
    data_021040ac.f_ba0 = 0;
    data_021040ac.f_bbc = 1;
    data_021040ac.f_bc0 = 0x26;
    data_021040ac.f_bc4 = 0x26 - 0x27;
    data_021040ac.f_bc8 = func_02011aec();
    data_021040ac.f_bb0 = 0;
    data_021040ac.f_bb4 = -1;
    data_021040ac.f_c38 = 0;
    data_021040ac.f_c3c = 0;
    data_021040ac.f_c48 = 2;
    data_021040ac.f_c40 = 1;
    data_021040ac.f_c44 = 1;
    data_021040ac.f_b88 = 0;
    data_021040ac.f_b84 = 1;
}
```

## Struct context

This function does not touch SystemWork or GameSingleton. It operates on `data_021040ac`, a file-static global struct confirmed by the sibling function `func_02012560`. The struct layout has been pinned via that sibling.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| 0xba0 | f_ba0 | int | zeroed first (asm order) |
| 0xbbc | f_bbc | int | set to 1 |
| 0xbc0 | f_bc0 | int | set to 0x26 (r0 held for sub) |
| 0xbc4 | f_bc4 | int | set to 0x26-0x27 = -1 (sub r3,r0,#0x27 in orig) |
| 0xbc8 | f_bc8 | int | receives func_02011aec() result |
| 0xbb0 | f_bb0 | int | zeroed |
| 0xbb4 | f_bb4 | int | set to -1 |
| 0xc38 | f_c38 | int | zeroed |
| 0xc3c | f_c3c | int | zeroed |
| 0xc48 | f_c48 | int | set to 2 |
| 0xc40 | f_c40 | int | set to 1 (r0=1 reuse) |
| 0xc44 | f_c44 | int | set to 1 (r0=1 reuse) |
| 0xb88 | f_b88 | int | zeroed |
| 0xb84 | f_b84 | int | set to 1 (r0=1 reuse, last store) |

## Closest matched example

**Path:** src/main/func_02012560.c
**Why similar:** Same `data_021040ac` struct, same constant-init pattern, sibling function with 11 stores in fixed asm order.
**Key lesson:** The struct typedef must keep `_pad_ba0` at exactly 0x10 bytes so `f_ba0` lands at 0xba0 (the sibling had an off-by-one bug there that was fixed). Store order in source must exactly match asm order; mwcc does not reorder straight-line stores. The `0x26-0x27` expression must stay as written — do not simplify to -1 literal, or mwcc will materialise the constant differently and the `sub r3,r0,#0x27` from the loaded r0=0x26 will not match.

## Try this

1. Drop the prep draft verbatim. The critical shape is `f_bc4 = 0x26 - 0x27` (not `-1`) so mwcc derives bc4 via `sub` from the 0x26 already in a register. Verify the store order in objdiff matches the 14-store sequence exactly.
2. If objdiff shows extra `mov`/`str` for c40/c44/b84 (mwcc rematerialising instead of reusing r0=1): try declaring a local `const int one = 1` and assigning `f_bbc = one; f_c40 = one; f_c44 = one; f_b84 = one;` to force register reuse.
3. If store count still diverges (padding wrong): re-check the `_pad_b90` vs `_pad_bb0` gap sizes against the confirmed func_02012560.c layout; the bb0/bb4 fields may need a different intermediate pad.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021040ac` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02011aec` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

