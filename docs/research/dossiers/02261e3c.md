# 02261e3c — func_ov002_02261e3c (ov002, class F retriage, 384B)

## Re-triage
**Original verdict:** F — previously intractable (high confidence)
**Upgrade reason:** Accesses PerPlayerRowTable and Ov002Slot. Embedded at offset +0x1F8 (504 bytes) within block func_ov002_02261c44 (EUR delink 0x02261C44–0x02262738, size=0xAF4). The containing block's structure (visible in func_ov002_02261c44.s) reveals: (1) a DSS state sub-counter at `+0xD30` driving a 10-case dispatch table at the top, (2) inner loops using `dss->card_list_slot_idx (+0xCEC)` XOR for player parity, (3) PerPlayerRowTable stride-0x868 mla, (4) Ov002Slot slot walks with `func_ov002_021baca8` / `func_ov002_021df62c` / `func_ov002_021d5b80` calls. With these fields named, case arm at offset +0x1F8 (approximately state 2 of the 10-case dispatch) is fully deterministic.
**Confidence:** high
**Structs needed:** PerPlayerRowTable (base 0x022CF16C, stride 0x868), Ov002Slot (id_lo13 lsl#19/lsr#19, +0x36 u16 field, +0x40 flag word)

## Assembly

```asm
; NOTE: func_ov002_02261e3c is NOT a standalone .s file in EUR.
; It begins at offset +0x1F8 within func_ov002_02261c44.s
; (EUR delink block 0x02261C44–0x02262738, size=0xAF4 = 2804 bytes).
; The containing block is a 10-case dispatch (addls pc, r0, lsl #2) on
; dss->f_d30 (DuelStateSingleton +0xD30 sub-state), with cases at:
;   state 0: .L_5f40 (func_ov002_02244fe4 + increment f_d30)
;   state 1: .L_5f68 (eor-player double loop reading dss->f_cec)
;   state 2: .L_62cc (starts at ~offset +0x288 from block base)
;   ... up to state 9: .L_6880
; func_ov002_02261e3c covers approximately state-2 and state-3 arms
; (offsets +0x1F8 to +0x37B from block base), or equivalently .L_62cc
; through .L_6358 in the .s labels.
; Key assembly from lines 290-330 of func_ov002_02261c44.s:
;   .L_62cc:  mov r7, #0
;             ldr r5, _LIT5           ; constant (slot id literal)
;   .L_62dc:  ldr r0, [r0, #0xcec]   ; dss->card_list_slot_idx
;             eor r8, r0, r7          ; XOR player parity
;   .L_62ec:  bl func_ov002_021b3ecc ; slot probe
;             bl func_ov002_021df6d4
;             bl func_ov002_021de480
;             add r9, r9, #1; cmp r9, #5; blt .L_62ec  ; slot loop 0..4
;             add r7, r7, #1; cmp r7, #2; blt .L_62dc  ; player loop 0..1
;             [increment dss->f_d30]
; Full assembly requires extraction from the binary at 0x02261E3C.
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelStateSingleton | +0xD30 | f_d30 | int | sub-state counter driving the dispatch; incremented at end |
| DuelStateSingleton | +0xCEC | card_list_slot_idx | int | XOR with player loop counter (eor-discriminant) |
| PerPlayerRowTable | base+(xi&1)*0x868 | row | ptr | per-player zone via mla stride 0x868 |
| Ov002Slot | row+0x30+s*0x14 (+0x00) | id_lo13 | u32:13 | lsl#19/lsr#19 in downstream dispatch calls |

## Recipe sketch

```c
/* F-class 384-byte: one of 10 dispatch arms from the DSS +0xD30 state machine.
 * This arm (state ~2) performs eor-player double loop + slot dispatch,
 * then increments f_d30 to advance the state machine. */
int func_ov002_02261e3c(int player) {
    DuelStateSingleton *dss = &data_ov002_022d016c;
    int p;
    for (p = 0; p < 2; p++) {
        int xi = dss->card_list_slot_idx ^ p;    /* +0xCEC */
        int s;
        for (s = 0; s < 5; s++) {
            if (!func_ov002_021b3ecc(xi, s, 0x15b8)) continue;
            func_ov002_021df6d4(xi, s);
            func_ov002_021de480(xi, s, 1, 0x15b8);
        }
    }
    dss->f_d30++;                                /* +0xD30 advance state */
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_02256a50.c (retriage doc recipe)
**Why similar:** Same DSS+0xCEC eor-discriminant double loop (player=0..1, slot=0..4), matching the `.L_62dc`/`.L_62ec` loop labels visible in the containing block.
**Key lesson:** The 10-case state machine at the top of func_ov002_02261c44 dispatches to sub-functions by incrementing `+0xD30`. Each arm increments `f_d30` at the end (seen at lines 285-288 of the .s file: `ldr r2, [r1, #0xd30] / add r2, r2, #1 / str r2, [r1, #0xd30]`). The increment must be the last statement before the return.

## Try this
1. This function requires delink before matching. The EUR symbols.txt entry should be `func_ov002_02261e3c kind:function(arm,size=0x180)` — note it is one of several arms within the larger 02261c44 dispatch block.
2. Once delinked, declare `int xi = dss->card_list_slot_idx ^ p;` inside the outer loop. The literal `0x15b8` (seen in `_LIT5` of the containing block at `.L_62dc`) is the slot-id argument to `func_ov002_021b3ecc`.
3. Confirm the `f_d30` increment is present as the final statement. If objdiff shows the return type is void (no useful return value), declare `void` — many state-machine arms in this block return nothing.
