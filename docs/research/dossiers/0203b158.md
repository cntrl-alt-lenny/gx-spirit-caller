# 0203b158 — func_0203b158 (main, class D, 412B)

## Recipe + Risk
**Recipe:** div-by-const 1016 via plain /; 7 stack temps incl dst[2]; reload f74/fb8; store-order e->f0c,f12
**Risk:** stack-slot assignment of the 7 temps (esp. dst[2] landing at sp#14/#18) and the several aliasing reloads (f74 x2-3, fb8/iter) are scheduling/layout dependent. permuter-class; expect iteration. Offsets struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0203b158 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     div-by-const 1016 via plain /; 7 stack temps incl dst[2]; reload f74/fb8; store-order e->f0c,f12
 *   risk:       stack-slot assignment of the 7 temps (esp. dst[2] landing at sp#14/#18) and the several aliasing reloads (f74 x2-3, fb8/iter) are scheduling/layout dependent. permuter-class; expect iteration. Offsets struct-guessed.
 *   confidence: low
 */
/* func_0203b158 (cls D): stack-temp-heavy render walk. FULL body recovered from
 * build/eur/dis/src/main/func_0203b158.s. Divisor recovered: magic 0x02040811 +
 * (n+umulhi)>>10 round form == n / 1016 (0x3F8); confirmed by q*0x3f8 alignment.
 * 7 stack temps at sp #0,4,8,c,10,14,18 (dst[2] occupies #14/#18, indexed by i).
 * MMIO read at 0x4000006 is REG_VCOUNT. Many globals reloaded due to aliasing. */

extern int func_01ff8000(void *a, int b, void *c);

int func_0203b158(char *ctx, int idx) {
    char    *base78  = *(char**)(ctx + 0x78);
    unsigned n       = *(unsigned*)(base78 + idx * 0x20);
    int      q       = (int)(n / 1016u);
    int      aligned = q * 0x3f8;
    int     *ptr60   = (int*)(ctx + 0x60) + idx;
    int      qp1     = q + 1;
    unsigned short qlo = (unsigned short)q;
    int      neg42   = ~0x29;
    char    *dst[2];
    int      qx512   = q << 9;
    char    *base84  = *(char**)(ctx + 0x84);
    char    *e       = base78 + idx * 0x20;
    int      i;

    dst[0] = base84 + idx * 0x200;
    dst[1] = (base84 + 0x400) + idx * 0x200;

    for (i = 0; i < *(unsigned short*)(ctx + 0xb8); i++) {
        if (q != *(unsigned short*)(e + 0x12)) {
            char *t   = *(char**)(ctx + 0x74);
            int   r6v = *(int*)(t + 0x34);
            int   r3v = *(int*)(t + 0x30);
            int   flag = 0;
            int   r1v;
            if ((unsigned)qp1 >= *(unsigned*)(t + 0x2c)) {
                r6v  = *(int*)(t + 0x3c);
                r3v  = *(int*)(t + 0x38);
                flag = 1;
            }
            r1v = *(int*)(t + 0x28) + *(int*)(ctx + 0x68);
            r1v = (int)(*(unsigned short*)(ctx + 0xb8)) * qx512 + r1v;
            if (idx == 0) {
                int ip = 0xb6 - *(unsigned short*)0x4000006;
                if (ip <= neg42)
                    ip += 0x107;
                if (ip < 0)
                    return 0;
            }
            r1v += i * r3v;
            if (func_01ff8000(ptr60, r1v, dst[i]) == 0)
                return 0;
            *(int*)(e + 0xc)  = aligned + r6v;
            *(unsigned short*)(e + 0x12) = qlo;
            if (flag != 0) {
                char *t2 = *(char**)(ctx + 0x74);
                *(int*)(e + 0xc) = *(int*)(t2 + 0x20);
                {
                    char *t3 = *(char**)(ctx + 0x74);
                    if (*(unsigned char*)(t3 + 0x19) == 0) {
                        *(int*)(e + 0xc) = *(int*)(t3 + 0x24);
                        *(unsigned short*)(ctx + 0xb0) |= 2;
                    }
                }
            }
        }
        e += 0xc;
    }
    return 1;
}
```

## Struct context
This function operates on `ctx` (a locally-defined blob) and does NOT touch GameSingleton or SystemWork. All offsets are inferred from the disassembly.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| ctx+0x60 | ptr60 | int* | address base for func_01ff8000 arg0 |
| ctx+0x68 | r1_base | int | added into r1v |
| ctx+0x74 | t ptr | char* | reloaded 2-3x for sub-fields 0x20/0x24/0x28/0x2c/0x30/0x34/0x38/0x3c/0x19 |
| ctx+0x78 | base78 | char* | source cell array base |
| ctx+0x84 | base84 | char* | dest page base |
| ctx+0xb0 | flags | unsigned short | bit 0x2 set in flag path |
| ctx+0xb8 | count | unsigned short | loop bound, also multiplied into r1v |
| e+0xc | f0c | int | store aligned+r6v / overwrite on flag |
| e+0x12 | f12 | unsigned short | compare vs q; store qlo |

## Closest matched example
**Path:** src/main/func_0200bca8.c
**Why similar:** Both are class-D stack-heavy functions with multiple aliasing reloads and a loop over an indexed array, with a size-computation and a dependent call chain.
**Key lesson:** For class-D candidates with 7 stack temps, declaration order directly controls `sp+N` slot assignment — declare variables in the exact asm order they first appear on the stack (q, aligned, ptr60, qp1, qlo, neg42, dst[], qx512) to lock sp offsets. Multiple reloads of `ctx+0x74` are intentional aliasing-safety; do not hoist to a single local.

## Try this
1. Ensure declaration order exactly matches the slot-assignment order observed in asm: `base78, n, q, aligned, ptr60, qp1, qlo, neg42, dst[2], qx512, base84, e, i` — this is the load-bearing layout for sp#0..#18.
2. If `dst[i]` indexing generates wrong offsets, verify that `dst[2]` is at sp+0x14/+0x18 by checking the stmdb frame size; if mwcc pads, try reducing the number of top-level declared temps (merge `neg42 = ~0x29` into the compare expression directly).
3. Park as permuter-class if stack layout or f74 reload count diverges; the aliasing reload pattern is compiler-scheduling dependent and cannot be forced with source reshaping alone.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x02040811` | MMIO/const |
| `_LIT1` | `0x04000006` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_01ff8000` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

