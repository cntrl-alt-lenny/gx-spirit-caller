# 021b2b40 — func_ov014_021b2b40 (ov014, class C, 220B)

## Recipe + Risk
**Recipe:** CLASS C: stride-0x14 loop; field at +4; ldrh/strh pair
**Risk:** stride-0x14. field at +4: ldrh at base+i*0x14+4.

## Prep draft

No prep file exists for this address (`docs/research/c-match-prep/021b2b40.c` absent). Dossier is built from recipe/risk notes and ov014 context.

The function is 220B, class C, in ov014. It loops over an array with stride 0x14, accessing a `u16` field at offset +4 of each element via `ldrh`.

```c
/* func_ov014_021b2b40 (ov014, class C, 220B): stride-0x14 array loop.
 * Each element is 0x14 bytes wide; a u16 field at element+4 is read/written.
 * UNVERIFIED — no m2c prep file; drafted from recipe notes only. */

/* Stride-0x14 element: */
typedef struct {
    unsigned int  f0;   /* +0x00 word */
    unsigned short f4;  /* +0x04 u16 — the ldrh target */
    unsigned char  f6[0xe]; /* +0x06 pad to stride 0x14 */
} Elem2b40;            /* sizeof = 0x14 */

void func_ov014_021b2b40(Elem2b40 *arr, int n, unsigned short val)
{
    int i;
    for (i = 0; i < n; i++) {
        arr[i].f4 = val;   /* ldrh/strh at base + i*0x14 + 4 */
    }
}
```

Note: the recipe says "ldrh/strh pair" implying both a load and a store at +4. If the loop reads the existing value (e.g., ORs into it), the body becomes `arr[i].f4 |= val` or similar. Confirm via objdiff.

## Struct context

The stride-0x14 element matches the `DataArrayStrides.md` entry for 0x14-byte stride arrays (confirmed in `EntityStruct0x284` sub-objects at +0x000 stride 0x58 is different; this 0x14 stride is a UI element table).

| field | offset | type | purpose |
|-------|--------|------|---------|
| f0 | +0x00 | u32 | word field (type/id?) |
| f4 | +0x04 | u16 | ldrh/strh target (index or flags) |
| _pad | +0x06 | u8[0xe] | pad to 0x14 |

## Closest matched example
**Path:** `src/overlay015/func_ov015_021b28f0.c`
**Why similar:** Short-element loop with `ldrh` post-increment read; count-driven loop body.
**Key lesson from that file:** Use a plain `for (i = 0; i < n; i++)` index form, not a pointer-increment form, when the asm uses `mla` to compute `base + i * stride`. If the asm instead increments a pointer by 0x14 each iteration, switch to `Elem2b40 *p = arr; for (...) { p->f4 = val; p++; }`.

## Try this
1. Confirm whether the asm uses `mla r?, r_i, #0x14, r_base` (index form) or `add r_p, r_p, #0x14` (pointer-increment form). Use the index form in C if mla is present, pointer-increment if add is present.
2. If the `ldrh/strh` pair means read-modify-write (not just store), check the intervening instructions between `ldrh` and `strh` in the asm. If it is `orr` or `and`, add the corresponding bitwise expression in the loop body.
3. If loop count `n` mismatches (extra/missing iteration), check whether the loop back-edge is `blt` (unsigned: `unsigned int i`) or `bge`-negated (signed: `int i`). Escalate to permuter only if strength-reduction fires (orig uses 3 pointer IVs).
