# 021d0ea8 — func_ov011_021d0ea8 (ov011, class D, 220B)

## Recipe + Risk
**Recipe:** D: mla stride-0x14; three stores per iter; reload loop bound
**Risk:** stride-0x14: mla i*0x14+base. three stores: f0, f4, f8 in order. reload bound.

## Prep draft

```c
/* func_ov011_021d0ea8 (ov011, cls D, 220B): counted loop over stride-0x14
 * array (actor table); stores three fields per element in order f0, f4, f8.
 * Loop bound is reloaded from memory each iteration (not cached in reg).
 *
 * Sits inside func_ov011_021d0e88.c range (021d0e88–021d0ed0) at offset
 * 0x20. The matched file 021d0e88.c is a search loop over stride-0x28;
 * this candidate is a DIFFERENT function — stride-0x14, store-only, class D.
 * Prep file not yet split.
 */

extern char data_ov011_021d41a0[];  /* actor table: stride 0x14 */
extern char data_ov011_021d4000[];  /* source of loop bound */

/* Proto: inferred from recipe — stores val into three fields of each slot */
void func_ov011_021d0ea8(int val) {
    int i;
    for (i = 0; i < *(int *)(data_ov011_021d4000 + 0x2a0); i++) {
        /* mla: base + i * 0x14 */
        char *slot = data_ov011_021d41a0 + i * 0x14;
        *(int *)(slot + 0x0) = val;   /* store f0 */
        *(int *)(slot + 0x4) = val;   /* store f4 */
        *(int *)(slot + 0x8) = val;   /* store f8 */
        /* loop bound reloaded here: i < *(int *)(data_ov011_021d4000 + 0x2a0) */
    }
}
```

## Struct context

The actor table `data_ov011_021d41a0` uses stride 0x14 (20B per element) per
ov011_core.h. The loop bound is reloaded from `data_ov011_021d4000` (primary
view state) each iteration, consistent with the "reload bound" risk note.

| table | stride | confirmed |
|-------|--------|-----------|
| `data_ov011_021d41a0` | 0x14 | ov011_core.h |
| `data_ov011_021d4000` | — | primary view-state base |

| field in slot | offset | type | purpose |
|---------------|--------|------|---------|
| f0 | 0x0 | int | first field stored per iter |
| f4 | 0x4 | int | second field stored per iter |
| f8 | 0x8 | int | third field stored per iter |

The "reload loop bound" risk: the `for` condition `i < *(int *)(...+0x2a0)` must
NOT be hoisted into a local variable before the loop — write the load inline in
the `for` condition so mwcc re-reads it each iteration.

## Closest matched example
**Path:** src/overlay011/func_ov011_021d0e88.c
**Why similar:** Both use a counted loop over an ov011 data array with an in-loop access at stride; search variant (0xe88) and this store variant (0xea8) are siblings sharing the same loop idiom.
**Key lesson from that file:** The stride multiplication emits as `mov rN, #stride; mla` — write `data_base + i * STRIDE` (not a pre-computed pointer advanced by `+= STRIDE`) so mwcc emits the `mla` form. The `i < bound_reload` in the for-condition must come from a fresh memory read, not a local copy; mwcc only reloads when the loop-condition expression itself dereferences the pointer.

## Try this
1. Write the loop as `for (i = 0; i < *(int *)(data_ov011_021d4000 + 0x2a0); i++)` (bound in the condition, not in a local). Write the three stores using `data_ov011_021d41a0 + i * 0x14` each time so mwcc emits three separate `str` instructions off the mla result.
2. In objdiff, if the mla disappears (separate `mul` + `add`), the stride constant is wrong — check the real multiplier from the assembly (must be 0x14 = 20). If three stores appear but in wrong field order, reorder `f0`, `f4`, `f8` assignment sequence in the C source.
3. Class D: if the three stores emit correctly but register assignment differs, try declaring a named `char *slot` local inside the loop vs computing inline. Escalate to permuter if reg alloc on `i` and `slot` diverges.
