# 021c8e98 — func_ov006_021c8e98 (ov006, class C, 264B)

## Recipe + Risk
**Recipe:** ternary-selected base+mla(20B); e->h reuse r4; cmd stores in order
**Risk:** mla: base+i*20; ternary: e->h = cond ? a : b (movne/moveq). cmd stores: ldrh/strh in asm order.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021c8e98 (ov006, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     ternary-selected base+mla(20B); e->h reuse r4; cmd stores in order +8,+0,+20,+12,+16
 *   risk:       cmd field store order is mwcc-scheduled, not source-pinned (021c74e0 template proves it reorders): the +8/+0/+20 emit order may not follow source. reshape-able if it drifts (reorder statements / split to volatile-like temps).
 *   confidence: med
 */
/* func_ov006_021c8e98: re-arm a deferred task into obj+0x38, build a fixed OAM
   command on the stack (handle, attrs, pos = obj[0x3c]/[0x40] << 5), submit it,
   then bind/free the handle. Active table row picks the bank id. Returns 1. */
extern void Task_InvokeLocked(int h);
extern int  Task_PostLocked(int a, int b, int c);
extern struct { int w0; unsigned chan : 3; } data_02104f4c;
struct E8e98 { int pad0; unsigned char flag4; char pad[15]; };  /* exactly 20-byte rows; flag4@+4 */
extern struct E8e98 data_ov006_021ce650[];
extern struct E8e98 data_ov006_021ce8a8[];
extern char data_ov006_021cebc8[];
extern char data_ov006_021cebe0[];
extern int  func_02006c0c(void *p, int b, int c);
extern void func_02006e1c(int h);
extern void func_0201d47c(void *cmd);
extern int  func_0201d6f8(int h);
extern void func_0201e5b8(void *cmd);
extern void func_020944a4(int h, int task, int code);
int func_ov006_021c8e98(char *obj) {
    struct E8e98 *e = (data_02104f4c.chan == 0 ? data_ov006_021ce650 : data_ov006_021ce8a8)
                      + *(int *)(obj + 0x74);
    int h;
    char cmd[40];
    if (*(int *)(obj + 0x38)) {
        Task_InvokeLocked(*(int *)(obj + 0x38));
        *(int *)(obj + 0x38) = 0;
    }
    *(int *)(obj + 0x38) = Task_PostLocked(0x480, 4, 0);
    if (e->flag4 == 0)
        h = func_02006c0c(data_ov006_021cebc8, 4, 0);
    else
        h = func_02006c0c(data_ov006_021cebe0, 4, 0);
    func_0201d47c(cmd);
    *(int *)(cmd + 8) = -1;
    *(int *)(cmd + 0) = h;
    *(unsigned short *)(cmd + 20) = (*(unsigned short *)(cmd + 20) & ~15) | 6;
    *(int *)(cmd + 12) = *(int *)(obj + 0x3c) << 5;
    *(short *)(cmd + 16) = *(int *)(obj + 0x40) << 5;
    func_0201e5b8(cmd);
    func_0201d6f8(h);
    func_020944a4(h, *(int *)(obj + 0x38), 0x480);
    func_02006e1c(h);
    return 1;
}
```

## Struct context
`struct E8e98` is a 20-byte row struct (stride for the MLA). The `data_02104f4c` global is the same `GlobalData02104e6c` singleton (3-bit `chan` field at word+0x04, HIGH confidence from types/).

| field | offset in E8e98 | type | purpose |
|-------|----------------|------|---------|
| `pad0` | +0x0 | int | padding |
| `flag4` | +0x4 | unsigned char | selects which data pool to use for handle allocation |
| `pad[15]` | +0x5..+0x13 | char[15] | pad to 20-byte row stride |

`obj` raw offsets used:

| offset | type | purpose |
|--------|------|---------|
| +0x38 | int | deferred task handle |
| +0x3c | int | X position (shifted left 5 for cmd+12) |
| +0x40 | int | Y position (shifted left 5 for cmd+16) |
| +0x74 | int | row index into the E8e98 table |

`cmd[40]` stack buffer stores at offsets: +8 (-1), +0 (h), +20 (RMW attr), +12 (x<<5), +16 (y<<5) — the prep lists them in asm order.

## Closest matched example
**Path:** src/overlay006/func_ov006_021c74e0.c
**Why similar:** same `func_0201d47c(cmd)` + `func_0201e5b8(cmd)` OAM command-record family; same `cmd[40]` stack buffer; same store-order sensitivity for cmd fields.
**Key lesson from that file:** cmd field stores must appear in exactly the asm order: `cmd+0 = h`, then `cmd+20 RMW`, then `cmd+8 = -1`, then `cmd+12`, `cmd+16`. The prep here already lists them as `+8, +0, +20, +12, +16` — which is the asm order, NOT ascending address order. Do NOT sort these by offset. The `data_02104f4c.chan` ternary produces a `movne/moveq` pair and must be written as a single conditional expression, not an if/else.

## Try this
1. Build as-is. The cmd store sequence is already written in asm order (+8 first, then +0, +20, +12, +16). If objdiff shows it reordered, move the `*(int *)(cmd + 8) = -1` assignment to be after `h` is set (try swapping the +8 and +0 lines).
2. If the ternary `(data_02104f4c.chan == 0 ? data_ov006_021ce650 : data_ov006_021ce8a8) + index` doesn't emit `movne/moveq`, try breaking the ternary into an if/else that both assign `e` — some ternary shapes don't fold when the branches are global addresses. If `e->flag4 == 0` doesn't produce `cmp r, #0 / movne/moveq`, try `if (e->flag4)` (no explicit `== 0`).
3. If `h` store order (func_0201d6f8 vs func_020944a4) is wrong, reorder those two calls. Escalate to permuter if multiple call-order adjustments are needed.
