# 021b7218 — func_ov010_021b7218 (ov010, class C, 212B)

## Recipe + Risk

**Recipe:** CLASS C: loop with accumulate+limit; movge/movlt clamp; bind base early
**Risk:** accumulate: sum += f[i]; limit: clamp sum. movge/movlt: signed clamp. bind base before loop.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021b7218 (ov010, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     explicit divmod helper (var divisor); /2 signed plain; bind f8; (hi<<8)|lo pack; subs-mi if-clamp
 *   risk:       reshape-able: f8 must bind into both subtract and divisor (orig single ldr r1); if mwcc reloads B+8 a 2nd ldr diverges -- keep the f8 local.
 *   confidence: med
 */
/* func_ov010_021b7218: subtract-clamp a counter, then drive sub-engine BLDCNT/BLDALPHA
 * (0x04001040/0x44) from a divmod-by-helper + /2 pack; branch tail clears B->f8.
 *
 *   subs r0,[A+0x18],arg ; str ; if(<0) B->fc=0
 *   q = func_020b3870((B->f8 - B->fc)*0x97, B->f8); h = q/2 (signed)
 *   *0x04001040 = 0x4e4 ; *0x04001044 = ((0x6f-h)<<8 &0xff00)|((h+0x6f)&0xff)
 *   if (B->fc) return 0; else 2nd MMIO pair + B->f8=0; return 1
 */

extern int func_020b3870(int a, int b);          /* signed div, quotient in r0 */
extern char data_ov010_021b9884[];               /* A: field +0x18 */
extern char data_ov010_021b9890[];               /* B: fields +0x8, +0xc */

int func_ov010_021b7218(int delta) {
    int v;
    int f8, fc, q, h;
    v = *(int *)(data_ov010_021b9884 + 0x18) - delta;
    *(int *)(data_ov010_021b9884 + 0x18) = v;
    if (v < 0) *(int *)(data_ov010_021b9890 + 0xc) = 0;
    f8 = *(int *)(data_ov010_021b9890 + 0x8);
    fc = *(int *)(data_ov010_021b9890 + 0xc);
    q = func_020b3870((f8 - fc) * 0x97, f8);
    h = q / 2;
    *(volatile unsigned short *)0x04001040 = 0x4e4;
    *(volatile unsigned short *)0x04001044 =
        (unsigned short)((((0x6f - h) << 8) & 0xff00) | ((h + 0x6f) & 0xff));
    if (*(int *)(data_ov010_021b9890 + 0xc) != 0) return 0;
    *(volatile unsigned short *)0x04001040 = 0x4e4;
    *(volatile unsigned short *)0x04001044 = 0x24bb;
    *(int *)(data_ov010_021b9890 + 0x8) = 0;
    return 1;
}
```

## Struct context

Two global char arrays used as structs (same as sibling 021b718c):
- `data_ov010_021b9884` (A): `+0x18` = countdown timer (int)
- `data_ov010_021b9890` (B): `+0x8` = f8 (blend range); `+0xc` = fc (current blend factor)

MMIO: `0x04001040` = BLDCNT halfword; `0x04001044` = BLDALPHA halfword.
The `*0x97 = 151 decimal` is a literal multiply coefficient (NOT a magic-divide constant).
`0x24bb` is a fixed packed halfword written to BLDALPHA on the reset path.

| field | offset | type | purpose |
|-------|--------|------|---------|
| A +0x18 | 0x18 | int | countdown timer |
| B +0x08 | 0x08 | int | f8: blend range / divisor |
| B +0x0c | 0x0c | int | fc: current blend factor |

## Closest matched example

**Path:** src/overlay010/func_ov010_021b718c.c (near-clone in this same batch)
**Why similar:** near-identical body — same A/B globals, same MMIO pair, same subs-flag reuse, same func_020b3870 pattern. The key difference: 021b7218 uses `(f8 - fc) * 0x97` as the numerator and `f8` as the divisor (both bound from B), whereas 021b718c uses `B[3] * 0x97` with `B[2]` as divisor (no subtraction step).
**Key lesson from that file:** bind `f8 = *(int *)(data_ov010_021b9890 + 0x8);` as a named local BEFORE the `fc` load — the orig uses a single `ldr r1,[rB,#8]` that serves both as the subtract operand and the divisor arg; if mwcc reloads B+8 a second time for the divisor, the extra `ldr` breaks the match. Keeping `f8` in a local forces one load.

## Try this

1. Ensure `f8` is declared and loaded (`f8 = *(int *)(data_ov010_021b9890 + 0x8);`) before `fc`, and that `f8` appears in both the `(f8 - fc) * 0x97` numerator AND the `func_020b3870(..., f8)` divisor argument — this forces mwcc to reuse the register rather than re-emitting a pool load.
2. In objdiff: if you see two `ldr rX,[rB,#8]` (B+0x8 loaded twice), mwcc is reloading instead of reusing; bind `f8` earlier (or check that the variable is not accidentally reassigned between the two uses).
3. Fallback (near-clone note): compare register allocation at the `push` instruction with 021b718c. If register count differs (e.g. 5 vs 4 callee-saved), the `h = q / 2` divide may need `/2` expressed as `>> 1` (arithmetic shift) if mwcc doesn't constant-fold it — try `h = q >> 1` as an alternative; escalate to permuter only after confirming the f8-binding reshape.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov010_021b9884` | raw |
| `_LIT1` | `data_ov010_021b9890` | raw |
| `_LIT2` | `0x000004e4` | MMIO/const |
| `_LIT3` | `0x04001040` | MMIO/const |
| `_LIT4` | `0x000024bb` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020b3870` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

