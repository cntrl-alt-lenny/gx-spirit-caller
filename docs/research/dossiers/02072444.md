# 02072444 — func_02072444 (main, class D, 132B)

## Recipe + Risk

**Recipe:** D divmod + or-operand-order: signed /8 magic; hw = b1 | (b2<<8); branch-table on tag
**Risk:** the /8 must emit the signed asr+lsr#30 magic — needs the masked byte typed signed int (an unsigned >>3 gives plain lsr#3); also tag dispatch order/branch layout may differ; mwcc may reorder cmp chain -> reshape-able (force signed int divide; keep case order 0,1,2)

## Prep draft

```c
void func_02072444(unsigned char *p, unsigned char *s) {
    int n, rem;
    unsigned char *q;
    *(unsigned short *)(s + 0x2e) = 0x218;
    n = (int)((unsigned)(p[0xc] & 0xf0)) / 8 - 0x14;
    q = p + 0x14;
    rem = n - 1;
    if (n == 0) return;
    for (;;) {
        unsigned int tag = *q++;
        if (tag == 0) return;
        if (tag != 1) {
            if (tag == 2) {
                unsigned int lo = q[1];
                unsigned int hi = q[2];
                q += 3;
                rem -= 3;
                *(unsigned short *)(s + 0x2e) = (unsigned short)(lo | (hi << 8));
            } else {
                unsigned int adv = q[0] - 1;
                rem -= adv;
                q += adv;
            }
        }
        if (rem == 0) return;
        rem -= 1;
    }
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `p[0xc]` | TLV byte at 0xc | `u8` | masked `& 0xf0` then signed-divided by 8 to get count n |
| `p[0x14]` | TLV body start | `u8*` | cursor q starts here |
| `s[0x2e]` | output halfword | `u16` | written with default 0x218 then optionally with tag-2 payload |
| `q[1]`, `q[2]` | TLV tag-2 payload | `u8` | lo byte then hi byte; recombined as `lo | (hi<<8)` |

## Closest matched example

**Path:** src/main/func_02073fac.c
**Key lesson:** For the signed-divide idiom, mwcc emits `(x >> 3)` as `asr #3` only when the dividend is a `signed int`. The masked byte `p[0xc] & 0xf0` extracted as `(unsigned)` and then cast to `(int)` for the divide is the correct pattern: `int n = (int)((unsigned)(p[0xc] & 0xf0)) / 8 - 0x14;`. Using `unsigned >> 3` gives `lsr#3` which does not match the orig `asr+lsr#30` pair. The `or-operand-order` for tag-2 is `lo | (hi << 8)` not `(hi << 8) | lo` — left operand must be `lo`.

## Try this

1. Declare `n` as `int` and compute via `(int)((unsigned)(p[0xc] & 0xf0)) / 8 - 0x14` — this forces mwcc to emit the signed `asr#3; add r?,r?,r?,lsr#30` magic-divide pair for `/8`. Any unsigned path gives `lsr#3` only and diverges.
2. For the tag-2 halfword: write `(unsigned short)(lo | (hi << 8))` with `lo` as the left (non-shifted) operand — the `or` operand order must match source order; swap to `(hi << 8) | lo` only if objdiff shows the OR operands inverted.
3. Keep the tag compare chain as `if(tag==0) return; if(tag!=1) { if(tag==2) ... else ... }` — this maps to a cmp-chain not a jump table; adding cases or reordering diverges.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

