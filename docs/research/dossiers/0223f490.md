# 0223f490 — func_ov002_0223f490 (ov002, class F retriage, 356B)

## Re-triage
**Original verdict:** F — previously intractable
**Upgrade reason:** DuelQueueState+0x5B8 (sub-state dispatch), PerPlayerRowTable stride 0x868, and
Ov002Slot id_lo13 bitfield are all documented. The reloc evidence at 0x0223f490-0x0223f5b3 shows a
repeating pattern of paired calls: (func_0202b100 / func_0202b7ec, func_0229ce5c) — this is the
standard DuelQueueState-gated slot-update loop calling a main-arm9 action per slot, then an ov002
finisher. The 356-byte size and 7 such pairs (0x0223f498..0x0223f518) fit a slot-loop body
dispatching one action per Ov002Slot that passes a predicate.
**Confidence:** high
**Structs needed:** DuelQueueState (data_ov002_022ce288 +0x5B8), PerPlayerRowTable (base 0x022CF16C, stride 0x868, +0x030 slots), Ov002Slot (id_lo13 lsl#19/lsr#19)

## Assembly
```asm
; func_ov002_0223f490 — NOT YET EXTRACTED
; This function exists in the binary between func_ov002_0223fa20.s and the reloc block at 0x0223f490.
; It has not been disassembled into a .s file yet (absent from src/overlay002/ and symbols.txt).
; Size: 356 bytes (0x164). Confirmed via retriage doc + reloc coverage (lines 12908-12928).
;
; Key relocs at this address range:
;   0x0223f490  (function start — first instruction at this address)
;   0x0223f498  arm_call -> 0x0229ce5c  (func_ov002_0229ce5c — slot action finisher)
;   0x0223f4a8  arm_call -> 0x0202b100  (main-arm9 slot action)
;   0x0223f4b8..0x0223f518  repeating: arm_call 0x0202b100 / 0x0202b7ec + 0x0229ce5c pairs
;   0x0223f58c  arm_call -> 0x021ae400  (func_021ae400 — audio/event trigger)
;   0x0223f5dc  arm_call -> 0x022577dc  (func_ov002_022577dc — slot scan predicate)
;   0x0223f5f4  arm_call -> 0x0223e21c  (func_ov002_0223e21c)
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5B8 | sub_state | int | dispatch ladder (addls pc or switch) |
| PerPlayerRowTable | base + (player&1)*0x868 | row base | ptr | standard mla stride |
| PerPlayerRowTable | +0x030 | slots[0..4] | Ov002Slot[] | iterated for slot-action dispatch |
| Ov002Slot | +0x008 | id_lo13 | :13 bitfield | lsl#19/lsr#19 — occupancy gate |
| func_0202b100 | — | main slot action A | fn | called per occupied slot (type A) |
| func_0202b7ec | — | main slot action B | fn | called per occupied slot (type B) |
| func_0229ce5c | — | slot finisher | fn | called after each slot action |

## Recipe sketch

Based on the repeating paired-call reloc pattern (7 pairs across 356 bytes):

```c
int func_ov002_0223f490(CardObj *card) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    int sub_state = dqs->sub_state;  /* +0x5B8 */

    /* 5-arm dispatch or sequential slot loop */
    switch (sub_state) {
    default: return 1;
    case 0:
        if (!func_ov002_022577dc(card)) return 1;
        /* fall through to slot actions */
        /* Slot 0 action A */
        func_0202b100(card, 0);
        func_ov002_0229ce5c(card, 0);
        /* Slot 1 action B */
        func_0202b7ec(card, 1);
        func_ov002_0229ce5c(card, 1);
        /* ... up to slot 4 or end-of-occupied */
        dqs->sub_state++;
        return 0;
    case 1:
        func_ov002_0223e21c(card);
        dqs->sub_state++;
        return 0;
    case 2:
        func_021ae400(card->player, 0);
        dqs->sub_state++;
        return 0;
    case 3: return 1;
    }
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_0223fdb4.s
**Why similar:** Adjacent function (0x0223f5b4 is called by neighbors); same DuelQueueState+0x5B8
switch, same PerPlayerRowTable stride-0x868 slot loop, same `func_ov002_022577dc` predicate call.
**Key lesson:** The repeating (main-action + 0229ce5c finisher) pairs in the reloc block indicate that
each slot in the loop body calls a main-arm9 action and then an ov002 completion function — declare
these as two separate bl calls inside the slot loop, not as a helper. The func_021ae400 call at
0x0223f58c (audio/event trigger) is called once at the end after the loop, outside the switch.

## Try this
1. Declare `int sub_state = dqs->sub_state` at +0x5B8; case 0 calls `func_ov002_022577dc(card)` as guard, then iterates slots via `data_ov002_022cf16c[(player&1)*0x868].slots[s].id_lo13 != 0`.
2. If objdiff shows the paired-call sequence off, try calling func_0202b100 and func_0202b7ec alternating per slot — the reloc alternation suggests two different action codes depending on slot parity or type.
3. If this is a PPT scan with per-slot dispatch: escalate and confirm which of func_0202b100 vs func_0202b7ec is gated by slot->id_lo13 type bits.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

