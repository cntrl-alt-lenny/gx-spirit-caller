# 0200dd58 — func_0200dd58 (main, class D, 108B)

## Recipe + Risk
**Recipe:** :4 bitfield@bit21 increment->lsl7;lsr28; blx callback; Fill32(0,obj,0x284) arg-order
**Risk:** reshape-able/struct-guessed: bitfield increment must re-extract via lsl#7;lsr#28 not `and`; mwcc may cache `count` across the cmp instead of reloading [0x60] -> bind-vs-reload flip. cb@0x58 inferred

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0200dd58 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     :4 bitfield@bit21 increment->lsl7;lsr28; blx callback; Fill32(0,obj,0x284) arg-order
 *   risk:       reshape-able/struct-guessed: bitfield increment must re-extract via lsl#7;lsr#28 not `and`; mwcc may cache `count` across the cmp instead of reloading [0x60] -> bind-vs-reload flip. cb@0x58 inferred
 *   confidence: med
 */
/* func_0200dd58 @ 0x0200dd58 (main, class D)
 * Increment a 4-bit bitfield at bit 21 of field 0x60 (mask 0x01E00000),
 * then if the new value > 2 run a teardown: two helper calls, an optional
 * indirect callback (fn at 0x58 with arg at 0x5c), then Fill32(0, obj, 0x284).
 *
 *   bf = ((*0x60) >> 21) & 0xF;  bf = bf + 1;  write back;
 *   if (bf <= 2) return;
 *   func_020109a8();
 *   func_02010a98(obj);
 *   if (obj->cb) obj->cb(obj->cb_arg);     // ldr r0,[r4,#0x5c]; blx r1
 *   Fill32(0, obj, 0x284);
 *
 * The (x<<7)>>28 extract + (y<<28)>>7 insert pair => a :4 bitfield at bit 21.
 * Use a bitfield struct so mwcc emits lsl#7;lsr#28 (a plain mask gives `and`).
 */

typedef void (*Cb0200dd58)(int arg);

struct Obj0200dd58 {
    char _pad0[0x58];
    Cb0200dd58 cb;          /* 0x58 */
    int        cb_arg;      /* 0x5c */
    /* 0x60 packed: bits [20:0] low, [24:21] = 4-bit counter, [31:25] high */
    unsigned int low   : 21;   /* 0x60, bits 0..20  */
    unsigned int count : 4;    /* 0x60, bits 21..24 */
    unsigned int high  : 7;    /* 0x60, bits 25..31 */
};

extern void Fill32(int value, void *dest, int size);
extern void func_020109a8(void);
extern void func_02010a98(struct Obj0200dd58 *obj);

void func_0200dd58(struct Obj0200dd58 *obj)
{
    obj->count = obj->count + 1;
    if (obj->count <= 2) {
        return;
    }
    func_020109a8();
    func_02010a98(obj);
    if (obj->cb != 0) {
        obj->cb(obj->cb_arg);
    }
    Fill32(0, obj, 0x284);
}
```

## Struct context
This function operates on a struct-guessed object. The critical bitfield is a 4-bit field starting at bit 21 of the word at offset 0x60. The struct layout must declare this as a `:4` bitfield member named `count` with a preceding 21-bit `low` field to position it correctly (MWCC must see `unsigned int count : 4` as member 2 in the 0x60 word, after `unsigned int low : 21`).

| offset | field | type | use |
|--------|-------|------|-----|
| +0x58 | cb | `void (*)(int)` | callback function pointer; called if non-null |
| +0x5c | cb_arg | `int` | argument passed to cb |
| +0x60 bits 20:0 | low | `unsigned int : 21` | padding/other bits (not touched) |
| +0x60 bits 24:21 | count | `unsigned int : 4` | 4-bit counter that is incremented; teardown triggers when > 2 |
| +0x60 bits 31:25 | high | `unsigned int : 7` | upper bits (not touched) |

Callee `func_020109a8` maps to `Duel_EvalCardAlt` and `func_02010a98` maps to `Duel_AdvanceSlotIndex` in the naming plan.

## Closest matched example
**Path:** src/main/func_02018a38.c
**Why similar:** Both use a bitfield insert/extract at a non-standard bit position within a packed word, where MWCC must emit `lsl#N;lsr#M` rather than a plain `and` mask.
**Key lesson:** A plain `(field >> 21) & 0xF` extract compiles to `lsr#21; and #0xF` — the orig uses `lsl#7;lsr#28` (shift-pair from a bitfield). Declare the field as `unsigned int count : 4` in a packed struct after `unsigned int low : 21`; MWCC then emits the `lsl#7;lsr#28` extract and the corresponding `lsl#21` insert on write-back. Do NOT use a manual mask expression.

## Try this
1. Build with the bitfield struct as-is. In objdiff check that the extract of `count` is `lsl #7; lsr #28` (not `lsr #21; and #0xf`). If you see `and`, the bitfield declaration is wrong — verify that `low : 21` appears first in the struct word at offset 0x60.
2. If `count` is extracted correctly but the compare `<= 2` diverges: the risk warns MWCC may cache the incremented value across the cmp instead of reloading `[r4,#0x60]`. If objdiff shows an extra `ldr` after the store, add a `volatile` qualifier to the word at 0x60 or split the increment into `unsigned int tmp = obj->count + 1; obj->count = tmp;` and compare `tmp`.
3. Park as `.s` if a register-scheduling wall remains after fixing the bitfield and the bind-vs-reload — this is reshape-able, not permuter-class; but the struct-guessed cb@0x58 must be confirmed before parking.
