# 0201a5c0 — func_0201a5c0 (main, class C, 52B)

## Recipe + Risk

**Recipe:** decl acc-first then i; orrge=if(>=) acc|=(1<<i); arg is i+1, counter i held
**Risk:** permuter-class: orig hoists const 1 into a dedicated reg r4 (`mov r4,#1` outside loop) so `1<<i` is `r4,lsl r5`; mwcc may rematerialise the 1 inline as `mov rX,#1;lsl` each iter — a reg-alloc/hoist coin-flip a C reshape can't force.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0201a5c0 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     decl acc-first then i; orrge=if(>=) acc|=(1<<i); arg is i+1, counter i held
 *   risk:       permuter-class: orig hoists const 1 into a dedicated reg r4 (`mov r4,#1` outside loop) so `1<<i` is `r4,lsl r5`; mwcc may rematerialise the 1 inline as `mov rX,#1;lsl` each iter — a reg-alloc/hoist coin-flip a C reshape can't force.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0201a5c0 (main, class C) — UNVERIFIED build-free draft.
 * Counted loop accumulating a bitmask. r6=acc(0), r5=i(0), r4=const 1.
 *   for (i=0; i<0x1a; i++) if (func_020097d4(i+1) >= 0x50) acc |= 1u << i;
 *   return acc;
 * Decl order matches reg alloc: acc(r6) then i(r5) then the constant 1(r4).
 * orrge => write as if(>=) acc|=... ; (1<<i) uses the standalone const reg r4 shifted by r5. */
extern int func_020097d4(int);

int func_0201a5c0(void) {
    int acc = 0;
    int i;
    for (i = 0; i < 0x1a; i++) {
        if (func_020097d4(i + 1) >= 0x50)
            acc |= 1 << i;
    }
    return acc;
}
```

## Struct context

This function does not access SystemWork or any struct fields directly. It calls `func_020097d4(i+1)` for `i` in 0..25 (0x1a iterations), accumulating a 26-bit bitmask in `acc`.

| extern | role |
|--------|------|
| `func_020097d4(int)` | per-slot predicate returning a value compared against `0x50`; confirmed matched at `src/main/func_020097d4.c` |

## Closest matched example

**Path:** src/main/func_0201a594.c
**Why similar:** near-identical loop structure — `for (i=0; i<0x1a; i++) acc |= func_...(i+1);` — same 26-iteration counted loop accumulating an OR result.
**Key lesson:** In `func_0201a594`, the declaration order `acc` before `i` matters for register allocation (acc → r0-shadow callee-saved, i → next). The key difference here is the `if (>= 0x50)` guard: the `orrge` (`acc |= ...` written as `if(>=)`) means mwcc emits a conditional OR, not an unconditional one. The `1 << i` shift form requires the compiler to hoist the constant `1` into a dedicated register `r4` outside the loop (`mov r4,#1` before the loop body) — the matched sibling `func_0201a594` has no such hoist because its OR is unconditional, so the const-hoist pattern is specific to the conditional-`|=` guard here.

## Try this

1. Park as permuter-class; the constant-1 hoist (`mov r4,#1` outside the loop to produce `orr acc,acc,r4,lsl r5` inside) is a register-allocation/hoist coin-flip that C reshape cannot force. mwcc may instead emit `mov rX,#1; lsl rX,rX,r5; orr acc,acc,rX` inside the loop body on each iteration (no hoist).
2. Before parking, try one reshape: add a named `const int one = 1;` variable declared before `acc` and `i` — declare in order `one`, `acc`, `i` — and write `acc |= one << i;`. The named variable may encourage the compiler to allocate it to a callee-saved register that survives the function call, mimicking the r4 hoist. If this produces the `mov r4,#1; ... orr acc,acc,r4,lsl r5` form, the hoist is recovered.
3. If neither reshape matches, park as permuter-class; the bit-guard `orrge` (`&= 1<<i`) is a register-allocation coin-flip that cannot be forced without the macOS permuter.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020097d4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

