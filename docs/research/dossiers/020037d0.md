# 020037d0 — func_020037d0 (main, class D, 240B)

## Recipe + Risk

**Recipe:** D: range jump-table (idx-8) selecting (stride,base,tag) + mla tail call
**Risk:** permuter-class: already .s-shipped. The jump-table pads odd cases to default (b .L_888), and the mla(stride*a1+base) plus 5 ordered stack stores need exact reg assignment (lr/ip/r4/r5). Switch lowering + arg scheduling is a coin-flip a reshape won't reliably fix.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020037d0 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: range jump-table (idx-8) selecting (stride,base,tag) + mla tail call
 *   risk:       permuter-class: already .s-shipped. The jump-table pads odd cases to default (b .L_888), and the mla(stride*a1+base) plus 5 ordered stack stores need exact reg assignment (lr/ip/r4/r5). Switch lowering + arg scheduling is a coin-flip a reshape won't reliably fix.
 *   confidence: low
 */
/* func_020037d0: range switch (idx-8, valid 0/2/4/6/8) selecting per-case
 * (stride, struct-field, tag), then mla index + tail call func_02001f18.
 *   sw (a_idx) { case 8: stride=0x20, base=g.f_88, tag=4; ...
 *     case 16:0x80,f_a8,8 } ; ptr = base+0x80 + stride*a1;
 *   func_02001f18(ptr, a3, *a0(stacked), a2, a4, a5, tag);
 * NOTE: already ships as src/main/func_020037d0.s (brief 207, C-34
 * candidate). Best-effort. a_idx,a4,a5 are stack params [sp+0x18..0x1c].
 * The case constants map: idx 8->(0x20,0x88,4) 10->(0x32,0x90,5)
 * 12->(0x48,0x98,6) 14->(0x62,0xa0,7) 16->(0x80,0xa8,8). mla r0=stride*a1+ptr. */

typedef struct {
    char         _pad0[0x88];
    char        *f_88;
    char        *f_8c;
    char        *f_90;
    char        *f_94;
    char        *f_98;
    char        *f_9c;
    char        *f_a0;
    char        *f_a4;
    char        *f_a8;
} state_02102c7c_t;

extern state_02102c7c_t data_02102c7c;
extern void func_02001f18(void *ptr, int a3, int s0, int a2,
                          int a4, int a5, int tag);

void func_020037d0(void *a0, int a1, int a2, int a3,
                   int a4, int a5, int a_idx) {
    char *base;
    int   stride;
    int   tag;

    switch (a_idx) {
    case 8:  stride = 0x20; base = data_02102c7c.f_88; tag = 4; break;
    case 10: stride = 0x32; base = data_02102c7c.f_90; tag = 5; break;
    case 12: stride = 0x48; base = data_02102c7c.f_98; tag = 6; break;
    case 14: stride = 0x62; base = data_02102c7c.f_a0; tag = 7; break;
    case 16: stride = 0x80; base = data_02102c7c.f_a8; tag = 8; break;
    default: return;
    }
    func_02001f18(base + 0x80 + stride * a1, a3, *(int *)a0, a2,
                  a4, a5, tag);
}
```

## Struct context

This function reads from `data_02102c7c` — the GlobalData02102c7c singleton. It accesses the pointer table at offsets `+0x88` through `+0xA8` (stride-8 pointer array, 5 used pointers: `f_88`, `f_90`, `f_98`, `f_a0`, `f_a8`). See [GlobalData02102c7c.md](../types/GlobalData02102c7c.md) for the full field table.

| offset | field | type | use |
|--------|-------|------|-----|
| +0x88 | f_88 | char* | base pointer for case 8 (stride=0x20, tag=4) |
| +0x90 | f_90 | char* | base pointer for case 10 (stride=0x32, tag=5) |
| +0x98 | f_98 | char* | base pointer for case 12 (stride=0x48, tag=6) |
| +0xa0 | f_a0 | char* | base pointer for case 14 (stride=0x62, tag=7) |
| +0xa8 | f_a8 | char* | base pointer for case 16 (stride=0x80, tag=8) |

The MedCandidateNeeds.md confirms this exact pattern: "data_02102c7c.f_88/.f_90/.f_98/.f_a0/.f_a8 pointer table (stride-8 from +0x88); jump-table switch on (idx-8)/2 selects stride+base+tag".

## Closest matched example

**Path:** src/main/func_02001c98.c
**Why similar:** Also accesses `data_02102c7c` fields in a fixed sequence and performs arithmetic on them. Different shape (switch vs. field writes), but shares the global.
**Key lesson:** This function is already documented as permuter-class in MedCandidateNeeds.md — the jump-table lowering for even-spaced cases 8/10/12/14/16 and the 7-arg tail call with `mla` pointer arithmetic require exact register assignment (lr/ip/r4/r5 for the stmdb frame). The C draft is best-effort documentation, not an expected first-build match.

## Try this

1. This function is permuter-class: already .s-shipped. Do not invest reshape iterations. The prep draft correctly documents the logic for reference only.
2. If attempting C anyway: confirm that `stride * a1` emits `mla` not `mul` — MWCC may choose `mul` then `add`; use `int ptr_offset = stride * a1 + 0x80;` as a separate local to control expression grouping.
3. Park as `.s` — permuter-class means the switch lowering + arg scheduling coin-flip cannot be reliably pinned via C reshape.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_020038bc` | `data_02102c7c` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02001f18` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

