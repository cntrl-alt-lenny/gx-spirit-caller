# 021ac35c — func_ov008_021ac35c (ov008, class C, 212B)

## Recipe + Risk

**Recipe:** bind data_021b2790 base once; bitfield insert via (a&~m)|v; store-order
**Risk:** bind global base in r4; bitfield insert: (x & ~mask) | val; store-order after insert.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021ac35c (ov008, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     bind data_021b2790 base once; bitfield insert via (a&~m)|v; store-order [0x34],[0x38],[0x2c] as asm
 *   risk:       struct-guessed: offsets/sizes of the 0x48 work area are inferred from raw sp displacements; the bic/orr field-insert order on [sp,#0x34] and [sp,#0x38] is store-order-rigid. If struct layout wrong, every strh/str displacement diverges.
 *   confidence: low
 */
/* func_ov008_021ac35c: build an OAM/affine entry with bitfield inserts,
   bind a global, two helper calls. cls C/D (bitfield insert + bind). */

extern unsigned short data_ov008_021b2790[]; /* _LIT0, [r0*0x34] guard */
extern int data_021040ac;                    /* _LIT1, [+0x34] mode */
extern int data_ov008_021b266c;              /* _LIT2 */
extern int data_ov008_021b2680;              /* _LIT3 */

extern int  func_02091554(void *dst, void *src);
extern void func_0201d47c(void *p);
extern void func_02006c0c(void *p, int a1, int a2);
extern void func_0201e5b8(void *p);
extern void func_02006e1c(int h);

typedef struct {            /* the 0x48 stack work area */
    int  handle;            /* +0x0  (sp+0x0 -> [sp,#0x20] stores handle) */
    char _pad4[0x20 - 0x4];
    int  oam0;              /* +0x20 */
    char _pad24[0x2c - 0x24];
    int  oam_c;            /* +0x2c */
    char _pad30[0x34 - 0x30];
    unsigned short attr;    /* +0x34 */
    char _pad36[0x38 - 0x36];
    int  f38;              /* +0x38 */
} Oam_021ac35c;

void func_ov008_021ac35c(int idx, int pal)
{
    Oam_021ac35c w;
    int prio;
    unsigned short a;

    if (((unsigned short *)data_ov008_021b2790)[idx * 0x34 / 2] == 0)
        return;

    if ((&data_021040ac)[0x34 / 4] == 0)
        func_02091554(&w, &data_ov008_021b266c);
    else
        func_02091554(&w, &data_ov008_021b2680);

    func_0201d47c(&w.oam0);
    func_02006c0c(&w, 4, 0);

    a = w.attr;
    prio = (pal << 4) + 0x93;
    a = (a & ~0xf) | 0x9;
    w.attr = a;
    prio = (prio + 0x200) << 7;

    w.handle = (int)&w;                 /* str r0,[sp,#0x20] ; r0 was sp+0 */
    w.f38 = (w.f38 & ~0x1c000) | 0x1c000;
    a = w.attr;
    w.oam_c = prio;
    w.attr = a | 0x10;
    func_0201e5b8(&w);
    func_02006e1c(w.handle);
}
```

## Struct context

`Oam_021ac35c` is a locally inferred stack struct. `data_ov008_021b2790` is the stride-52 (0x34) row array from `ov008_core.h`, here used as a guard: if `row[idx]` is zero, early return. `data_021040ac` is `GlobalAudioState` (confirmed HIGH in `docs/research/types/GlobalAudioState.md`); at +0x34 (offset 0x34) it is an unconfirmed mode field (word index 0x34/4 = 0xd).

| field | offset | type | purpose |
|-------|--------|------|---------|
| handle | +0x00 | int | self-pointer: `(int)&w` stored here |
| oam0 | +0x20 | int | OAM object word 0 (passed to func_0201d47c) |
| oam_c | +0x2c | int | packed priority word |
| attr | +0x34 | unsigned short | OAM attribute; bitfield inserts: `& ~0xf | 0x9`, then`| 0x10` |
| f38 | +0x38 | int | flags field; `(x & ~0x1c000) | 0x1c000` |

## Closest matched example

**Path:** src/overlay008/func_ov008_021aeeac.c
**Why similar:** Both are ov008 initialization functions that build a stack-resident work block, call `func_020211c8`-family initializers into it, then pass `&w` to a command-record sink (`func_0201e5b8` / `func_02006e1c` family confirmed in `ov008_core.h`).
**Key lesson from that file:** The base pointer (`g` / `w`) must be bound into r4 as the first callee-saved register. The store sequence for the work block fields (`oam_c`, `attr`, `f38`) must appear in asm order: `[sp,#0x38]` first, then `[sp,#0x34]`, then `[sp,#0x2c]` (as the recipe notes). If the bitfield inserts on `attr` and `f38` are reordered in C, the strh/str sequence shifts and mismatches.

## Try this

1. The store sequence `w.attr = a` (first insert), `w.f38 = ...` (bic/orr), `w.oam_c = prio`, `w.attr = a | 0x10` (second insert) must appear in that exact order. Do NOT merge the two `w.attr` writes: read `a = w.attr`, do `a = (a & ~0xf) | 0x9; w.attr = a;`, then later `a = w.attr; w.oam_c = prio; w.attr = a | 0x10;`. Collapsing the two writes causes the bic/orr to merge and the store order diverges.
2. If the guard expression `data_ov008_021b2790[idx * 0x34 / 2]` diverges (wrong stride), verify whether the stride should be 0x34 (52 bytes) with a cast to `unsigned short*` (i.e., halfword at offset 0 of each row, stride 52 bytes = 26 halfwords). The expression `idx * 0x34 / 2` gives halfword index, which is correct for the `unsigned short*` cast.
3. Fallback: if the `w.handle = (int)&w` self-pointer assignment produces wrong code (mwcc may not keep sp-relative address in a register), try `int *hp = &w.handle; *hp = (int)hp;` to force the address capture. If struct displacement mismatches persist after 2 iterations, the inferred `Oam_021ac35c` layout is wrong — cross-check against the raw asm sp offsets and adjust pad sizes.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov008_021b2790` | raw |
| `_LIT1` | `data_021040ac` | data symbol |
| `_LIT2` | `data_ov008_021b266c` | raw |
| `_LIT3` | `data_ov008_021b2680` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02091554` | main TU call |
| `bl` | `func_0201d47c` | main TU call |
| `bl` | `func_02006c0c` | main TU call |
| `bl` | `func_0201e5b8` | main TU call |
| `bl` | `Task_Invoke` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

