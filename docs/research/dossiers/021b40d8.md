# 021b40d8 — func_ov016_021b40d8 (ov016, class C, 224B)

## Recipe + Risk
**Recipe:** CLASS C: BgCfg conditional init; flag test before each field
**Risk:** BgCfg conditional: test flag before writing each field. movne pattern.

## Prep draft

Prep file exists at `docs/research/c-match-prep/021b40d8.c` BUT it describes a completely
different function: ov017's decimal-digit sprite-tile renderer (smull /10 %10, two loop
bodies). The EUR config confirms `func_ov017_021b40d8` (ov017, size=0x124=292B).

The TASK assigns this address to ov016, class C, size=224B with recipe "BgCfg conditional
init". These are incompatible with the prep file. The campaign's batch metadata takes
precedence for the dossier. The prep file content is reproduced for reference, but the
dossier is written to the task spec.

```c
/* PREP FILE (DO NOT USE — wrong overlay/function) ov017 decimal-digit renderer:
 * func_ov017_021b40d8: render decimal digits of val as 6-byte sprite tile commands.
 * This is ov017 size=0x124, NOT the ov016 BgCfg init. Reproduced verbatim: */

/*
extern int  func_02005dac(int a, int b);
extern void func_020944a4(void *src, int handle, int len);

int func_ov017_021b40d8(int a0, int val, int x, int flags, int count)
{
    unsigned char buf[8];
    int hdr;
    if (count == 0) {
        hdr = (flags & 0xff) | 0x40000000;
        do {
            int digit = val % 10;
            int ret;
            *(unsigned short *)&buf[4] = (unsigned short)((digit + 0x27c) | 0x1400);
            x -= 8;
            *(unsigned int *)&buf[0] = hdr | (((unsigned int)(x << 23)) >> 7);
            val = val / 10;
            ret = func_02005dac(2, 0);
            func_020944a4(buf, ret, 6);
        } while (val > 0);
    } else {
        hdr = (flags & 0xff) | 0x40000000;
        do {
            int digit = val % 10;
            int ret;
            *(unsigned short *)&buf[4] = (unsigned short)((digit + 0x27c) | 0x1400);
            val = val / 10;
            x -= 8;
            *(unsigned int *)&buf[0] = hdr | (((unsigned int)(x << 23)) >> 7);
            ret = func_02005dac(2, 0);
            func_020944a4(buf, ret, 6);
        } while (--count > 0);
    }
    return x;
}
*/

/* CAMPAIGN TARGET (ov016, class C, 224B): BgCfg conditional init.
 * Test a flag before writing each field — MWCC emits movne for the conditional
 * store (predicated move). The flag test produces a movne pattern per-field. */

typedef struct BgCfg {
    int            f0;
    char           gap04[0x10];
    unsigned short f14;
    unsigned short gap16;
    int            f18;
    char           gap1c[0x0C];
} BgCfg; /* sizeof = 0x28 */

extern void func_bg_init(void *cfg);

void func_ov016_021b40d8(BgCfg *cfg, int flag, int v0, unsigned short v14, int v18)
{
    if (flag) cfg->f0  = v0;
    if (flag) cfg->f14 = v14;
    if (flag) cfg->f18 = v18;
    func_bg_init(cfg);
}
```

Alternate — single flag test, all fields inside the if-block:

```c
void func_ov016_021b40d8(BgCfg *cfg, int flag, int v0, unsigned short v14, int v18)
{
    if (flag) {
        cfg->f0  = v0;
        cfg->f14 = v14;
        cfg->f18 = v18;
    }
    func_bg_init(cfg);
}
```

The recipe says "test flag before EACH field" which suggests the per-field `movne` pattern
(each conditional store uses the same flag condition but is tested independently in the asm).

## Struct context

Touches `BgCfg` (HIGH confidence, `docs/research/types/BgCfg.md`).

| field | offset | type | purpose |
|-------|--------|------|---------|
| f0 | 0x00 | s32 | BG control word |
| f14 | 0x14 | u16 | tile format flags |
| f18 | 0x18 | s32 | tile stride |

The "movne pattern" means each conditional store uses a `cmpne + strne` or `movne r, val`
sequence rather than a branch around a block of stores.

## Closest matched example
**Path:** src/overlay016/func_ov016_021b5a28.c
**Why similar:** Same overlay; builds a BgCfg-style config with non-contiguous field writes
in asm order; similar size (88B — much smaller, but the per-field-test pattern differs).
**Key lesson from that file:** Non-contiguous BgCfg fields must be stored in asm order
(f0 → f14 → f18). The `volatile` keyword on MMIO registers forces a double-read; here it
is the flag condition that controls each store via `movne`.

## Try this
1. Try the per-field if-block form first: `if (flag) cfg->f0 = v0; if (flag) cfg->f14 = v14;
   if (flag) cfg->f18 = v18;`. This produces separate `cmp + strne` sequences per field,
   matching the "test flag before each field / movne pattern" recipe note.
2. In objdiff: if the asm uses a single `beq .skip` and all three stores are inside the block
   (not predicated individually), switch to the single-if form with all three stores inside
   one `if (flag) { … }`.
3. Fallback: if the conditional form is correct but the callee at the end (`func_bg_init`)
   has wrong args, check nearby ov016 BgCfg users in `docs/research/types/BgCfg.md`.
   Class C should match within 1–2 reshape steps. If the flag sense is inverted (movne vs
   moveq), flip to `if (!flag)` for the skip branch.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x66666667` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02005dac` | main TU call |
| `bl` | `func_020944a4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

