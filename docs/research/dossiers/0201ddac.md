# 0201ddac — func_0201ddac (main, class D, 136B)

## Recipe + Risk
**Recipe:** two u8-window extracts (lsl;lsr#0x18); reload on else-branch; nested >=0x20 compare ladder
**Risk:** reshape-able/permuter-class: orig RELOADS a[0x1c] in the lo<=0x20 branch (second ldr) and reuses ip across the cmp ladder; if mwcc hoists one load or rotates lr, keep the explicit reload + match movge-chains via if/else-if — branch fold order is fragile.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0201ddac (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     two u8-window extracts (lsl;lsr#0x18); reload on else-branch; nested >=0x20 compare ladder
 *   risk:       reshape-able/permuter-class: orig RELOADS a[0x1c] in the lo<=0x20 branch (second ldr) and reuses ip across the cmp ladder; if mwcc hoists one load or rotates lr, keep the explicit reload + match movge-chains via if/else-if — branch fold order is fragile.
 *   confidence: low
 */
/* func_0201ddac (main, class D) — two signed :6 bitfields packed in word a[0x1c]
 * (bits 0..5 = lo at lsl#0x18>>0x18, bits 8..13 = hi at lsl#0x10>>0x18),
 * compared against threshold 0x20, plus two more args r1/r2. Returns one of
 * {0, 0x800, 0x1000, 0x1800} encoding which of (lo>0x20, hi>0x20, r1>=0x20,
 * r2>=0x20) hold. The two field extracts use lsl#k;lsr#0x18 => unsigned :8 of
 * a byte window; but the field width is a byte so model as u8 reads at the
 * matching sub-offsets of the word. */
unsigned int func_0201ddac(char *a, int r1, int r2) {
    unsigned int w  = *(unsigned int *)(a + 0x1c);
    unsigned int lo = (w << 0x18) >> 0x18;   /* byte 0 of word */
    unsigned int hi;
    int r = 0;
    if (lo > 0x20) {
        hi = (w << 0x10) >> 0x18;            /* byte 1 of word */
        if (hi > 0x20) {
            if (r1 >= 0x20 && r2 >= 0x20)      r = 0x1800;
            else if (r1 >= 0x20)               r = 0x800;
            else if (r2 >= 0x20)               r = 0x1000;
        } else {
            if (r1 >= 0x20)                    r = 0x800;
        }
    } else {
        /* reload a[0x1c] and re-extract byte 1 (orig does ldr again here) */
        hi = ((unsigned int)(*(unsigned int *)(a + 0x1c)) << 0x10) >> 0x18;
        if (hi > 0x20) {
            if (r2 >= 0x20)                    r = 0x800;
        }
    }
    return (unsigned int)r;
}
```

## Struct context
No GameSingleton/SystemWork fields. The struct pointer `a` is a caller-supplied object; only offset `+0x1c` is accessed, treated as a packed word containing two byte-wide fields. The field layout is inferred:

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| a+0x1c | packed word | unsigned int | loaded (twice in else-branch) for byte-field extraction |
| a+0x1c byte 0 | lo | u8 (extracted via lsl#0x18;lsr#0x18) | compare > 0x20 |
| a+0x1c byte 1 | hi | u8 (extracted via lsl#0x10;lsr#0x18) | compare > 0x20 (in both branches) |

## Closest matched example
**Path:** src/main/func_0201c198.c
**Why similar:** Both functions extract a sub-byte field from a packed word using the shift-pair idiom, then compare to a threshold and branch — demonstrating the `(w << k) >> 0x18` byte-extract pattern mwcc emits for :8 bitfield reads.
**Key lesson:** The explicit reload of `*(unsigned int *)(a + 0x1c)` in the `else` branch (the second `ldr`) is the critical reshape. If the compiler CSEs the first load `w` and uses it in both branches, it will hoist the single ldr and the two-ldr sequence collapses. The prep draft correctly writes a second `*(unsigned int *)(a + 0x1c)` dereference in the else-branch body. Do not rename it to use `w` — that would fold the two loads. The `>=0x20` comparisons should emit `cmp; movge` predicated moves via if/else-if chains, not a jump table.

## Try this
1. Build as drafted. The most likely first divergence is in the else-branch: verify the reload `*(unsigned int *)(a + 0x1c)` really causes a second `ldr` in objdiff. If mwcc still CSEs it, try adding `volatile` to the pointer cast: `hi = ((unsigned int)(*(volatile unsigned int *)(a + 0x1c)) << 0x10) >> 0x18;`.
2. If the `>=0x20` compare ladder emits a jump table or rearranges the if/else-if order, restructure the comparison chain explicitly with separate `if (r1 >= 0x20)` checks before each `else if (r2 >= 0x20)` to match the exact branch-fold order in the orig.
3. If the register reuse of `ip` across the `cmp` ladder cannot be replicated (the `r = 0x800/0x1000/0x1800` assignments use different registers than orig), park as permuter-class; bit-guard and#1 is un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

