# 0220e988 — func_ov002_0220e988 (ov002, class F retriage, 376B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Walks both players' slot arrays (0..4) using PerPlayerRowTable stride 0x868 `mla` and reads `Ov002Slot.id_lo13` via the documented `lsl#19/lsr#19` bitfield extraction. The `data_ov002_022cf26c` companion table lookup at `[r1, r2]` is a halfword guard — parallel to the `+0x38` slot field pattern. All struct offsets now documented.
**Confidence:** high
**Structs needed:** PerPlayerRowTable (stride 0x868, +0x30 slot array, +0x38, +0xF8), Ov002Slot (id_lo13 bitfield lsl#19/lsr#19, stride 0x14)

## Assembly

```asm
; func_ov002_0220e988 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022cf16c
        .extern data_ov002_022cf26c
        .extern func_ov002_021d59cc
        .extern func_ov002_021d95fc
        .extern func_ov002_021de480
        .global func_ov002_0220e988
        .arm
func_ov002_0220e988:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, lr}
    sub sp, sp, #0x8
    mov sl, r0
    ldrh r0, [sl, #0x2]
    ldr r1, _LIT0
    ldr r5, _LIT1
    mov r2, r0, lsl #0x1f
    mov r2, r2, lsr #0x1f
    mov r4, r0, lsl #0x1a
    and r0, r2, #0x1
    mla r6, r0, r1, r5
    rsb r0, r2, #0x1
    and r7, r0, #0x1
    mul r2, r7, r1
    add r1, r5, r2
    ldrh r3, [sl, #0x4]
    ldr r1, [r1, #0xf8]
    mov r4, r4, lsr #0x1b
    mov r5, r3, lsl #0x11
    mov r3, #0x14
    mul r3, r4, r3
    add r4, r6, #0x30
    ldr r4, [r4, r3]
    mov r1, r1, lsl #0x13
    mov r3, r4, lsl #0x2
    mov r3, r3, lsr #0x18
    mov r1, r1, lsr #0x13
    mov r4, r4, lsl #0x12
    mov r3, r3, lsl #0x1
    mov r5, r5, lsr #0x17
    add r3, r3, r4, lsr #0x1f
    cmp r5, r3
    str r1, [sp, #0x4]
    addne sp, sp, #0x8
    movne r0, #0x0
    ldmneia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    cmp r1, #0x0
    ldrne r1, _LIT2
    ldrneh r1, [r1, r2]
    cmpne r1, #0x0
    beq .L_2c0
    mov r2, #0x0
    mov r3, r2
    mov r1, #0xa
    bl func_ov002_021de480
.L_2c0:
    mov r6, #0x0
    ldr fp, _LIT1
    mov r5, r6
    mov r4, #0x9
.L_2d0:
    ldr r0, _LIT0
    and r1, r6, #0x1
    mla r9, r1, r0, fp
    mov r7, #0x0
    add r8, r9, #0x30
.L_2e4:
    ldr r0, [r8]
    mov r0, r0, lsl #0x13
    movs r0, r0, lsr #0x13
    ldrneh r0, [r9, #0x38]
    cmpne r0, #0x0
    beq .L_338
    str r5, [sp]
    ldrh r3, [sl, #0x2]
    mov r0, r6
    mov r1, r7
    mov r2, r3, lsl #0x1f
    mov r2, r2, lsr #0x1f
    and r2, r2, #0xff
    mov r3, r3, lsl #0x1a
    mov r3, r3, lsr #0x1b
    and r3, r3, #0xff
    orr r2, r2, r3, lsl #0x8
    mov r2, r2, lsl #0x10
    mov r3, r4
    mov r2, r2, lsr #0x10
    bl func_ov002_021d59cc
.L_338:
    add r7, r7, #0x1
    cmp r7, #0x4
    add r8, r8, #0x14
    add r9, r9, #0x14
    ble .L_2e4
    add r6, r6, #0x1
    cmp r6, #0x2
    blt .L_2d0
    ldrh r0, [sl]
    ldr r1, [sp, #0x4]
    cmp r0, r1
    beq .L_36c
    bl func_ov002_021d95fc
.L_36c:
    mov r0, #0x0
    add sp, sp, #0x8
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
_LIT0: .word 0x00000868
_LIT1: .word data_ov002_022cf16c
_LIT2: .word data_ov002_022cf26c
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| PerPlayerRowTable | stride 0x868 | base | char[] | own-player row: `mla r6, player, 0x868, base`; opposite-player row: `mla opp_row, (1-player), 0x868, base` |
| PerPlayerRowTable | +0xF8 | (f_0f8) | int | opposite-player field loaded before slot check; `ldr r1, [opp_row, #0xF8]` |
| PerPlayerRowTable | +0x30 | slots[] start | Ov002Slot[] | `add r8/r9, row, #0x30` — slot array base |
| PerPlayerRowTable | +0x38 | (slot count / guard halfword) | u16 | `ldrh r0, [row, #0x38]` — nonzero guard per-player |
| Ov002Slot | +0x000 | id_lo13 | u32:13 | `ldr; lsl#19; lsr#19` — occupied check |
| Ov002Slot | stride 0x14 | (slot stride) | — | `add r8, r8, #0x14` inner loop advance |

## Recipe sketch

```c
int func_ov002_0220e988(CardObj *self) {
    u16 hw2       = self->f2;
    int player    = (hw2 << 0x1f) >> 0x1f;           /* bit0 */
    int slot_idx  = (hw2 << 0x1a) >> 0x1b;           /* bits 5..1 (5 bits) */
    int opp       = 1 - (player & 1);
    char *own_row = (char *)data_ov002_022cf16c + (player & 1) * 0x868;
    char *opp_row = (char *)data_ov002_022cf16c + opp * 0x868;

    /* compare self's slot field against opposite player's +0xF8 field */
    int opp_f0f8  = *(int *)(opp_row + 0xF8);
    u32 *own_slot = (u32 *)(own_row + 0x30 + slot_idx * 0x14);
    u32  s0       = *own_slot;

    /* extract composite comparison value from slot word */
    int s_byte = (s0 << 2) >> 0x18;                   /* bits 29..22 */
    int s_lo   = (s_byte << 1) + ((s0 << 0x12) >> 0x1f); /* bit-pack */

    int hw4_bit = (self->f4 << 0x11) >> 0x17;        /* bits 6..0 of f4 */
    if (hw4_bit != s_lo) return 0;

    /* save opp_f0f8 (id_lo13 extracted); check companion table */
    int id13 = (opp_f0f8 << 0x13) >> 0x13;
    if (id13 != 0) {
        u16 cf26c_val = *(u16 *)((char *)data_ov002_022cf26c + opp * 2);
        if (cf26c_val != 0)
            func_ov002_021de480(0, 0, 0xa, 0);    /* id13!=0, cf26c!=0 */
    }

    /* scan both players' slots 0..4 */
    for (int p = 0; p < 2; p++) {
        char *row  = (char *)data_ov002_022cf16c + (p & 1) * 0x868;
        u32  *slot = (u32 *)(row + 0x30);
        for (int s = 0; s <= 4; s++, slot = (u32 *)((char *)slot + 0x14)) {
            struct Ov002Slot *sp = (struct Ov002Slot *)slot;
            if (sp->id_lo13 == 0) continue;
            u16 row38 = *(u16 *)(row + 0x38);
            if (row38 == 0) continue;
            u16 hw2b    = self->f2;
            u8  arg2_lo = (u8)((hw2b << 0x1f) >> 0x1f);
            u8  arg2_hi = (u8)((hw2b << 0x1a) >> 0x1b);
            u16 arg2    = (u16)(arg2_lo | (arg2_hi << 8));
            func_ov002_021d59cc(p, s, arg2, 0x9);
        }
    }

    if ((u16)self->f0 != id13)
        func_ov002_021d95fc();
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_021b9830.c (described in retriage doc)
**Why similar:** Same double-loop over (player=0..1, slot=0..4) with `mla row, player, 0x868, base` and `Ov002Slot.id_lo13` via lsl#19/lsr#19 extraction, plus a `ldrh [row, #0x38]` nonzero guard.
**Key lesson:** The slot array starts at `row + 0x30` and each element is 0x14 bytes; declare slot pointer as `(u32 *)(row + 0x30)` and advance with `slot = (u32 *)((char *)slot + 0x14)`. The `id_lo13` bitfield must be typed as `unsigned int id_lo13 : 13` (not a mask) to get `lsl#19;lsr#19` rather than `and #0x1fff`. The `(u8)` byte-pack lever applies to the `arg2` construction: `(u8)lo | ((u8)hi << 8)`.

## Try this
1. Declare `struct Ov002Slot { unsigned int id_lo13 : 13; };` and use `sp->id_lo13 == 0` for the occupancy check — do NOT use `(word << 19) >> 19` in C (mwcc folds that to `& 0x1fff` which gives `and` not `lsl/lsr`).
2. For the `arg2` byte-pack: write `(u8)(player_bit) | ((u8)(slot_field) << 8)` — the `(u8)` cast triggers `and #0xff` + `orr lsl#8`, not the `lsl#24;lsr#16` peephole.
3. If the pre-loop `opp_f0f8` load and `hw4_bit` comparison don't match, check that `slot_idx` is extracted as `(u16 hw2 << 0x1a) >> 0x1b` with unsigned shifts (5-bit field); a mismatch here is a reg-alloc near-miss — escalate to `.s`.
