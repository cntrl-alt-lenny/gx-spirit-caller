# 02054a30 — func_02054a30 (main, class D, 160B)

## Recipe + Risk
**Recipe:** shared loop counter; signed-char asr shifts; ordered out[] stores
**Risk:** reshape-able: orig loads the stack temp with ldrb yet shifts asr; signed-char tmp gives ldrsb+asr (load-instr mismatch). Toggle tmp signedness / add a mask to flip ldrsb->ldrb while keeping asr.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02054a30 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     shared loop counter; signed-char asr shifts; ordered out[] stores
 *   risk:       reshape-able: orig loads the stack temp with ldrb yet shifts asr; signed-char tmp gives ldrsb+asr (load-instr mismatch). Toggle tmp signedness / add a mask to flip ldrsb->ldrb while keeping asr.
 *   confidence: med
 */
/* func_02054a30 (main, class D) — base64-encode up to 3 input bytes (count r2)
 * into 4 output chars. Copy n<=3 signed bytes to a stack temp, zero-pad to 3,
 * then the canonical 6-bit pack with arithmetic right shifts.
 *   out[0]= in[0]>>2
 *   out[1]= ((in[0]&3)<<4) | (in[1]>>4)
 *   out[2]= ((in[1]&0xf)<<2) | (in[2]>>6)
 *   out[3]= in[2]&0x3f
 * STORE-ORDER recipe: emit the four out[] stores in order; shifts are asr (signed).
 */

void func_02054a30(const signed char *src, signed char *out, int count) {
    signed char tmp[3];
    int i;

    for (i = 0; i < count; i++)
        tmp[i] = src[i];
    for (; i < 3; i++)
        tmp[i] = 0;

    out[0] = tmp[0] >> 2;
    out[1] = ((tmp[0] & 3) << 4) | (tmp[1] >> 4);
    out[2] = ((tmp[1] & 0xf) << 2) | (tmp[2] >> 6);
    out[3] = tmp[2] & 0x3f;
}
```

## Struct context
No struct / global access. Pure computation on src/out byte arrays and a stack-allocated 3-byte temp. No SystemWork or GameSingleton involvement.

| offset | field | type | use |
|--------|-------|------|-----|
| (none) | — | — | — |

The key tension is `tmp[]` signedness: the original asm loads `tmp[i]` with `ldrb` (unsigned byte load) but then right-shifts with `asr` (arithmetic/signed shift). Declaring `tmp` as `signed char tmp[3]` causes MWCC to emit `ldrsb` (sign-extending byte load), which does NOT match `ldrb`. Declaring `tmp` as `unsigned char tmp[3]` gives `ldrb` but then shifts become `lsr` (logical/unsigned), not `asr`.

## Closest matched example
**Path:** src/main/Rand_Next.c
**Why similar:** Arithmetic on a small accumulator with signedness-sensitive shifts and masked results.
**Key lesson:** The `ldrb + asr` combination requires the temp to be loaded as an unsigned byte but shifted as a signed integer. The fix is to declare `tmp` as `unsigned char tmp[3]` (giving `ldrb`) and then mask the loaded values before shifting: `int t0 = (signed char)tmp[0];` (explicit cast to `signed char` after loading from `unsigned char`). This forces MWCC to load with `ldrb` and then sign-extend via shift, producing `asr` on the sign-extended register. Alternatively, load the bytes into `int` temps with explicit sign-extension: `int b0 = (signed char)tmp[0];` then use `b0 >> 2`.

## Try this
1. Declare `unsigned char tmp[3];` and cast to `signed char` at each use: `out[0] = ((signed char)tmp[0]) >> 2;` — the `ldrb` + sign-extension cast should give `ldrb; mov r,r,asl#24; mov r,r,asr#24; asr#2` which collapses if MWCC optimises the double-shift; alternatively try `int t0 = tmp[0]; out[0] = (t0 << 24) >> 26;` to force `ldrb; lsl#24; asr#26`.
2. If objdiff shows `lsr` instead of `asr` on the shift instructions, the shift operands are still unsigned — try the explicit `(signed char)` cast approach. If `ldrsb` appears instead of `ldrb`, the tmp array is still `signed char` — switch to `unsigned char`.
3. When to park: if the `ldrb` + `asr` combination cannot be forced by any signedness or cast manipulation (MWCC always picks one consistent path), this is a permuter-class load-instruction wall — park as `.s`.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

