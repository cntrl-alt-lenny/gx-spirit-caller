# 0208d6f4 — func_0208d6f4 (main, class C, 184B)

## Recipe + Risk

**Recipe:** equality if/else-if (beq cascade); per-arm RMW-then-strb store order; bit31 set/clear
**Risk:** reshape-able: equality cascade should match; main divergence is whether mwcc keeps 0x04000000 as mov-immediate base re-derived per arm vs a held reg — orig re-derives mov r2,#0x4000000 per arm, which the natural C produces.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0208d6f4 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     equality if/else-if (beq cascade); per-arm RMW-then-strb store order; bit31 set/clear
 *   risk:       reshape-able: equality cascade should match; main divergence is whether mwcc keeps 0x04000000 as mov-immediate base re-derived per arm vs a held reg — orig re-derives mov r2,#0x4000000 per arm, which the natural C produces.
 *   confidence: med
 */
/* func_0208d6f4 (main, class C) — field idx8 (=0x10); 3-way EQUALITY dispatch
 * (beq cascade => if/else-if). MMIO RMW of main sound reg 0x04000000 bit31.
 *   arg==0    -> 0x04000000 &= ~bit31
 *   arg==0x20 -> 0x04000000 |= bit31 ; sound[0x245]=0x85
 *   arg==0x40 -> 0x04000000 |= bit31 ; sound[0x246]=0x85
 * 0x04000000 is materialized via mov (immediate), not a literal load.
 */
extern unsigned short data_021a6304[13];
extern void func_0208dce4(int bits);

void func_0208d6f4(int arg) {
    unsigned short old = data_021a6304[8];
    data_021a6304[8] = (unsigned short)arg;
    data_021a6304[0] = (data_021a6304[0] | old) & ~arg;
    if (arg == 0) {
        *(volatile unsigned int *)0x04000000 &= ~0x80000000;
    } else if (arg == 0x20) {
        *(volatile unsigned int *)0x04000000 |= 0x80000000;
        *(volatile unsigned char *)0x04000245 = 0x85;
    } else if (arg == 0x40) {
        *(volatile unsigned int *)0x04000000 |= 0x80000000;
        *(volatile unsigned char *)0x04000246 = 0x85;
    }
    func_0208dce4(data_021a6304[0]);
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `data_021a6304[0]` | mask word | `unsigned short` | running bitmask OR'd/AND-NOT per dispatch |
| `data_021a6304[8]` | current-state slot (idx8, byte offset 0x10) | `unsigned short` | stores incoming arg; old read before write |
| `0x04000000` | main sound control register | `volatile unsigned int` | bit31 (0x80000000) cleared for arg==0, set for arg==0x20 and arg==0x40 |
| `0x04000245` | sound channel register | `volatile unsigned char` | written 0x85 for arg==0x20 (after the RMW) |
| `0x04000246` | sound channel register | `volatile unsigned char` | written 0x85 for arg==0x40 (after the RMW) |

## Closest matched example

**Path:** src/main/func_0208cd18.c
**Key lesson:** The critical pattern is that `0x04000000` must be materialized as a `mov r2, #0x4000000` immediate per arm (not loaded from a literal pool). Using `*(volatile unsigned int *)0x04000000` directly in each arm's expression — without binding to a local pointer — forces mwcc to re-emit the `mov` immediate for each arm, which matches the orig. The store order within each arm (RMW on 0x04000000 first, then `strb` to the sound register) is fixed by the C statement order.

## Try this

1. Do not introduce a `volatile unsigned int *p = (volatile unsigned int *)0x04000000;` local pointer. The orig re-derives `mov r2, #0x4000000` in each arm — binding the address to a local would hold the register across arms and produce a different load pattern.
2. Confirm in objdiff that the equality cascade uses `cmp r0, #0` / `beq` then `cmp r0, #0x20` / `beq` then `cmp r0, #0x40` / `beq` — not a switch binary-search tree. The three values (0, 0x20, 0x40) are all powers-of-two multiples so mwcc should keep the if/else-if chain when written as equality tests.
3. If the 0x04000000 base is held in a register across arms (ldr from pool once), force per-arm re-derivation by explicitly writing `*(volatile unsigned int *)0x04000000` in each arm's expression rather than extracting a shared expression.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021a6304` | data symbol |
| `_LIT1` | `0x04000245` | MMIO/const |
| `_LIT2` | `0x04000246` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208dce4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

