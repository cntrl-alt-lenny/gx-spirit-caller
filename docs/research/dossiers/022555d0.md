[//]: # (markdownlint-disable MD013 MD041)

# 022555d0 — func_ov002_022555d0 (ov002, class F retriage, 312B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** 312B class F with confirmed KB struct hits on DuelQueueState and Ov002Slot. At 312B this sits between the 296B (02253528) and 348B (02254bf4) DuelQueueState patterns. DuelQueueState+0x5A8 (queue_state) controls the outer gate; Ov002Slot id_lo13 (lsl#19/lsr#19) drives the inner scan. Both structs are now fully documented.
**Confidence:** high
**Structs needed:** DuelQueueState (base 0x022CE288, +0x5A8 queue_state, +0x5C0 re-entrancy lock, +0x5D4 cleared_on_edge), Ov002Slot (id_lo13 via lsl#19/lsr#19, f_0e at +0x00E, stride 0x14), PerPlayerRowTable (stride 0x868)

## Assembly

```asm
; NOTE: src/overlay002/func_ov002_022555d0.s does not yet exist in the worktree.
; The function must be extracted from the ROM / config before a .s file is present.
; The assembly sketch is inferred from the retriage doc (312B, class F,
; DuelQueueState + Ov002Slot), the func_ov002_0228791c recipe, and neighboring
; .s files (022555a4.s, 0225585c.s).
; Replace this block with the real disassembly once extracted.
;
; Expected shape (312B, class F, ARM mode):
;   stmdb sp!, {r4–fp, lr}           ; class F multi-reg save
;   ldr  r?, _LIT_dqs                ; &data_ov002_022ce288
;   ldr  r?, [r?, #0x5a8]            ; queue_state
;   cmp  r?, #0x7f
;   beq/bne ...                      ; 0x7f vs 0x80 dispatch
;   ldr  r?, [r?, #0x5d4]            ; cleared_on_edge (loop bound variant)
;   ldr  r?, _LIT_ppt                ; &data_ov002_022cf16c
;   and/mla (player & 1) * 0x868     ; stride walk
;   lsl#19 / lsr#19                  ; id_lo13 extraction
;   ... scan body ...
;   ldmia sp!, {r4–fp, pc}
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState (022ce288) | +0x5A8 | queue_state | int | entry gate: 0x7f=done, 0x80=active |
| DuelQueueState (022ce288) | +0x5C0 | re-entrancy lock | int | skip if nonzero |
| DuelQueueState (022ce288) | +0x5D4 | cleared_on_edge | int | may be used as loop bound (per 0228791c recipe) |
| PerPlayerRowTable (022cf16c) | (player&1)*0x868 + 0x030 | slots[0] | Ov002Slot* | slot walk base |
| Ov002Slot | slot+0x008 via lsl#19/lsr#19 | id_lo13 | u13 bitfield | occupancy check |
| Ov002Slot | slot+0x00E as ldrh | f_0e | u16 | secondary slot filter |

## Recipe sketch

```c
/* 312B DuelQueueState-gated Ov002Slot scan.
 * Combines 0x5A8 queue-state gate with Ov002Slot slot walk.
 * Shape: queue-state check → re-entrancy guard → slot scan → result. */
int func_ov002_022555d0(CardObj *card) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    int qs = dqs->f_5a8;   /* +0x5A8 */
    if (qs == 0x80) {
        func_ov002_some_delegate(card);
        return 0x7f;
    }
    if (qs != 0x7f)
        return 0;
    if (dqs->f_5c0)        /* +0x5C0: re-entrancy lock */
        return 0;

    int found = 0;
    for (int p = 0; p < 2; p++) {
        char *row = (char *)data_ov002_022cf16c + (p & 1) * 0x868;
        Ov002Slot *slot = (Ov002Slot *)(row + 0x30);

        for (int s = 0; s < 5; s++, slot++) {
            unsigned int id = (slot->f_000_word << 19) >> 19;
            if (id == 0) continue;

            unsigned short f_0e = *(unsigned short *)((char *)slot + 0xe);
            if (f_0e != 0) continue;

            if (!func_ov002_0225764c(card, p, s)) continue;
            func_ov002_02253458(p, 0xe, s);
            found = 1;
        }
    }
    return found;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_02252558.s
**Why similar:** Both are class F PerPlayerRowTable + Ov002Slot scans that call func_ov002_02253458 at the end of a slot match — the "scan and dispatch kind-N" skeleton that dominates ov002 F-class functions.
**Key lesson:** The DuelQueueState 0x7f/0x80 pair must appear at the top of the function body before any slot-walk loop. Reversed order of `cmp #0x80 / beq / cmp #0x7f / bne` vs `cmp #0x7f / beq / cmp #0x80 / bne` changes the branch-target layout and will misalign all subsequent labels.

## Try this

1. Check `dqs->f_5a8` first against `0x80` (active), then `0x7f` (done), exactly as in the func_ov002_022157e4 recipe — this is the standard DuelQueueState class F entry pattern and the cmp ordering controls the branch ladder.
2. After the .s file is extracted: confirm whether `dqs->f_5d4` (cleared_on_edge, +0x5D4) appears as a loop bound — if the inner loop count comes from `dqs->f_5d4` rather than the fixed PerPlayerRowTable slot count, adjust the loop bound accordingly.
3. Escalate: at 312B with multiple DuelQueueState guards and a full slot loop, the callee-saved frame is at least r4–r9; use decl-order lever (variable declaration order controls mwcc register assignment) before escalating to permuter.

**PREREQUISITE:** Extract `func_ov002_022555d0.s` from the ROM before attempting C matching. The .s file is absent from src/overlay002/.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

