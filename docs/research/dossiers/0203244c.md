# 0203244c — func_0203244c (main, class D, 116B)

## Recipe + Risk
**Recipe:** store-order; r6=result moveq 0; reload base into entry after head=0; ldrb [+1]==1 guard
**Risk:** reshape-able: the early `beq .L_020324b4` after head=0 (count==0) keeps r6=0 then jumps to RestoreIrq; my nested early-return may emit an extra branch. Risk: control-flow shape, restructure as goto-to-restore if diverges.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0203244c (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store-order; r6=result moveq 0; reload base into entry after head=0; ldrb [+1]==1 guard
 *   risk:       reshape-able: the early `beq .L_020324b4` after head=0 (count==0) keeps r6=0 then jumps to RestoreIrq; my nested early-return may emit an extra branch. Risk: control-flow shape, restructure as goto-to-restore if diverges.
 *   confidence: low
 */
typedef struct Ring {
    /* 0x00 */ int head;
    /* 0x04 */ int count;
    /* 0x08 */ char _pad08[4];
    /* 0x0c */ unsigned char *base;
} Ring;

extern int OS_DisableIrq(void);
extern void OS_RestoreIrq(int);

unsigned char *func_0203244c(Ring *ring, int *out)
{
    int saved;
    int head;
    unsigned char *entry;
    unsigned char *result;

    if (ring == 0)
        return 0;
    saved = OS_DisableIrq();
    head = ring->head;
    if (head == ring->count) {
        result = 0;
    } else {
        entry = ring->base + head;
        if (entry[1] == 1) {
            ring->head = 0;
            if (ring->count == 0) {
                result = 0;
                OS_RestoreIrq(saved);
                return result;
            }
            entry = ring->base + 0;
        }
        *out = *(unsigned short *)(entry + 2);
        result = entry + 4;
    }
    OS_RestoreIrq(saved);
    return result;
}
```

## Struct context
No GameSingleton/SystemWork offsets. This is a ring-buffer dequeue function protected by IRQ brackets. The `ldrb [entry+1] == 1` check is a sentinel/wrap flag.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| ring+0x00 | head | int | read as index; compared with count; written to 0 on wrap |
| ring+0x04 | count | int | compared with head (full check); checked == 0 after wrap |
| ring+0x0c | base | unsigned char* | used to compute entry address: base+head and base+0 |
| entry+0x01 | [+1] | unsigned char | ldrb tested == 1 (wrap sentinel) |
| entry+0x02 | [+2] | unsigned short | ldrh into *out |
| entry+0x04 | [+4] | (payload) | pointer returned as result |

## Closest matched example
**Path:** src/main/Task_InvokeLockedIrq.c
**Why similar:** Same IRQ-bracket + null-guard pattern with OS_DisableIrq / OS_RestoreIrq bracketing a critical section.
**Key lesson:** The control-flow shape is the hardest part: the asm has a single RestoreIrq at the bottom (`.L_020324b4`) with ONE early RestoreIrq + return inside the `count==0` branch. The C draft models this with an explicit early return inside the `ring->count == 0` block and a single `OS_RestoreIrq(saved)` at the end of the function body. If objdiff shows an extra branch or the early-return path is merged with the bottom, restructure using a `goto restore` idiom: set `result = 0`, then `goto restore;`, and put `restore: OS_RestoreIrq(saved); return result;` at the bottom.

## Try this
1. If the early-return path inside `ring->count == 0` emits an extra branch vs the asm, convert to goto: replace `OS_RestoreIrq(saved); return result;` in the inner block with `result = 0; goto restore_irq;` and add `restore_irq: OS_RestoreIrq(saved); return result;` at the end of the function.
2. The `ldrb [entry+1] == 1` wrap check: `entry[1]` must be `unsigned char` access (not `int`). If objdiff shows a widened load, cast explicitly: `if ((unsigned char)entry[1] == 1)`.
3. The `ldrh` at `entry+2` for `*out = *(unsigned short *)(entry + 2)` must emit as `ldrh` not `ldr` — if it widens, add an explicit `unsigned short` cast on the read: `*out = (int)(*(unsigned short *)(entry + 2));`.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `OS_DisableIrq` |  |
| `bl` | `OS_RestoreIrq` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

