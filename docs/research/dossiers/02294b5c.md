[//]: # (markdownlint-disable MD013 MD041)

# 02294b5c — func_ov002_02294b5c (ov002, class F retriage, 328B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses DuelQueueState (data_ov002_022ce288) and Ov002Slot id_lo13. The previously opaque branching on DuelQueueState fields (f_5a8 queue state, f_5b4 pending flag) and the Ov002Slot walk are both now documented. At 328B the function is large but structurally a gated slot scan — deterministic once the KB structs are named.
**Confidence:** high
**Structs needed:** DuelQueueState (base 0x022CE288, f_5a8 queue_state, f_5b4 pending-action flag, f_5c0 re-entrancy lock), Ov002Slot (id_lo13 :13 via lsl#19/lsr#19), PerPlayerRowTable (stride 0x868)

## Assembly

```asm
; Source file not present — func_ov002_02294b5c has no .s file in the working tree.
; This function was either already matched and its .s removed, or was never emitted
; as a standalone unit. The retriage entry is preserved for campaign tracking.
; Size: 328B, Conf: high, KB hits: DuelQueueState + Ov002Slot
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state 0x7f / 0x80 gate |
| DuelQueueState | +0x5B4 | f_5b4 | int | pending-action flag |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock |
| Ov002Slot | +0x008 | id_lo13 | u32 :13 | card id via lsl#19/lsr#19 |
| PerPlayerRowTable | base | data_ov002_022cf16c | extern | player slot array, stride 0x868 |

## Recipe sketch

```c
typedef unsigned short u16;
struct Ov002Self { u16 f0; u16 b0 : 1; u16 rest : 15; };
struct DuelQueueState {
    char _pad5a8[0x5a8]; int f_5a8;
    void *f_5ac;
    char _pad5b4[4]; int f_5b4;
    char _pad5c0[8]; int f_5c0;
};
extern struct DuelQueueState data_ov002_022ce288;
extern char data_ov002_022cf16c[];

/* 328B: DuelQueueState gated + Ov002Slot scan, pending-flag update */
int func_ov002_02294b5c(struct Ov002Self *self) {
    if (data_ov002_022ce288.f_5c0) return 0;
    int qs = data_ov002_022ce288.f_5a8;
    if (qs == 0x80) {
        /* set pending if any matching slot found */
        int player = (int)self->b0 & 1;
        char *row = data_ov002_022cf16c + player * 0x868;
        for (int s = 0; s < 5; s++) {
            unsigned int w = *(unsigned int *)(row + 0x30 + s * 0x14);
            unsigned int id = (w << 19) >> 19;
            if (id == 0) continue;
            /* check card condition; if met, set pending flag */
            data_ov002_022ce288.f_5b4 = 1;
        }
        return 0x7f;
    }
    if (qs == 0x7f) {
        /* done-state: process pending flag */
        if (data_ov002_022ce288.f_5b4) {
            data_ov002_022ce288.f_5b4 = 0;
            /* invoke handler */
        }
    }
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_0222b2e0.c
**Why similar:** Same DuelQueueState f_5a8 switch (0x80 / 0x7f) with Ov002Self player-bit based slot access and f_5b4 pending-action flag manipulation.
**Key lesson:** The `f_5b4` pending flag update inside the 0x80 arm must store into DuelQueueState at exact offset +0x5B4 — declare it in the padded struct. The 0x80 arm returns 0x7f (done-marker), not 1. Any `if (f_5b4)` check in the 0x7f arm must be a separate `if` not a `switch` fall-through.

## Try this
1. Load `data_ov002_022ce288.f_5a8` and dispatch `if (qs == 0x80)` before `if (qs == 0x7f)` — verify `cmp r0,#0x80` is the first branch instruction in objdiff.
2. Inside the 0x80 arm, check that `data_ov002_022ce288.f_5b4 = 1;` uses the exact +0x5B4 offset (not +0x5B0 or +0x5B8) — objdiff will show a `str r3,[rX,#0x5b4]`.
3. This is a DuelQueueState dispatch — if the slot loop body calls an external function on each matched slot, that call is inside the 0x80 arm before the return 0x7f; don't escalate to permuter unless the branch-table ordering is wrong.
