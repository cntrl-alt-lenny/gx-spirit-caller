# 02040478 — func_02040478 (main, class C, 180B)

## Recipe + Risk
**Recipe:** reload global each use (orig re-ldr r1 every block); stack args 0x1000,0x10; r3=base+0x1000
**Risk:** the func_02091d24 call: orig builds r1=func_0203fe78, r3=*g+0x1000, ip=0x10/r3-stack=0x1000 into a specific arg slot order I inferred; arg count/positions are struct-guessed against the real prototype.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02040478 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     reload global each use (orig re-ldr r1 every block); stack args 0x1000,0x10; r3=base+0x1000
 *   risk:       the func_02091d24 call: orig builds r1=func_0203fe78, r3=*g+0x1000, ip=0x10/r3-stack=0x1000 into a specific arg slot order I inferred; arg count/positions are struct-guessed against the real prototype.
 *   confidence: low
 */
/* func_02040478 (C, 180B): setup + conditional early-out + stack-arg call + teardown.
 * func_02092844(*g+0x13d8). *(g+0x1000+0x3f0)=0. if((g+0x1000)[0x384]!=0){ if(func_02091ae0(*g+0x1318)==0) return; }
 * func_02091d24(*g+0x1318, func_0203fe78, ?, *g+0x1000, /stack:/ 0x1000, 0x10). func_020919d8(*g+0x1318).
 */
extern char *data_0219d9d4;
extern void func_0203fe78(void);
extern void func_020919d8(void *a);
extern void func_02091ae0(void *a);       /* returns int via r0 */
extern void func_02091d24(void *a, void *fn, void *b, void *c, int d, int e);
extern void func_02092844(void *a);

void func_02040478(void) {
    char *g;
    g = data_0219d9d4;
    func_02092844(g + 0x13d8);
    *(int *)(data_0219d9d4 + 0x1000 + 0x3f0) = 0;
    if (*(int *)(data_0219d9d4 + 0x1000 + 0x384) != 0) {
        if ((int)(long)func_02091ae0(data_0219d9d4 + 0x1318) == 0) {
            return;
        }
    }
    func_02091d24(data_0219d9d4 + 0x1318, func_0203fe78, (void *)0x1000,
                  data_0219d9d4 + 0x1000, 0x1000, 0x10);
    func_020919d8(data_0219d9d4 + 0x1318);
}
```

## Struct context
All accesses are through `data_0219d9d4` (same global as 0204037c family). Two sub-regions are involved: `+0x1000` (sub-struct with fields at +0x384 and +0x3f0) and a separate buffer at `+0x1318`.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| `data_0219d9d4` | global base ptr | `char *` | reloaded each use block (no caching) |
| `g+0x13d8` | buffer/handle | `void *` | passed to func_02092844 (setup call) |
| `g+0x1000+0x3f0` (`g+0x13f0`) | flag word | `int` | zeroed (store 0) |
| `g+0x1000+0x384` (`g+0x1384`) | guard flag | `int` | tested != 0 for inner conditional |
| `g+0x1318` | handle/context | `void *` | passed to func_02091ae0, func_02091d24, func_020919d8 |
| `g+0x1000` | sub-struct base | `void *` | passed as arg to func_02091d24 |

No SystemWork or GameSingleton offsets touched.

## Closest matched example
**Path:** src/main/func_0200ad90.c
**Why similar:** guarded global-reload pattern, early-return on condition, final clean teardown call sequence.
**Key lesson:** the recipe explicitly says `reload global each use`. Every access to `data_0219d9d4` in the orig emits a fresh `ldr` from the literal pool — do NOT assign to a local `g` and reuse it. The draft assigns `g` locally but then re-references `data_0219d9d4` directly in subsequent lines — verify this is preserved and no compiler substitution collapses them. The `func_02091d24` call has two stack args (5th=0x1000, 6th=0x10); mwcc pushes stack args right-to-left in the ARM AAPCS `str [sp,#n]` encoding, so the arg order in the C call must be exactly as written.

## Try this
1. Build as-is. Check objdiff at each `data_0219d9d4` use: every access must be a fresh `ldr r?, [pc, #pool]`. If mwcc caches it (e.g., saves the g local in a callee-saved reg for reuse), remove the `char *g` local entirely and expand every use to `data_0219d9d4 + offset` inline.
2. If the `func_02091d24` arg layout diverges, check objdiff for r0/r1/r2/r3 at the call site. The third argument typed as `(void *)0x1000` is a literal-pool word — if it disappears or shifts, try reordering the 3rd/4th args or checking whether arg3 is actually the `+0x1000` sub-struct pointer and arg2 is something else.
3. Confidence is low; if multiple arg slots disagree with the real prototype after one reshape attempt, park as struct-guessed / low-confidence and escalate to decomper for prototype resolution.
