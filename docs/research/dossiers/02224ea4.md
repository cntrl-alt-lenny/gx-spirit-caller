[//]: # (markdownlint-disable MD013 MD041)

# 02224ea4 — func_ov002_02224ea4 (ov002, class F retriage, 356B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Accesses DuelQueueState and Ov002Slot. The function reads `dqs->f_5a8` (queue_state) for state dispatch and iterates Ov002Slot entries using the `id_lo13` lsl#19/lsr#19 bitfield. Once DuelQueueState+0x5A8 and Ov002Slot layout are documented, the scheduling dependency on queue state and slot walk become deterministic.
**Confidence:** high
**Structs needed:** DuelQueueState (+0x5A8 queue_state, +0x5AC secondary ptr), Ov002Slot (id_lo13 :13 bitfield), PerPlayerRowTable (stride 0x868 implied by Ov002Slot walk)

## Assembly

```asm
; No .s file available for func_ov002_02224ea4.
; This function exists in the EUR binary (relocs.txt confirms a load reloc from
; 0x02224ea4 to 0x022d0d8c in ov002 BSS) but has not been processed by
; batch_carve — no GLOBAL_ASM .s was generated.
; The dossier is derived from the retriage table entry and KB struct patterns.
; Assembly must be extracted from the baserom before attempting a C match.
```

## Struct context

From the retriage table: **DuelQueueState, Ov002Slot** (high confidence, 356B, class F).

The reloc `from:0x02224ea4 kind:load to:0x022d0d8c` confirms this function loads from BSS address `0x022D0D8C`, which is within the DuelStateSingleton / DuelQueueState region. At 356B this is a substantial function that likely combines a DuelQueueState state-machine with an Ov002Slot scan — the standard ov002 F-class pattern.

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 (queue_state) | `int` | state dispatch (0x7F done / 0x80 active) |
| DuelQueueState | +0x5AC | f_5ac | `void *` | secondary state pointer |
| Ov002Slot | +0x008 | id_lo13 | `unsigned int :13` | slot walk guard (lsl#19/lsr#19) |
| PerPlayerRowTable | +0x000 | (base) | stride 0x868 | per-player row selector `(player&1)*0x868` |

## Recipe sketch

```c
int func_ov002_02224ea4(CardObj *self) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    int qs = dqs->f_5a8;           /* +0x5A8 */

    if (qs == 0x80) {
        /* active: process ongoing action */
        int player = (int)dqs->f_5ac & 1;
        Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + player * 0x868);
        for (int s = 0; s < 5; s++) {
            Ov002Slot *slot = &row->slots[s];
            if (slot->id_lo13 == 0) continue;     /* :13 bitfield */
            /* slot-specific action — call determined by baserom disassembly */
            func_ov002_helper_a(self, player, s, slot->id_lo13);
        }
        return 0x7f;
    }
    if (qs == 0x7f) {
        /* done: cleanup pass */
        for (int p = 0; p < 2; p++) {
            Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (p & 1) * 0x868);
            for (int s = 0; s < 5; s++) {
                Ov002Slot *slot = &row->slots[s];
                if (slot->id_lo13 == 0) continue;
                func_ov002_helper_b(self, p, s);
            }
        }
        return 0;
    }
    return 0;
}
```

Note: the exact call targets and argument lists require baserom disassembly. The BSS load reloc to `0x022D0D8C` (offset +0xE20 from `data_ov002_022d016c` at 0x022D016C, or +0x5A4 from `data_ov002_022ce288` at 0x022CE288 + 0x5A4 = 0x022CE82C — actually 0x022CE288 + 0xB1C = 0x022CF3A4, or it may be a different global entirely) must be confirmed in the disassembly.

## Closest matched example

**Path:** src/overlay002/func_ov002_022157e4 (Ov002EFRetriage.md sketch)
**Why similar:** Same DuelQueueState+0x5A8 state pair (0x7F / 0x80), same nested player/slot loop with id_lo13 guard.
**Key lesson:** For F-class functions with DuelQueueState dispatch, always write the `if (qs == 0x80)` arm first (active branch returns early) and `if (qs == 0x7f)` second — the comparison order in the .s is deterministic and controls which `cmp` mwcc emits first.

## Try this

1. Extract the assembly for this function from the EUR baserom using `dsd dis` or batch_carve before attempting a C match — no .s file exists yet; run `tools/batch_carve.py --min-addr 0x0 --overlay ov002 --start 0x02224ea4 --count 1`.
2. Once the .s is available: load `dqs->f_5a8` (+0x5A8) as the primary switch; check whether it is a two-value test (0x7F/0x80) or a four-value range switch (0x7D–0x80) by counting the `cmp` instructions in the prologue.
3. If the BSS load reloc target (`0x022D0D8C`) doesn't match any documented field, check whether it is `data_ov002_022d016c + 0xE20` (DuelStateSingleton sub-field beyond the documented layout) — escalate to DSS dispatch tier and add a new field entry to DuelStateSingleton.md.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

