# 0220cb50 — func_ov002_0220cb50 (ov002, class F retriage, 328B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses DuelQueueState+0x5D4 (cleared_on_edge = loop bound at `ldr r0,[r7,#0x5d4]`), and iterates byte entries at DuelQueueState+0x5DC checking for type 0x1B (`ldrb r0,[r7,#0x5dc]`). The companion global data_ov002_022ce884 is the queue entry table. The function also reads PerPlayerRowTable companion `data_ov002_022cf17c` for a per-player count guard (same 0x868 stride). The "eor-discriminant" on the entry's player parity vs self->f2.bit0 is now explained by the documented queue-entry layout.
**Confidence:** high
**Structs needed:** DuelQueueState, Ov002Slot (via queue entry word)

## Assembly

```asm
; func_ov002_0220cb50 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022ce288
        .extern data_ov002_022ce884
        .extern data_ov002_022cf17c
        .extern data_ov002_022d0250
        .extern func_0202b8a8
        .extern func_0202eac8
        .global func_ov002_0220cb50
        .arm
func_ov002_0220cb50:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, lr}
    mov sl, r0
    ldrh r0, [sl, #0x2]
    mov r9, #0x0
    mov r1, r0, lsl #0x14
    mov r1, r1, lsr #0x1a
    cmp r1, #0x1b
    movne r0, r9
    ldmneia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    mov r0, r0, lsl #0x1f
    mov r1, r0, lsr #0x1f
    ldr r0, _LIT0
    and r1, r1, #0x1
    mul r2, r1, r0
    ldr r0, _LIT1
    ldr r0, [r0, r2]
    cmp r0, #0x0
    moveq r0, r9
    ldmeqia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
    ldr r7, _LIT2
    mov r5, r9
    ldr r0, [r7, #0x5d4]
    cmp r0, #0x0
    bls .L_f8
    ldr r8, _LIT3
    ldr fp, _LIT4
    mov r4, r7
.L_6c:
    ldrb r0, [r7, #0x5dc]
    cmp r0, #0x1b
    bne .L_e0
    ldr r0, [r8]
    ldrh r1, [sl, #0x2]
    mov r2, r0, lsl #0x17
    mov r2, r2, lsr #0x15
    ldrh r3, [fp, r2]
    mov r2, r0, lsl #0x16
    mov r2, r2, lsr #0x1f
    mov r3, r3, lsl #0x13
    mov r1, r1, lsl #0x1f
    cmp r2, r1, lsr #0x1f
    mov r6, r3, lsr #0x13
    bne .L_e0
    mov r0, r0, lsl #0xe
    movs r0, r0, lsr #0x1f
    beq .L_e0
    mov r0, r6
    bl func_0202eac8
    cmp r0, #0x0
    beq .L_e0
    mov r0, r6
    bl func_0202b8a8
    cmp r9, r0
    bgt .L_e0
    mov r0, r6
    bl func_0202b8a8
    mov r9, r0
.L_e0:
    ldr r0, [r4, #0x5d4]
    add r5, r5, #0x1
    cmp r5, r0
    add r7, r7, #0x1
    add r8, r8, #0x4
    bcc .L_6c
.L_f8:
    cmp r9, #0x0
    beq .L_12c
    ldr r1, [sl, #0x14]
    ldr r0, _LIT4
    mov r1, r1, lsl #0x17
    mov r1, r1, lsr #0x15
    ldrh r0, [r0, r1]
    mov r0, r0, lsl #0x13
    mov r0, r0, lsr #0x13
    bl func_0202b8a8
    cmp r9, r0
    moveq r0, #0x1
    ldmeqia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
.L_12c:
    mov r0, #0x0
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, sl, fp, pc}
_LIT0: .word 0x00000868
_LIT1: .word data_ov002_022cf17c
_LIT2: .word data_ov002_022ce288
_LIT3: .word data_ov002_022ce884
_LIT4: .word data_ov002_022d0250
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| self (card object) | +0x2 | f2 bits[11:6] | u16 | `lsl#14/lsr#1a` = 6-bit kind field; must == 0x1B to proceed |
| self (card object) | +0x2 | f2 bit0 | u16 | player parity for cf17c guard |
| self (card object) | +0x14 | f_14 | u32 | used in tail check: `lsl#17/lsr#15` extracts index into data_ov002_022d0250 |
| PerPlayerRowTable companion | base | data_ov002_022cf17c | count guard | `[player*0x868]` must be nonzero to proceed |
| DuelQueueState | +0x5D4 | f_5d4 (cleared_on_edge) | int | main loop upper bound; re-read via r4 each iteration |
| DuelQueueState | +0x5DC | f_5dc (entry type byte) | u8 | iterated byte-by-byte; must == 0x1B for inner logic |
| QueueEntry (ce884 table) | +0x0 | entry word | u32 | bits[12:0] = id_lo13 (lsl#13→lsr#13); bit[17] = player parity; bits[22:16] = index into d0250 |
| data_ov002_022d0250 | +index*2 | u16 node | u16 | `ldrh r3,[fp,r2]` then `lsl#13/lsr#13` extracts id; for both loop and tail |

## Recipe sketch

```c
int func_ov002_0220cb50(SelfCard *self) {
    /* kind6 must be 0x1B */
    unsigned int kind6 = ((unsigned short)self->f2 << 12) >> 26;
    if (kind6 != 0x1b) return 0;

    /* cf17c per-player count guard */
    unsigned int player = (unsigned short)self->f2 & 1u;
    if (*(int *)(data_ov002_022cf17c + (player & 1u) * 0x868) == 0)
        return 0;

    DuelQueueState *dqs = &data_ov002_022ce288;
    int loop_bound = dqs->f_5d4;   /* cleared_on_edge */
    int best = 0;                   /* r9 */

    if (loop_bound > 0) {
        int  *entries = (int *)data_ov002_022ce884;
        char *type_ptr = (char *)dqs + 0x5dc;

        for (int i = 0; i < loop_bound; i++, type_ptr++, entries++) {
            if (*type_ptr != 0x1b) goto next_entry;

            int entry = *entries;
            /* extract index into d0250: (entry << 9) >> 11 */
            unsigned int idx = ((unsigned int)entry << 9) >> 11;
            unsigned short node = *(unsigned short *)(data_ov002_022d0250 + idx);
            /* extract id from node: (node << 19) >> 19 */
            unsigned int id = ((unsigned int)(node << 13)) >> 13;

            /* entry player parity must match self */
            unsigned int ent_player = ((unsigned int)entry << 10) >> 31;
            if (ent_player != player) goto next_entry;

            /* entry bit 17 must be set */
            if (!(((unsigned int)entry << 14) >> 31)) goto next_entry;

            if (func_0202eac8(id) == 0) goto next_entry;
            int val = func_0202b8a8(id);
            if (best <= val) {
                val = func_0202b8a8(id);
                best = val;
            }
        next_entry:
            loop_bound = dqs->f_5d4;  /* re-read each iter */
        }
    }

    if (best == 0) return 0;

    /* tail: compare best against self->f_14 node value */
    unsigned int tail_idx = ((unsigned int)self->f_14 << 9) >> 11;
    unsigned short tail_node = *(unsigned short *)(data_ov002_022d0250 + tail_idx);
    unsigned int tail_id = ((unsigned int)(tail_node << 13)) >> 13;
    int tail_val = func_0202b8a8(tail_id);
    return (best == tail_val) ? 1 : 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_022028ac.c  (DuelQueueState pattern)
**Why similar:** The pattern `ldr r0,[r7,#0x5d4]; cmp r0,#0x0; bls .L_f8` is the standard DuelQueueState cleared_on_edge loop-bound guard seen in the 0x0228791c example in the retriage doc.
**Key lesson:** The loop bound is DuelQueueState+0x5D4 and the type byte array starts at +0x5DC — they are re-read each iteration via `ldr r0,[r4,#0x5d4]` (r4 = r7 = &dqs). The type byte stride is 1 (r7++), while the entry word stride is 4 (r8+=4). Keep both as separate pointer advances, not index offsets.

## Try this
1. Load `dqs->f_5d4` (DuelQueueState+0x5D4) as the loop bound; read `*(char *)((char*)dqs + 0x5dc + i)` as the type byte; read `*(int *)(data_ov002_022ce884 + i*4)` as the entry word; check `type == 0x1b` before any inner logic.
2. If objdiff shows player-parity vs self comparison wrong, note the entry player bit is `(entry << 10) >> 31` (bit 21 of entry word), compared to `(self->f2 << 31) >> 31` (bit 0 of f2); both must be equal.
3. The tail check outside the loop uses `self->f_14` (not self->f2) to compute the node index — if it does not match, confirm `f_14` at offset +0x14 is a u32 with the same bit-extraction as the loop.
