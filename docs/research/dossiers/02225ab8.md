[//]: # (markdownlint-disable MD013 MD041)

# 02225ab8 — func_ov002_02225ab8 (ov002, class F retriage, 384B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Accesses DuelQueueState and Ov002Slot. The USA reloc `from:0x02225ab8 kind:arm_call to:0x021ae320` confirms a bl to `func_ov002_021ae320` (or the main arm9 equivalent), consistent with an F-class function that dispatches via the DuelQueueState state machine and walks Ov002Slot entries. Once dqs->f_5a8 is documented, the state dispatch is deterministic.
**Confidence:** high
**Structs needed:** DuelQueueState (+0x5A8 queue_state, +0x5AC secondary), Ov002Slot (id_lo13 :13 bitfield, stride 0x14), PerPlayerRowTable (stride 0x868, implied)

## Assembly

```asm
; No .s file available for func_ov002_02225ab8.
; USA reloc: from:0x02225ab8 kind:arm_call to:0x021ae320
; This function has not been processed by batch_carve — no GLOBAL_ASM .s exists.
; The dossier is derived from the retriage table entry, USA reloc data, and KB patterns.
; Assembly must be extracted from the baserom before attempting a C match.
```

## Struct context

From the retriage table: **DuelQueueState, Ov002Slot** (high confidence, 384B, class F). The `arm_call to:0x021ae320` reloc in the USA config identifies one of the callee functions. At 384B with two key structs, the function most likely follows the DuelQueueState 0x7F/0x80 handshake and iterates slot arrays. This is the same structural pattern as `func_ov002_02225ab8`'s retriage-table neighbours (02224ea4 at 356B, 0222542c at 388B).

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 (queue_state) | `int` | primary state dispatch (0x7F done / 0x80 active) |
| DuelQueueState | +0x5AC | f_5ac | `void *` | secondary state / player selector |
| Ov002Slot | +0x008 | id_lo13 | `unsigned int :13` | slot occupancy: `lsl#19/lsr#19` |
| PerPlayerRowTable | +0x000 | (base) | stride 0x868 | player row selection |

## Recipe sketch

```c
int func_ov002_02225ab8(CardObj *self) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    int qs = dqs->f_5a8;       /* +0x5A8 */

    if (qs == 0x80) {
        /* active: mark transition, trigger cleanup call */
        dqs->f_5ac = 0;
        func_ov002_021ae320(self);   /* confirmed by USA arm_call reloc */
        return 0x7f;
    }
    if (qs == 0x7f) {
        /* done: walk both player rows and act on occupied slots */
        for (int p = 0; p < 2; p++) {
            Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (p & 1) * 0x868);
            for (int s = 0; s < 5; s++) {
                Ov002Slot *slot = &row->slots[s];
                if (slot->id_lo13 == 0) continue;   /* :13 bitfield */
                func_ov002_slot_action(self, p, s, slot->id_lo13);
            }
        }
        return 0;
    }
    return 0;
}
```

Note: `func_ov002_021ae320` is known from the USA reloc; its EUR equivalent address must be verified. The exact inner body and additional callee calls require baserom disassembly. The 384B size suggests multiple call sites beyond the loop body.

## Closest matched example
**Path:** src/overlay002/func_ov002_022157e4 (Ov002EFRetriage.md sketch)
**Why similar:** Same DuelQueueState+0x5A8 handshake (0x7F done / 0x80 active), same slot walk with id_lo13 guard, same early-return 0x7F pattern on the active branch.
**Key lesson:** The `arm_call to:0x021ae320` reloc is in the USA binary; in EUR the address may differ by the USA/EUR text delta. Verify the EUR equivalent using `config/eur/arm9/overlays/ov002/symbols.txt` before hard-coding the call target in the C file.

## Try this
1. Extract the assembly from the EUR baserom first — run `tools/batch_carve.py --min-addr 0x0 --overlay ov002 --start 0x02225ab8 --count 1` to generate the .s file; then verify the EUR address of `func_ov002_021ae320` (the USA callee) in `config/eur`.
2. Once the .s is available: load `dqs->f_5a8` (+0x5A8) as the primary dispatch; if it is a four-value range switch (0x7D–0x80) rather than a two-value test, write it as a `switch` statement to generate the `addls pc, pc, r1, lsl #2` jump table.
3. Declare PPT stride as `0x868` and use id_lo13:13 bitfield — if first-build objdiff shows only decl-order register mismatches (no structural difference), swap the declaration order of `p` and `s` to move the player counter to the lower callee-saved register.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

