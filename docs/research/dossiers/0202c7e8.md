# 0202c7e8 — func_0202c7e8 (main, class D, 340B)

## Recipe + Risk
**Recipe:** D: bitfield pack ((lo&m)|((hi<<16)&(m<<16))) replicated per-case via branch table
**Risk:** each case must reload _LIT0 mask and recompute the and/and/orr in the SAME order (mov r3,m<<16; and r1; and r0; orr) — but mwcc will likely hoist the invariant pack ONCE before the switch instead of per-case, collapsing 8 identical blocks. To match the duplicated bodies the pack must NOT be hoisted: permuter-class / may need per-case inline.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0202c7e8 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: bitfield pack ((lo&m)|((hi<<16)&(m<<16))) replicated per-case via branch table
 *   risk:       each case must reload _LIT0 mask and recompute the and/and/orr in the SAME order (mov r3,m<<16; and r1; and r0; orr) — but mwcc will likely hoist the invariant pack ONCE before the switch instead of per-case, collapsing 8 identical blocks. To match the duplicated bodies the pack must NOT be hoisted: permuter-class / may need per-case inline.
 *   confidence: low
 */
// func_0202c7e8 — pack two 9-bit fields and store to one of 8 globals by selector.
// value = (lo & 0x1ff) | ((hi << 16) & (0x1ff << 16)); written to gN per case.
// cases 0..8 with case 0 and 5 = no-op (bx lr); 1->g1,2->g2,3->g3,4->g4,6->g5,7->g6,8->g7,9? .
extern volatile unsigned int data_g1;  // _LIT1
extern volatile unsigned int data_g2;  // _LIT2
extern volatile unsigned int data_g3;  // _LIT3
extern volatile unsigned int data_g4;  // _LIT4
extern volatile unsigned int data_g5;  // _LIT5
extern volatile unsigned int data_g6;  // _LIT6
extern volatile unsigned int data_g7;  // _LIT7
extern volatile unsigned int data_g8;  // _LIT8

void func_0202c7e8(int sel, unsigned int lo, unsigned int hi)
{
    unsigned int packed = (lo & 0x1ff) | ((hi << 16) & (0x1ffu << 16));
    switch (sel) {
    case 1: data_g1 = packed; break;
    case 2: data_g2 = packed; break;
    case 3: data_g3 = packed; break;
    case 4: data_g4 = packed; break;
    case 6: data_g5 = packed; break;
    case 7: data_g6 = packed; break;
    case 8: data_g7 = packed; break;
    default: break;  /* sel 0,5,>8 -> bx lr; LIT8/g8 case is unreached idx9 */
    }
}
```

## Struct context
No struct fields. Pure arithmetic on the three integer arguments. The destination globals (`data_g1`..`data_g7`) are the real addresses mapped from `_LIT1`.._LIT7 in the prep — actual symbol names need confirmation from the `.s`. Does not touch GameSingleton or SystemWork.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| (none) | — | — | Pack and store to one of 7 per-case globals |

## Closest matched example
**Path:** src/main/func_0202c730.c
**Why similar:** Same `addls pc, pc, r0, lsl #2` dense table dispatch with uniform per-case bodies, same small function frame.
**Key lesson:** The defining challenge is that the asm replicates the entire `and/and/orr` pack sequence inside EACH case body rather than hoisting it before the switch. mwcc will hoist any expression that is invariant across all branches. The only known C reshaping to prevent hoisting is to move the pack expression into each `case` body as a separate local variable (computed inline per case, not shared). Even so, mwcc may still hoist; this is permuter-class if it does.

## Try this
1. Move the pack expression into each case body separately — do NOT compute `packed` before the switch. Write each case as `data_gN = (lo & 0x1ff) | ((hi << 16) & (0x1ffu << 16));` with no shared variable. This prevents mwcc from seeing the computation as loop-invariant.
2. If objdiff shows mwcc still hoisted (single pack before the branch table), declare `lo` and `hi` as `volatile` parameters or insert `__asm__` barriers — but on mwcc these are unavailable. At that point escalate to permuter.
3. Park as permuter-class; the invariant-hoist cannot be prevented via pure C reshape on mwcc without macOS permuter access.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x000001ff` | MMIO/const |
| `_LIT1` | `0x04000010` | MMIO/const |
| `_LIT2` | `0x04000014` | MMIO/const |
| `_LIT3` | `0x04000018` | MMIO/const |
| `_LIT4` | `0x0400001c` | MMIO/const |
| `_LIT5` | `0x04001010` | MMIO/const |
| `_LIT6` | `0x04001014` | MMIO/const |
| `_LIT7` | `0x04001018` | MMIO/const |
| `_LIT8` | `0x0400101c` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

