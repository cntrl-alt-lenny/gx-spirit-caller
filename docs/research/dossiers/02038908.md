# 02038908 — func_02038908 (main, class D, 80B)

## Recipe + Risk
**Recipe:** store s1(+0x60) before s0(+0x5c); orr/eor shift chain; attempt reload via repeated g-> reads
**Risk:** VERIFIED: store-order (+0x60 then +0x5c) and eor/orr shift chain match. BUT mwcc -O4 CSE'd the 8 redundant ldr (orig reloads s1 3x/s0 4x) into 2 loads, and folds s1+s1 to s1<<1. permuter-class/opt-level (redundant reloads need volatile or lower -O)

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02038908 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store s1(+0x60) before s0(+0x5c); orr/eor shift chain; attempt reload via repeated g-> reads
 *   risk:       VERIFIED: store-order (+0x60 then +0x5c) and eor/orr shift chain match. BUT mwcc -O4 CSE'd the 8 redundant ldr (orig reloads s1 3x/s0 4x) into 2 loads, and folds s1+s1 to s1<<1. permuter-class/opt-level (redundant reloads need volatile or lower -O)
 *   confidence: low
 */
/* func_02038908 (class D, PRNG, store-order + reload): state at
 * data_0219b2e0 with s0 @ +0x5c, s1 @ +0x60. Orig RELOADS each global rather
 * than caching. Update s1 FIRST then s0:
 *   s1' = s1 + s1 + (s0 & 1);                       // store +0x60
 *   t   = (s0_old >> 1) | (s1_old << 31);
 *   t  ^= s0 << 12;
 *   t  ^= t >> 20;
 *   s0' = t;                                        // store +0x5c
 *   return s0';
 * s1_old and (s0>>1) are captured before the s1 store. */

extern unsigned char data_0219b2e0[];

struct Rng {
    char _p[0x5c];
    unsigned int s0;            /* 0x5c */
    unsigned int s1;            /* 0x60 */
};

int func_02038908(void) {
    struct Rng *g = (struct Rng *)data_0219b2e0;
    unsigned int s1_old;
    unsigned int s0_sr;
    unsigned int t;

    s1_old = g->s1;
    s0_sr = g->s0 >> 1;
    g->s1 = g->s1 + g->s1 + (g->s0 & 1);

    t = s0_sr | (s1_old << 31);
    t = t ^ (g->s0 << 12);
    t = t ^ (t >> 20);
    g->s0 = t;
    return g->s0;
}
```

## Struct context
Accesses `data_0219b2e0` global (same global used by many main functions) via a typed `Rng` struct:

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x5c (data_0219b2e0) | s0 | unsigned int | PRNG state word 0; read multiple times, stored second |
| +0x60 (data_0219b2e0) | s1 | unsigned int | PRNG state word 1; read multiple times, stored first |

## Closest matched example
**Path:** src/main/func_020388f0.c
**Why similar:** `func_020388f0` is the only other matched function that uses `volatile int` fields at offsets +0x5c and +0x60 of `data_0219b2e0`, and demonstrates the reload-via-volatile pattern.
**Key lesson:** This function is VERIFIED for store order and shift chain, but the residual is critical: the original emits 8 memory loads across the body (reloading `s0` and `s1` multiple times), while mwcc -O4 CSEs them to 2 loads. The only way to force repeated reloads in C is to declare `s0` and `s1` fields as `volatile`. See `func_020388f0.c` which already uses `volatile int` for the same struct offsets. However, adding `volatile` may also change the instruction sequence for the shift chain. Try `volatile` fields first; if it produces extra reloads but changes the orr/eor order, a different approach is needed. Also note: `g->s1 + g->s1` should not become `g->s1 << 1` if both reads are volatile.

## Try this
1. Add `volatile` to both `s0` and `s1` fields in `struct Rng` and rerun objdiff. This is the most likely way to prevent mwcc -O4 from CSE'ing the repeated loads, which is what the orig shows.
2. If volatile produces the right reload count but the `s1+s1` is still emitted as `lsl#1` instead of `add r,r,r`, try breaking the expression: `unsigned int s1a = g->s1; unsigned int s1b = g->s1; g->s1 = s1a + s1b + (g->s0 & 1);` — but only with volatile fields, otherwise mwcc will still CSE it.
3. Park as permuter-class if volatile does not reproduce the orig's 8-load pattern; opt-level-driven CSE suppression is un-forceable without a compiler flag change or macOS permuter.
