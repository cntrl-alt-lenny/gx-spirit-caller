[//]: # (markdownlint-disable MD013 MD041)

# 021f8348 — func_ov002_021f8348 (ov002, class F retriage, 328B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Reads `DuelQueueState+0x5B4` (pending-action flag) as a 3-way dispatch (0 / 1 / 2). In step 2, reads `DuelStateSingleton+0xD70`, `+0xD74`, `+0xD78` — three consecutive offset fields just above the documented range, used as arguments to `func_ov002_021d91e0`. These are named relative to the confirmed singleton base. The large stack frame (0x100 = 256 bytes) is a fixed-size buffer passed to `func_ov002_0229cf10` — not a variable-length structure.
**Confidence:** high
**Structs needed:** DuelQueueState (+0x5B4 pending-action flag), DuelStateSingleton (+0xD70, +0xD74, +0xD78 — sub-state args)

## Assembly

```asm
; func_ov002_021f8348 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022ce288
        .extern data_ov002_022d016c
        .extern func_ov002_021ae400
        .extern func_ov002_021d91e0
        .extern func_ov002_021f4194
        .extern func_ov002_021f82b8
        .extern func_ov002_022593a8
        .extern func_ov002_022593f4
        .extern func_ov002_0229cf10
        .global func_ov002_021f8348
        .arm
func_ov002_021f8348:
    stmdb sp!, {r4, lr}
    sub sp, sp, #0x100
    mov r4, r0
    ldrh r2, [r4, #0x2]
    mov r1, r2, lsl #0x14
    mov r1, r1, lsr #0x1a
    cmp r1, #0x23
    addeq sp, sp, #0x100
    moveq r0, #0x1
    ldmeqia sp!, {r4, pc}
    ldr r1, _LIT0
    ldr r1, [r1, #0x5b4]
    cmp r1, #0x0
    beq .L_4c
    cmp r1, #0x1
    beq .L_9c
    cmp r1, #0x2
    beq .L_cc
    b .L_130
.L_4c:
    bl func_ov002_021f4194
    mov r2, r0
    add r0, sp, #0x0
    mov r1, #0x11c
    bl func_ov002_0229cf10
    ldrh r0, [r4, #0x2]
    add r1, sp, #0x0
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_021ae400
    mov r0, r4
    bl func_ov002_021f4194
    strh r0, [r4, #0xc]
    ldr r1, _LIT0
    add sp, sp, #0x100
    ldr r2, [r1, #0x5b4]
    mov r0, #0x0
    add r2, r2, #0x1
    str r2, [r1, #0x5b4]
    ldmia sp!, {r4, pc}
.L_9c:
    ldrh r1, [r4]
    mov r0, r2, lsl #0x1f
    ldr r2, _LIT1
    mov r0, r0, lsr #0x1f
    bl func_ov002_022593a8
    ldr r1, _LIT0
    add sp, sp, #0x100
    ldr r2, [r1, #0x5b4]
    mov r0, #0x0
    add r2, r2, #0x1
    str r2, [r1, #0x5b4]
    ldmia sp!, {r4, pc}
.L_cc:
    bl func_ov002_022593f4
    cmp r0, #0x0
    beq .L_114
    ldr r0, _LIT2
    mvn r2, #0x0
    ldr r3, [r0, #0xd74]
    ldr r1, [r0, #0xd78]
    ldr r0, [r0, #0xd70]
    add r1, r3, r1
    bl func_ov002_021d91e0
    ldrh r0, [r4, #0xc]
    sub r0, r0, #0x1
    strh r0, [r4, #0xc]
    ldrh r0, [r4, #0xc]
    cmp r0, #0x0
    addeq sp, sp, #0x100
    moveq r0, #0x1
    ldmeqia sp!, {r4, pc}
.L_114:
    ldr r1, _LIT0
    add sp, sp, #0x100
    ldr r2, [r1, #0x5b4]
    mov r0, #0x0
    sub r2, r2, #0x1
    str r2, [r1, #0x5b4]
    ldmia sp!, {r4, pc}
.L_130:
    mov r0, #0x0
    add sp, sp, #0x100
    ldmia sp!, {r4, pc}
_LIT0: .word data_ov002_022ce288
_LIT1: .word func_ov002_021f82b8
_LIT2: .word data_ov002_022d016c
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5B4 | f_5b4 | `int` | 3-way step dispatch (0/1/2); incremented or decremented per step |
| DuelStateSingleton | +0xD70 | f_d70 | `int` | arg0 to func_ov002_021d91e0 |
| DuelStateSingleton | +0xD74 | f_d74 | `int` | added to f_d78 → arg1 to func_ov002_021d91e0 |
| DuelStateSingleton | +0xD78 | f_d78 | `int` | summed with f_d74 → arg1 to func_ov002_021d91e0 |
| self (r4) | +0x2 | f2 bits[11:6] | `unsigned short` | lsl#0x14/lsr#0x1a → 6-bit tag; compared to 0x23 |
| self (r4) | +0x2 | f2 bit0 | `unsigned short` | lsl#1f/lsr#1f → player bit |
| self (r4) | +0x0 | f0 | `unsigned short` | card id arg to func_022593a8 |
| self (r4) | +0xc | fc | `unsigned short` | counter: set in step 0, decremented in step 2; 0 → return 1 |

## Recipe sketch

```c
int func_ov002_021f8348(SelfObj *self) {
    unsigned short raw_f2 = self->f2;
    /* early exit: tag field == 0x23 */
    if (((raw_f2 << 0x14) >> 0x1a) == 0x23) return 1;
    DuelQueueState *dqs = &data_ov002_022ce288;
    int step = dqs->f_5b4;
    if (step == 0) {
        int count = func_ov002_021f4194(self);
        char buf[0x100];
        func_ov002_0229cf10(buf, 0x11c, count);
        unsigned int player_bit = (unsigned int)raw_f2 << 31 >> 31;
        func_ov002_021ae400(player_bit, buf);
        int new_count = func_ov002_021f4194(self);
        self->fc = (unsigned short)new_count;
        dqs->f_5b4++;
        return 0;
    }
    if (step == 1) {
        unsigned int player_bit = (unsigned int)raw_f2 << 31 >> 31;
        func_ov002_022593a8(player_bit, self->f0, func_ov002_021f82b8);
        dqs->f_5b4++;
        return 0;
    }
    if (step == 2) {
        if (!func_ov002_022593f4()) {
            dqs->f_5b4--;
            return 0;
        }
        DuelStateSingleton *dss = &data_ov002_022d016c;
        int a0 = dss->f_d70;
        int a1 = dss->f_d74 + dss->f_d78;
        func_ov002_021d91e0(a0, a1, -1);
        self->fc--;
        if (self->fc == 0) return 1;
        return 0;
    }
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_021f85f8.c
**Why similar:** Same DuelQueueState `f_5b4` step-dispatch pattern with increment/decrement on success/fail.
**Key lesson:** The `mvn r2, #0x0` before the `func_ov002_021d91e0` call encodes `-1` as the third argument — use the literal `-1` not `0xffffffff` in C. The `ldr r3,[r0,#0xd74]; ldr r1,[r0,#0xd78]; ldr r0,[r0,#0xd70]` load order means arg1 = f_d74+f_d78, arg0 = f_d70 — load order matters for mwcc register assignment.

## Try this
1. Load `dss->f_d70`, `dss->f_d74`, `dss->f_d78` in that order (d70 last into r0 since it becomes arg0), then call `func_ov002_021d91e0(dss->f_d70, dss->f_d74 + dss->f_d78, -1)`.
2. If objdiff shows the `sub sp, sp, #0x100` stack frame missing, ensure `char buf[0x100]` (or `int buf[0x40]`) is declared as a named local array — the compiler must emit the stack allocation.
3. If step-counter `f_5b4` increments don't match, confirm the re-load pattern: after incrementing `dqs->f_5b4++`, the assembly re-loads the field (not a cached copy) — write through `dqs->f_5b4` directly each time.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov002_022ce288` | raw |
| `_LIT1` | `func_ov002_021f82b8` | raw |
| `_LIT2` | `data_ov002_022d016c` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021f4194` | overlay call (ov002) |
| `bl` | `func_ov002_0229cf10` | overlay call (ov002) |
| `bl` | `func_ov002_021ae400` | overlay call (ov002) |
| `bl` | `func_ov002_022593a8` | overlay call (ov002) |
| `bl` | `func_ov002_022593f4` | overlay call (ov002) |
| `bl` | `func_ov002_021d91e0` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

