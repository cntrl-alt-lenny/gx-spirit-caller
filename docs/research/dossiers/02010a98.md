# 02010a98 — func_02010a98 (main, class C, 112B)

## Recipe + Risk

**Recipe:** C: mla idx*0x18 inline, 16-bit wrap via (u16)(idx+1), movcs reset, reload f42
**Risk:** wrap uses lsl#0x10;lsr#0x10 (u16 truncate) and movcs(>=0x100) resets to a held zero reg (ip); reloads limit f42 each iter. if mwcc keeps idx in 32-bit or hoists the limit load, the cmp/wrap diverges. reshape-able via u16 + per-iter reload

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02010a98 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     C: mla idx*0x18 inline, 16-bit wrap via (u16)(idx+1), movcs reset, reload f42
 *   risk:       wrap uses lsl#0x10;lsr#0x10 (u16 truncate) and movcs(>=0x100) resets to a held zero reg (ip); reloads limit f42 each iter. if mwcc keeps idx in 32-bit or hoists the limit load, the cmp/wrap diverges. reshape-able via u16 + per-iter reload
 *   confidence: low
 */
/* func_02010a98 (main, class C) — table scan with mla index + 16-bit wrap counter.
 * A=data_0218fcc8 (head: cur idx @+0x40, limit @+0x42). T=data_0218cbc8.
 * tbl base = T+0x1940 (stride 0x18). lim2 ptr = T+0x3100, reads f42.
 * Loop: e=base+idx*0x18; if e->f8==arg0 clear it; idx=(idx+1)&0xffff wrap;
 * if idx>=0x100 idx=0; until idx==lim2. Returns 1. */

typedef struct { int pad0[2]; int f8; } Ent;  /* f8 @ 0x8, stride 0x18 */

extern char data_0218fcc8;  /* A */
extern char data_0218cbc8;  /* T */

int func_02010a98(int target)
{
    char *A = &data_0218fcc8;
    char *T = &data_0218cbc8;
    unsigned short idx = *(unsigned short *)(A + 0x40);
    unsigned short first = *(unsigned short *)(A + 0x42);
    if (idx != first) {
        char *base = T + 0x1940;
        char *lim  = T + 0x3100;
        unsigned short zero = 0;
        do {
            Ent *e = (Ent *)(base + idx * 0x18);
            if (e->f8 == target) e->f8 = 0;
            idx = (unsigned short)(idx + 1);
            if (idx >= 0x100) idx = zero;
        } while (idx != *(unsigned short *)(lim + 0x42));
    }
    return 1;
}
```

## Struct context

Two globals are accessed:
- `data_0218fcc8` — control block; `+0x40` = current u16 index, `+0x42` = limit u16
- `data_0218cbc8` — table/arena block; `+0x1940` = entry array base (stride 0x18), `+0x3100` = secondary limit pointer block (also reads `+0x42` offset)

| offset | field | type | use |
|--------|-------|------|-----|
| A+0x40 | idx | u16 | current scan position in entry ring |
| A+0x42 | first | u16 | initial limit (loop pre-condition) |
| lim+0x42 | lim2 | u16 | per-iter reload of limit (do NOT cache as local) |
| entry+0x8 | f8 | int | match field cleared when equal to `target` |

Neither global maps to SystemWork or GlobalAudioState. The `0x18`-stride entry array at `T+0x1940` is the same stride-24 entry array pattern noted in struct-context-main.md for the SystemWork sub-array, but rooted in a different global.

## Closest matched example

**Path:** src/main/func_02018bc0.c
**Why similar:** Index arithmetic with stride multiplication (mla pattern) to compute a pointer into a flat array, and a do-while counter loop.
**Key lesson:** The u16 wrap requires `idx` to be declared `unsigned short` (not `int`) so MWCC emits `lsl#0x10;lsr#0x10` truncation after the increment. The `movcs` reset to zero requires `if (idx >= 0x100) idx = zero;` where `zero` is a local held in `ip` — if MWCC hoists the zero literal as an immediate, the `movcs` form is lost; hold it in an explicit `unsigned short zero = 0` local. The limit reload `*(unsigned short *)(lim + 0x42)` must remain inside the loop condition (no hoisted copy) or MWCC will emit a single load before the loop instead of reloading each iteration.

## Try this

1. Build as-is; in objdiff verify the loop body emits `mla` for `base + idx * 0x18`. If MWCC pre-computes the base offset outside the loop, remove the `base` local and write `(Ent *)(&data_0218cbc8 + 0x1940 + idx * 0x18)` inline so the index multiply stays inside the loop.
2. If the u16 wrap diverges (32-bit `cmp` vs 16-bit `cmp`): check that `idx` is `unsigned short` at the declaration site. If objdiff shows `cmp r_idx, #0x100` without the `lsl;lsr` pair, the compiler promoted to int — cast explicitly: `idx = (unsigned short)(idx + 1);`.
3. When to park: if the `movcs` reset and the limit reload cannot both be forced simultaneously by C reshape, this is permuter-class scheduling — accept as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0218fcc8` | data symbol |
| `_LIT1` | `data_0218cbc8` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

