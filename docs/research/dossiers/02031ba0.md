# 02031ba0 — func_02031ba0 (main, class C, 124B)

## Recipe + Risk
**Recipe:** CLASS C: load r5=f4,r6=f0,r7=f2c early (decl-order); result temp inited 0.
**Risk:** The early hoist of prev(r5)/node(r6)/f2c(r7) before the conditional blx is liveness-driven; if mwcc loads them lazily after the call the ldr order diverges. reshape-able via decl-order/early-read placement.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02031ba0 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS C: load r5=f4,r6=f0,r7=f2c early (decl-order); result temp inited 0.
 *   risk:       The early hoist of prev(r5)/node(r6)/f2c(r7) before the conditional blx is liveness-driven; if mwcc loads them lazily after the call the ldr order diverges. reshape-able via decl-order/early-read placement.
 *   confidence: med
 */
/* func_02031ba0: doubly-linked-list unlink with two callbacks + flag tests.
 *   obj->f8 = data_0219adb8[+0x10] (install a fn ptr)
 *   if (obj->fc) result = obj->fc()            ; blx
 *   flags = obj->f2c
 *   if (flags & 0x8000) && result==1: return obj->f4
 *   unlink: prev=obj->f4, node=obj->f0 ; prev->f4=... node->f0=...
 *   if (!(flags & 0x4000) && obj->f8) obj->f8(obj)
 *   return obj->f4
 */

typedef struct { char _pad[0x10]; void *f10; /* +0x10 */ } Mgr_02031ba0;
extern Mgr_02031ba0 data_0219adb8;

typedef int (*cb0_t)(void);
typedef void (*cb1_t)(void *);

struct Node_02031ba0 {
    struct Node_02031ba0 *f0;   /* +0x0 */
    struct Node_02031ba0 *f4;   /* +0x4 */
    void                 *f8;   /* +0x8 */
    cb0_t                 fc;   /* +0xc */
    char                  _pad[0x1c];
    int                   f2c;  /* +0x2c */
};

void *func_02031ba0(struct Node_02031ba0 *obj) {
    struct Node_02031ba0 *prev, *node;
    int flags;
    int result = 0;

    obj->f8 = data_0219adb8.f10;
    if (obj->fc) {
        result = obj->fc();
    }
    prev = obj->f4;
    node = obj->f0;
    flags = obj->f2c;
    if (flags & 0x8000) {
        if (result == 1) return prev;
    }
    node->f4 = prev;
    prev->f0 = node;
    if (!(flags & 0x4000)) {
        if (obj->f8) ((cb1_t)obj->f8)(obj);
    }
    return prev;
}
```

## Struct context
No GameSingleton/SystemWork fields. The function operates on a generic doubly-linked list node with callback fields and a flags word.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| obj+0x00 | f0 (next/fwd) | Node* | read into `node` (r6); used to patch node->f4 |
| obj+0x04 | f4 (prev/back) | Node* | read into `prev` (r5); return value |
| obj+0x08 | f8 (fn ptr) | void* | written from data_0219adb8.f10; called if !(flags&0x4000) |
| obj+0x0c | fc (callback) | cb0_t | tested and blx'd; returns result |
| obj+0x2c | f2c (flags) | int | read into `flags` (r7); bits 0x8000 and 0x4000 tested |

## Closest matched example
**Path:** src/main/func_020320c8.c
**Why similar:** Same `movs rN,r0` alloc-guard / early-return shape and callback-dispatch via indirect blx; similar size (60B vs 124B).
**Key lesson:** Declaring `prev`, `node`, and `flags` before the `if (obj->fc)` block forces mwcc to emit the three loads (ldr r5=obj->f4, ldr r6=obj->f0, ldr r7=obj->f2c) into callee-saved registers before the conditional blx, which matches the assembly liveness order. Initializing `result = 0` at declaration (not inside the if) pins the `mov rN,#0` above the call.

## Try this
1. Declare `prev`, `node`, and `flags` at the top of the function body (before the `if (obj->fc)` block) and add explicit reads — e.g., `prev = obj->f4; node = obj->f0; flags = obj->f2c;` — right after the `obj->f8 = ...` store. This forces mwcc to hoist the three ldr into r5/r6/r7 before the blx.
2. If objdiff shows the loads happen after the blx instead, wrap the three reads in a dummy `(void)(prev + node + flags)` after them — the forced use pins the loads above the call.
3. If the `!(flags & 0x4000)` guard on obj->f8 still diverges (extra branch or merged condition), split it into two nested `if` statements rather than `&&`.
