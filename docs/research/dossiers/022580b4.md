# 022580b4 — func_ov002_022580b4 (ov002, class F retriage, 380B)

## Re-triage
**Original verdict:** F — previously intractable (high confidence)
**Upgrade reason:** This function accesses DuelQueueState and Ov002Slot. The original "intractable" verdict reflected opaque struct offsets. With DuelQueueState fields (+0x5A8 queue_state, +0x5C0 re-entrancy lock) and the Ov002Slot id_lo13 bitfield (lsl#19/lsr#19) now documented, the per-slot scan and queue dispatch are fully deterministic. The function is embedded within the large block func_ov002_02257db4 (EUR delink range 0x02257DB4–0x02258168). It is not yet a standalone .s file; it must be carved from that block before independent C-matching.
**Confidence:** high
**Structs needed:** DuelQueueState (+0x5A8 queue_state, +0x5C0 re-entrancy lock), Ov002Slot (id_lo13 bitfield lsl#19/lsr#19, +0x00 word at row+0x30+slot*0x14)

## Assembly

```asm
; NOTE: func_ov002_022580b4 is NOT a standalone .s file in EUR.
; It resides at offset +0x300 within func_ov002_02257db4.s
; (EUR delink block 0x02257DB4–0x02258168, size=0x3B4).
; The containing block func_ov002_02257db4.s is a large card-type dispatch
; function unrelated to this sub-function's struct accesses.
; Assembly must be extracted from the binary at address 0x022580B4.
; Extern references (from surrounding block context):
;   data_ov002_022ce288 (DuelQueueState base)
;   data_ov002_022cf16c (PerPlayerRowTable base)
;   func_ov002_021b9ecc, func_ov002_021df62c, func_ov002_021d6870
; Estimated size: 380 bytes (~95 ARM instructions).
; See src/overlay002/func_ov002_02257db4.s for the containing block.
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state: 0x7f=done, 0x80=active; dispatch guard |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock (0=free); skip if nonzero |
| Ov002Slot | row+0x30+s*0x14 (+0x00) | id_lo13 | u32:13 | lsl#19/lsr#19 bitfield extraction |
| Ov002Slot | row+0x30+s*0x14 (+0x40) | flag word | u32 | slot occupancy / flag bits |

## Recipe sketch

```c
/* F-class DuelQueueState + Ov002Slot scan, 380 bytes.
 * Pattern: check dqs->f_5a8 state, guard on f_5c0 re-entrancy,
 * then walk PerPlayerRowTable slots reading id_lo13 bitfield. */
int func_ov002_022580b4(int player, int slot, int arg2) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    if (dqs->f_5c0) return 0;              /* +0x5C0 re-entrancy lock */
    int qs = dqs->f_5a8;                   /* +0x5A8 queue_state */
    if (qs != 0x7f && qs != 0x80) return 0;
    Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c
                          + (player & 1) * 0x868);
    for (int s = 0; s <= 4; s++) {
        unsigned int word = row->slots[s].id_lo13_word; /* +0x30+s*0x14 */
        int id = (int)(word << 19) >> 19;              /* lsl#19/lsr#19 */
        if (id == 0) continue;
        /* dispatch via func_ov002_021df62c, func_ov002_021d6870 etc. */
        func_ov002_021df62c(player, s, arg2);
    }
    return 1;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_022157e4.c (retriage doc recipe)
**Why similar:** Same DuelQueueState+0x5A8 state test (0x7f/0x80) gating a slot iteration; class F, high confidence.
**Key lesson:** The re-entrancy lock at +0x5C0 must be tested first — missing it shifts the entire branch structure. The `lsl#19/lsr#19` id_lo13 extraction is the canonical Ov002Slot pattern and must use `: 13` bitfield syntax in C, not `& 0x1FFF`.

## Try this
1. Before any slot iteration, load `dqs->f_5c0` (`+0x5C0`) and return 0 if nonzero. Then load `dqs->f_5a8` (`+0x5A8`) and guard for `0x7f` / `0x80`.
2. This function is not yet delinked as a standalone .s in EUR — carve it from `func_ov002_02257db4.s` at offset +0x300 using `batch_carve.py` or `dsd delink` before attempting to match.
3. If the DuelQueueState path doesn't produce the right branch shape after step 1, escalate: the function may test additional DQS fields like `+0x5B4` (pending-action flag) before the slot walk.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

