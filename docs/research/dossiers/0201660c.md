# 0201660c — func_0201660c (main, class D, 436B)

## Recipe + Risk
**Recipe:** Outer w[0x920] jump-table (no bit0 guard) with nested w[0x924] table in case4; unsigned bound returns; bic|orr set
**Risk:** permuter-class: inner bit0 `and #1` wall (case3) + double jump-table register/scheduling; reshape-able: both case-0=return numbering and unsigned +1 bound polarity. Two tables raise reg-alloc coin-flip odds

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0201660c (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     Outer w[0x920] jump-table (no bit0 guard) with nested w[0x924] table in case4; unsigned bound returns; bic|orr set
 *   risk:       permuter-class: inner bit0 `and #1` wall (case3) + double jump-table register/scheduling; reshape-able: both case-0=return numbering and unsigned +1 bound polarity. Two tables raise reg-alloc coin-flip odds
 *   confidence: low
 */
/* func_0201660c (main, class D, MED) — GetSystemWork state machine.
 * w[0x8d8] is NOT guarded here; w[0x920] (0..4 jump table, case0=return) selects
 * the handler; case 4 contains a NESTED w[0x924] jump-table (0..4, case0=return).
 * o[8] message words 0x1b0100..0x1b0403. Modeled on src/main/func_0201660c.s. */
extern char *GetSystemWork(void);
extern int  func_020139c4(void);
extern int  func_02019034(void);
extern int  func_020190ec(void);
extern int  func_020195ec(void);
extern int  func_02019664(void);
extern void func_0201aabc(int);
extern void func_0201aaf4(int);
extern void func_0201b708(int);
extern void func_0201b730(int);
extern void func_0201b7b4(void);
extern int  func_0201b7e0(void *);

void func_0201660c(void *o) {
    char *w = GetSystemWork();
    switch (*(int *)(w + 0x920)) {
    case 1:
        if ((unsigned int)func_02019034() < (unsigned int)(*(int *)(w + 0x8dc) + 1)) return;
        if (func_020190ec() == 0) return;
        if (func_020139c4() != 5) return;
        *(int *)((char *)o + 8) = 0x001b0100;
        func_0201aabc(0x78);
        *(int *)(w + 0x900) |= 0x1000000;
        return;
    case 2:
        if (func_0201b7e0(o) != 3) return;
        if (func_02019664() != 0x78) return;
        *(int *)((char *)o + 8) = 0x001b0200;
        func_0201aaf4(0x78);
        func_0201b708(0xa);
        return;
    case 3:
        if (func_02019664() != 0x6f) return;
        *(int *)((char *)o + 8) = 0x001b0300;
        func_0201b7b4();
        *(int *)(w + 0x900) &= ~0x1000000;
        return;
    case 4:
        switch (*(int *)(w + 0x924)) {
        case 1:
            *(int *)(w + 0x924) = 4;
            /* fallthrough */
        case 4:
            if (func_02019664() != 0x6f) return;
            if ((unsigned int)func_02019034() < (unsigned int)(*(int *)(w + 0x8dc) + 1)) return;
            *(int *)((char *)o + 8) = 0x001b0400;
            *(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;
            return;
        case 3:
            if (((*(unsigned int *)(w + 0x8e0) << 0x1f) >> 0x1f)) return;
            if (func_020195ec() == 0)
                *(int *)((char *)o + 8) = 0x001b0401;
            else
                *(int *)((char *)o + 8) = 0x001b0402;
            return;
        case 2:
            *(int *)((char *)o + 8) = 0x001b0403;
            func_0201b730(0xa);
            return;
        }
        return;
    }
}
```

## Struct context

All offsets are from `GetSystemWork()` (SystemWork, BSS `data_02104f58`, size 0x92c).
Note: `+0x8d8` is NOT checked in this function — the outer `switch` on `+0x920` starts from case 1 with no enable guard.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x8dc | bound word | s32 | lower-bound for unsigned compare: `w[0x8dc]+1` in cases 1 and 4 |
| +0x8e0 | bitfield word | s32 | bit0 `(v<<0x1f)>>0x1f` guard in inner case-3; bic\|orr `(x&~1)\|1` sets bit0 in inner case-4 |
| +0x900 | flag word | s32 | OR'd with 0x1000000 in case-1; AND'd with ~0x1000000 in case-3 |
| +0x920 | mode/state | s32 | outer `addls` jump-table (cmp#4, entries 0..4; case0=implicit return since no case 0) |
| +0x924 | sub-state | s32 | nested `addls` jump-table inside case-4 (cmp#4, entries 1,2,3,4; case1 self-sets to 4) |

## Closest matched example
**Path:** src/main/func_02015494.c (near-sibling)
**Why similar:** Same double jump-table structure (`w[0x920]` outer + `w[0x924]` inner) and same `bic|orr` `(x&~1)|1` idiom — but without the outer bit0 guard on `+0x8d8`.
**Key lesson:** The absence of the `+0x8d8` enable guard is significant — the outer `switch` starts directly without a bitfield test. The inner case-3 bit0 guard `(v<<0x1f)>>0x1f` on `+0x8e0` is still the `and #1` wall. The two `addls` tables mean register allocation and scheduling are a coin-flip; if the outer table layout is correct but the inner one diverges, there is little C reshaping possible.

## Try this
1. Drop the prep draft; check in objdiff that case-0 of `+0x920` is handled implicitly (no `case 0:` in the C) and that mwcc generates `cmp r?,#4; addlscc pc,pc,r?,lsl#2` for the outer table.
2. If the inner jump-table in case-4 schedules with wrong register ordering vs the `.s`, try extracting the inner `w[0x924]` value to a local variable before the `switch` to force mwcc's load scheduling.
3. If the inner bit0 `and #1` wall diverges or either table layout flips, park as permuter-class and ship as `.s` — two stacked `addls` tables are un-forceable without the macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x001b0100` | MMIO/const |
| `_LIT1` | `0x001b0200` | MMIO/const |
| `_LIT2` | `0x001b0300` | MMIO/const |
| `_LIT3` | `0x001b0400` | MMIO/const |
| `_LIT4` | `0x001b0401` | MMIO/const |
| `_LIT5` | `0x001b0402` | MMIO/const |
| `_LIT6` | `0x001b0403` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_02019034` | main TU call |
| `bl` | `func_020190ec` | main TU call |
| `bl` | `func_020139c4` | main TU call |
| `bl` | `func_0201aabc` | main TU call |
| `bl` | `func_0201b7e0` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_0201aaf4` | main TU call |
| `bl` | `func_0201b708` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201b730` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

