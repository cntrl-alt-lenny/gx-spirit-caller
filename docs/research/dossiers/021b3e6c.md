# 021b3e6c — func_ov017_021b3e6c (ov017, class D, 220B)

## Recipe + Risk

**Recipe:** D: two-Cell update; different fields per Cell; guard-each
**Risk:** two Cells: different attr/flag values. guard: check ptr for each Cell.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021b3e6c (ov017, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     cmp-chain dispatch; volatile-unsigned FLG read (2 loads+lsr); reuse live r3=1 on final call
 *   risk:       struct-guessed: only the _LIT0/2/3 -> data_02104f4c/_021b8894 symbol identities are inferred; codegen matched in disasm, so any diff is a relocation/literal-pool name fix.
 *   confidence: high
 */
/* CAMPAIGN-PREP candidate for func_ov017_021b3e6c (ov017, class C, MED) — batch p_0138.
 * DISASM-VERIFIED against target (arm-none-eabi-objdump on the mwcc 2.0/sp1p5 -O4,p .o):
 * the full dispatch chain, both func_02094504 calls, and the final func_02037208(0x3a)
 * (which REUSES the live r3=1 from `s->w_04=1`, emitting no `mov r3`) match byte-for-byte,
 * and the .L_a48 bit-pack now matches exactly (two loads + lsr) via the volatile-unsigned read. */

extern int  func_02037208(int a, int b, int c, int d);
extern int  func_0208e1ac(void);
extern int  func_0208e0a0(void);
extern void func_02094504(int val, void *dst, int len);
extern void func_ov017_021b4570(void *a, int b);

extern char data_02104f4c[];          /* G: large global, +0xa64 / +0xa74 (_LIT0) */
extern char data_ov017_021b8610[];    /* data_in: byte +0x1ce (_LIT1) */
extern char data_ov017_021b8894[];    /* FLG word (_LIT2) + halfword target (_LIT3/_LIT4) */

int func_ov017_021b3e6c(char *s) {
    int m;
    char *g;
    int w;
    unsigned int field;

    m = *(int *)(s + 0xc);
    if (m == 4) {
        func_02037208(0x42, 0x42 - 0x43, 0, 1);
        return 1;
    }
    if (m == 2) {
        g = data_02104f4c;
        if (((*(int *)(g + 0xa64) & 0x1f00000) >> 0x14) == 0
                && *(int *)(g + 0xa74) == 0) {
            func_02037208(0x43, 0x43 - 0x44, 0, 1);
            return 0;
        }
    } else if (m == 1) {
        int b = *(unsigned char *)(data_ov017_021b8610 + 0x1ce);
        if (b == 6 || b == 0xb) {
            func_02037208(0x43, 0x43 - 0x44, 0, 1);
            return 0;
        }
    }

    /* .L_a48 : target reads FLG TWICE (not CSE'd) and shifts the field LOGICALLY.
     * volatile-unsigned read reproduces both (two ldr + lsr); store stays plain str. */
    field = (((volatile unsigned int *)(data_ov017_021b8894 + 0))[0] & 0x1f00) >> 8;
    w     = ((volatile unsigned int *)(data_ov017_021b8894 + 0))[0];
    ((volatile unsigned int *)(data_ov017_021b8894 + 0))[0] =
        (w & ~0x1f00) | ((field & ~6) << 8);

    func_02094504(0, (void *)func_0208e1ac(), 0x800);
    func_02094504(0, (void *)func_0208e0a0(), 0x800);
    *(short *)(data_ov017_021b8894 + 0x10) = 0;
    func_ov017_021b4570((void *)(data_ov017_021b8894 + 0x20), *(int *)(s + 0xc));
    *(int *)(s + 4) = 1;
    func_02037208(0x3a, 0x3a - 0x3b, 0, 1);
    return 0;
}
```

## Struct context

The prep file uses raw pointer arithmetic throughout. Key globals:

| symbol | offset | type | purpose |
|--------|--------|------|---------|
| `data_02104f4c + 0xa64` | 0xa64 | int | bitfield with 0x1f00000 field (bits 20–24) |
| `data_02104f4c + 0xa74` | 0xa74 | int | zero-check gate |
| `data_ov017_021b8610 + 0x1ce` | 0x1ce | u8 | mode byte (compared to 6 and 0xb) |
| `data_ov017_021b8894 + 0x00` | 0x00 | volatile u32 | FLG register-style word; bits 12:8 = field |
| `data_ov017_021b8894 + 0x10` | 0x10 | s16 | cleared to 0 after the RMW |
| `data_ov017_021b8894 + 0x20` | 0x20 | void * | context pointer passed to 021b4570 |
| `s + 0x04` | 0x04 | int | written 1 before final call (r3 reused live) |
| `s + 0x0c` | 0x0c | int | dispatch key m (4 / 2 / 1 / other) |

## Closest matched example

**Path:** `src/overlay017/func_ov017_021b4db0.c`
**Why similar:** Same RMW pattern on a volatile MMIO register: two loads (to get separate `field` and `w`), `bic/orr` to clear+set bits, then `str` back; same overlay.
**Key lesson from that file:** Declare the register pointer as `volatile unsigned int *`
so mwcc emits two `ldr` instructions (one for the AND+LSR to extract `field`, one for
the BIC+ORR to compute the new value). Without `volatile`, mwcc CSEs the load and emits
only one — which does not match the original.

## Try this

1. The prep file is marked DISASM-VERIFIED. Drop it directly into
   `src/overlay017/func_ov017_021b3e6c.c` and run `ninja objdiff` first.
2. If there is a mismatch, look at the literal-pool entries in objdiff —
   the most likely failure is a relocation name differing from what the linker
   expects (e.g. `data_02104f4c` vs a different extern declaration). Compare
   the `.word` entries in the pool against the delinks relocs for ov017.
3. If the dispatch chain branch order is wrong (e.g. `m == 1` arm before `m == 2`),
   reorder the `if/else if` chain to match the asm branch sequence exactly.
   Escalate to permuter only if the volatile-RMW block itself diverges.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02104f4c` | data symbol |
| `_LIT1` | `data_ov017_021b8894` | raw |
| `_LIT2` | `0x04001000` | MMIO/const |
| `_LIT3` | `0x04001050` | MMIO/const |
| `_LIT4` | `data_ov017_021b8610` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02037208` | main TU call |
| `bl` | `func_0208e1ac` | main TU call |
| `bl` | `func_02094504` | main TU call |
| `bl` | `func_0208e0a0` | main TU call |
| `bl` | `func_ov017_021b4570` | overlay call (ov017) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

