# 0208df94 — func_0208df94 (main, class D, 128B)

## Recipe + Risk
**Recipe:** D: addls-pc switch on DISPCNT&7; sb=(BG0CNT>>8)&0x1f; condmov on bit7
**Risk:** the if/movne/moveq tail (case4-6) must regen as movne/addne + moveq/addeq, not a branch; asm hoists sb before the switch. case0 and default(7) both ret0 (the 8th table slot is b .L_9c). reshape-able; condmov vs branch is the main coin-flip.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208df94 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: addls-pc switch on DISPCNT&7; sb=(BG0CNT>>8)&0x1f; condmov on bit7
 *   risk:       the if/movne/moveq tail (case4-6) must regen as movne/addne + moveq/addeq, not a branch; asm hoists sb before the switch. case0 and default(7) both ret0 (the 8th table slot is b .L_9c). reshape-able; condmov vs branch is the main coin-flip.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0208df94 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. recipe: D, branch-table (addls pc) over the
 * low 3 BG-mode bits of DISPCNT-sub (0x04001000); builds a VRAM/screen base
 * from BG0CNT-sub screen-base field ((0x0400100e>>8)&0x1f). func() -> u32 addr.
 *   mode = *0x04001000 & 7;  sb = (*0x0400100e & 0x1f00) >> 8;
 *   modes 1..3 : (sb<<11) + 0x6200000
 *   modes 4..6 : if BG0CNT&0x80 (sb<<14)+0x6200000 else (sb<<11)+0x6200000
 *   mode 0 / else : 0
 */
#include <nitro/types.h>

u32 func_0208df94(void)
{
    int mode = *(vu32 *)0x04001000 & 7;
    u16 bgcnt = *(vu16 *)0x0400100e;
    int sb = (bgcnt & 0x1f00) >> 8;

    switch (mode) {
    case 1:
    case 2:
    case 3:
        return (sb << 11) + 0x6200000;
    case 4:
    case 5:
    case 6:
        if (bgcnt & 0x80)
            return (sb << 14) + 0x6200000;
        return (sb << 11) + 0x6200000;
    default:
        return 0;
    }
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| `0x04001000` | DISPCNT sub-screen | `vu32` | low 3 bits = mode; read into `mode` before switch |
| `0x0400100e` | BG0CNT sub-screen | `vu16` | read into `bgcnt`; `(bgcnt & 0x1f00) >> 8` = screen-base field; `bgcnt & 0x80` = tile-size bit |

## Closest matched example
**Path:** src/main/func_0208e200.c
**Key lesson:** The `(sb << 11) + 0x6200000` and `(sb << 14) + 0x6200000` expressions are closely related to the VRAMCNT field-extraction pattern in `func_0208e200`/`Vram_GetBankBaseCD`. The key codegen fact is that orig computes `sb` from `bgcnt` BEFORE entering the switch (asm hoists the `lsr #8; and #0x1f` extraction above the `addls pc` table). Declaring `sb` as a local computed from `bgcnt` before the switch body matches this. For cases 4..6, the `if (bgcnt & 0x80)` conditional produces `movne/addne` + `moveq/addeq` predicated moves, not a branch — this only works if the two return arms differ only in the shift constant (11 vs 14).

## Try this
1. Confirm that `sb = (bgcnt & 0x1f00) >> 8` is computed before the switch, not inside case arms. If mwcc delays the extraction into the switch body, hoist it explicitly by declaring `int sb = (bgcnt & 0x1f00) >> 8;` as a separate statement before the switch.
2. For cases 4..6, the `if (bgcnt & 0x80)` with two simple `return (sb << K) + 0x6200000` arms must produce `tst bgcnt, #0x80` / `movne` / `addne` / `moveq` / `addeq` (predicated) — not a `bne`/branch. If objdiff shows a branch instead of predicated moves, try writing it as a ternary: `return ((bgcnt & 0x80) ? (sb << 14) : (sb << 11)) + 0x6200000;`.
3. The `default:` case and `case 0:` (implied by the addls-pc 8-slot table's 8th entry branching to `.L_9c`) both return 0. Ensure the switch has no explicit `case 0:` label — `case 0` falls into `default` in the orig, and an explicit `case 0: return 0;` might generate a separate arm rather than sharing the default block.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x04001000` | MMIO/const |
| `_LIT1` | `0x0400100e` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

