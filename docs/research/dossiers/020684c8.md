# 020684c8 — func_020684c8 (main, class C, 176B)

## Recipe + Risk
**Recipe:** alloc-guard via movs; re-read [p+0x18] for null test (orig reloads); init stores in exact asm order; r3=func_02068594 then 2 stacked
**Risk:** orig RELOADS [p+0x18] for the cmp instead of reusing h; if mwcc keeps h in a reg the second ldr is dropped. reshape-able (force reload by re-reading the field, as written).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_020684c8 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     alloc-guard via movs; re-read [p+0x18] for null test (orig reloads); init stores in exact asm order; r3=func_02068594 then 2 stacked
 *   risk:       orig RELOADS [p+0x18] for the cmp instead of reusing h; if mwcc keeps h in a reg the second ldr is dropped. reshape-able (force reload by re-reading the field, as written).
 *   confidence: med
 */
/* func_020684c8 (main, class C) — alloc + many init stores, decl-order */
extern void *func_020453e8(int size);
extern void  func_020453b4(void *p);
extern void *func_02054700(int a, int b, int c, void *cb0,
                           void *cb1, void *cb2);
extern void  func_02068580(void);
extern void  func_02068594(void);
extern void  func_020685a4(void);

void *func_020684c8(int unused, int a, short b) {
    unsigned char *p;
    void *h;

    p = func_020453e8(0x24);
    if (p == 0) return 0;

    h = func_02054700(8, 8, 4, func_02068594, func_02068580, func_020685a4);
    *(void **)(p + 0x18) = h;
    if (*(void **)(p + 0x18) == 0) {
        func_020453b4(p);
        return 0;
    }

    p[0x14] = 0;
    p[0x15] = 0;
    *(int *)(p + 0x20) = 0;
    *(int *)(p + 0x1c) = 0;
    *(int *)(p + 0x10) = 0;
    *(int *)(p + 0x0)  = a;
    *(short *)(p + 0x4) = (short)b;
    *(int *)(p + 0x8)  = 0;
    *(short *)(p + 0xc) = 0;
    return p;
}
```

## Struct context
Allocates a 0x24-byte object and initializes its fields. No SystemWork or GameSingleton. The three callback functions `func_02068594`, `func_02068580`, and `func_020685a4` are all matched in `src/main/` already.

| offset | field | type | use |
|--------|-------|------|-----|
| `p+0x00` | `f_0` | `int` | stored `a` |
| `p+0x04` | `f_4` | `short` | stored `(short)b` |
| `p+0x08` | `f_8` | `int` | stored `0` |
| `p+0x0c` | `f_c` | `short` | stored `0` |
| `p+0x10` | `f_10` | `int` | stored `0` |
| `p+0x14` | `f_14` | `u8` | stored `0` |
| `p+0x15` | `f_15` | `u8` | stored `0` |
| `p+0x18` | `f_18` | `void *` | stored `h`; then RE-READ to null-check (forces ldr from memory) |
| `p+0x1c` | `f_1c` | `int` | stored `0` |
| `p+0x20` | `f_20` | `int` | stored `0` |

The reload at `*(void **)(p + 0x18) == 0` after the store `*(void **)(p + 0x18) = h` is the key codegen constraint. The current draft already reads back from the field instead of comparing `h == 0`, which should force the reload ldr.

## Closest matched example
**Path:** src/main/func_020680cc.c
**Why similar:** func_020680cc is in the same address cluster and also accesses `p+0x20` as an int field (writing `-1` after a notify call). The cluster's struct layout is shared and the store-order convention is the same.
**Key lesson:** The reload ldr for the `p->f_18 == 0` check is forced by writing `*(void **)(p + 0x18) == 0` (reading from memory) rather than `h == 0` (using the just-computed value). This is already done in the draft. If mwcc still CSEs the load, declare `f_18` as a `volatile void *` field.

## Try this
1. Build as-is. In objdiff, check that after `str h,[p,#0x18]` there is a `ldr r?,[p,#0x18]` followed by `cmp r?,#0` — if the cmp uses the stored `h` value still in a register (no intermediate ldr), mwcc CSE'd the field.
2. If CSE'd: change `*(void **)(p + 0x18) = h;` to store through a volatile cast: `*(volatile void **)(p + 0x18) = h;`. The `volatile` qualifier forces a reload on the subsequent read of the same address.
3. Verify that `func_02054700`'s argument order matches: orig passes `r3=func_02068594, [sp]=func_02068580, [sp+4]=func_020685a4`. If generated code has a different stack-slot order for the two stacked callbacks, swap `func_02068580` and `func_020685a4` in the call (these are the two args that land on the stack as the 5th and 6th args of a 6-arg call on ARM).

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `func_02068580` | func ptr |
| `_LIT1` | `func_020685a4` | func ptr |
| `_LIT2` | `func_02068594` | func ptr |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020453e8` | main TU call |
| `bl` | `func_02054700` | main TU call |
| `bl` | `func_020453b4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

