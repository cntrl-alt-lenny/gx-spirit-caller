# 021ca4b8 — func_ov006_021ca4b8 (ov006, class D, 96B)

## Recipe + Risk
**Recipe:** D: single base ptr; bind mask 0x1ff (derive <<16 once); store-order HW,HW,word
**Risk:** mask: 0x1ff = lsl#23;lsr#23 (9-bit field). bind once then reuse for both fields. store-order.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021ca4b8 (ov006, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: single base ptr; bind mask 0x1ff (derive <<16 once); store-order HW,HW,word
 *   risk:       reshape-able: AND operand-order — asm is `and r0, r3, x<<16` (mask 0x1ff0000 FIRST); if mwcc folds/commutes the <<16 or pools 0x1ff twice the pack diverges. Offsets struct-guessed.
 *   confidence: med
 */
/* func_ov006_021ca4b8 (ov006, class D): affine-MMIO RMW + negated-pair pack.
 * Halfword reg @0x0400100a: (r&~3)|1, then (r&0x43)|0x210|0x2000. Then word
 * @+0xa = (-this->f78 & 0x1ff) | ((-this->f7c << 16) & 0x1ff0000). Returns 1.
 * recipe: ONE base ptr (word store reuses [base,#0xa]); bind mask 0x1ff in a
 * local, derive 0x1ff0000 once; store-order = 2 halfword RMW then the word. */

typedef struct {
    char _pad[0x78];
    int  f78;   /* +0x78 */
    int  f7c;   /* +0x7c */
} ov006_ca_obj;

int func_ov006_021ca4b8(ov006_ca_obj *this_) {
    volatile unsigned short *base = (volatile unsigned short *)0x0400100a;
    unsigned int m = 0x1ff;
    base[0] = (base[0] & ~3) | 1;
    base[0] = (base[0] & 0x43) | 0x210 | 0x2000;
    *(volatile unsigned int *)((char *)base + 0xa) =
        ((unsigned int)(-this_->f78) & m) | (((unsigned int)(-this_->f7c) << 16) & (m << 16));
    return 1;
}
```

## Struct context
`ov006_ca_obj` — inline struct defined in prep. The real struct is the larger ov006 object; only +0x78 and +0x7c are accessed here.

| field | offset | type | purpose |
|-------|--------|------|---------|
| `f78` | +0x78 | int | negated and masked into bits 0-8 of the word store |
| `f7c` | +0x7c | int | negated, shifted left 16, masked into bits 16-24 |

MMIO target: `0x0400100a` — the sub-engine affine BG scroll/rotation register (NDS hardware). The two halfword RMWs set the control register bits before writing the scroll pair.

The matched sibling `func_ov006_021c8278` at `src/overlay006/func_ov006_021c8278.c` writes the SAME two halfword RMWs (`(r&~3)|1` then `(r&0x43)|0x210|0x2000`) to `0x0400100a`, confirming the register and mask constants.

## Closest matched example
**Path:** src/overlay006/func_ov006_021c8278.c
**Why similar:** exact same MMIO base address `0x0400100a`, same two halfword RMW expressions `(r&~3)|1` and `(r&0x43)|0x210|0x2000`. The matched function is the stub version with no struct args; 021ca4b8 adds the word store after.
**Key lesson from that file:** the two halfword writes must stay as sequential `base[0] = (base[0] & ~3) | 1; base[0] = (base[0] & 0x43) | 0x210 | 0x2000;` — if combined into one expression or reordered the match fails. The `volatile` qualifier is mandatory to prevent the compiler from eliding the intermediate read.

## Try this
1. Build as-is. The prep binds `unsigned int m = 0x1ff` as a local — this is the key binding. The word store expression writes `(m << 16)` rather than the literal `0x1ff0000`; mwcc should derive `0x1ff0000` once via lsl#16 and reuse it. If objdiff shows two separate pool loads of `0x1ff`, try `unsigned int m16 = m << 16;` as an explicit local and use `m16` in the expression.
2. If the AND operand order in the word store is wrong (asm shows mask applied before negation), try writing the negation first: `int neg78 = (unsigned int)(-this_->f78); int neg7c = (unsigned int)(-this_->f7c);` then `(neg78 & m) | ((neg7c << 16) & m16)`. This forces the negate-before-mask order.
3. If the word store address `(char *)base + 0xa` doesn't match (asm uses an offset from the same base register), confirm `0x0400100a + 0xa = 0x04001014` — if the word store is at a different relative offset, the `+0xa` in the cast is wrong. Escalate to permuter if the mask-derive CSE is the divergence point.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x0400100a` | MMIO/const |
| `_LIT1` | `0x000001ff` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

