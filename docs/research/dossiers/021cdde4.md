# 021cdde4 — func_ov011_021cdde4 (ov011, class D, 228B)

## Recipe + Risk
**Recipe:** D: mla with stride-12; halfword sign-extend via (s16); store-order
**Risk:** mla stride-12. (s16) cast for sign-extend. store-order: extend then store.

## Prep draft

No prep file was generated for this candidate. The recipe and risk notes are the
primary input.

Based on recipe: (a) an `mla` instruction with stride 12 (0x0C) for address
computation, (b) a halfword loaded with sign-extension via an `(s16)` cast (which
lowers to `ldrsh` or `ldr + lsl/asr`), and (c) the store happens after the
sign-extend in source order.

```c
/* func_ov011_021cdde4 (ov011, class D, 228B):
 * MLA with stride 12 (0xC); halfword sign-extend via (s16) cast; store-order.
 *
 * CONSTRUCTION NOTES (no prep file):
 *  - "mla stride-12" = address is base + idx * 0xC + field_offset
 *  - "(s16) cast" = (short) or (s16) applied to a loaded value -> ldrsh
 *  - "store-order: extend then store" = sign-extend the halfword, THEN store it
 *    as an int — do not store before extending
 *  - 228B: likely a loop over elements in a stride-12 array writing sign-extended values
 *
 * Stride-12 arrays appear in path-finding geometry (21d2d44 reads a short pair
 * from +0x20 of a path object at stride implied by 0xC-byte records).
 */
extern char data_ov011_021d403c[];

void func_ov011_021cdde4(int idx, int base) {
    /* stride-12 address: base + idx * 0xC */
    char *row = data_ov011_021d403c + 0x20 + idx * 0x0C;
    /* sign-extend the loaded halfword: cast to (s16) explicitly */
    int x = (short)(*(short *)(row + 0x0));
    int y = (short)(*(short *)(row + 0x2));
    /* store-order: after extend, store result */
    *(int *)(data_ov011_021d403c + 0x258) = x;
    *(int *)(data_ov011_021d403c + 0x25c) = y;
}
```

## Struct context

| field | offset | type | purpose |
|-------|--------|------|---------|
| path_obj+0x20[i] | stride 0x0C or per-record | short pair | x,y short coordinates |
| data_ov011_021d403c+0x258 | +0x258 | int | coord x destination |
| data_ov011_021d403c+0x25c | +0x25c | int | coord y destination |
| data_ov011_021d403c+0x26c | +0x26c | int | scroll target x |
| data_ov011_021d403c+0x270 | +0x270 | int | scroll target y |

The `(s16)` sign-extend + `mla stride-12` pattern matches
`func_ov011_021d2d44` which reads a `(x,y)` short pair from `obj->+0x20`
at stride index. The store-order risk means the two stores must appear after
the sign-extend expressions in source order.

## Closest matched example
**Path:** `src/overlay011/func_ov011_021d2d44.c`
**Why similar:** Reads a `(x, y)` short pair from an object's `+0x20` array
and returns them as sign-extended ints — the `ldrsh` / `(short)` cast pattern.
**Key lesson from that file:** Use `(short)(*(short*)(ptr))` for `ldrsh` (signed
halfword load). The stride-12 `mla` requires writing the address as
`base + idx * 0x0C` (not idx * 12 — write the hex constant). Store the sign-extended
result in an `int` local BEFORE the store instruction in source order; never
combine the extend and store into a single expression if the asm separates them
into a `ldrsh; …; str` sequence.

## Try this
1. Write `(short)(*(short*)(row + N))` for each halfword load — this forces
   `ldrsh`. Compute `row = base + idx * 0x0C` (hex constant). Write each sign-extend
   into a named `int` local, then the stores in the same textual order as the asm.
2. In objdiff: verify that `ldrsh` appears (not `ldrh`). If the compiler emits `ldrh`
   followed by a shift pair instead, the cast is wrong — use `(signed short)` or
   `(s16)` typedef. Check that the `mla` uses the correct constant (0xC vs 0x8 vs 0x14).
3. If store-order is wrong (stores appear before the extends), the compiler hoisted
   the loads — split into explicit statement sequences. Escalate to permuter if the
   sign-extend and store match but a callee-saved register holding the base is wrong.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

