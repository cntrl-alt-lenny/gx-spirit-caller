# 020212cc — func_020212cc (main, class D, 352B)

## Recipe + Risk
**Recipe:** class D: switch(item->type) 0..6 dense -> addls-pc table; loop reloads count each iter
**Risk:** struct-guessed AND incomplete: batch asm is TRUNCATED past .L_020213a0, so the tail (f0c/f10 flush, Fill32, real return values) is unknown and the stubbed return is a guess. Loop+switch head should match; tail will diverge until full asm is fetched.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_020212cc (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     class D: switch(item->type) 0..6 dense -> addls-pc table; loop reloads count each iter
 *   risk:       struct-guessed AND incomplete: batch asm is TRUNCATED past .L_020213a0, so the tail (f0c/f10 flush, Fill32, real return values) is unknown and the stubbed return is a guess. Loop+switch head should match; tail will diverge until full asm is fetched.
 *   confidence: low
 */
/* func_020212cc (class D) -- WARNING: source asm TRUNCATED at 90 lines (size=352,
 * ~88 instrs); only the head + dispatch loop are visible. Tail (from .L_020213a0
 * onward: [r5+0xc] flush, [r5+0x10], Fill32, epilogue) is NOT in the batch and is
 * reconstructed as a best-effort stub -- DO NOT trust past the loop.
 *   r5=arg; if([data_02194340+0]==0) return 1; if([r5+4]==0) return 0;
 *   n=[r5+0x14]; if(n>0){ p=r5+0x18; for(i=0;i<n;i++,p+=0xc){ switch(p[0]) 0..6: handlers(p[1]); } }
 *   ...TRUNCATED: if([r5+0xc]) { x=[r5+0x10]; ... } ...
 */
typedef struct Item {
    int  type;   /* +0x00 */
    int  arg;    /* +0x04 */
    int  _pad8;  /* +0x08 */
} Item;          /* stride 0xc */
typedef struct Obj {
    char _pad00[0x04];
    int  f04;            /* +0x04 */
    char _pad08[0x0c - 0x08];
    int  f0c;            /* +0x0c */
    int  f10;            /* +0x10 */
    int  count;          /* +0x14 */
    Item items[1];       /* +0x18 */
} Obj;

extern int  data_02194340;
extern void Fill32(int val, void *dst, int len);
extern void func_020211a4(void);
extern int  func_02021cbc(int);
extern int  func_02023574(int);
extern int  func_020240a0(int);
extern int  func_020245e8(int);
extern int  func_02027144(int);
extern int  func_02028804(int);
extern int  func_0202a2f8(int);

int func_020212cc(Obj *o) {
    int i, n;
    Item *p;
    if (data_02194340 == 0) return 1;
    if (o->f04 == 0) return 0;
    n = o->count;
    if (n > 0) {
        p = &o->items[0];
        for (i = 0; i < n; i++, p++) {
            switch (p->type) {
            case 0: func_02021cbc(p->arg); break;
            case 1: func_02023574(p->arg); break;
            case 2: func_020240a0(p->arg); break;
            case 3: func_020245e8(p->arg); break;
            case 4: func_02027144(p->arg); break;
            case 5: func_02028804(p->arg); break;
            case 6: func_0202a2f8(p->arg); break;
            default: break;
            }
            n = o->count; /* orig reloads [r5+0x14] each iter */
        }
    }
    /* TRUNCATED tail -- reconstructed stub only */
    return 0;
}
```

## Struct context
Accesses `data_02194340` (module-local global, same base as func_020211c8 / func_020214ac). Not GameSingleton/SystemWork. The `Obj` struct layout is guessed from asm offsets.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| data_02194340+0 | (global int) | int | guard: if == 0 return 1 |
| o+0x04 | f04 | int | guard: if == 0 return 0 |
| o+0x0c | f0c | int | TRUNCATED tail — read in unknown context |
| o+0x10 | f10 | int | TRUNCATED tail — read in unknown context |
| o+0x14 | count | int | loop bound; reloaded inside loop each iter |
| o+0x18 | items[0] | Item[] | dispatch loop base; stride 0xc |
| item+0x00 | type | int | switch selector (0..6) |
| item+0x04 | arg | int | passed to each case handler |

## Closest matched example
**Path:** src/main/func_02014bf4.c
**Why similar:** dense switch 0..6 with one handler call per case, followed by a count-reload pattern that is structurally similar to other class-D dispatch loops in this batch.
**Key lesson:** The `n = o->count` reload inside the loop body (after each switch case) is load-bearing — it must appear as a separate `ldr` each iteration, not hoisted. The switch must emit a `cmp r_type, #6; addls pc, pc, r_type, lsl #2` jump table (7 entries + default). If mwcc emits a cmp-chain instead, add an explicit `case 6:` label before the last handler and ensure all 7 cases are contiguous 0..6.

## Try this
1. Confirm the `n = o->count;` reload is the LAST statement in the loop body (after the switch), not hoisted — if objdiff shows `count` cached across iterations, move the reload to immediately after the switch's closing brace.
2. WARNING: the tail of this function (past .L_020213a0) is TRUNCATED in the prep draft. After the loop-head matches, fetch the full asm for the tail region and complete the stub before attempting a full match — the return values and f0c/f10 flush are unknown.
3. Park as permuter-class if the tail reveals reg-alloc pressure across the switch; the dense 7-way dispatch with live accumulators is a permuter boundary on Windows.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_02021420` | `data_02194340` | data symbol |
| `.L_02021424` | `data_02194340` | data symbol |
| `.L_02021428` | `0x618` | MMIO/const |

> **Mode D warning:** duplicate pool value(s) `data_02194340` — clean C deduplicates → 4-byte size diff → sha1 fail. `.s`-only candidate.

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02021cbc` | main TU call |
| `bl` | `func_02023574` | main TU call |
| `bl` | `func_020240a0` | main TU call |
| `bl` | `func_020245e8` | main TU call |
| `bl` | `func_02027144` | main TU call |
| `bl` | `func_02028804` | main TU call |
| `bl` | `func_0202a2f8` | main TU call |
| `blx` | `r1` |  |
| `bl` | `Fill32` |  |
| `bl` | `func_02021bac` | main TU call |
| `bl` | `func_020211a4` | main TU call |
| `bl` | `func_02023478` | main TU call |
| `bl` | `func_02023fec` | main TU call |
| `bl` | `func_020244e8` | main TU call |
| `bl` | `func_02027048` | main TU call |
| `bl` | `func_020286a0` | main TU call |
| `bl` | `func_0202a240` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

