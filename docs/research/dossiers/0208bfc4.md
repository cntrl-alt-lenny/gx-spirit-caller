# 0208bfc4 — func_0208bfc4 (main, class D, 136B)

## Recipe + Risk

**Recipe:** D: sqrt-MMIO 64-bit mode, sumsq[1,0,2], (u64)sq<<2 param, (res+1)>>1
**Risk:** store-order: orig interleaves low-word str then finishes high-word (orr ip>>0x1e) then str; a u64 volatile store may emit both shifts then both str. reshape-able by splitting into explicit lo/hi int stores if it diverges.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0208bfc4 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: sqrt-MMIO 64-bit mode, sumsq[1,0,2], (u64)sq<<2 param, (res+1)>>1
 *   risk:       store-order: orig interleaves low-word str then finishes high-word (orr ip>>0x1e) then str; a u64 volatile store may emit both shifts then both str. reshape-able by splitting into explicit lo/hi int stores if it diverges.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0208bfc4 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. recipe: D, DS hardware sqrt unit in 64-bit
 * mode. d = a - b componentwise; sumsq = d.x^2+d.y^2+d.z^2 (signed 64-bit);
 * write (u64)sumsq<<2 to SQRT_PARAM (0x040002b8), busy-poll SQRTCNT bit15,
 * return (SQRT_RESULT+1)>>1 (rounds the doubled root back down).
 * Component eval order in asm is [1] then [0] then [2]; preserved.
 * Sibling precedent: docs/research/c-match-prep/0208be70.c, 0208bf3c.c.
 */
#include <nitro/types.h>

int func_0208bfc4(int *a, int *b)
{
    int d1 = a[1] - b[1];
    int d0 = a[0] - b[0];
    int d2 = a[2] - b[2];
    long long sq = (long long)d1 * d1 + (long long)d0 * d0 + (long long)d2 * d2;

    *(vu16 *)0x040002b0 = 1;
    *(volatile unsigned long long *)0x040002b8 = (unsigned long long)sq << 2;
    while (*(vu16 *)0x040002b0 & 0x8000)
        ;
    return (*(vu32 *)0x040002b4 + 1) >> 1;
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| a[0..2], b[0..2] | r0/r1 + 0,4,8 | int[] | componentwise subtract; eval order [1],[0],[2] |
| 0x040002b0 | SQRTCNT | vu16 | write 1 = set 64-bit mode; bit15 poll = busy |
| 0x040002b8 | SQRT_PARAM | volatile u64 | input: `(u64)sumsq << 2` |
| 0x040002b4 | SQRT_RESULT | vu32 | output: read after poll; return `(result+1)>>1` |

The DS sqrt unit result is `sqrt(input)` where input = `sumsq<<2` in 64-bit mode, so result = `2*sqrt(sumsq)` → `(result+1)>>1` rounds to nearest integer sqrt. The eval order `[1],[0],[2]` is load-driven: mwcc loads `a[1]` and `b[1]` first because the smull for `d1*d1` feeds the initial accumulator.

## Closest matched example

**Path:** src/main/func_0208c428.c
**Key lesson:** The 3-component vector subtract in `func_0208c428` confirmed that mwcc uses `[1],[0],[2]` or `[0],[1],[2]` load order driven by declaration order in the C expression, not ascending index order. Declaring `d1 = a[1]-b[1]; d0 = a[0]-b[0]; d2 = a[2]-b[2]` in that exact sequence forces the [1],[0],[2] load schedule needed here. For the volatile u64 write, the store-order issue arises from `*(volatile u64 *)p = val << 2` — mwcc may emit `lsl` for lo-word and `orr+lsr` for hi-word in either order relative to the two `str` instructions.

## Try this

1. If objdiff shows both shift instructions (`lsl r?,#2` + `orr r?,r?,r?,lsr#0x1e`) emitting before both `str` instructions, split the volatile write into two explicit 32-bit stores: `*(volatile unsigned int *)0x040002b8 = (unsigned int)((unsigned long long)sq << 2); *(volatile unsigned int *)0x040002bc = (unsigned int)(((unsigned long long)sq << 2) >> 32);` — this forces lo-str first, hi-str second.
2. Verify in objdiff that the `d1, d0, d2` declaration order (not `d0, d1, d2`) is preserved in the load sequence; if mwcc reorders to ascending index, swap the first two lines to `d0 = a[0]-b[0]; d1 = a[1]-b[1];` and check which matches.
3. If the busy-poll loop emits a `ldrh` + `tst` + `bne` pattern instead of the expected `ldrh r?,... ; ands r?,r?,#0x8000 ; bne` form, try replacing the `while` condition with `while (*(vu16 *)0x040002b0 & (vu16)0x8000)` to force the half-word mask.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x040002b0` | MMIO/const |
| `_LIT1` | `0x040002b8` | MMIO/const |
| `_LIT2` | `0x040002b4` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

