# 021aefd4 — func_ov008_021aefd4 (ov008, class C, 212B)

## Recipe + Risk
**Recipe:** OAM fill/commit; field stores in orig order; two-f34 RMW kept separate
**Risk:** orig RMWs +0x34 TWICE (|9 then |0x10) around other stores; mwcc may coalesce the two strh or re-order +0x20/+0x2c/+0x38 stores. reshape-able (field-store order) + struct-guessed

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021aefd4 (ov008, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     OAM fill/commit; field stores in orig order; two-f34 RMW kept separate; mode picks cfg
 *   risk:       orig RMWs +0x34 TWICE (|9 then |0x10) around other stores; mwcc may coalesce the two strh or re-order +0x20/+0x2c/+0x38 stores. reshape-able (field-store order) + struct-guessed
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_ov008_021aefd4 (ov008, class C, MED) — built-free.
 * UNVERIFIED + ITERATION-EXPECTED. OAM marshalling, mul index, struct-field RMW.
 *
 * a0 -> sprite index (*0x34 into a u16 table data_021b2790), a1 -> a 0xc-stride
 * index folded into the OAM "char base". A 0x48 local OAM record is filled by
 * func_0201d47c, fields RMW'd in the orig's STORE ORDER, then committed via
 * func_0201e5b8 and the handle freed via func_02006e1c. The mode flag at
 * data_021040ac+0x34 picks one of two glyph-config args (LIT2 vs LIT3).
 */
extern void func_02091554(void *dst, int glyph, void *cfg);
extern void func_0201d47c(void *p);
extern int  func_02006c0c(void *p, int a, int b);
extern void func_0201e5b8(void *p);
extern void func_02006e1c(int h);

extern char data_021040ac[];          /* _LIT0: int@+0x34 mode flag */
extern char data_ov008_021b2790[];    /* _LIT1: u16 glyph table, stride 0x34 */
extern char data_ov008_021b26ac[];    /* _LIT2: glyph cfg (mode==0) */
extern char data_ov008_021b26c4[];    /* _LIT3: glyph cfg (mode!=0) */

void func_ov008_021aefd4(int a0, int a1) {
    char buf[0x48];
    int handle;
    int glyph = *(unsigned short *)(data_ov008_021b2790 + a0 * 0x34);

    if (*(int *)(data_021040ac + 0x34) == 0) {
        func_02091554(buf + 0x0, glyph, data_ov008_021b26ac);
    } else {
        func_02091554(buf + 0x0, glyph, data_ov008_021b26c4);
    }

    func_0201d47c(buf + 0x20);
    handle = func_02006c0c(buf + 0x0, 4, 0);

    *(unsigned short *)(buf + 0x34) = (*(unsigned short *)(buf + 0x34) & ~0xf) | 0x9;
    *(int *)(buf + 0x20) = handle;
    *(int *)(buf + 0x2c) = ((a1 * 0xc + 0xab + 0x200) << 7);
    *(unsigned short *)(buf + 0x34) = *(unsigned short *)(buf + 0x34) | 0x10;
    *(int *)(buf + 0x38) = (*(int *)(buf + 0x38) & ~0x1c000) | 0x1c000;

    func_0201e5b8(buf + 0x20);
    func_02006e1c(*(int *)(buf + 0x20));
}
```

## Struct context
The `buf[0x48]` stack record is an OAM command record. Known field layout from the pattern (matched-reference `store-order` family):

| field | offset | type | purpose |
|-------|--------|------|---------|
| glyph/sprite | +0x00 | int | sprite/char index set by func_02091554 |
| … | +0x20 | int | handle slot (written by func_02006c0c, read by func_0201e5b8 / func_02006e1c) |
| … | +0x2c | int | char base / position word `(a1*0xc+0xab+0x200)<<7` |
| flags_lo | +0x34 | u16 | low nibble: set to 0x9 (bic+orr); then OR'd with 0x10 |
| flags_hi | +0x38 | int | bits 14:12 set to 0x1c000 mask |

`data_021040ac` is `GlobalAudioState`; the field at +0x34 is an audio mode flag (see `GlobalAudioState.md`). `data_ov008_021b2790` is a stride-0x34 u16 table (declared in `ov008_core.h`).

## Closest matched example
**Path:** src/overlay008/func_ov008_021ab120.c
**Why similar:** also a 0x38-offset field loop with `func_0201ef3c` commit + zero-clear — same OAM record shape and store-order discipline.
**Key lesson from that file:** the `handle` field at `buf+0x38` (or `o[14]`) must be read and written as separate statements; mwcc never fuses the load-check-clear into a single expression. The store order must exactly reproduce the asm sequence: do not hoist `buf+0x34` RMWs before or after the `buf+0x20` / `buf+0x2c` stores.

## Try this
1. The two `buf+0x34` RMWs MUST remain as two separate statements in the exact interleaved position shown: `(& ~0xf)|0x9` first, then `buf+0x20` and `buf+0x2c` stores, then `|0x10`. Do not combine into a single `(& ~0xf)|0x19`. If objdiff shows the two `strh` coalesced, split them with an explicit `volatile` cast on the field or a barrier dummy read.
2. If `buf+0x38` RMW (`& ~0x1c000)|0x1c000`) diverges, check that mwcc emits `bic+orr` and not `mov` with a constant — the `|` must keep the existing bits, not overwrite. Inspect the generated instruction pair.
3. If the `a1*0xc+0xab+0x200` shift-by-7 produces a wrong constant folding, try `(a1*0xc + 0x2ab) << 7` as the pre-folded form; mwcc may fold the +0xab+0x200 differently. If still mismatched after both orderings, route to permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021040ac` | data symbol |
| `_LIT1` | `data_ov008_021b2790` | raw |
| `_LIT2` | `data_ov008_021b26ac` | raw |
| `_LIT3` | `data_ov008_021b26c4` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02091554` | main TU call |
| `bl` | `func_0201d47c` | main TU call |
| `bl` | `func_02006c0c` | main TU call |
| `bl` | `func_0201e5b8` | main TU call |
| `bl` | `Task_Invoke` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

