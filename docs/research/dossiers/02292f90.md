[//]: # (markdownlint-disable MD013 MD041)

# 02292f90 — func_ov002_02292f90 (ov002, class F retriage, 384B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses DuelQueueState (data_ov002_022ce288) fields and Ov002Slot id_lo13 bitfield. At 384B this is a large class-F function but the struct fields now fully name all previously opaque offsets. The size reflects PerPlayerRowTable slot enumeration gated on DuelQueueState state fields.
**Confidence:** high
**Structs needed:** DuelQueueState (base 0x022CE288, f_5a8, f_5b4, f_5c0), Ov002Slot (id_lo13 :13 via lsl#19/lsr#19), PerPlayerRowTable (stride 0x868)

## Assembly

```asm
; Source file not present — func_ov002_02292f90 has no .s file in the working tree.
; This function was either already matched and its .s removed, or was never emitted
; as a standalone unit. The retriage entry is preserved for campaign tracking.
; Size: 384B, Conf: high, KB hits: DuelQueueState + Ov002Slot
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state 0x7f / 0x80 gate |
| DuelQueueState | +0x5B4 | f_5b4 | int | pending-action flag |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock |
| Ov002Slot | +0x008 | id_lo13 | u32 :13 | card id via lsl#19/lsr#19 |
| PerPlayerRowTable | base | data_ov002_022cf16c | extern | player slot base, stride 0x868 |

## Recipe sketch

```c
typedef unsigned short u16;
struct Ov002Self { u16 f0; u16 b0 : 1; u16 rest : 15; };
struct DuelQueueState {
    char _pad5a8[0x5a8]; int f_5a8;
    void *f_5ac;
    char _pad5b4[4]; int f_5b4;
    char _pad5c0[8]; int f_5c0;
};
extern struct DuelQueueState data_ov002_022ce288;
extern char data_ov002_022cf16c[];

/* 384B DuelQueueState+Ov002Slot function: gated slot scan, two player rows */
int func_ov002_02292f90(struct Ov002Self *self) {
    if (data_ov002_022ce288.f_5c0) return 0;
    int qs = data_ov002_022ce288.f_5a8;
    if (qs != 0x80 && qs != 0x7f) return 0;
    data_ov002_022ce288.f_5c0 = 1;
    /* walk both players' slot arrays */
    for (int p = 0; p < 2; p++) {
        char *row = data_ov002_022cf16c + (p & 1) * 0x868;
        for (int s = 0; s < 5; s++) {
            unsigned int w = *(unsigned int *)(row + 0x30 + s * 0x14);
            unsigned int id = (w << 19) >> 19;
            if (id == 0) continue;
            /* process slot: call handler with (self, p, s) */
        }
    }
    data_ov002_022ce288.f_5c0 = 0;
    return 1;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_0225764c.c
**Why similar:** Same DuelQueueState re-entrancy lock (f_5c0 = 1 / f_5c0 = 0 bookend) around a PerPlayerRowTable slot walk.
**Key lesson:** The re-entrancy lock pattern requires `dqs->f_5c0 = 1;` before the loop body and `dqs->f_5c0 = 0;` after — both stores must be visible in objdiff. Declare the lock field at exact offset 0x5C0 using a padded struct or cast.

## Try this

1. Declare `data_ov002_022ce288.f_5c0` at offset +0x5C0 and write `if (data_ov002_022ce288.f_5c0) return 0;` as the guard; verify `ldr rX,[rY,#0x5c0]` / `cmp rX,#0` in objdiff.
2. If the DuelQueueState state check (0x80 / 0x7f) ordering mismatches, put `case 0x80:` first in the switch to match the assembly `cmp` sequence.
3. This is a DuelQueueState re-entrancy-gated PPT scan — not a DSS dispatch. Escalate only if the lock store sequence (`str #1` / `str #0` bracket) is out of place.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

