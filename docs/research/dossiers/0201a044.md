# 0201a044 — func_0201a044 (main, class D, 116B)

## Recipe + Risk

**Recipe:** bind sys r4; (x<<k>>k) extracts; 2*term operand-first adds; /409600 magic 0x51eb851f>>17
**Risk:** permuter-class: scale computed first but multiplied last + 2a+2b+1 reassociation -> mwcc may reschedule the mul/add chain or pick highword>>17 only if it folds /409600 as 4096*100.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0201a044 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     bind sys r4; (x<<k>>k) extracts; 2*term operand-first adds; /409600 magic 0x51eb851f>>17
 *   risk:       permuter-class: scale computed first but multiplied last + 2a+2b+1 reassociation -> mwcc may reschedule the mul/add chain or pick highword>>17 only if it folds /409600 as 4096*100.
 *   confidence: low
 */
/* CAMPAIGN-PREP candidate for func_0201a044 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. Bitfield extracts + unsigned magic divide.
 *   sys=GetSystemWork() bound r4; scale=func_0201a170(5) computed FIRST, multiplied LAST.
 *   acc = (lit52>>1) + 2*((e0<<11>>24)) + 2*((d4&0xffff)-(e4&0xffff));
 *   return scale * (50*(acc+1)) / 409600;   // 409600 = 4096*100, magic 0x51eb851f >>17
 */
extern char *GetSystemWork(void);
extern int func_0201a170(int);
extern unsigned short data_0210594c[];

int func_0201a044(void) {
    char *r4 = GetSystemWork();
    int scale = func_0201a170(5);
    int e4 = (unsigned int)((unsigned int)*(int *)(r4 + 0x8e4) << 0x10) >> 0x10;
    int e0 = (unsigned int)((unsigned int)*(int *)(r4 + 0x8e0) << 0xb) >> 0x18;
    int d4 = (unsigned int)((unsigned int)*(int *)(r4 + 0x8d4) << 0x10) >> 0x10;
    int h = (int)data_0210594c[0x52 / 2] >> 1;
    int acc = h + (e0 << 1) + ((d4 - e4) << 1);
    return (unsigned int)(scale * ((acc + 1) * 0x32)) / 409600U;
}
```

## Struct context

This function is one of the most struct-offset-dense functions in this batch. It accesses three SystemWork bitfield words:

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| `sys+0x8e4` | multi-byte status field | `int` (bitfield word) | low 16 bits extracted: `(v<<16)>>16` = `e4`; confirmed by struct-context-main.md |
| `sys+0x8e0` | two-bit flag register | `int` (bitfield word) | bits 16..23 extracted: `(v<<11)>>24` = `e0`; struct-context confirms this is a bitfield word |
| `sys+0x8d4` | dense bitfield word | `int` (bitfield word) | low 16 bits extracted: `(v<<16)>>16` = `d4`; struct-context confirms `bits 0..15` is a u16 field |

The `/409600` divisor uses magic constant `0x51eb851f` with `>>17` shift. `409600 = 4096 * 100`; mwcc may express this as `smull hi,lo,val,0x51eb851f; mov r0,hi,lsr#17` (confirmed pattern for this magic constant in unsigned divide context).

## Closest matched example

**Path:** src/main/func_0201a0b8.c
**Why similar:** same pattern — `GetSystemWork()` bound in r4, field at `sys+0x8e4`, field extract via right-shift, then multiply by a scale factor from `func_0201a170`.
**Key lesson:** In `func_0201a0b8`, `scale * field_value` computes the scale FIRST (`bl func_0201a170` before the field load), and the field is the second multiply operand. The same ordering applies here: `func_0201a170(5)` is called before any `sys+0x8e4` loads. The `lsr r1,r1,#0x19` vs `(v<<k)>>k` extract form used in `0201a0b8` should guide the extract expression for `e4` and `d4` (low-16 via `lsl#16; lsr#16`) and `e0` (byte-2 via `lsl#11; lsr#24`).

## Try this

1. Build as-is; the ordering of `scale = func_0201a170(5)` BEFORE the `sys+0x8e4` / `sys+0x8e0` / `sys+0x8d4` loads is critical — do not reorder. Also verify the `2*term` expressions use `<< 1` (shift) not `term + term` (add), as mwcc may emit `add r0,r1,r1` vs `lsl#1` differently.
2. If the `/409600U` magic gives a wrong shift count or constant, try spelling it as `/ (4096U * 100U)` — mwcc may fold the product of two constants into `409600` and pick a different magic; alternatively try `>> 12` then `/ 100U` to guide the two-step magic-divide path.
3. Park as permuter-class if the `smull`/`lsr` schedule for the final multiply-and-divide chain mismatches — the reassociation `scale * (acc+1) * 50 / 409600` has multiple valid orderings that C reshape cannot force; the permuter is needed to find the exact intermediate binding.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0210594c` | data symbol |
| `_LIT1` | `0x51eb851f` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201a170` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

