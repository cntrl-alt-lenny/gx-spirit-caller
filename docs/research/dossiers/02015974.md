# 02015974 — func_02015974 (main, class D, 412B)

## Recipe + Risk
**Recipe:** GetSystemWork bit0 guard + w[0x924] jump-table (case0=return); bic|orr field set, bit1 conditional dec
**Risk:** permuter-class: (x<<31)>>31 bit-guards peephole to `and #1` (020186b0 wall, same as 02016b60 which shipped .s); also case-0=bare-return numbering must hold cmp #5

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02015974 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     GetSystemWork bit0 guard + w[0x924] jump-table (case0=return); bic|orr field set, bit1 conditional dec
 *   risk:       permuter-class: (x<<31)>>31 bit-guards peephole to `and #1` (020186b0 wall, same as 02016b60 which shipped .s); also case-0=bare-return numbering must hold cmp #5
 *   confidence: low
 */
/* func_02015974 (main, class D, MED) — GetSystemWork state machine.
 * w[0x8d8]:bit0 enable guard; require w[0x920]==1; dispatch on w[0x924] (0..5
 * jump table, case0 = bare return). o[8] = action-id message word.
 * Modeled byte-for-byte on src/main/func_02015974.s (already shipped .s).
 * NOTE: bit0/bit1 reads use the (x<<31)>>31 / (x<<30)>>31 shift form on
 * purpose; mwcc 2.0 peepholes these to `and` (the 020186b0 wall). */
extern char *GetSystemWork(void);
extern int  func_020139b4(void);
extern int  func_02019034(void);
extern int  func_020195ec(void);
extern void func_02019640(int);
extern int  func_02019664(void);
extern int  func_0201969c(void);
extern void func_0201abb0(int);
extern void func_0201abd4(int);
extern void func_0201b7b4(void);
extern int  func_0201b7e0(void *);

void func_02015974(void *o) {
    char *w = GetSystemWork();
    if (!((*(unsigned int *)(w + 0x8d8) << 0x1f) >> 0x1f)) return;
    if (*(int *)(w + 0x920) != 1) return;
    switch (*(int *)(w + 0x924)) {
    case 1:
        *(int *)(w + 0x924) = 5;
        /* fallthrough */
    case 5:
        if (func_0201b7e0(o) != 4) return;
        if (func_020139b4() != 2) return;
        if ((unsigned int)func_02019034() < (unsigned int)(*(int *)(w + 0x8dc) + 2)) return;
        *(int *)((char *)o + 8) = 0x00140100;
        func_0201abb0(5);
        *(int *)(w + 0x900) |= 0x1000000;
        return;
    case 2:
        *(int *)((char *)o + 8) = 0x00140103;
        if (!((*(unsigned int *)(w + 0x8e0) << 0x1e) >> 0x1f)) {
            *(int *)(w + 0x920) = 1;
            *(int *)(w + 0x924) = 5;
        }
        if (((*(unsigned int *)(w + 0x8e0) << 0x1e) >> 0x1f)) return;
        func_02019640(0);
        return;
    case 3:
        if (((*(unsigned int *)(w + 0x8e0) << 0x1f) >> 0x1f)) return;
        if (func_020195ec() != 0) {
            *(int *)((char *)o + 8) = 0x00140102;
            return;
        }
        *(int *)((char *)o + 8) = 0x00140104;
        if (!((*(unsigned int *)(w + 0x8e0) << 0x1e) >> 0x1f))
            *(int *)(w + 0x924) = *(int *)(w + 0x924) - 1;
        func_0201b7b4();
        func_0201abd4(5);
        *(int *)(w + 0x900) &= ~0x1000000;
        return;
    case 4:
        if (func_02019664() != 0x65) return;
        if (func_0201969c() != 5) return;
        *(int *)((char *)o + 8) = 0x00140101;
        *(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;
        return;
    }
}
```

## Struct context

All offsets are from `GetSystemWork()` (SystemWork, BSS `data_02104f58`, size 0x92c).

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x8d8 | enable bit | s32, bit0 | gate via `(v<<0x1f)>>0x1f`; permuter-class `and #1` wall |
| +0x8dc | bound word | s32 | lower-bound for unsigned compare in case-5 (`w[0x8dc]+2`) |
| +0x8e0 | bitfield word | s32 | bit0 `(v<<0x1f)>>0x1f` guard in cases 2/3; bit1 `(v<<0x1e)>>0x1f` gates reset and `0x924--`; bic\|orr `(x&~1)\|1` in case-4 |
| +0x900 | flag word | s32 | OR'd with 0x1000000 in case-5; AND'd with ~0x1000000 in case-3 |
| +0x920 | mode/state | s32 | checked == 1 (early return if not); also written in case-2 |
| +0x924 | sub-state | s32 | `addls` jump-table (cmp#5, 6 entries; case1 self-sets to 5 then falls through); also written in cases 1/2/3 |

## Closest matched example
**Path:** src/main/func_02018dcc.c
**Why similar:** Same permuter-class bit-guard wall: `(v<<N)>>N` shift form that mwcc 2.0 peepholes to `and #N_mask`, matching the 020186b0 wall documented in func_02018dcc.
**Key lesson:** The `(x<<31)>>31` bit0 and `(x<<30)>>31` bit1 extracts are intentionally written as shifts (not `&1`/`&2`) to try to coerce `lsl#31;lsr#31` in the output. However, mwcc 2.0 typically still peepholes to `and #1` for the guard at `+0x8d8`. The matched `.s` for `func_02016b60` (same wall) was the solution — expect the same outcome here if the shift trick fails.

## Try this
1. Park as permuter-class; bit-guard `and #1` is un-forceable without macOS permuter. The `.s` file is the primary fallback path (same as `func_02016b60.s`).
2. As a pre-park check: try replacing the inline shift extracts with named `:1` bitfield struct reads (as in `func_02014bf4.c`) — occasionally the struct form emits the `lsl;lsr` pair instead of `and`.
3. If step 2 also fails, ship as `func_02015974.s` rather than spending more time on C reshaping.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00140100` | MMIO/const |
| `_LIT1` | `0x00140101` | MMIO/const |
| `_LIT2` | `0x00140104` | MMIO/const |
| `_LIT3` | `0x00140102` | MMIO/const |
| `_LIT4` | `0x00140103` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201b7e0` | main TU call |
| `bl` | `func_020139b4` | main TU call |
| `bl` | `func_02019034` | main TU call |
| `bl` | `func_0201abb0` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_0201969c` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |
| `bl` | `func_0201abd4` | main TU call |
| `bl` | `func_02019640` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

