# 0202ed90 — func_0202ed90 (main, class D, 176B)

## Recipe + Risk
**Recipe:** class-D switch->membership; cases source-ascending so tree+jump-table reproduce
**Risk:** Pivot/tree-balance: mwcc must split the 0x1a02-05 run into the pl jump-table and pick 0x1a61 as root; if it balances differently the cmp-tree diverges. permuter-class (switch shape, not reshape-able).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0202ed90 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     class-D switch->membership; cases source-ascending so tree+jump-table reproduce
 *   risk:       Pivot/tree-balance: mwcc must split the 0x1a02-05 run into the pl jump-table and pick 0x1a61 as root; if it balances differently the cmp-tree diverges. permuter-class (switch shape, not reshape-able).
 *   confidence: med
 */
/* func_0202ed90: card-id membership test (switch -> 1, default 0).
 * mwcc lowers this sparse switch into a binary-search compare tree
 * (cmp/bgt/bge) plus a 4-entry pl jump-table for the 0x1a02..0x1a05 run
 * and lit-base +offset derivations around pivot 0x1a61. */
int func_0202ed90(int a0) {
    switch (a0) {
    case 0x1a02:
    case 0x1a03:
    case 0x1a04:
    case 0x1a05:
    case 0x1a60:
    case 0x1a61:
    case 0x1a62:
    case 0x1a63:
    case 0x1a64:
    case 0x1a65:
    case 0x1b25:
        return 1;
    }
    return 0;
}
```

## Struct context
Pure card-ID membership predicate — no struct field accesses.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| — | — | — | no struct accesses |

## Closest matched example
**Path:** src/main/func_0202effc.c
**Why similar:** Same class-D switch->membership pattern, returns 1 for matched cases and 0 via fallthrough after the switch.
**Key lesson:** Two separate dense sub-runs exist here: 0x1a02..0x1a05 (4 entries, emitted as a `pl` table) and 0x1a60..0x1a65 (6 entries, also a candidate for `pl` table or +offset chain). The key structural decision is whether mwcc tables the 0x1a02..0x1a05 run separately from the 0x1a60..0x1a65 cluster or merges them. The lone 0x1b25 is a separate compare branch. The pivot root 0x1a61 is mwcc's midpoint for the upper cluster — this is compiler-internal.

## Try this
1. Drop the prep draft as-is (note the `return 0` is placed after the switch, not in a `default:` — match the prep form exactly). Verify that objdiff shows a `pl` jump-table for the 0x1a02..0x1a05 run (bounded by `cmp rN,#4; addls pc,...`) and a separate path for the 0x1a60..0x1a65 cluster with root pivot 0x1a61.
2. If mwcc merges both runs into a single wider table or emits a pure compare chain for the small 0x1a02..0x1a05 run: there is no C reshape to force a split — the jump-table emission threshold is mwcc-internal.
3. Park as permuter-class; the table/tree split decision and pivot balancing are compiler-internal and un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001a61` | MMIO/const |
| `_LIT1` | `0x00001a60` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

