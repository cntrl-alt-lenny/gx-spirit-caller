# 02086334 — func_02086334 (main, class D, 304B)

## Recipe + Risk
**Recipe:** flag-branch dispatch; r5 carry flag via if-assign; smull>>12 FX_Mul; o+N word index
**Risk:** reshape-able: branch order + early-return on (o[0]&1) ne path must match (addne/ldmneia tail) — if mwcc emits a join block instead of the conditional epilogue, restructure the final if into two returns.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02086334 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     flag-branch dispatch; r5 carry flag via if-assign; smull>>12 FX_Mul; o+N word index
 *   risk:       reshape-able: branch order + early-return on (o[0]&1) ne path must match (addne/ldmneia tail) — if mwcc emits a join block instead of the conditional epilogue, restructure the final if into two returns.
 *   confidence: med
 */
/* func_02086334: flag-gated FX12 transform of obj fields then func_02084e0c dispatch.
 * obj @ r0. obj[0] flags: 0x18,4,2,1. smull>>12 = NitroSDK FX_Mul (no round). */
typedef signed int s32;
typedef signed long long s64;
typedef s32 fx32;

extern void func_02084e0c(int, int *, int);

void func_02086334(fx32 *o) {
    int v[4];                   /* sp..sp+0xc */
    int r5 = 0;
    int r4 = o[0] & 0x18;
    if (!r4) {
        func_02084e0c(0x1b, o + 7, 3);   /* obj+0x1c */
    }
    if (!(o[0] & 4)) {
        if (r4) { r5 = 1; }
        else {
            v[0] = (s32)(((s64)o[0x13] * o[4]) >> 12);  /* [0x4c]*[0x10] */
            v[1] = (s32)(((s64)o[0x14] * o[5]) >> 12);  /* [0x50]*[0x14] */
            v[2] = (s32)(((s64)o[0x15] * o[6]) >> 12);  /* [0x54]*[0x18] */
            func_02084e0c(0x1c, v, 3);
        }
    }
    if (!(o[0] & 2)) {
        if (r5) func_02084e0c(0x19, o + 10, 0xc);   /* obj+0x28 */
        else    func_02084e0c(0x1a, o + 10, 9);
    } else if (r5) {
        func_02084e0c(0x1c, o + 0x13, 3);            /* obj+0x4c */
    }
    if (!r4) {
        func_02084e0c(0x1b, o + 4, 3);              /* obj+0x10 */
    }
    if (!(o[0] & 1)) {
        func_02084e0c(0x1b, o + 1, 3);              /* obj+0x4 */
    }
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| o[0] | flags | u32 | bits: 0x18 (mask, sets r4), 4 (skip FX block), 2 (selects 0x19 vs 0x1c call), 1 (skip final call) |
| o[1] (obj+0x04) | | fx32 | passed to final func_02084e0c(0x1b, o+1, 3) |
| o[4] (obj+0x10) | | fx32 | FX_Mul multiplier for v[0]; also passed to func_02084e0c(0x1b, o+4, 3) |
| o[5] (obj+0x14) | | fx32 | FX_Mul multiplier for v[1] |
| o[6] (obj+0x18) | | fx32 | FX_Mul multiplier for v[2] |
| o[7] (obj+0x1c) | | fx32 | passed to func_02084e0c(0x1b, o+7, 3) in first block |
| o[10] (obj+0x28) | | fx32* | passed to 0x19 or 0x1a call |
| o[0x13] (obj+0x4c) | | fx32 | FX_Mul factor for v[0]; also target of 0x1c call when r5=1 |
| o[0x14] (obj+0x50) | | fx32 | FX_Mul factor for v[1] |
| o[0x15] (obj+0x54) | | fx32 | FX_Mul factor for v[2] |

## Closest matched example
**Path:** src/main/func_02085e74.c
**Key lesson:** func_02085e74 (class C, 212B, reshape-able) demonstrates the carried-bool pattern: `int a = (f & 0x4) == 0` declared first lands in r4 across multiple `func_02084e0c` calls. The same func_02084e0c is the dispatch sink here; both functions share the same `if-assign` idiom for a callee-saved carry flag (`r5` here via `r5 = 1` inside a branch, vs `r4` in func_02085e74). The final `if (!(o[0]&1))` must emit as a conditional return (early-return path) not a join block.

## Try this
1. Declare `int r5 = 0` before `int r4 = o[0] & 0x18` so that r5 (the carry flag) gets the first callee-saved assignment. The `if (r4) { r5 = 1; }` inside the `!(o[0]&4)` block must remain as an `if-assign` (not a ternary) so that mwcc emits a conditional `moveq r5,#1` rather than computing it unconditionally.
2. If objdiff shows the final two `if (!r4)` and `if (!(o[0]&1))` blocks emitting as a join (shared epilogue), split the last `if (!(o[0]&1))` into: `if (o[0]&1) return; func_02084e0c(0x1b, o+1, 3);` — the early-return form forces the `addne/ldmneia` conditional pop idiom instead of a join block.
3. Verify via objdiff that each `(s32)(((s64)o[N] * o[M]) >> 12)` emits `smull rL,rH,rA,rB; asr rL,rH,#12` (not the `lsr/orr` form used for >>8). If `>>12` emits `lsr#12;orr`, switch to `(s64)` cast rather than `(long long)` and retry.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02084e0c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

