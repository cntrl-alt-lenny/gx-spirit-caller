# 021d1110 — func_ov011_021d1110 (ov011, class D, 208B)

## Recipe + Risk
**Recipe:** D: halfword pair from two fields; bic-orr; strh in order
**Risk:** halfword pair: two ldrh then two bic-orr then two strh in asm order.

## Prep draft

```c
/* func_ov011_021d1110 (ov011, cls D, 208B): clone of 021d1080, only the
 * ldrh offsets differ (+0x52/+0x54 vs +0x2a/+0x2c). Described in the
 * existing prep file docs/research/c-match-prep/021d1110.c.
 *
 * This is also the function documented in func_ov011_021d1110.s (a
 * GLOBAL_ASM ship confirmed by brief 302). The halfword pair recipe:
 * two ldrh from consecutive offsets, two <<0xc shifts, two str results.
 */

extern char data_ov011_021d334a[];
extern char data_ov011_021d403c[];
extern char data_ov011_021d4660[];
extern int  func_ov011_021d0ffc(int a0);

void func_ov011_021d1110(int a0, int *out1, int *out2) {
    char *t4660 = data_ov011_021d4660;
    char *b403c = data_ov011_021d403c;

    if (func_ov011_021d0ffc(a0) == 1) {
        unsigned int v   = *(unsigned int *)(b403c + 0x268);
        unsigned int idx = (v << 0x17) >> 0x1c;       /* bits [8:5] */
        char *row  = data_ov011_021d334a + (idx - 1) * 0x72;
        char *cell = row + (a0 - 0x11) * 4;
        *out1 = *(unsigned short *)(cell + 0x52) << 0xc;
        *out2 = *(unsigned short *)(cell + 0x54) << 0xc;
    } else {
        char *e = t4660 + a0 * 0x28;
        *out1 = *(int *)e;
        *out2 = *(int *)(e + 4);
    }
}
```

## Struct context

This is a near-exact clone of `func_ov011_021d1080`. All struct references are
shared:

| symbol | offset | type | purpose |
|--------|--------|------|---------|
| `data_ov011_021d4660` | base | char[] | stride-0x28 coord array (cached in t4660, callee-saved r7) |
| `data_ov011_021d403c` | +0x268 | unsigned int | packed bitfield, bits[8:5] = row index |
| `data_ov011_021d334a` | base | char[] | stride-0x72 lookup table |
| `cell + 0x52` | ldrh | unsigned short | halfword field 1 (vs 0x2a in 021d1080) |
| `cell + 0x54` | ldrh | unsigned short | halfword field 2 (vs 0x2c in 021d1080) |

The ONLY difference from 021d1080: halfword offsets 0x52/0x54 instead of 0x2a/0x2c.

## Closest matched example
**Path:** src/overlay011/func_ov011_021d1080.s (sibling — the actual template)
**Why similar:** This function IS a clone of 021d1080 with the two ldrh offsets bumped. If 021d1080 lands, this lands with the same source structure and only the two hex constants changed.
**Key lesson from that file:** Both bases (`t4660` = data_4660, `b403c` = data_403c) must be bound BEFORE the call to `func_ov011_021d0ffc` (they survive as callee-saved r7/r8 across the call). The `out1`/`out2` pointer args must also go to callee-saved registers (r4/r5) — declare `t4660` and `b403c` as named locals at the TOP of the function, before the `if` condition, so mwcc binds them immediately.

## Try this
1. Copy the 021d1080 prep draft exactly; change `cell + 0x2a` → `cell + 0x52` and `cell + 0x2c` → `cell + 0x54`. Nothing else should differ.
2. In objdiff, if the callee-save list differs (e.g., r6 appears unexpectedly), check declaration order of `t4660` / `b403c` — the one declared first gets the lower-numbered callee-saved register. Mirror whatever order 021d1080 uses when it matches.
3. If 021d1080 has NOT matched yet, treat this function the same — both are class D (load-dest reg-numbering wall per ov011_core.h brief 313 notes). Escalate both together to permuter once the C structure is confirmed by objdiff showing only a reg-number diff.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov011_021d4660` | raw |
| `_LIT1` | `data_ov011_021d403c` | raw |
| `_LIT2` | `data_ov011_021d334a` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov011_021d0ffc` | overlay call (ov011) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

