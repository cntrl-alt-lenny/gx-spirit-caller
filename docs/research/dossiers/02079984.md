# 02079984 — func_02079984 (main, class D, 132B)

## Recipe + Risk
**Recipe:** do/while(i<n) guarded by if(n>0); mla via u16*u32+carry; tail memset
**Risk:** tail `if(i<count){dst[i]=carry;i++;}` is fully PREDICATED in orig (movlt/addlt/strlth with `sub count-i` interleaved); if mwcc branches instead of if-converting, the whole tail diverges. permuter-class (scheduling/if-conversion).

## Prep draft
```c
/* func_02079984: scalar*vector multiply with 16-bit carry propagate.
 * r0=dst(r7), r1=src(r6), r2=scalar(r5), r3=count(r4).
 * n = func_02079e20(src, count);   // n = significant halfword length
 * for(i=0;i<n;i++){ acc = src[i]*scalar + carry; dst[i]=acc; carry=acc>>16; }
 * if(i<count){ dst[i]=carry; i++; }
 * func_020945f4(dst+i, 0, (count-i)*2);   // zero remaining halfwords
 */
extern int func_02079e20(unsigned short *src, int count);
extern void func_020945f4(void *p, int b, int n);

void func_02079984(unsigned short *dst, unsigned short *src, unsigned int scalar, int count) {
    int n;
    int i;
    unsigned int carry;

    n = func_02079e20(src, count);
    carry = 0;
    i = 0;
    if (n > 0) {
        do {
            unsigned int acc = (unsigned int)src[i] * scalar + carry;
            dst[i] = (unsigned short)acc;
            i++;
            carry = acc >> 16;
        } while (i < n);
    }
    if (i < count) {
        dst[i] = (unsigned short)carry;
        i++;
    }
    func_020945f4(dst + i, 0, (count - i) * 2);
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| — | dst | `unsigned short *` | output limb array (r0/r7) |
| — | src | `unsigned short *` | input limb array (r1/r6) |
| — | scalar | `unsigned int` | 32-bit multiplier (r2/r5) |
| — | count | `int` | total limb count (r3/r4) |

No struct globals; all stack-local. `func_02079e20` trims trailing-zero limb count; `func_020945f4` is memset-for-halfwords.

## Closest matched example
**Path:** src/main/func_0207d430.c
**Key lesson:** mwcc emits a do-while with a guarding `if (cond) return 0` preamble matching `if(n>0){do{...}while(i<n);}` exactly; the key is that the do-while body stays inside the `if` brace rather than unconditionally falling to the loop label. The `mla` for `u16*u32+carry` must keep carry as the accumulator operand: `acc = (unsigned int)src[i] * scalar + carry` (not `carry + ...`) to match `mla dst, src_limb, scalar, carry` register ordering.

## Try this
1. Confirm `(unsigned int)src[i] * scalar + carry` order — carry MUST be the `+carry` addend (not the first operand) so mwcc maps it to the `mla Rd, Rm, Rs, Ra` accumulator slot (`mla acc, src[i], scalar, carry`). If objdiff shows `mul + add` instead of `mla`, drop the cast: try `src[i] * scalar + carry` with `src[i]` typed `unsigned short` naturally.
2. The tail `if(i<count){dst[i]=(unsigned short)carry; i++;}` must emit as predicated `strlt`+`addlt` — check objdiff: if mwcc branches, rewrite as `if(i<count) dst[i++]=(unsigned short)carry;` (combine store and increment).
3. Park as permuter-class; the movlt/addlt/strlth predicated tail with interleaved `sub count-i` scheduling is an if-conversion coin-flip that cannot be forced by C reshape alone without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02079e20` | main TU call |
| `bl` | `func_020945f4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

