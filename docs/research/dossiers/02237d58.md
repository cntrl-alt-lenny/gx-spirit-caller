# 02237d58 — func_ov002_02237d58 (ov002, class F retriage, 316B)

## Re-triage
**Original verdict:** F — previously intractable
**Upgrade reason:** DuelQueueState and PerPlayerRowTable + Ov002Slot are all documented. The reloc at
0x02237d58 calls func_ov002_0221074c, and the surrounding relocs (0x02237d24 calls func_0220e74c;
0x02237d50 calls func_02237d60; loads at 0x02237d30/34 hit data_ov002_022cf16c and data_ov002_022cf1a4)
show a slot scan that delegates to the sibling func_ov002_02237d60. This function sits in a dense
cluster of PPT+slot scan functions — its reloc pattern matches the DuelQueueState+Ov002Slot category.
**Confidence:** high
**Structs needed:** DuelQueueState (data_ov002_022ce288 +0x5B8 sub-state), PerPlayerRowTable (base 0x022CF16C, stride 0x868, +0x030 slot array), Ov002Slot (id_lo13 lsl#19/lsr#19 bitfield, +0x004 f4)

## Assembly
```asm
; func_ov002_02237d58 — NOT YET EXTRACTED
; This function exists in the binary between func_ov002_02237538 and func_ov002_02237d60.s.
; It has not been disassembled into a .s file yet (absent from src/overlay002/ and symbols.txt).
; Size: 316 bytes (0x13C). Confirmed via retriage doc + reloc coverage (lines 12258-12262).
;
; Key relocs at this address range:
;   0x02237d24  arm_call -> 0x0220e74c  (func_0220e74c)
;   0x02237d30  load    -> 0x022cf16c   (data_ov002_022cf16c = PerPlayerRowTable)
;   0x02237d34  load    -> 0x022cf1a4   (data_ov002_022cf1a4 = companion array)
;   0x02237d50  arm_call -> 0x02237d60  (func_ov002_02237d60 — slot-id/f4 checker)
;   0x02237d58  arm_call -> 0x0221074c  (func_ov002_0221074c)
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5B8 | sub_state | int | dispatch ladder index (addls pc pattern) |
| PerPlayerRowTable | base + (player&1)*0x868 | row base | ptr | standard mla stride |
| PerPlayerRowTable | +0x030 | slots[0] | Ov002Slot[] | slot array base, stride 0x14 |
| Ov002Slot | +0x008 | id_lo13 | :13 bitfield | lsl#19/lsr#19 — slot occupancy test |
| Ov002Slot | +0x004 | f4 | u16 | node lookup value, used in sibling func_02237d60 |
| data_ov002_022cf1a4 | +0x0 | companion u16 array | u16[] | loaded via ldrh, same 0x868 stride |

## Recipe sketch

Based on reloc evidence and sibling func_ov002_02237d60 (which this function calls and mirrors):

```c
int func_ov002_02237d58(CardObj *card) {
    DuelQueueState *dqs = &data_ov002_022ce288;
    int sub_state = dqs->sub_state;  /* +0x5B8 */

    switch (sub_state) {
    default: return 1;
    case 0: {
        /* slot scan: for each player, for each slot, check func_0220e74c then call func_02237d60 */
        int player_bit = (card->f2 >> 0) & 1;
        Ov002FieldZone *row = (Ov002FieldZone *)((u8 *)&data_ov002_022cf16c
                                + (player_bit & 1) * 0x868);
        for (int s = 0; s < 5; s++) {
            Ov002Slot *slot = &row->slots[s];
            if (slot->id_lo13 == 0) continue;
            if (!func_ov002_0220e74c(card, s)) continue;
            if (!func_ov002_02237d60(card)) continue;
            func_ov002_0221074c(card, s);
            return 0;
        }
        return 1;
    }
    case 1:
        /* advance sub_state */
        dqs->sub_state++;
        return 0;
    }
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_02237d60.s
**Why similar:** Directly called by func_ov002_02237d58; same PPT+slot-array access at +0x030 with
stride 0x14, same mla stride 0x868, same companion array data_ov002_022cf1a4.
**Key lesson:** func_ov002_02237d60 does the slot-id/f4 pair check and returns 0 on mismatch — it is
a pure predicate. The caller (this function) provides the loop and the action call. Declare the
companion array ldrh from `data_ov002_022cf1a4 + player*0x868/2` as `u16` to reproduce the `ldrh r2,[r3,r2]` reloc.

## Try this
1. Load `dqs->sub_state` at +0x5B8 as the switch index; case 0 does the slot scan via `data_ov002_022cf16c[(player&1)*0x868].slots[s].id_lo13 != 0` guard then calls func_ov002_02237d60.
2. If objdiff shows the companion-array ldrh wrong, declare `unsigned short *cf1a4 = (unsigned short *)((u8 *)&data_ov002_022cf1a4 + (player&1)*0x868)` and index by slot byte-offset.
3. If the switch arms are reordered, escalate — the sub_state dispatch may be a DuelQueueState multi-step sequence needing the exact addls-pc encoding.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

