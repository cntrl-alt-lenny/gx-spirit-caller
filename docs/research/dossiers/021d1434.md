# 021d1434 — func_ov011_021d1434 (ov011, class C, 224B)

## Recipe + Risk
**Recipe:** CLASS C: three-ptr bind; r4/r5/r6 from decl order; sequential calls
**Risk:** decl-order: three ptrs map to r4/r5/r6. calls use each in declared order.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021d1434 (ov011, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     two stack aggregates; if/else-if on kind; store fields in asm order
 *   risk:       sp+0x48 frame layout and the two overlapping stack structs are inferred; exact slot offsets (cmd0 vs rec) and a stray r3 in the saved set may shift the frame -> stack-layout / struct-guessed
 *   confidence: low
 */
/* func_ov011_021d1434: build a command record on the stack and submit it.
 * Two stack aggregates: cmd0 @ sp+0x00 (passed to func_02091554/02006c0c) and
 * rec @ sp+0x20 (filled by func_0201d47c, submitted by func_0201e5b8). A 3-way
 * branch on kind selects how cmd0 is populated. STRUCT OFFSETS INFERRED.
 *  func_0201d47c(&rec); rec.h34 = (rec.h34 & ~0xf) | 0x4;
 *  rec.w2c = p->w10; rec.h30 = p->w14; rec.h34 |= 0x10;
 *  kind==0: rec.h34 |= 0x10;
 *           func_02091554(&cmd0, &data_021d3fcc, (p->w8 >> 11) & 0xff);
 *           rec.w20 = func_02006c0c(&cmd0, 4, 0);
 *  kind==1: cmd0.w2c=-1; cmd0.w28=-1;
 *           func_02091554(&cmd0, &data_021d3fcc, 1);
 *           rec.w20 = func_02006c0c(&cmd0, 4, 0);
 *  then:    func_0201e5b8(&rec); func_02006e1c(rec.w20); */
extern char data_ov011_021d3fcc[];
extern int  func_02006c0c(void *table, int b, int c);
extern void func_02006e1c(int handle);
extern void func_0201d47c(void *cmd);
extern void func_0201e5b8(void *cmd);
extern void func_02091554(void *cmd, void *desc, int n);

typedef struct {                 /* rec @ sp+0x20, 0x18+ bytes */
    int            w20;          /* +0x00 (sp+0x20) */
    char           _p04[0x2c - 0x20 - 4];
    int            w2c;          /* +0x0c (sp+0x2c) = p->w10 */
    unsigned short h30;          /* +0x10 (sp+0x30) = p->w14 */
    char           _p32[2];
    unsigned short h34;          /* +0x14 (sp+0x34) flags */
} Ov011Rec;

typedef struct {                 /* cmd0 @ sp+0x00, 0x30 bytes */
    char           _p00[0x28];
    int            w28;          /* +0x28 */
    int            w2c;          /* +0x2c */
} Ov011Cmd;

typedef struct { char _p[0x18]; int w8_dummy; } _force_align;

void func_ov011_021d1434(void *parg, int kind) {
    char *p = (char *)parg;
    Ov011Cmd cmd0;
    Ov011Rec rec;

    func_0201d47c(&rec);
    rec.h34 = (unsigned short)((rec.h34 & ~0xf) | 0x4);
    rec.w2c = *(int *)(p + 0x10);
    rec.h30 = *(unsigned short *)(p + 0x14);
    rec.h34 = (unsigned short)(rec.h34 | 0x10);

    if (kind == 0) {
        rec.h34 = (unsigned short)(rec.h34 | 0x10);
        func_02091554(&cmd0, data_ov011_021d3fcc,
                      (*(int *)(p + 0x8) >> 11) & 0xff);
        rec.w20 = func_02006c0c(&cmd0, 4, 0);
    } else if (kind == 1) {
        cmd0.w2c = -1;
        cmd0.w28 = -1;
        func_02091554(&cmd0, data_ov011_021d3fcc, 1);
        rec.w20 = func_02006c0c(&cmd0, 4, 0);
    }
    func_0201e5b8(&rec);
    func_02006e1c(rec.w20);
}
```

## Struct context
Two local stack structs, both inferred. No match to any indexed type.

| field | offset | type | purpose |
|-------|--------|------|---------|
| Ov011Rec.w20 | +0x00 (sp+0x20) | int | task/handle returned by func_02006c0c |
| Ov011Rec.w2c | +0x0c (sp+0x2c) | int | copied from p->w10 |
| Ov011Rec.h30 | +0x10 (sp+0x30) | unsigned short | copied from p->w14 |
| Ov011Rec.h34 | +0x14 (sp+0x34) | unsigned short | flags: ~0xf\|4 then \|0x10 |
| Ov011Cmd.w28 | +0x28 | int | set to -1 in kind==1 arm |
| Ov011Cmd.w2c | +0x2c | int | set to -1 in kind==1 arm |

`data_ov011_021d3fcc` is a raw `char[]` descriptor table passed to func_02091554.

## Closest matched example
**Path:** src/overlay011/func_ov011_021d1b48.c
**Why similar:** stack-allocating wrapper that makes several sequential calls using local aggregates, with an early-return conditional.
**Key lesson from that file:** The `if (func_ov011_021cf060() == 0) return 1` early-return maps to `moveq r0, #1 / ldmeqia` — the condition must be inverted in C. Also confirmed that calling three sinks sequentially with void result compiles cleanly when declared in the right order; no extra temporaries needed.

## Try this
1. The recipe says three pointers bind to r4/r5/r6 in declaration order. Declare `char *p`, `Ov011Cmd cmd0`, `Ov011Rec rec` in that exact order at function entry (p first so it lands in r4, cmd0 address in r5, rec address in r6). If objdiff shows r4/r5 swapped, swap the two struct declarations.
2. If step 1 doesn't fix a frame mismatch: check that `stmdb sp!, {r3, r4, r5, r6, lr}` matches the prologue (the prep comment mentions a stray r3). If r3 is absent, add a dummy local `int _dummy` or remove the trailing `func_02006e1c` call's int temp. Watch that the `sp+0x20` / `sp+0x00` slot assignments for rec vs cmd0 match what objdiff shows.
3. If frame layout still diverges after 2 reshape attempts, ship as `.s` — the inferred struct offsets and the stray r3 together make this permuter-class rather than clean C.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov011_021d3fcc` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0201d47c` | main TU call |
| `bl` | `func_02091554` | main TU call |
| `bl` | `func_02006c0c` | main TU call |
| `bl` | `func_0201e5b8` | main TU call |
| `bl` | `Task_Invoke` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

