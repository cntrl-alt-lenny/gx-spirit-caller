[//]: # (markdownlint-disable MD013 MD041)

# 02234528 — func_ov002_02234528 (ov002, class F retriage, 296B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** Accesses PerPlayerRowTable and Ov002Slot. The USA reloc `from:0x02234528 kind:arm_call to:0x021d70f0` identifies one callee. At 296B with both structs documented, the PPT stride (0x868) and slot bitfield (id_lo13 :13) are now fully known. The original intractability came from the unknown slot stride and bitfield extraction pattern — both are now deterministic.
**Confidence:** high
**Structs needed:** PerPlayerRowTable (base 0x022CF16C, stride 0x868), Ov002Slot (id_lo13 :13 bitfield at slot+0x000, f_0e at slot+0x00E, stride 0x14)

## Assembly

```asm
; No .s file available for func_ov002_02234528.
; USA reloc: from:0x02234528 kind:arm_call to:0x021d70f0
; This function has not been processed by batch_carve — no GLOBAL_ASM .s exists.
; The dossier is derived from the retriage table entry, USA reloc data, and KB patterns.
; Assembly must be extracted from the baserom before attempting a C match.
```

## Struct context

From the retriage table: **PerPlayerRowTable, Ov002Slot** (high confidence, 296B, class F). The `arm_call to:0x021d70f0` reloc confirms one callee in the function body. At 296B this is likely a single-player or dual-player slot scan with a conditional action call, similar in shape to `func_ov002_02233504` (212B, same struct pair).

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| Ov002FieldZone | +0x000 | zone_count | `int` | loop bound (card-in-zone count) |
| Ov002FieldZone | +0x010 | f_010 | `int` | occupied flag nonzero check |
| Ov002FieldZone | +0x000 × 0x868 | (player row) | stride 0x868 | `mla player, 0x868, base` |
| Ov002Slot | +0x008 | id_lo13 | `unsigned int :13` | `lsl#19/lsr#19` slot guard |
| Ov002Slot | +0x00E | f_0e | `unsigned short` | secondary slot field check |

## Recipe sketch

```c
int func_ov002_02234528(CardObj *self, int arg1) {
    for (int p = 0; p < 2; p++) {
        Ov002FieldZone *row = (Ov002FieldZone *)((char *)data_ov002_022cf16c + (p & 1) * 0x868);
        if (row->f_010 == 0) continue;          /* +0x010 occupied check */
        for (int s = 0; s < 5; s++) {
            Ov002Slot *slot = &row->slots[s];
            if (slot->id_lo13 == 0) continue;   /* :13 bitfield */
            int r = func_ov002_021d70f0(self, p, s, arg1);  /* confirmed USA callee */
            if (r != 0) {
                /* conditional secondary action */
                func_ov002_secondary(self, p, s, slot->f_0e);
            }
        }
    }
    return 0;
}
```

Note: `func_ov002_021d70f0` is known from the USA reloc; verify the EUR equivalent address before use. The exact inner body, additional callee calls, and return value require baserom disassembly.

## Closest matched example

**Path:** src/overlay002/func_ov002_02233504.md (this batch)
**Why similar:** Same PerPlayerRowTable + Ov002Slot struct pair, same loop structure, same id_lo13 guard pattern. The nearby address (0x02233504 vs 0x02234528) suggests they are in the same functional cluster.
**Key lesson:** The USA `arm_call to:0x021d70f0` reloc address is the USA text address; compute the EUR equivalent as `0x021d70f0 + EUR_text_delta` or look it up in `config/eur/arm9/overlays/ov002/symbols.txt`. Do not hard-code the USA address in the EUR C file.

## Try this

1. Extract the assembly from the EUR baserom first — run `tools/batch_carve.py --min-addr 0x0 --overlay ov002 --start 0x02234528 --count 1` to generate the .s file; verify the EUR equivalent of `func_ov002_021d70f0` in `config/eur`.
2. Once the .s is available: confirm whether the outer loop is over 1 or 2 players, and whether the inner loop runs 5 or 11 slots — the `cmp` constant after `add r_slot, r_slot, #0x1` determines the exact bound.
3. Declare PPT stride as `0x868` and slot stride as id_lo13:13 bitfield — if objdiff shows only register assignment mismatches after a first-build attempt, apply the decl-order lever (declare `p`, `s`, and `slot` before any local variables used in the loop body).

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

