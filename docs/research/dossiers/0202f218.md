# 0202f218 — func_0202f218 (main, class D, 204B)

## Recipe + Risk
**Recipe:** class-D switch->membership, sparse cmp-tree; ascending cases
**Risk:** Widely-spread cases force a multi-pool cmp-tree; risk is which constants mwcc loads as ldr pivots vs derives via +offset (0x182a..2c, 0x183f), and tree balance. permuter-class (switch tree/pool choice).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0202f218 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     class-D switch->membership, sparse cmp-tree; ascending cases
 *   risk:       Widely-spread cases force a multi-pool cmp-tree; risk is which constants mwcc loads as ldr pivots vs derives via +offset (0x182a..2c, 0x183f), and tree balance. permuter-class (switch tree/pool choice).
 *   confidence: med
 */
/* func_0202f218: card-id membership test (switch -> 1, default 0).
 * Sparse binary-search tree (no jump-table) with several distinct loaded
 * literals (0x1477,0x1128,0xfe5,0x1246,0x182a) as tree pivots, plus the
 * 0x181f / 0x182a..0x182c / 0x183f cluster derived via +offset. */
int func_0202f218(int a0) {
    switch (a0) {
    case 0xfe4:
    case 0xfe5:
    case 0x1128:
    case 0x1246:
    case 0x1477:
    case 0x181f:
    case 0x182a:
    case 0x182b:
    case 0x182c:
    case 0x183f:
        return 1;
    }
    return 0;
}
```

## Struct context
Pure card-ID membership predicate — no struct field accesses.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| — | — | — | no struct accesses |

## Closest matched example
**Path:** src/main/func_0202effc.c
**Why similar:** Same class-D switch->membership with a pure binary-search compare tree; multiple distinct literal loads as pivots with offset-derived siblings.
**Key lesson:** This function is distinctive in having cases spread from 0xfe4 all the way to 0x183f — a range too sparse for any jump-table. mwcc must issue multiple `ldr` pool loads (at least 5 distinct pivots) and derive nearby constants via `addne`/`subne` from each base. The specific choice of which values become pool roots vs derived offsets (e.g., 0x182a as pool root with 0x182b/0x182c derived via +1/+2, and 0x183f derived further via +0xf) is mwcc's cost-model decision and cannot be forced from C source.

## Try this
1. Drop the prep draft as-is. Verify in objdiff that the 0x182a cluster appears as a single `ldr rN,=0x182a` followed by two `addne` steps for 0x182b and 0x182c, and then either another `addne` or a fresh `ldr` for 0x183f. Also check that 0xfe4/0xfe5 are handled as an adjacent pair (one load + one `addne #1`).
2. If the pool assignment differs (e.g., mwcc makes 0x182b or 0x181f the pool root instead): there is no C reshape that controls which constant is loaded vs derived — this is mwcc's instruction-selection heuristic.
3. Park as permuter-class; the multi-pool pivot selection and offset-chain assignment are compiler-internal and un-forceable without macOS permuter.
