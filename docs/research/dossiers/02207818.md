# 02207818 — func_ov002_02207818 (ov002, class F retriage, 364B)

## Re-triage

**Original verdict:** E/F — previously intractable
**Upgrade reason:** PerPlayerRowTable stride 0x868 (mla pattern) and Ov002Slot id_lo13 bitfield (lsl#19/lsr#19 at +0x30 offset) are now documented. The function walks PerPlayerRowTable slots via the companion array at data_ov002_022cf178 and data_ov002_022cf16c; the "blx callback" (func_ov002_021ca5bc) is an internal helper, not a scheduling coin-flip. The loop bound comes from a count field at base+0xC — all deterministic.
**Confidence:** high
**Structs needed:** PerPlayerRowTable, Ov002Slot

## Assembly

```asm
; func_ov002_02207818 — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .extern data_ov002_022cf16c
        .extern data_ov002_022cf178
        .extern func_0202b878
        .extern func_ov002_021b99b4
        .extern func_ov002_021c23ac
        .extern func_ov002_021c8940
        .extern func_ov002_021ca2b8
        .extern func_ov002_021ca5bc
        .global func_ov002_02207818
        .arm
func_ov002_02207818:
    stmdb sp!, {r3, r4, r5, r6, r7, r8, r9, lr}
    mov r7, r0
    ldrh r0, [r7, #0x2]
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_021ca2b8
    cmp r0, #0x0
    moveq r0, #0x0
    ldmeqia sp!, {r3, r4, r5, r6, r7, r8, r9, pc}
    ldrh r1, [r7, #0x2]
    ldrh r0, [r7, #0x4]
    mov r2, r1, lsl #0x1f
    mov r1, r0, lsl #0x11
    mov r0, r2, lsr #0x1f
    mov r1, r1, lsr #0x17
    bl func_ov002_021b99b4
    cmp r0, #0x0
    movlt r0, #0x0
    ldmltia sp!, {r3, r4, r5, r6, r7, r8, r9, pc}
    mov r4, #0x0
.L_50:
    ldrh r0, [r7, #0x2]
    mov r1, r4
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_021c23ac
    cmp r0, #0x0
    beq .L_98
    ldrh r0, [r7, #0x2]
    mov r1, r4
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    bl func_ov002_021c8940
    cmp r0, #0xb
    cmpne r0, #0xc
    cmpne r0, #0x10
    bne .L_98
    mov r0, #0x1
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, pc}
.L_98:
    add r4, r4, #0x1
    cmp r4, #0x4
    ble .L_50
    ldrh r1, [r7, #0x2]
    ldr r8, _LIT0
    ldr r0, _LIT1
    mov r1, r1, lsl #0x1f
    mov r1, r1, lsr #0x1f
    and r1, r1, #0x1
    mul r2, r1, r8
    ldr r0, [r0, r2]
    mov r6, #0x0
    cmp r0, #0x0
    bls .L_158
    ldr r4, _LIT2
    mov r5, r6
.L_d8:
    add r0, r4, r2
    add r0, r0, #0x120
    ldr r2, [r0, r5]
    mov r0, r2, lsl #0x2
    mov r0, r0, lsr #0x18
    mov r1, r2, lsl #0x12
    mov r0, r0, lsl #0x1
    mov r2, r2, lsl #0x13
    add r0, r0, r1, lsr #0x1f
    mov r9, r2, lsr #0x13
    bl func_ov002_021ca5bc
    cmp r0, #0x0
    beq .L_12c
    mov r0, r9
    bl func_0202b878
    cmp r0, #0xb
    cmpne r0, #0xc
    cmpne r0, #0x10
    bne .L_12c
    mov r0, #0x1
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, pc}
.L_12c:
    ldrh r0, [r7, #0x2]
    add r6, r6, #0x1
    add r5, r5, #0x4
    mov r0, r0, lsl #0x1f
    mov r0, r0, lsr #0x1f
    and r0, r0, #0x1
    mul r2, r0, r8
    add r0, r4, r2
    ldr r0, [r0, #0xc]
    cmp r6, r0
    bcc .L_d8
.L_158:
    mov r0, #0x0
    ldmia sp!, {r3, r4, r5, r6, r7, r8, r9, pc}
_LIT0: .word 0x00000868
_LIT1: .word data_ov002_022cf178
_LIT2: .word data_ov002_022cf16c
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| self (card object) | +0x2 | f2 (bit0 = player bit) | u16 | lsl#1f/lsr#1f extracts player parity for all PPT lookups |
| self (card object) | +0x4 | f4 (multi5 field) | u16 | lsl#11/lsr#17 extracts 5-bit sub-field for 021b99b4 |
| PerPlayerRowTable | base | data_ov002_022cf16c | base | row base for slot array at +0x120 |
| PerPlayerRowTable companion | base | data_ov002_022cf178 | count array | [player*0x868] = event count, used as loop guard |
| PerPlayerRowTable (via cf16c+0xC) | +0x00C | zone count? | int | loop upper-bound: `bcc .L_d8` when r6 < [base+0xC] |
| Ov002Slot | +0x120 offset from PPT base | word at +0x120+i*4 | u32 | packed: bits[25:18] = high field, bits[13:0] = id_lo13 |

## Recipe sketch

```c
int func_ov002_02207818(SelfCard *self) {
    unsigned int player = (unsigned short)self->f2 & 1u;

    /* guard 1: player must be eligible */
    if (func_ov002_021ca2b8(player) == 0)
        return 0;

    /* guard 2: f4 multi-field check */
    unsigned int multi = ((unsigned short)self->f4 << 9) >> 15;
    if (func_ov002_021b99b4(player, multi) < 0)
        return 0;

    /* slot scan 0..4 on own player row */
    for (int s = 0; s <= 4; s++) {
        if (func_ov002_021c23ac(player, s) == 0) continue;
        int kind = func_ov002_021c8940(player, s);
        if (kind == 0xb || kind == 0xc || kind == 0x10)
            return 1;
    }

    /* companion array scan on PPT event list */
    unsigned int stride_off = (player & 1u) * 0x868;
    int count = *(int *)(data_ov002_022cf178 + stride_off);  /* event count */
    if (count <= 0) return 0;

    int *entries = (int *)((char *)data_ov002_022cf16c + stride_off + 0x120);
    for (int i = 0; i < count; i++) {
        int word = entries[i];
        /* extract packed player/slot indices from entry word */
        unsigned int hi_field = ((unsigned int)word << 2) >> 24;
        unsigned int slot_fld = ((unsigned int)word << 18) >> 31;
        unsigned int packed_player = hi_field * 2 + slot_fld;
        unsigned int id = ((unsigned int)word << 19) >> 19;  /* id_lo13 */

        if (func_ov002_021ca5bc(packed_player, id) == 0) continue;
        int kind = func_0202b878(id);
        if (kind == 0xb || kind == 0xc || kind == 0x10)
            return 1;

        /* re-read count for bcc loop check */
        count = *(int *)((char *)data_ov002_022cf16c + (((unsigned short)self->f2 & 1u) * 0x868) + 0xc);
    }
    return 0;
}
```

## Closest matched example

**Path:** src/overlay002/func_ov002_022087f8.c
**Why similar:** Same PerPlayerRowTable stride pattern `(bit0 & 1) * 0x868` for companion array lookup before calling an internal helper.
**Key lesson:** The companion array (cf17c / cf178) uses the same 0x868 stride as cf16c; read its count field first as the loop guard, then index into cf16c at `+0x120` for the slot word array. The id_lo13 extraction `(word << 19) >> 19` must be written as a bitfield or mwcc will use a different shift sequence.

## Try this

1. Declare `self->f2` with `bit0 : 1` bitfield; read `(player & 1) * 0x868` as the stride; read loop bound from `*(int *)(data_ov002_022cf178 + stride_off)` and the slot words from `data_ov002_022cf16c + stride_off + 0x120`.
2. If objdiff shows wrong shift counts on the packed-word extraction, check that the high field uses `(word << 2) >> 24` (6-bit, lsl#2/lsr#18 → lsr#18 for 8-bit then lsl#1) and the id_lo13 uses `(word << 19) >> 19` as the Ov002Slot bitfield extraction.
3. Escalate if the PPT scan inner loop (`bcc .L_d8`) does not match — the loop re-reads player parity each iteration and recomputes stride_off, which may require a separate local var to keep mwcc from hoisting it.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00000868` | MMIO/const |
| `_LIT1` | `data_ov002_022cf178` | raw |
| `_LIT2` | `data_ov002_022cf16c` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021ca2b8` | overlay call (ov002) |
| `bl` | `func_ov002_021b99b4` | overlay call (ov002) |
| `bl` | `func_ov002_021c23ac` | overlay call (ov002) |
| `bl` | `func_ov002_021c8940` | overlay call (ov002) |
| `bl` | `func_ov002_021ca5bc` | overlay call (ov002) |
| `bl` | `func_0202b878` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

