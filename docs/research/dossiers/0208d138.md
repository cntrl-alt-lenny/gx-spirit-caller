# 0208d138 — func_0208d138 (main, class C, 172B)

## Recipe + Risk

**Recipe:** switch (int) for signed bgt/bge tree; 0x180 falls through to 0x80; case order 4/0x180/0x80
**Risk:** permuter-class: switch binary-search pivot (orig roots at cmp #0x80) and the 0x180->0x80 fallthrough block layout are mwcc-version-specific; tree shape may differ even with correct cases.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0208d138 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     switch (int) for signed bgt/bge tree; 0x180 falls through to 0x80; case order 4/0x180/0x80
 *   risk:       permuter-class: switch binary-search pivot (orig roots at cmp #0x80) and the 0x180->0x80 fallthrough block layout are mwcc-version-specific; tree shape may differ even with correct cases.
 *   confidence: med
 */
/* func_0208d138 (main, class C) — field idx9 (=0x12); SWITCH on int (signed
 * bgt/bge tree). Cases 4, 0x80, 0x180 with 0x180 FALLING THROUGH into 0x80
 * (shared .L_244 store reached by both arg==0x80 and the 0x180 arm).
 *   case 4     -> sound[0x242]=0x84
 *   case 0x180 -> sound[0x249]=0x81 ; (ft) sound[0x248]=0x81
 *   case 0x80  -> sound[0x248]=0x81
 */
extern unsigned short data_021a6304[13];
extern void func_0208dce4(int bits);

void func_0208d138(int arg) {
    unsigned short old = data_021a6304[9];
    data_021a6304[9] = (unsigned short)arg;
    data_021a6304[0] = (data_021a6304[0] | old) & ~arg;
    switch (arg) {
    case 0x4:
        *(volatile unsigned char *)0x04000242 = 0x84;
        break;
    case 0x180:
        *(volatile unsigned char *)0x04000249 = 0x81;
        /* fallthrough */
    case 0x80:
        *(volatile unsigned char *)0x04000248 = 0x81;
        break;
    }
    func_0208dce4(data_021a6304[0]);
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| `data_021a6304[0]` | mask word | `unsigned short` | running bitmask OR'd/AND-NOT per arm |
| `data_021a6304[9]` | current-state slot (idx9, byte offset 0x12) | `unsigned short` | stores incoming arg; old read before write |
| `0x04000242` | sound channel register | `volatile unsigned char` | written 0x84 for case 4 |
| `0x04000248` | sound channel register | `volatile unsigned char` | written 0x81 for case 0x80 and the fallthrough from 0x180 |
| `0x04000249` | sound channel register | `volatile unsigned char` | written 0x81 for case 0x180 only |

## Closest matched example

**Path:** src/main/func_0208cc18.c
**Key lesson:** The `data_021a6304` state-table pattern (read old, write new, RMW mask word) is the canonical prologue for this sound-dispatch family. The C switch form is correct for a signed binary-search tree — mwcc's pivot selection (rooting at `cmp #0x80`) is determined by the case-value spread and version, not by C case ordering. The fallthrough from 0x180 into 0x80's body is the key structural constraint: the shared `strb 0x81, [r, #0x248]` store must be reachable from both arms.

## Try this

1. Confirm that the C fallthrough written as `case 0x180:` store then `/* fallthrough */` into `case 0x80:` store is what drives the `.L_244` shared block. Do not use an explicit `goto` — the implicit fallthrough is what mwcc lowers to a single block.
2. Objdiff check: orig roots the comparison tree at `cmp arg, #0x80` (the middle value). If your build roots at a different pivot (e.g. #4 or #0x100), the binary-search tree shape differs — this is the permuter-class divergence.
3. Park as permuter-class if the comparison tree pivot does not match after confirming the case bodies and fallthrough are correct; the tree pivot is set by mwcc's internal case-range analysis and is not directly controllable from C.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021a6304` | data symbol |
| `_LIT1` | `0x04000242` | MMIO/const |
| `_LIT2` | `0x04000249` | MMIO/const |
| `_LIT3` | `0x04000248` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208dce4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

