# 0206e38c — func_0206e38c (main, class C, 220B)

## Recipe + Risk
**Recipe:** orr-accumulator(acc-first); orrgt signed>0; d==1||(flag) gate; irq bracket
**Risk:** `cmp r0,#0;orrgt` is SIGNED >0 on the two helper results -> use `>0` not `!=0` (reshape-able). The `(p->m70&0x40)` ands result is consumed two insns later across the m73 load; if mwcc reorders the ldrsb the orrne flag-carry breaks (permuter-class scheduling).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0206e38c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     orr-accumulator(acc-first); orrgt signed>0; d==1||(flag) gate; irq bracket
 *   risk:       `cmp r0,#0;orrgt` is SIGNED >0 on the two helper results -> use `>0` not `!=0` (reshape-able). The `(p->m70&0x40)` ands result is consumed two insns later across the m73 load; if mwcc reorders the ldrsb the orrne flag-carry breaks (permuter-class scheduling).
 *   confidence: med
 */
/* func_0206e38c (main, class C): accumulate status bits about a sub-object
 * into a flag word, with an IRQ-bracketed middle section. param is the
 * sub-object (m8 u8, m70 s16 flags, m73 s8). */

typedef struct {
    char         _pad_00[0x8];
    unsigned char m8;        /* +0x8  ldrb */
    char         _pad_09[0x70 - 0x9];
    short        m70;        /* +0x70 ldrsh/strh */
    char         _pad_72[1];
    signed char  m73;        /* +0x73 */
} obj_t;

extern int  func_0206e4a4(obj_t *p);
extern int  func_0206e33c(obj_t *p);
extern int  func_0206d79c(obj_t *p);
extern int  OS_DisableIrq(void);
extern void OS_RestoreIrq(int saved);

int func_0206e38c(obj_t *p) {
    int acc = 0;
    signed char d;

    if (func_0206e4a4(p) != 0)
        return acc | 0x80;

    if (p->m70 & 0x40) acc |= 0x20;
    d = p->m73;
    if (d == 1 || (p->m70 & 4)) {
        int saved = OS_DisableIrq();
        if (func_0206e33c(p) > 0) acc |= 1;
        if (func_0206d79c(p) > 0) acc |= 8;
        OS_RestoreIrq(saved);
    }

    if (p->m73 == 0 || p->m73 == 4) {
        if (p->m70 & 4) {
            if (p->m8 != 4) {
                if (!(acc & 1))
                    p->m70 &= ~6;
            }
        }
        if (!(p->m70 & 2)) {
            if (!(p->m70 & 4))
                acc |= 0x40;
        }
    }
    return acc;
}
```

## Struct context
Accesses `obj_t` via pointer param. No SystemWork.

| offset | field | type | use |
|--------|-------|------|-----|
| `+0x8` | `m8` | `unsigned char` | `ldrb`; compared `!= 4` |
| `+0x70` | `m70` | `short` | `ldrsh`: bits 0x40 (acc |= 0x20), 0x4 (IRQ gate + tail), 0x8, 0x2; `strh` via `&= ~6` |
| `+0x73` | `m73` | `signed char` | `ldrsb`: `d = p->m73`; gate `d == 1 || (m70 & 4)`; tail `== 0 || == 4` |

## Closest matched example
**Path:** src/main/func_02062438.c
**Why similar:** Orr-accumulator pattern with IRQ bracket and conditional `|=` from helper results.
**Key lesson:** Both `func_0206e33c(p) > 0` and `func_0206d79c(p) > 0` must use SIGNED `>0` comparison — using `!= 0` produces `orrne` instead of `orrgt`. The `>0` form emits `cmp r0,#0; orrgt acc,acc,#N` exactly as in the baserom. Separately: `d = p->m73` must be declared as `signed char d` and loaded BEFORE entering the IRQ bracket; if `d` is reloaded inside the bracket the ldrsb timing diverges.

## Try this
1. Change any `!= 0` comparisons on the results of `func_0206e33c` and `func_0206d79c` to `> 0` — this forces `orrgt` in the output. If the existing draft already has `> 0`, check objdiff for `orrne` vs `orrgt` at those two sites.
2. If `ldrsb` for `m73` appears inside the IRQ bracket (after `OS_DisableIrq`), move the `d = p->m73;` declaration to before the `if (d == 1 || ...)` test, which is already outside the bracket in the draft.
3. When to park: the orrgt reshape is solvable (reshape-able); however the `(p->m70&0x40)` ands timing across the m73 ldrsb load is noted as permuter-class scheduling — if the ordering of those two loads does not resolve after adding a compiler barrier or reordering the `acc |= 0x20` and `d = p->m73` lines, accept as `.s`.
