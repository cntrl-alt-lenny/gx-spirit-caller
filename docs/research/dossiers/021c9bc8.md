# 021c9bc8 — func_ov006_021c9bc8 (ov006, class C, 140B)

## Recipe + Risk

**Recipe:** void; bind co(r9)/by(r6)/ci(r8)/bx(r7); span via +0x13 guard; stmia out on hit
**Risk:** decl-order: co/by/ci/bx map to r9/r6/r8/r7 (high callee-saved). Bind all before first use. +0x13 guard = early return if out of bounds.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_021c9bc8 (ov006, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     void; bind co(r9)/by(r6)/ci(r8)/bx(r7); span via +0x13 guards; stmia out on hit
 *   risk:       hit path stmleia r3,{r0,r4,r5} bundles {1,inner,outer} as one store; mwcc only fuses it if out[0..2] are sequential int* writes — it should, but a movle-predicated form vs branch may diverge. struct-guessed (out triple) + reshape-able.
 *   confidence: med
 */
/* func_ov006_021c9bc8: locate which [0x7c]x[0x78] cell (origin grids at +0x8c/+0x88,
   step 0x14, span 0x14) contains point (x,y); write {hit, innerIdx, outerIdx} via
   out (r3), or {0} if none. */
void func_ov006_021c9bc8(char *base, int x, int y, int *out) {
    int oi, ii;
    int co = *(int *)(base + 0x7c);
    if (co > 0) {
        int by = *(int *)(base + 0x8c);
        for (oi = 0; oi < co; oi++, by += 0x14) {
            if (y < by) continue;
            if (y > by + 0x13) continue;
            {
                int ci = *(int *)(base + 0x78);
                int bx;
                if (ci <= 0) continue;
                bx = *(int *)(base + 0x88);
                for (ii = 0; ii < ci; ii++, bx += 0x14) {
                    if (x < bx) continue;
                    if (x <= bx + 0x13) {
                        out[0] = 1;
                        out[1] = ii;
                        out[2] = oi;
                        return;
                    }
                }
            }
        }
    }
    out[0] = 0;
    return;
}
```

## Struct context

No named struct; `base` is a `char *` with raw offset accesses:

| offset | type | purpose |
|--------|------|---------|
| +0x78 | int | inner (column) count `ci` |
| +0x7c | int | outer (row) count `co` |
| +0x88 | int | starting X origin for inner loop `bx` |
| +0x8c | int | starting Y origin for outer loop `by` |

`out` is a 3-int array: `out[0]` = hit flag, `out[1]` = inner index `ii`, `out[2]` = outer index `oi`. The risk note identifies the asm produces `stmleia r3,{r0,r4,r5}` — a single STM that bundles the three stores; mwcc fuses sequential `out[0..2]` writes into this automatically.

The decl-order assignment `co(r9)/by(r6)/ci(r8)/bx(r7)` — four callee-saved regs up to r9 — indicates the function pushes 6 callee-saved registers in the prologue. Declaration order in C determines which variable gets which register.

## Closest matched example

**Path:** src/overlay006/func_ov006_021c7edc.c (candidate in this batch — see its dossier)
**Why similar:** both scan the same [0x7c]x[0x78] grid with outer=row / inner=col loops and use bounds from the same base offsets; 021c9bc8 adds a hit-rect test and an output triple.
**Key lesson from that file:** mwcc reloads `*(int *)(base + 0x78)` and `*(int *)(base + 0x7c)` from memory on each outer iteration unless explicitly bound to a local before the loop. For this function `ci` and `bx` are INSIDE the outer-loop body (reloaded each outer pass), while `co` is bound once before the outer loop — matching the register assignment pattern r9/r6 (outer) vs r8/r7 (inner, rebound).

## Try this

1. Build as-is. The key shape is: `co` and `by` bound before the outer loop; `ci` and `bx` re-bound inside the outer-loop body each pass (not hoisted). Confirm the prep already reflects this — `ci` is inside the braced inner block and `bx` is assigned inside the outer loop.
2. If objdiff shows the three `out[0..2]` stores as separate `str` instructions instead of an `stm`, wrap them as `int tmp0 = 1, tmp1 = ii, tmp2 = oi; out[0] = tmp0; out[1] = tmp1; out[2] = tmp2;` to try to force the fusion. If the asm shows `movle r0,#1 / stmleia r3,{r0,r4,r5}`, the `out[0] = 1` may need to be the LAST write (asm order), not the first — try `out[1] = ii; out[2] = oi; out[0] = 1;`.
3. If the decl-order is wrong (r9 not assigned to `co`), swap `int co` and `int by` declarations relative to `oi`/`ii`. Permuter is warranted if only the register numbering in the prologue stmdb differs.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

