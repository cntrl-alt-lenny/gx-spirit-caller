# 02085b38 — func_02085b38 (main, class D, 216B)

## Recipe + Risk
**Recipe:** store-order via out[idx]; reload s16 each use; rsb=negate; asr=signed >>12
**Risk:** dense liveness across 4 calls + interleaved out[0x30]/0x34 mul-shift; mwcc may reorder the two trig-result stores or pick different temp regs — permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02085b38 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     store-order via out[idx]; reload s16 each use; rsb=negate; asr=signed >>12
 *   risk:       dense liveness across 4 calls + interleaved out[0x30]/0x34 mul-shift; mwcc may reorder the two trig-result stores or pick different temp regs — permuter-class.
 *   confidence: low
 */
/* func_02085b38: fixed-point 2x2 rotation/scale matrix setup with 4 trig calls.
 * in @ r1: s16 @0x20,0x22 (angles); s32 @0x24,0x28 (scale); u16 @0x2c,0x2e (scale.frac).
 * out @ r0: fx32 @0x00,0x04,0x10,0x14,0x30,0x34.
 * func_0208be38(int,int) sets the DS divider/sincos; func_0208bed8() returns the result. */
typedef signed short s16;
typedef unsigned short u16;
typedef signed int s32;
typedef signed long long s64;
typedef s32 fx32;

typedef struct {
    char _00[0x20];
    s16 a20;
    s16 a22;
    s32 a24;
    s32 a28;
    char _2c_pre[0]; /* placeholder */
    u16 a2c;
    u16 a2e;
} In;

extern void func_0208be38(int x, int y);
extern int  func_0208bed8(void);

void func_02085b38(fx32 *out, In *in) {
    int c2c = in->a2c << 12;
    int c2e = in->a2e << 12;
    int t;
    func_0208be38(c2e, c2c);
    out[0] = in->a22;
    out[5] = in->a22;
    t = func_0208bed8();
    out[1] = (-in->a20 * t) >> 12;
    func_0208be38(c2c, c2e);
    out[12] = ((in->a2c * (0x1000 - (in->a20 + in->a22))) << 3) - ((in->a24 * in->a2c) << 4);
    out[13] = ((in->a2e * (0x1000 + (in->a20 - in->a22))) << 3) + ((in->a28 * in->a2e) << 4);
    t = func_0208bed8();
    out[4] = (in->a20 * t) >> 12;
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| in+0x20 | a20 | s16 | angle A — must be ldrsh-reloaded at each use site (not cached) |
| in+0x22 | a22 | s16 | angle B — ldrsh-reloaded each use; stored to out[0] and out[5] |
| in+0x24 | a24 | s32 | scale component for out[12] mul correction |
| in+0x28 | a28 | s32 | scale component for out[13] mul correction |
| in+0x2c | a2c | u16 | ldrh; shifted <<12 to form c2c (callee-saved across calls) |
| in+0x2e | a2e | u16 | ldrh; shifted <<12 to form c2e (callee-saved across calls) |
| out[0] | | fx32 | = in->a22 (angle B) |
| out[1] | | fx32 | = (-a20 * t) >> 12 |
| out[4] | | fx32 | = (a20 * t) >> 12 |
| out[5] | | fx32 | = in->a22 (angle B again) |
| out[12] | | fx32 | compound mul-shift (negate+scale correction) |
| out[13] | | fx32 | compound mul-shift (add+scale correction) |

## Closest matched example
**Path:** src/main/func_020808b8.c
**Key lesson:** func_020808b8 (192B, store-order reshape) demonstrates that the exact C statement order controls the `str` interleave — `f4,f8,fc,f0` not ascending address order. Here, the out[12]/out[13] expressions contain compound mul-shift chains; the two calls to `func_0208be38` with swapped argument order (first `(c2e,c2c)` then `(c2c,c2e)`) must appear in the draft exactly as written to match the `bl` pair in the original.

## Try this
1. Keep `in->a20` and `in->a22` as direct struct dereferences at every use — do NOT introduce named locals for them. The s16 fields must emit `ldrsh` from `[r5,#0x20]` / `[r5,#0x22]` each time so that mwcc does not CSE them into scratch registers that displace c2c/c2e from their callee-saved slots.
2. The `out[12]` expression must use the exact sub-form `(0x1000 - (a20 + a22))` and the `out[13]` expression the add-form `(0x1000 + (a20 - a22))` — do not factor or commute these, as the `rsb` vs `add` shape of the negate differs between the two slots.
3. Park as permuter-class; the dense liveness of c2c/c2e across 4 function calls and the interleaved trig-result stores make the register assignment a coin-flip that cannot be forced via C reshape without the macOS permuter.
