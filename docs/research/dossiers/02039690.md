# 02039690 — func_02039690 (main, class C, 336B)

## Recipe + Risk
**Recipe:** mla stride 0xc index; flag bits r7; struct-base bind node(r4); ordered stores 14,8,1c,18,1f,1e,c,1d
**Risk:** struct-guessed and TRUNCATED asm (ends mid .L_b0c). data_0219b314 mla(stride0xc) feeds func_020891fc; func_02038c84 args (#0x60, sub r1-#0x80) are register-derived placeholders. confirm full asm + struct

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02039690 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     mla stride 0xc index; flag bits r7; struct-base bind node(r4); ordered stores 14,8,1c,18,1f,1e,c,1d
 *   risk:       struct-guessed and TRUNCATED asm (ends mid .L_b0c). data_0219b314 mla(stride0xc) feeds func_020891fc; func_02038c84 args (#0x60, sub r1-#0x80) are register-derived placeholders. confirm full asm + struct
 *   confidence: low
 */
#include <stdio.h>

typedef unsigned char u8;
typedef unsigned short u16;
typedef unsigned int u32;
typedef int s32;

struct Desc_9690 {
    u8 _pad0[0x2];
    u16 field_2;       /* +0x2 -> arg to func_020891fc */
    u8 _pad4[0xc - 4];
};

struct Node_9690 {
    u8 _pad0[0x8];
    u32 field_8;
    u32 field_c;
    u8 _pad10[0x14 - 0x10];
    void *field_14;
    u32 field_18;
    u8 _pad1c0;
    u8 field_1d;
    u8 field_1e;
    u8 field_1f;
    u8 sub20[0x20];
};

struct Cfg2 { u8 _pad0[4]; u16 field_4; };
extern struct Cfg2 data_0219c480;
extern struct Desc_9690 data_0219b314[];
extern u32 data_0219b330;
extern u32 data_0219c494;

extern struct Node_9690 *func_020891fc(int idx);
extern struct Node_9690 *func_0203a84c(int a0);
extern struct Node_9690 *func_02038c84(u32 g, void *sub, int sz);
extern int func_02038e58(int z0, int a0, void *sub, int r4);
extern void func_02089008(int a0, void *sub);

int func_02039690(int a0, int r1, int r6) {
    int r7;
    struct Node_9690 **pp;
    struct Node_9690 *node;
    int r5;
    void *sub;
    struct Desc_9690 *d;

    if (data_0219c480.field_4 != 0) return 0;

    d = &data_0219b314[(u16)r1];
    r7 = 0;
    if (r6 & 0x400) {
        r7 = 2;
        if (r6 & 0x200) r7 = 1;
    }

    pp = func_020891fc(d->field_2);
    r5 = (int)pp[0];

    node = func_0203a84c(r5);
    if (node != 0) {
        ((u8 *)node)[0x1c]++;
        return 1;
    }

    sub = (void *)func_02038c84(data_0219b330, (void *)0, 0x60 - 0x80);
    /* mla idx already used; func_02038c84(global, r1<-#0x60, r1-#0x80) */
    if (node == 0) return 0;

    node->field_14 = (void *)(long)r5;
    node->field_8 = 0;
    ((u8 *)node)[0x1c] = 1;
    node->field_18 = 0;
    node->field_1f = (u8)r7;
    node->field_1e = 4;
    node->field_c = 0x40;
    node->field_1d = 2;

    if (r6 & 0x8000) {
        if (func_02038e58(0, r5, &node->sub20, (int)sub) != 0x40) return 0;
        node->field_1d = 1;
        func_02089008(r5, &node->sub20);
    }
    return 0;
}
```

## Struct context
Near-sibling of func_0203953c (same batch). Same global guard (`data_0219c480.field_4`), same node store order (14,8,1c,18,1f,1e,c,1d), same slow-path pattern. Key differences: uses an mla stride-0xc index into `data_0219b314[]` to derive the desc pointer, and the func_02038c84 args are placeholder-derived (`0x60 - 0x80` is a placeholder, must be fixed). No GameSingleton or SystemWork access.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| data_0219b314[idx] +0x2 | field_2 | u16 | passed to func_020891fc as the idx arg |
| node +0x8 | field_8 | u32 | written 0 |
| node +0xc | field_c | u32 | written 0x40 (constant) |
| node +0x14 | field_14 | void* | written with r5 cast |
| node +0x18 | field_18 | u32 | written 0 |
| node +0x1c | (byte) | u8 | fast-path increment; slow-path written 1 |
| node +0x1d | field_1d | u8 | written 2 normally, 1 in tail branch |
| node +0x1e | field_1e | u8 | written constant 4 |
| node +0x1f | field_1f | u8 | written with r7 (flag) |
| node +0x20 | sub20[0x20] | u8[0x20] | address passed to func_02038e58 and func_02089008 |

**WARNING:** The prep draft has placeholders for func_02038c84 args (`(void *)0, 0x60 - 0x80`). Real args must be derived from the full asm. The `if (node == 0)` guard after `func_02038c84` checks the wrong variable (should check `sub`) — this is a known bug in the draft.

## Closest matched example
**Path:** src/main/Task_InvokeLocked.c
**Why similar:** Near-clone of 0203953c — same structure, same store order, same confidence level. See that dossier's example for the matching approach.
**Key lesson:** Near-clone of 0203953c — same recipe, different index computation (mla stride-0xc via data_0219b314 instead of direct nested-if). The store order 14,8,1c,18,1f,1e,c,1d is identical. Fix the func_02038c84 placeholder args and the wrong null-check variable (`sub` not `node`) before attempting to match.

## Try this
1. Fix the two known draft bugs: (a) the func_02038c84 second/third args are placeholders — read full asm; (b) the null-check after func_02038c84 must be `if (sub == 0) return 0;` not `if (node == 0)`.
2. Verify the mla stride-0xc indexing: `d = &data_0219b314[(u16)r1]` requires `data_0219b314` element size to be exactly 0xc bytes — check if `Desc_9690` needs padding to reach 0xc total.
3. If the node store order diverges after fixing args, apply the same store-reordering lesson as func_0203953c; if truncated asm prevents full reconstruction, park for human review.
