# 02027e44 — func_02027e44 (main, class D, 144B)

## Recipe + Risk
**Recipe:** Class D: frame store-order (f2c,f24,f28,f30,f34); bind sub base; (a<0&&b<0) cmplt guard
**Risk:** struct-guessed: stack-slot offsets (0x24/0x28/0x2c/0x30/0x34) and func_020aadf8's &body=sp+4 buffer size are inferred; if any slot/size is off the frame stores reshuffle. Guard is cmp;cmplt;blt.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02027e44 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     Class D: frame store-order (f2c,f24,f28,f30,f34); bind sub base; (a<0&&b<0) cmplt guard
 *   risk:       struct-guessed: stack-slot offsets (0x24/0x28/0x2c/0x30/0x34) and func_020aadf8's &body=sp+4 buffer size are inferred; if any slot/size is off the frame stores reshuffle. Guard is cmp;cmplt;blt.
 *   confidence: low
 */
/* func_02027e44 (main, class D) - build a 0x3c-byte stack descriptor and submit it.
 * if (!p->h68) return 1; Fill32(0,&s,0x3c); s.f00=1; func_020aadf8(&s.body, p->h68);
 * s.f2c=&p->sub; s.f24=sub->f0c; s.f28=(s16)sub->f10; s.f30=&func_02027ed4; s.f34=p;
 * if (!(s.f24<0 && s.f28<0)) func_02022b74(&s); set de bit4. Returns 1.
 * sub is at p+0x8c; bound across the 3 reads. */
typedef short s16;
typedef unsigned short u16;

extern void Fill32(int v, void *dst, int n);
extern void func_020aadf8(void *dst, void *src);
extern void func_02022b74(void *desc);
extern void func_02027ed4(void);

typedef struct Sub8c {
    char _p0[0xc];
    int   f0c;          /* p+0x98 */
    short f10;          /* p+0x9c signed */
} Sub8c;

typedef struct Obj27e44 {
    char  _p0[0x68];
    void *h68;          /* 0x68 */
    char  _p1[0x8c - 0x6c];
    Sub8c sub;          /* 0x8c */
    char  _p2[0xde - (0x8c + 0x12)];
    u16   b0:1, b1:1, b2:1, b3:1, b4:1;   /* 0xde */
} Obj27e44;

typedef struct StkE44 {
    int   f00;                 /* 0x00 = 1 */
    char  body[0x24 - 0x04];   /* 0x04.. filled by func_020aadf8 */
    int   f24;                 /* sub->f0c */
    int   f28;                 /* (s16)sub->f10 */
    void *f2c;                 /* &p->sub */
    void *f30;                 /* &func_02027ed4 */
    void *f34;                 /* p */
    char  _tail[0x3c - 0x38];
} StkE44;

int func_02027e44(Obj27e44 *p) {
    StkE44 s;
    Sub8c *sub = &p->sub;
    if (p->h68 == 0)
        return 1;
    Fill32(0, &s, 0x3c);
    s.f00 = 1;
    func_020aadf8(&s.body, p->h68);
    s.f2c = sub;
    s.f24 = sub->f0c;
    s.f28 = sub->f10;
    s.f30 = (void *)func_02027ed4;
    s.f34 = p;
    if (!(s.f24 < 0 && s.f28 < 0))
        func_02022b74(&s);
    p->b4 = 1;
    return 1;
}
```

## Struct context
This function operates on a local object struct `Obj27e44` and a stack-allocated descriptor `StkE44`. It does NOT touch GameSingleton or SystemWork directly.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x68 | h68 | void* | null guard; passed as src to func_020aadf8 |
| +0x8c | sub (Sub8c) | struct | base of sub-object; pointer bound to `sub` |
| +0x98 (sub+0x0c) | sub->f0c | int | copied to frame[0x24/4]; used in (<0) guard |
| +0x9c (sub+0x10) | sub->f10 | short | ldrsh; copied to frame[0x28/4]; used in (<0) guard |
| +0xde | b4 (u16 bitfield) | :1 | set to 1 at the end |

Stack frame slots (StkE44):

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| sp+0x00 | f00 | int | set to 1 |
| sp+0x04 | body | char[] | filled by func_020aadf8 |
| sp+0x24 | f24 | int | sub->f0c |
| sp+0x28 | f28 | int | (s16)sub->f10 |
| sp+0x2c | f2c | void* | &p->sub |
| sp+0x30 | f30 | void* | &func_02027ed4 |
| sp+0x34 | f34 | void* | p itself |

## Closest matched example
**Path:** src/main/func_02027ddc.c
**Why similar:** Sibling function in the same object family; same h68/sub/de bitfield layout, ~same size, store-order discipline.
**Key lesson:** The `int h = p->cc; if (h >= 0)` pattern binds a signed halfword once to force a single ldrsh used for both the branch and the call arg. For 02027e44, the dual-field guard `(s.f24 < 0 && s.f28 < 0)` similarly requires the stored int temporaries — once stored into the stack frame they are reloaded from there (not re-ldrsh'd from sub). If objdiff shows two ldrsh for f0c or f10, bind them to explicit `int` locals before the stores.

## Try this
1. Build as-is. If frame stores reorder in objdiff, swap the C store lines for f2c/f24/f28 to match the exact orig sequence: `s.f2c = sub; s.f24 = sub->f0c; s.f28 = sub->f10; s.f30 = ...; s.f34 = p;` — the prep already has this order; verify it landed that way in the asm output.
2. If objdiff shows an extra ldrsh after func_020aadf8 for sub->f10, add an explicit `s16 tmp10 = sub->f10;` bound before `Fill32`, then `s.f28 = tmp10;` — this pins the single ldrsh.
3. If the `!(a<0 && b<0)` guard emits as cmp+bge instead of cmp+cmplt+blt, try inverting: `if (s.f24 < 0 && s.f28 < 0) goto skip; func_02022b74(&s); skip:` with a label — or escalate to permuter for scheduling if reshapes exhaust.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `func_02027ed4` | func ptr |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Fill32` |  |
| `bl` | `func_020aadf8` | main TU call |
| `bl` | `func_02022b74` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

