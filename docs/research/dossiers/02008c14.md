# 02008c14 — func_02008c14 (main, class D, 112B)

## Recipe + Risk
**Recipe:** class D: signed int %/>> => asr/lsr/ror bit-index; decl base+i before buf; bitset OR
**Risk:** reshape-able: the signed `i>>5`/`i&31` MUST stay signed int (an unsigned i collapses the ror trick to a plain lsr and diverges). Secondary: store-order base[1] then base[0]; struct word[] size guessed but only [0..1] touched here.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02008c14 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     class D: signed int %/>> => asr/lsr/ror bit-index; decl base+i before buf; bitset OR
 *   risk:       reshape-able: the signed `i>>5`/`i&31` MUST stay signed int (an unsigned i collapses the ror trick to a plain lsr and diverges). Secondary: store-order base[1] then base[0]; struct word[] size guessed but only [0..1] touched here.
 *   confidence: med
 */
/* func_02008c14: zero two global words, then for signed int i in [0,50):
 *   if (func_02008d60(i, &buf)) bitset[i>>5] |= 1u << (i & 31);
 * The asr/lsr/ror dance in asm is exactly mwcc's signed i/32 (word idx)
 * and signed i%32 (bit pos) for an `int` loop var. Use signed int i and
 * a u32* base; declare base(r6) and i(r7) BEFORE buf so r6/r7 alloc first.
 * buf is a 0x124-byte (>=292B) scratch passed to the callee. */
typedef struct {
    unsigned int word[2 + 0x10]; /* +0x0,+0x4 zeroed; bitset words follow */
} Glob_02105bb0;

extern Glob_02105bb0 data_02105bb0;
extern int func_02008d60(int i, void *buf);

void func_02008c14(void) {
    unsigned int *base = data_02105bb0.word;
    int i;
    char buf[0x124];
    base[1] = 0;
    base[0] = 0;
    for (i = 0; i < 50; i++) {
        if (func_02008d60(i, buf)) {
            base[i >> 5] |= 1u << (i & 31);
        }
    }
}
```

## Struct context
`data_02105bb0` is the render-slot bitset array. The naming plan names this function `Render_ScanSlots`.

| offset | field | type | use |
|--------|-------|------|-----|
| data_02105bb0 +0x0 | word[0] | u32 | zeroed (base[0] = 0, written second) |
| data_02105bb0 +0x4 | word[1] | u32 | zeroed (base[1] = 0, written first) |
| data_02105bb0 +0x0 .. +0x4 | bitset | u32[2] | OR-accumulated by `1u << (i & 31)` indexed on `i >> 5` |

**Store order:** `base[1] = 0` comes BEFORE `base[0] = 0` in asm. This is already reflected in the draft. If objdiff shows them swapped, swap those two lines.

**Register allocation rule:** `base` and `i` must be declared BEFORE `buf` so MWCC assigns them to r6/r7 (callee-saved), leaving `buf` on the stack frame beyond those regs. If the order is wrong the bitset index computation diverges.

## Closest matched example
**Path:** src/main/func_020092c8.c
**Why similar:** Also uses a bitset pattern — `data_02105f3c[0x874] & (1 << bit)` — with `int` typed bit index producing the `lsl rN` register-shift form.
**Key lesson:** MWCC's signed `int i` division/modulo by 32 emits `asr #5` for `i >> 5` and the `ror` trick for `i & 31` (= `i % 32` when `i >= 0`). If `i` is declared `unsigned`, both collapse to `lsr #5` and plain `lsr`, and the ror disappears. The signed int type is non-negotiable.

## Try this
1. Build as-is. First check in objdiff that `i` is signed — the asm should contain an `asr #5` (not `lsr #5`) for the word index. If `lsr` appears instead, `i` has been widened to unsigned somewhere; verify `int i` is `signed int` (default in C) and no cast to `unsigned` is present.
2. If the store-order `base[1] = 0` / `base[0] = 0` pair is swapped in the output, swap the two lines in source — MWCC emits in source order for adjacent stores to the same struct field.
3. When to park: if the `buf` pointer scheduling (when func_02008d60 is called with `&buf`) diverges due to the frame layout (wrong `sp+N` offset in the bl arg), that means buf's sp offset is wrong — try changing `char buf[0x124]` to `int buf[0x49]` (same size, word-aligned) to see if alignment changes the offset. If the offset still diverges after one attempt, park as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02105bb0` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02008d60` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

