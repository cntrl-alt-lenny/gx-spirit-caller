# 020097f0 — func_020097f0 (main, class D, 376B)

## Recipe + Risk
**Recipe:** D branch-table: ldrh-clamp, even-round divmod, fall-through addls pc jump-table
**Risk:** struct-guessed + truncated: asm ends mid-body at `cmp r0,#0x7`; the epilogue and remaining bit-ORs (reading data_02104f3c[0x938]) are absent, so a full byte-match is impossible from this input. The fall-through case ordering and `sel & ~1` rounding are inferred.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_020097f0 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D branch-table: ldrh-clamp, even-round divmod, fall-through addls pc jump-table
 *   risk:       struct-guessed + truncated: asm ends mid-body at `cmp r0,#0x7`; the epilogue and remaining bit-ORs (reading data_02104f3c[0x938]) are absent, so a full byte-match is impossible from this input. The fall-through case ordering and `sel & ~1` rounding are inferred.
 *   confidence: low
 */
/* WARNING: source asm is TRUNCATED at `cmp r0,#0x7` (no epilogue / tail).
   Only the reconstructable prefix is modeled; the full body is incomplete. */
extern short data_0210593c[];
extern unsigned int data_02104f3c[];
extern void func_0201a3ec(void);
extern void func_0201a498(void);

int func_020097f0(void)
{
    int mask;
    int i;
    int sel;

    mask = 0;
    for (i = 0; i < 4; i++)
        mask |= 1 << i;

    sel = data_0210593c[0x62 / 2];
    if (sel > 0x1c)
        sel = 0x1c;
    if (sel % 2 != 0)
        sel -= 1;

    switch (sel) {
    case 0x1c: mask |= 0x2000000;
    case 0x1a: mask |= 0x1000000;
    case 0x18: mask |= 0x800000;
    case 0x16: mask |= 0x400000;
    case 0x14: mask |= 0x200000;
    case 0x12: mask |= 0x80000;
    case 0x10: mask |= 0x20000;
    case 0x0e: mask |= 0x4000;
    case 0x0c: mask |= 0x1000;
    case 0x0a: mask |= 0x400;
    case 0x08: mask |= 0x200;
    case 0x06: mask |= 0x100;
    case 0x04: mask |= 0x40;
    case 0x02: mask |= 0x10;
    default: break;
    }

    /* body continues: reads data_02104f3c[0x938/4], cmp #7, ... (asm truncated) */
    return mask;
}
```

## Struct context
This function reads two globals:

| offset | field | type | use |
|--------|-------|------|-----|
| `data_0210593c[0x31]` (`0x62/2`) | — | `short` | selector word: clamped to 0x1c, rounded down to even, drives switch |
| `data_02104f3c[0x938/4]` | — | `unsigned int` | read in the truncated tail; role unknown (likely another mask OR) |

`data_02104f3c` appears in MedCandidateNeeds main section as a "system control word" with `:4` bitfield at bits accessed via `lsl#15;lsr#28`. The `[0x938]` offset is separate from those confirmed fields and is referenced from `struct-context-main.md` as `+0x938` of SystemWork (`data_02104f58`), but the access here is via the independent global `data_02104f3c`, not GetSystemWork. The naming plan proposes `func_020097f0 | Overlay_DispatchLoad`.

The externs `func_0201a3ec` / `func_0201a498` are named `SysWork_ApplyLevelFilter` / `SysWork_ApplyTripleFilter` in the naming plan; they appear in the tail that is truncated from the asm dump.

## Closest matched example
**Path:** src/main/func_0201a530.c
**Why similar:** Also a class-D function that discards GetSystemWork, uses a switch with a `cmp #7` branch in the reconstructed portion, and reads from `data_02104f3c`.
**Key lesson:** The even-rounding `sel & ~1` (written as `if (sel % 2 != 0) sel -= 1`) must keep the explicit subtract form — mwcc emits `tst r0,#1; subne r0,r0,#1` from the conditional subtract, not a `bic`. If it emits `bic r0,r0,#1` instead, reshape to `sel &= ~1` (direct AND), which matches the `bic` encoding.

## Try this
1. The asm is **truncated**: a byte-identical match of the full function is not achievable with the current prep draft — the epilogue and the tail `cmp r0,#0x7` / subsequent case arms are missing. Submit what is here as a starting point, note the `/* body continues */` comment, and confirm in objdiff that the prefix (up to the switch) matches before attempting to reconstruct the tail.
2. If objdiff shows the switch arms in the wrong order or the fall-through collapses to a jump-table with a different base, reorder the `case` values: mwcc emits fall-through switch as `addls pc, pc, r0, lsl #2` — the case values must be in descending order exactly as written. If mwcc emits individual `bic`/`orr` pairs instead of a fall-through chain, add `default:` as the last arm (already present) and ensure no intermediate `break` statements exist.
3. Park condition: this function is **truncated at source** — full byte-match requires the missing tail from a fuller asm dump. Do not spend more than one iteration on the prefix; file as `.s` or revisit when a complete asm capture is available.
