# 0207cff4 — func_0207cff4 (main, class D, 104B)

## Recipe + Risk
**Recipe:** e=self+off bound once; predicated ldreq/streq/ldrne branches; ordered prev/next zeroing
**Risk:** orig reads the discriminant e->prev via index form `ldr [r1,ip]` while keeping `lr=self+off` for the rest, and RELOADS off (`ldrh [r0,#0xa]`) on the next-side; mwcc with one `e` ptr emits `[e,#0]` and reuses cached off -> diverges on those 2 loads. permuter-class (mixed index/imm addressing won't come from clean C).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0207cff4 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     e=self+off bound once; predicated ldreq/streq/ldrne branches; ordered prev/next zeroing
 *   risk:       orig reads the discriminant e->prev via index form `ldr [r1,ip]` while keeping `lr=self+off` for the rest, and RELOADS off (`ldrh [r0,#0xa]`) on the next-side; mwcc with one `e` ptr emits `[e,#0]` and reuses cached off -> diverges on those 2 loads. permuter-class (mixed index/imm addressing won't come from clean C).
 *   confidence: med
 */
/* func_0207cff4: unlink a node from an intrusive doubly-linked list.
 * r0=hdr, r1=this node's base. off = hdr->off (+0xa, u16).
 * The Link record (prev@+0, next@+4) is embedded at  node_base + off.
 * prev/next/head/tail all store NODE BASE pointers (same off indexes each).
 *   e        = (Link*)(self + off);                 // [r1,ip] / add lr,r1,ip
 *   if (e->prev == 0)  hdr->head(+0) = e->next;
 *   else  ((Link*)(e->prev + off))->next = e->next;  // [ (r3+ip)+4 ]
 *   if (e->next == 0)  hdr->tail(+4) = e->prev;
 *   else  ((Link*)(e->next + off))->prev = e->prev;  // [r3, r1(=off reload)]
 *   e->prev = 0; e->next = 0;
 *   hdr->count(+0x8,u16)--;
 */
typedef struct {
    char *prev;  /* +0x0 : neighbour node base */
    char *next;  /* +0x4 : neighbour node base */
} Link;

typedef struct {
    char *         head;   /* +0x0 */
    char *         tail;   /* +0x4 */
    unsigned short count;  /* +0x8 */
    unsigned short off;    /* +0xa */
} ListHdr;

void func_0207cff4(ListHdr *hdr, char *self) {
    unsigned int off = hdr->off;
    Link *e = (Link *)(self + off);

    if (e->prev == 0) {
        hdr->head = e->next;
    } else {
        ((Link *)(e->prev + off))->next = e->next;
    }

    if (e->next == 0) {
        hdr->tail = e->prev;
    } else {
        ((Link *)(e->next + off))->prev = e->prev;
    }

    e->prev = 0;
    e->next = 0;
    hdr->count = hdr->count - 1;
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| `hdr+0x0` | head | `char*` | list head (node base ptr) |
| `hdr+0x4` | tail | `char*` | list tail (node base ptr) |
| `hdr+0x8` | count | `unsigned short` | decremented on unlink |
| `hdr+0xa` | off | `unsigned short` | byte offset of Link pair inside each node |
| `node+off+0x0` | prev | `char*` | previous node base (0 = at head) |
| `node+off+0x4` | next | `char*` | next node base (0 = at tail) |

## Closest matched example
**Path:** src/main/List_Unlink.c
**Key lesson:** The standard List_Unlink uses simple struct-pointer access (`node->prev`, `node->next`) with predicated `strne` for the if-not-null guards. Here the intrusive offset-based form (`ldr [r1,ip]` index addressing to read `e->prev`) is fundamentally different from what mwcc emits for a clean C `Link *e` pointer — the asm keeps `self` (r1) and `off` (ip) as separate registers and forms the address at load time, which clean C cannot force. This is the root of the permuter-class classification.

## Try this
1. Park as permuter-class; the `ldr [r1,ip]` / `ldrh [r0,#0xa]` reload pair is a register-addressing wall that mwcc will not reproduce from clean C pointer arithmetic — accept as `.s`.
2. If attempting anyway: split `off` into a second `unsigned int` reload before the `e->next` side (declare `unsigned int off2 = hdr->off;` between the two if-blocks) to try to force the `ldrh` reload on the else branch of the next test.
3. Escalation: if any C reshape closes the prev-side but the next-side `ldrh` reload still diverges, park as permuter-class; bit-guard and addressing form are un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

