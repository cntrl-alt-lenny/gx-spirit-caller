# 021aaa80 — func_ov020_021aaa80 (ov019, class C, 252B)

## Recipe + Risk
**Recipe:** C: bind r4 base, store-order clamps, if-assign min, inline mul
**Risk:** func_020b3870 returns in r0 but orig consumes its result via add r5/lsl chains; if mwcc schedules the post-bl mov differently the rotation diverges. permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021aaa80 (ov020, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     C: bind r4 base, store-order clamps, if-assign min, inline mul
 *   risk:       func_020b3870 returns in r0 but orig consumes its result via add r5/lsl chains; if mwcc schedules the post-bl mov differently the rotation diverges. permuter-class.
 *   confidence: low
 */
/* func_ov020_021aaa80: per-entry window animator. Decrement-clamp a timer,
 * lerp a base offset via func_020b3870, pack into the 0x04000018 window MMIO,
 * then either fade-in (func_020944a4) or run a second 0x8c0 timer + func_ov020_021add24.
 * (ov020) class C: hold r4 base across all uses, store-order on the clamps. */
extern int data_ov020_021ae060;
extern int data_ov020_021ae0a8;
extern int func_020944a4(void *dst, int src, int n);
extern int func_020b3870(int a, int b);
extern void func_ov020_021add24(void *dst, int src, int mode);

void func_ov020_021aaa80(int idx) {
    int base = idx * 0x28 + 0x18;
    char *g = (char *)&data_ov020_021ae0a8;
    if (*(int *)((char *)&data_ov020_021ae060 + 0x8d0) > 0) {
        int d = (*(int *)(g + 0x89c) - idx) * 0x28;
        int t = *(int *)(g + 0x88c) - 0x1000;
        int v;
        *(int *)(g + 0x88c) = t;
        if (t < 0) {
            t = 0;
            *(int *)(g + 0x88c) = 0;
        }
        t = *(int *)(g + 0x88c);
        v = func_020b3870(d * t, *(int *)(g + 0x888));
        base += v;
        if (t == 0)
            *(int *)(g + 0x888) = 0;
    }
    *(volatile int *)0x04000018 = (0x01ff0000 & (-base << 0x10)) | 0x1fc;
    if ((*(unsigned int *)(g + 0xa50) << 0x17) >> 0x1f) {
        int t = *(int *)(g + 0x8c0) - 0x1000;
        int v;
        *(int *)(g + 0x8c0) = t;
        if (t < 0)
            *(int *)(g + 0x8c0) = *(int *)(g + 0x8c4);
        v = func_020b3870(*(int *)(g + 0x8c0) * 0x168, *(int *)(g + 0x8c4));
        v = (unsigned short)v;
        func_ov020_021add24(g + 0x8a0, 0x05000180, (v << 0x10) | 0x10);
    } else {
        func_020944a4(g + 0x8a0, 0x05000180, 0x20);
    }
}
```

## Struct context
Two ov020 globals accessed by byte-offset. No confirmed struct covers these offsets yet (struct-guessed):

| symbol | offset | type | purpose |
|--------|--------|------|---------|
| data_ov020_021ae060 +0x8d0 | +0x8d0 | int | gate: if > 0, run the lerp block |
| data_ov020_021ae0a8 +0x888 | +0x888 | int | lerp denominator / scale |
| data_ov020_021ae0a8 +0x88c | +0x88c | int | timer 1: decremented by 0x1000, clamped to 0 |
| data_ov020_021ae0a8 +0x89c | +0x89c | int | base index for `d` computation |
| data_ov020_021ae0a8 +0x8a0 | +0x8a0 | void* | destination object for fade/animate calls |
| data_ov020_021ae0a8 +0x8c0 | +0x8c0 | int | timer 2: decremented, then reset to +0x8c4 on underflow |
| data_ov020_021ae0a8 +0x8c4 | +0x8c4 | int | timer 2 reset value |
| data_ov020_021ae0a8 +0xa50 | +0xa50 | uint | bitfield: bit tested via (x << 0x17) >> 0x1f |

The `(*(unsigned int *)(g + 0xa50) << 0x17) >> 0x1f` expression extracts bit 8 (0-indexed from LSB) of the word at +0xa50 — this is the standard single-bit extract idiom.

The clamp pattern at +0x88c mirrors src/overlay018/func_ov018_021accc8.c exactly: decrement, store, if-underflow assign 0 and re-store, then re-read from memory before the division call.

## Closest matched example
**Path:** src/overlay018/func_ov018_021accc8.c
**Why similar:** identical decrement-clamp-reload pattern (`t = *(ptr) - 0x1000; *(ptr) = t; if (t < 0) *(ptr) = 0; t = *(ptr);`), followed by `func_020b3870` called with the clamped value, and a final MMIO write with a negative-shifted pack.
**Key lesson from that file:** After the clamp `if (t < 0) *(ptr) = 0;`, the value used in `func_020b3870` must be a FRESH LOAD (`t = *(ptr)`), not the local `t` — even though `t` was just set to 0. mwcc emits an unconditional reload here. The post-`bl` result of `func_020b3870` goes directly into a register that feeds the `add r5` chain; do not bind it to a named local before the add if that disrupts the chain.

## Try this
1. Build as-is. In objdiff, look at the instructions after `bl func_020b3870`: if `v` (r0 return) is consumed by `add base,base,r0` but orig shows `add r5,r5,r0,lsl#k`, try writing `base += v << k` where k is the observed shift amount (confirm from disasm).
2. For the second `func_020b3870` call (timer-2 block): if `v = (unsigned short)v` causes an extra zero-extension instruction, try casting inline: `func_ov020_021add24(g + 0x8a0, 0x05000180, ((unsigned short)func_020b3870(...) << 0x10) | 0x10)` — this avoids a `lsl`/`lsr` pair.
3. Fallback: confidence LOW and permuter-class. The `func_020b3870` post-call scheduling divergence is a known permuter trigger. After one reshape round, submit to permuter with the add/lsl chain as focus, or park as .s.
