# 02000d9c — func_02000d9c (main, class C, 152B)

## Recipe + Risk
**Recipe:** bind e(r3)+g(r4) across uses; cmpne OR-chain; reload g->0xb64 in loop
**Risk:** orig holds g=data_021040ac in r4 across all stores AND reloads g->0xb64 each loop iter; if mwcc reloads the g base or hoists the b64 load, the ldr/str pattern diverges — reshape-able (single g local, re-read field in loop). f_10/0x3c struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02000d9c (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     bind e(r3)+g(r4) across uses; cmpne OR-chain; reload g->0xb64 in loop
 *   risk:       orig holds g=data_021040ac in r4 across all stores AND reloads g->0xb64 each loop iter; if mwcc reloads the g base or hoists the b64 load, the ldr/str pattern diverges — reshape-able (single g local, re-read field in loop). f_10/0x3c struct-guessed.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_02000d9c (main, class C) — front-load.
 *   recipe: BIND e=base+(n-1)*16 (held r3) and g=data_021040ac (held r4); cmpne OR-chain
 * count at base+0x80; copy e->{f_4,f_8,f_c} into g->{0x3c,0x40,0x44}; g->0xb64=e->f_10-1;
 * walk while (g->0xb64+1) in {0x16,0x36,0x2e,0x38,0x26}; return (val>=4).
 */
extern char data_02105904[];
extern char data_021040ac[];
extern int func_02000d4c(void);

int func_02000d9c(void) {
    char *g;
    char *e;
    int n;
    int x;
    n = *(int *)(data_02105904 + 0x80);
    if (n == 0)
        return 0;
    e = data_02105904 + ((n - 1) << 4);
    g = data_021040ac;
    *(int *)(g + 0x3c) = *(int *)(e + 0x4);
    *(int *)(g + 0x40) = *(int *)(e + 0x8);
    *(int *)(g + 0x44) = *(int *)(e + 0xc);
    *(int *)(g + 0xb64) = *(int *)(e + 0x10) - 1;
    while (1) {
        x = *(int *)(g + 0xb64) + 1;
        if (x == 0x16 || x == 0x36 || x == 0x2e || x == 0x38 || x == 0x26) {
            *(int *)(g + 0xb64) = func_02000d4c() - 1;
            *(int *)(g + 0x3c) = 0;
            *(int *)(g + 0x40) = 0;
        } else {
            break;
        }
    }
    return x >= 4;
}
```

## Struct context
This function touches `data_02105904` (snapshot stack) and `data_021040ac` (GlobalAudioState). State-machine values at `+0xb64` are pending-action codes (0x16/0x36/0x2e/0x38/0x26 = blocked states that need further unwinding). See MedCandidateNeeds.md main section.

| offset | field | type | use |
|--------|-------|------|-----|
| `data_02105904+0x80` | count word | `int` | read; if 0 return early |
| `data_02105904+(n-1)<<4+0x4` | e->f_4 | `int` | restored to data_021040ac+0x3c |
| `data_02105904+(n-1)<<4+0x8` | e->f_8 | `int` | restored to data_021040ac+0x40 |
| `data_02105904+(n-1)<<4+0xc` | e->f_c | `int` | restored to data_021040ac+0x44 |
| `data_02105904+(n-1)<<4+0x10` | e->f_10 | `int` | source of new g->0xb64 (= e->f_10 - 1); struct-guessed |
| `data_021040ac+0x3c` | snapshot field 0 | `int` | written from e->f_4; zeroed on loop continue |
| `data_021040ac+0x40` | snapshot field 1 | `int` | written from e->f_8; zeroed on loop continue |
| `data_021040ac+0x44` | snapshot field 2 | `int` | not zeroed on loop continue |
| `data_021040ac+0xb64` | state machine code | `int` | written, then re-read each loop iteration (must NOT be hoisted) |

## Closest matched example
**Path:** src/main/func_02000ec8.c
**Why similar:** same data_021040ac base held in one local across multiple field stores/reads, with a call interleaved; demonstrates that MWCC keeps a single pointer variable in one register across stores.
**Key lesson:** The `g` pointer local must be assigned once (`g = data_021040ac`) and then all field accesses must go through `g`. If mwcc reloads the base pointer across the loop boundary, MWCC will emit extra ldr instructions. The critical requirement is that `*(int *)(g + 0xb64)` inside the loop is re-read from memory each iteration (i.e. `x = *(int*)(g+0xb64) + 1` at the top of the loop body), NOT hoisted before the loop. If objdiff shows the 0xb64 load is outside the loop, introduce a `volatile` read or restructure as a `do { ... } while(...)`.

## Try this
1. Build the prep draft as-is. In objdiff verify that `ldr` of `g+0xb64` appears inside the loop body at the top (producing `x`), not hoisted before the while(1). If it is hoisted, change `*(int *)(g + 0xb64)` in the condition to use a `volatile int *` cast: `x = *(volatile int *)(g + 0xb64) + 1;`.
2. If the OR-chain (`x == 0x16 || x == 0x36 || ...`) generates a jump-table instead of a compare tree, the case ordering is wrong — MWCC uses a cmp-tree for a sparse set. Verify by checking objdiff for a `ldr pc, [...]` jump-table pattern; if present, convert the if-chain to a switch with those exact case values in ascending order.
3. When to park: if the instruction sequence and operands match but the `cmpne` chain uses different intermediate registers, that is permuter-class — accept as `.s`.
