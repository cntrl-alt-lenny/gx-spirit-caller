# 021aa5b0 — func_ov022_021aa5b0 (ov022, class C, 244B)

## Recipe + Risk

**Recipe:** CLASS C: six-call dispatch; each call with different subset of live regs
**Risk:** six calls: different live regs per call. decl-order controls which regs stay live.

## Prep draft

```
NO PREP FILE EXISTS for 021aa5b0 in docs/research/c-match-prep/.
The dossier is written from the index recipe/risk alone.
```

The batch index describes this as a 244B ov022 class C function. "Six-call dispatch" means the function makes six sequential calls (to different callees), with each call having a different subset of live caller-saved registers. At class C (HIGH confidence), the shape should be straightforward once the decl-order is correct.

Based on the recipe, the expected shape is:

```c
/* INFERRED SKELETON — no prep file; reconstruct from recipe + overlay context */
/* func_ov022_021aa5b0: six-call dispatch sequence. Decl-order of locals
 * controls which regs remain live across each call boundary. */
int func_ov022_021aa5b0(/* args TBD */) {
    /* declare locals in an order that binds them to callee-saved regs */
    /* call 1 */ func_A(...);
    /* call 2 */ func_B(...);
    /* call 3 */ func_C(...);
    /* call 4 */ func_D(...);
    /* call 5 */ func_E(...);
    /* call 6 */ func_F(...);
    return 1; /* or some accumulation */
}
```

The exact callees and arguments are unknown without the prep file or disassembly.

## Struct context

ov022 has no established struct vocabulary yet — only data files are matched (`data_ov022_021ab8a8.c`, `data_ov022_021ab9a0.c`). The six callees are likely a mix of ov022-local functions and shared engine helpers.

The closest structural analogs are the ov021 "enter" methods which also sequence multiple calls to bind subobjects and set state:

| pattern | source | purpose |
|---------|--------|---------|
| multi-call init | func_ov021_021aa898.c | 3-call sequence; state word set at end |
| multi-call layout | func_ov021_021aaadc.c | 8-call sequence; returns 1 |

## Closest matched example

**Path:** src/overlay021/func_ov021_021aaadc.c (relative to C:/Users/leona/Dev/gx-spirit-caller/kb-map)
**Why similar:** Multi-call dispatch sequence (8 calls) with locals bound to callee-saved regs across call boundaries; returns 1 at the end. The decl-order lever is confirmed for ov021 (wave 2, 60%->100%): declare counter/accumulator locals BEFORE the `char *base` pointer to give them r4/r5 and push the base to r6.
**Key lesson from that file:** When a sequence of calls has different live regs per call, mwcc chooses callee-saved register assignments based on the first use of each local. Declare locals that must survive across the most call boundaries first. If a return value from call N is used in call N+2, bind it to a named local declared at the top, not inline.

## Try this

1. No prep file exists — extract or locate the full disassembly for `021aa5b0` from the EUR ov022 delink. List all six callees and their argument sources to understand which intermediates must survive across call boundaries.
2. Once callees are known: declare any result-holding locals in order of first use. Declare long-lived values (used in calls 4-6) before short-lived ones (used only in call 1-2). This is the decl-order lever from ov021 wave 2.
3. If live-reg assignment still mismatches after decl-order adjustment: try splitting the six calls into named sub-expressions (`int r = func_A(); func_B(r, func_C());`) vs flat sequence and observe which form shifts the register binding in objdiff. At 244B and class C this should not require permuter.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

