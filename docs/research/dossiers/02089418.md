# 02089418 — func_02089418 (main, class C, 200B)

## Recipe + Risk
**Recipe:** scalar fixups in source order off18/off20/off10; loop RELOADS p->p84 each iter; cond add (addne)
**Risk:** loop reg-alloc: orig reloads ldr [r5,#0x84] every iteration and walks r2 with r3=i*0x10 accumulator. mwcc may hoist p->p84 or strength-reduce differently. permuter-class for loop body; header fixups reshape-able. struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02089418 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     scalar fixups in source order off18/off20/off10; loop RELOADS p->p84 each iter; cond add (addne)
 *   risk:       loop reg-alloc: orig reloads ldr [r5,#0x84] every iteration and walks r2 with r3=i*0x10 accumulator. mwcc may hoist p->p84 or strength-reduce differently. permuter-class for loop body; header fixups reshape-able. struct-guessed.
 *   confidence: low
 */
// func_02089418 — relocate offset fields to pointers (header + per-element loop)
// data_021a5800: written at end to point at the relocated block.
struct Elem { int pad0; int pad4; int pad8; int reloc; };       // reloc field at +0x8?  NB: see risk
struct Sub  { int pad0; int pad4; int count; };                 // count at +0x8, elems at +0xc
struct Blk {
    char pad0[0x10];
    int  off10;   // +0x10
    char pad14[0x04];
    int  off18;   // +0x18
    char pad1c[0x04];
    int  off20;   // +0x20
    char pad24[0x10];
    int  p30;     // +0x30
    char pad34[0x50];
    int  p84;     // +0x84
    int  p88;     // +0x88
    int  p8c;     // +0x8c
};
extern void Copy32(void *dst, const void *src, unsigned int n);
extern void *data_021a5800;

void func_02089418(struct Blk *p, char *base)
{
    int i;
    Copy32(base, p, 0x30);
    p->p8c = p->off18 ? (int)(base + p->off18) : 0;
    p->p84 = p->off20 ? (int)(base + p->off20) : 0;
    p->p88 = p->off10 ? (int)(base + p->off10) : 0;
    {
        struct Sub *s = (struct Sub *)p->p84;
        if ((unsigned int)s->count > 0) {
            for (i = 0; (unsigned int)i < (unsigned int)((struct Sub *)p->p84)->count; i++) {
                int *fld = (int *)((char *)p->p84 + 0xc + i * 0x10 + 0x8);
                *fld = *fld ? (int)(base + *fld) : 0;
            }
        }
    }
    p->p30 = 0;
    data_021a5800 = p;
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| `Blk+0x10` | `off10` | `int` | raw file offset; relocated to `p->p88` (base+off10) if nonzero |
| `Blk+0x18` | `off18` | `int` | raw file offset; relocated to `p->p8c` (base+off18) if nonzero |
| `Blk+0x20` | `off20` | `int` | raw file offset; relocated to `p->p84` (base+off20) if nonzero |
| `Blk+0x30` | `p30` | `int` | set to 0 after relocation; init flag |
| `Blk+0x84` | `p84` | `int` | relocated pointer to Sub array; reloaded each loop iteration |
| `Blk+0x88` | `p88` | `int` | relocated pointer |
| `Blk+0x8c` | `p8c` | `int` | relocated pointer |
| `Sub+0x08` | `count` | `int` | element count for loop bound |

## Closest matched example
**Path:** src/main/func_02089400.c
**Key lesson:** `data_021a5800` is the global module context pointer in this block; the matched `func_02089400` shows it is written with a raw value and the previous value returned. Here it is written as `data_021a5800 = p` at the tail. The fixup scalar order `p8c = off18, p84 = off20, p88 = off10` must match the asm sequence exactly — any reordering of the three `str` instructions will diverge.

## Try this
1. The three scalar fixup assignments must appear in exactly this source order: `p->p8c = ...off18...`, then `p->p84 = ...off20...`, then `p->p88 = ...off10...` — this is NOT ascending offset order and reordering to numeric order will produce a divergent `str` sequence.
2. In the loop body, force a reload of `p->p84` every iteration by writing `(struct Sub *)p->p84` in the loop condition expression (not caching it as a local `s` across iterations) — orig emits `ldr [r5,#0x84]` inside the loop body, not a hoisted value.
3. Park as permuter-class for the loop body if the `i*0x10 accumulator` stride pattern (orig uses `r3=i<<4` as a running accumulator, not `add r3,r3,#0x10`) diverges after reshaping the loop condition reload — the strength-reduction choice is not recoverable via C alone.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021a5800` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Copy32` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

