# 0201b504 — func_0201b504 (main, class D, 204B)

## Recipe + Risk

**Recipe:** func fills array+count; loop (idx-1)*0x18 mla; two bitfields at elem+8 store-order; byte-accum sys+0x8f8
**Risk:** permuter-class + struct-guessed: the elem layout (pad[8]+f0:8+f9:8 at bit 9, stride 0x18) and the double-store-with-intermediate-mask (and r2,0xfffe01ff) are inferred; reg-alloc over r4-r9 loop is likely to need permuter.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0201b504 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     func fills array+count; loop (idx-1)*0x18 mla; two bitfields at elem+8 store-order; byte-accum sys+0x8f8
 *   risk:       permuter-class + struct-guessed: the elem layout (pad[8]+f0:8+f9:8 at bit 9, stride 0x18) and the double-store-with-intermediate-mask (and r2,0xfffe01ff) are inferred; reg-alloc over r4-r9 loop is likely to need permuter.
 *   confidence: low
 */
/* CAMPAIGN-PREP candidate for func_0201b504 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. func_0201afdc fills a stack array + count; loop maps each
 * (u16) entry via func_02019210 to a 1-based index into a 24-byte-stride struct array at sys,
 * packs two bitfields at elem+0x8 (byte0 = arg&0xff, then :8 field at bit 9 = entry low byte),
 * then accumulates count into the low byte of sys+0x8f8 (& 0xff, no clamp).
 */
extern char *GetSystemWork(void);
extern int func_02019210(int);
extern void func_0201afdc(int, int, unsigned short *, int *);

struct elem {
    char pad[8];
    unsigned f0 : 8;    /* bits 0-7  = arg & 0xff   */
    unsigned f9 : 8;    /* bits 9-16 = entry low byte */
};

struct sysf8 { unsigned lo8 : 8; unsigned rest : 24; };

void func_0201b504(int arg) {
    char *sys = GetSystemWork();
    unsigned short buf[16];
    int count = 0;
    int i;
    func_0201afdc(arg, 1, buf, &count);
    for (i = 0; i < count; i++) {
        int idx = func_02019210((unsigned short)buf[i]);
        struct elem *e = (struct elem *)(sys + (idx - 1) * 0x18);
        e->f0 = arg & 0xff;
        e->f9 = (unsigned short)buf[i];
    }
    ((struct sysf8 *)(sys + 0x8f8))->lo8 = ((struct sysf8 *)(sys + 0x8f8))->lo8 + count;
}
```

## Struct context

This function touches SystemWork via `GetSystemWork()` at the stride-0x18 sub-array (base+0) and at `+0x8f8`.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| sys + (idx-1)*0x18 + 8 | SysWorkEntry bits 0-7 | u8 bitfield (`f0`) | Written `arg & 0xff` |
| sys + (idx-1)*0x18 + 8 | SysWorkEntry bits 9-16 | u8 bitfield (`f9`) | Written low byte of `buf[i]` |
| sys + 0x8f8 | dense bitfield word | s32 | Low byte (bits 0-7) incremented by `count` |

Per `struct-context-main.md`, the stride-0x18 entry at `+0x8` holds a bitfield word; the `SysWorkEntry.f10` is at entry+0x10, but here the write target is entry+0x8, so these are distinct fields in the first half of the entry. The `+0x8f8` field is confirmed as a dense bitfield word with bit-11, bit-15, bit-16, bits-19..21 sub-fields; the low byte increment here is a separate confirmed use.

## Closest matched example

**Path:** src/main/func_0201b5d0.c
**Why similar:** Sibling caller — `func_0201b5d0` calls `func_0201b504(i+1)` and `func_0201b504(i+0x11)` in two loops. The loop structure with `GetSystemWork()` binding and an `(idx-1)*0x18` stride is the same family.
**Key lesson:** The double-bitfield write at `elem+0x8` (two separate 8-bit fields packed into the same word, one at bits 0-7 and one at bits 9-16) emits as two separate RMW sequences: first `bic/orr` to insert `arg&0xff` at bits 0-7 with mask `0xfffe01ff` (clears bits 1-8 and the f9 field), then a second `bic/orr` to insert the entry's low byte at bits 9-16. The intermediate mask `0xfffe01ff` is inferred — verify it matches the bitfield struct layout. If it does not, adjust the struct pad/field sizes.

## Try this

1. Park as permuter-class; the double-bitfield RMW (`and r2,0xfffe01ff`) intermediate mask is inferred and unverified, and the r4-r9 loop register allocation cannot be controlled by C-shape — expect heavy iteration or `.s`. If attempting C-match first, ensure the two bitfield writes are in strict source-order (f0 first, f9 second) so the two `bic/orr` pairs emit in that order.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0xfffe01ff` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201afdc` | main TU call |
| `bl` | `func_02019210` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

