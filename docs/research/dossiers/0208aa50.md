# 0208aa50 — func_0208aa50 (main, class D, 72B)

## Recipe + Risk
**Recipe:** D: n>>1 count; (a+b+1)>>1 signed asr average; ldrsh via short*; index i*2
**Risk:** reshape-able: the `lr = i<<1` byte-offset addressing and post-increment-then-compare loop shape are mwcc-natural for short[]. Risk: loop reads dst[i] (RMW) — if mwcc keeps base in reg vs reindexing, ldrsh offset form may shift.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208aa50 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: n>>1 count; (a+b+1)>>1 signed asr average; ldrsh via short* ; index i*2
 *   risk:       reshape-able: the `lr = i<<1` byte-offset addressing and post-increment-then-compare loop shape are mwcc-natural for short[]. Risk: loop reads dst[i] (RMW) — if mwcc keeps base in reg vs reindexing, ldrsh offset form may shift.
 *   confidence: med
 */
/* func_0208aa50 (main, class D): halfword rounding-average of two
 * s16 arrays into the first, count = n>>1, then a fixed tail call.
 *   movs r5,r2,lsr#1 => half-count, beq skip.
 *   lr = i*2 (halfword index); a[i] = (a[i]+b[i]+1) >> 1 (asr#1).
 *   loop var i declared first (r4), then result count (r5). */
extern void func_02094550(void);

void func_0208aa50(short *dst, short *src, int n) {
    int i;
    int count = (unsigned int)n >> 1;

    for (i = 0; i < count; i++) {
        dst[i] = (short)((dst[i] + src[i] + 1) >> 1);
    }
    func_02094550();
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| `dst[i]` | halfword | `short` | read-modify-write; `ldrsh` then computed average stored back via `strh` |
| `src[i]` | halfword | `short` | source array; `ldrsh` paired with dst read |
| `n >> 1` | count | `unsigned int` | `lsr#1` of n; loop bound; `movs r5,r2,lsr#1; beq skip` skips loop if count == 0 |
| `i` | loop index | `int` | declared first (r4); byte offset `i*2` used via `lsl#1` addressing |

## Closest matched example
**Path:** src/main/func_0207d430.c
**Key lesson:** The do-while post-test loop pattern in `func_0207d430` demonstrates that mwcc prefers a bottom-tested loop (beq skip at entry, bne at bottom) over a top-tested while, which is exactly the `movs r5,r2,lsr#1; beq .done` pattern here. The rounding-average `(a+b+1)>>1` must use `>> 1` as arithmetic right-shift on `int` (not `unsigned`) to emit `asr#1`; casting `dst[i]` and `src[i]` to `int` for the sum before the shift ensures signed arithmetic.

## Try this
1. Declare `i` before `count` in the function body — orig assigns `i` to r4 and `count` to r5 (`movs r5,r2,lsr#1`), and mwcc's register assignment follows declaration order; reversing the order would flip r4/r5 and produce wrong code.
2. Write the average as `(dst[i] + src[i] + 1) >> 1` with `>> 1` on a signed `int` sum — do NOT use `unsigned` or `>> 1` on a short expression, as the `asr#1` instruction requires signed arithmetic right shift from a 32-bit int.
3. If objdiff shows `ldrsh [r0,r4,lsl#1]` vs `ldrsh [r0,#offset]` (base+offset form vs indexed form), try replacing `dst[i]` with `*(dst + i)` or using a pointer-walk `short *p = dst` incremented each iteration — the exact `ldrsh` addressing mode depends on whether mwcc chooses base+index or base+accumulator form.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02094550` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

