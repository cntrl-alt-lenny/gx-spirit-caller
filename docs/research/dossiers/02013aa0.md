# 02013aa0 — func_02013aa0 (main, class D, 172B)

## Recipe + Risk

**Recipe:** compare-chain switch (few cases); bit0 test via &1; fallthrough case1->case3; OR-into-*ptr
**Risk:** struct-guessed: SystemWork offsets 0x8d8/0x920/0x924 and dst+8 are inferred (no typedef). Also bit0 read: orig uses lsl#31;lsr#31 (a :1 bitfield); plain &1 may emit `and` not lsl/lsr. reshape-able (use unsigned :1 bitfield).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02013aa0 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     compare-chain switch (few cases); bit0 test via &1; fallthrough case1->case3; OR-into-*ptr
 *   risk:       struct-guessed: SystemWork offsets 0x8d8/0x920/0x924 and dst+8 are inferred (no typedef). Also bit0 read: orig uses lsl#31;lsr#31 (a :1 bitfield); plain &1 may emit `and` not lsl/lsr. reshape-able (use unsigned :1 bitfield).
 *   confidence: low
 */
/* func_02013aa0(dst): if SystemWork enable-bit(0x8d8 bit0) clear, return.
 * Then dispatch on SystemWork[0x920]:
 *   1 -> nested dispatch on [0x924] (case1 promotes [0x924]=3 then shares
 *        the case3 body), writing packed status words to dst->f8 (off 8);
 *        the [0x924]==2 arm also OR's 0x2000 into *func_0201b75c().
 *   2 -> dst->f8 = 0x00010200
 *   3 -> dst->f8 = 0x00010300
 * SystemWork accessed via the codebase's char*+offset idiom. */
extern char *GetSystemWork(void);
extern int  *func_0201b75c(void);

void func_02013aa0(int *dst)
{
    char *sw = GetSystemWork();
    if ((*(int *)(sw + 0x8d8) & 1) == 0)
        return;
    switch (*(int *)(sw + 0x920)) {
    case 1:
        switch (*(int *)(sw + 0x924)) {
        case 1:
            *(int *)(sw + 0x924) = 3;
            /* fallthrough */
        case 3:
            dst[2] = 0x00010100;
            break;
        case 2:
            dst[2] = 0x00010101;
            {
                int *p = func_0201b75c();
                *p |= 0x2000;
            }
            break;
        }
        break;
    case 2:
        dst[2] = 0x00010200;
        break;
    case 3:
        dst[2] = 0x00010300;
        break;
    }
}
```

## Struct context

This function reads SystemWork (returned by `GetSystemWork()`) at three confirmed offsets. The `dst+8` write (`dst[2]`) is an output parameter to the caller, not a SystemWork field. `func_0201b75c()` returns a pointer to SystemWork+0x8f4 (the confirmed flag-word).

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| SystemWork+0x8d8 | enable gate | s32 / bit 0 | `:1` bitfield — master enable; early return if bit0==0 |
| SystemWork+0x920 | dispatch selector | s32 | outer switch: values 1/2/3 (compare-chain, NOT jump-table) |
| SystemWork+0x924 | sub-state selector | s32 | inner switch when 0x920==1: values 1/2/3; case1 self-promotes to 3 |
| SystemWork+0x8f4 (via func_0201b75c) | flag word | s32* | OR'd with 0x2000 in the 0x924==2 arm |
| dst[2] (=dst+8) | output param | int | packed status code written per arm |

## Closest matched example

**Path:** src/main/func_02014bf4.c
**Why similar:** Same `GetSystemWork()` guard-chain with `:1` bitfield at 0x8d8, same pattern of comparing 0x920 against a small value and writing a packed word to `o+8`.
**Key lesson:** The bit0 test at `sw+0x8d8` MUST use a `:1` unsigned bitfield struct — not `& 1` — so the compiler emits `lsl#31; lsr#31` (or `lsls#31; ldrne` in the guard pattern). Using `& 1` emits an `and r,r,#1` which does not match. The `switch` on 0x920 with values 1/2/3 is a compare-chain (cmp/beq sequence), NOT a jump-table — keeping it as a C `switch` may cause mwcc to emit `addls pc` if it decides the range is dense; use explicit `if/else if` for the 0x920 dispatch if the jump-table appears.

## Try this

1. Replace `(*(int *)(sw + 0x8d8) & 1) == 0` with the `:1` bitfield idiom: add `struct Flag { unsigned int b0 : 1; };` and test `((struct Flag *)(sw + 0x8d8))->b0 == 0`. This is the single most likely first-match unlocker.
2. If the outer `switch(*(int *)(sw + 0x920))` emits `addls pc` (jump-table) instead of `cmp; beq` chain: replace with `if (*(int*)(sw+0x920) == 1) { ... } else if (... == 2) { ... } else if (... == 3) { ... }` to force the compare-chain form.
3. If the nested 0x924 switch also emits a jump-table unexpectedly: apply the same if/else if conversion; if the fallthrough from case1 to case3 does not survive the conversion cleanly, use a `goto` label to model the promotion `*(sw+0x924)=3; goto case3_body`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00010300` | MMIO/const |
| `_LIT1` | `0x00010100` | MMIO/const |
| `_LIT2` | `0x00010101` | MMIO/const |
| `_LIT3` | `0x00010200` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201b75c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

