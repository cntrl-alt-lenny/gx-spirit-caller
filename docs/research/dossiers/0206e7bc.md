# 0206e7bc — func_0206e7bc (main, class D, 388B)

## Recipe + Risk

**Recipe:** 5x inlined bswap32; out-of-order tail stores (0x18,0x1c,0x14) + scalar globals; exact store-order
**Risk:** Orig hoists the LIT pool (func ptrs at d+0x18/0x1c, globals ed58/ed54) and stores 0x18/0x1c BEFORE 0x14, interleaving with the 5th bswap held in r1 — a fixed schedule a bswap macro is unlikely to reproduce verbatim. permuter-class (register/schedule pin); store-order partly reshape-able.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_0206e7bc (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     5x inlined bswap32; out-of-order tail stores (0x18,0x1c,0x14) + scalar globals; exact store-order
 *   risk:       Orig hoists the LIT pool (func ptrs at d+0x18/0x1c, globals ed58/ed54) and stores 0x18/0x1c BEFORE 0x14, interleaving with the 5th bswap held in r1 — a fixed schedule a bswap macro is unlikely to reproduce verbatim. permuter-class (register/schedule pin); store-order partly reshape-able.
 *   confidence: low
 */
/* func_0206e7bc (main, class D) — repeated bswap32 stores into a global struct. */
typedef unsigned long u32;

extern int  func_0206bd30(void *cfg);
extern void func_0206e940(void);
extern void func_0206e980(void);
extern u32  data_0219ed54;
extern u32  data_0219ed58;
extern char data_0219ed84[];   /* struct base (ip) */

static u32 bswap32(u32 v)
{
    return ((v >> 24) & 0xff) | ((v >> 8) & 0xff00) |
           ((v << 8) & 0xff0000) | (v << 24);
}

int func_0206e7bc(void *src)
{
    char *s  = (char *)src;
    char *d  = data_0219ed84;

    *(u32 *)(d + 0x0)  = (*(u32 *)(s + 0xc) == 1) ? 1 : 0;
    *(u32 *)(d + 0x4)  = bswap32(*(u32 *)(s + 0x10));
    *(u32 *)(d + 0x8)  = bswap32(*(u32 *)(s + 0x14));
    *(u32 *)(d + 0xc)  = bswap32(*(u32 *)(s + 0x18));
    *(u32 *)(d + 0x10) = bswap32(*(u32 *)(s + 0x1c));
    *(u32 *)(d + 0x18) = (u32)&func_0206e980;
    *(u32 *)(d + 0x1c) = (u32)&func_0206e940;
    *(u32 *)(d + 0x14) = bswap32(*(u32 *)(s + 0x20));
    data_0219ed58      = *(u32 *)(s + 0x4);
    *(u32 *)(d + 0x20) = 0x40;
    data_0219ed54      = *(u32 *)(s + 0x8);
    *(u32 *)(d + 0x30) = *(u32 *)(s + 0x2c);
    *(u32 *)(d + 0x34) = *(u32 *)(s + 0x30);
    return func_0206bd30(d);
}
```

## Struct context

Accesses `data_0219ed84` (global char array, treated as a struct base pointer) and scalar globals `data_0219ed54`/`data_0219ed58`. No SystemWork.

| global | type | use |
|--------|------|-----|
| `data_0219ed84` | `char[]` (struct base) | destination for all stores; `d = data_0219ed84` |
| `data_0219ed58` | `u32` | written from `*(u32 *)(s + 0x4)` |
| `data_0219ed54` | `u32` | written from `*(u32 *)(s + 0x8)` |
| `func_0206e980` | function pointer | stored at `d+0x18` |
| `func_0206e940` | function pointer | stored at `d+0x1c` |

## Closest matched example

**Path:** src/main/func_0206bad4.c
**Why similar:** Global scalar write using literal pool address, same module cluster (`0219ece4`/`0219ed54`/`0219ed58` family).
**Key lesson:** The out-of-order store sequence in the source (`d+0x18` and `d+0x1c` BEFORE `d+0x14`, then globals, then `d+0x20`, then `d+0x34`) is intentional and reflects the LIT-pool hoisting the compiler does. Reproducing this verbatim requires writing the stores in EXACTLY the order shown in the prep draft — any reshuffling (e.g., storing `d+0x14` with the other bswap32 stores) will produce a schedule mismatch in objdiff.

## Try this

1. Build as-is and check if the 5th bswap (for `s+0x20` → `d+0x14`) appears with `d+0x18` and `d+0x1c` already stored — the schedule in the prep draft places `d+0x18` and `d+0x1c` stores BEFORE the 5th bswap's destination store. If they appear after, swap the store order for `d+0x18` and `d+0x1c` to before `d+0x14` in the C source.
2. If the `data_0219ed58`/`data_0219ed54` global stores appear in the wrong position relative to `d+0x20` and `d+0x30/0x34`, try inserting explicit load-store ordering: `u32 tmp1 = *(u32*)(s+0x4); *(u32*)(d+0x20) = 0x40; data_0219ed58 = tmp1;` — binding the load early may pin the store position.
3. When to park: risk explicitly says permuter-class (register/schedule pin) and confidence is LOW. After one attempt to fix store order, if the 5-bswap interleave still does not match, accept as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219ed84` | data symbol |
| `_LIT1` | `func_0206e980` | func ptr |
| `_LIT2` | `func_0206e940` | func ptr |
| `_LIT3` | `data_0219ed58` | data symbol |
| `_LIT4` | `data_0219ed54` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206bd30` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

