# 020659ac — func_020659ac (main, class D, 288B)

## Recipe + Risk
**Recipe:** ordered sig+tag stores; htonl/htons term-order; status bool; post-call field stamps
**Risk:** `ldr r0,[r4,#4]; cmp r0,r1(=-1); ldreq r0,[r4]` is a conditional reload I modelled as if-assign — mwcc may instead keep r0 and not reload; reshape-able. Plus htonl grouping permuter-class; struct-guessed.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_020659ac (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     ordered sig+tag stores; htonl/htons term-order; status bool; post-call field stamps
 *   risk:       `ldr r0,[r4,#4]; cmp r0,r1(=-1); ldreq r0,[r4]` is a conditional reload I modelled as if-assign — mwcc may instead keep r0 and not reload; reshape-able. Plus htonl grouping permuter-class; struct-guessed.
 *   confidence: low
 */
/* func_020659ac: build a larger packet header (6-byte sig + tag bytes 2,7),
 * byteswap self+0x8 (htonl), copy self+0x2c, htons self+0x30, byte self+0x32,
 * a status byte = (self+0x10 != 2), a sentinel-or-fallback word, emit via
 * func_02065dc0, then stamp self+0x28/0x24 and a conditional self+0x33. */

extern unsigned char data_02101598[];
extern int func_02055330(void);
extern int func_02065dc0(int a, int b, int c, int d);

typedef struct {
    int  word0;     /* +0x00 */
    int  word4;     /* +0x04 */
    unsigned int word8; /* +0x08 */
    char _padc[0x10 - 0xc];
    int  word10;    /* +0x10 */
    char _pad14[0x24 - 0x14];
    int  word24;    /* +0x24 */
    int  word28;    /* +0x28 */
    int  word2c;    /* +0x2c */
    short half30;   /* +0x30 */
    unsigned char byte32; /* +0x32 */
    unsigned char byte33; /* +0x33 */
} Self;

int func_020659ac(Self *self)
{
    unsigned char hdr[0x18];
    unsigned int w;
    int hh;
    int body0;

    hdr[0] = data_02101598[0];
    hdr[1] = data_02101598[1];
    hdr[2] = data_02101598[2];
    hdr[3] = data_02101598[3];
    hdr[4] = data_02101598[4];
    hdr[5] = data_02101598[5];
    hdr[6] = 2;
    hdr[7] = 7;

    w = self->word8;
    *(unsigned int *)(hdr + 8) = ((w >> 24) & 0xff) | ((w >> 8) & 0xff00) |
                                 ((w << 8) & 0xff0000) | ((w << 24) & 0xff000000);
    *(int *)(hdr + 0xc) = self->word2c;

    hh = self->half30;
    *(short *)(hdr + 0x10) = (short)(((hh >> 8) & 0xff) | ((hh << 8) & 0xff00));
    hdr[0x12] = self->byte32;
    hdr[0x13] = (self->word10 != 2);

    body0 = self->word0;
    if (self->word4 == -1) body0 = self->word0;   /* ldreq r0,[r4] */

    func_02065dc0(body0, self->word2c, self->half30, 0x14);

    self->word28 = func_02055330() + 0x2bc;
    self->word24 = 0xc;
    if (self->byte32 != 0) self->byte33 = 1;
}
```

Near-clone of 02065470: both use `data_02101598[]` for the 6-byte signature, both call `func_02065dc0`, both apply htonl to a self field, both stamp post-call fields.

## Struct context
No SystemWork/GameSingleton. Uses `data_02101598[]` (shared 6-byte signature literal, same as func_02065470). The conditional reload `if (self->word4 == -1) body0 = self->word0` models the `ldreq` — the branch condition is `word4 == -1` (mvn#0) and the reload is of `self->word0` (same word, same register). `func_02055330()` provides a timestamp used for the post-call stamp at word28.

| offset | field | type | use |
|--------|-------|------|-----|
| +0x00 | word0 | int | body0 base value; conditionally reloaded if word4 == -1 |
| +0x04 | word4 | int | sentinel guard: == -1 triggers ldreq reload |
| +0x08 | word8 | unsigned int | htonl source → hdr[8..11] |
| +0x10 | word10 | int | status bool: (word10 != 2) → hdr[0x13] |
| +0x24 | word24 | int | post-call stamp: set to 0xc |
| +0x28 | word28 | int | post-call stamp: func_02055330() + 0x2bc |
| +0x2c | word2c | int | copied to hdr[0xc]; also passed to func_02065dc0 |
| +0x30 | half30 | short | htons source → hdr[0x10]; also passed to func_02065dc0 |
| +0x32 | byte32 | unsigned char | copied to hdr[0x12]; guards byte33 conditional |
| +0x33 | byte33 | unsigned char | conditionally set to 1 if byte32 != 0 |

## Closest matched example
**Path:** src/main/func_02065f78.c
**Why similar:** conditional field reload (`word4 == -1` → reload word0) followed by a call and post-call field stamps.
**Key lesson:** The `if (self->word4 == -1) body0 = self->word0;` line models the `ldreq` — but note that `body0` is already set to `self->word0` before the if, so the `ldreq` is a no-op reload (both branches produce the same value). This is a codegen artifact of how mwcc handles the cmpeq/ldreq pattern; the C form must keep the redundant-looking reload explicit to reproduce it. The htonl orr-grouping is permuter-class and cannot be fixed by reshape.

## Try this
1. Build as-is; in objdiff verify the conditional reload: `ldr r0,[r4,#0]; cmp rX,#-1; ldreq r0,[r4,#0]` (both loads from self+0x0). If mwcc eliminates the redundant reload, try: `body0 = (self->word4 == -1) ? self->word0 : self->word0;` to force the conditional load.
2. If the hdr byte stores diverge (load-all then store-all instead of interleaved), rewrite the six signature stores with individual temp reads: `unsigned char s0=data_02101598[0]; hdr[0]=s0;` etc.
3. When to park: the htonl orr-grouping is permuter-class — if byteswap instruction order diverges after all structural fixes, accept as .s.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02101598` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02065dc0` | main TU call |
| `bl` | `func_02055330` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

