# 02006950 — func_02006950 (main, class D, 164B)

## Recipe + Risk

**Recipe:** class D: align math (bic #3 both, sub), Fill32 marshal, store -1 pair order
**Risk:** reshape-able-ish: arg signatures (esp. func_020983f4 second call passes only r1=r4, r0 stale) and store order of the two -1 writes (asm: negc then neg8) are inferred; the final func_020983f4 r0 reuse is struct-guessed and likely diverges.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02006950 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     class D: align math (bic #3 both, sub), Fill32 marshal, store -1 pair order
 *   risk:       reshape-able-ish: arg signatures (esp. func_020983f4 second call passes only r1=r4, r0 stale) and store order of the two -1 writes (asm: negc then neg8) are inferred; the final func_020983f4 r0 reuse is struct-guessed and likely diverges.
 *   confidence: low
 */
/* func_02006950: marshal-heavy init. Fill32(dst,0,0x20); set two -1 words
 * at +0x8/+0xc of global; a sequence of calls; align math
 *   r4 = alloc_top; r0b = alloc_cur;
 *   size = (alloc_top+3 & ~3) - (alloc_cur & ~3);  // bic r0,#3 on both, sub
 * Then more calls; store result at global[+0x1c]; two Task posts.
 * Call ORDER is load-bearing; r4 reused as size then as task handle. */
typedef struct {
    char _pad_00[0x8];
    int neg8;     /* +0x8  = -1 */
    int negc;     /* +0xc  = -1 */
    char _pad_10[0xc];
    int p1c;      /* +0x1c stores func_0207d4dc result */
} Glob_02104f1c;

extern Glob_02104f1c data_02104f1c;
extern void Fill32(unsigned int value, void *dst, unsigned int size);
extern void Task_PostLocked(int a0, int a1, int a2);
extern int  func_0207d4dc(int a0, int a1, int a2);
extern void func_02090624(int a0);
extern int  func_020929ac(int a0, int a1, int a2);
extern int  func_02092c78(int a0);
extern int  func_02092c8c(int a0);
extern int  func_020983f4(int a0, int a1);

void func_02006950(void) {
    int top, cur, size, handle;
    Fill32(0, &data_02104f1c, 0x20);
    data_02104f1c.negc = -1;
    data_02104f1c.neg8 = -1;
    top = func_02092c78(0);
    cur = func_02092c8c(0);
    size = ((top + 3) & ~3) - (cur & ~3);
    func_020929ac(0, size, 4);
    data_02104f1c.p1c = func_0207d4dc(0, size, 0);
    func_02090624(0x40000);
    handle = func_020983f4(0, 0);
    Task_PostLocked(handle, 4, 4);
    func_020983f4(handle, 0/*r1 set from blx? actually mov r1,r4 only*/);
}
```

## Struct context

This function initialises `data_02104f1c` (GlobalData02104f1c, frame/timer). According to `GlobalData02104f1c.md`, offsets +0x00, +0x04, +0x10, +0x14, +0x18, +0x1C are confirmed fields.

| offset | field | type | use |
|--------|-------|------|-----|
| +0x00..+0x1f | (entire struct) | zeroed | Fill32(0, &data_02104f1c, 0x20) wipe |
| +0x08 | neg8 | int | written -1 AFTER negc (asm: negc then neg8 store order) |
| +0x0c | negc | int | written -1 FIRST |
| +0x1c | p1c | int | receives func_0207d4dc(0, size, 0) result |

**Store order is critical:** the asm writes `+0xc` (negc) first, then `+0x8` (neg8). The prep draft declares `negc` before `neg8` in the struct so field-declaration order may influence scheduling. If objdiff shows the pair swapped, swap the two assignment statements in source so `data_02104f1c.negc = -1` comes before `data_02104f1c.neg8 = -1` — or adjust struct field declaration order to match.

## Closest matched example

**Path:** src/main/func_02006918.c
**Why similar:** Also uses `data_02104f1c` at +0x1c and performs a task-queue init sequence with callee-saved regs holding a handle.
**Key lesson:** `data_02104f1c.f_1c` is the task handle field (confirmed at +0x1c); the struct typedef must expose that offset exactly. MWCC places the callee-saved register (r4) holding the accumulated `size`/`handle` value without reloading — the C must bind the intermediate to a named local (`handle`) so r4 is kept live across all calls.

## Try this

1. Build as-is and check objdiff for the two `-1` store pair. If `str r0,[base,#0xc]` comes AFTER `str r0,[base,#0x8]`, swap the two assignment lines so `data_02104f1c.negc = -1` precedes `data_02104f1c.neg8 = -1` — MWCC emits stores in source order for adjacent writes to the same global.
2. If step 1 is correct but the last `func_020983f4` call diverges (wrong r0 or r1), the second call's arguments are inferred only — examine the objdiff register state at that bl; r0 may be the stale return value from `Task_PostLocked` (which is typically void) or from the prior `func_020983f4`; adjust the second call's first argument accordingly.
3. When to park: if both the store-order and the second-call argument are correct but any instruction in the align math (`bic #3 both, sub`) diverges, that is scheduling-class — park as `.s`.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_020069ec` | `data_02104f1c` | data symbol |
| `.L_020069f0` | `0x02104f1c` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Fill32` |  |
| `bl` | `func_02092c78` | main TU call |
| `bl` | `func_02092c8c` | main TU call |
| `bl` | `func_020929ac` | main TU call |
| `bl` | `func_0207d4dc` | main TU call |
| `bl` | `func_02090624` | main TU call |
| `bl` | `func_020983f4` | main TU call |
| `bl` | `Task_PostLocked` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

