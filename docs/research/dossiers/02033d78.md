# 02033d78 — func_02033d78 (main, class C, 112B)

## Recipe + Risk

**Recipe:** for-loop ptr-walk +=0x28; load b4 (arg) before branch on b5; reload b4 after store
**Risk:** asm loads b5(flags) then b4(arg) before branch (ldrb;cmp;ldrb;bne) — mwcc must hoist the b4 arg-load above the branch; likely matches but scheduling of the dual ldrb could rotate — permuter-class if it flips.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02033d78 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     for-loop ptr-walk +=0x28; load b4 (arg) before branch on b5; reload b4 after store
 *   risk:       asm loads b5(flags) then b4(arg) before branch (ldrb;cmp;ldrb;bne) — mwcc must hoist the b4 arg-load above the branch; likely matches but scheduling of the dual ldrb could rotate — permuter-class if it flips.
 *   confidence: med
 */
/* func_02033d78 (main, class C) — counted struct-stride(0x28) loop, two-branch dispatch.
 * r0=base(r6), r1=count(r5), r4=i. per entry: if(!f0)skip; arg=b4; branch on b5; result->b4; if(!b4)return. */
typedef struct {
    int           f0;           /* +0x00 active */
    unsigned char b4;           /* +0x04 state (call arg & result) */
    unsigned char b5;           /* +0x05 mode (branch selector) */
    unsigned char buf6[0x18 - 6];
    unsigned char buf18[0x28 - 0x18];
} Ent_02033d78;

extern int func_02051b10(unsigned char st, unsigned char *b6, unsigned char *b18);
extern int func_02051c4c(unsigned char st, unsigned char *b6, unsigned char *b18);

void func_02033d78(Ent_02033d78 *base, int count)
{
    int i;
    for (i = 0; i < count; i++) {
        if (base->f0 != 0) {
            if (base->b5 != 0)
                base->b4 = (unsigned char)func_02051b10(base->b4, base->buf6, base->buf18);
            else
                base->b4 = (unsigned char)func_02051c4c(base->b4, base->buf6, base->buf18);
            if (base->b4 == 0)
                return;
        }
        base = (Ent_02033d78 *)((char *)base + 0x28);
    }
}
```

## Struct context

`Ent_02033d78` is a local per-entry struct; no GameSingleton or SystemWork fields are touched.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| +0x00 | f0 | int | active flag; zero means skip this entry |
| +0x04 | b4 | unsigned char | call argument and result storage; tested for zero after call |
| +0x05 | b5 | unsigned char | mode selector: non-zero -> func_02051b10, zero -> func_02051c4c |
| +0x06..+0x17 | buf6 | unsigned char[18] | second arg to both callee functions |
| +0x18..+0x27 | buf18 | unsigned char[16] | third arg to both callee functions |

## Closest matched example

**Path:** src/main/func_0201b7b4.c
**Why similar:** Both functions have a dual-ldrb load scheduling concern where the order of two byte loads before a branch must be preserved.
**Key lesson:** The orig loads `b5` (for the branch) and `b4` (call arg) in the order `ldrb b5; cmp; ldrb b4; bne`. mwcc tends to hoist `b4` early when it is needed as a call argument in both branches — this is likely correct. If the load order rotates (`ldrb b4` before `ldrb b5`), the diff will show different register traffic. The `base->b5 != 0` branch selector must be evaluated before loading `base->b4` in the source; the current code already does this because `b4` is used as a call argument (so mwcc may or may not hoist it). Watch the objdiff for the `ldrb` pair order at the top of the `if(base->f0)` block.

## Try this

1. Drop as written and check objdiff for the dual-ldrb order: `ldrb [base,#5]; cmp; ldrb [base,#4]`. If both loads appear correctly, the function likely matches.
2. If `b4` is loaded after the `b5`-based branch (inside each `if` branch separately), try binding `unsigned char st = base->b4;` before the `if (base->b5 != 0)` check to hoist the load unconditionally.
3. If the load-order scheduling still rotates after reshaping, escalate to permuter-class; dual-ldrb scheduling is un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02051c4c` | main TU call |
| `bl` | `func_02051b10` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

