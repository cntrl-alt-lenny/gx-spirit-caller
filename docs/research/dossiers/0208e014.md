# 0208e014 — func_0208e014 (main, class D, 140B)

## Recipe + Risk
**Recipe:** D: addls-pc switch on DISPCNT&7; blk=(DISPCNT>>27)<<16; sb base; condmov bit7
**Risk:** DISPCNT is read TWICE in asm (mov r2,#0x4000000; ldr; ldr) not bound to one reg - mwcc may CSE both reads into one ldr, dropping the second. reshape-able by reading via separate volatile expressions (volatile prevents CSE - should hold). condmov tail also coin-flip.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208e014 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     D: addls-pc switch on DISPCNT&7; blk=(DISPCNT>>27)<<16; sb base; condmov bit7
 *   risk:       DISPCNT is read TWICE in asm (mov r2,#0x4000000; ldr; ldr) not bound to one reg - mwcc may CSE both reads into one ldr, dropping the second. reshape-able by reading via separate volatile expressions (volatile prevents CSE - should hold). condmov tail also coin-flip.
 *   confidence: med
 */
/* CAMPAIGN-PREP candidate for func_0208e014 (main, class D, MED tier).
 * UNVERIFIED + ITERATION-EXPECTED. recipe: D, branch-table over DISPCNT-main
 * (0x04000000) BG-mode bits, with a VRAM-block base from DISPCNT bits 27..29
 * (display/VRAM block) shifted into bit16. BG0CNT-main = 0x0400000e.
 *   mode  = *0x04000000 & 7;
 *   blk   = ((*0x04000000 & 0x38000000) >> 0x1b) << 0x10;
 *   sb    = (*0x0400000e & 0x1f00) >> 8;
 *   modes 1..3 : blk + 0x6000000 + (sb<<11)
 *   modes 4..6 : if BG0CNT&0x80 (sb<<14)+0x6000000 else blk+0x6000000+(sb<<11)
 *   mode 0 / else : 0
 */
#include <nitro/types.h>

u32 func_0208e014(void)
{
    u16 bgcnt = *(vu16 *)0x0400000e;
    int mode = *(vu32 *)0x04000000 & 7;
    int blk = ((*(vu32 *)0x04000000 & 0x38000000) >> 0x1b) << 0x10;
    int sb = (bgcnt & 0x1f00) >> 8;

    switch (mode) {
    case 1:
    case 2:
    case 3:
        return blk + 0x6000000 + (sb << 11);
    case 4:
    case 5:
    case 6:
        if (bgcnt & 0x80)
            return (sb << 14) + 0x6000000;
        return blk + 0x6000000 + (sb << 11);
    default:
        return 0;
    }
}
```

## Struct context
No struct globals; all access is direct MMIO via fixed addresses.

| Address | Register | Access |
|---------|----------|--------|
| `0x04000000` | DISPCNT-main | read ×2 for mode bits (0-2) and block bits (27-29) |
| `0x0400000e` | BG0CNT-main | read once for screen-base field (bits 8-12) and bit7 |

## Closest matched example
**Path:** src/main/func_0208e1ac.c
**Key lesson:** Sibling VRAM-address resolver matched via single-read VRAMCNT mask pattern. The double DISPCNT read in this function (mode read, then block-field read) must use two separate `*(volatile u32 *)0x04000000` expressions — binding to one local would CSE them into a single ldr and drop the second hardware read. The prep draft already shows this as two distinct volatile dereferences in the `mode` and `blk` initialisers.

## Try this
1. Confirm two separate `*(vu32 *)0x04000000` dereferences are present: one for `& 7` (mode) and one for `& 0x38000000` (blk). If objdiff shows only one ldr for DISPCNT, the compiler CSE'd them — split into two independent `volatile u32 *` reads with no shared local.
2. Check objdiff jump-table shape: orig emits `cmp #6 / addls pc, pc, ip, lsl #2` for an 8-entry table covering cases 0-6 plus the fallthrough-to-zero. Confirm case 1-3 and 4-6 fall through to the same return path.
3. If the condmov tail (cases 4-6 `if (bgcnt & 0x80)`) compiles to a branch instead of `movcc`/`addcc`, try swapping the if-branch order so the false arm uses the common `blk+0x6000000+(sb<<11)` path — mwcc picks condmov when the conditional is the return.
