# 02086ad4 — func_02086ad4 (main, class D, 396B)

## Recipe + Risk
**Recipe:** store-order obj writes (hi then lo); strh for 0x20/0x22; blx table; reload obj[0x30]
**Risk:** struct-guessed + permuter-class: batch asm truncated mid-second-smull; the 3 strne pairs write hi-field-before-lo (0x1c before 0x18) — mwcc must preserve that order; reload-vs-bind of obj[0x30] also.

## Prep draft
```c
/* func_02086ad4: like 02086464 — 0x50 stack-struct + flag-gated obj writes (0x18/0x1c,
 * 0x20/0x22 h, 0x24/0x28) + blx dispatch + FX12 scale by obj[0x30]/[0x34]. */
typedef signed int s32;
typedef signed short s16;
typedef signed long long s64;
typedef s32 fx32;

extern void (*data_021023a0[])(int *, void *);
extern void func_02084e0c(int, int *, int);

void func_02086ad4(int *o) {
    int s[20];
    s[0] = (o[0] & 8) ? 0x00101810 : 0x00101a10; /* LIT0/LIT1 placeholders */
    s[1] = 3;
    s[18] = 2;
    s[17] = 0x1000;
    s[16] = 0;
    s[13] = 0; s[12] = 0; s[11] = 0; s[10] = 0; s[9] = 0; s[8] = 0;
    s[5] = 0; s[4] = 0;
    if (o[0] & 1) { o[7] = 0x1000; o[6] = o[7]; }            /* [0x1c]then[0x18] */
    if (o[0] & 2) { ((s16 *)o)[0x11] = 0x1000; ((s16 *)o)[0x10] = 0; } /* h@0x22 then 0x20 */
    if (o[0] & 4) { o[10] = 0; o[9] = o[10]; }               /* [0x28] then [0x24] */
    data_021023a0[o[0] & 7](&s[2], o);
    if (o[0x0c] != 0x1000) {
        s[2]  = (s32)(((s64)o[0x0c] * s[2])  >> 12);
        s[3]  = (s32)(((s64)o[0x0c] * s[3])  >> 12);
        s[14] = (s32)(((s64)o[0x0c] * s[14]) >> 12);
    }
    if (o[0x0d] != 0x1000) {
        s[6]  = (s32)(((s64)o[0x0d] * s[6])  >> 12);
        s[7]  = (s32)(((s64)o[0x0d] * s[7])  >> 12);
        s[15] = (s32)(((s64)o[0x0d] * s[15]) >> 12);
    }
    func_02084e0c(s[0], &s[1], 0x12);
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| o[0] (0x00) | flags | int | bits 0/1/2/3 gated writes; bits 0..2 blx index |
| o[6] (0x18) | lo word | int | written second in pair (o[7] first) |
| o[7] (0x1c) | hi word | int | written first to 0x1000 |
| o[9] (0x24) | lo word2 | int | written second (o[10] first) |
| o[10] (0x28) | hi word2 | int | written first to 0 |
| o[0x0c] (0x30) | scale X | int | FX12 multiplier; reloaded after blx (not bound) |
| o[0x0d] (0x34) | scale Y | int | FX12 multiplier; separate from 0x30 |
| s16 at 0x20 | lo half | s16 | written second to 0 |
| s16 at 0x22 | hi half | s16 | written first to 0x1000 |

## Closest matched example
**Path:** src/main/func_02086c60.c
**Key lesson:** Struct-array inits must appear in exact statement order to drive mwcc store sequencing — the compiler issues str instructions in source-statement order for plain int-array writes. The hi-before-lo pattern (o[7]=0x1000; o[6]=o[7]) replicates the strne-pair ordering seen in the original. For the smull>>12 blocks, `(s32)(((s64)a * b) >> 12)` maps reliably to smull+asr on ARM.

## Try this
1. Confirm the hi-before-lo write order is preserved exactly: `o[7] = 0x1000; o[6] = o[7];` must appear in that sequence — if objdiff shows the str at 0x18 before 0x1c, swap the two lines.
2. For `o[0x0c]` (offset 0x30): declare a separate local `int sx = o[0x0c];` AFTER the blx call to force a fresh ldr; if mwcc binds the pre-blx value the smull operand will diverge.
3. Park as permuter-class; the blx table dispatch and truncated smull tail mean the second-smull scheduling is un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00101610` | MMIO/const |
| `_LIT1` | `0x00101810` | MMIO/const |
| `_LIT2` | `data_021023a0` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `blx` | `r2` |  |
| `bl` | `func_02084e0c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

