# 020152ec — func_020152ec (main, class D, 424B)

## Recipe + Risk
**Recipe:** 0x8d8/0x8e0 :1 bitfield guards; 0x920 cmp-chain + state-1 0x924 jump-table; bit1 gates streq 0x924--; (x&~1)|1
**Risk:** struct-guessed: asm TRUNCATED inside .L_1978 — that arm, the state-2 arm and _LIT consts are reconstructed; verify vs full .s. The bit1==0 predicated `0x924--` (ldreq/subeq/streq) must stay an if-guarded single decrement, not a branch.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_020152ec (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     0x8d8/0x8e0 :1 bitfield guards; 0x920 cmp-chain + state-1 0x924 jump-table; bit1 gates streq 0x924--; (x&~1)|1
 *   risk:       struct-guessed: asm TRUNCATED inside .L_1978 — that arm, the state-2 arm and _LIT consts are reconstructed; verify vs full .s. The bit1==0 predicated `0x924--` (ldreq/subeq/streq) must stay an if-guarded single decrement, not a branch.
 *   confidence: low
 */
extern char *GetSystemWork(void);
extern int  func_02019034(void);
extern void func_020190c0(void);
extern void func_020190ec(void);
extern int  func_020195ec(void);
extern int  func_02019640(int);
extern int  func_02019664(void);
extern int  func_0201969c(void);
extern void func_0201abd4(int);
extern void func_0201b7b4(void);

struct Flag { unsigned int b0 : 1; unsigned int b1 : 1; };

void func_020152ec(void *o) {
    char *w = GetSystemWork();
    if (((struct Flag *)(w + 0x8d8))->b0 == 0) return;
    switch (*(int *)(w + 0x920)) {
    case 1:
        switch (*(int *)(w + 0x924)) {
        case 1:
            *(int *)(w + 0x924) = 4;
            /* fallthrough */
        case 4:
            if (func_02019664() != 0x65) return;
            if (func_0201969c() != 1) return;
            *(int *)((char *)o + 8) = 0x000d0100;
            *(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;
            return;
        case 3:
            if (((struct Flag *)(w + 0x8e0))->b0) return;
            if (func_020195ec() != 0) {
                *(int *)((char *)o + 8) = 0x000d0102;
                return;
            }
            *(int *)((char *)o + 8) = 0x000d0101;
            if (((struct Flag *)(w + 0x8e0))->b1 == 0)
                *(int *)(w + 0x924) = *(int *)(w + 0x924) - 1;
            func_0201b7b4();
            func_0201abd4(1);
            return;
        case 2: /* .L_1978, reconstructed: tail truncated after mov r0,#0x5 */
            *(int *)((char *)o + 8) = 0x000d0103;
            if (((struct Flag *)(w + 0x8e0))->b1 == 0) {
                *(int *)(w + 0x920) = 1;
                *(int *)(w + 0x924) = 5;
            }
            return;
        }
        return;
    case 2: /* reconstructed */
        func_02019034();
        return;
    }
}
```

## Struct context

Near-sibling to func_02015190. Asm TRUNCATED inside case2 of the 0x924 switch (.L_1978) and the state-2 arm are reconstructed. The critical codegen constraint is the `0x924--` decrement in case3, which must stay as a predicated `if (b1==0)` guard, not a branch.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| SystemWork+0x8d8 | enable gate | s32 / bit 0 | `:1` bitfield — master enable; early return if b0==0 |
| SystemWork+0x920 | dispatch selector | s32 | cmp-chain: 1 or 2 |
| SystemWork+0x924 | sub-state | s32 | jump-table when 0x920==1: cases 1/2/3/4 (case1 falls to case4); decrement in case3 |
| SystemWork+0x8e0 | two-bit flag | s32 / bits 0,1 | b0 tested in case3 and case4; b1 gates `0x924--` in case3 and `920/924` reset in case2; `(x&~1)|1` sets bit0 in case4 |
| o+8 | output param | int | packed status codes 0x000d0100..0x000d0103 |

## Closest matched example

**Path:** src/main/func_02014bf4.c (for the guard structure); see also docs/research/c-match-prep/02015190.c for the sibling family.
**Why similar:** Near-clone of func_02015190 — same recipe, different message constants (0x000c vs 0x000d), different ceiling constant (func_0201969c expected value is 1 vs 2), and the distinctive `0x924--` decrement in case3 guarded by bit1.
**Key lesson:** The `*(w+0x924) = *(w+0x924) - 1` decrement in case3, guarded by `if (b1 == 0)`, corresponds to `ldreq; subeq; streq` in ARM (predicated on EQ after the bit1 test). It must stay as a simple if-guarded subtraction — not `0x924 -= 1` in a different branch context — so mwcc can keep it predicated. The asm for case2 is reconstructed; verify the `920=1; 924=5` reset and the `mov r0,#0x5` before it against the full .s.

## Try this

1. Both 0x8d8 and 0x8e0 bit tests need `struct Flag { unsigned int b0:1; unsigned int b1:1; }` and pointer casts at their respective offsets. The b1 tests (three occurrences in case3 and case2) must produce `lsl#31; lsr#31` not `and #2`.
2. The `if (b1==0) *(w+0x924) = *(w+0x924) - 1;` pattern (case3) must stay as written — a single read-subtract-write inside an if, producing `ldreq r,r,+offset; subeq r,r,#1; streq r,r,+offset`. Do not simplify to `*(w+0x924)--` and do not move outside the if.
3. If the reconstructed case2 body (`.L_1978`) or state-2 arm diverges from the full .s: update the body from the full .s disassembly; if the `mov r0,#5` sequence cannot be placed correctly, park as permuter-class — the truncation makes the tail un-verifiable without the full disassembly.
