# 0208d7ac — func_0208d7ac (main, class C, 276B)

## Recipe + Risk
**Recipe:** switch (int) incl case 0; 0x60->0x20 fallthrough; case 0x20 strb-BEFORE-RMW per orig
**Risk:** permuter-class: 5-case signed tree (orig roots cmp #0x20) with case 0 + 0x60->0x20 fallthrough; tree shape and the inverted strb/RMW order in case 0x20 (fixed in C) vs other arms. Tree pivot is the fragile part.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0208d7ac (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     switch (int) incl case 0; 0x60->0x20 fallthrough; case 0x20 strb-BEFORE-RMW per orig
 *   risk:       permuter-class: 5-case signed tree (orig roots cmp #0x20) with case 0 + 0x60->0x20 fallthrough; tree shape and the inverted strb/RMW order in case 0x20 (fixed in C) vs other arms. Tree pivot is the fragile part.
 *   confidence: low
 */
/* func_0208d7ac (main, class C) — field idx7 (=0xe); DEEP SWITCH on int
 * (signed tree), MMIO RMW of 0x04000000 bit30 (0x40000000). Cases 0,0x10,0x20,0x40,0x60;
 * 0x60 FALLS THROUGH into 0x20.
 *   case 0    -> 0x04000000 &= ~bit30                                  (orig predicates this arm)
 *   case 0x10 -> 0x04000000 |= bit30 ; sound[0x244]=0x84
 *   case 0x40 -> 0x04000000 |= bit30 ; sound[0x246]=0x8c
 *   case 0x60 -> sound[0x246]=0x8c ; (ft 0x20)
 *   case 0x20 -> sound[0x245]=0x84 ; 0x04000000 |= bit30   (NOTE: strb BEFORE the RMW here)
 */
extern unsigned short data_021a6304[13];
extern void func_0208dce4(int bits);

void func_0208d7ac(int arg) {
    unsigned short old = data_021a6304[7];
    data_021a6304[7] = (unsigned short)arg;
    data_021a6304[0] = (data_021a6304[0] | old) & ~arg;
    switch (arg) {
    case 0x0:
        *(volatile unsigned int *)0x04000000 &= ~0x40000000;
        break;
    case 0x10:
        *(volatile unsigned int *)0x04000000 |= 0x40000000;
        *(volatile unsigned char *)0x04000244 = 0x84;
        break;
    case 0x40:
        *(volatile unsigned int *)0x04000000 |= 0x40000000;
        *(volatile unsigned char *)0x04000246 = 0x8c;
        break;
    case 0x60:
        *(volatile unsigned char *)0x04000246 = 0x8c;
        /* fallthrough */
    case 0x20:
        *(volatile unsigned char *)0x04000245 = 0x84;
        *(volatile unsigned int *)0x04000000 |= 0x40000000;
        break;
    }
    func_0208dce4(data_021a6304[0]);
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| `data_021a6304[0]` | mask word | `unsigned short` | running bitmask OR'd/AND-NOT per dispatch |
| `data_021a6304[7]` | current-state slot (idx7, byte offset 0xe) | `unsigned short` | stores incoming arg; old read before write |
| `0x04000000` | main sound control register | `volatile unsigned int` | bit30 (0x40000000) cleared for case 0, set for cases 0x10, 0x40, and the 0x60->0x20 fallthrough |
| `0x04000244` | sound channel register | `volatile unsigned char` | written 0x84 for case 0x10 (after the RMW) |
| `0x04000245` | sound channel register | `volatile unsigned char` | written 0x84 for case 0x20 (and fallthrough from 0x60), BEFORE the RMW |
| `0x04000246` | sound channel register | `volatile unsigned char` | written 0x8c for case 0x40 (after RMW) and case 0x60 (before fallthrough) |

## Closest matched example
**Path:** src/main/func_0208cd18.c
**Key lesson:** The critical asymmetry in this function is case 0x20: unlike cases 0x10 and 0x40 where the `0x04000000 |= bit30` RMW comes first and the `strb` follows, case 0x20 puts the `strb` to 0x04000245 BEFORE the RMW. This ordering is fixed in the C source by statement order and must match exactly. The 0x60->0x20 fallthrough also means case 0x60's `strb 0x04000246` lands before falling into case 0x20's `strb 0x04000245` then RMW.

## Try this
1. Confirm that case 0x20's body has `*(volatile unsigned char *)0x04000245 = 0x84;` as the FIRST statement, then `*(volatile unsigned int *)0x04000000 |= 0x40000000;` second — this inverted order vs the other arms is load-bearing and must be preserved.
2. Objdiff check: orig roots the comparison tree at `cmp arg, #0x20` (the median of 0/0x10/0x20/0x40/0x60). If your build roots at a different pivot (e.g. #0x40 or #0x10), the tree shape is the permuter-class divergence.
3. Park as permuter-class if the comparison tree pivot does not match after confirming the case bodies and fallthrough are correct; the tree pivot for a 5-case switch with case 0 included is determined by mwcc's internal analysis and is not directly controllable.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021a6304` | data symbol |
| `_LIT1` | `0x04000244` | MMIO/const |
| `_LIT2` | `0x04000246` | MMIO/const |
| `_LIT3` | `0x04000245` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208dce4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

