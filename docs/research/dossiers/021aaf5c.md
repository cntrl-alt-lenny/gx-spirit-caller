# 021aaf5c — func_ov021_021aaf5c (ov021, class C, 256B)

## Recipe + Risk
**Recipe:** C: precompute reserved ids r4/r5/r6, i in r8, count-1 bound
**Risk:** reserved-id chain cmp/cmpne ordering: orig is r6,r5,r4 (0x1386,0x1387,0x1388) — || order must match that reg-alloc; also k held vs reloaded for the array store. reshape-able (reorder || / reload k).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021aaf5c (ov021, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     C: precompute reserved ids r4/r5/r6, i in r8, count-1 bound
 *   risk:       reserved-id chain cmp/cmpne ordering: orig is r6,r5,r4 (0x1386,0x1387,0x1388) — || order must match that reg-alloc; also k held vs reloaded for the array store. reshape-able (reorder || / reload k).
 *   confidence: med
 */
/* func_ov021_021aaf5c: zero the 0x19c work block, seed header fields, Task_PostLocked,
 * then scan ids 1..N-1 collecting those that pass three filters (0202e2f8==0,
 * not one of the 3 reserved tail ids, 0202bb20!=0) into the +0x58 array until +0x5c hits 0x44c.
 * (ov021) class C: i held in r8, reserved ids precomputed in r4/r5/r6. */
extern void Fill32(int val, void *dst, int n);
extern int Task_PostLocked(int a, int b);
extern int data_ov021_0222cf44;
extern int data_021040ac;
extern int data_020b4768;
extern int func_0202b0e0(int a);
extern int func_0202bb20(int a);
extern int func_0202e2f8(int a);

int func_ov021_021aaf5c(char *o) {
    int n = data_020b4768;
    int i;
    Fill32(0, &data_ov021_0222cf44, 0x19c);
    *(int *)(o + 0x38) = 0;
    *(int *)(o + 0x40) = 0;
    *(int *)(o + 0x44) = 0;
    *(int *)(o + 0x48) = 0;
    *(int *)(o + 0x4c) = 0;
    *(int *)(o + 0x54) = (unsigned short)*(int *)((char *)&data_021040ac + 0x38);
    *(int *)(o + 0x58) = Task_PostLocked(0x1130, 4);
    *(int *)(o + 0x5c) = 0;
    for (i = 0; i < n - 1; i++) {
        int id = func_0202b0e0(i + 1);
        if (func_0202e2f8(id) != 0)
            continue;
        if (id == 0x1386 || id == 0x1387 || id == 0x1388)
            continue;
        if (func_0202bb20(id) == 0)
            continue;
        {
            int k = *(int *)(o + 0x5c);
            *(int *)(o + 0x5c) = k + 1;
            ((int *)*(int *)(o + 0x58))[k] = id;
            if (*(int *)(o + 0x5c) >= 0x44c)
                break;
        }
    }
    return 1;
}
```

## Struct context
The function writes to an output struct at `o` (passed by pointer) and accesses two globals:

| field | offset | type | purpose |
|-------|--------|------|---------|
| o+0x38 | +0x38 | int | cleared to 0 at init |
| o+0x40..0x4c | +0x40..+0x4c | int[4] | cleared to 0 at init |
| o+0x54 | +0x54 | int | seeded from data_021040ac+0x38 (u16 cast) |
| o+0x58 | +0x58 | int* | handle from Task_PostLocked (used as int array base) |
| o+0x5c | +0x5c | int | running count of collected ids; capped at 0x44c |

`data_021040ac` is `GlobalAudioState`; offset +0x38 is not in the confirmed field list (GlobalAudioState.md covers 0x3C onward) — it may be a different global. `data_021040ac + 0x38` is cast to `(unsigned short)` before storing to `o+0x54`.

The three reserved IDs `0x1386`, `0x1387`, `0x1388` map to r6/r5/r4 in the original — precomputing them as local constants forces mwcc to allocate them to callee-saved registers. Declaration order matters: declare them in the order that yields r4=0x1388, r5=0x1387, r6=0x1386 (reverse of source order), i.e. declare `id1386` first so it gets the highest register number.

## Closest matched example
**Path:** src/overlay021/func_ov021_021aaadc.c
**Why similar:** same ov021 module, similar init pattern (multiple sequential `*(int *)(o + offset) = val` stores) followed by function calls using `o` fields as arguments, and a `return 1` epilogue.
**Key lesson from that file:** The sequential field stores at `o+0x44`, `o+0x48`, etc. must appear in source order — mwcc does not reorder sequential stores. The pattern `func = func_X(o); *(o + field) = func;` places the store immediately after the call with no intervening operations.

## Try this
1. Build as-is. In objdiff check the `if (id == 0x1386 || id == 0x1387 || id == 0x1388)` comparison sequence: the risk note says orig emits cmp/cmpne in order r6(0x1386), r5(0x1387), r4(0x1388). If the order differs, reverse the `||` chain: `if (id == 0x1388 || id == 0x1387 || id == 0x1386)`. mwcc evaluates `||` left-to-right so the first operand gets the lowest register.
2. For the array store `((int *)*(int *)(o + 0x58))[k] = id`: if `k` is reloaded from `*(o + 0x5c)` instead of held in a register, try pre-binding: `int k = *(int *)(o + 0x5c);` before the store, then `*(int *)(o + 0x5c) = k + 1;` after — exactly as written in the prep. If orig reloads, remove the `k` binding and write `((int *)*(int *)(o + 0x58))[*(int *)(o + 0x5c)] = id;`.
3. Fallback: this is reshape-able per the risk note. The reserved-id order and k-binding are the two levers; two reshape rounds should suffice. If neither fixes the cmpne ordering, check that `n - 1` in `i < n - 1` does not get optimised differently (try `int lim = n - 1; for (i = 0; i < lim; i++)`).
