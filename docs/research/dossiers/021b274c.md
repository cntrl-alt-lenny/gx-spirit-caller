# 021b274c — func_ov014_021b274c (ov013, class D, 196B)

## Recipe + Risk
**Recipe:** divmod: plain / and % by 360/255/60; switch returning per-case value
**Risk:** /360 etc: smull magic via plain /. switch: returning different constants per case.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_021b274c (ov014, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     CLASS D: 3-bit fields as :3; memset(val,dst,len); store 0x2c/8/0/4 in source order
 *   risk:       struct-guessed + reshape-able: a3c&7 vs :3 field on f4 may differ (orig masks BOTH with lsl#29;lsr#29 -> both want :3 fields). Confirm g_104f4c layout; store-order of f2c/f8/f0/f4 must stay as written.
 *   confidence: low
 */
/* func_ov014_021b274c: object init. If not already inited (f2c==0) memset a
 * 0x4c blob, then compare two 3-bit fields of g_104f4c; equal -> strcpy a
 * label into obj+0xc, else zero obj+0xc[0x20]. Clear f2c/f8/f0/f4, then by
 * g_1b5040.f44 either arm a sub-state (f2c=1) or run the fallback init. */

extern void  func_02094504(int val, void *dst, int len);      /* memset(dst,val,len) */
extern char *func_020aadf8(char *dst, const char *src);       /* strcpy */
extern void  func_ov005_021ad018(void *p, int a, int b);
extern void  func_ov014_021b2fb8(void *self);

extern char data_02105989[];                 /* _LIT2 strcpy source */
extern void *data_ov014_02235094;            /* _LIT0 memset dst */

typedef struct {
    unsigned int f4   : 3;   /* +0x4 low 3 bits */
    unsigned int      : 29;
    /* ... */
    unsigned char a3c;       /* +0xa3c (only low 3 bits used) */
} G104f4c;
extern G104f4c data_02104f4c;                /* _LIT1 */

typedef struct { int f44; } G1b5040;
extern G1b5040 data_ov014_021b5040;          /* _LIT3 */

typedef struct {
    int  f0;        /* 0x0 */
    int  f4;        /* 0x4 */
    int  f8;        /* 0x8 */
    char pad_c[0x20]; /* 0xc.. label/sub-object */
    char pad2[0x2c - 0xc - 0x20];
    int  f2c;       /* 0x2c gate */
} Obj14_274c;

int func_ov014_021b274c(Obj14_274c *self)
{
    if (self->f2c == 0) {
        func_02094504(0, &data_ov014_02235094, 0x4c);
        if (data_02104f4c.f4 == (data_02104f4c.a3c & 7))
            func_020aadf8((char *)self + 0xc, data_02105989);
        else
            func_02094504(0, (char *)self + 0xc, 0x20);
    }
    self->f2c = 0;
    self->f8  = 0;
    self->f0  = 0;
    self->f4  = 0;
    if (data_ov014_021b5040.f44 != 0) {
        self->f2c = 1;
        func_ov005_021ad018((char *)self + 0xc, 3, 1);
    } else {
        func_ov014_021b2fb8(self);
    }
    return 1;
}
```

## Struct context
The batch recipe/risk header says "divmod: plain / and % by 360/255/60; switch returning per-case value" — this conflicts with the prep file content (which describes a struct init function with bitfields and memset). The prep file is the ground truth; the batch index recipe label may have been misassigned to this address. The prep file describes an object-init pattern (no division, no switch over constant return values).

Key types from the prep file:

| field | offset | type | purpose |
|-------|--------|------|---------|
| Obj14_274c.f0 | +0x00 | int | cleared to 0 (third store) |
| Obj14_274c.f4 | +0x04 | int | cleared to 0 (fourth store) |
| Obj14_274c.f8 | +0x08 | int | cleared to 0 (second store) |
| Obj14_274c.pad_c | +0x0c | char[0x20] | label/sub-object; strcpy'd or zeroed |
| Obj14_274c.f2c | +0x2c | int | gate (0 = not inited); armed to 1 or cleared |
| G104f4c.f4 | +0x04 | unsigned int:3 | 3-bit comparison field |
| G104f4c.a3c | +0xa3c | unsigned char | 3-bit comparison (low 3 bits used) |
| G1b5040.f44 | +0x44 | int | branch selector for the sub-state arm |

`data_ov014_02235094` is the memset destination (a separate global, not `self`). `func_02094504` is memset with arg order (val, dst, len) — reversed from standard C memset.

## Closest matched example
**Path:** src/overlay014/Ov014_UpdatePanes.c
**Why similar:** ov014 matched function with multi-branch logic, struct field reads/writes in asm order, and a guard at function entry.
**Key lesson from that file:** The store order `f2c=0, f8=0, f0=0, f4=0` after the conditional block must appear in exactly that sequence in C — do not sort by offset. Verify by checking the asm instruction order; the prep file has already inferred this as `f2c / f8 / f0 / f4`.

## Try this
1. The critical risk is the 3-bit field comparison: `data_02104f4c.f4` is a `:3` bitfield, and `data_02104f4c.a3c & 7` is the manual mask on the char. If objdiff shows different shift/mask sequences for both sides of the `==`, try writing both as `:3` bitfields in the struct. Alternatively, if only one side uses `lsl#29;lsr#29`, make only that side a bitfield and leave the other as `& 7`.
2. The store order `self->f2c = 0; self->f8 = 0; self->f0 = 0; self->f4 = 0;` must stay in that exact C order. If objdiff reorders them, do not factor into a loop or a memset — keep each as a separate assignment statement.
3. If `func_02094504`'s arg order (val=0, dst=&data_ov014_02235094, len=0x4c) produces wrong asm (e.g. dst and val swapped), double-check the extern signature — this helper has reversed memset argument order. If the struct layout (especially the pad between +0x2c and +0x0c) is wrong, adjust the pad sizes and re-run.
