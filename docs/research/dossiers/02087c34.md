# 02087c34 — func_02087c34 (main, class C, 180B)

## Recipe + Risk
**Recipe:** two stride loops; hoist constants 0x7f/1/0 into callee-saved (mov r6/r5/r4 before loop); store-order +0x20,+0x18,+0x1c
**Risk:** Orig hoists loop-invariant constants (0x7f,1,0,0xc) and base ptrs into callee-saved regs BEFORE loop B; mwcc may rematerialize them inside the loop. reshape-able only partly; constant-hoist is permuter-class.

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_02087c34 (main, class C, MED tier) — brief 496. */

struct A87c34 { char _pad[0x44]; };  /* stride 0x44 */
struct B87c34 {                       /* stride 0x24 */
    char _pad00[0x18];
    int  f18;                         /* +0x18 */
    int  f1c;                         /* +0x1c */
    unsigned char b20;                /* +0x20 */
};

extern char data_021a4868[];
extern char data_021a485c[];
extern struct A87c34 data_021a4874[];
extern struct B87c34 data_021a4cb4[];
extern void func_0207d1b8(void *p, int n);
extern void func_0207d12c(void *base, void *elem);

void func_02087c34(void) {
    int i;
    char *a;
    struct B87c34 *b;

    func_0207d1b8(data_021a4868, 0x14);
    func_0207d1b8(data_021a485c, 0x14);

    a = (char *)data_021a4874;
    for (i = 0; i < 0x10; i++) {
        a[0x2c] = 0;
        a[0x3c] = 0;
        func_0207d12c(data_021a485c, a);
        a += 0x44;
    }

    b = data_021a4cb4;
    for (i = 0; i < 0x20; i++) {
        func_0207d1b8(b, 0xc);
        func_0207d1b8((char *)b + 0xc, i);
        b->b20 = 0x7f;
        b->f18 = 1;
        b->f1c = 0;
        b += 0x24 / 0x24;
    }
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| struct A87c34 +0x2c | byte | `char` | zeroed each iter loop A |
| struct A87c34 +0x3c | byte | `char` | zeroed each iter loop A |
| struct B87c34 +0x18 | f18 | `int` | store 1 (store-order 3rd, after b20) |
| struct B87c34 +0x1c | f1c | `int` | store 0 (store-order 4th) |
| struct B87c34 +0x20 | b20 | `unsigned char` | store 0x7f (store-order 1st in triple) |

## Closest matched example
**Path:** src/main/func_02087ce8.c
**Key lesson:** Loop B store order `b20=0x7f` then `f18=1` then `f1c=0` is non-ascending (+0x20, +0x18, +0x1c). This exact non-monotonic store sequence must be preserved; mwcc with -O4 can reorder stores when no alias risk is evident. The matched func_02087ce8 demonstrates that reloading the base pointer between each non-contiguous store is what forces the correct emission order — here the struct pointer `b` must not be held in a different reg across all three stores.

## Try this
1. In loop B, place the store statements in exactly the order `b->b20 = 0x7f; b->f18 = 1; b->f1c = 0;` — do NOT reorder to ascending offset order. If mwcc reorders, introduce a `volatile` write for `b->b20` to force the ldrb/strb first.
2. Confirm in objdiff that loop A emits `a[0x2c]=0` before `a[0x3c]=0` in each iteration; if reversed, swap statement order.
3. If the constant-hoist (0x7f/1/0/0xc hoisted to callee-saved regs before loop B) diverges and cannot be forced by C reshape (mwcc rematerializes inside loop), park as permuter-class — the register hoist of loop-invariant immediates is scheduling-sensitive and not forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_021a4868` | data symbol |
| `_LIT1` | `data_021a485c` | data symbol |
| `_LIT2` | `data_021a4874` | data symbol |
| `_LIT3` | `data_021a4cb4` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0207d1b8` | main TU call |
| `bl` | `func_0207d12c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

