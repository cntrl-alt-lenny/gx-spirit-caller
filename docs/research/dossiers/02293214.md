[//]: # (markdownlint-disable MD013 MD041)

# 02293214 — func_ov002_02293214 (ov002, class F retriage, 396B)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** Uses DuelQueueState (data_ov002_022ce288) offsets alongside Ov002Slot id_lo13. At 396B this is the largest in the batch but both struct layouts are now documented. The multi-branch gating pattern (DuelQueueState state field → slot scan → card handler call) is a standard duel-dispatch sequence.
**Confidence:** high
**Structs needed:** DuelQueueState (base 0x022CE288, f_5a8 queue_state, f_5b4 pending-action flag, f_5c0 re-entrancy lock, f_5c8 status, f_5d4 cleared-on-edge), Ov002Slot (id_lo13 :13), PerPlayerRowTable (stride 0x868)

## Assembly

```asm
; Source file not present — func_ov002_02293214 has no .s file in the working tree.
; This function was either already matched and its .s removed, or was never emitted
; as a standalone unit. The retriage entry is preserved for campaign tracking.
; Size: 396B, Conf: high, KB hits: DuelQueueState + Ov002Slot
```

## Struct context

| struct | offset | field name | type | use in this function |
|--------|--------|-----------|------|---------------------|
| DuelQueueState | +0x5A8 | f_5a8 | int | queue state 0x7f / 0x80 |
| DuelQueueState | +0x5B4 | f_5b4 | int | pending-action flag |
| DuelQueueState | +0x5C0 | f_5c0 | int | re-entrancy lock |
| DuelQueueState | +0x5C8 | f_5c8 | int | general status (0 = idle) |
| DuelQueueState | +0x5D4 | f_5d4 | int | cleared on 0->1 edge |
| Ov002Slot | +0x008 | id_lo13 | u32 :13 | card id via lsl#19/lsr#19 |
| PerPlayerRowTable | base | data_ov002_022cf16c | extern | player slot array |

## Recipe sketch

```c
typedef unsigned short u16;
struct Ov002Self { u16 f0; u16 b0 : 1; u16 rest : 15; };
struct DuelQueueState {
    char _pad5a8[0x5a8]; int f_5a8;
    void *f_5ac;
    char _pad5b4[4]; int f_5b4;
    char _pad5c0[8]; int f_5c0;
    int f_5c8;
    int f_5cc;
    int f_5d0;
    int f_5d4;
    int f_5d8;
};
extern struct DuelQueueState data_ov002_022ce288;
extern char data_ov002_022cf16c[];

/* 396B: DuelQueueState multi-state gated + Ov002Slot scan per player */
int func_ov002_02293214(struct Ov002Self *self) {
    if (data_ov002_022ce288.f_5c0) return 0;
    int qs = data_ov002_022ce288.f_5a8;
    if (qs == 0x80) {
        data_ov002_022ce288.f_5c0 = 1;
        /* active: iterate both players' slots */
        for (int p = 0; p < 2; p++) {
            char *row = data_ov002_022cf16c + (p & 1) * 0x868;
            for (int s = 0; s < 5; s++) {
                unsigned int w = *(unsigned int *)(row + 0x30 + s * 0x14);
                if (((w << 19) >> 19) == 0) continue;
                /* invoke card handler(self, p, s) */
            }
        }
        data_ov002_022ce288.f_5c0 = 0;
        return 0x7f;
    }
    if (qs == 0x7f) {
        if (data_ov002_022ce288.f_5c8 == 0) {
            data_ov002_022ce288.f_5d4 = 0;
            /* cleanup dispatch */
        }
    }
    return 0;
}
```

## Closest matched example
**Path:** src/overlay002/func_ov002_0223483c.c
**Why similar:** Same DuelQueueState f_5a8 switch (0x80 active / 0x7f done) with per-player Ov002Self operations inside each arm.
**Key lesson:** The 0x80 arm must return 0x7f (the done marker), not 1; mwcc emits `mov r0,#0x7f` / `ldmia` for the active-path return. The `f_5c0` re-entrancy lock must bookend the loop body precisely — any extra store reorders the `str #0` epilogue.

## Try this
1. Load `data_ov002_022ce288.f_5a8` as `*(int *)(data_ov002_022ce288 + 0x5a8)` and guard with `if (f_5a8 == 0x80)` first; verify `cmp r0,#0x80` is the first branch in objdiff.
2. Inside the 0x80 arm, declare `data_ov002_022ce288.f_5c0 = 1;` before the slot loop and `data_ov002_022ce288.f_5c0 = 0;` after — check the `str r3,[rY,#0x5c0]` stores are bracketing the loop in the diff.
3. If the 0x7f arm has a `f_5c8 == 0` sub-guard mismatching, bind `f_5c8` to a local before the check rather than re-loading from the struct — this is a DuelQueueState dispatch, not a PPT scan.
