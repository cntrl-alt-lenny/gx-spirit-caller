# 021b4764 — func_ov010_021b4764 (ov010, class C, 196B)

## Recipe + Risk

**Recipe:** CLASS C: bitset write via 1<<(n&7) byte addressing; word-align check
**Risk:** bitset byte: (1<<(n&7)) at byte offset n>>3. word-align: check n>>5 for word index.

## Prep draft

No prep file exists at `docs/research/c-match-prep/021b4764.c`. The following is reconstructed from the recipe/risk notes and ov010 matched patterns.

```c
/* func_ov010_021b4764 (ov010, class C, 196B): bitset byte-write.
 * Sets or clears a bit in a byte-addressed bitset array.
 * Bit n occupies byte n>>3, bit position n&7.
 * Word-align check: the word index is n>>5.
 *
 * RECONSTRUCTION — verify offsets and array address from objdiff/symbols.txt.
 */

/* Bitset base — replace with actual data symbol from disassembly. */
extern char data_ov010_bitset[];

void func_ov010_021b4764(int n, int val) {
    /* byte offset = n >> 3, bit position = n & 7 */
    int byte_off = n >> 3;
    int bit = 1 << (n & 7);

    if (val != 0)
        data_ov010_bitset[byte_off] |= (char)bit;
    else
        data_ov010_bitset[byte_off] &= (char)~bit;
}
```

## Struct context

No named struct documented for the ov010 bitset array. The function operates on a flat byte array (BSS global). The word-align check (`n>>5` for word index) suggests the array may also be accessed as an `int[]` in a companion function, but this function uses byte addressing only.

| field     | offset  | type    | purpose |
|-----------|---------|---------|---------|
| bitset[n>>3] | n>>3 | char    | byte holding the target bit |
| bit pos   | n&7     | —       | bit index within the byte |
| word idx  | n>>5    | —       | word-level index (may gate the byte op) |

## Closest matched example

**Path:** src/overlay009/func_ov009_021ad1bc.c
**Why similar:** Same pattern of computing a bit index with shifting and masking, then testing a bitset array.
**Key lesson from that file:** The `(1 << (n - 1))` mask applied to an int array element is a common bitset read in ov009. For a write, the key is that `|=` on a `char` array element emits `ldrb; orr; strb` (three instructions), while `|=` on an `int *` emits `ldr; orr; str`. The recipe says byte addressing, so use `char *` cast. If mwcc folds the `1 << (n & 7)` into a different bit-extract sequence, bind the shift result to a named `int bit` local first.

## Try this

1. Implement as `data_ov010_bitset[n >> 3] |= (unsigned char)(1 << (n & 7));` for the set path and `&= ~(...)` for the clear path. The `(n & 7)` mask is the key — without it mwcc may emit a full shift without the AND.
2. In objdiff, check whether the word-align check (`n >> 5`) generates a guard branch before the byte op. If the orig has a `cmp rX, #max_word; bge skip` before the byte write, add a bounds check: `if ((n >> 5) < limit) { ... }`.
3. If the `ldrb/orr/strb` sequence doesn't appear (mwcc emits ldr/orr/str on a word), verify the array extern is declared as `char[]` not `int[]`. If the bit computation order is off, escalate to permuter.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

