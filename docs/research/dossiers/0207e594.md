# 0207e594 — func_0207e594 (main, class C, 164B)

## Recipe + Risk
**Recipe:** self-relative reloc adds; bind img in r5; counted u16 loop; halfword zero-extend
**Risk:** loop counter: orig does (i+1)<<16>>16 zero-extend each iter and reloads ldrh count inside loop; if mwcc keeps i in 32-bit reg or hoists count load, the lsl/lsr#0x10 + reload diverge. permuter-class (loop reg-alloc/zero-extend).

## Prep draft
```c
/* CAMPAIGN-PREP candidate for func_0207e594 (main, class C, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     self-relative reloc adds; bind img in r5; counted u16 loop; halfword zero-extend
 *   risk:       loop counter: orig does (i+1)<<16>>16 zero-extend each iter and reloads ldrh count inside loop; if mwcc keeps i in 32-bit reg or hoists count load, the lsl/lsr#0x10 + reload diverge. permuter-class (loop reg-alloc/zero-extend).
 *   confidence: low
 */
#include <stdint.h>

typedef struct {
    uint16_t count;
    int reloc4;
    int field8;
    int fieldc;
    int field10;
    int field14;
} Img;

extern int func_0207e638(Img *base, int idx);
extern void func_0207e6ac(int p);
extern int func_0207e6bc(void);

void func_0207e594(Img *img) {
    int delta;
    int i;
    int *p;

    img->reloc4 += (int)img;
    delta = func_0207e6bc();
    for (i = 0; i < img->count; i++) {
        p = (int *)func_0207e638(img, i);
        p[1] += delta;
    }
    if (img->fieldc != 0) {
        img->fieldc += (int)img;
        p = (int *)img->fieldc;
        p[1] += (int)img;
    }
    if (img->field10 != 0) {
        img->field10 += (int)img;
        func_0207e6ac(img->field10);
    }
}
```

## Struct context
| offset | field | type | use |
|--------|-------|------|-----|
| +0x00 | count | uint16_t | loop upper bound; reloaded as ldrh each iteration |
| +0x04 | reloc4 | int | self-relative field; patched by `+= (int)img` |
| +0x0c | fieldc | int | optional pointer; if non-zero, self-reloc then deref p[1] += img |
| +0x10 | field10 | int | optional pointer; if non-zero, self-reloc then call func_0207e6ac |

## Closest matched example
**Path:** src/main/func_0207e738.c
**Key lesson:** The self-relative reloc pattern `p->f_c += (int)p` is the exact idiom used in matched siblings — add the struct pointer (cast to int) into the field in-place. The loop body calls `func_0207e638` which bounds-checks via u16 count; the loop variable `i` must appear as a 16-bit zero-extend per iteration (`(i+1)<<16>>16`), which is the permuter-class pressure point.

## Try this
1. Declare loop counter `i` as `unsigned short` (not `int`) so mwcc emits `uxth`/`lsl#16;lsr#16` zero-extend each iteration; keep `img->count` read inside the loop condition (not hoisted to a local) so the `ldrh` reappears in the loop body.
2. Check objdiff: the `lsl#16; lsr#16` pair on `i` in the loop test and the `ldrh` for `img->count` inside the loop body must both appear — if mwcc hoists count or keeps `i` in 32-bit, this is the divergence.
3. Park as permuter-class; the zero-extend/reload scheduling on a u16 loop counter is un-forceable without macOS permuter.
