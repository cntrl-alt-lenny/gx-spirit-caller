# 02013b4c — func_02013b4c (main, class D, 280B)

## Recipe + Risk

**Recipe:** dense 0..10 switch => addls-pc jump table; bit0 &1 gate; conditional store via if(==N); BIC=&~
**Risk:** struct-guessed: SystemWork 0x8d8/0x920 and dst+8 inferred. Table only forms if mwcc keeps cases 0..10 dense (incl. empty case10) and bit0 read matches; orig bit0 is lsl#31;lsr#31 (:1 bitfield) vs &1->`and`. reshape-able (bitfield + dense cases).

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02013b4c (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     dense 0..10 switch => addls-pc jump table; bit0 &1 gate; conditional store via if(==N); BIC=&~
 *   risk:       struct-guessed: SystemWork 0x8d8/0x920 and dst+8 inferred. Table only forms if mwcc keeps cases 0..10 dense (incl. empty case10) and bit0 read matches; orig bit0 is lsl#31;lsr#31 (:1 bitfield) vs &1->`and`. reshape-able (bitfield + dense cases).
 *   confidence: low
 */
/* func_02013b4c(dst): SystemWork enable-bit(0x8d8 bit0) gate, then a DENSE
 * jump-table dispatch on SystemWork[0x920] for values 0..10 (orig uses
 * `cmp #0xa; addls pc,pc,r0,lsl#2`). Most arms store a packed status word to
 * dst->f8 (off 8); arm1 also clears 0x2000 in *func_0201b75c(); arms 4/5
 * gate their store on func_0201b7e0(dst)==4 / ==5. */
extern char *GetSystemWork(void);
extern int  *func_0201b75c(void);
extern int  func_0201b7e0(int *dst);

void func_02013b4c(int *dst)
{
    char *sw = GetSystemWork();
    if ((*(int *)(sw + 0x8d8) & 1) == 0)
        return;
    switch (*(int *)(sw + 0x920)) {
    case 0:
        dst[2] = 0x00020100;
        break;
    case 1:
        dst[2] = 0x00020200;
        {
            int *p = func_0201b75c();
            *p &= ~0x2000;
        }
        break;
    case 2:
        dst[2] = 0x00020300;
        break;
    case 3:
        dst[2] = 0x00020400;
        break;
    case 4:
        if (func_0201b7e0(dst) == 4)
            dst[2] = 0x00020500;
        break;
    case 5:
        if (func_0201b7e0(dst) == 5)
            dst[2] = 0x00020600;
        break;
    case 6:
        dst[2] = 0x00020700;
        break;
    case 7:
        dst[2] = 0x00020800;
        break;
    case 8:
        dst[2] = 0x00020900;
        break;
    case 9:
        dst[2] = 0x00020a00;
        break;
    case 10:
        break;
    }
}
```

## Struct context

Reads SystemWork at two confirmed offsets. The `dst[2]` write is an output parameter. `func_0201b75c()` returns a pointer to SystemWork+0x8f4.

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| SystemWork+0x8d8 | enable gate | s32 / bit 0 | `:1` bitfield — master enable; early return if bit0==0 |
| SystemWork+0x920 | dispatch selector | s32 | dense switch cases 0..10 → `addls pc` jump-table |
| SystemWork+0x8f4 (via func_0201b75c) | flag word | s32* | AND'd with ~0x2000 (BIC) in case1 |
| dst[2] (=dst+8) | output param | int | packed status codes 0x00020100..0x00020a00 per arm |

## Closest matched example

**Path:** src/main/func_02013a64.c
**Why similar:** Same `GetSystemWork()` char*+offset pattern, same SystemWork+0x8d8 gate, same read-modify-write of the 0x8f4 flag word (that function sets bit 0x100; this clears 0x2000).
**Key lesson:** The jump-table `addls pc,pc,r0,lsl#2` for cases 0..10 only forms when all 11 cases (including the empty `case 10: break;`) are present and the range is truly dense. If any case is omitted, mwcc may fall back to a compare-chain. The `*p &= ~0x2000` (BIC) in case1 must remain as a pointer dereference via `func_0201b75c()` — inlining the address directly would change the addressing sequence.

## Try this

1. The bit0 test at `sw+0x8d8` must use `:1` bitfield, not `& 1`: add `struct Flag { unsigned int b0 : 1; };` and test `((struct Flag *)(sw + 0x8d8))->b0 == 0`. This is near-certain to be the first match-blocker.
2. If the switch does not become a `addls pc` jump-table (11 entries 0..10): verify the empty `case 10: break;` is present in the source. If mwcc still emits a compare-chain, try rewriting with an explicit range guard `if ((unsigned)*(int*)(sw+0x920) > 10) return;` before the switch — this hints the dense-table shape.
3. If the conditional stores for cases 4 and 5 (`if (func_0201b7e0(dst) == 4/5)`) produce extra branches: keep them as simple `if` statements (not ternary or predicated), which gives the cleanest `bl; cmp; strne/streq` pattern.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00020100` | MMIO/const |
| `_LIT1` | `0x00020200` | MMIO/const |
| `_LIT2` | `0x00020300` | MMIO/const |
| `_LIT3` | `0x00020500` | MMIO/const |
| `_LIT4` | `0x00020600` | MMIO/const |
| `_LIT5` | `0x00020700` | MMIO/const |
| `_LIT6` | `0x00020900` | MMIO/const |
| `_LIT7` | `0x00020a00` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201b75c` | main TU call |
| `bl` | `func_0201b7e0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

