# 02079b48 — func_02079b48 (main, class C, 116B)

## Recipe + Risk

**Recipe:** C: decl i first; borrow=(t>>16)&1; if-break via goto for the beq; i++ before tail-copy guard mirrors asm
**Risk:** The post-break `i++; if(i>=n)` before the copy loop and the `dst==src` early-out ordering. reshape-able by exact statement order, but mwcc may hoist the i<<1 index recompute (mov lr/ip) differently. reshape-able.

## Prep draft

```c
/* func_02079b48 — big-integer subtract-immediate with borrow over a
 * u16 limb array: dst[i] = src[i] - borrow, borrow = bit16, stop once
 * borrow clears; then copy the untouched tail when dst != src.
 */

void func_02079b48(unsigned short *dst, unsigned short *src,
                   unsigned int sub, int n) {
    int i = 0;
    if (n > 0) {
        do {
            unsigned int t = src[i] - sub;
            sub = (t >> 16) & 1;
            dst[i] = (unsigned short)t;
            if (sub == 0) goto tail;
            i++;
        } while (i < n);
    }
tail:
    if (dst == src) return;
    i++;
    if (i >= n) return;
    do {
        dst[i] = src[i];
        i++;
    } while (i < n);
}
```

## Struct context

| offset | field | type | use |
|--------|-------|------|-----|
| — | dst | `unsigned short *` | output limb array |
| — | src | `unsigned short *` | input limb array |
| — | sub | `unsigned int` | initial borrow/subtrahend (u32, becomes borrow carry after first limb) |
| — | n | `int` | limb count |

No struct globals. Twin of `func_02079cc0` (add-carry version at 02079cc0).

## Closest matched example

**Path:** src/main/func_0207d430.c
**Key lesson:** mwcc's do-while guarded by `if(n>0)` emits the guard `cmp; beq` before the loop label. The `goto tail` after the borrow-clear check emits as `beq .Ltail` in the original, not `bne; continue` — the goto form is mandatory to match the branch direction. Declaration `int i = 0` inside the function (not inside the `if`) controls which register mwcc assigns i into (callee-saved vs temp).

## Try this

1. Keep `i` declared at function scope (before `if (n > 0)`), NOT inside the do-while body — this forces mwcc to keep i in a callee-saved register across the goto-split control flow. If objdiff shows `mov lr/ip` index recompute at the copy-loop entry, add an explicit `int j = i + 1;` and use j for the tail loop.
2. The `(t >> 16) & 1` borrow extract must stay as a separate statement (`sub = (t >> 16) & 1;`) not folded into the dst store — mwcc must emit `lsr#16; and#1` before the `strh`. Confirm in objdiff that `sub = (t>>16)&1` appears BEFORE `dst[i] = (unsigned short)t;`.
3. If mwcc converts the `if(sub==0) goto tail` to a conditional-suffix instruction block rather than a branch, reshape: split `dst[i] = (unsigned short)t; if(sub==0) goto tail; i++;` into a helper-variable form to force the branch.

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

