# 02030b84 — func_02030b84 (main, class D, 248B)

## Recipe + Risk

**Recipe:** INVERTED polarity: matched cases return 0, default return 1; pure cmp-tree
**Risk:** Polarity inversion is load-bearing (.L_654=0 is the match target, .L_65c=1 default) -- already encoded. Residual: pure-tree pivot/balance + base offset signs. Polarity is reshape-able (confirmed); tree shape is permuter-class.

## Prep draft

```c
/* CAMPAIGN-PREP candidate for func_02030b84 (main, class D, MED tier) — brief 496.
 * UNVERIFIED + ITERATION-EXPECTED: the MED tier rarely first-build-matches.
 * The campaign drops this into src/, runs ninja + objdiff, and applies the
 * risk note below (these usually need 1+ reshape or a permuter/.s fallback).
 *   recipe:     INVERTED polarity: matched cases return 0, default return 1; pure cmp-tree
 *   risk:       Polarity inversion is load-bearing (.L_654=0 is the match target, .L_65c=1 default) -- already encoded. Residual: pure-tree pivot/balance + base offset signs. Polarity is reshape-able (confirmed); tree shape is permuter-class.
 *   confidence: med
 */
/* func_02030b84: INVERTED card-id membership test.
 * Unlike its siblings the matched cases fall through to `mov r0,#0`
 * (.L_654) and the default tail is `mov r0,#1` (.L_65c) -- so the C is
 * `switch{case..: return 0;} return 1;`. Binary-search tree over pivot
 * 0x1954 with lit-base +/-offset derivations; pairs/triples at
 * 0x15f9/0x15fa, 0x1953/0x1954, 0x1a6d..0x1a6f, 0x1b4c..0x1b4e. */
int func_02030b84(int a0) {
    switch (a0) {
    case 0x15b1:
    case 0x15b4:
    case 0x15f9:
    case 0x15fa:
    case 0x1953:
    case 0x1954:
    case 0x1a6d:
    case 0x1a6e:
    case 0x1a6f:
    case 0x1b4c:
    case 0x1b4d:
    case 0x1b4e:
        return 0;
    }
    return 1;
}
```

## Struct context

No struct fields accessed. Pure switch over integer argument `a0` (card ID).

| offset | field | type | use in this function |
|--------|-------|------|---------------------|
| n/a | n/a | n/a | pure card-id predicate, no struct |

## Closest matched example

**Path:** src/main/func_02031684.c
**Why similar:** Same inverted-polarity pattern (in-set->0, default->1) over a sparse card-ID set, same class D, same size range.
**Key lesson:** The polarity flip (case block returns 0, implicit default returns 1) is correct as written; the main residual risk is mwcc choosing a different pivot for the binary-search tree. If objdiff shows the compare-tree is mirrored or re-rooted, try reordering source cases to hint the pivot — or park as permuter-class if tree shape diverges.

## Try this

1. Drop prep draft verbatim; check objdiff for polarity first (both `mov r0,#0` and `mov r0,#1` tails present and in correct positions relative to the case block).
2. If polarity is correct but tree shape diverges (wrong pivot order), try splitting the 12 cases into two explicit `if (a0 < 0x1954)` / `else` blocks to nudge mwcc's balance point toward the 0x1954 pivot noted in the prep comment.
3. Park as permuter-class if the compare-tree pivot/balance remains wrong after reshaping; tree shape is un-forceable without macOS permuter.

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001954` | MMIO/const |
| `_LIT1` | `0x000015f9` | MMIO/const |
| `_LIT2` | `0x00001a6d` | MMIO/const |
| `_LIT3` | `0x00001b4d` | MMIO/const |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

