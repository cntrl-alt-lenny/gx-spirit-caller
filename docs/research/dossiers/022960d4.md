# 022960d4 — func_ov002_022960d4 (ov002, class E/F retriage)

## Re-triage
**Original verdict:** E/F — previously intractable
**Upgrade reason:** N/A — address not found in the retriage doc's full tractable table and has no corresponding .s file in src/overlay002/.
**Confidence:** N/A
**Structs needed:** Unknown

## Status

**No source file found.** `src/overlay002/func_ov002_022960d4.s` does not exist in the repository. The address `0x022960D4` also does not appear in the full tractable list in `docs/research/retriage/Ov002EFRetriage.md`.

Nearby addresses that do exist:
- `func_ov002_02296048.s` — 0x02296048
- `func_ov002_022960e8.c` — 0x022960E8 (already matched)
- `func_ov002_02296134.s` — 0x02296134

It is possible that `0x022960D4` was superseded, merged into an adjacent function, or the address in the batch list was a transcription error. Verify against `config/eur/**/symbols.txt` for the correct address range.

## Assembly

No assembly available — .s file absent.

## Struct context

No assembly to analyze.

## Recipe sketch

No assembly to analyze.

## Closest matched example

No assembly available for comparison.

## Try this
1. Check `config/eur/**/symbols.txt` for the symbol at or near address `0x022960D4` to confirm the correct function address.
2. If the nearest unmatched function is `func_ov002_02296048.s` or `func_ov002_02296134.s`, those addresses should be used instead.
3. Escalate: the batch list address `022960d4` may be a typo for `022960e8` (already a matched .c file) or `022960d0` / `022960dc` — re-verify the intended address before attempting a match.

## GROUND TRUTH (from .s)

**BLOCKED — no `.s` ship exists.** Exhaustively searched `brain/src/main/` and all `brain/src/overlayNNN/` for this address (as `func_{addr}.s` and every `func_ovNNN_{addr}.s` variant) and found no `.s` file and no matched `.c`/`.legacy.c`/`.legacy_sp3.c` either. This dossier was generated from the c-match-prep batch index recipe/risk alone, with no backing disassembly. Cannot extract pool words or callee targets — there is nothing to extract. Needs re-carving (fresh `dsd`/`batch_carve` pass) or index triage before this candidate is campaign-ready; out of scope for the dossier-writing lane.

