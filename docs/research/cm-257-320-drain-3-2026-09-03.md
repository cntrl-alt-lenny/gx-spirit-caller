[//]: # (markdownlint-disable MD013 MD041)

# cm-257-320-drain-3: JPN ledger-visible tranche — 2/16 attempted (12.5%), 2/17 pool (11.8%)

**Status:** done. Two JPN ports shipped, fourteen candidates were attempted and
parked, and one candidate was screened before writing C. All screening and
attempt outcomes are now ledgered. The work was single-threaded and touched no
USA source or configuration.

## Premise and census

The required scoped selector, `wall_aware_headroom.py --exclude-attempted
--min-size 257 --max-size 320 --min-bl-blx 4`, measured **232 candidates before
this tranche**. After 17 ledgered candidates, it measures **215 candidates**
remaining and 923 excluded-attempted rows. The stated 232 premise therefore
held; this round drained an examined tranche, not the entire remaining pool.

The required `port_census.py` baseline was run from a clean `origin/main`
checkout and completed with exit 0: JPN had 54 `BACKLOG-s` entries / 9,392 B,
with byte-similarity unavailable in that clean checkout. The post-change census
also completed with exit 0: JPN had 54 `BACKLOG-s` entries / 9,888 B, including
24 byte-identical entries / 4,888 B. The scoped selector is the comparable
before/after measure for this tranche because it directly accounts for the
attempt ledger.

## Result and ledger coverage

Seventeen candidates were examined: 16 real attempts and one sight-screen.
Two of 16 attempts shipped (**12.5% attempted-rate**); two of 17 examined
shipped (**11.8% pool-rate**). The prior BR-4/BR-8/BR-9 all-time denominator was
8/51. Including this tranche, the cumulative figures are **10/67 = 14.9%**
by attempted candidate and **10/68 = 14.7%** by examined pool. These are kept
separate from this round's rates.

The new screen was `0x021b0df8`, parked as `not-attempted` on the formal
`complexity` wall. Screens this round: **P-20: 0; other grounds: 1**. The
cumulative confirmed P-20 encounter count remains **18**. The first three
canary ledger rows were validated; the screen row was accepted by the ledger
validator with a formal wall tag. No new P-NN wall was introduced. The
existing provisional `m2c-draft-near-miss` class was added to
`tools/park_class_map.tsv` for the near-miss rows.

| Address | Size | Result | Match | Park / shape |
|---|---:|---|---:|---|
| `0x021c9c94` | 280 | shipped | 100.0% | guard-chain |
| `0x021c9fc4` | 272 | shipped | 100.0% | guard-chain |
| `0x021af9d0` | 288 | parked | 54.2% | m2c draft near miss |
| `0x021b0388` | 320 | parked | 86.3% | m2c draft near miss |
| `0x021b03b8` | 264 | parked | 3.0% | m2c draft near miss |
| `0x021b1998` | 320 | parked | 6.3% | m2c draft near miss |
| `0x021bca00` | 272 | parked | 29.4% | m2c draft near miss |
| `0x021b60cc` | 272 | parked | 39.7% | m2c draft near miss |
| `0x021ca024` | 312 | parked | 2.6% | m2c draft near miss |
| `0x021cff18` | 272 | parked | 0.0% | m2c draft near miss |
| `0x021f2e0c` | 260 | parked | 92.3% | m2c draft near miss |
| `0x021f0848` | 268 | parked | 14.9% | m2c draft near miss |
| `0x021b9254` | 276 | parked | not measured | tool anomaly |
| `0x021b9368` | 276 | parked | not measured | tool anomaly |
| `0x021baa04` | 308 | parked | not measured | tool anomaly |
| `0x021bc8c8` | 312 | parked | not measured | tool anomaly |
| `0x021b0df8` | 300 | not-attempted | unknown | complexity screen |

The four tool-anomaly rows record raw m2c compile-context failures, not
measured matching scores. The temporary P-20 classification considered for
`0x021b0388` was checked against BR-9's exact data-table addresses and removed:
it was a real attempt, not a screen.

## Gate and compiler observations

Both accepted ports passed the single-candidate SHA-1 helper. The full canary
gate then passed all three ROM regions after regenerating `state-table.md` and
`dashboard.md`; the initial test-only failure was stale generated documentation
and disappeared on the mandated regeneration. The three final regional clean
gates and the tests gate are recorded in the PR evidence.

The machine-wide compiler process check was idle before every build/gate check;
no `mwcc`, `mwld`, `mwasm`, or `ninja` process was active. `port_harvest.py`
does not accept a region argument, so this JPN work used the direct single-
candidate tooling and ledger recording path.

The push-vs-`sub sp,#N` alignment signature remains a compiler-internal wall;
no further attempts were spent on it. The pop/lr/bx epilogue and unconditional
incoming-register-spill signatures remain observations, not promoted levers.
The prior volatile MMIO case remains inconclusive because its structural
mismatch prevented isolating the volatile effect.

**Provenance:** `cm-257-320-drain-2-2026-09-02.md`, BR-9 in
`codegen-walls.md`, and the `cm-257-320-drain-3` rows in `attempts.tsv`.
