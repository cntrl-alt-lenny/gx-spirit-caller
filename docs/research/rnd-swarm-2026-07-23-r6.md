# R&D Swarm — Round 6 (2026-07-23)

**Method:** 20 lenses across external / technique / tooling / strategy; refute-by-default verification; each survivor independently reproduced against the live repo; deduped against five prior rounds. **105 raised, 31 survived.** This is the first round run entirely inside the readable-C chapter — byte-coverage is complete (all three regions rebuild byte-identical) and the routing-tier discovery (b665/b667) is in hand — so nothing here ships ROM bytes and every survivor is judged on readable-C leverage or on gate/metric integrity, not on claimed byte impact.

## The honest read

- **Coverage is done; the live number is readable-C, and it is small.** ~8.6% EUR (8.6261% live tree / 8.5911% at HEAD clean), 7.23% USA/JPN. Everything below is about widening that number honestly or about the tooling that measures and merges it.
- **The "wall" corpus is systematically overstated, and this round proved it a third time.** 7 functions from brief-641's largest park-on-sight class (6 of 8 named reg-alloc instances + P-4-cited func_02084ac4) now reach byte-identical `.text` from ordinary C, object-verified at the prescribed tiers. Mechanism is mostly routing-tier gap + one signature-arity fix — a third instance of the b665/b667 pattern, not a new allocator lever. But the standing park rules and 3 of 17 P-wall headings still read live and are actively misdirecting budget.
- **The merge gate cannot currently be trusted — three independent breaks, all hours-scale.** gate3 can print `GATE PASS` having executed zero checks; the required `drift-check` never fires on the canonical `config+src+queue` ship-PR shape (2 PRs BLOCKED right now, `main` itself sitting red on it); `compile-check` is 100% red on 2 of 3 legs (17–18 of 20 runs). Until fixed, none of the three legs is evidence of anything.
- **The readable-C headline is contaminated and the contamination is accelerating.** Inline asm is −0.1448 pp of the EUR number (3,456 B / 92 TUs); the asm-escape rate jumped 0.80%→4.23% by bytes inside the 32-day window — a discrete 3-day burst where asm was 70.8% of new EUR C bytes on 2026-07-21 — and no CI inspects new `.c` content. The partition is clean (zero mixed TUs corpus-wide), so `q-natural-c-metric` is a ~15–40-line change, not a research problem.
- **The sweep selector measures the wrong thing, and a lane declared itself swept on it.** `wall_aware_headroom.py` emits no size field, so briefs 663/664 sized candidates by `.s` file bytes (~10× the `.text` span), voiding the "shape not size" premise, and brief 665 concluded in writing that the decomper's 1,224-candidate ≤256 B-`.text` range was "already swept" (it saw 10 files by disk bytes). The small tier is real runway — ~69% of the tranche by count — not exhausted.
- **Peer evidence is under-mined at zero byte-risk.** The sm64ds catalogue moved repos and grew ~970 lines of same-binary (1.2/sp2p3) evidence whose §7a directly overturns our reg-alloc parking rule; `-lang c99` is `.text`-neutral (1,106 SAME / 0 DIFF across all tiers/regions) and both pret NDS repos ship it — a source-space widening we've been leaving on the table because we deduped against a stale snapshot.
- **Highest-leverage next move:** ship the 7 reg-alloc functions (bytes tomorrow), add `text_size` to the selector, and re-sweep the small tier — one sequence that converts a falsified wall class and a mis-measured lane into the round's first concrete readable-C runway. Fix the three gate breaks in the same wave so the ships can actually merge.

## Top 10 R&D bets (ranked)

1. **Ship the 7 reg-alloc functions and retire the park-on-sight rule.** Brief-641's largest lever-insensitive class is byte-falsified for 6 of 8 named + P-4's func_02084ac4; all reach byte-identical `.text` from C (plain `.c`, `.legacy.c`, `.thumb.c` — routing is load-bearing). *Action:* ship from `scratchpad/rega/` with the verified per-tier routing; restate as "6 of 8 + P-4"; record func_020b3850 and func_0208b1ac as SURVIVING; scope doc edits to codegen-walls.md P-4 and lever-payoff.md L21 only. **Effort: hours.**

2. **Give `wall_aware_headroom.py` a `text_size` field and re-sweep the small tier.** The selector emits bare path strings; a 122× disk-vs-`.text` units error made the decomper lane believe its 1,224-candidate range was swept (10 by disk bytes). *Action:* emit `{path,addr,text_size}` (a join on delinks it already parses) + `--min/--max-size`; repoint the three sweep items at `main-small-tier-worklist.md`; delete the `ls -la … sort -k5` queue lines and the "already swept" claim; correct 663/664 and void Q3. **Effort: hours.**

3. **Restore the merge gate (three fixes, one wave).** (a) gate3: extract `verdict()`, return exit-2 `GATE VACUOUS` when nothing ran, reject `--scope tests --no-tests` at argparse, add `tests/test_gate3.py`. (b) drift-check: delete the `paths:` block, add `push:[main]`, regenerate `docs/research/README.md`. (c) compile-check: gate the EUR disjunct to `eur` only, require `^src/$REGION/`|`^libs/` on non-eur legs, add libs to the trigger. *Evidence:* 2 PRs BLOCKED, main red on drift, 17–18/20 compile runs red. **Effort: hours.**

4. **Re-ingest the sm64ds delta and verify against 1.2/sp2p3 first.** ~970 new lines / 21 dated sections banked against the exact `.legacy.c` binary; §7a reports the uniform "register allocation" near-miss label was a categorizer failure and 6/8 then matched — direct contradiction of our brief-641 rule. *Action:* re-ingest to `imported-sm64ds-r2.md`, run brief-524's harness on §6k/6q/6u/6y/6z/7a/7b at 1.2/sp2p3 first, fix the false "different SP revisions" line. **Effort: days.**

5. **Implement `q-natural-c-metric` and add a per-PR asm-void counter.** Partition is clean (zero mixed TUs), so a per-TU boolean gives natural-C EUR 8.5911%→8.4462%; the escape rate is accelerating and ungated. *Action:* add `natural_c_code_bytes()` skipping `^\s*(?:static\s+)?asm\s+…\(`, print the natural-C line, add a zero-mixed-TU regression test; retarget `scope_gate.py` to count added/modified asm-void `.c` under `src/` and `libs/` and surface it in the PR template. **Effort: hours.**

6. **Adopt `-lang c99` behind a per-TU flag table in `configure.py`.** c99 is `.text`-neutral (1,106 SAME / 0 DIFF, all tiers/regions), legalises declaration-at-point-of-use (a proven allocator lever), and both pret NDS repos ship it; C89 is strictly smaller source-space at zero byte gain. *Action:* table-ise the three version constants into `{suffix:(binary,flags)}` + a path-glob→flags map (landed as a `ninja sha1`-proven no-op), patch the `DSD_OBJDIFF_ARGS` copy, flip default on neutrality. This also turns `-str reuse`, the dsi tier, and the decomp.me-ID fix into one-line experiments. Treat the reg-alloc *payoff* as unproven — adopt on hygiene. **Effort: day.**

7. **Fix the progress-badge publisher broken by `main-protection`.** github-actions[bot] is not a bypass actor, so the direct `git push HEAD:main` can never satisfy checks reported after push; README front-page shows EUR 8.5% while HEAD is 8.6% and the weekly cron will keep failing. *Action:* retarget the workflow to push SVGs to an unprotected `progress-visuals` branch and reference via raw.githubusercontent (pokeplatinum's `xmap` pattern). **Effort: hours.**

8. **Build `inc_oracle.py` from the ROM's own shipped source fragments.** `extract/<ver>/files/BSC/` holds 16 `.inc` C/Makefile fragments + 45 plaintext `.ydq` — byte-verified against arm9.bin (PresentCard.inc int16 @0x020b5e5c, CharParam.inc int8 @0x020b5b87), both resolving to `complete` opaque-blob TUs. *Action:* pack each `.inc` as {int8,int16,int32}, search `extract/<ver>/{arm9,arm9_overlays}/*.bin`, rewrite confirmed blobs as named typed arrays preserving the generator comment, gate on `ninja sha1`; feed `makelist_src.inc` into symbols.txt. **Effort: days.**

9. **Add a machine-readable attempts ledger.** The failed-candidate exclusion set exists only as prose across ~10 briefs; one queue item admits brief 661's 8 candidate names were never recorded — permanently lost. *Action:* append-only `attempts.tsv {addr,module,text_size,tier,shape,result,match_pct,park_class,brief}` with `merge=union` in `.gitattributes`, one required protocol row per candidate touched, a `wall_aware_headroom --exclude-attempted` flag; consolidate with cmatch_loop.py's existing state schema. **Effort: day.**

10. **Correctness sweep on the queue + wall docs.** Two live surfaces misdirect agents: 3 of 17 P-walls (P-7/P-8/P-10) still read as live permanent walls while `cm-p6-followup` will re-test them; `work_queue.py next` claims body-less duplicate-ID items and its status regex deletes the post-header blank line, manufacturing MD022 (a required check) on every transition. *Action:* append `— SUPERSEDED BY C-27/C-25/C-29` to the three headings and name the 13 live entries in the queue item; change `\s*$`→`[ \t]*$`, give the PARKED-note its own leading newline, reject duplicate IDs / blank-body claims, de-dupe the 4 live pairs. **Effort: hours.**

## external

Byte-coverage is already 100%, so nothing here ships ROM bytes; verification repeatedly discounted "A" claims on that basis. The real payoffs are (1) fixing live-broken tooling and (2) generating readable-C levers. Findings ranked on verified value, not claimed impact.

### S

**sm64ds lever catalogue is 3 weeks stale — ~970 lines / 21 dated sections of new same-compiler evidence unmined.** Our 2026-07-03 ingest snapshotted `notes/mwccarm-codegen.md` at 493 lines; the repo moved (bmanus2 → tangosdev), hit 94.6% functions, and the file is now 1,465 lines — all 21 new sections dated after our ingest, every one banked against byte-exact **mwccarm 1.2/sp2p3, the exact binary our `.legacy.c`/`.thumb.c` tiers use** (verified: our 207 `.legacy.c` counts exact). §7a reports a uniform "register allocation" near-miss label was a *categorizer* failure, not a floor, and 6/8 such functions then matched — directly contradicting our brief-641 rule that parks the reg-alloc class as lever-insensitive. Our peer-sweep deduped against the stale snapshot and structurally couldn't see the delta. Bonus: `imported-sm64ds.md` wrongly calls their tiers "a different pair of SP revisions" — both are literally 1.2/sp2p3, so every borrowed lever was under-weighted as cross-version-risky. Action: re-ingest the delta to `imported-sm64ds-r2.md`, verify §6k/6q/6u/6y/6z/7a/7b against 1.2/sp2p3 first via brief-524's harness, fix the tier-mismatch line. Effort: **days**.

**`compile-check.yml`, our only baserom-free gate, is a 2/3-red noise generator (17/20 runs red).** The path filter admits any changed file not under `src/(usa|jpn)/`, so every EUR-frontier PR feeds `src/main/**` targets to the usa/jpn legs, which don't exist in their ninja graph → `ninja: error: unknown target`. Reproduced off-CI: `ninja -t query build/jpn/src/main/...legacy_sp3.o` errors; usa/jpn delinks carry only `src/usa/**`+`libs/**`. The 3 green runs are workflow-only PRs hitting the sentinel. Secondary hole: `libs/**/*.c` is never compile-checked in any region (100 real TUs). Action: require `^src/$REGION/` or `^libs/` for non-eur legs, `-notmatch '^src/(usa|jpn)/'` for eur, add libs to trigger+pathspec; until fixed treat this workflow as evidence of nothing. Effort: **hours**.

### A

**The new `main-protection` ruleset silently broke the progress-badge publisher (GH013 on every run since 2026-07-22).** Ruleset 19573966 requires 3 status checks on `refs/heads/main` with only RepositoryRole-5 as bypass; github-actions[bot] isn't a bypass actor, so `progress-badge.yml`'s direct `git push origin HEAD:main` can never satisfy checks reported *after* push. Both post-ruleset runs failed; `assets/progress-*.svg` last updated pre-ruleset, so README's front-page shows EUR 8.5% while HEAD is 8.6%, and the weekly cron will keep failing. Action: retarget the workflow to push SVGs to an unprotected `progress-visuals` branch and reference via raw.githubusercontent (pokeplatinum's `xmap` pattern) — smallest fix that keeps visuals; decomp.dev hosted badge is prior art but gated on owner-only onboarding (#1022). Effort: **hours**.

**We're pinned to C89 by `-lang=c`; `-lang c99` is `.text`-neutral on our compiler and unlocks declaration-at-point-of-use for free.** Both pret NDS repos ship `-lang c99`. Locally, swapping only that token across 1,106 real TUs (all 4 routing tiers, all 3 regions) gave 1,106 SAME / 0 DIFF on every non-debug section; C89 rejects declaration-after-statement and `for(int i…)` on all three compiler binaries. Declaration placement is one of our few proven allocator levers, so we've been operating in a strictly smaller source-shape space at zero byte risk. Caveats: full both-ways diff is ~26 min not a day; `//` already compiled under C89; must also patch the hardcoded copy in `DSD_OBJDIFF_ARGS`; the reg-alloc *payoff* is unproven — adopt on neutrality/hygiene, not on promised allocator movement. Effort: **day** (verify), then flip default.

### B

**`configure.py` has no per-TU flag override — the one routing dimension every peer has and we lack.** Routing is a single global `CC_FLAGS` plus a suffix→binary map; the emitted `build.ninja` carries `cc_flags = -lang=c` on all 4,188 compile edges with zero variation, and `mwcc_thumb`/`mwcc_legacy` rule bodies are byte-identical duplicates. Peers route flags per group (st: `-str reuse`, `-thumb`; pokeheartgold: per-dir `MWCCVER`). The `$cc_flags` per-edge variable already exists. Action: table-ise the three version constants into `{suffix:(binary,flags)}` and add a path-glob→flags map, landed as a no-op refactor proven by 3-region `ninja sha1`. This turns c99, `-str reuse`, and a **dsi 5th tier** (9 never-compiled `dsi/*` builds already on disk; zeldaret/st ships retail DS on `dsi/1.2p1`; empirically a distinct 3rd codegen class that fuses to `mla`, though a P-1 spot-check showed no wall unlock and payoff is speculative) into one-line experiments. Effort: **hours** (refactor).

**Our decomp.me scratch export hard-codes `mwcc_30_131` for all four tiers — 470 legacy TUs export as the wrong compiler.** `dsd objdiff --compiler` stamps every `.legacy.c`/`.legacy_sp3.c`/`.thumb.c` unit as 2.0/sp1p5, unmatchable by construction for exactly the cohort routed to legacy *because* sp1p5 can't reproduce it (empirically confirmed: 470/470 units mis-stamped). decomp.me hosts the right IDs (`mwcc_20_82`=1.2sp2p3, `mwcc_20_84`=1.2sp3) and objdiff's `scratch` is per-unit. No API needed (distinct from Cloudflare-blocked DEAD #7). Action: suffix→ID rewrite over `objdiff.json` in the existing post-gen chain. Papercut-tier (export path is manual-paste-dominated). Effort: **hours**.

**DEAD #4's "`opt_*` pragmas are inert" verdict never tested `opt_propagation` — the one optimizer pragma a peer actually ships.** r4's zero-byte-diff result was explicitly scoped to 8 CSE/reg-alloc tokens; `opt_propagation` (copy/constant propagation) was untested and is demonstrably **not** inert — an effect-test with a validated positive control produced byte-diffs on both tiers and 5/52 real functions, while bogus/peephole tokens flag as illegal. tangosdev ships `#pragma opt_propagation off` in matched 1.2/sp2p3 source. Action: run the effect-test over `opt_propagation` (+recognized peephole/schedule variants) on the b641 reg-alloc and b663 predication parks; any diff reopens a per-function axis, a clean zero closes DEAD #4 with the full token set. Effort: **hours**.

**mwcc's inline assembler has its own pool dialect (`dcd`/`lda`/`ldconst`) that refutes DEAD #11 and #13.** DEAD #11/#13 tested mwasmarm `.word` inside mwcc's inline assembler and wrongly concluded no duplicate-pool escape exists; in fact two `dcd gSym` emit two independent pool slots with two `R_ARM_ABS32` relocs — exactly the shape C-34 declares impossible in a `.c` file (canonical C-34 example `func_02023f7c` reproduced instruction-for-instruction as an `asm int` body). But an `asm`-bodied `.c` is asm-void (q-natural-c-metric), so this ships zero readable-C bytes and manufactures the artifact the project just decided doesn't count. Action: strike DEAD #11/#13, correct codegen-walls.md's C-34 entry and brief-654's "permanent" verdict, add `docs/research/mwcc-inline-asm-dialect.md` (noting `ldr rX,<label>` is rejected — every pool load needs hand-computed `[pc,#imm]`). Catalogue correction only; skip the cohort sweep. Effort: **hours** (doc).

**Biggest win in this domain:** the sm64ds re-ingest — ~970 lines of same-binary (1.2/sp2p3) lever evidence whose §7a directly overturns our standing brief-641 reg-alloc parking rule, reopening a class we've been abandoning as unmatchable.

## technique

**S — The reg-alloc/scratch-register "wall" is falsified: ship 7 functions from C.** Brief 641's largest documented lever-insensitive class (DEAD #12, "park on sight") is refuted for 6 of its 8 named functions plus P-4-cited func_02084ac4. Each reaches byte-identical `.text` from ordinary C, independently verified at object level (bytes + relocs + size + alignment + delink extent) at the prescribed tiers: 020a71e4/020a724c plain `.c`; 0207e214/02096040 `.legacy.c`; ov004_021dbe68/ov004_021de264 `.thumb.c` (the Thumb routing is load-bearing, not cosmetic); 02084ac4 `.c`. Mechanism is mostly routing-tier gap + one signature-arity fix, not a new allocator lever — a third instance of the b665/b667 pattern. Action: ship the 7 sources from `scratchpad/rega/`; restate as "6 of 8 + P-4"; record func_020b3850 and func_0208b1ac as SURVIVING; scope doc edits to codegen-walls.md P-4 and lever-payoff.md L21 only (do NOT touch P-11/P-12/P-15 — no verified function is cited there). Effort: hours.

**S — The "shape not size" premise rests on a 10× units error; add `text_size` to the selector.** Briefs 663/664 sorted candidates by on-disk `.s` file bytes, not `.text` span, because `tools/wall_aware_headroom.py --json` emits no size field. Brief 663's "650–780 B tier" is 40–76 B of `.text`; brief 664's "505 B+" is 40–52 B (505 is literally `wc -c func_ov002_021b91d0.s`). Open-question Q3 ("663 shipped 82% at 650–780 B vs 0/4 at 257–512 B → gate is shape not size") compares a 40–76 B batch against a real 260 B batch and is void. Root cause is written into the queue: `docs/queue/claude-decomper.md`'s `ls -la src/**/*.s | sort -k5 -n` lines. Action: add `text_size` + `--max/min-size` to wall_aware_headroom.py (a join on delinks paths it already parses); fix the two decomper-queue sort lines and the scaffolder cm-main-small-d/e size filter; correct 663/664 docs; delete Q3. Effort: hours.

**A — Quantify + implement the natural-C metric: EUR 8.5911% → 8.4462% (−0.1448 pp), partition is clean.** Inline-asm contamination of the readable-C headline is exactly 3,456 B/92 TUs EUR, 832 B/27 USA, 840 B/29 JPN, reproduced to the byte at HEAD e846a8e8. Critically, every asm-bearing code TU is 100% asm — zero `asm{...}` blocks inside real C functions corpus-wide — so `q-natural-c-metric` (already an ACTIVE TODO at `docs/queue/codex-scaffolder.md:134`, which already prescribes the per-TU boolean) collapses from an unscoped attribution problem to a ~15–40-line change. Action: add `natural_c_code_bytes()` skipping TUs matching `^\s*(?:static\s+)?asm\s+…\(`; print a natural-C line; add a regression test asserting zero mixed TUs (fail forces the per-function path); resolve routing suffixes so in-flight `.legacy_sp3.c` renames aren't silently skipped; refresh scorecard.md's stale totals in the same edit. Note EUR figures are a moving snapshot. Effort: hours.

**A — The asm-escape rate is accelerating and nothing gates it; add a per-PR asm-void counter.** EUR C TUs added before the 32-day window are 0.80% asm by bytes; TUs added inside it are 4.23% (53 TUs/2,240 B) — a 5.3× jump, reproduced digit-for-digit. All 53 landed in the final three days (07-20..22, not the finding's 07-21/22 split), so "accelerating" is a discrete burst, not a smooth trend — but on 2026-07-21, asm was 70.8% of new EUR C bytes, and no CI workflow inspects new `.c` content. Action: add an asm-void counter to `tools/scope_gate.py` (retarget it — carve-kind gate does NOT run on the offending lanes; hook the PR template + Claude lane protocols or gate3) covering added AND modified `.c` under both `src/` and `libs/`; report the asm-void trend. Fold the rate/scorecard table split into `q-natural-c-metric` rather than re-proposing it. Effort: hours.

**B — The r4-A3 `.init` fix chose the lossy branch, dropping 2,224 B of real C and hiding a region hole.** progress.py restricts numerator and denominator to `.text`, excluding EUR's entire 2,224-byte `.init` section — 51 C TUs, 100% byte-matched, counted as zero. The additive branch was strictly better: EUR 8.5911% → 8.6762% (+0.0851 pp) while still fixing r4-A3's 99.907% cap; USA/JPN move −0.0068 pp, surfacing an asymmetry (EUR 100% `.init` coverage vs USA/JPN 0%) that no metric shows. Action: change `.text`-only checks at progress.py:210, :236, AND :473+:485 (both denominator and numerator, or per-module C% worsens) to `name in CODE_SECTIONS`; INVERT the existing `test_init_excluded` test, don't merely add one; file the USA/JPN 2,224 B `.init` + 2,156 B itcm holes as a trivial EUR-twin carve target. Effort: hours.

**B — `data: 88.23%` is 95.7% `.bss`; the real initialised-data recovery is 25.3% EUR vs 6.6% USA.** The `data:` headline includes `.bss`, whose zero-fill supplies 4,034,784 of EUR's 4,214,213 matched bytes. Stripping it, initialised data (`.rodata`+`.data`+`.ctor`) is EUR 25.31%, USA 6.62%, JPN 6.51% — EUR's real recovery is 3.8× USA's, yet the printed line puts the regions 2.09 pp apart. Reproduced exactly (no report.json exists, so the Tier-2 delinks path is what prints today). Action: split the line into `data(init)` over `{.rodata,.data,.ctor,.dtor}` and `bss` alone (mirror the code-vs-C split at line 656); handle the Tier-1 objdiff path, which exposes no per-section breakdown; note `summarize_by_module` emits no data columns yet (this is an addition); refresh the stale docstrings in update_progress_badge.py and data_worklist.py. Effort: hours.

Biggest win in this domain: falsifying the reg-alloc wall — 7 byte-verified ships that overturn the project's largest park-on-sight class and prove "walls" keep turning out to be routing-tier gaps.

## tooling

Ranked by impact; overlapping findings merged. Every entry below survived adversarial verification against the live repo.

### S — required-check integrity (the merge gate cannot currently be trusted)

**gate3 prints "GATE PASS" having executed zero checks.** `python tools/gate3.py --scope tests --no-tests` exits 0 with GATE PASS — no configure, no `ninja sha1`, no pytest, no preflights — because `scope=="tests"` sets `regions=[]` (all preflights are gated on `if regions:`) and `--no-tests` skips the test arm, leaving `passed = True ∧ True`; the docstring promises "gate3 NEVER prints a PASS it didn't read" and there are zero behavioural tests. Latent (this exact combo has never been invoked) but 44 of 69 documented invocations already pass `--no-tests`. **Fix:** extract the last 6 lines of `main()` into a pure `verdict()`, return exit-2 "GATE VACUOUS" when nothing ran, reject the contradictory flag combo at argparse, add `tests/test_gate3.py`. `impact:S · effort:hours`

**Sweep candidates are size-gated on disk bytes, but the tier is defined in `.text` bytes — a 122x undercount that made a lane declare itself swept.** `wall_aware_headroom.py --json` emits bare path strings with no size field, so queue items fall back to the `ls -la … sort -k5` disk-byte proxy they prescribe; the Decomper's 0x02040000+ range holds 1,224 candidates ≤256 `.text` bytes but only 10 ≤512 disk bytes, and brief 665 used the proxy to conclude *in writing* "most of this range has already been swept" — a false verdict now baked into the queue body future dispatches read. **Fix:** emit `{path,addr,text_size}` (parse via existing `parsers.parse_delinks_file`, ~10 lines), repoint the three sweep items at `main-small-tier-worklist.md`, delete the `ls -la` instruction and the "already swept" claim. `impact:S · effort:hours`

**Required check `drift-check` never runs on ship PRs, yet main is red on it and no workflow can detect that.** `generated-files-drift.yml` fires only on `paths:` (tools/docs), so a canonical `config+src+queue` ship PR creates no check-run and GitHub holds it BLOCKED forever — PR #1292 is live proof, hidden only by admin bypass; it also has no `push: branches:[main]` trigger, so four briefs merged with drift-check=`failure` left origin/main failing `generate_research_index --check` (4 missing README rows), now surfacing as phantom failures on unrelated PRs #1286–1288. **Fix:** delete the `paths:` block, add `push:[main]`, regenerate `docs/research/README.md`, gate the index generators before the integ→main merge. `impact:S · effort:hours`

### A

**`Compile changed C (usa)/(jpn)` have failed 100% of runs — an EUR-shaped region filter applied to all three legs.** The predicate keeps any file *not* under `src/usa|jpn/`, so every EUR-only `.c` (which lives at `src/main/`, `src/overlayNNN/`) survives onto the usa/jpn legs and names a target absent from those build graphs → guaranteed `ninja: error: unknown target`; 15/15 recent runs failed, 13/13 recent C-touching PRs were red on 2 of 3 legs, normalizing red and training reviewers to ignore the third. **Fix:** gate the first disjunct to `eur` only, keep the per-region sentinel fallback; also filter targets to paths that exist in the checked-out merge ref (a separate eur-leg desync bug found in #1273). `impact:A · effort:hours`

**`work_queue.py next` claims empty body-less items, and its status-flip regex deletes the post-header blank line (MD022) on every transition.** Merge churn left duplicate `### id` headers where a body-less `[TODO]` sits above the real `[PARKED]`/`[DONE]` copy; `next` prints only the first section → a claimed item with zero instructions (reproduced live on codex-scaffolder), and `_HDR`'s trailing `\s*$` eats the newline so each flip manufactures an MD022 violation on the *required* Markdown check (two hand-fix commits already spent). **Fix:** change `\s*$`→`[ \t]*$` (and give the PARKED-note insertion its own leading newline — the one-char change alone doesn't cover `park`), reject duplicate IDs / blank-body claims in `cmd_next`/`_transition`, de-dupe the 4 live pairs. `impact:A · effort:hours`

**No machine-readable attempts ledger — the failed-candidate exclusion set exists only as prose across ~10 briefs.** Parks revert their C attempt so leave no git or source trace; the one catalogue (`cmatch-parked-and-floor.md`) stops at brief 562 while 641/650/654/655/661/663–666 each parked more, and a queue item admits brief 661's 8 candidate names "weren't recorded" — permanently unrecoverable. Later items must re-derive exclusions by hand-reading multiple reports, or cannot. **Fix:** append-only `attempts.tsv {addr,module,text_size,tier,shape,result,match_pct,park_class,brief}` with `merge=union` in `.gitattributes`, one required protocol row per candidate touched, and a `wall_aware_headroom --exclude-attempted` flag; consolidate with cmatch_loop.py's existing (gitignored) state schema rather than inventing a parallel one. `impact:A · effort:day`

**q-walls-doc-audit's corrections landed in a second doc and were never applied back — 3 of 17 P-walls still read live.** `codegen-walls.md` buries "SUPERSEDED BY" 53–116 lines below the P-7/P-8/P-10 headings (only P-6 carries status in-heading), the section header still says "Permanent (15 patterns)" against 13 live, and the queued `cm-p6-followup` tells an agent to re-test "the remaining P-NN entries" — burning budget on walls superseded 550+ briefs ago (P-6's own retirement yielded 2 instant ships). **Fix:** append `— SUPERSEDED BY C-27/C-25/C-29` to the three headings, correct the counts (three inconsistent totals exist), name the 13 live entries in `cm-p6-followup`; add a queue rule that audit output is applied in-PR. `impact:A · effort:hours`

### B

**The "atomic claim" guarantee is checkout-local — origin/main still advertises in-flight items as TODO.** Each of 5 worktrees writes its own copy of `docs/queue/*.md` and the claim isn't pushed before work starts, so an agent resumed from a clean main re-claims an item another worktree is several commits deep (reconciled by hand twice). **Fix:** have `cmd_next` print the claim-commit command as its first output line and add it to the 4 protocol headers; add `status --remote` scanning *all* lane branch tips — note a claim on the item's own feature branch does NOT close the hole, it must reach main or the reconcile must scan branches. `impact:B · effort:hours`

**4,040 LOC of ledger-DEAD permuter code still ships in the gate suite, and two live agent-facing surfaces route to it.** permute.py/permute_batch.py + tests = 4,040 LOC / 112 gated tests for a lane declared dead; worse, `diff_reasons.py`'s rendered 95–99% bucket and `lever-payoff.md`'s anti-pattern row (linked from a *standing* scaffolder queue header) still tell agents to "escalate to permuter" on the campaign's dominant reg-alloc residue class. **Fix:** delete the four files + the gate3 `_vendor` note, repoint `diff_reasons.py`/`lever-payoff.md` (and `decomp-workflow.md:616-630` + `permuter_settings.toml`) to "park — reg-alloc residue", regen tools-index. `impact:B · effort:hours`

**The tools re-audit's anti-circular signal is textual, not AST — it misclassifies in both directions.** It scored `strip_overlay_func_collisions.py` `runtime-imports:1` off a configure.py *comment* reading "NOT wired into this rule", and conversely gave `parsers.py` (13 real importers, incl. 4 CORE tools) no signal → classified HISTORICAL/archive; acting on that row would break the build. **Fix:** commit `tool_signal_audit.py` using an `ast.walk` import graph + subprocess-invocation scan and re-derive the OCCASIONAL class; separately add `tests/test_batch_sha1.py` — `_revert_many` discards `_revert_one`'s bool and prints "reverted" for a *failed* revert on live routing state (fix all 4 call sites, it drives the bisect). `impact:B · effort:hours`

**Queue headers cache stale tool output — three live files state three pool sizes and give "32" two meanings.** `claude-decomper.md:7` says "139 coercible / 32 no_marker / only 32 permanent" while the tool it tells the agent to run reports 88 / 55 / 32; `codex-scaffolder.md` says 6,117, `claude-scaffolder.md` 6,093. **Fix:** delete every hardcoded pool count from the 4 headers (plus `state.md` and the analytics docs) and replace with the command + interpretation rule only; a 4-digit-count lint is tempting but `kickoff_lint.py` is the wrong linter and uncwired — treat as a doc sweep, not a new gate. `impact:B · effort:hours`

**Refuted (negative result worth keeping): no Windows-Defender I/O wall, no meaningful baserom re-hash tax.** On the 8-core/16-thread 5800X, assembler/mwcc scale 6.6–7.2x at P16 (~0.85× physical cores = textbook CPU saturation, not a scanner wall); configure's 128 MB SHA-1 costs ~0.1 s. **Fix:** close `speedup-roadmap.md` lever #8 and the parallel-vs-serial I/O arm with a two-line correction (keep the untested two-lane arm), and do NOT add `--skip-sha1` to gate3. `impact:C · effort:hours`

**Biggest win in this domain:** the required-check trust chain is broken three independent ways — gate3 can print PASS without running anything, drift-check blocks the real ship-PR shape while sitting red on main, and compile-check is 100%-red on 2 of 3 legs — all hours-scale fixes that together restore a merge gate you can actually trust to protect the byte-identical ROM.

## strategy

**Original developer source fragments ship inside the ROM's own filesystem as a ground-truth naming oracle.** `extract/<ver>/files/BSC/` contains 16 `.inc` C/Makefile fragments and 45 plaintext `.ydq` INI files the developers shipped by accident — including `#define SCRIPT_INDEX_MAX 35`, Perl-generator headers (`// Made by mkPresentCard.pl`), and C array initializers byte-identical to tables currently sitting in the repo as opaque `const unsigned char data_XXXXXXXX[]` blobs. Byte-verified: `PresentCard.inc` packed int16 hits `arm9.bin` at 0x020b5e5c and `CharParam.inc` int8 at 0x020b5b87, both resolving via `config/eur/arm9/delinks.txt` to existing `complete` TUs (`src/main/data_020b5e20.c`, `src/main/data_020b5b80.c`). Zero project doc or tool references them; distinct from DEAD #21 (in-binary debug-string stripping) and DEAD #8 (third-party NitroSDK import) — these are the game's own pre-compilation source text. *Action:* build `tools/inc_oracle.py` to pack each `.inc` as {int8,int16,int32}, search `extract/<ver>/{arm9,arm9_overlays}/*.bin`, and emit `{inc_file, address, width, dims, owning TU}`; rewrite confirmed blobs as named typed arrays preserving the generator comment, gate on `ninja sha1`; feed `makelist_src.inc`'s ordered `.ysc` list into `symbols.txt` and `.ydq` keys into the duel-state struct. *Effort: days.* (A)

**The "100% byte coverage" claim is ARM9-only; ARM7 is an uncounted, undocumented, region-identical blob.** The byte-identical-ROM gate passes with the entire 166,336 B ARM7 binary shipped unmodified — `extract/{eur,usa,jpn}/arm7/arm7.bin` all hash to SHA-1 `e9c06dea…1625d` (one binary, three regions). `config/<ver>/` contains only `arm9`, `git ls-files | grep -i arm7` is empty, and no doc states ARM7 is out of scope (the 5 doc hits are all incidental compiler-flag/cross-project mentions). At 7.0% of ARM9's code size, it exceeds the entire current EUR readable-C corpus. Distinct from DEAD #8: this is a scope-honesty correction, not an import proposal. *Action:* add one scope paragraph to `README.md`/`docs/state.md`/`endgame-ledger.md` stating coverage is 100% of *ARM9* code+data, with ARM7 and the FAT filesystem reproduced from baserom blobs; do NOT open an ARM7 lane (overwhelmingly NitroSDK/sound-driver under DEAD #8's block), but record whether region-identical ARM7 could become a 3x-multiplier target if a license path appears. *Effort: hours.* (B)

**Biggest win in this domain:** the shipped `.inc`/`.ydq` source artifacts are a free, byte-verifiable identifier/type/dimension oracle that turns opaque data blobs into named typed arrays — build `inc_oracle.py` first.

## Negative / confirmatory results worth recording

- **CLOSE the C-34 cohort sweep temptation.** mwcc's inline-asm dialect (`dcd`/`lda`/`ldconst`) genuinely refutes DEAD #11/#13 and proves the C-34 "permanent" verdict is a catalogue error — but an `asm`-bodied `.c` is asm-void by the project's own natural-C metric, so it ships zero readable-C bytes. Correct codegen-walls.md's C-34 entry and strike DEAD #11/#13; do **not** run the 66-file cohort sweep. Doc-only.
- **CLOSE speedup-roadmap lever #8 (Windows Defender exclusion) and the baserom re-hash suspicion.** Build scales 6.6–7.2× at P16 on 8 physical cores (CPU saturation, no scanner wall); the 128 MB SHA-1 costs ~0.1 s. No admin-escalation task, no `--skip-sha1` on gate3. Only the two-lane parallel arm remains untested and worth keeping open.
- **dsi/* tier: one behavior class, not nine, and payoff speculative.** All 9 never-compiled `dsi/*` builds run natively and produce a genuinely distinct 3rd codegen class (fuses to `mla`), but every dsi build emitted identical `.text` on both probes, a P-1 spot-check showed no wall unlock, and the codegen is *more* aggressive than 2006 output. Worth one sweep of the parked corpora via the per-TU flag hook — budget for +1 class, treat as speculative.
- **opt_propagation is NOT inert — but low expected value.** It produces byte-diffs (5/52 functions, both tiers), so DEAD #4's blanket "opt_* pragmas inert" is over-broad; however the peer's own matching-style.md reports byte-diff never *improves* the match on their first-access-fold wall. Run the effect-test once on the b641/b663 parks: a clean zero closes DEAD #4 with the full token set, any diff reopens a per-function axis. Hours either way.
- **Two reg-alloc functions genuinely SURVIVE the falsification.** func_020b3850 (all `mul64` variants DIFF across every SP/opt combination) and func_0208b1ac (register-name residue, DIFF at all 3 tiers) are confirmed reg-alloc residue, not routing-tier gaps — record them so the next sweep does not re-attempt them.
- **The permuter lane is dead in code, not just in the ledger.** 4,040 LOC / 112 gated tests for a technique DEAD #5 already killed (b602: two ov002 plateaus at 23,370 / 14,682 iterations, no match), plus `diff_reasons.py` and `lever-payoff.md` still route agents to it on the campaign's dominant residue class. Delete the four files and repoint both surfaces (and `decomp-workflow.md:616-630`, `permuter_settings.toml`) to "park — reg-alloc residue."
