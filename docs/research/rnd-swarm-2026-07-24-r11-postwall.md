# R11 swarm — post-wall-collapse frontiers (2026-07-24)

**Run:** `wf_43046057-191`, 52 agents dispatched / 36 completed. 59 findings raised across 10 lenses;
25 went through refute-by-default verification (**23 survived, 2 refuted**).

> ⚠️ **Partial run.** 16 agents — 15 verifiers and the final synthesis agent — died on an account
> weekly-usage limit, not on error. This report was reconstructed by the brain from the run journal.
> Findings below are tagged **[VERIFIED]** (survived adversarial refutation), **[UNVERIFIED]** (raised
> with evidence, verifier never ran — treat as a lead, re-check before acting), or **[REFUTED]**.

## Executive summary — the top 5 bets

| # | Bet | Lens | Payoff | Effort |
|---|-----|------|--------|--------|
| 1 | Harvest the re-censused cross-region ports — **438 ports** (244/region HIGH, 219 byte-exact), ~**+0.70pt** readable-C per region. Use a `sim==1.0` floor to eliminate the struct-offset bug class outright. | Cross-region port automation | **S** | hours |
| 2 | Build `batch_port` on `batch_carve`'s existing Ops seam — turns the port harvest into a turnkey gated loop instead of hand-driving 438 ports. | Cross-region port automation | **S** | day |
| 3 | Fix the prototypes bank's **11 uncompilable decls** (BOOL/fx32/s32/s8 undefined in the header chain) + **12 asm-void arity landmines** before any go-live. Wrong-extern rate in matched TUs measured at **50.2%** — that is the payoff size. | Include / prototypes go-live | **S** | hours |
| 4 | Re-audit **P-11** (10 of 16 catalogued 'permanent' members already shipped) and **P-16** (its sole falsification used the exact pointer-cast idiom the typed-struct lever kills). | Wall-taxonomy hygiene | **S** | day |
| 5 | Retire the self-contradicting ceiling model (**48.03%** vs the 14–24% band from r7–r14) and adopt an explicit 'readable-C done' definition before it anchors any more planning. | Metrics truth & the readable-C endgame | **S** | hours |

## Per-lens findings

### Cross-region port automation

#### [VERIFIED] Backlog re-census: 244/region HIGH, 219 byte-exact (438 ports, ~+0.70pt each region)

- **Payoff:** S · **Effort:** hours
- **Claim:** The cm-crossregion-ports backlog at HEAD 2e7cec0a is 244 EUR .c files per region with a HIGH-confidence USA/JPN twin still shipping as .s — 219/region at masked-byte-sim exactly 1.0 (16,680 B each, ~+0.70pt natural-C per region vs the 2,385,948 B .text base), concentrated main=108 + ov002=83 + ov008=8; ov002 nearly doubled since r10 (42-45 → 83), so the queue item's ~170/region undersells the harvest by ~29%.
- **Evidence:** Full reproduction of the r10 method (find_region_siblings.find_siblings byte_disambiguate=True over all 3,604 address-keyed EUR .c × usa+jpn, size-bucketed for speed) run read-only at HEAD 2e7cec0a: per region — already-ported 2744, no-HIGH-sibling 604, BACKLOG-s 244 (sim=1.0: 219 / sim 0.925-0.987: 25 / [0.99,1.0): ZERO), HIGH-but-no-target-file 11 (10 genuine after drift-band correction). USA and JPN numbers identical. Routing-tier mix of the 219: 197 default, 16 legacy_sp3, 6 legacy, 0 thumb. Enumerated JSON: scratchpad\port_backlog.json; script: scratchpad\port_census.py (both under C:\Users\leona\AppData\Local\Temp\claude\C--Users-leona-Dev-gx-spirit-caller\b1d604d9-197e-468c-b069-5066df93024d\scratchpad\). Caveat: census predates the ~76 pending-branch ships — re-run after coordinator merges (script takes ~3 min).
- **Action:** Adopt the enumerated 219/region sim=1.0 list as the batch_port work queue (start main+ov002); commit port_census.py into tools/ so the census is re-runnable post-merge; update the cm-crossregion-ports queue numbers (244/219 per region, not 170).

#### [VERIFIED] sim==1.0 floor kills the struct-offset bug class; sub-1.0 diffs auto-decode the offsets

- **Payoff:** S · **Effort:** hours
- **Claim:** Gating ports at masked-byte-sim EXACTLY 1.0 eliminates the region-specific struct-offset bug class (briefs 673/676) by construction — identical non-reloc bytes mean EUR's literal offsets compile to the correct target immediates — and for the sim<1.0 tail the bug is mechanically detectable: diff the reloc-masked words and decode ARM load/store immediate fields to emit the exact EUR→target offset map.
- **Evidence:** Detector demo (scratchpad\offset_detector_demo.py) on brief 676's documented bug case: EUR func_0204c384 → USA func_0204c310, byte_sim=0.9706, and ALL 4 differing masked words decode to load immediates matching the brief exactly: +0x010/+0x054 e5d001a6→e5d0019e (LDRB imm 0x1a6→0x19e), +0x020/+0x064 e59001a0→e5900198 (LDR imm 0x1a0→0x198). Backlog sim distribution is strictly bimodal (219 at 1.0, 25 at 0.925-0.987, zero in [0.99,1.0)) so the 1.0 floor costs nothing today — but it must be exact-1.0, not 0.99: a one-offset shift in an 800B function still clears 0.99 (4/800 bytes = 0.995). find_region_siblings keeps HIGH for lone candidates at 0.90-0.99 (tools/find_region_siblings.py demotion rules ~lines 495-510), so the queue's '--confidence-floor HIGH' sweep alone WOULD attempt all 25 landmines.
- **Action:** In the batch_port driver, require byte_sim==1.0 to auto-port; for the 25/region sim<1.0 candidates, run the mask-diff + immediate-decoder and park each with its decoded offset map (turns each into a 5-minute manual port instead of a re-derived bug hunt).

#### [VERIFIED] batch_port is a bounded day-build on batch_carve's existing Ops seam

- **Payoff:** S · **Effort:** day
- **Claim:** A batch_port tool following batch_carve's commit-on-green pattern needs only: (1) enumeration from the census JSON, (2) per-candidate port_to_region.py --json, (3) a pure delinks-flip helper (replace 'src/usa/main/func_X.s:' header with '.c:' — the complete/.text block is byte-identical between .s and .c TUs), (4) .s content cached before deletion so bisect-revert can restore it, (5) gate = Ops(version=target).gate() per region; everything else (branch guard, dirty-delinks refusal, check_delink_dupes preflight, bisect-and-park, commit-on-green) is inherited unchanged.
- **Evidence:** tools/batch_carve.py:24-27 docstring: the driver 'is written against the Ops/Strategy seam so the region-port lane (port_to_region.py, gate on USA+JPN sha1) can be slotted in later without touching the batch/commit/bisect core'. Verified delinks format parity in config/usa/arm9/delinks.txt: 'src/usa/main/func_0204c310.c:\n complete\n .text start..end' vs identical .s-form blocks — same stem, extension-only flip. batch_carve._stage (~line 752) already caches s_content for exact re-apply — the mirror of what port-revert needs; brief 675 proved batch_sha1's bisection silently false-negatives a whole batch when a candidate's .s was deleted from disk, and batch_port deletes .s BY DESIGN, so revert-from-cache is load-bearing. port_to_region.py --json already emits rewritten source + output_path + delinks_entry + new_symbols_txt_lines (lines 1339-1350); defer SYNTHESIZED-symbol candidates in v1.
- **Action:** Build tools/batch_port.py: PURE helpers (flip_delinks_entry, suffix-aware existence check) + PortStrategy against the existing Ops; per-batch gate 'configure.py <region> && ninja sha1' (configure re-run already inside Ops.gate handles the single-build.ninja race); v1 scope = the 197 default-tier sim=1.0 candidates, then the 22 legacy-tier after the suffix-aware fixes land.

#### [UNVERIFIED] 26 orphaned never-activated ports; batch_carve's dedup is routing-suffix-blind

- **Payoff:** A · **Effort:** hours
- **Claim:** 13 identical function sets per region (all .legacy.c/.legacy_sp3.c, e.g. func_02032c74.legacy_sp3.c, func_0206e8cc.legacy.c) exist as committed port .c files that NEVER entered delinks.txt, and batch_carve later carved .s over the same ranges because its on-disk dedup checks only literal func_X.c/func_X.s — blind to routing suffixes (third confirmed instance of the trailing-suffix-blindness bug class after fastmatch --gap and batch_sha1's reverse-map).
- **Evidence:** Enumerated both regions: exactly 13 .c+.s stem-pairs each, identical sets, all legacy-tier, all in main. git log: the .c files landed 2026-05-16 (99329527, 'brief 110: cross-region apply wave 3', PR #512); git log -S shows 'src/usa/main/func_02032c74.legacy_sp3.c' NEVER appeared in config/usa/arm9/delinks.txt while its .s entry was added 2026-06-22 by auto-commit 7754b71b ('batch_carve: main .s +10 [auto, usa sha1 OK]'). Blind dedup sites: tools/batch_carve.py ~line 880 ('if not any(f"{f}{ext}" in existing for ext in (".s", ".c"))') and census_breakdown ~line 336 — func_02032c74.legacy_sp3.c != func_02032c74.c, so the guard misses.
- **Action:** Two-part task: (a) activate the 26 orphans — flip each delinks .s entry to the existing .c, rm the .s, gate per region (near-free ships, but MUST re-gate: they were never gated as .c); (b) fix both existence checks in batch_carve.py to glob func_X.* via routing_suffixes.split_routing_suffix so no future re-carve-over-port can happen.

#### [UNVERIFIED] Renamed-symbol blind spot: 153 EUR .c invisible to the lane; 14/region harvestable by stem

- **Payoff:** B · **Effort:** hours
- **Claim:** port_to_region.py v1 rejects every semantically-renamed EUR source (FILENAME_RE accepts only address-keyed stems; renames declared out of scope in the docstring), and my census inherits the same blind spot — but 14 of the 153 renamed EUR .c files have a same-stem .s twin already carved in USA (Copy32, CpuFastSet, Fill32, Halt, OS_DisableIrq, OS_RestoreIrq, IntrWait, HuffUnCompReadByCallback, ...), making them name-keyed free ports the 244-count excludes.
- **Evidence:** Counted at HEAD 2e7cec0a: 365 non-address-keyed EUR baseline .c = 212 data_*.c + 153 renamed function sources; same-stem scan against src/usa: 34 already ported, 14 still .s (sample: Bg_FillTileRect, Ov013_SlotToCell, SNDi_SetTrackParam, WaitByLoop). tools/port_to_region.py:127-130 FILENAME_RE requires (func|func_ovNNN|main|ovNNN)_<8hex>; lines 58-61 declare renamed symbols out of scope for v1.
- **Action:** Add a name-keyed path to the port lane: same-stem EUR .c ↔ target .s pairing, resolve the target address from the region's symbols.txt by symbol name, byte-verify via function_byte_similarity, then port with --output-path override (no FILENAME_RE change needed).

#### [UNVERIFIED] The 10 uncarved-target HIGH siblings enumerated: 9 ov004 thumb (known) + 1 new ov007 gap

- **Payoff:** B · **Effort:** hours
- **Claim:** Exactly 10 backlog-adjacent EUR .c files have a HIGH USA/JPN sibling that exists in the target's symbols.txt but has NO carved file at all (not .s, not .c) — porting them requires a target-side carve (new delinks entry) first: 9 are the ov004 .thumb.c cluster brief 674 already flagged (func_ov004_021dbe5c/021dbe68/021dbf30/021dcd1c/021dd584/021dd590/021de23c/021de250/021de264), plus one previously unflagged ARM gap: func_ov007_021b2280 → USA func_ov007_021b2180 (0x58 B).
- **Evidence:** Census no-file bucket: 11 entries; verified func_ov007_021b2180 is in config/usa/arm9/overlays/ov007/symbols.txt (kind:function(arm,size=0x58) addr:0x021b2180) with no file in src/usa/overlay007/ (listing starts at func_ov007_021b21d8.s); func_ov004_021dbc48 confirmed kind:thumb in ov004 symbols with no file. The 11th entry (func_0209153c → 0x02091454) is a census keying artifact of the 15-function name-vs-addr drift band in USA main — src/usa/main/func_0209153c.c actually exists (already ported); drift-band error is bounded at ≤15 functions and this was the only hit.
- **Action:** Fold the ov007_021b2180 carve into the already-flagged ov004 delinks-gap task (same fix shape: add the delinks .s entry in both target regions, then port); afterwards these 10 move into the normal port queue.

### Include / prototypes go-live

#### [VERIFIED] Bank cannot compile: 11 decls use BOOL/fx32/s32/s8, which game/types.h never defines

- **Payoff:** S · **Effort:** hours
- **Claim:** The first TU to #include game/prototypes.h will fail to compile regardless of pilot choice: the bank references BOOL/fx32/s32/s8, but the branch's types.h defines only u8/u16/u32. The u32 clash IS reconciled (branch diff drops nitro/types.h, adopts unsigned int), but the signed/fx/BOOL half of the type layer is missing.
- **Evidence:** git show origin/claude/q-prototypes-h-redo:include/game/types.h — typedefs u8/u16/u32 only, deliberately no nitro include. Bank (3,816 decls parsed) uses fx32 x16, s32 x5, s8 x1, BOOL x1 in exactly 11 declarations: lines 23 (CTRDG_IsOptionCartridge BOOL), 34, 54, 106, 231, 1559-1561, 1687-1688, 3834 of include/game/prototypes.h on the branch. Nothing under src/ includes any game/ header (grep 'game/' over src — only a false-positive comment 'endgame/' in src/overlay016/ov016_core.h:105), so the defect is latent and invisible to the byte-neutral sha1 gate. Src precedent for the missing spellings exists: C:/Users/leona/Dev/gx-spirit-caller/brain/src/main/SysWork_GetPointDistance.c lines 13-15 (typedef signed int s32; typedef signed long long s64; typedef s32 fx32). Full typedef census (src+include+libs): u32 'unsigned int' x83 files vs 'unsigned long' only at libs/nitro/include/nitro/types.h:20 (consumed by 72 TUs: 26 EUR = 23 main + ov016/017/019, 46 USA/JPN); s32 'signed int' x4 vs nitro 'signed long'; s16 'short' x3 vs nitro 'signed short' (same type, different spelling); u16/u8 single-spelling everywhere (527/78 files, no clash).
- **Action:** On the prototypes branch (before merge, or as an immediate follow-up), extend include/game/types.h with the int-based signed set matching src's own precedent: s8/s16/s32 (signed char/short/int), u64/s64 (unsigned/signed long long), BOOL (int), fx16 (s16), fx32 (s32), fx64 (s64). Byte-neutral by construction (include layer is dormant); gate with configure+ninja sha1 as usual.

#### [VERIFIED] 12 asm-void landmines: bank says 0-arg for functions whose real callers pass 3 args

- **Payoff:** A · **Effort:** hours
- **Claim:** gen_prototypes.py faithfully copies 'asm void f(void)' definition lines, so 67 of the 74 asm-bodied functions are banked as 0-arg — and 12 of them have real matched callers passing arguments; any such caller including the bank gets a hard 'too many arguments' compile error, and a new draft consulting the bank would model the callee's ABI wrongly.
- **Evidence:** Computed cross-reference: 74 asm-defined EUR functions (grep '^asm ' over EUR src), all 74 in the bank, 67 with params=[] in docs/research/data/prototypes-provenance.json. Of those 67, exactly 12 have callers declaring arity>0 externs: Copy32, CpuSet, Fill32, OS_RestoreIrq, WaitByLoop, func_0200093c, func_020536bc, func_0209448c, func_020944a4, func_020b41e8, func_ov002_021b4254, func_ov002_0226ae30. Concrete: src/main/Fill32.c:17 'asm void Fill32(void)' with the (value,dest,count) register convention documented in its own line-1 comment, vs caller src/main/func_02001d0c.c 'extern void Fill32(int value, void *dest, int size);'. 232 of my audit's 1,398 contradiction rows are purely this artifact.
- **Action:** Patch tools/gen_prototypes.py on the branch to detect 'asm'-qualified definitions and either skip them or emit them under a curated ABI-override table (the 12 names above, signatures taken from their own header comments/callers); re-run generator + tools/check_prototypes_provenance.py + tests/test_gen_prototypes.py.

#### [VERIFIED] Wrong-extern rate in matched TUs is 50.2% — match-time bank payoff now quantified

- **Payoff:** A · **Effort:** day
- **Claim:** Half of all hand-written inline externs referencing bank-covered functions contradict the callee's ground-truth signature (arity or return class), confirming the wrong-signature failure class seen in briefs 674/#1327 is systemic, not incidental; a bank lookup at draft time removes the whole class.
- **Evidence:** Scripted audit over EUR src: 2,784 inline externs reference bank functions; 813 wrong arity + 585 wrong return-class = 1,398 (50.2%) hard contradictions; excluding the 232 asm-void artifact rows leaves 1,166 genuine ones across 769 distinct matched TUs and 596 distinct callees (results JSON in my scratchpad, extern_audit_results.json). Documented first-try failures already root-caused to this class: docs/research/codegen-walls.md ~6259-6300 — func_020a724c (modeled void-no-arg, actually 2 forwarded ints) and func_ov004_021dbe68 (2-arg model, callee reads r2 for a 3rd arg) both matched byte-identical FIRST TRY once corrected; PR #1327's independent hand-check found 59/102 sampled sigs contradicted bodies (33 arity, 26 return). Coverage: the bank knows 1,046 of 2,096 distinct extern'd names in EUR src (49.9%). Note the contradictions were byte-neutral in these shipped TUs (they matched anyway) — the payoff is at NEW-draft time, where a wrong extern either misses a match or gets a candidate falsely parked as a reg-alloc wall.
- **Action:** Wire a bank lookup (read prototypes-provenance.json, not a #include) into the draft path: cmatch_loop.py's dossier/skeleton stage should emit externs from the bank when the callee is covered, and the reg-alloc parking checklist (codegen-walls.md action list) should cite 'bank disagrees with your extern' as an automatic pre-park check. Zero compile risk since it is a tooling lookup, shippable independently of header wiring.

#### [UNVERIFIED] Pilot TU found: src/main/func_0202e2c8.c, plus a verified 112-TU zero-conflict wave-1

- **Payoff:** S · **Effort:** day
- **Claim:** There is a provably safe first wiring pilot, and 112 EUR TUs where swapping inline externs for #include "game/prototypes.h" cannot introduce a signature change: every one of their fn externs is textually equal (normalized) to the bank's decl and their typedefs are a subset of {u8,u16,u32}.
- **Evidence:** Scripted audit (scratchpad extern_audit.py + candidate scan) over 4,229 EUR .c TUs: 112 TUs have no #include, typedefs only from the u8/u16/u32 set, and ALL fn externs exactly match the bank after normalization. Smallest: C:/Users/leona/Dev/gx-spirit-caller/brain/src/main/func_0202e2c8.c — delinks-complete (config/eur/arm9/delinks.txt:5636, .text 0x0202e2c8..0x0202e2e0 = 24 bytes), zero typedefs, single extern 'extern int func_0202de9c(void);' identical to bank line 666. Object path convention confirmed from build.ninja: build/<region>/src/<module>/<name>.o (e.g. line 3789 'build build\usa\src\usa\main\func_02026f24.o: mwcc'). -i include and -i libs\nitro\include are already on all 4 compiler tiers (build.ninja lines 20/28/36/44), so no configure.py change is needed to reach the header.
- **Action:** After the types.h fix: (1) python tools/configure.py eur; ninja build/eur/src/main/func_0202e2c8.o; save its sha1 as baseline. (2) Replace the TU's one extern line with #include "game/prototypes.h". (3) Rebuild that single .o and byte-compare (brief-533 protocol: per-file ninja + ELF compare catches divergence in seconds). (4) Full 3-region ninja sha1. Then batch the remaining 111 zero-conflict TUs the same way, one .o-diff each.

#### [UNVERIFIED] cmatch_loop.py injects nitro/types.h (long-u32) into every m2c skeleton — convention clash

- **Payoff:** A · **Effort:** hours
- **Claim:** The match-time pipeline is currently hard-coded to the OPPOSITE u32 convention from game/types.h: every m2c skeleton is prepended with #include <nitro/types.h>, so the first person who adds game/prototypes.h to a cmatch_loop draft gets an immediate mwcc typedef-redefinition error, and drafts shipped from skeletons keep growing the long-convention TU population.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/tools/cmatch_loop.py:363 — includes = ["#include <nitro/types.h>"] with a brief-620 comment justifying it (s32 not defined by any ovNNN_core.h). Current long-convention consumers: 26 EUR TUs (23 main + 1 each ov016/017/019) plus 46 USA/JPN mirrors; 14 of the 26 are themselves provenance sources for bank entries (their u32/fx32 spellings were re-homed under the int convention — ABI-identical on ARM32, byte-safe, consistent with the generator's void*/scalar normalization rules in tools/gen_prototypes.py header comments).
- **Action:** In the same change that first wires prototypes.h into the draft pipeline, switch cmatch_loop.py's skeleton include from <nitro/types.h> to "game/types.h" (after finding 1 lands, it will define the full s32/fx32 set the m2c output needs), and leave the 26 existing nitro-convention TUs untouched/quarantined.

#### [UNVERIFIED] No CI guard exists against typedef-source mixing — a pure-text pytest closes the gap

- **Payoff:** B · **Effort:** hours
- **Claim:** Nothing in tests/ or tools/ asserts that a TU never sees both u32 conventions (or a bank decl conflicting with a local extern), yet the queue item itself calls the clash 'a compile-time faceplant the moment the substrate goes live'; a text-only include-graph test is cheap and rides the existing pytest gate.
- **Evidence:** grep over tests/ and tools/ for nitro/types|game/types|typedef finds only incidental mentions (test_libs_nitro_d5.py:132 comment, test_port_external_source.py:499 fixture, cmatch_loop.py comment) — no conflict assertion. The gate already runs 'python -m pytest -q tests' (docs/queue/claude-scaffolder.md line 222, q-prototypes-h-redo entry), and the branch already ships a precedent-setting checker (tools/check_prototypes_provenance.py, exit 1 on drift) plus tests in tests/test_gen_prototypes.py — so a sibling guard fits the established pattern. All include-resolution facts needed are static: quoted includes resolve TU-dir-first, angle includes via -i include and -i libs\nitro\include (build.ninja lines 20-44).
- **Action:** Add tests/test_include_typedef_conflicts.py: transitively resolve #includes for every src/**/*.c against {TU dir, include/, libs/nitro/include}, then assert (a) no TU reaches both libs/nitro/include/nitro/types.h and include/game/types.h, (b) no TU that reaches include/game/prototypes.h also contains an inline extern for a name declared in the bank (C89 conflicting-declaration risk — my audit shows 919 TUs would trip this today, so the assert doubles as the wiring worklist), (c) no TU sees two different typedef spellings for the same primitive name.

### Wall-taxonomy hygiene

#### [VERIFIED] P-11 'budget zero matches' contradicted: 10 of 16 catalogued members already shipped

- **Payoff:** S · **Effort:** day
- **Claim:** P-11's park-on-sight verdict (and predict_walls.py's P-11 cue) is wrong for the majority of its own catalogued population, and the 3 remaining flagship clones (E-12/E-13/E-14) have an on-point, never-tried recipe: their #1 divergence is a duplicated same-value pool word, which is exactly what C-27's dual-extern + symbols.txt alias produces at the default tier.
- **Evidence:** codegen-walls.md P-11 (lines 7033-7226) catalogues 16 members: 5 cohort + 8 brief-254 census + 3 brief-258 census. Disk check at brain/integ HEAD 9f2acd00: shipped C exists for 10 — src/main/func_0200b0c8.c (commit 'Brief 516'), ALL 8 brief-254 members (func_02032efc.c/_02032f38.c/_02032e8c.c/_02032ec4.c via PR #760 2026-05-30; func_02054fd0.legacy_sp3.c/_02055000.legacy_sp3.c via PR #878 2026-06-06; func_ov002_02238bc8.c/_0226db7c.c via PR #808 2026-06-01 — all sha1-gated drain-wave PRs), plus func_ov002_02292020.c from the 'no clean lever' brief-258 trio. Still .s: func_02024574/020270d0/02028790 (E-12/13/14), func_ov011_021d2ca8, ov002_02200084, ov002_022319f4. E-12's orig has 2 identical data_0219a8ec pool words vs mwcc's 1 (doc lines 7088-7127); first-wave-wall-reg-alloc-plateau.md Part 3 lists only volatile + .legacy.c + inlining attempts — grep confirms zero mention of C-27/dual-extern/alias. tools/predict_walls.py still emits the P-11 'no recipe yet' cue (lines 45/602/723).
- **Action:** Re-scope P-11 from 'budget zero, skip-and-document' to a diagnostic checklist entry and strike the 10 shipped members from its affected-picks tables. Then attempt C-27's dual-extern recipe on func_02024574 (E-12) and zero-shot the clones func_020270d0/func_02028790 (identical 0x74 shape, different strides). Demote the predict_walls.py P-11 cue to 'diagnose, don't park'.

#### [VERIFIED] P-16's sole falsification used the exact pointer-cast idiom the typed-struct lever kills

- **Payoff:** A · **Effort:** hours
- **Claim:** P-16 (repeated global+K rematerialization after calls) is likely not a wall: its sole investigation compiled *(int*)(data_ov002_022d016c+0xd20)+=1 from extern char[] — the precise pointer-cast shape whose typed-struct-extern replacement just reversed 4+ of 6 'permanent' overlay verdicts — and the typed form's documented codegen (single bare-base pool word + [reg,#imm]) is byte-for-byte the shape orig has at all 3 divergent sites.
- **Evidence:** codegen-walls.md P-16 lines 7599-7657: miscoded form verbatim ('extern char data_ov002_022d016c[];' + cast-arithmetic increment) emits a FOLDED symbol+0xd20 own pool word + [r2,#0], while orig reloads the bare symbol + [r1,#0xd20] indexed; brief-582-c-ceiling-probe.md:84-91 confirms the same source text. cm-overlay-small-sweep-2026-07-24.md:55-64,89-94: '*(int*)(base+N) folds N into a second pool constant (wrong shape) where base->field keeps a single pool-loaded base register plus [reg,#imm]' — responsible for at least 4 of 6 wall reversals; the doc's own rule says any citation predating the typed-struct convention is unverified. src/overlay002/func_ov002_02269534.s still parked (548 B, 65.2%, all 12 divergent instructions attributed to this one pattern). The entry also names its second untested lever — routing the 3 sites through one shared tail — which is now the proven brief-655/672 shared-return convergence lever.
- **Action:** Draft func_ov002_02269534 with a typed struct extern for data_ov002_022d016c (field at 0xd20); expect the 3 increment sites to emit bare-base pool load + [reg,#0xd20]. If a residue remains from the 3 textual duplicates, apply brief-655 shared-return convergence. Retire or re-scope P-16 on the result; this also validates the 'recheck all pre-convention citations' rule against a P-index entry.

#### [UNVERIFIED] Suite made genuinely green, but every gate including the only live item still skips it

- **Payoff:** B · **Effort:** hours
- **Claim:** q-green-pytest's stated end state ('update the queue headers + gate3 guidance to drop the --no-tests advice') was never executed, so the regression-catching value of greening the suite is unrealized: pipeline bugs of exactly the class brief 675 hit (batch_sha1 false negatives, fastmatch crashes) remain invisible to agent gates.
- **Evidence:** Commit e24d4c9b 'test: make Windows suite genuinely green' merged 2026-07-23 (origin/main log). Yet 34 occurrences of --no-tests remain across docs/queue/*.md; the sole open [TODO] (cm-crossregion-ports, docs/queue/claude-decomper.md:142, written 2026-07-24 — after greening) still instructs 'gate3.py --scope all --no-tests'; briefs 675 and 676 (run 2026-07-24) both gated with --no-tests. tools/gate3.py:117-131 confirms pytest is a hard gate only when not skipped.
- **Action:** Edit the live cm-crossregion-ports gate line and all four queue headers to make full gate3 (with pytest) the default, reserving --no-tests for explicitly build-only briefs; add one line to the queue protocol header stating the suite is green as of e24d4c9b.

#### [UNVERIFIED] 82% of shipped C TUs are placeholder-named; the Codex lanes that fix this sit idle

- **Payoff:** B · **Effort:** day
- **Claim:** Naming-at-scale is still correctly below frontier work for the CC lanes, but the calculus changed on the Codex side: both Codex queues are fully drained, the proven mechanical naming lanes are all [DONE] with no successor, and the just-landed prototypes provenance JSON (3,816 evidence-backed signatures) makes a propagation pass cheaper than ever — while every one of the ~76 pending ships adds to the placeholder stock.
- **Evidence:** Counted on disk: find src -name 'func_*.c' = 8,865 vs 1,946 named .c files (82% placeholders); only 212 of the func_*.c contain asm, so ~8.6K are natural-C TUs counted by the readable-C headline. Queue states verified: codex-decomper 30/30 and codex-scaffolder 29/29 items [DONE], zero [TODO]; drained naming lanes = q-sig-refresh-2/3, q-name-crossprop-2/3, q-name-batch/-2, q-nitro-names (35 byte-verified SDK names). Substrate: q-prototypes-h-redo (origin/claude/q-prototypes-h-redo, pending merge) commits provenance JSON + self-checker with 0 mismatches.
- **Action:** Queue q-name-batch-3 for the idle Codex lanes plus a one-time 'name the top-N most-called func_* symbols using the prototypes provenance JSON as signature evidence' item, gated by the existing dsd check 3-region + scope_gate --kind naming pattern.

#### [UNVERIFIED] decomp.dev registration (#1020) fully built, blocked a month on ~30 min of owner action

- **Payoff:** B · **Effort:** hours
- **Claim:** The only externally-visible progress surface still missing is complete and rotting in an open PR: everything left is owner-only (build+push the private ROM image, grant pull, register at decomp.dev), it consumes zero agent capacity, and each passing toolchain change (objdiff v3 action, c99 flag hook) increases revalidation cost.
- **Evidence:** gh pr view 1020: OPEN, updatedAt 2026-06-24T10:20:05Z (30 days); branch origin/brain/decomp-dev-ci not an ancestor of origin/main (git merge-base check); diff = decomp-report.yml (135 lines) + Dockerfile (194) + docs/decomp-dev-setup.md owner checklist (250). PR body: 'merge only AFTER the image exists', steps 1-3 owner-only. Drift check run: all four configure.py flags the workflow uses (-w/--compiler/--no-extract/--dsd) still exist at tools/configure.py:59-64, so it has not rotted yet. Adjacent public-presentation work is already live (PR #1023 README revamp merged; origin/progress-visuals badge branch updated 2026-07-24).
- **Action:** Relay the 5-step docs/decomp-dev-setup.md checklist to the user this round (in its own copy-paste code block per user preference) with an explicit do-or-close decision; if do, merge #1020 after image push and register; if close, close the PR so the branch stops carrying silent revalidation debt.

#### [UNVERIFIED] Six shipped functions still cited as live walls in P-4's text and lever-payoff.md

- **Payoff:** A · **Effort:** hours
- **Claim:** The newest P-4 discriminator text and lever-payoff's top anti-pattern row cite functions as confirmed-permanent that are byte-matched C on disk: all 3 of brief-671's 'RE-CONFIRMED PERMANENT' calls were reversed within hours-to-days, and the discriminator's '3 range-check r2/r3 confirmations' is actually 2 — func_0207dab4 was shipped at 100% by brief 672 itself.
- **Evidence:** src/main/func_02084ac4.c shipped (commit adf23a9f, 2026-07-23 21:29, 'P-4 falsified') 33 minutes before brief 671 closed (22:02) claiming it 'RE-CONFIRMED PERMANENT' (codegen-walls.md:6469); the P-4 sub-shape table (line 6386) still lists func_02084a9c/_02084ac4 as wall examples though both .c files exist. Brief-671's two 'newly confirmed permanent Thumb instances' both shipped: src/overlay004/func_ov004_021dbe68.thumb.c (2026-07-24, 3-arg trampoline fix) and _021de264.thumb.c. codegen-walls.md:6350 lists func_0207dab4 among 3 parked r2/r3 confirmations, but brief-672-epilogue-resweep-2.md:49 ships it (src/main/func_0207dab4.legacy_sp3.c, commit 22:44 07-23; the actual parks were 0207d4dc at 88.2% and 0207db00 only). lever-payoff.md row 1 says 'Only func_020a71e4 remains an actual residue' — src/main/func_020a71e4.c shipped 07-23 22:02 by brief 671 itself (typed struct extern + 3 forwarded args). Verified true survivors, all still .s on disk: func_020b3850, func_0208b1ac (arity-verified), func_0207d4dc, func_0207db00.
- **Action:** Edit codegen-walls.md P-4 (sub-shape example table, the brief-671 re-test block, and the discriminator's example list) and lever-payoff.md row 1 down to the 4-survivor list, adding tombstones pointing at the ship commits so future sessions don't re-park fnptr-cache/trampoline shapes on the reversed verdicts.

#### [UNVERIFIED] P-2 is the oldest live wall and brief 671 already showed its symptom is tier-sensitive

- **Payoff:** A · **Effort:** hours
- **Claim:** P-2's founding instances (briefs 020-031) were never re-tested under any current convention, and its core claim ('C source can't sit on either side of the fusion threshold reliably') is already contradicted in one direction: .legacy.c routing removed the stmia over-fusion outright on func_0207e214.
- **Evidence:** codegen-walls.md P-2 (lines 6186-6216) cites only briefs 020/022/028/031 — the oldest evidence base in the live index; per-PR table lines 8484/8503 date func_0209bf18/func_0207dee0 to briefs 028/031 with zero later re-test. func_0209bf18.s and func_0207deb0.s still on disk; func_0207dee0 closed only via asm-void+nofralloc escape (src/main/func_0207dee0.c), not a C reversal. brief-671-wall-retire.md:72-79: on func_0207e214, .legacy.c routing made 'the stmia fusion disappear' (28.6%->42.9%), leaving an unrelated r1/r2 residue. brief-670-main-small-f.md:50-58 added the 2 over-fuse instances trying only field reordering + a local-struct intermediate — no tier matrix, no typed-struct field access.
- **Action:** Run the 3-tier matrix (.c/.legacy.c/.legacy_sp3.c per the refined epilogue-routing rule) plus typed-struct field access on func_0209bf18 and func_0207deb0. If either direction unfuses, re-scope P-2 to default-tier-only and add 'fusion mismatch -> tier-route before parking' to the sweep checklist.

#### [UNVERIFIED] Half of P-9's named candidates have shipped; the 36-candidate pool was never scrubbed

- **Payoff:** B · **Effort:** hours
- **Claim:** P-9's strict mask-form core (mvnNE+andNE+movEQ) still stands, but its candidate accounting is contaminated: 3 of 6 named candidates shipped — two via levers that postdate the 90-compile falsification (missed out-parameter signature + legacy_sp3 routing; C-29 polarity) — and the '~24 mask-form' population estimate mixes recoverable early-return forms with mask forms.
- **Evidence:** Shipped: src/main/func_02037b34.c (C-29), func_02033488.c (early-return form), func_02054c0c.legacy_sp3.c (brief-668 ship table line 36, 'call+bool-to-sentinel' — the real defect was a missed out-parameter, exactly brief 674's wrong-signature checklist item). Still .s: func_020534b4 (mask exemplar), func_02022540, func_02000d4c. The briefs 103/105 falsification matrix (6 variants x 15 SPs + permuter) predates the brief-676 branch-polarity-swap lever and brief-675 ||-guard-merge lever by ~570 briefs; brief 669 explicitly declined re-testing P-9 for lack of a new hypothesis — these levers are that hypothesis.
- **Action:** Mechanically re-scan the 36-candidate pool (discriminate mvnNE-at-end-of-cascade vs mvnEQ-early-return signatures; drop already-shipped members), then run one polarity-swap + shared-return attempt on func_020534b4 before reaffirming the mask-form population count in the entry.

#### [UNVERIFIED] epilogue-wall-corpus routing column 25% stale: 52/205 shipped rows say assembly-only

- **Payoff:** B · **Effort:** hours
- **Claim:** The census that produced the lane's best hit rates (68-70% across briefs 668/672/673) now overstates remaining runway by a third and would feed already-shipped picks to the next sweep.
- **Evidence:** Script over the corpus table at HEAD 9f2acd00 (docs/research/campaign-analytics/epilogue-wall-corpus.md): 52 of 205 rows have a same-stem C routing file on disk with the .s deleted, yet every row still reads 'assembly-only (.s)' and the counts block claims A 112 / B 93 all-assembly. The doc's own routing rule is mechanical (same-stem .c/.legacy.c/.legacy_sp3.c/.thumb.c existence), so regeneration is a pure re-run; real remaining corpus is 153 rows.
- **Action:** Regenerate the census table from disk with the same signature scan, moving shipped rows to a 'closed' section so the cumulative 45/64 attempt accounting survives; land it with the next corpus-lane brief so pick selection uses the 153-row live set.

#### [UNVERIFIED] ov002-wall-cohort 'do not sweep' verdict refuted 2 days later; ~890-candidate lane

- **Payoff:** S · **Effort:** days
- **Claim:** The campaign-analytics authority doc (brief 654, 2026-07-22) classifies all 2,719 brief-294-stamped ov002 .s as a verified reg-alloc-wall cohort and says 'Keep as .s; do not schedule a blind C sweep' — but the 07-24 overlay sweep shipped 43 ov002 functions FROM that exact cohort at ~54% hit rate, leaving ~890 unattempted ov002 ≤160B candidates projected at 360-480 EUR ships (~+1-2pt readable-C), the largest quantified lane in the project.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/docs/research/campaign-analytics/ov002-wall-cohort.md rows: 2,719 files 'brief-294 reg-alloc wall (P-16-class)... do not schedule a blind C sweep'. Merge b80caafe stat: 64 overlay .s deleted, 43 in src/overlay002. Verified `git show origin/main:src/overlay002/func_ov002_021aff4c.s` (shipped by the sweep) carries the exact counted evidence string 'reg-alloc-walled functions with no C match (brief 294 endgame)'. Sweep doc (docs/research/cm-overlay-small-sweep-2026-07-24.md): 64/118 = 54%, ov002 = 971 of 1,010 non-main small candidates; 43/0.54 ≈ 80 attempted → ~890 remain.
- **Action:** Two-line annotation at the top of docs/research/campaign-analytics/ov002-wall-cohort.md (and its cross-refs in codegen-walls-audit.md/endgame-ledger.md) recording the 07-24 contradiction, then queue repeated ov002 ≤160B sweep batches (~24 candidates/agent, the sweep's exact 5-worktree playbook) until hit rate decays below ~20%.

#### [UNVERIFIED] 9 named 'field-pool' parks with already-cracked siblings: one-batch re-audit

- **Payoff:** A · **Effort:** day
- **Claim:** Nine specific pre-convention parks carry the exact pool-constant-folding wall signature the typed-struct lever reverses, and every one has a documented class-sibling that has since shipped: ov011 021ca630+021ccf3c (trio-mate 021ca600 cracked by sweep batch 4 via the struct convention), ov011 021cfa50, ov004 021cc2a4+021d6ed0+021d641c (co-members 021d0afc/021cb518 shipped 2026-07-04), ov017 021b768c ('catalogued, not re-tested' since brief 340), ov002 02269534 (P-16's sole affected pick — the typed struct for its exact symbol is already banked), ov000 021aa840.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/docs/research/brief-308-ov011-clean-c-wave1.md HALT section: tried only 3 pointer phrasings ('&= RMW, int*p+scaled index, char-cast') — typed struct member never tried; 1 of its 3 (021ca600.c, committed 07-24 batch 4) since cracked. src/overlay004/ov004_core.h:88-89 field-pool citations '== ov011 021d0afc wall' — 021d0afc.c and 021cb518.c shipped 07-04. P-16 entry (docs/research/codegen-walls.md:7583+) shows the miscoded form verbatim as `extern char data_ov002_022d016c[]; *(int*)(data_ov002_022d016c+0xd20) += 1` and its 'not yet tried' list omits typed-struct; struct Ov002D016c with f_d20/f_d2c exists at src/overlay002/ov002_core.h:239-270, extern at :88. All 9 .s confirmed on disk; sizes 44-168B except 02269534 (548B). brief-362 re-tested 021cc2a4 with a kit that did NOT include the typed-struct lever. None of the 9 appear in the sweep doc or queue docs as re-attempted.
- **Action:** Single decomper batch: for each of the 9, declare/extend a typed struct extern (tools/build_struct_bank.py mines field layouts automatically), redraft with base->field access, fastmatch resolved-mode, 3-region gate; update the P-16 entry in codegen-walls.md and the ov011/ov004/ov017 core.h wall notes with outcomes either way.

#### [UNVERIFIED] Census: 99 still-parked core.h wall citations, all pre-convention; 36% already reversed

- **Payoff:** A · **Effort:** days
- **Claim:** All 21 canonical ov*_core.h wall/park sections predate the typed-struct convention (they cite briefs 304-403, June 2026), and their historical reversal base rate is already 36% without any dedicated re-audit — 78 of 216 wall-context citations are now .c, 99 remain .s and constitute the re-audit worklist, with an expected 25-40 further EUR ships at the observed 25-54% re-test hit rates.
- **Evidence:** Mechanical census I ran (addresses within ±3 lines of WALL/DEFERRED/near-miss/unsteerable keywords in C:/Users/leona/Dev/gx-spirit-caller/brain/src/overlay*/ov*_core.h, checked against on-disk .s/.c): 216 total → 99 still .s, 78 now .c, 39 unresolved. Ledger corroboration: brief-568's cmatch-parked-and-floor.md (07-13) rows — 27/29 still parked but both reversals happened incidentally within days (func_02078ebc 'volatile-load destination wall' shipped 07-20; ov006 021b79ac shipped); brief-340's catalogued-miss list has 2 of 5 classes with shipped members (ov019 021b4c30 cracked by sweep; ov017 021b2c8c 'no named locals exist to steer' shipped 07-04). Convention timeline: lever-payoff.md lever #4 cites brief 579 (07-15); brief 609 (07-18) formalized the struct bank; core.h wall notes all predate both.
- **Action:** Queue a wall-citation resweep item over the 99 still-.s citations (list generated by my census script) with the modern kit: typed-struct extern first on any candidate whose orig .s shows bare-symbol pool-load + [reg,#imm] while the note mentions pool/base folding, then arity-check, polarity swap, declaration order, corrected sp3-routing. Park-on-sight only for pure scheduling/register-choice diagnoses made WITH the modern kit.

#### [UNVERIFIED] Stale-citation annotator: 36-69% of catalogued wall addresses are already shipped

- **Payoff:** B · **Effort:** hours
- **Claim:** Every re-audit attempt so far has burned most of its budget discovering that catalogued wall addresses were already shipped (brief 362: 155/225 = 69% stale; my census: 78/216 shipped + 39 unresolved), and one documented failure mode (m2c_feed returning disasm for already-carved functions) made an already-shipped function masquerade as an uncarved wall — a small status-mapping script would make every future re-audit start from live truth.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/docs/research/brief-362-backlog-resweep-pilot.md: 'The catalog is ~69% STALE... Always confirm a candidate is genuinely uncarved... before counting it' (the 021b2280 masquerade). My census reproduces the rate today: 78/216 core.h wall-context citations shipped, 39 unresolved (renamed/thumb-suffixed/data symbols my naive path-mapping missed — exactly what a real tool must resolve). The batch_sha1/fastmatch suffix-mapping bugs flagged in brief 675 compound the same address→file mapping fragility.
- **Action:** Write tools/wall_citation_status.py: extract every 8-hex address from core.h WALL blocks + campaign-analytics ledgers (cmatch-parked-and-floor.md, ov002-wall-cohort.md, epilogue-wall-corpus.md), resolve to current src path handling .thumb.c/.legacy*.c suffixes and renames via symbols.txt, emit live worklist (still-.s) + stale list (shipped) with citing doc and brief number. Feeds the resweep directly.

#### [UNVERIFIED] Reversal lever sat in-repo 7 weeks; core.h recipes don't reach re-audit kits

- **Payoff:** B · **Effort:** hours
- **Claim:** The typed-struct/offset-member lever was banked in ov000_core.h on 2026-06-03 (brief 319 recipe #1, verbatim: pointer-cast 'folds N into the pool constant'), yet the two dedicated backlog re-audit pilots run the SAME WEEK (briefs 340 and 362, both dated 06-06) omitted it from their lever kits and re-walled field-pool candidates it would likely have cracked — kit composition for any future re-audit must be drawn from all core.h recipe banks plus lever-payoff.md, not the sponsoring overlay's notes.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/src/overlay000/ov000_core.h:82-87 (brief 319, git-dated 2026-06-03): 'Offset-member, not pointer-cast... folds N into the pool constant'. brief-362 (06-06) lists its full kit as 'tri-compile, (u8) cast, dispatch-order inversion, pass-through' — no typed-struct — and its 021cc2a4 field-pool re-test failed; brief-340 (06-06) left 021b768c 'catalogued, not re-tested'. Global lever-payoff.md only gained the lever as #4 via brief 579 (07-15), with evidence count still '3/0' — not yet updated with the sweep's 6 reversals or the 78 shipped citations.
- **Action:** Update docs/research/reshape-recipes/lever-payoff.md lever #4: raise evidence to include the sweep's 6 documented-wall reversals and mark it 'try FIRST on any pool-word or base-offset diff before parking'; add one line to the re-audit queue template requiring the kit be assembled from lever-payoff.md + all core.h VERIFIED recipe sections.

### Metrics truth & the readable-C endgame

#### [VERIFIED] Ceiling model self-contradicts: 48.03% vs r7-14's 14-24% band; unfit as a done anchor

- **Payoff:** S · **Effort:** hours
- **Claim:** progress.py's tractable-C ceiling (EUR 48.03% of .text, attainment 18.70%) rests on a one-month rate extrapolation (main headroom 0.10 -> 0.75, commit f8e1dad5, 2026-07-24) that sits 2-3.4x above the r7 swarm's independently derived 14-24% band; any done-target defined as attainment=100% moved 18.5pp of .text in a single estimate commit and can move again.
- **Evidence:** Live run `python tools/progress.py --version eur --by-module` on brain/integ (91997ac4): TOTAL ceiling 1,145,881/2,385,948 B = 48.03%, attainment 18.70%. docs/research/brief-615-honest-metric.md line 77: the same tool printed 29.56% before the change. docs/research/campaign-analytics/ceiling-measured.md: main 10%->75% justified by '+33,832 C-bytes ~ 49% of old headroom in one month' — a rate measured while sweeps deliberately cherry-picked the easiest size tiers. Contrast committed per-candidate rates: brief 661 true random sample of main's unknown tranche = 3/35 shipped (8.6%); brief 675 <=128B easiest cell = 9/30 on-check (30%). progress.py's printed footer still cites the 14-24% band as a mere 'c.f.'.
- **Action:** Re-derive main's headroom stratified by size tier from committed brief data (661/673/675/676 ship rates weighted by tranche byte counts in the <=128B / 129-256B / 257B+ cells) and record the basis in ceiling-measured.md; until that lands, document attainment as a trend indicator, not a completion criterion, in state.md's metric block.

#### [VERIFIED] Proposed 'readable-C done': pret-style public ladder outside, verdict-complete gate inside

- **Payoff:** S · **Effort:** day
- **Claim:** Three candidate definitions were evaluated; the implementable-today answer is (a) a pret-style 3-tier public ladder — D1 byte-matched (ALREADY DONE: 3-region sha1, 99.44% EUR code matched), D2 decompiled-modulo-documented-floor, D3 documented/named — plus (c) an internal 'verdict-complete' gate: every .text byte is natural-C OR on the ISA-inexpressible floor OR carries a per-function wall verdict dated after the typed-struct-extern convention. Candidate (b), attainment=100% of the tractable ceiling, should be rejected as 'done' until the ceiling is measured per-tier (see finding 1).
- **Evidence:** D1 evidence: live progress.py shows 99.44% code matched, units 10,335/10,335; CLAUDE.md documents sha1 as the project gate. D2 floor is already documented: endgame-ledger.md's ~42 EUR mcr/mrc/swi/msr .s files genuinely inexpressible in C (cited in progress.py lines 639-641). Verdict-freshness is load-bearing: the overlay small sweep reversed SIX pre-convention 'permanent' wall verdicts (briefs 356/358 era), and state.md records the honest pool at 6,093 candidates / 32 confirmed-permanent — so a done-gate must timestamp verdicts. Peer norms verified by web survey: pret/pokeemerald = byte-matching build + full C source ('decompiled'), with documentation an explicitly separate ongoing tier (https://github.com/pret/pokeemerald); 'shiftable' (rebuilds with moved addresses, no frozen pointers) is the ecosystem's structural-doneness property (https://github.com/laqieer/fireemblem8j). Pros/cons: (a) community-legible but needs the wall registry as an exception list peers never had (mwcc byte-exact reg-alloc walls are real here, ~2,750 verified in ov002 alone); (c) falsifiable, no estimated denominator, mechanically auditable from committed data, but requires per-function investigation of the ~6,093-candidate pool; (b) already implemented and per-module, but its denominator is an estimate that moved 18.5pp in one commit.
- **Action:** Add a 'Definitions of done' section to docs/state.md declaring D1 achieved, D2 = verdict-complete (with verdict-date >= typed-struct-convention requirement), D3 = naming; add a bytes-with-fresh-verdict / total-.text counter to progress.py --by-module so D2 coverage becomes the tracked endgame number.

#### [VERIFIED] Current honest dashboard (delinks tier, draft-free): EUR 8.83% natural + 0.14% asm = 8.98%

- **Payoff:** A · **Effort:** hours
- **Claim:** The post-merge integ tree already exceeds the coordinator's ~8.8% EUR estimate — natural-C ALONE is 8.83%, total C-decompiled 8.98%; committed state.md (8.591%, 2026-07-23) is one merge-round stale; USA and JPN are each 7.41% natural / 7.44% total. Data lane: typed-array 1.69%, named-struct 0.15%, named-data 0.02%. These delinks-tier numbers are safe to publish without a build because the numerator contains zero draft inflation.
- **Evidence:** Ran `python tools/progress.py --version eur|usa|jpn` on brain/integ HEAD 91997ac4 (all 6 pending branches merged): EUR natural 210,786 / asm 3,456 / .text 2,385,948; USA 176,680+832 / 2,384,964; JPN 176,680+840. build/eur|usa|jpn/report.json verified absent (higher-fidelity tier unavailable, correctly marked stale-unknown). Draft-inflation check: c_code_bytes_by_class with require_complete=True equals the default — 214,242 B both ways, delta 0. Cross-check: named-struct 7,092 B = canary 4,932 + inference-probe 2,160 exactly, confirming the tier tracks the two shipped retype PRs byte-for-byte.
- **Action:** When the coordinator finishes merging to main, paste this split (with 'delinks-tier, draft-free verified' provenance) into state.md's metric block; defer report.json regeneration to the next build that happens anyway.

#### [VERIFIED] q-natural-c-metric LANDED end-to-end; last asm-inflated surface is the README SVG bar

- **Payoff:** B · **Effort:** hours
- **Claim:** The asm-void split shipped everywhere in the metrics chain (queue item [DONE], progress.py classifier, state.md block, per-PR asm_void_counter, PR template section), but the public progress-combined.svg still sums asm-C into its 'decompiled to C' green bar and label, and all 4 queue headers still describe the split as 'incoming'.
- **Evidence:** docs/queue/codex-scaffolder.md:116 '### q-natural-c-metric ... [DONE]' (and q-asm-void-counter [DONE] at :143); tools/progress.py c_code_bytes_by_class (commit 53dadabb); tools/asm_void_counter.py wired into .github/pull_request_template.md:65; state.md lines 14-16 carry the split. Counter-evidence: tools/generate_progress_bars.py region_metrics computes c = sum(c_split.values()) and renders that as the green bar + 'C n%' text; published SVG on origin/progress-visuals verified to read 'C 8.8% / matched 99%'. All 4 docs/queue/*.md headers (line ~11) still say 'metric split incoming, q-natural-c-metric'.
- **Action:** In generate_progress_bars.py, switch c_pct's numerator to c_split['natural-c'] (or draw a thin amber asm-C slice with its own legend entry) and update the 4 stale header lines; the next push to main auto-republishes the SVG.

#### [UNVERIFIED] Named-struct tier fits the bars SVG in ~25 lines; visuals-branch mechanics verified

- **Payoff:** B · **Effort:** hours
- **Claim:** The visuals pipeline regenerates build-free from committed delinks on every relevant main push and force-pushes main+1-commit to the unprotected progress-visuals branch (README hotlinks raw URLs), so adding data-readability rows is a generator-only change with no workflow edits; the branch is fresh (2026-07-24 14:46 UTC) but transiently carries the retracted #1327 prototypes bank until the next push — harmless since only assets/*.svg is referenced.
- **Evidence:** .github/workflows/progress-badge.yml: triggers on config/**/delinks.txt + tools/progress.py + generators, regenerates via generate_progress_bars.py + generate_heatmap.py, then `git push --force origin HEAD:refs/heads/progress-visuals`; README.md lines 25/36-38 reference raw.githubusercontent.com/.../progress-visuals/assets/*.svg only. git diff origin/main..origin/progress-visuals: 4 SVGs + include/game/prototypes.h +2,046 lines — verified to be the reverted 'Canonical game-function prototype bank' (main reverted it in 7b5da654); merge-base 02d81661 is a main ancestor, so the next force-push self-heals. Cheapness: generate_progress_bars.py is a 160-line single-file emitter already importing c_code_bytes_by_class/c_code_total_bytes from progress.py; summarize_data_readability() is equally importable. Caveat for the SVG: named-struct classification is per-TU first-match (progress.py line 506 uses .search once), so a future multi-array TU counts ALL its data bytes on one named-struct hit — exact today (7,092 B reconciles), coarse later.
- **Action:** Extend generate_progress_bars.py with one per-region data row (typed-array and named-struct slices on the existing 0-100% axis) calling summarize_data_readability; no change to progress-badge.yml needed.

#### [UNVERIFIED] Documented tier at literal zero for game data: 2/10,901 named symbols, both infra

- **Payoff:** B · **Effort:** hours
- **Claim:** If 'readable-C done' includes pret's documentation/naming tier (D3 in finding 2), that lane has not started: the only named data symbols region-wide are BuildInfo and OSi_IrqCallbackTable, and even the canary/inference retyped struct arrays kept their data_* placeholder names — so retyping as currently practiced moves typed-array/named-struct but leaves named-data pinned at 0.02% forever.
- **Evidence:** Replicated progress.py's named-data walk over config/eur/**/symbols.txt: exactly 2 qualifying symbols of 10,901 kind:data lines — BuildInfo (arm9) and OSi_IrqCallbackTable (dtcm); live progress.py prints 'Named-data: 2 / 10901 (0.02%)'. The recon snapshot confirms the inference-probe retypes deliberately used placeholder field names; the two shipped retype PRs (CharParam/PresentCard + the 2 inference blobs) added 7,092 named-struct bytes yet zero named-data symbols.
- **Action:** Add one line to the data-lane convention (docs/research/data/struct-array-initializer-mwcc.md and the queue headers): a retype PR that establishes semantics (oracle-backed, not shape-only) must also rename the symbol in config/<ver>/**/symbols.txt in the same PR; shape-only retypes keep placeholders by design.

### Throughput with 2 agent seats

#### [VERIFIED] Standardize worktree-parallel sweeps: proven ~7x per merge slot, failure modes spec'd

- **Payoff:** S · **Effort:** day
- **Claim:** The 5-way worktree-isolated sweep pattern (disjoint candidate batches, per-batch branch commits, plain `git merge --no-ff` consolidation, ONE consolidated 3-region gate) is reproducible and should be the standard for sweep-class queue items; its real failure modes are provisioning, index.lock contention, and background-sha1 stalls — all specifiable, none fundamental. On Windows it scales BETTER than macOS because the link lock is a documented no-op.
- **Evidence:** docs/research/cm-overlay-small-sweep-2026-07-24.md lines 150-167 + merge commits a1451290..20105d28 on origin/claude/cm-overlay-small-sweep: 64/118 shipped, zero conflicts across 5 batches independently editing ov002's delinks.txt. Failure modes verified: (a) fresh worktrees lack gitignored assets — measured orig/ 385MB, tools/mwccarm 87MB, dsd.exe, build/ 309MB (cold rebuild per worktree); configure.py fails loudly on missing baserom. (b) tools/prune_worktrees.py docstring documents .git/index.lock contention when dispatching many worktree subagents at once (claude-code#55724) plus the 'silently failed commit still looks clean and gets swept' hazard. (c) tools/wine_link_lock.py lines 31-44: fcntl=None on win32, lock is a no-op — native mwld has no serialization, so parallel worktree builds don't queue on Windows (they do on macOS). (d) Sweep doc lines 159-167: 2 of 5 batch agents stalled mid-report on a background whole-ROM `ninja sha1` that was redundant — fastmatch resolved-mode was the load-bearing per-function check. (e) tools/integrate.py's delinks conflict policy is keep-ours — correct for cross-item integration but would silently drop a batch's .s→.c flips if used for intra-sweep consolidation (failure is loud at ninja, but wasteful); the sweep correctly used plain git merge.
- **Action:** Write the protocol into the queue preambles + a provisioning helper: (1) per-worktree setup = NTFS junction (or copy) of orig/ and tools/mwccarm + own configure/build (never share build/); (2) stagger worktree creation and verify each batch branch tip contains its expected commit before merging or pruning; (3) forbid background whole-ROM sha1 inside batch agents — fastmatch resolved per function, one consolidated gate3 at the end; (4) consolidate batches with plain `git merge --no-ff`, reserving integrate.py for cross-item rounds. Prereq: the gate3 dsd.exe fix (separate finding) — every FRESH Windows worktree currently false-fails the gate.

#### [VERIFIED] gate3.py probes ./dsd not dsd.exe — false GATE FAIL in every fresh Windows worktree

- **Payoff:** A · **Effort:** hours
- **Claim:** check_dsd_binary() probes `ROOT / "dsd"` while the build system's own convention is `dsd.exe` on Windows, so any build-scoped gate3 run in a worktree provisioned by the normal ninja download rule exits 2 at preflight before building anything — this lands exactly on the worktree-parallel protocol, since long-lived worktrees may carry a legacy extensionless `dsd` PE (Windows CreateProcess runs explicit paths regardless of extension) but fresh ones never will.
- **Evidence:** tools/gate3.py line 67 `dsd = ROOT / "dsd"`; verified on this machine: `Path('dsd').exists()` is False, `dsd.exe` exists (PE32+ x86-64 via `file`). tools/configure.py line 243 does it right: `DSD = './dsd' + EXE` with EXE from get_platform.py ('.exe' on Windows), and the ninja download rule outputs that platform-suffixed path. The preflight (commit e754864e, 2026-07-19) is in origin/main; check_dsd_binary()==False makes main() return 2 with 'GATE FAIL' at gate3.py lines 198-204, matching the coordinator's observed elapsed=0s failure.
- **Action:** 2-line fix in tools/gate3.py check_dsd_binary(): `suffix = '.exe' if sys.platform == 'win32' else ''` and `dsd = ROOT / f'dsd{suffix}'` (or import get_platform like configure.py does); also append the suffix in the two error-message download hints (`--path ./dsd{suffix}`) so the suggested repair command produces the file the check looks for. Add a trivial test asserting the probed filename matches configure.DSD's basename.

#### [VERIFIED] batch_sha1.py: suffix-strip gap aborts whole batch; bisect false-negatives on missing .s

- **Payoff:** A · **Effort:** hours
- **Claim:** Two confirmed defects: (1) `_c_to_s_rel` does a naive `c_rel[:-2] + '.s'`, producing `func_X.thumb.s` for tier-routed candidates when the real delinks TU header is `func_X.s` — and because infra errors abort the ENTIRE invocation (exit 2), one `.thumb.c`/`.legacy*.c` candidate blocks every plain-.c candidate batched with it; (2) `_run_sha1` returns only an exit code, so when a candidate's .s revert target no longer exists on disk, every bisect step's ninja failure (missing input) is indistinguishable from a hash mismatch and the algorithm reverts the whole batch as culprits — brief 675's false '0 confirmed'.
- **Evidence:** tools/batch_sha1.py lines 79-83 (`_c_to_s_rel`), 360-363 (infra_errors → return 2 for the whole batch), 215-222 (`_run_sha1` exit-code-only), 229-292 (bisect treats any rc!=0 as culprit evidence). Verified naming on disk: config/eur/arm9/overlays/ov004/delinks.txt has tier-suffixed C headers (`src/overlay004/func_ov004_021dbd08.thumb.c:`) while .s headers carry no infix (`src/overlay004/func_ov004_021c9dd4.s:`). tools/routing_suffixes.py exists specifically as 'the ONE shared source of truth' for this suffix bug family (brief 587) with a `split_routing_suffix()` helper — batch_sha1.py simply never imports it. The missing-.s scenario is structural: cmatch_loop's accept path deliberately leaves the .s deleted with delinks still .s-routed until batch_sha1 flips it (cmatch_loop.py lines 746-758), so bisect's revert can always point at a deleted file. Brief 675's queue entry confirms both were hit live.
- **Action:** Three fixes in tools/batch_sha1.py: (1) rewrite `_c_to_s_rel` to strip the routing suffix via routing_suffixes.split_routing_suffix (import with the same try/except fallback fix_delink_suffixes.py uses); (2) at candidate resolution, require `(ROOT / s_rel).is_file()` and fail exit-2 with a named 'revert target missing — bisect impossible' error BEFORE applying any flips; (3) capture ninja output in `_run_sha1` and abort the bisect (exit 2, restore all) when the failure is a build error (`ninja: error:` / missing input) rather than a sha1 comparison mismatch. Add unit tests with a fake delinks tree covering .thumb.c/.legacy.c/.legacy_sp3.c and a deleted-.s candidate.

#### [VERIFIED] cmatch_loop --keep-drafts recreates the fatal .c+.s 'multiple rules' ninja state

- **Payoff:** B · **Effort:** hours
- **Claim:** For an already-.s-matched candidate that fails to match, the --keep-drafts path keeps the draft .c in src/ AND restores the sibling .s AND re-runs configure — creating the .c/.s pair that configure.py turns into `ninja: error: multiple rules generate build/.../X.o`, a state the same file's displace_sibling_s docstring documents as a hard, non-hypothetical failure. Every non-accept in a --keep-drafts run leaves the module unbuildable until the draft is manually removed.
- **Evidence:** tools/cmatch_loop.py: docstring lines 448-459 ('configure.py adds an unconditional ninja build rule for EVERY .c and EVERY .s... hit on the very first real ov008 c-match attempt, all 3 smoke-test candidates') versus the keep_drafts non-accept path lines 731-745, which skips unstage_source when keep_drafts is set, then unconditionally calls restore_sibling_s + _reconfigure with the comment that the draft sits 'alongside it purely for inspection'. The two blocks directly contradict; the coordinator's recon confirms the resulting 'multiple rules generate the same output' death on the next delink/configure.
- **Action:** In the s_rel (TemporaryGap) branch of process_candidate, when keep_drafts and classification != accept: move the draft OUT of the build graph before restoring the .s — write it to a non-compiled location (e.g. build/<region>/cmatch_drafts/<func>.c, or rename to <c_path>.draft) and record that path in the run report/dossier. Leave the in-src keep behavior only in the gap-object branch where no .s sibling exists. Add a test: keep_drafts + forced non-accept on an .s-routed candidate must leave `configure.py && ninja -n` runnable.

#### [UNVERIFIED] Queue preambles teach the superseded one-signal sp3 routing rule (6 mis-routes in a batch)

- **Payoff:** A · **Effort:** hours
- **Claim:** The scaffolder and decomper preambles say 'fused pop {…, pc} → *.legacy_sp3.c' with no prologue condition, but the overlay sweep proved fused-pop alone is insufficient — the sp3 tier additionally requires a prologue `sub sp, #4`, and correcting exactly this mis-route produced 6 of batch 2's 10 ships. The authoritative doc (sp3-routing-decision.md) has carried the two-signal rule all along; only the preamble abbreviation is wrong, and it is the text every future sweep agent reads first.
- **Evidence:** docs/queue/claude-decomper.md line 13 and docs/queue/claude-scaffolder.md preamble both read 'fused `pop {..., pc}` → `*.legacy_sp3.c`; otherwise plain `.c`'. docs/research/sp3-routing-decision.md lines 29-38 defines sp3 as the COMBINATION 'push WITHOUT r3 + sub sp, #4 + pop into pc'. docs/research/cm-overlay-small-sweep-2026-07-24.md lines 72-85: fused-pop WITHOUT the `sub sp,#4` prologue routes to default 2.0/sp1p5, and this single correction fixed 6 mis-routes contributing 6 of batch 2's 10 ships (candidates failing in .legacy_sp3.c that matched at default tier).
- **Action:** One-line edit in each of docs/queue/claude-decomper.md and docs/queue/claude-scaffolder.md: change the routing bullet to 'fused pop {…, pc} AND prologue `sub sp,#4` → *.legacy_sp3.c; fused pop WITHOUT that prologue → plain .c (default tier)', citing cm-overlay-small-sweep-2026-07-24.md. Optionally append the same refinement note to sp3-routing-decision.md's quick-reference table.

#### [UNVERIFIED] Size sweep items at ~100+ candidates — ships per merge slot is the binding metric

- **Payoff:** A · **Effort:** hours
- **Claim:** With 2 CC seats and the coordinator's merge round as the measured bottleneck, ships/day ≈ (ships per merged branch) × (merge rounds the brain completes per day). The overlay sweep delivered 64 ships in one merge slot versus an 8.6-ship mean for the last five single-agent items — consolidation is worth ~7x per slot, while integrate.py (now merged) attacks the slots/day term. Small tooling/doc items should stay separate; sweep items should be sized so one queue item = one 100-120-candidate multi-batch sweep with per-batch commits preserved for revert granularity.
- **Evidence:** Ships per merged branch, measured from queue entries and branch stats: cm-overlay-small-sweep 64; brief 673 = 17, 672 = 13, 675 = 9 (origin/claude/main-128-drain-cc, one ship commit, 18-line delinks diffs x3 regions), 676 = 3 (origin/claude/small-resweep-upper-cc), 674 = 1 → mean 8.6. Six branches are pending merge right now carrying ~76 ships across 6 slots. docs/research/rnd-swarm-2026-07-24-r9-rate.md: brain hand-drives ~10-15 serial merges per round, 86 hand-merge commits + ~40% bookkeeping in last-300 commits, ~8h idle overnight ≈ 25-35 lost ships/night — the brain, not agent seats, is the bottleneck. Fixed per-item costs (3-region gate3, PR, queue bookkeeping, research writeup) are the same for a 3-ship item as a 64-ship item.
- **Action:** Amend the queue preamble's 'ONE PR per item' protocol with a sizing rule: sweep-class items target 100-120 candidates, executed as 4-6 disjoint worktree batches consolidated on one branch (per-batch commits kept), one consolidated gate3, one PR; tooling/doc fixes remain individual small items. Keep the one-branch-per-queue-item rule (feedback_one-branch-per-queue-item) — this changes item SIZE, not branch discipline.

### External & GitHub research

#### [VERIFIED] Steal khdays-decomp's CC0 toolkit: declperm.py + audit_callsite_arity.py

- **Payoff:** S · **Effort:** day
- **Claim:** A new NDS matching decomp (Yokimitsuro/khdays-decomp, created 2026-06-26, CC0-1.0, 36.01% of 4.7MB code bytes matched in 4 weeks) has small license-clean tools that mechanize our two most expensive manual diagnostics: brute-force declaration-order permutation for register-permutation residues, and call-site-arity-vs-corpus-consensus auditing for the wrong-arity wall class from brief 674.
- **Evidence:** Read tools/declperm.py in full (95 lines, saved to scratchpad): splits `int x = 0;` into bare decl + assignment, permutes only the decl block (24-120 orders for 4-5 locals), compiles and reloc-masked-diffs each in seconds; closed their func_ov008_02069954 at permutation 33/120. Read audit_callsite_arity.py docstring: an unprototyped `extern int VEC_Mag();` called with 4 args vs corpus-consensus 1 produced 3 phantom live values whose residue surfaced ~0x99 bytes later looking exactly like a reg-alloc tie — cost them ~75 experiments; the tool keys on CALL SITE arity, covering named symbols our callee-body check (brief 674) reads one at a time. License API: `gh api repos/Yokimitsuro/khdays-decomp/license` → CC0-1.0. Their tools/ has 29+ scripts incl. allcc/allopt/census_coloring/dedupprop.
- **Action:** Port the declperm concept onto tools/fastmatch.py + the existing post_edit.py per-TU ninja compile (plain Python, Windows-native — sidesteps the parked decomp-permuter lane), and add a callsite-arity consensus audit over src/ as a pre-park diagnostic step in the reg-alloc flow (extends the brief-674 discriminator from read-one-callee to scan-all-callsites). CC0 permits direct adaptation.

#### [VERIFIED] sm64ds catalogue: notes frozen at our ingest, but 100+ commits/37h carry new levers

- **Payoff:** A · **Effort:** hours
- **Claim:** docs/research/reshape-recipes/imported-sm64ds-r2.md (2026-07-23) is already stale the wrong way: notes/mwccarm-codegen.md is unchanged (1,465 lines, still ends at 7c), yet the repo has landed ≥100 commits since 2026-07-23T04:00Z (page-capped) at ~30 matches/day, and the new levers live only in commit/PR messages — a notes-file-keyed re-ingest will miss them until a 7d section eventually lands.
- **Evidence:** curl of raw notes/mwccarm-codegen.md today = 1,465 lines (matches r2's ingest); `gh api repos/tangosdev/sm64ds-decomp/commits?since=2026-07-23T04:00Z&per_page=100 | jq length` = 100, pushed_at 2026-07-24T17:36:44Z. Sampled post-ingest commit bodies contain levers absent from any notes section: 'declare callee's real 3rd arg (hidden-arg liveness)' div 2→0; 'inverted-guard suppresses pool-ldr hoist over str'; 'col=(u16)(x>>1) canonicalizes lsl15/lsr16 w/o rotating coloring'; 'u64-mask laundering the star-count pointer → ROM's addne+ldrbne RMW form'; plus a new repo policy (#641) requiring provenance + attempt-ledger rows in every match PR and a nearmiss/db.jsonl.
- **Action:** Add a small watch/mining script (gh api commits since <last-ingest>, extract lever lines from commit bodies) and run an imported-sm64ds-r3 transcription keyed on repo pushed_at rather than notes-section number; the commit-message stream is now the primary lever source, the notes file the lagging summary.

#### [VERIFIED] dsd frozen (no release, PR #60 dormant) but new Thumb $t-mapping bug #62 woke maintainer

- **Payoff:** B · **Effort:** hours
- **Claim:** dsd upstream remains frozen for us (latest release v0.11.0 2026-04-03; PR #60 open, untouched since 2026-05-30 — delink-tolerance is NOT coming soon), but issue #62 (filed 2026-07-14 by ai-tdd-labs/mkds-decomp: `dsd delink` omits the resuming $t mapping symbol after an inline jump table in a Thumb function, so mwldarm's -interworking pass emits BL where the ROM has BLX on rebuild) got a same-day maintainer reply — the first AetiasHax engagement since our #58 went unanswered, and a plausible trigger for an eventual v0.12.0 that would also unlock ds-rom 0.8.0 (r8's inert item).
- **Evidence:** gh api releases: v0.11.0 is newest (2026-04-03); pulls/60: open, updated_at 2026-05-30, 48 commits/35 files unmerged; issues/62 body read in full (dsd v0.11.0, MKDS EUR, --all-mapping-symbols, concrete disasm at 0x020cbf74-84), comment by AetiasHax 2026-07-14: 'Good catch... Will look into this soon!'. Our 3-region sha1 gates currently pass, so no active corruption in our pipeline, but our flagged Thumb tool bugs (batch_sha1 .thumb.c mapping, fastmatch Thumb multi-label '0 orig words') touch the same mapping-symbol machinery.
- **Action:** Cheap read-only audit: scan our delinked Thumb objects/asm for $d islands mid-function without a resuming $t (the #62 signature) to confirm our ROM has no affected sites rather than inferring it from gate passes; add #62 to the toolchain-watch note alongside r8's objdiff-#383 trigger, with 'dsd release > v0.11.0' as the re-evaluation trip-wire.

#### [VERIFIED] Negative: no external recipe for TST-vs-ANDS or byte-swap; one lead on spill shape

- **Payoff:** B · **Effort:** hours
- **Claim:** Both peer mwccarm catalogues contain nothing on TST-vs-ANDS instruction selection or unrolled byte-swap emission order — those two brief-675/676 residuals remain ours to characterize — while the third residual class (unexplained stack slot / spill shape, func_0209cae4) has exactly one external lead: 7c's 'spill slots are grouped by TYPE; declaring col0 signed instead of unsigned pinned the stack-slot order; decl order was never the lever', which our r2 import banked but explicitly left OFF the verify checklist.
- **Evidence:** grep -i 'tst|ands' over scratchpad copies of tangosdev notes (1,465 lines) and ai-tdd-labs notes (935 lines): zero TST content; the only ands material (6l named-u16 narrowing, READ8 '(q&1)' macro) concerns narrowing idioms, not discard-vs-keep selection. grep 'byte-swap|byteswap|endian|__rev': zero hits in both. imported-sm64ds-r2.md line 59 carries 7c's spill-type lever marked 'not in the requested first-pass checklist'. Also confirmed mkds-decomp's mwccarm-codegen.md is a stale fork of tangosdev's (identical 6h-6p sections, same sm64ds function names like func_ov006_02115b0c inside the MKDS repo) — do not double-ingest it as an independent source.
- **Action:** Bank the two negatives in lever-payoff.md so future sweeps stop searching externally; run one retry session: (a) TST→ANDS on func_0208be70/func_0207e6bc by giving the masked value a REAL downstream use (brief 675 tried only a named-but-condition-only intermediate; our own brief-668 'unused param needs a real use' lever is the local analog); (b) byte-swap per-term evaluation-order form on func_02054ec0/func_0206ea00 (brief 676's own explicitly-untried plan); (c) promote the 7c signed/unsigned spill-type lever into the VERIFY-on-2.0/sp1p5 checklist and try it on func_0209cae4's sub sp,#4.

#### [UNVERIFIED] khdays tie census: register-permutation residues are compiler-build-invariant

- **Payoff:** B · **Effort:** hours
- **Claim:** khdays' allcc.py empirically established (their class-A/class-B tie census) that pure register-permutation residues produce the IDENTICAL residue across every mwccarm 2.0/3.0 build, while scheduling-class residues respond to opt-level/flag changes (allopt.py) — if it holds on our 2.0/sp1p5-vs-legacy tiers, our diagnostic-before-parking flow should skip the tier-route step for pure register-permutation diffs and reserve it for scheduling-shaped ones, saving one compile cycle per parked candidate.
- **Evidence:** allopt.py docstring (read from raw, dated 2026-07-19): 'aquel [allcc.py] varia la BUILD del compilador y dejo demostrado que para la clase A (permutacion de registros) todas las 2.0/3.0 dan el mismo residuo'; allopt exists because class-B (instruction scheduling) is what varies with -O4,p vs -O4,s vs -O3,p. Their config/arm9/file_compilers.json per-file compiler override is the shipping mechanism when a flag hit is found (README: middleware TUs built with an older mwccarm) — the same concept as our .legacy*/.thumb tier routing, independently converged on.
- **Action:** Verify on 2-3 of our parked reg-alloc-wall functions (e.g. brief 673's mov r1,r0 copy class): compile the same draft across our tiers and confirm the register-permutation residue is invariant; if confirmed, amend the wall-taxonomy diagnostic order (tier-route only scheduling-shaped residues) and note allopt-style -O sweeps as the flag-level analog.

#### [UNVERIFIED] objdiff and decomp.me confirmed quiet — r8's bump trigger has not fired

- **Payoff:** B · **Effort:** hours
- **Claim:** As of today there is nothing to adopt from objdiff or decomp.me: objdiff's latest release is still v3.7.3 (2026-07-10) and both watched items are unmerged (PR #383 ARM trailing-data-directive/pool mis-size fix: open; issue #384 Metrowerks .comment alignment: open), so r8's 'bump only when a release carries #383' trigger stands untripped; decomp.me has zero ARM/NDS-relevant changes since May.
- **Evidence:** gh api repos/encounter/objdiff/releases → v3.7.3 newest; pulls/383 state=open merged=false; issues/384 state=open. gh api repos/decompme/decomp.me/commits since 2026-05-01 grepped for arm/mwcc/nds/nitro → 0 hits; the stream is asm-differ/m2c bumps and Dreamcast/Saturn/agbcc work (#2050/#2044/#2046). Bonus corroboration from tangosdev's own deep-research file (notes/research-matching-levers.md F1.8, pulled to scratchpad): m2c's documented mwcc path is PowerPC-only with no arm-mwcc idiom path — consistent with our existing m2c-pin analysis.
- **Action:** No action beyond keeping the v2.7.1 pin; fold the 'checked 2026-07-24, trigger unfired' line into the same toolchain-watch note as the dsd #62 watch so the next external sweep starts from a dated baseline.

### Completeness critic

#### [VERIFIED] Flagged follow-ups evaporate: 2 round-breaking tool bugs + best data item in no queue

- **Payoff:** A · **Effort:** day
- **Claim:** Work flagged via spawn_task or 'dedicated follow-up' prose never lands in docs/queue/*.md, so the 4 loop-until-QUEUE-EMPTY agents can never pick it up: the cmatch_loop.py --keep-drafts stray-draft bug (kills the next delink/configure with 'multiple rules generate the same output'), the batch_sha1.py .c-to-.s suffix gap (blocks every .thumb.c/.legacy*.c candidate in future batches), and the data_020b52d0 carve (the probe's own strongest-evidence retype) are all un-queued.
- **Evidence:** docs/queue/claude-decomper.md:136 says both bugs were 'flagged via spawn_task rather than fixed inline'; grep of all 4 docs/queue/*.md + docs/state.md finds zero queue items for either bug. docs/queue/claude-scaffolder.md:205: data_020b52d0 'Flagged as a dedicated follow-up rather than rushed here' — 4 matched C consumers, 145x12B records, count confirmed 2 ways, but the only [TODO] across all four queue files is cm-crossregion-ports (verified by grep). spawn_task chips render in the session owner's UI, not the repo the agents read.
- **Action:** Coordinator adds 3 queue items this merge round: (1) fix cmatch_loop.py --keep-drafts draft cleanup, (2) fix batch_sha1.py reverse-mapping for routing suffixes + guard the missing-.s false-negative, (3) carve the 4-byte data_020b52d0 base from the gap pool and retype the 145x12B table, 3-region gated. Adopt a standing rule: any 'flagged separately' in a brief result must cite a queue item id.

#### [VERIFIED] ITCM fix is shovel-ready since 2026-07-22, never actioned, no queue item exists

- **Payoff:** A · **Effort:** hours
- **Claim:** The q-itcm-reach diagnosis identified the exact one-regex root cause keeping the last invisible module (10 EUR + 14 USA + 14 JPN unmatched functions, ~6.2 KB) out of every census and worklist, and specified the two-layer fix plus regression tests — but the code is untouched and no follow-up was queued.
- **Evidence:** docs/research/campaign-analytics/itcm-reachability.md (2026-07-22) names _MODULE_RE = r'^src/(main|overlay\d+)/[^/]+\.s$' as the cause and proposes path-part classification + size_census union + 2 named regression tests. tools/wall_aware_headroom.py:76 still contains that identical regex today (verified by grep). grep of docs/queue/*.md finds no implementation item — only the [DONE] read-only investigation. Scale check: EUR 1,884 B / USA+JPN 2,156 B each unmatched (report's size_census table) — roughly one sweep-batch of ships (brief 675 shipped 9 functions).
- **Action:** Queue an 'itcm-feeder-fix' item implementing the report's own proposal verbatim: module-aware path classification in wall_aware_headroom.py, union with size_census.py's unmatched itcm rows, regression tests for src/main/itcm/func_01ff8400.s and one symbol-only gap, assert dtcm contributes zero function rows.

#### [VERIFIED] state.md body ~2 months stale; 22-commit integration branch exists on one machine only

- **Payoff:** B · **Effort:** hours
- **Claim:** The cross-machine handoff bridge fails its own under-a-minute contract: a cold brain reading state.md gets a May-era metrics table, a false 'Open PRs: 0, both agents idle' status, and a Next-brain TODO pointing at briefs 234-236 while the project is at 676; meanwhile the current merge round (brain/integ, ahead 22 incl. 5 merged PR branches) has no origin backup ref.
- **Evidence:** docs/state.md:11 header fresh (2026-07-23) but line 84 'Open PRs: 0 ... Both agents idle' (6 branches pending, 4 agents active); lines 133-140 metrics cite 'post #671 + #672 merge', complete_units 1,749/2,660, matched_code 5.03% — PR-number era verified via gh (#706 merged 2026-05-27); lines 193-208 Next-brain TODO cites briefs 234/235/236. Lines 6-9 state the under-a-minute contract; line 325 'State.md is the bridge'. git branch -r shows no origin/brain/integ; git status shows integ ahead 22 with uncommitted queue edits. MEMORY.md documents a prior shared-worktree race that corrupted a commit parent on this exact setup.
- **Action:** In this round's brain-PR: move state.md lines ~81-310 to STATE-LOG.md and regenerate the body from current queue results (q-doc-archive covered research STATUS docs but never state.md itself); adopt 'git push origin brain/integ' at each merge checkpoint as standing practice.

#### [UNVERIFIED] Suite made genuinely green, but every gate including the only live item still skips it

- **Payoff:** B · **Effort:** hours
- **Claim:** q-green-pytest's stated end state ('update the queue headers + gate3 guidance to drop the --no-tests advice') was never executed, so the regression-catching value of greening the suite is unrealized: pipeline bugs of exactly the class brief 675 hit (batch_sha1 false negatives, fastmatch crashes) remain invisible to agent gates.
- **Evidence:** Commit e24d4c9b 'test: make Windows suite genuinely green' merged 2026-07-23 (origin/main log). Yet 34 occurrences of --no-tests remain across docs/queue/*.md; the sole open [TODO] (cm-crossregion-ports, docs/queue/claude-decomper.md:142, written 2026-07-24 — after greening) still instructs 'gate3.py --scope all --no-tests'; briefs 675 and 676 (run 2026-07-24) both gated with --no-tests. tools/gate3.py:117-131 confirms pytest is a hard gate only when not skipped.
- **Action:** Edit the live cm-crossregion-ports gate line and all four queue headers to make full gate3 (with pytest) the default, reserving --no-tests for explicitly build-only briefs; add one line to the queue protocol header stating the suite is green as of e24d4c9b.

#### [UNVERIFIED] 82% of shipped C TUs are placeholder-named; the Codex lanes that fix this sit idle

- **Payoff:** B · **Effort:** day
- **Claim:** Naming-at-scale is still correctly below frontier work for the CC lanes, but the calculus changed on the Codex side: both Codex queues are fully drained, the proven mechanical naming lanes are all [DONE] with no successor, and the just-landed prototypes provenance JSON (3,816 evidence-backed signatures) makes a propagation pass cheaper than ever — while every one of the ~76 pending ships adds to the placeholder stock.
- **Evidence:** Counted on disk: find src -name 'func_*.c' = 8,865 vs 1,946 named .c files (82% placeholders); only 212 of the func_*.c contain asm, so ~8.6K are natural-C TUs counted by the readable-C headline. Queue states verified: codex-decomper 30/30 and codex-scaffolder 29/29 items [DONE], zero [TODO]; drained naming lanes = q-sig-refresh-2/3, q-name-crossprop-2/3, q-name-batch/-2, q-nitro-names (35 byte-verified SDK names). Substrate: q-prototypes-h-redo (origin/claude/q-prototypes-h-redo, pending merge) commits provenance JSON + self-checker with 0 mismatches.
- **Action:** Queue q-name-batch-3 for the idle Codex lanes plus a one-time 'name the top-N most-called func_* symbols using the prototypes provenance JSON as signature evidence' item, gated by the existing dsd check 3-region + scope_gate --kind naming pattern.

#### [UNVERIFIED] decomp.dev registration (#1020) fully built, blocked a month on ~30 min of owner action

- **Payoff:** B · **Effort:** hours
- **Claim:** The only externally-visible progress surface still missing is complete and rotting in an open PR: everything left is owner-only (build+push the private ROM image, grant pull, register at decomp.dev), it consumes zero agent capacity, and each passing toolchain change (objdiff v3 action, c99 flag hook) increases revalidation cost.
- **Evidence:** gh pr view 1020: OPEN, updatedAt 2026-06-24T10:20:05Z (30 days); branch origin/brain/decomp-dev-ci not an ancestor of origin/main (git merge-base check); diff = decomp-report.yml (135 lines) + Dockerfile (194) + docs/decomp-dev-setup.md owner checklist (250). PR body: 'merge only AFTER the image exists', steps 1-3 owner-only. Drift check run: all four configure.py flags the workflow uses (-w/--compiler/--no-extract/--dsd) still exist at tools/configure.py:59-64, so it has not rotted yet. Adjacent public-presentation work is already live (PR #1023 README revamp merged; origin/progress-visuals badge branch updated 2026-07-24).
- **Action:** Relay the 5-step docs/decomp-dev-setup.md checklist to the user this round (in its own copy-paste code block per user preference) with an explicit do-or-close decision; if do, merge #1020 after image push and register; if close, close the PR so the branch stops carrying silent revalidation debt.

### Parked-corpus re-audit

#### [UNVERIFIED] ov002-wall-cohort 'do not sweep' verdict refuted 2 days later; ~890-candidate lane

- **Payoff:** S · **Effort:** days
- **Claim:** The campaign-analytics authority doc (brief 654, 2026-07-22) classifies all 2,719 brief-294-stamped ov002 .s as a verified reg-alloc-wall cohort and says 'Keep as .s; do not schedule a blind C sweep' — but the 07-24 overlay sweep shipped 43 ov002 functions FROM that exact cohort at ~54% hit rate, leaving ~890 unattempted ov002 ≤160B candidates projected at 360-480 EUR ships (~+1-2pt readable-C), the largest quantified lane in the project.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/docs/research/campaign-analytics/ov002-wall-cohort.md rows: 2,719 files 'brief-294 reg-alloc wall (P-16-class)... do not schedule a blind C sweep'. Merge b80caafe stat: 64 overlay .s deleted, 43 in src/overlay002. Verified `git show origin/main:src/overlay002/func_ov002_021aff4c.s` (shipped by the sweep) carries the exact counted evidence string 'reg-alloc-walled functions with no C match (brief 294 endgame)'. Sweep doc (docs/research/cm-overlay-small-sweep-2026-07-24.md): 64/118 = 54%, ov002 = 971 of 1,010 non-main small candidates; 43/0.54 ≈ 80 attempted → ~890 remain.
- **Action:** Two-line annotation at the top of docs/research/campaign-analytics/ov002-wall-cohort.md (and its cross-refs in codegen-walls-audit.md/endgame-ledger.md) recording the 07-24 contradiction, then queue repeated ov002 ≤160B sweep batches (~24 candidates/agent, the sweep's exact 5-worktree playbook) until hit rate decays below ~20%.

#### [UNVERIFIED] 9 named 'field-pool' parks with already-cracked siblings: one-batch re-audit

- **Payoff:** A · **Effort:** day
- **Claim:** Nine specific pre-convention parks carry the exact pool-constant-folding wall signature the typed-struct lever reverses, and every one has a documented class-sibling that has since shipped: ov011 021ca630+021ccf3c (trio-mate 021ca600 cracked by sweep batch 4 via the struct convention), ov011 021cfa50, ov004 021cc2a4+021d6ed0+021d641c (co-members 021d0afc/021cb518 shipped 2026-07-04), ov017 021b768c ('catalogued, not re-tested' since brief 340), ov002 02269534 (P-16's sole affected pick — the typed struct for its exact symbol is already banked), ov000 021aa840.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/docs/research/brief-308-ov011-clean-c-wave1.md HALT section: tried only 3 pointer phrasings ('&= RMW, int*p+scaled index, char-cast') — typed struct member never tried; 1 of its 3 (021ca600.c, committed 07-24 batch 4) since cracked. src/overlay004/ov004_core.h:88-89 field-pool citations '== ov011 021d0afc wall' — 021d0afc.c and 021cb518.c shipped 07-04. P-16 entry (docs/research/codegen-walls.md:7583+) shows the miscoded form verbatim as `extern char data_ov002_022d016c[]; *(int*)(data_ov002_022d016c+0xd20) += 1` and its 'not yet tried' list omits typed-struct; struct Ov002D016c with f_d20/f_d2c exists at src/overlay002/ov002_core.h:239-270, extern at :88. All 9 .s confirmed on disk; sizes 44-168B except 02269534 (548B). brief-362 re-tested 021cc2a4 with a kit that did NOT include the typed-struct lever. None of the 9 appear in the sweep doc or queue docs as re-attempted.
- **Action:** Single decomper batch: for each of the 9, declare/extend a typed struct extern (tools/build_struct_bank.py mines field layouts automatically), redraft with base->field access, fastmatch resolved-mode, 3-region gate; update the P-16 entry in codegen-walls.md and the ov011/ov004/ov017 core.h wall notes with outcomes either way.

#### [UNVERIFIED] Census: 99 still-parked core.h wall citations, all pre-convention; 36% already reversed

- **Payoff:** A · **Effort:** days
- **Claim:** All 21 canonical ov*_core.h wall/park sections predate the typed-struct convention (they cite briefs 304-403, June 2026), and their historical reversal base rate is already 36% without any dedicated re-audit — 78 of 216 wall-context citations are now .c, 99 remain .s and constitute the re-audit worklist, with an expected 25-40 further EUR ships at the observed 25-54% re-test hit rates.
- **Evidence:** Mechanical census I ran (addresses within ±3 lines of WALL/DEFERRED/near-miss/unsteerable keywords in C:/Users/leona/Dev/gx-spirit-caller/brain/src/overlay*/ov*_core.h, checked against on-disk .s/.c): 216 total → 99 still .s, 78 now .c, 39 unresolved. Ledger corroboration: brief-568's cmatch-parked-and-floor.md (07-13) rows — 27/29 still parked but both reversals happened incidentally within days (func_02078ebc 'volatile-load destination wall' shipped 07-20; ov006 021b79ac shipped); brief-340's catalogued-miss list has 2 of 5 classes with shipped members (ov019 021b4c30 cracked by sweep; ov017 021b2c8c 'no named locals exist to steer' shipped 07-04). Convention timeline: lever-payoff.md lever #4 cites brief 579 (07-15); brief 609 (07-18) formalized the struct bank; core.h wall notes all predate both.
- **Action:** Queue a wall-citation resweep item over the 99 still-.s citations (list generated by my census script) with the modern kit: typed-struct extern first on any candidate whose orig .s shows bare-symbol pool-load + [reg,#imm] while the note mentions pool/base folding, then arity-check, polarity swap, declaration order, corrected sp3-routing. Park-on-sight only for pure scheduling/register-choice diagnoses made WITH the modern kit.

#### [UNVERIFIED] Stale-citation annotator: 36-69% of catalogued wall addresses are already shipped

- **Payoff:** B · **Effort:** hours
- **Claim:** Every re-audit attempt so far has burned most of its budget discovering that catalogued wall addresses were already shipped (brief 362: 155/225 = 69% stale; my census: 78/216 shipped + 39 unresolved), and one documented failure mode (m2c_feed returning disasm for already-carved functions) made an already-shipped function masquerade as an uncarved wall — a small status-mapping script would make every future re-audit start from live truth.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/docs/research/brief-362-backlog-resweep-pilot.md: 'The catalog is ~69% STALE... Always confirm a candidate is genuinely uncarved... before counting it' (the 021b2280 masquerade). My census reproduces the rate today: 78/216 core.h wall-context citations shipped, 39 unresolved (renamed/thumb-suffixed/data symbols my naive path-mapping missed — exactly what a real tool must resolve). The batch_sha1/fastmatch suffix-mapping bugs flagged in brief 675 compound the same address→file mapping fragility.
- **Action:** Write tools/wall_citation_status.py: extract every 8-hex address from core.h WALL blocks + campaign-analytics ledgers (cmatch-parked-and-floor.md, ov002-wall-cohort.md, epilogue-wall-corpus.md), resolve to current src path handling .thumb.c/.legacy*.c suffixes and renames via symbols.txt, emit live worklist (still-.s) + stale list (shipped) with citing doc and brief number. Feeds the resweep directly.

#### [UNVERIFIED] Reversal lever sat in-repo 7 weeks; core.h recipes don't reach re-audit kits

- **Payoff:** B · **Effort:** hours
- **Claim:** The typed-struct/offset-member lever was banked in ov000_core.h on 2026-06-03 (brief 319 recipe #1, verbatim: pointer-cast 'folds N into the pool constant'), yet the two dedicated backlog re-audit pilots run the SAME WEEK (briefs 340 and 362, both dated 06-06) omitted it from their lever kits and re-walled field-pool candidates it would likely have cracked — kit composition for any future re-audit must be drawn from all core.h recipe banks plus lever-payoff.md, not the sponsoring overlay's notes.
- **Evidence:** C:/Users/leona/Dev/gx-spirit-caller/brain/src/overlay000/ov000_core.h:82-87 (brief 319, git-dated 2026-06-03): 'Offset-member, not pointer-cast... folds N into the pool constant'. brief-362 (06-06) lists its full kit as 'tri-compile, (u8) cast, dispatch-order inversion, pass-through' — no typed-struct — and its 021cc2a4 field-pool re-test failed; brief-340 (06-06) left 021b768c 'catalogued, not re-tested'. Global lever-payoff.md only gained the lever as #4 via brief 579 (07-15), with evidence count still '3/0' — not yet updated with the sweep's 6 reversals or the 78 shipped citations.
- **Action:** Update docs/research/reshape-recipes/lever-payoff.md lever #4: raise evidence to include the sweep's 6 documented-wall reversals and mark it 'try FIRST on any pool-word or base-offset diff before parking'; add one line to the re-audit queue template requiring the kit be assembled from lever-payoff.md + all core.h VERIFIED recipe sections.

### Data-lane scaling

#### [UNVERIFIED] Suite made genuinely green, but every gate including the only live item still skips it

- **Payoff:** B · **Effort:** hours
- **Claim:** q-green-pytest's stated end state ('update the queue headers + gate3 guidance to drop the --no-tests advice') was never executed, so the regression-catching value of greening the suite is unrealized: pipeline bugs of exactly the class brief 675 hit (batch_sha1 false negatives, fastmatch crashes) remain invisible to agent gates.
- **Evidence:** Commit e24d4c9b 'test: make Windows suite genuinely green' merged 2026-07-23 (origin/main log). Yet 34 occurrences of --no-tests remain across docs/queue/*.md; the sole open [TODO] (cm-crossregion-ports, docs/queue/claude-decomper.md:142, written 2026-07-24 — after greening) still instructs 'gate3.py --scope all --no-tests'; briefs 675 and 676 (run 2026-07-24) both gated with --no-tests. tools/gate3.py:117-131 confirms pytest is a hard gate only when not skipped.
- **Action:** Edit the live cm-crossregion-ports gate line and all four queue headers to make full gate3 (with pytest) the default, reserving --no-tests for explicitly build-only briefs; add one line to the queue protocol header stating the suite is green as of e24d4c9b.

#### [UNVERIFIED] 82% of shipped C TUs are placeholder-named; the Codex lanes that fix this sit idle

- **Payoff:** B · **Effort:** day
- **Claim:** Naming-at-scale is still correctly below frontier work for the CC lanes, but the calculus changed on the Codex side: both Codex queues are fully drained, the proven mechanical naming lanes are all [DONE] with no successor, and the just-landed prototypes provenance JSON (3,816 evidence-backed signatures) makes a propagation pass cheaper than ever — while every one of the ~76 pending ships adds to the placeholder stock.
- **Evidence:** Counted on disk: find src -name 'func_*.c' = 8,865 vs 1,946 named .c files (82% placeholders); only 212 of the func_*.c contain asm, so ~8.6K are natural-C TUs counted by the readable-C headline. Queue states verified: codex-decomper 30/30 and codex-scaffolder 29/29 items [DONE], zero [TODO]; drained naming lanes = q-sig-refresh-2/3, q-name-crossprop-2/3, q-name-batch/-2, q-nitro-names (35 byte-verified SDK names). Substrate: q-prototypes-h-redo (origin/claude/q-prototypes-h-redo, pending merge) commits provenance JSON + self-checker with 0 mismatches.
- **Action:** Queue q-name-batch-3 for the idle Codex lanes plus a one-time 'name the top-N most-called func_* symbols using the prototypes provenance JSON as signature evidence' item, gated by the existing dsd check 3-region + scope_gate --kind naming pattern.

#### [UNVERIFIED] decomp.dev registration (#1020) fully built, blocked a month on ~30 min of owner action

- **Payoff:** B · **Effort:** hours
- **Claim:** The only externally-visible progress surface still missing is complete and rotting in an open PR: everything left is owner-only (build+push the private ROM image, grant pull, register at decomp.dev), it consumes zero agent capacity, and each passing toolchain change (objdiff v3 action, c99 flag hook) increases revalidation cost.
- **Evidence:** gh pr view 1020: OPEN, updatedAt 2026-06-24T10:20:05Z (30 days); branch origin/brain/decomp-dev-ci not an ancestor of origin/main (git merge-base check); diff = decomp-report.yml (135 lines) + Dockerfile (194) + docs/decomp-dev-setup.md owner checklist (250). PR body: 'merge only AFTER the image exists', steps 1-3 owner-only. Drift check run: all four configure.py flags the workflow uses (-w/--compiler/--no-extract/--dsd) still exist at tools/configure.py:59-64, so it has not rotted yet. Adjacent public-presentation work is already live (PR #1023 README revamp merged; origin/progress-visuals badge branch updated 2026-07-24).
- **Action:** Relay the 5-step docs/decomp-dev-setup.md checklist to the user this round (in its own copy-paste code block per user preference) with an explicit do-or-close decision; if do, merge #1020 after image push and register; if close, close the PR so the branch stops carrying silent revalidation debt.

#### [UNVERIFIED] Reloc+autocorr evidence miner validated 3/4 exact; ranks all 201 blobs in one pass

- **Payoff:** S · **Effort:** hours
- **Claim:** The probe's per-blob manual evidence-mining is automatable: one read-only script pass over config/eur/**/relocs.txt (consumer load sites) plus kappa-corrected byte autocorrelation over blob contents (stride) produces a ranked retype queue for every opaque blob, and its stride calls match independent ground truth on 3 of 4 validation cases.
- **Evidence:** Prototype at C:/Users/leona/AppData/Local/Temp/claude/C--Users-leona-Dev-gx-spirit-caller/b1d604d9-197e-468c-b069-5066df93024d/scratchpad/{blob_census.py,stride_detect.py,*.json}. Validation: data_020b52d8 -> 12 (probe's 4-consumer ground truth), data_020b6ec4 -> 20 (DataArrayCatalog.md), data_020c7b44 -> 8 (catalog); data_020c8e84 got 36 vs catalog's 32 (neither divides 2060B cleanly - catalog itself suspect). Bonus validation: data_ov002_022c357c (19,488B) scored stride 24, exactly Ov002DataComplete.md's documented 6-word records. 99/201 blobs (134,820B of 158,601B) have stride score >=0.25. Zero-heavy blobs handled by collision-probability correction; smallest-lag-at-90%-of-peak picks the fundamental period.
- **Action:** Land the two scratchpad scripts as tools/data_evidence_miner.py (merge into one; add the pointer-word placeholder fix from the tool-caveats finding), commit the ranked JSON under docs/research/data/, and have the next data sweep work top-down from its ranking instead of hand-picking blobs.

#### [UNVERIFIED] Untyped-data frontier is 3.9x the blob framing: 456KB is in NO source file at all

- **Payoff:** S · **Effort:** day
- **Claim:** The 201 opaque blob files (158.6KB) are the minority of untyped data - 456,755 bytes of rodata+data across all EUR modules sit in dsd's unclaimed-gap filler with no owning source file, yet contain 8,890 referenced data_ symbols, so the real data frontier is ~617KB and mostly not yet visible to any file-based sweep.
- **Evidence:** split_census.py diff of config/eur/**/delinks.txt file coverage vs section bounds: main 246,308B unowned = 76.5% of its 321,852B rodata+data; ov004 141,519B; ov002 18,252B; all-module total 456,755B across 300 gap runs, 8,890 data_ symbols inside (each symbol exists only because relocs reference it). Method validated against the known 4-byte data_020b52d0 gap (0x020b52d0..0x020b52d4), reproduced exactly. Largest single gap: 0x020c9694..0x020cd700 (16,492B) = the deferred 'D-3 mega' from docs/state.md.
- **Action:** Extend the evidence miner to gap regions: extract gap bytes from extract/eur at known addresses, run the same stride+reloc scoring on each of the 300 gap runs, and rank them alongside the blob files - this is where the bulk of the 501KB-class campaign actually lives.

#### [UNVERIFIED] Split-table class mechanically detectable; 2 new confirmed cases, one easier than 020b52d8

- **Payoff:** A · **Effort:** hours
- **Claim:** stride-strong + size%stride != 0 is a working detector for the data_020b52d0 sub-symbol-split class: it flags 18 suspects, and neighborhood checks already confirm 2 new physical tables split by dsd - data_020c7b40/44 (616 x 8B records) and data_020c3198/9c (4 x 24B records) - with data_020c7b44's carve strictly easier than the flagged 020b52d8 protocol because its base already owns a file.
- **Evidence:** stride_detect.py flagged 18 stride-strong-nondivisible blobs including the known case (data_020b52d8: stride 12, rem 4 - exactly the 8B base shift the probe documented). New: data_020c7b44.s (4,924B, stride 8, score 0.668, rem 4) has data_020c7b40.c as a 4-byte file directly before it (delinks.txt lines 20390-20396); 4+4924 = 4928 = 616*8 exact; only 1 load-consumer. data_020c319c (92B, stride 24) has a 4B unowned gap holding data_020c3198/data_020c319a immediately before; 4+92 = 96 = 4*24 exact. Also found 26 blobs with interior data_ sub-symbols (one-file-many-symbols variant, e.g. data_ov004_02200e00: 9 interior syms, reloc offsets gcd 4) - these need interior symbol preservation on retype but no delinks surgery.
- **Action:** Execute the data_020c7b40+data_020c7b44 merge as the carve protocol's low-risk second test case (merge two adjacent TUs, one consumer, no gap extraction), then apply the proven protocol to data_020c319c and the remaining 15 suspects after neighborhood checks.

#### [UNVERIFIED] Overlays add 130 blobs / 93KB; top-15 ranking computed, one 8.8KB decoy identified

- **Payoff:** A · **Effort:** hours
- **Claim:** Beyond main's 71 opaque blobs (65.6KB) the overlays hold 130 more (93.0KB), 85% concentrated in ov004 (41.3KB) + ov002 (38.1KB), and the size-x-consumer ranking puts three >19KB stride-confirmed tables at the top while exposing data_ov004_021ded69 (8,780B, rank 4 by size) as a no-structure decoy to skip.
- **Evidence:** blob_census.json ranking (size x (1+load-consumers), stride annotated): #1 data_ov004_02200f18 22,560B stride 2 (= short[] char map, catalog-confirmed Unicode/SJIS), #2 data_020b6ec4 30,720B stride 20 (validated), #3 data_ov002_022c357c 19,488B stride 24 (catalog-confirmed), then 020bff80 5,904B/stride 4, ov006_021cec04 1,168B/stride 20/8 load-sites, 020c7b44 4,924B/stride 8. data_ov004_021ded69 scored 0.07 (no periodic structure, odd-aligned start 0x021ded69 - the brief-182 odd-aligned ov004 class docs/state.md already defers). ov006's 40 blobs are only 9.5KB total - many tiny state tables, low per-blob payoff.
- **Action:** Seed the next data-retype queue item with the computed top-15 (excluding 021ded69 and pointer-table entries pending the parser fix), starting with the two catalog-confirmed >19KB tables where record shape is already independently documented.

#### [UNVERIFIED] Tool caveats: pointer-word compaction skews 21 tables; census regex counts comments

- **Payoff:** B · **Effort:** hours
- **Claim:** Two prototype defects must be fixed before trusting miner output on pointer-bearing tables: symbolic .word entries are silently dropped (shifting the byte lag structure - ov006's 600B Type-C tables scored 36 vs the catalog's 20B pointer records, data_02101928 parsed 0 of 1,016B), and the c-blob census regex matches 'const unsigned char data_X[N]' inside comments (2 false positives: the already-retyped data_020bef80 and data_020b4d60).
- **Evidence:** 21 .s data files under src/{main,overlay002,overlay004,overlay006}/data contain symbolic '.word func_|data_' entries (grep count). data_ov006_021ce650: 404 of 600B parsed, stride call 36 vs Ov004Ov006DataTables.md's documented 30 pointer-pair 20B records. Counter-signal available for free: relocs FROM inside a blob's range reveal pointer fields directly (data_02101928 has 6 kind:load relocs from 0x02101928..0x02101940 = pointer array stride 4, confirmed in config/eur/arm9/relocs.txt). data_020bef80.c header comment confirms it is already retyped (commit e3de95fb) yet census counted it as opaque; corrected totals: 201 blobs / 158,601B, not 203 / 160,761B.
- **Action:** In the landed miner: emit 4 placeholder bytes per symbolic .word before autocorrelation, add a from-reloc-inside-blob pointer-field channel, and anchor the census regex to declaration position (line start, require '= {') to exclude comment mentions.

#### [UNVERIFIED] Catalog conflict on ov006 640B blocks: autocorr says 20B period, catalog says 16B

- **Payoff:** B · **Effort:** hours
- **Claim:** All seven ov006 640-byte Type-B blocks score a clean 20-byte period (0.697-0.711, full 640B parsed, no pointer words to distort), contradicting Ov004Ov006DataTables.md's '1x8B header + 39x16B records + 8B tail' layout - one of the two is wrong and it must be resolved before retyping those blocks.
- **Evidence:** stride_results.json: data_ov006_021ccb88/021cce08/021cd088/021cd308/021cd588/021cd808/021cda88 all 640B, all stride 20, scores 0.697-0.711 (highest consistency of any blob family in the pool); 640 = 32*20 exact vs catalog's composite 8+39*16+8 layout (docs/research/data/Ov004Ov006DataTables.md lines 42-67, 125-130). Seven independent tables agreeing on the same period is hard to dismiss as noise, but a periodic 16B payload with interleaved sub-headers could alias; the catalog read was manual.
- **Action:** Hand-decode one block (data_ov006_021cce08) against both hypotheses - lay out 32x20B vs 8B+39x16B+8B and check which produces column-consistent field types - then correct whichever source is wrong before the ov006 retype batch.

### Codegen residual mining

#### [UNVERIFIED] TST-vs-ANDS parks are documented wall C-23; the routing fix was never tried

- **Payoff:** S · **Effort:** day
- **Claim:** All four TST-vs-ANDS parks (func_0208be70 90.91%, func_0207e6bc, func_0209aa48 and func_0209d788 both 85.7%) are instances of the already-documented C-23 ANDS-dead peephole (mwcc 2.0 emits TST when the masked value is dead, 'regardless of source form'), whose documented fix is .legacy*/mwcc-1.2 routing — and none of the parking briefs (655, 675) tried routing; brief 655 even recorded it as 'a previously undocumented mwcc quirk'.
- **Evidence:** docs/research/codegen-walls.md:2424-2597 (C-23: ANDS→TST peephole is mwcc-2.0-only, fix = rename to .legacy.c, verified 75-compile SP sweep; :2587 'regardless of source form'). docs/research/brief-655-main-sweep.md:90,144-153 ('previously undocumented'); brief-675-main-128-drain.md:127-143 (source reshapes only, 'Mechanism not identified'). Corpus proof I ran: of 3,968 matched EUR functions' ORIGINAL delinked .o (build/eur/delinks/src/**), exactly 15 keep `ands rN,rN,#imm`; 14 are .legacy/.legacy_sp3-routed; the sole default-tier one (func_02076260) has the masked value LIVE (strb next insn) so the peephole legitimately doesn't fire — 15/15 consistent. Bonus: parked func_0208be70 is the hardware-SQRT busy-wait in the same MMIO math block as the two SHIPPED C-23 exemplars func_0208bd88/func_0208bde0.legacy.c, and parked func_0207e6bc sits 0x84 from matched func_0207e638.legacy.c which keeps `ands r2,r2,#1`. Census: 149 EUR-baseline unmatched .s files carry the ands-dest-plausibly-dead signature (my scan of 5,920 files; upper bound, includes live-after-loop cases).
- **Action:** Draft the 4 diagnosed parks as .legacy.c (func_0209aa48/func_0209d788 also fit: `ands` + movne/moveq-overwrite = dead by construction), fastmatch-verify, ship. Then mechanically triage the 149-file signature list (dead-value check + epilogue-tier compatibility) as a new corpus lane like epilogue-wall-corpus. Also append these instances to C-23's confirmed list and delete brief 655's 'undocumented quirk' framing.

#### [UNVERIFIED] The '4-register-spill ABI puzzle' is varargs/double home-area; 40-function cluster

- **Payoff:** S · **Effort:** days
- **Claim:** The `stmdb sp!,{r0,r1,r2,r3}` entry shape that resisted brief 675's four source models is mwcc's parameter home-area materialization, with two sub-recipes: (a) variadic signature + address-of-last-named-param (proven by matched libs/nitro/OS_SPrintf.legacy.c, whose original .o starts `push {r0,r1,r2,r3}` and whose C is literally the recipe including a no-stdarg va hack), and (b) double/64-bit parameters accessed word-wise AND returned (func_020b0034 decodes cleanly as soft-float copysign(x,y): r1 = (x.hi&~0x80000000)|(y.hi&0x80000000), return x in r0:r1) — and the cluster is 40 unmatched EUR functions, not 2.
- **Evidence:** build/eur/delinks/libs/nitro/OS_SPrintf.legacy.o disasm: push {r0,r1,r2,r3}; add r2,sp,#12; bic r2,#3; add r2,#4 — identical va-derivation shape to parked func_020551b8's `add r3,sp,#0xc`. libs/nitro/OS_SPrintf.legacy.c: `s32 OS_SPrintf(s8*, const s8*, ...)` + `(void*)((u32*)((u32)&format & ~0x3)+1)`. My census of src/main+src/overlay* .s (5,920 files): 40 files spill r0-r3 at entry — 24 va-pointer shape (add rX,sp,#N into spill area, then bl helper), 13 dword-stack-access shape (e.g. func_020b0034/func_020b005c/func_020b1854, plus 5 in ov011), 3 other. brief-675-main-128-drain.md:170-185: the four failed models included struct-return (changes ABI via hidden ret-pointer) and address-of-with-unrelated-return (dead-code-eliminated) — none were variadic, none returned the modified double itself.
- **Action:** Ship the small dword-family first: model func_020b0034 as `double f(double x, double y){ ((u32*)&x)[1] = (((u32*)&x)[1]&~0x80000000)|(((u32*)&y)[1]&0x80000000); return x; }` (return type double keeps the spill live; sibling func_020b005c is the same family). Then work the 24 va-pointer functions with the OS_SPrintf variadic recipe (identify the vprintf-like helper each one bl's, e.g. func_02054c64). Note tier: OS_SPrintf needed .legacy.c — check each target's epilogue before routing.

#### [UNVERIFIED] Matched-corpus counter-example grep costs ~10s and cracked 3 clusters; promote to tools/

- **Payoff:** S · **Effort:** day
- **Claim:** A systematic 'grep the matched corpus for the pattern I can't produce' step is trivially cheap (enumerate build/eur/delinks/src/**/*.o having a same-stem src .c → 3,968 objects; batch arm-none-eabi-objdump 400-per-invocation → single text cache in seconds, no build) and no such tool exists in tools/ — predict_walls.py regex-classifies UNMATCHED functions for walls, and export_matched_pairs.py has the objdump plumbing but exports retriever rows, not pattern queries; neither answers 'which C shape produced this instruction pattern', the question that unlocked findings 1, 2 and 4 here.
- **Evidence:** Ran end-to-end this session: cache = 3,968 matched originals (91,977 lines) built via `xargs -n400 tools/arm-none-eabi/bin/arm-none-eabi-objdump.exe -d` in seconds; queries for ands-keeping (15 hits→C-23 confirmation), byte-swap idiom (2 true hits with exact C source), ≥6-reg LDM/STM (0 hits), 4-reg spill (0 hits in game corpus, 1 decisive hit in the 136-object libs corpus). tools/predict_walls.py:1-40 (unmatched-side classifier, already has StyleA and C-23 MMIO detectors); tools/export_matched_pairs.py:1-30 (objdump mnemonic extraction already written). Gotcha for the implementer: objdump renders standalone shifts as UAL (`lsr r1,r3,#24`), so patterns must match both UAL and inline-shift (`mov r1,r3,lsr #24`) forms — my first byte-swap query missed all hits because of this.
- **Action:** Add tools/corpus_grep.py: inputs = a disasm regex (+ optional 'value dead after' predicate), output = matching matched-function rows with .c path, routing tier, and the matching asm lines; reuse export_matched_pairs.py's objdump/mnemonic code and cache the disasm under the scratch/build dir. Wire a one-line 'query the corpus before parking any 1-2-word residual' step into the sweep playbook.

#### [UNVERIFIED] Byte-swap parks: corpus pins exact expression and schedule; func_0206ea00 draft-ready

- **Payoff:** A · **Effort:** hours
- **Claim:** The canonical single-expression byteswap `((v>>24)&0xff)|((v>>8)&0xff00)|((v<<8)&0xff0000)|((v<<24)&0xff000000)` produces the 4-shifts-first schedule on BOTH tiers (two matched counter-examples), and parked func_0206ea00's 32-bit swap block is instruction-for-instruction that schedule — its residual is most plausibly the surrounding details (16-bit half needs a SIGNED int local to get `asr #8`; Style-A-like epilogue `add sp,#8; ldmia sp!,{r4,lr}; bx lr` suggests legacy routing like its matched neighbor func_0206eb08.legacy.c 0x108 bytes away), while func_02054ec0's different interleave correlates with the swapped value being an intermediate consumed by field extraction rather than a return value.
- **Evidence:** src/main/func_0206eb08.legacy.c (matched, canonical expression) → original .o schedule: lsr24,lsr8,lsl8,lsl24,and,and,and,orr,and,orr,orr. src/main/func_0203ed2c.c (matched, DEFAULT 2.0 tier, same expression) → same 4-shifts-first schedule. Parked src/main/func_0206ea00.s: identical schedule modulo register names; its u16 half uses `asr #0x8` (signed) not lsr; epilogue = separate pop + bx lr. Parked src/main/func_02054ec0.s: interleaved schedule (3 shifts, 2 ands, lsl24 late) with result in r1 feeding byte-extraction (it is an is-private-IPv4-style range check on the swapped value) — no matched counter-example for that consumer context (searched all 3,968).
- **Action:** Draft func_0206ea00 as .legacy.c: canonical nested-OR for the 32-bit half, signed-int local for the u16 half (`(h>>8&0xff)|(h<<8&0xff00)` with h as plain int), fastmatch-verify. For func_02054ec0 try the canonical expression assigned to a local (`u32 sw = ...;` then extract bytes from sw) at default tier before parking again.

#### [UNVERIFIED] Sweeps skip the project's own signature indices — func_0209cae4 was already classified

- **Payoff:** A · **Effort:** hours
- **Claim:** Brief 676 parked func_0209cae4 as a 'stack-layout mystery' (unexplained sub sp,#4) when it is row 215 of epilogue-wall-corpus.md, mechanically pre-classified Style A → .legacy.c — the exact corpus the 668/672/673 lane converts at 70.3% (45/64); the same non-consultation pattern produced finding 1 (C-23 re-derived as 'undocumented' twice, ~500 briefs after it was documented), i.e. the sweep loop has no mandatory pre-park lookup against epilogue-wall-corpus.md, codegen-walls.md recognition cues, or predict_walls.py output.
- **Evidence:** docs/research/campaign-analytics/epilogue-wall-corpus.md line 215: `0209cae4 | main | ... | 0x84 | Style A: add sp,#4; ldmfd sp!,{lr}; bx lr | assembly-only`. src/main/func_0209cae4.s prologue `stmfd sp!,{lr}; sub sp,sp,#0x4` = the .legacy.c discriminator banked in memory (cm-epilogue-wall-cracked) and encoded in tools/predict_walls.py's StyleA rule. brief-676-small-resweep-upper.md:93-97 ('flagged as needing a closer read rather than continued guessing'). Cumulative corpus hit rate: brief-673-epilogue-resweep-3.md:161-165.
- **Action:** Two-line fix to the sweep playbook/queue template: before parking ANY candidate, (1) grep its address in epilogue-wall-corpus.md and attempts.tsv, (2) run its .s through predict_walls.py (or grep codegen-walls.md recognition cues for the residual instruction pair). Also draft func_0209cae4 as .legacy.c now — it additionally shows the C-3 volatile re-read pattern (str/ldr same slot back-to-back) already in the lever catalog.

#### [UNVERIFIED] Negative result: LDM-transpose has zero corpus support; reg-alloc stays parked

- **Payoff:** B · **Effort:** hours
- **Claim:** Corpus mining cannot help two documented residual classes: (a) func_0208b300's 9-register LDM + 3 STM transpose has no counter-example anywhere in the matched game corpus or built libs (0 hits for any ≥6-register LDM/STM on a non-sp base across 3,968 + 136 objects), so the struct-assignment hypothesis can only be tested by compiling; and (b) register-choice reg-alloc remains numerically the largest parked mechanism (29 of 63 parked rows in attempts.tsv plus 4 of brief 673's 6 parks) but has a verified park-on-sight discriminator (brief 674), so residual-mining effort should go to findings 1-4, not here.
- **Evidence:** My ≥6-reg LDM/STM scans over both disasm caches: 0 hits (matched cache 3,968 objects, libs cache 136). docs/research/campaign-analytics/attempts.tsv: 63 parked, park_class breakdown 29 reg-alloc / 11 predication / 5 instruction-selection — but the ledger covers briefs 650-672 only; 673/675/676 and overlay-sweep parks are prose-only, so any cluster census computed from the TSV undercounts by ~35 rows. brief-675-main-128-drain.md:186-191 (both transpose source forms compiled to per-word ldr/str, 23 vs 11 words).
- **Action:** When func_0208b300 is next attempted, test the one untried shape the struct-copy mechanism predicts (row-struct assignment / whole-struct `*dst = *src` per C-2/C-2a in codegen-walls.md) and record the result either way; separately, append the 673/675/676 + overlay-sweep parks to attempts.tsv so the next cluster census is complete.

## Refuted — do not re-derive

- The central claim — that q-green-pytest's end state ("update the queue headers + gate3 guidance to drop the --no-tests advice") "was never executed" — is directly contradicted by the greening commit's own diff: e24d4c9b itself rewrote docs/queue/claude-scaffolder.md's protocol header to "gate `python tools/gate3.py --scope all` (including pytest)" and replaced the "Gate with --no-tests" banner with "The full gate is required by default", and also updated docs/queue/codex-scaffolder.md ("the normal build gate includes the full pytest suite") and tools/gate3.py. The claude-decomper protocol header likewise instructs plain "gate3.py --scope all". "Every gate including the only live item still skips it" is also false: there are 7 open [TODO]s (not 1), and all 4 newest (claude-scaffolder.md:200-218, added 2026-07-24 post-greening) gate with full gate3 --scope all or pytest explicitly. The cited count is 35 occurrences/33 lines (not 34), and the large majority are historical Gate/Result records inside [DONE] items or items *about* --no-tests, which should not be rewritten. What survives is only a narrow residue: 2-3 open decomper item-level gate lines (cm-small-resweep-upper:120, cm-main-128-drain:132, cm-crossregion-ports:139 — the last added post-greening in 5920b58a) still carry --no-tests, and briefs 675/676 (working tree only, not yet on origin/main) followed those stale lines. That is a minor per-item copy-paste inconsistency against already-updated default guidance, not an unrealized greening.
  - **Corrected:** The q-green-pytest follow-up WAS executed: commit e24d4c9b itself updated the scaffolder queue protocol headers and gate3 guidance to make the full gate (including pytest) the default, and all four post-greening scaffolder TODOs gate with the full suite. Residue: the per-item Gate lines of 2-3 still-open claude-decomper TODOs (cm-small-resweep-upper, cm-main-128-drain, cm-crossregion-ports — the last written 2026-07-24 after greening) still say `gate3.py --scope all --no-tests`, and briefs 675/676 gated per those stale lines. A one-line edit to those 3 item gate lines (leaving [DONE] items' historical records untouched) would close the gap.
- Refuted on the 'already queued' criterion, not on the facts. The evidence reproduces almost entirely: all 9 .s parks exist on disk; sibling 021ca600.c shipped in batch 4 and its own header comment confirms the typed-struct convention cracked exactly the brief-308 wall (which tried only the 3 pointer phrasings claimed); ov011 021d0afc.c and ov004 021cb518.c shipped; ov004_core.h carries the field-pool citations at the cited lines; P-16 in codegen-walls.md matches verbatim (022d016c+0xd20, 02269534 parked-not-shipped) and struct Ov002D016c with f_d20/f_d2c exists in ov002_core.h with the typed extern at ~:88; brief-362's 021cc2a4 retest kit had no typed-struct lever; none of the 9 appear in the batch-4 sweep report. HOWEVER, the proposed action duplicates queue item cm-parked-reaudit-1 [TODO], added to docs/queue/claude-scaffolder.md by origin/main commit a9a35d63 (2026-07-24): a ~25-candidate parked-corpus re-audit that collects precisely the parks whose notes cite pool-constant/offset-arithmetic language (grep codegen-walls.md, briefs' park lists, GLOBAL_ASM comments) and re-attempts them with typed-struct externs plus the full current toolkit. All 9 named candidates fall inside that collection criteria, so a separate 9-item batch would be redundant with work already queued this same day.
  - **Corrected:** The 9 named field-pool parks and their cracked-sibling evidence are real and verified, but the re-attempt action is already queued as cm-parked-reaudit-1 in docs/queue/claude-scaffolder.md (commit a9a35d63, 2026-07-24). The finding's residual value is as INPUT to that queued batch: hand its executor the 9 pre-verified addresses (ov011 021ca630/021ccf3c/021cfa50, ov004 021cc2a4/021d6ed0/021d641c, ov017 021b768c, ov002 02269534, ov000 021aa840) as confirmed members of the ~25-candidate collection, saving the grep/triage step and guaranteeing the strongest-evidence cases are included in batch 1.

## Provenance

Reconstructed from `journal.jsonl` of run `wf_43046057-191` after a partial-completion
(weekly-limit) abort. Raw per-agent transcripts remain under the workflow transcript dir.
