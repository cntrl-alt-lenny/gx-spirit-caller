---
# func_020490dc (main, HIGH class A, 68B)

**Recipe:** null-guard + clear + 64-bit tick split into two stores; sp3
**Risk:** func_020930b0 must return r0:r1 as u64; the >>32 split could emit shifts instead of plain hi/lo. f_8=0 before call, reload after.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — Wrong pool word value (hidden from verify.py; only ninja report catches it)

**Why this recipe hits this mode:** `data_0219dc80` is loaded by an LDR-from-pool instruction twice (guard check and post-call reload); if the C compiler emits a different pool-word value for this global, verify.py wildcards the pool slot and the mismatch is invisible until `ninja report`.

**Pool words to verify against orig .s:**
- `data_0219dc80` — guard load base and post-call reload base for the struct ptr; both LDR pool slots must agree with the orig .s .word entries

**Verify gate sequence:**
1. verify.py src/main/func_020490dc.c func_020490dc [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep `data_0219dc80->f_8 = 0` before the call and reload `data_0219dc80` (via a fresh load) after `func_020930b0()` returns, matching the two separate pool-load slots in the .s
- The `(unsigned int)(t >> 32)` form is needed to avoid a shift instruction; verify.py should show a visible diff if the compiler emits `lsr r1,#32` instead of using r1 directly
- If the u64 split emits extra shift instructions, try assigning `t` to a `volatile unsigned long long` local to suppress the optimizer
- Match the store sequence exactly: f_c (low word) stored first using the reloaded pointer, then f_10 (high word) — reversing them changes the instruction order
- Confirm the sp3 routing (`.legacy_sp3.c` suffix) matches the prologue in the orig .s; wrong routing changes the frame setup bytes
---
