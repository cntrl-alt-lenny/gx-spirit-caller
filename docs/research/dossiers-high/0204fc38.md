---
# func_0204fc38 (main, HIGH class A, 100B)

**Recipe:** linear search loop, early return, tail call (RELOAD global)
**Risk:** field 0xf4 is word array; func_0204fa7c(i,count) arg order; ble/blt loop shape
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; ble/blt loop shape and cmp order caught by verify.py

**Secondary mode:** Mode B — BL to func_0204fa7c wildcarded by verify.py reloc

**Why this recipe hits this mode:** The loop termination condition controls whether the compiler emits `blt` or `ble`; the wrong form produces a visible instruction diff that verify.py catches immediately. The BL reloc for `func_0204fa7c` is wildcarded and needs ninja report confirmation.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0204fc38.c func_0204fc38 [--cc all] — instruction diff (blt/ble) is visible directly
2. ninja report — resolves BL target for func_0204fa7c; catches Mode B callee mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write the null-pointer guard first (`if (func_020498f0() == 0) return 0`) and the count guard second (`if (n <= 0) return 0`) — reversing these two guards changes the branch order and produces a visible diff.
- Use `i < n` (strict less-than) for the loop condition to produce a `blt` branch; switching to `i <= n-1` may or may not fold identically depending on the compiler version.
- Access the array element as `func_020498f0()->arr[i]` via the struct, where `arr` is typed `int arr[1]` at +0xf4 — this ensures a word-indexed `ldr` relative to the struct pointer, not a raw byte offset.
- The argument order to `func_0204fa7c` is `(i, n)` — index first, count second; swapping them produces a wrong-operand diff visible immediately.
- The accessor `func_020498f0()` must be reloaded on each loop iteration for the array access — the asm reloads inside the loop; hoisting the pointer changes the instruction sequence.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020498f0` | main TU call |
| `bl` | `func_0204fa7c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

