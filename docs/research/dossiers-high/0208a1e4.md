# func_0208a1e4 (main, HIGH class A, 156B)

**Recipe:** GUARD (ret 2) + ldrh field + and-test branch + cond-store tail
**Risk:** Non-zero func_0208a09c path returns its own r0 (not 0) — the `return 0` here may mismatch r0; consider returning the call result. p5 is 6th-slot arg at [sp+0x18]. Field offsets guessed.
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_0208a1e4.c func_0208a1e4 --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0208938c` | main TU call |
| `bl` | `func_0208a09c` | main TU call |
| `bl` | `func_02089ee8` | main TU call |
| `bl` | `func_02089024` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Read the orig .s pool section; C must reference the same globals in the same ORDER
- Named `data_XXXX` globals are pool-word risks; wrong address silently passes verify.py
- Use `ninja report` (not just `ninja objdiff`) before sha1

## Reference

- Prep: `docs/research/c-match-prep/0208a1e4.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
