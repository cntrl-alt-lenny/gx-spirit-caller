# func_0205d944 (main, HIGH class A, 92B)

**Recipe:** clear two fields, allocator with 5 args, store, bool-ize
**Risk:** the 5th (stack) arg is the dtor func_0205d9a0; allocator arg order guessed; field offsets 0x428/0x42c/0x430 fixed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_0205d944.c func_0205d944 --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `func_0205d9a0` | func ptr |
| `_LIT1` | `func_0205da2c` | func ptr |
| `_LIT2` | `func_0205da1c` | func ptr |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02054840` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

