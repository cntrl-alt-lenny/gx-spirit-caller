---
# func_02016b60 (main, HIGH class A, unknown B)

**Recipe:** GetSystemWork bitfield-guard + nested jump-table dispatch, tail stores
**Risk:** 0x8d8&1 guard compiles to `and #1` not `lsl;lsr` (020186b0 wall); jump-tables + cond loads likely diverge
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches `and #1` vs `lsl#31`/`lsr#31` bitfield-guard encoding

**Secondary mode:** Mode B (HIDDEN) — all extern callee addresses (`GetSystemWork`, `func_02018a38`, `func_02018a64`, `func_020195ec`, `func_02019664`, `func_0201aaf4`, `func_0201b708`, `func_0201b730`, `func_0201b7b4`, `func_0201b7e0`) are BL targets wildcarded by verify.py; only `ninja report` catches a wrong callee address

**Why this recipe hits this mode:** The bit0 guard `(*(unsigned int *)(w + 0x8d8) << 0x1f) >> 0x1f` is documented to lower to `and r0, #1` under mwcc, which does NOT match the baserom's `lsl #31` / `movs lsr #31` sequence. This is the same wall as `func_020186b0` (see `.s` entry in-tree). Additionally the nested switch dispatch may produce jump tables that diverge from the baserom layout.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

WARNING: This candidate is documented as shipped as .s in-tree (brief 302) because the bit0 guard cannot be reproduced as clean C under mwcc. Do NOT attempt .c replacement until the `and #1` wall is resolved for this function specifically.

**Verify gate sequence:**
1. verify.py src/main/func_02016b60.c func_02016b60 [--cc all] — and#1 vs lsl#31/lsr#31 is visible directly
2. ninja report — resolves BL relocations for all extern callees; catches Mode B mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The bit0 guard `(v << 0x1f) >> 0x1f` compiles to `and #1` under mwcc, not the baserom's `lsl #31; movs lsr #31` — this function likely cannot be matched as clean C without a Mac/permuter run; treat as .s-only until proven otherwise
- Cross-check ALL nine extern callee addresses against the .s BL instructions — `GetSystemWork`, `func_02018a38`, `func_02018a64`, `func_020195ec`, `func_02019664`, `func_0201aaf4`, `func_0201b708`, `func_0201b730`, `func_0201b7b4`, `func_0201b7e0` — any misspelling is invisible to verify.py
- The nested `switch (*(int *)(w + 0x924))` inside case 3 must produce a jump table matching the baserom; if it emits a branch-chain instead, try reordering the cases to match the baserom's table layout
- The `case 0` / `case 3` fallthrough inside the inner switch must be preserved (`case 0: *(int*)(w+0x924)=4; /* fallthrough */ case 3:`) to match the baserom's dispatch sequence
- For the `0x8e0` bitfield RMW `(*(int *)(w + 0x8e0) & ~1) | 1`, write as a read-modify-write in source order and verify the bic/orr encoding does not get fused by mwcc
---
