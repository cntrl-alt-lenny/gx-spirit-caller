---
# func_0204bd8c (main, HIGH class A, 56B)

**Recipe:** accessor flag-set(2) + double-deref call + flag-clear(0)
**Risk:** f1a8/f4 offsets; the **(int**) double-deref of f4 may need cast tweak
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — Wrong callee address (hidden from verify.py; BL reloc wildcarded, only ninja report catches it)

**Why this recipe hits this mode:** `func_020498f0` is called three times via BL to obtain the accessor pointer, and `func_02062530` is called once; verify.py wildcards all BL relocs, so a wrong address for either callee passes instruction-byte comparison silently until `ninja report` resolves the relocations.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0204bd8c.c func_0204bd8c [--cc all] — instruction bytes
2. ninja report — resolves BL relocs; catches Mode B callee-address mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Call `func_020498f0()` separately for each use (three times) rather than caching the result; the .s shows three independent BL instructions to the accessor before each field access or call, not a single cached pointer
- The double-deref `*func_020498f0()->f4` must dereference the `int *` field; if the compiler emits an extra load or cast, ensure the struct typedef declares `f4` as `int *` (not `int **`) and the source reads `*ptr->f4`
- Field offsets f4 (0x04) and f1a8 (0x1a8) are guessed; cross-check each `ldr`/`strb` offset in the .s against these values and adjust the struct padding if they differ
- The flag-set and flag-clear sequence (2 then 0) must bracket the `func_02062530` call exactly; inserting the call outside the bracket changes the instruction order and will be visible in verify.py
- Cross-check the BL target addresses for `func_020498f0` and `func_02062530` in the orig .s before running `ninja sha1`
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020498f0` | main TU call |
| `bl` | `func_02062530` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

