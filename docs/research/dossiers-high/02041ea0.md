---
# func_02041ea0 (main, HIGH class A, 100B)

**Recipe:** ldrb guard ==0xff; +0x1000 fields recomputed off param; guard + tail call
**Risk:** base param held in r4 with +0x1000/+offsets recomputed each use (not bound) — matches asm; 0x1bf4/0x1b34 pool words
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches it directly

**Why this recipe hits this mode:** The risk flags that +0x1000/+offset addresses are recomputed each use rather than bound to a register; if the compiler CSEs the add or reorders the recomputations, the instruction sequence differs from the original and objdiff will show a mismatch that verify.py surfaces directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py). The 0x1bf4/0x1b34 pool words are plain offsets added to the `base` parameter, not pooled global addresses.

**Verify gate sequence:**
1. verify.py src/main/func_02041ea0.c func_02041ea0 [--cc all] — instruction bytes; diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Keep all three `base + 0x1000 + 0x...` address expressions inline (no local variable for base+0x1000) so the compiler recomputes the add each time, matching the original sequence
- If verify.py shows a cmp/bne ordering issue on the 0xff guard, try `if (*(u8 *)(base + 0x1000) == 0xff)` inverted to match the asm branch direction
- The calls at base+0x1bf4 are in a specific order (func_020927b8 then func_02092748 bracketing the store); preserve that interleaving exactly
- For the final tail-like call to func_02091af4(base+0x1b34), if a branch-and-link appears where a branch is expected, try ensuring the function is declared void and the call is the last statement
- Run verify.py --cc all across compiler variants; a scheduler difference between sp1p5 and legacy compilers may explain any residual diff
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00001bf4` | MMIO/const |
| `_LIT1` | `0x00001b34` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020927b8` | main TU call |
| `bl` | `func_02092748` | main TU call |
| `bl` | `func_02091af4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

