---
# func_02046b3c (main, HIGH class A, 108B)

**Recipe:** multi-guard chain + lookup + call, tri-valued return; sp3
**Risk:** f_24 reloaded after the call (second daec ref) — split refs should match; ==5/==6 may merge into a range compare
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible instruction diff (range-compare vs two separate cmps)

**Why this recipe hits this mode:** `data_0219daec` is a named global loaded from the pool multiple times across the guard chain; verify.py wildcards all such pool loads, so a wrong symbol address passes silently and only `ninja report` catches it. Additionally, if mwcc merges the `f_24 == 5 || f_24 == 6` into a single range-compare (SUB + CMP), that produces a byte diff that verify.py will surface.

**Pool words to verify against orig .s:**
- `data_0219daec` — null-guard base and twice-dereferenced struct base for f_24 field; pool .word must match orig .s

**Verify gate sequence:**
1. verify.py src/main/func_02046b3c.c func_02046b3c [--cc all] [--module main] — instruction bytes; merged-range-compare diff visible here
2. ninja report — resolves pool word for data_0219daec; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep `f_24 != 5 && f_24 != 6` as two separate comparisons (not a range) to match the .s `cmp r0,#5; beq .L_a4; cmp r0,#6; beq .L_a4` pair; a range-form `f_24 < 5 || f_24 > 6` may compile to a range-check.
- The second `data_0219daec` reference for f_24 must come from a second global load (`ldr r0,=data_0219daec; ldr r0,[r0]; ldr r0,[r0,#0x24]`) — do not cache the struct pointer in a local across the `func_02045280()` call.
- Write the tri-valued returns as literal `return -1;` / `return -2;` / `return 0;` — mwcc should emit MVN r0,#0 / MVN r0,#1 / MOV r0,#0 respectively.
- For the `func_02046888(key)` null-check path: test `if (e == 0) return -2;` explicitly — mwcc emits `mvneq r0,#1` in the .s, indicating a conditional-move pattern; if that diverges, keep the branch explicit.
- Cross-check `data_0219daec` pool .word in the .s against config/eur/arm9/symbols.txt before running ninja report.
---
