# func_021b4324 (ov019, HIGH class A, 132B)

**Recipe:** STORE-ORDER struct-fill once + MIN if-assign clamp + counted hit-test scan
**Risk:** Rect field offsets and the +0x18 row stride assumed; the movgt clamp and movne r0,r4 conditional-return codegen may reshape
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay019/func_ov019_021b4324.c func_ov019_021b4324 --cc all --module ov019` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov019_021b2728` | overlay call (ov019) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Read the orig .s pool section; C must reference the same globals in the same ORDER
- Named `data_XXXX` globals are pool-word risks; wrong address silently passes verify.py
- Use `ninja report` (not just `ninja objdiff`) before sha1

## Reference

- Prep: `docs/research/c-match-prep/021b4324.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
