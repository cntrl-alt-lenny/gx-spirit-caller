---
# func_02054840 (main, HIGH class A, 40B)

**Recipe:** tail-forward with inserted constant arg (stack-arg shuffle)
**Risk:** arg widths assumed int; outgoing-stack slot ordering should match mwcc default
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; MWCC's stack-arg slot ordering and the width of each argument type directly control the str/push sequence for the 5th and 6th outgoing args and are visible in the instruction stream

**Why this recipe hits this mode:** Five incoming args are remapped to six outgoing args by inserting a constant 4 as the third argument. Args beyond r0-r3 are placed on the stack; if MWCC uses a different slot stride (e.g., word vs halfword for an arg assumed int) or emits args in a different push order, the resulting str/stm instruction sequence will differ byte-for-byte.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054840.c func_02054840 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Declare all args as plain `int` (no unsigned, no short) so MWCC uses word-sized pushes for every stack slot — narrower types can change the str stride
- The constant `4` inserted as the third arg must be a literal integer in the call, not a named const, to avoid pool allocation
- If verify.py shows stack-slot ordering wrong, try reordering the incoming parameter declarations (a0..a4) to match whatever register/stack assignment the orig .s uses for the forwarded call
- Because this is a pure forwarding wrapper with no logic, zero reshaping is expected beyond fixing the arg types — if the diff is large, double-check the prototype of func_02054700 has exactly 6 int args
- Confirm the function has no local variables to avoid stack-frame allocation that would change the prologue
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02054700` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

