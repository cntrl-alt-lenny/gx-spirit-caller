---
# func_02054338 (main, HIGH class A, 128B)

**Recipe:** null-assert + indexed-array loop + early-return nonzero
**Risk:** loop-count signedness; assert pool-literal order; struct field types assumed word
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word order mismatch; data_020ffb7c and data_020ffb80 are named globals whose pool-slot order may differ from the orig .s, wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E — visible instruction diff; loop-count signedness (blt vs bne/bcc) and struct field type assumptions may produce differing load encodings

**Why this recipe hits this mode:** The assert call loads data_020ffb7c into r0 and data_020ffb80 into r1. Pool slot allocation follows declaration/first-use order; a mismatch here places the wrong literal in the wrong slot and verify.py's BL-reloc wildcarding will not surface it — only ninja report resolves these. The items pointer (+0x00) and count field (+0x04) being assumed `word`-sized is an additional visible-mode risk if MWCC widens or narrows the loads.

**Pool words to verify against orig .s:**
- `data_020ffb7c` — assert file-string pointer, loaded into r0 (first arg to func_020a6d54)
- `data_020ffb80` — assert expr-string pointer, loaded into r1 (second arg to func_020a6d54)

**Verify gate sequence:**
1. verify.py src/main/func_02054338.c func_02054338 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A pool-slot mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Declare data_020ffb7c before data_020ffb80 to keep pool slot ordering matching the expected r0/r1 assert arg sequence; verify with ninja report
- Loop counter must use signed `int` comparison (`i < p->count` producing blt) — if bcc appears, the count field may be unsigned; adjust the struct field type accordingly
- Keep items as `void **` (pointer to pointer) and count as `int` — if either field produces an unexpected ldr halfword or ldrb, widen to `int` or `void *`
- The early-exit `return r` is inside the loop body on a non-zero result — do not hoist or reorder; the orig expects a branch-out mid-loop pattern
- Write the loop as a straight for-loop without a pre-guard (no count<=0 early-exit) — this function returns 0 implicitly after the loop, no addle/ldmle prefix expected here
---
