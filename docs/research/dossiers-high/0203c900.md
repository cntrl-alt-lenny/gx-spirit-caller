# func_0203c900 (main, HIGH class B, 104B)

**Recipe:** GUARD bit-test chain returning globals (B/commutative ands)
**Risk:** Each if(flags&bit) must predicate as ands+ldrne/bxne; data_* must be ptr-typed so ldr [addr] (value), not addr, is returned.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203c900.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0203c900.c func_0203c900 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0203c900.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219d9c8` | data symbol |
| `_LIT1` | `data_0219d9bc` | data symbol |
| `_LIT2` | `data_0219d9c0` | data symbol |
| `_LIT3` | `data_0219d9c4` | data symbol |
| `_LIT4` | `data_0219d9b8` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

