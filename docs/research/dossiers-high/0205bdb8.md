# func_0205bdb8 (main, HIGH class A, 168B)

**Recipe:** guard (state==3) then teardown; decrement+reload+assert; tail frees
**Risk:** g->+0x210 reloaded after store for the bge check; m->+0x4 reloaded per call; assert block scheduling; bge=signed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/main/func_0205bdb8.c func_0205bdb8 --cc all` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02100ae8` | data symbol |
| `_LIT1` | `data_02100ad8` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020a6d54` | main TU call |
| `bl` | `func_02054e54` | main TU call |
| `bl` | `func_02054e70` | main TU call |
| `bl` | `func_020453b4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

