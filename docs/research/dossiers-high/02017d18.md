---
# func_02017d18 (main, HIGH class A, 156B)

**Recipe:** GetSystemWork bitfield-guard + cmp/beq state chain (1,2,3), RMW + tail stores
**Risk:** 0x8d8&1 guard -> `and #1` wall; cond loads `ldr r1,_LIT;str` and call ordering may diverge
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and #1` wall and literal-pool conditional load ordering

**Why this recipe hits this mode:** The `(v<<31)>>31` entry guard is the canonical `and #1` mismatch visible in verify.py output immediately. The `ldr r1,_LIT; str r1,[r4,#ofs]` sequences for the message-id stores are pool-word loads of immediate constants — mwcc may reorder or schedule them differently relative to surrounding calls, and the call ordering in state 2 (`func_0201abb0` then `func_02018a38`) and state 3 (`func_0201abd4` then `func_0201b7b4`... wait — state 3 ends with bit24 clear then `func_02018a64`) must match the .s call sequence exactly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02017d18.c func_02017d18 [--cc all] — instruction bytes (and #1 wall + call order visible)
2. ninja sha1 — final gate

## Reshape hints

- Keep call order within each state arm exactly as written: state 2 emits the message-id store, then `func_0201abb0(2)`, then `func_02018a38(0x13)` — swapping these will change the .s sequence
- State 3 must order: message-id store, `func_0201abd4(2)`, `*(int *)(w+0x900) &= ~0x1000000`, `func_02018a64()` — the `&=` must precede the final call to match the .s bic+bl ordering
- The state-1 early `|= 0x1000000` must be a naked RMW with no temp (keeps it as a single `ldr;orr;str` sequence); same for the state-3 `&= ~0x1000000`
- State chain is cmp/beq (not a switch/jump-table) — keep three separate `if (*(int *)(w+0x920) == N)` blocks rather than a switch to avoid the compiler emitting a jump-table for only 3 arms
- The state-3 double-call check (`func_02019664() != 0x65` then `func_0201969c() != 2`) must stay as two sequential guards with independent returns — binding either result to a temp risks reordering
---
