---
# func_02052ecc (main, HIGH class A, 56B)

**Recipe:** GUARD/early-return null check + conditional call result
**Risk:** offsets 0x1c/0x24 guessed; r2/r1 zero-init order may flip the two mov #0; bl-not-tail forces frame
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B (HIDDEN) — BL target `func_02053560` is a reloc; wrong callee address wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E — visible instruction byte diff from r2/r1 zero-init order (two mov #0 instructions may be swapped)

**Why this recipe hits this mode:** The BL to `func_02053560` is a reloc wildcarded by verify.py, so a wrong callee address is only caught by `ninja report`. Additionally, the risk flags that the two `mov #0` instructions for the zero-default path may have swapped r2/r1 order — a visible byte diff caught by verify.py.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02052ecc.c func_02052ecc [--cc all] [--module main] — instruction bytes (r2/r1 mov order visible)
2. ninja report — resolves BL reloc target for func_02053560; catches Mode B mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Offsets 0x1c and 0x24 are flagged as guessed — cross-check `[r0, #0x1c]` and `[r0, #0x24]` ldr instructions in the orig .s before committing to the struct layout
- The zero-init order for the early-return path: if objdiff shows `mov r2,#0` and `mov r1,#0` swapped, try reordering the struct field declarations or binding `int ret = 0; return ret;` to influence register assignment
- The BL (not BX) shape forces a stack frame (stmfd/sub sp,#4) — do not write this as a tail call; the frame is required
- `func_02053560` signature is `(void *, int)` — confirm arg order is `(p, self->f_24)` matching r0/r1 at the BL site in orig .s
- If the null guard uses `!p` vs `p == 0`, try both forms; they should emit the same cmp but the compiler may differ on condition code selection
---
