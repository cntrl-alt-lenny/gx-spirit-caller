---
# func_020457d8 (main, HIGH class A, 80B)

**Recipe:** double null GUARD + SETTER + post-store call, return const
**Risk:** minimal; struct field offsets (f0 word, f4 halfword) assume the inferred layout
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word mismatch; wrong singleton address wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** `data_0219dad0` is a named BSS pointer whose pool .word is wildcarded by verify.py's reloc comparison; a wrong address for the singleton passes byte-comparison silently until ninja report resolves the symbol. The risk also notes that field offsets (f0 at +0x00 as int, f4 at +0x04 as unsigned short) are inferred — a wrong offset assumption would also produce a silent pool-word difference.

**Pool words to verify against orig .s:**
- `data_0219dad0` — BSS singleton pointer (S*); load base for null guard on f0 (+0x00) and setter for f4 (+0x04)

**Verify gate sequence:**
1. verify.py src/main/func_020457d8.c func_020457d8 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatch (wrong data_0219dad0 address) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Confirm the struct layout against the orig .s: f0 must be a 4-byte word at +0x00 (ldr for the null guard), f4 must be a 2-byte halfword at +0x04 (strh for the setter = 3); a wrong field type produces ldrb/strb instead of ldr/strh
- All field accesses precede the call to func_02045678, so the singleton pointer `s` is loaded once (no post-call reload puzzle); keep all field touches before the BL
- The setter `s->f4 = 3` must use the local `s` (not a reload of `data_0219dad0`) since no call intervenes; verify this matches the orig .s store instruction
- The function returns 1 as a constant after the call; if verify.py shows a moveq or cmp before the return, the call result is being used — ensure func_02045678 return value is discarded (do not assign it)
- Confirm data_0219dad0 pool address via ninja report; verify.py wildcards it and a wrong BSS address passes silently
---
