# func_020457d8 (main, HIGH class A, 80B)

**Recipe:** double null GUARD + SETTER + post-store call, return const
**Risk:** minimal; struct field offsets (f0 word, f4 halfword) assume the inferred layout

## SHA1 pitfall

**Primary mode:** Mode A - Wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/020457d8.c)

**Verify gate:**
1. python tools/verify.py src/main/func_020457d8.c func_020457d8 --cc all
2. ninja report (resolves pool/BL words)
3. ninja sha1 (final)

## Reshape hints

- Read orig .s pool section; C must reference same globals in same order
- Named data_XXXX globals in recipe are pool-word risks; wrong address passes verify.py silently
- Use ninja report before sha1

## Reference

- Prep: docs/research/c-match-prep/020457d8.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
