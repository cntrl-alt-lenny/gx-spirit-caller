---
# func_020534b4 (main, HIGH class A, 32B)

**Recipe:** cond ? value : 0 via and-with-mask select
**Risk:** mwcc may emit a plain movne x/moveq 0 instead of the mvnne/andne mask trio; reg-alloc-walled per .s header
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; the `mvnne`/`andne` vs plain `movne`/`moveq` emission choice is compared directly by verify.py

**Why this recipe hits this mode:** the `func_020534d4(x) ? x : 0` ternary can be lowered either as a simple `movne r0, r4 / moveq r0, #0` pair or as the mask-trio `mvnne r0, #0 / andne r0, r4, r0 / moveq r0, #0`; the two emit different bytes and verify.py catches the mismatch immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020534b4.c func_020534b4 [--cc all] — instruction diff visible directly (mask-trio vs plain movne)
2. ninja sha1 — final gate

## Reshape hints

- If verify.py shows `movne r0, r4 / moveq r0, #0` but the orig .s has `mvnne r0, #0 / andne r0, r4, r0 / moveq r0, #0`, try casting through an unsigned mask: `return (int)((unsigned)x & (unsigned)(func_020534d4(x) ? ~0u : 0u))` to force the mask idiom
- The `reg-alloc-walled` flag means r4 must hold `x` across the BL; ensure `x` is not reloaded from the stack after the call (the prep binds it as parameter r4 which mwcc should preserve if it is the only saved live value)
- If the plain `movne/moveq` form matches, accept it — the prep comment says "best-effort reshape" and the mask form may not be reachable in clean C
- This is a one-liner body so the only tuning knobs are: the cast chain, the ternary operand order, and whether the result type is `int` vs `unsigned int`
- Cross-check with the caller `func_02052f04` case 2 which passes the result of this function to `func_020534d4` — the return type must be consistent
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020534d4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

