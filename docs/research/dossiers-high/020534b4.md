# func_020534b4 (main, HIGH class A, 32B)

**Recipe:** cond ? value : 0 via and-with-mask select
**Risk:** mwcc may emit a plain movne x/moveq 0 instead of the mvnne/andne mask trio; reg-alloc-walled per .s header

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/020534b4.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_020534b4.c func_020534b4 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/020534b4.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
