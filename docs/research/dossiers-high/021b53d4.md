# func_021b53d4 (ov006, HIGH class A, 104B)

**Recipe:** capture two fields (+0x44,+0x48) held across calls, 2 sinks+enter, guard, state-set
**Risk:** two callee-saved field reads (r4,r5) must precede calls; called sinks need a ptr-arg prototype to force r0 loads; state[4]=handle then state[0]=5
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay006/func_ov006_021b53d4.c func_ov006_021b53d4 --cc all --module ov006` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode B mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov006_0225dc34` | raw |
| `_LIT1` | `data_ov006_0225deb4` | raw |
| `_LIT2` | `data_ov006_0224f330` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov006_021c6c2c` | overlay call (ov006) |
| `bl` | `func_ov006_021c50c4` | overlay call (ov006) |
| `bl` | `func_ov006_021b52d0` | overlay call (ov006) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Cross-check every BL/BLX target address in the .s against your `extern func_XXXX` declarations
- BL relocs are wildcarded by verify.py; only `ninja report` resolves them
- Wrong callee address passes verify.py at 100% but fails sha1

## Reference

- Prep: `docs/research/c-match-prep/021b53d4.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
