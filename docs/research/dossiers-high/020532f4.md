---
# func_020532f4 (main, HIGH class A, 136B)

**Recipe:** INIT/CLEAR memset + STORE-ORDER field writes; CRC; OR-flag
**Risk:** offsets/struct shape guessed; func_02053230 call interleaved between f_1c and f_24 stores must keep that order; r5=self hoist
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; store ordering, field offset mismatches, and `0xedb88320` literal are compared directly by verify.py

**Why this recipe hits this mode:** mwcc is sensitive to the exact order of field stores relative to intervening calls; reordering `f_1c = 0` / `func_02053230` / `f_24 = arg1` changes the STR instruction sequence and verify.py catches the byte diff immediately. The `0xedb88320` poly literal and `0x3c` count are also pool-compared directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (`0x40`, `0xedb88320`, `0x3c`, `0x1`) are compared directly by verify.py.

**Verify gate sequence:**
1. verify.py src/main/func_020532f4.c func_020532f4 [--cc all] — instruction diff visible directly (store order, literal pool)
2. ninja sha1 — final gate

## Reshape hints

- Preserve the exact interleaved store order from the orig .s: `f_0=0x40`, `f_1c=0`, `func_02053230(self+4)`, `f_24=arg1`, `func_02053600(self+0x10, 0)`, CRC compute, `f_3c=crc`, `f_20|=1` — any reorder produces a different STR sequence
- The `func_02053230(self+4)` call sits between `f_1c=0` and `f_24=arg1`; do not hoist `f_24` above the call or sink it below `func_02053600` — the risk note flags this explicitly
- Keep `buf[0x400]` as one stack array; the CRC helper `func_020a66e8` takes a pointer to it and the frame must have exactly one `sub sp, #0x400`
- The `self->f_20 |= 1` OR-flag must come after the CRC store `f_3c = crc`, not before — verify the orig .s stores CRC then OR
- If struct shape is wrong (e.g. `_sub` or `_sub2` size off), verify the sub-init call arguments: `func_02053230` writes at `self+4` and `func_02053600` writes at `self+0x10`; those offsets pin the subfield boundaries
---
