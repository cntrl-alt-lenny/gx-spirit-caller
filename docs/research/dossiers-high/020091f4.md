---
# func_020091f4 (main, HIGH class A, unknown B)

**Recipe:** GUARD/DISPATCH switch + TAIL CALL (case0 returns callee)
**Risk:** case1 third-arg may emit mov r2,#1 vs orig's mov r2,r0; func arg types guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff if mwcc emits `mov r2,#1` (constant) instead of `mov r2,r0` (copy from incoming `sel` already loaded as 1) for the third argument of the `func_02008c84(1, a, 1)` call

**Why this recipe hits this mode:** When `sel == 1`, mwcc may reuse the already-loaded value of `sel` (which equals 1 at that branch) as the third argument by emitting `mov r2,r0`, rather than synthesizing the constant `#1` via `mov r2,#1`. This register-reuse vs immediate-load choice produces a visible byte difference that `verify.py` will catch directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020091f4.c func_020091f4 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- If mwcc emits `mov r2,#1` but orig has `mov r2,r0`: change the call to `func_02008c84(1, a, sel)` — when the branch is taken `sel` is known to be 1, so the value is identical but mwcc may reuse the register rather than loading an immediate
- Conversely if orig has `mov r2,#1` but mwcc emits `mov r2,r0`: use the literal `1` as the third argument (already done in the prep) and verify the compiler version; the legacy compiler may differ
- The `func_02008fb8(a, b)` in case 0 is a tail-call-style return; keep it as `return func_02008fb8(a, b)` so mwcc can potentially merge it with the outer return
- Verify the argument types (`int a, int b, int c` for `func_02008c84`; `int a, int b` for `func_02008fb8`) against the orig .s register setup; wrong types may cause unwanted zero-extend or sign-extend instructions before the BL
- The `return 0` fallthrough must produce a `mov r0,#0` before the epilogue; if objdiff shows a missing `mov`, try adding an explicit `else { return 0; }` branch instead of the fallthrough
---
