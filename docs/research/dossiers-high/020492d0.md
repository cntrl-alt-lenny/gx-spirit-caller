---
# func_020492d0 (main, HIGH class A, 76B)

**Recipe:** GUARD g->f18 + stack-arg pack + single call
**Risk:** stack-slot order [sp]/[sp+4] may reorder; func ptr passed as data
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — Wrong pool word value (hidden from verify.py; only ninja report catches it)

**Why this recipe hits this mode:** `func_020484c0` is passed as a function-pointer value `(void *)func_020484c0` pushed onto the stack, not called via BL; its address enters the pool as a .word and is wildcarded by verify.py's BL-reloc scan, so a wrong address is silent until `ninja report`. `data_0219dc80` similarly appears as a guard-load pool slot.

**Pool words to verify against orig .s:**
- `data_0219dc80` — guard load base; LDR pool slot for the global struct ptr
- `func_020484c0` — function pointer passed as a stack data argument (not a BL target); its address is a pool .word that verify.py wildcards

**Verify gate sequence:**
1. verify.py src/main/func_020492d0.c func_020492d0 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches (including func-ptr pool word) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep the cast `(void *)func_020484c0` for the fifth argument; casting to `void *` is what causes MWCC to emit a pool LDR rather than a BL, which matches the .s pattern
- Stack argument order for `func_02055c70`: arguments 5 and 6 go on the stack; verify that `[sp]` holds the func ptr and `[sp+4]` holds 0 — if they swap, reorder the call arguments
- The guard checks `g->f18 == 0` (not the global ptr itself); ensure the struct typedef offsets the `f18` field to 0x18 to match the orig .s ldr offset
- Dereference `*a1` inline in the call argument, not as a pre-fetched local, to keep the load close to the BL as in the .s
- Confirm `data_0219dc80->f18` is the correct field offset by comparing the LDR offset in the guard instruction against 0x18
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dc80` | data symbol |
| `_LIT1` | `func_020484c0` | func ptr |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02055c70` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

