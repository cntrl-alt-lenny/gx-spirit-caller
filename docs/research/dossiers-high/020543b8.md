---
# func_020543b8 (main, HIGH class A, 116B)

**Recipe:** null-assert + indexed-array loop (void body, count pre-guard)
**Risk:** void vs int return (trailing r0 leftover); loop-count signedness; assert pool order
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word order mismatch; data_020ffb7c and data_020ffb80 are named globals whose pool-slot order may differ from the orig .s, wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E — visible instruction diff; the count<=0 pre-guard emits addle/ldmle which depends on signed loop-count type; void return with trailing r0 leftover may differ if MWCC elides the value

**Why this recipe hits this mode:** The assert call loads data_020ffb7c into r0 and data_020ffb80 into r1; pool slot order follows first-use and a swap here is invisible to verify.py's BL-reloc wildcarding. Additionally, the count pre-guard (addle/ldmle early-out before the loop) depends on the signed comparison emitted for `count <= 0`, which ties to the signedness of the loop counter.

**Pool words to verify against orig .s:**
- `data_020ffb7c` — assert file-string pointer, loaded into r0 (first arg to func_020a6d54)
- `data_020ffb80` — assert expr-string pointer, loaded into r1 (second arg to func_020a6d54)

**Verify gate sequence:**
1. verify.py src/main/func_020543b8.c func_020543b8 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A pool-slot mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Declare data_020ffb7c before data_020ffb80 to match the r0/r1 assert argument order; run ninja report to confirm pool slot ordering in the output .o
- The count pre-guard (addle/ldmle) requires signed `int` for `p->count` — if the pre-guard is absent or emits bcc instead of ble, declare count as `int` explicitly
- The return type is void, but the orig .s has a trailing r0 leftover from the last call result — MWCC preserves r0 through a void function naturally; do not add a dummy return variable that forces an extra mov
- Keep loop counter `i` as signed `int` with `i < p->count` to emit blt; if bcc appears, convert p->count to `unsigned int`
- The loop body calls func_02053ca8(p->items[i], a1, a2) with no result captured — do not assign the return value to a local, which could alter the epilogue's callee-saved register pop sequence
---
