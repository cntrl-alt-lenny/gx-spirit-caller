---
# func_020548b4 (main, HIGH class A, 64B)

**Recipe:** dual separate-index loop + bind base across iters
**Risk:** loop bound emits blt vs the original bcc (unsigned) -> may need unsigned i
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word order mismatch; data_020ffbcc is a named global bound to a local before the loop, and its pool slot is wildcarded by verify.py; only ninja report catches a wrong literal value

**Secondary mode:** Mode E — visible instruction diff; loop bound emits blt (signed) vs bcc (unsigned) depending on whether `i` is signed or unsigned

**Why this recipe hits this mode:** `data_020ffbcc` is loaded once before the loop and held in r4 across all iterations. As a named data_ global its pool-word address is assigned by MWCC at compile time; if the local binding or the declaration ordering places it in a different slot than the orig .s expects, verify.py's BL-reloc wildcard does not catch it — ninja report must be run. The blt vs bcc loop-bound issue is a separate, visible-mode concern that verify.py will surface directly.

**Pool words to verify against orig .s:**
- `data_020ffbcc` — format-string base pointer held in r4 across the loop; passed as second arg to func_020a978c each iteration

**Verify gate sequence:**
1. verify.py src/main/func_020548b4.c func_020548b4 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A data_020ffbcc slot mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- If verify.py shows blt instead of bcc for the loop bound, change `int i` to `unsigned int i` (or `unsigned i`) for the loop counter — bcc is the unsigned comparison
- Keep the `fmt` binding (`char *fmt = data_020ffbcc`) before the loop as a single assignment to force r4 allocation before the first iteration; do not reload inside the loop
- Maintain `j` as a separate incrementing counter (not computed as `i * 2` on each iteration) to match the orig's dual-index pattern — an `i*2` multiplication may produce a different instruction sequence
- The output pointer arithmetic `dst + j` should use plain pointer addition with `char *dst` — do not cast dst to a wider type which could add a shift instruction
- Confirm func_020a978c has the prototype `(char *out, char *fmt, int val)` — a different third-arg type (e.g., unsigned char) would change the zero-extension of `src[i]` in r2
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ffbcc` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020a978c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

