---
# func_02054148 (main, HIGH class A, 112B)

**Recipe:** null-assert guard + count-reload loop + tail frees
**Risk:** loop-count signedness (blt) and assert pool-literal order; res14 load type assumed pointer
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word order mismatch; data_020ffb5c and data_020ffb04 are named globals whose pool-slot order may differ from the orig .s, wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E — visible instruction diff; loop-count signedness (blt vs bne) may produce a different loop epilogue encoding

**Why this recipe hits this mode:** The assert call loads two named data_ globals (data_020ffb5c into r0, data_020ffb04 into r1). MWCC allocates pool slots in first-use order; a declaration-order swap would emit the literals in the wrong sequence, and verify.py wildcards pool-word loads so ninja report is required to surface the mismatch. The blt (signed) vs bne loop condition is an additional visible-mode risk.

**Pool words to verify against orig .s:**
- `data_020ffb5c` — assert file-string pointer, loaded into r0 (first arg to func_020a6d54)
- `data_020ffb04` — assert expr-string pointer, loaded into r1 (second arg to func_020a6d54)

**Verify gate sequence:**
1. verify.py src/main/func_02054148.c func_02054148 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A pool-slot mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Declare data_020ffb5c before data_020ffb04 at the top of the file to match the expected r0/r1 load order for the assert call; check ninja report output to confirm the pool slot ordering
- Loop count must be treated as signed (int i < p->count with blt encoding) — if the loop emits bne, add an explicit `(int)` cast on p->count
- The `res14` field at +0x14 is freed directly as `p->res14` (pointer type) — if the load produces unexpected sign-extension, declare the field as `void *` exactly
- Both tail frees (p->res14 then p itself) must appear in that order after the loop; do not hoist either free into the loop body
- If verify.py shows a count-reload diff, confirm the loop reads p->count fresh each iteration (not cached in a register) — the recipe note says count is reloaded, so avoid assigning it to a local before the loop
---
