---
# func_0203058c (main, HIGH class A, 48B)

**Recipe:** call + dispatch (|| eq) + min/max if-assign tail
**Risk:** case 2||3 may fold to a sub/range check; tail movgt/movle ordering may flip; r4 save assumed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff if `r == 2 || r == 3` folds to a range-subtract (`sub/cmp`) rather than two separate `cmp/cmpne` instructions, or if `movgt r0,#1; movle r0,#0` polarity flips; verify.py catches it

**Secondary mode:** Mode B — callees `func_0202de9c` and `func_020300bc` addresses wildcarded by verify.py BL reloc, only ninja report catches wrong values

**Why this recipe hits this mode:** The `|| eq` pattern `r == 2 || r == 3` can compile as `cmp r0,#2; cmpne r0,#3; beq return_1` or as a range check `sub r0,r0,#2; cmp r0,#1; bls return_1`; these produce different instruction bytes. Separately, `func_020300bc(arg0) > 0` must emit `cmp r0,#0; movgt r0,#1; movle r0,#0` in that polarity — flipping to `movle/movgt` produces a visible byte diff.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203058c.c func_0203058c [--cc all] — instruction diff is visible directly (cmp/cmpne vs range-check, movgt/movle polarity)
2. ninja report — resolves BL relocs for func_0202de9c and func_020300bc; catches Mode B mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write the two-value check as `if (r == 2 || r == 3)` (separate equality tests) rather than a range expression to bias mwcc toward emitting `cmp r0,#2; cmpne r0,#3` instead of a subtracted range-check
- For the boolean tail, use `return func_020300bc(arg0) > 0` (not `!= 0` or `>= 1`) to match the `.s` `cmp r0,#0; movgt r0,#1; movle r0,#0` form
- The `r4` save for `arg0` across the first BL is assumed; declare `arg0` as a function parameter (not a local) so mwcc callee-saves it naturally
- If verify.py shows the `|| eq` compiled as a range-subtract, try extracting `r == 2` and `r == 3` into a temporary: `int eq23 = (r == 2) | (r == 3); if (eq23) return 1;`
- Cross-check each BL offset in the .s against `func_0202de9c` and `func_020300bc` expected addresses via ninja report before sha1 to rule out Mode B
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0202de9c` | main TU call |
| `bl` | `func_020300bc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

