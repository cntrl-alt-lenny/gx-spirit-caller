---
# func_0203de2c (main, HIGH class A, 84B)

**Recipe:** GE-ladder dispatch to tail calls; cached arg in callee-saved r4
**Risk:** func_0203c848 return drives <4 / <5 / ==5 ladder; r4=func_0203c900 result passed to two tails
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee address wildcarded by verify.py BL reloc; only ninja report catches it

**Why this recipe hits this mode:** The risk note flags callee signatures as guessed, meaning the BL target addresses for func_0203dde8, func_0203dde0, func_0203ddc4, and func_0203dc94 are inferred from context; verify.py wildcards all BL relocations, so a wrong address silently passes verify but fails at ninja report / ninja sha1.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203de2c.c func_0203de2c [--cc all] — instruction bytes (BL offsets wildcarded)
2. ninja report — resolves BL target addresses; catches Mode B mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Cross-check each BL target address in the orig .s (`func_0203dde8`, `func_0203dde0`, `func_0203ddc4`, `func_0203dc94`) against the extern declarations; a wrong address passes verify.py but fails at link time
- Ensure `h = func_0203c900(1)` is computed before `func_0203c848()` so mwcc caches `h` in r4 across the second call; if the order is swapped, r4 may hold a different value at the `func_0203ddc4(h)` and `func_0203dc94(h)` tail calls
- The GE-ladder must emit `cmp n, #4 / blt` then `cmp n, #5 / blt` then `cmp n, #5 / beq`; if mwcc emits a jump table instead of a compare chain, add an empty unreachable case to break the density
- `func_0203dde8` and `func_0203dde0` (no `h` arg) must appear before the two `h`-taking tails to match the `.text` ordering
- Verify `func_0203c900` return type is `int` (not `void`) so mwcc issues a callee-saved store of the result into r4
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203c900` | main TU call |
| `bl` | `func_0203c848` | main TU call |
| `bl` | `func_0203dde8` | main TU call |
| `bl` | `func_0203dde0` | main TU call |
| `bl` | `func_0203ddc4` | main TU call |
| `bl` | `func_0203dc94` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

