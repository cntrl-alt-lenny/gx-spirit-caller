---
# func_02048050 (main, HIGH class A, 100B)

**Recipe:** leaf guard + teardown calls + store-order + null global; sp3
**Risk:** arg ordering of func_0204548c (r0=0,r1=f_24,r2=0) guessed; final `data_0219dc78=0` nulls the pointer global itself, type must be ptr
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible instruction diff (arg ordering of func_0204548c)

**Why this recipe hits this mode:** `data_0219dc78` is a named global loaded from the pool multiple times (once for the f_24 guard, again for the 0204548c call, again for the f_24 clear, and once for the final null); verify.py wildcards all those pool loads, so a wrong symbol address passes silently and only `ninja report` catches it. Additionally, the three-argument ordering for `func_0204548c(0, f_24, 0)` is inferred from the .s register assignments (r0=0, r1=f_24, r2=0) — if the C argument order diverges, verify.py will catch the diff.

**Pool words to verify against orig .s:**
- `data_0219dc78` — struct pointer global; loaded from pool four times (guard, call arg, f_24 clear, null store); pool .word must match orig .s

**Verify gate sequence:**
1. verify.py src/main/func_02048050.c func_02048050 [--cc all] [--module main] — instruction bytes; arg-order diff visible here
2. ninja report — resolves pool word for data_0219dc78; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write `func_0204548c(0, data_0219dc78->f_24, 0)` with arguments in exactly that order (r0=0, r1=f_24, r2=0) — if it diverges, the .s register assignments are the ground truth.
- The final null must assign the pointer global itself: `data_0219dc78 = 0;` (type `state_02048050_t *`) — assigning a field (e.g. `data_0219dc78->f_24 = 0` a second time) would not match the `str r1,[r0]` that stores 0 to the global's own address.
- The sequence `func_020403d4(); func_0204037c(); func_0204548c(0, f_24, 0); data_0219dc78->f_24 = 0;` must follow exactly — no reordering; mwcc should reload the global before each step.
- The f_24 guard check `if (data_0219dc78->f_24 != 0)` must be a raw field read, not a cached local, so the struct pointer is reloaded from pool each time.
- Cross-check `data_0219dc78` pool .word in the .s against config/eur/arm9/symbols.txt before running ninja report.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dc78` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020403d4` | main TU call |
| `bl` | `func_0204037c` | main TU call |
| `bl` | `func_0204548c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

