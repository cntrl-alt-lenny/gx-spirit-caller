---
# func_02052ddc (main, HIGH class A, 24B)

**Recipe:** TAIL-CALL thunk via word ptr; field load + arg swap
**Risk:** f_24 offset/sign guessed; pure-tail bx-via-ip should match; return type may be void
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode B (HIDDEN) — BX target `func_0205337c` is a reloc; wrong callee address wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** The entire function body is a single tail call via BX to `func_0205337c`; the BX reloc byte is wildcarded by verify.py, so a wrong callee address is only caught by `ninja report`.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02052ddc.c func_02052ddc [--cc all] [--module main] — instruction bytes
2. ninja report — resolves BX/BL reloc target for func_0205337c; catches Mode B mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- The argument swap is critical: `func_0205337c(x, p->f_24)` — the second parameter `x` becomes arg0 and the field `p->f_24` becomes arg1; swapping them changes r0/r1 at the BX site
- If objdiff shows a frame instead of a pure tail (bx ip), the return type may need to be `void`; try `void func_02052ddc(obj_t *p, int x)` calling without return
- The field offset 0x24 is flagged as guessed — cross-check `[r0, #0x24]` in the orig .s ldr instruction to confirm before committing
- This is a tiny thunk; if bx-via-ip does not appear, try a raw cast: `((int(*)(int,int))func_0205337c)(x, *(int*)((char*)p + 0x24))`
- Cross-check `func_0205337c` address against the .s BX instruction target to confirm the callee is correct
---
