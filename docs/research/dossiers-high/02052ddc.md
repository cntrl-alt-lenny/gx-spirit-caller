# func_02052ddc (main, HIGH class A, 24B)

**Recipe:** TAIL-CALL thunk via word ptr; field load + arg swap
**Risk:** f_24 offset/sign guessed; pure-tail bx-via-ip should match; return type may be void

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02052ddc.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02052ddc.c func_02052ddc --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02052ddc.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
