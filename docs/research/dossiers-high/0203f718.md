---
# func_0203f718 (main, HIGH class A, unknown B)

**Recipe:** trivial two-call wrapper; stack buffer; arg pass-through
**Risk:** frame size 0x18 fixed; if mwcc rounds the single 0x18 buf differently the sub-sp imm shifts
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E (VISIBLE) — frame size rounding may shift the sub-sp immediate; instruction byte diff visible in verify.py

**Why this recipe hits this mode:** the only structural risk is the stack frame size rounding; if mwcc emits `sub sp, #0x20` instead of `sub sp, #0x18` the prologue bytes differ and verify.py catches it immediately as an instruction-byte diff.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203f718.c func_0203f718 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- If verify.py shows a frame-size mismatch (`sub sp, #0x20` vs `#0x18`), reduce the buf declaration or add a dummy local to force the expected alignment
- Both calls pass `&buf[0]`; ensure no intermediate pointer variable is introduced that causes mwcc to spill/reload the address
- arg0 and arg1 are pass-through; they should remain in r0/r1 without callee-save if the function body is short enough for mwcc to keep them in scratch registers
- This is a two-call wrapper with no conditionals; if instruction count looks off, check that neither callee is inlined (both are extern)
- Frame 0x18 must accommodate the buf plus any alignment padding; if mwcc insists on 8-byte alignment the true frame becomes 0x20 — match whatever the orig .s shows
---
