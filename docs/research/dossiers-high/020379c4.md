# func_020379c4 (main, HIGH class A, 40B)

**Recipe:** store-order field clear; void tail call (ldr;bx)
**Risk:** f0e/f10 share -1 via sub-reuse; mwcc may emit mvn vs sub for -1, or split the two -1 stores differently

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/020379c4.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_020379c4.c func_020379c4 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/020379c4.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219b2e0` | data symbol |
| `_LIT1` | `func_02088f94` | func ptr |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

