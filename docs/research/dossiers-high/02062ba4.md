# func_02062ba4 (main, HIGH class A, 116B)

**Recipe:** out-param probe (b+7 arg), middle helper on p+0x50, bool tail
**Risk:** `(char*)p+0x50` add-base vs struct member; reg-alloc of a,b (r5/r4); arg b+7 fold
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/main/func_02062ba4.c func_02062ba4 --cc all` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02062eec` | main TU call |
| `bl` | `func_020613d8` | main TU call |
| `bl` | `func_02062e6c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

