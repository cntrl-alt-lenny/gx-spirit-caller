---
# func_0203f030 (main, HIGH class A, 100B)

**Recipe:** linear call seq; guard-then-tail; store-order for global
**Risk:** data_0219ecdc decl type (u32 vs int*) may flip the str reloc class; callee signatures guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — `data_0219ecdc` is a named pool global; its address is wildcarded by verify.py and only resolved (and checked) by ninja report

**Secondary mode:** Mode B — callee signatures guessed for func_0203edd4 and func_0206e7bc; BL target addresses wildcarded by verify.py, only ninja report catches mismatches

**Why this recipe hits this mode:** `data_0219ecdc` is declared as `extern u32 data_0219ecdc` and used as a store target (`data_0219ecdc = 4`); the linker resolves its address in the pool, which verify.py wildcards. If the address or reloc class is wrong (e.g., u32 vs int* changes the STR encoding), ninja report is the first gate that catches it. Additionally, callee addresses for `func_0203edd4` and `func_0206e7bc` are flagged as guessed.

**Pool words to verify against orig .s:**
- `data_0219ecdc` — global store target for the literal value `4`; STR reloc class depends on declaration type (u32 scalar vs pointer)

**Verify gate sequence:**
1. verify.py src/main/func_0203f030.c func_0203f030 [--cc all] — instruction bytes (pool address and BL offsets wildcarded)
2. ninja report — resolves data_0219ecdc pool address and all BL targets; catches Mode A and Mode B mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Declare `data_0219ecdc` as `extern u32 data_0219ecdc` (scalar, not pointer) so the `str` instruction uses a direct STR reloc rather than a pointer-load-then-store; check the orig .s STR encoding to confirm
- Cross-check BL addresses for `func_0203edd4(v5, a, v4)` and `func_0206e7bc(v4)` against the orig .s; these are the guessed callees most likely to be wrong
- The store `data_0219ecdc = 4` must appear after `func_0203edd4(v5, a, v4)` and before `func_0206e7bc(v4)` — the prep already has this order; verify the STR appears between the two BL instructions in the .s
- `func_0203c900` is called twice with different args (1 and 4); ensure both calls appear in the correct order (1 first, then 4) so mwcc assigns results to v5 and v4 in the right registers
- If `func_0206e7bc` is a tail call in the orig .s (BX instead of BL + return), restructure the C to let mwcc optimise it: `return func_0206e7bc(v4) == 0 ? 0xc : (func_0203c85c(2), 0x11)` — but only if the .s confirms a tail call
---
