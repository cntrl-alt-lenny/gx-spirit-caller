---
# func_0203deac (main, HIGH class A, 68B)

**Recipe:** Tiny scan loop, bit7 test via (b<<24)>>31, leaf bx lr
**Risk:** Offset 0x447 + stride 4 guessed; u8 loop counter wrap (and #0xff) and bls/bcc unsigned compares
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong field offset wildcarded by verify.py (offset 0x447 and stride 4 are guessed); only ninja report catches a mismatch against the linker's relocated address

**Secondary mode:** Mode E — visible instruction byte diff from the lsl/lsr shift pair and unsigned counter wrap (and #0xff / bls); verify.py catches these directly

**Why this recipe hits this mode:** The byte offset `0x447` and stride `4` are explicitly flagged as guessed in the risk note; if the actual struct field is at a different offset, the LDR/LDRB immediate encodes a different value, which verify.py wildcards as a pool-word address constant but ninja report resolves correctly. Additionally the `(b<<24)>>31` shift pair and `(u8)i` counter wrap produce specific lsl/lsr/and instructions that verify.py can compare byte-for-byte.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203deac.c func_0203deac [--cc all] — lsl/lsr/and instruction bytes visible; offset constants compared directly
2. ninja report — catches any offset mismatch that the linker resolves differently
3. ninja sha1 — final gate

## Reshape hints

- If the bit7 shift pair produces the wrong instruction, confirm the order is `lsl #24` then arithmetic `asr #31` (not logical `lsr #31`); the prep uses `>> 31` on a signed int which should emit `asr`, but the comment says `movs r1,r1,lsr#0x1f` — check orig .s and use `(unsigned int)(b << 24) >> 31` if `lsr` is needed
- The `u8` counter `i` must produce `and ip,<reg>,#0xff` at the loop-back edge; declare `i` as `unsigned char` and keep the cast `(unsigned char)(i + 1)` explicit to force the `and` mask
- The loop condition `(u8)i < cnt` must emit an unsigned `bls`/`bcc`; if mwcc emits a signed `blt`, try comparing `(unsigned char)i < (unsigned char)cnt` or switching cnt to `unsigned char`
- Cross-check offset `0x447` against the orig .s LDRB immediate; if wrong, adjust the array base offset from `0x447` to match
- Stride 4 in `i * 4` must emit `lsl #2`; if mwcc emits a multiply instead, ensure `i` is a small integer type so the compiler recognises the power-of-two shift
---
