# func_0202c948 (main, HIGH class A, 88B)

**Recipe:** field stores + conditional-move select (movne/moveq) then store
**Risk:** if/else may branch instead of movne/moveq; base reload-after-call and store order 0x4/0x18/0x14 must hold

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0202c948.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0202c948.c func_0202c948 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0202c948.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
