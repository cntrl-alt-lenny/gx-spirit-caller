---
# func_0200fbd4 (main, HIGH class A, unknown B)

**Recipe:** two linked-list searches (single bucket vs bucket-array) by key@8
**Risk:** data_0218cbc8+0x1000 base; offsets 0x90c/0x914/0x900 from that base; node next@0,key@8; loop-counter/ldrsb shape may differ
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — `data_0218cbc8` is a named pool global; its value is wildcarded by verify.py and only `ninja report` catches a wrong address

**Why this recipe hits this mode:** `data_0218cbc8` is used as a load base (`&data_0218cbc8 + 0x1000`) for all struct field accesses; verify.py wildcards the pool LDR that loads this symbol's address, so an incorrect global address passes instruction-byte checks but breaks `ninja report`/`ninja sha1`.

**Pool words to verify against orig .s:**
- `data_0218cbc8` — list-manager singleton base; loaded as `&data_0218cbc8 + 0x1000` to reach fields at +0x90c (single-list head), +0x914 (bucket count), +0x900 (bucket array)

**Verify gate sequence:**
1. verify.py src/main/func_0200fbd4.c func_0200fbd4 [--cc all] — instruction bytes (pool LDR wildcarded)
2. ninja report — resolves pool word for data_0218cbc8; catches Mode A address mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Confirm `data_0218cbc8` is at address `0x0218cbc8` against the .s pool word; the `+0x1000` addend means the effective base is `0x0218cbc8 + 0x1000 = 0x0218dbc8` (note: `0200fdc0` uses `data_0218dbc8` which appears to be this computed address)
- The type-byte guard `*(signed char *)arg == 5` must use a signed-char cast; `ldrsb` vs `ldrb` changes the instruction and the compare sense
- The single-list path (`==5`) must use `*(Node **)(base + 0x90c)` as the head, and the bucket path must use `*(unsigned int *)(base + 0x914)` for the count and `*(Node **)(base + i*4 + 0x900)` for each bucket head
- Keep `do { ... } while (n != 0)` inner loops (not `while (n != 0) { ... }`) to match the `do`-loop branch shape from the .s
- If the `ldrsb` for the type-byte is missing, check that `*(signed char *)arg` is not decaying to `*(char *)arg` through a typedef
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0218cbc8` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

