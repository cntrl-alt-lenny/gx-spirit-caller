---
# func_0201a3ec (main, HIGH class B, ?B)

**Recipe:** Bind-base GetSystemWork + reload field, lsl/lsrs#31 bit-test guard, orr-set+store
**Risk:** the empty-if/else to mirror beq fall-through may invert; mwcc could pick movs/lsr operand reg differently. Offset 0x8f8 from siblings.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible diff from inverted empty-if/else branch direction and lsl/lsr operand-reg selection

**Why this recipe hits this mode:** The bit-test `((v << 0x16) >> 0x1f) == 0` must emit as a `movs`/`lsr` pair with the branch on the zero flag; the empty `if` body with code in the `else` mirrors a `beq fall-through` pattern that mwcc may invert, swapping which branch is conditional — resulting in a visible instruction-byte diff.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_0201a3ec.c func_0201a3ec [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- Write the bit-test as `(unsigned int)((unsigned int)v << 0x16) >> 0x1f` with an explicit empty `if` body and code in the `else` to mirror the `beq` fall-through shape; if mwcc inverts it, try removing the empty `if` and negating the condition instead.
- Keep `sys = GetSystemWork()` bound in a local `char *sys` across both the store-read and the `func_0200a26c` call so r4 holds the base the entire function.
- The field offset 0x8f8 is shared with sibling functions — cross-check it against the orig .s `ldr`/`str` displacements.
- For the `orr-set`: write `*(int *)(sys + 0x8f8) = v | 0x200;` directly; do not reread the field before the orr.
- If the movs/lsr operand order differs, try swapping the shift operands: `(v << 0x16) >> 0x1f` vs computing `v >> 9` directly — only the shift-pair form matches the orig encoding.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190c0` | main TU call |
| `bl` | `func_0201a3b8` | main TU call |
| `bl` | `func_0200a26c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

