---
# func_02041520 (main, HIGH class A, unknown B)

**Recipe:** ands/bicne mask-clear idiom (if-test then in-place clear)
**Risk:** mwcc may emit tst;bicne or fold to a plain bic instead of ands;bicne — the if-guard form is the steer
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E (VISIBLE) — mwcc instruction selection between ands/bicne vs tst/bicne vs plain bic is sensitive to the C form of the conditional; any mismatch is a visible instruction-byte diff caught by verify.py

**Why this recipe hits this mode:** the recipe relies on mwcc emitting `ands` (which sets flags) followed by `bicne` (conditional clear); alternative idioms (`tst` + `bicne`, or an unconditional `bic`) differ in byte layout. The if-guard form `if (n & 0x8000) { n &= ~0x8000; }` is specifically chosen to steer mwcc toward the ands/bicne pair.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02041520.c func_02041520 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Use the explicit if-guard form `if (n & 0x8000) { n &= ~0x8000; }` rather than a ternary or unconditional `n &= ~0x8000` to steer mwcc toward the ands/bicne idiom
- The constant `0x8000` must be representable as an ARM immediate for both the and-test and the bic-clear; `~0x8000` encodes as `bic rX, rX, #0x8000` — verify the orig .s uses the same constant form
- If mwcc emits `tst` + `bicne` instead of `ands` + `bicne`, try replacing the in-place clear with `n = n & ~0x8000` inside the if-body to force mwcc to reconsider
- If mwcc folds to an unconditional `bic`, the if-guard must be present and the return value must depend on the conditionally-cleared `n` to make the branch observably necessary
- This is a single-register, no-memory function with no pool words; verify.py is the definitive gate and will catch any instruction form mismatch immediately
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

