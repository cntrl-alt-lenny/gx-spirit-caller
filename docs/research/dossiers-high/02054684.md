---
# func_02054684 (main, HIGH class A, 124B)

**Recipe:** double null-test + free-each loop + two tail frees
**Risk:** loop-count signedness; assert pool order; items load type assumed pointer
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word order mismatch; data_020ffb8c and data_020ffb80 are named globals whose pool-slot order may differ from the orig .s, wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E — visible instruction diff; loop-count signedness and the double null-check pattern (assert then early-return void) affect branch encoding

**Why this recipe hits this mode:** The assert call loads data_020ffb8c into r0 and data_020ffb80 into r1. These two pool literals share the slot with 02054338/020543b8's data_020ffb80 by value but their declaration order in this TU controls the pool slot assignment — a swap is invisible to verify.py. The double null-test (first asserts, second returns void) also contributes Mode E risk if MWCC merges them.

**Pool words to verify against orig .s:**
- `data_020ffb8c` — assert file-string pointer, loaded into r0 (first arg to func_020a6d54); note different string from 02054338/020543b8
- `data_020ffb80` — assert expr-string pointer, loaded into r1 (second arg to func_020a6d54)

**Verify gate sequence:**
1. verify.py src/main/func_02054684.c func_02054684 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A pool-slot mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep both null checks structurally separate: first `if (p == 0) func_020a6d54(...)` (assert, no return), then `if (p == 0) return` (early-return void) — do not merge or reorder
- Declare data_020ffb8c before data_020ffb80 to match the r0/r1 assert load sequence; confirm with ninja report
- Loop counter `i` must be signed `int` with `i < p->count` to emit blt; if bcc appears, make the count field `int` explicitly in the struct
- `p->items` must be declared as `void **` (pointer-to-pointer) so the indexed load `p->items[i]` emits a word-size ldr without sign-extension
- After the loop, free `p->items` (the array itself, at +0x00) before freeing `p` (self) — reversal of this order would change the sequence of BL func_020453b4 instructions
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ffb8c` | data symbol |
| `_LIT1` | `data_020ffb80` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020a6d54` | main TU call |
| `bl` | `func_02054148` | main TU call |
| `bl` | `func_020453b4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

