# func_02046ae0 (main, HIGH class A, 92B)

**Recipe:** null-guard + byte dispatch + (call+1) tails; sp3 tier
**Risk:** b369 must be re-loaded per compare (two ldrb) — caching in a local would emit one ldrb and diverge.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02046ae0.c)

**Verify gate:**
1. python tools/verify.py src/main/func_02046ae0.c func_02046ae0 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/02046ae0.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
