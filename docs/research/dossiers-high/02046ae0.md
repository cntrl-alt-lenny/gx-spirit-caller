---
# func_02046ae0 (main, HIGH class A, 92B)

**Recipe:** null-guard + byte dispatch + (call+1) tails; sp3 tier
**Risk:** b369 must be re-loaded per compare (two ldrb) — caching in a local would emit one ldrb and diverge
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible instruction diff (ldrb count)

**Why this recipe hits this mode:** `data_0219daec` is a named global loaded twice from the literal pool (once for the null guard and once for the b_369 byte reads); verify.py wildcards pool loads, so a wrong symbol address passes silently. Additionally the b369 byte must be re-read with two separate LDRB instructions per the .s — if a local cache is used instead, only one LDRB is emitted and verify.py catches the byte diff immediately.

**Pool words to verify against orig .s:**
- `data_0219daec` — null-guard load base and b_369 struct base; pool .word must match orig .s (loaded twice)

**Verify gate sequence:**
1. verify.py src/main/func_02046ae0.c func_02046ae0 [--cc all] [--module main] — instruction bytes; ldrb-count diff visible here
2. ninja report — resolves pool word for data_0219daec; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Do NOT cache `data_0219daec->b_369` in a temp; write both comparisons as direct struct member accesses so mwcc emits two LDRB r0,[r1,#0x369] instructions.
- Write the condition as two separate `||` arms (`== 2 || == 3`) not a range test (`>= 2 && <= 3`) — the .s shows separate cmp+beq / cmp+bne pairs.
- The `+ 1` on each tail's return value: write `return func_0204fa24() + 1;` (not a temp), so mwcc emits the ADD r0,r0,#1 immediately after the BL return.
- The null guard must use the global itself (`if (data_0219daec == 0)`), not a pointer local — the .s loads ldr r0,=data_0219daec then ldr r1,[r0], so the guard checks the pointer variable directly.
- Cross-check `data_0219daec` pool .word in the .s against config/eur/arm9/symbols.txt before running ninja report.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219daec` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0204fa24` | main TU call |
| `bl` | `func_0204fa50` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

