---
# func_0203f740 (main, HIGH class A, unknown B)

**Recipe:** stack-buffer; compare returning bool 1/0 (moveq/movne)
**Risk:** frame 0x1c vs 0x18 in siblings — buf size guessed; eq->1/ne->0 order must match (==0 form chosen)
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named pool global `data_020fe550` used as load base; wrong value wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E (VISIBLE) — moveq/movne return-bool order; eq->1/ne->0 must match orig .s conditional move sequence

**Why this recipe hits this mode:** `data_020fe550` is a pool-loaded address for the comparison; verify.py BL-wildcards it so a wrong address passes instruction checks but fails the linked ROM. Additionally, the bool return uses conditional moves whose order is sensitive to the `== 0` vs `!= 0` form of the comparison.

**Pool words to verify against orig .s:**
- `data_020fe550` — comparison base passed as second arg to `func_020a7440` (8-byte compare target)

**Verify gate sequence:**
1. verify.py src/main/func_0203f740.c func_0203f740 [--cc all] — instruction bytes (catches Mode E moveq/movne order)
2. ninja report — resolves pool words; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The `== 0` form (`if (... == 0) return 1; return 0;`) is chosen to produce moveq->1 / movne->0; if the orig .s has the opposite order, flip to `!= 0` form
- Frame 0x1c is one word larger than the 0x18 siblings; if verify.py shows a frame mismatch, try `u8 buf[0x1c]` vs `u8 buf[0x18]` and pick whichever the orig .s shows in the prologue
- `data_020fe550` pool word must match; cross-check its LDR pool entry in the orig .s against the actual symbol address
- No arg1 in this variant (single-arg form); ensure the function signature is `int func_0203f740(void *arg0)` with no extra params
- If moveq/movne still mismatches after the form flip, try rewriting as `return (func_020a7440(...) == 0) ? 1 : 0;` to steer mwcc's conditional-move selection
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020fe550` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203f590` | main TU call |
| `bl` | `func_020a7440` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

