# func_021cb1c0 (ov002, HIGH class A, 124B)

**Recipe:** tst guard; two calls; bic clear-bit; bound const for r3=r2 reuse
**Risk:** mwcc may emit mov r3,#1 instead of mov r3,r2 despite local binding; field offsets guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay002/func_ov002_021cb1c0.c func_ov002_021cb1c0 --cc all --module ov002` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov002_022d016c` | raw |
| `_LIT1` | `data_ov002_022cf26c` | raw |
| `_LIT2` | `0x000019b8` | MMIO/const |
| `_LIT3` | `data_ov002_022cfa6c` | raw |
| `_LIT4` | `data_ov002_022ce950` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021b218c` | overlay call (ov002) |
| `bl` | `func_ov002_021b2ab4` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Read the orig .s pool section; C must reference the same globals in the same ORDER
- Named `data_XXXX` globals are pool-word risks; wrong address silently passes verify.py
- Use `ninja report` (not just `ninja objdiff`) before sha1

## Reference

- Prep: `docs/research/c-match-prep/021cb1c0.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
