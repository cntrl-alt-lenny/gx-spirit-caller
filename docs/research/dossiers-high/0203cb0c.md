# func_0203cb0c (main, HIGH class B, 52B)

**Recipe:** shifted-add address (acc-first), Copy32 call wrapper
**Risk:** Copy32 arg order (src,dst,n) assumed; add base+(a0<<8) must keep base as first add operand; func_0203c900 retyped char* (was void*).

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203cb0c.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0203cb0c.c func_0203cb0c --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0203cb0c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
