# func_0205bc24 (main, HIGH class A, 168B)

**Recipe:** DISPATCH switch on field, result store-if-nonzero, default assert
**Risk:** jump-table layout/case3-into-default ordering; the default error block's two-call sequence + sub sp,#4 may schedule differently
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/main/func_0205bc24.c func_0205bc24 --cc all` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02100a8c` | data symbol |
| `_LIT1` | `data_02100ad4` | data symbol |
| `_LIT2` | `data_02100ad8` | data symbol |
| `_LIT3` | `0x00000146` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02058a08` | main TU call |
| `bl` | `func_0205d7e4` | main TU call |
| `bl` | `func_0205af90` | main TU call |
| `bl` | `func_0205febc` | main TU call |
| `bl` | `func_020604a4` | main TU call |
| `bl` | `func_020a6d54` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

