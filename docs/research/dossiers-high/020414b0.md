---
# func_020414b0 (main, HIGH class A, unknown B)

**Recipe:** blx through ctx+0x1000 fn-ptr; movs-arg guard; store-order field set then add
**Risk:** field offsets/types of Out guessed; the [r4] reload before the add and store-order (0x4,0xc,8) must match; ctor sig
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named pool global `data_020fe8d4` used as first argument to the function-pointer call; wrong value wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E (VISIBLE) — store-order for `out->p`, `out->q`, `out->len`, `out->end` must match orig .s sequence exactly; any reordering produces a visible instruction-byte diff

**Why this recipe hits this mode:** `data_020fe8d4` is a pool-loaded u32 passed as the `arg` parameter to the `ctor` function-pointer; verify.py wildcards this LDR pool word so the wrong value passes instruction checks silently. Additionally, the four struct-field stores have a specific order in the orig .s that must be reproduced exactly.

**Pool words to verify against orig .s:**
- `data_020fe8d4` — first argument (`arg`) passed to the `ctor` function pointer

**Verify gate sequence:**
1. verify.py src/main/func_020414b0.c func_020414b0 [--cc all] — instruction bytes (catches Mode E store-order)
2. ninja report — resolves pool words; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write the struct stores in the order the risk note specifies: `out->q` (0x4), then `out->len` (0xc), then `out->end` (0x8 via `out->p + out->len`); reordering changes which registers mwcc schedules and produces a byte diff
- The function-pointer load `*(Ctor_020414b0 *)((char *)ctx + 0x1000 + 0x10)` must use the combined offset form so the LDR immediate encodes as `0x1010`; avoid introducing an intermediate `char *` base variable that forces a two-instruction sequence
- Verify `data_020fe8d4` pool word in the orig .s LDR pool before declaring a match; it appears once as the `arg` to the ctor
- The `movs`-arg guard `if (n == 0) return 0` should produce a `movs`-on-parameter or `cmp r2, #0` idiom; if verify.py shows the wrong condition instruction, try `if (!n)` or `if (n == 0)` to steer mwcc
- The `[r4] reload before the add` means `out->p` is re-read from the struct to compute `out->p + out->len` rather than reusing the register; write `out->end = out->p + out->len;` without binding `out->p` to a local to trigger the reload
---
