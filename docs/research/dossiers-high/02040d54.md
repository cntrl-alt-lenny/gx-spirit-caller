---
# func_02040d54 (main, HIGH class A, unknown B)

**Recipe:** guard chain (movs/moveq, cmp/movge) + tail call returning bool
**Risk:** arg3 must be signed for the movge (>= signed); h held in r4 across calls; movs-on-call-result idiom
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E (VISIBLE) — movs/moveq and cmp/movge instruction order is sensitive to how the guards are written; any deviation produces a visible instruction-byte diff caught by verify.py

**Why this recipe hits this mode:** the two-stage guard uses `movs` (set flags on call result) for the null check and `cmp/movge` for the signed comparison; mwcc's idiom selection depends on the exact form of the conditions and the signedness of `arg3`. Wrong signedness or guard form flips the conditional-move instruction.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02040d54.c func_02040d54 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Declare `arg3` as `int` (signed) so mwcc emits `cmp / movge` (signed greater-or-equal); `u32` would emit `movcs` (unsigned carry-set) which is wrong
- The null check `if (h == 0)` should produce a `movs`-on-call-result pattern; if verify.py shows a plain `cmp r0, #0`, try assigning the result directly: `void *h = func_02040de8(...); if (h == 0)`
- `h` must be held across the `func_020aaddc()` call in r4; declare it as a local `void *h` so mwcc spills it to a callee-saved register
- The final `func_020aadf8(arg2, h)` followed by `return 1` is a near-tail call; verify mwcc does not tail-optimise it (if it does, the return-1 disappears — add a volatile barrier or reorder)
- If the guard chain order differs from the orig .s, try combining the two conditions or splitting the `return 0` paths to let mwcc re-select its preferred conditional-move idiom
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02040de8` | main TU call |
| `bl` | `func_020aaddc` | main TU call |
| `bl` | `func_020aadf8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

