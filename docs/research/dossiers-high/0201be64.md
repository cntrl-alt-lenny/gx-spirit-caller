---
# func_0201be64 (main, HIGH class A, ?B)

**Recipe:** Guard (==2) + bit-extract-to-bool via shift pair (lsl#7;lsrs#0x1f)
**Risk:** func_020139b4 arg passing incidental (r0=leftover ptr); +0x900 offset and bit-24 test guessed.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible diff if the lsl#7/lsrs#0x1f shift pair emits with wrong shift counts or the zero-return encoding differs

**Why this recipe hits this mode:** The bit-24 test `(x << 7) >> 31` must emit as a specific `lsl`/`lsrs` pair so the zero flag is set for the conditional return; if mwcc emits a different shift count or a plain `tst`/`and` form, the instruction bytes differ visibly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_0201be64.c func_0201be64 [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- Write the bit-test as `(*(int *)(p + 0x900) << 7) >> 31 == 0` with the signed shift pair; if mwcc emits `tst` or `and` instead of `lsl`/`lsrs`, try `((unsigned)(*(int *)(p+0x900) << 7)) >> 31`.
- The `func_020139b4()` call uses `r0=leftover ptr` incidentally — keep `p = GetSystemWork()` assigned before the call so p is in r4 when func_020139b4 is called (no explicit arg needed).
- Field offset +0x900 is guessed — cross-check the `ldr` displacement in the orig .s before claiming a match.
- Keep the early-return structure: `if (n != 2) return 0; if (bit == 0) return 1; return 0;` to match the orig branching shape rather than a nested ternary.
- If the final `return 0;` and `return 1;` swap, invert the bit-test condition (test for non-zero instead).
---
