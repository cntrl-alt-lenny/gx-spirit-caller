---
# func_02055170 (main, HIGH class A, 72B)

**Recipe:** stack marshalling, literal args, !=-1 guard, return out
**Risk:** same as 02055128: out-on-failure-path uninitialised + dual &len passing; clone, so matches iff 02055128 does
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (ldrne on success path, uninitialised-out on failure path, dual &len passing)

**Why this recipe hits this mode:** Identical pitfall to `02055128` — the orig emits a conditional `ldrne` for the success path and falls through with `out` unread on failure; mwcc must emit the same uninitialised-variable load-on-not-equal pattern, which is hard to force without the specific dual-return form.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02055170.c func_02055170 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- This is an exact clone of `02055128` with the second literal changed from `0x1001` to `0x1002`; use the same C form and only change that one constant.
- If `02055128` does not match, do not attempt this function independently — fix `02055128` first, then apply the same structural fix here.
- The dual `return out` structure (one inside the if, one after) must be preserved to produce the single `ldrne` without a conditional branch; do not consolidate.
- `len = 4` must remain a stack-allocated `int` at the correct offset; do not promote it to a register variable or inline constant.
- The literal `0x1002` must not be simplified by the compiler; confirm `mov r1, #0x1002` (or equivalent) appears in the diff and not `add r1, r1, #1` off a cached `0x1001`.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x0000ffff` | MMIO/const |
| `_LIT1` | `0x00001002` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02054c78` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

