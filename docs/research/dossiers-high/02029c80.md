---
# func_02029c80 (main, HIGH class A, 192B)

**Recipe:** bitfield :1 (lsl;lsr), if/else dispatch, tail-call groups
**Risk:** struct offsets/padding guessed; bit2 position and the 2x2 branch nesting order may flip on objdiff
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; 2x2 branch nesting order and bitfield extract encoding are directly visible to verify.py
**Secondary mode:** Mode B — BL targets (`func_02092904`, `func_0208f458`, `func_0208f3e4`, `func_0208f38c`, `func_0208ff1c`, `func_0208f284`, `func_0208f210`, `func_0208f1c4`, `func_0208feb4`) are extern guessed addresses; wrong callees wildcarded by BL reloc, only ninja report catches

**Why this recipe hits this mode:** The nesting order of `(f_18c != 0)` outer and `flag` inner (or vice versa) and which branch is the fall-through vs taken path produce visible instruction differences. The `bit2` lsl;lsr extract encoding is also instruction-visible. All nine callee addresses are guessed extern symbols whose BL relocations are wildcarded by verify.py.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02029c80.c func_02029c80 [--cc all] — instruction bytes (2x2 nesting and bitfield extract visible)
2. ninja report — resolves BL targets; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The `bit2` field is declared `:1` at bit-position 2 (after `b0:2`) — confirm the struct bitfield ordering matches; the `lsl#29;lsr#31` shift pair in the orig confirms bit-2 extraction
- If objdiff shows the outer/inner branch order flipped, try swapping: `if (flag)` outer and `if (self->f_18c != 0)` inner — test both 2x2 nestings since the compiler may prefer either order
- The `tmp = self->f_84` vs `tmp = self->f_90` selection (based on `bit2`) must occur before `func_02092904()` — do not reorder the bit test after the call
- `flag = tmp & 1` must use raw `& 1` mask (not a `:1` bitfield) to match the `and r,r,#1` instruction; lsl;lsr is used only for the `bit2` extraction
- Struct offsets 0x84 (f_84), 0x90 (f_90), 0x18c (f_18c), 0x19a (bitfield) are guessed — cross-check against orig .s ldr/ldrsh/ldrh immediate values if objdiff shows layout errors
- Cross-check all nine BL callee addresses against orig .s BLX instructions via `ninja report` before declaring a sha1 match
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02092904` | main TU call |
| `bl` | `func_0208f458` | main TU call |
| `bl` | `func_0208f3e4` | main TU call |
| `bl` | `func_0208f38c` | main TU call |
| `bl` | `func_0208ff1c` | main TU call |
| `bl` | `func_0208f284` | main TU call |
| `bl` | `func_0208f210` | main TU call |
| `bl` | `func_0208f1c4` | main TU call |
| `bl` | `func_0208feb4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

