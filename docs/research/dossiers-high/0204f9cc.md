---
# func_0204f9cc (main, HIGH class A, 88B)

**Recipe:** 32-bit bit-scan count loop (RELOAD global accessor), leaf
**Risk:** func_020498f0 reloaded every iter (matches asm); field 0x2f0 int, 0xe byte; ands+addne order
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; ands+addne ordering caught by verify.py

**Secondary mode:** Mode B — BL to func_020498f0 wildcarded by verify.py reloc

**Why this recipe hits this mode:** The loop body must emit `ands` followed by `addne` for each iteration; any reshape that hoists the accessor, reorders the bit-test and increment, or changes the operand order of the `&` expression will produce a visible instruction diff that verify.py catches directly. The BL reloc for the accessor is wildcarded and needs ninja report confirmation.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0204f9cc.c func_0204f9cc [--cc all] — instruction diff (ands/addne order) is visible directly
2. ninja report — resolves BL target for func_020498f0; catches Mode B callee mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Start `count` at `-1` (mvn r6, #0) so that the first matching bit increments it to 0, making the final value the zero-based count of set bits; initialising at 0 shifts all counts by one.
- The accessor `func_020498f0()` must be reloaded inside the loop body on every iteration — the asm reloads per iter; hoisting the pointer out of the loop changes the instruction schedule visibly.
- Write the bit-test as `(1u << i) & func_020498f0()->mask` (shift operand on the left of `&`) to match the asm operand order and produce `ands` with the expected register layout.
- Use `unsigned int` for the mask field at +0x2f0 to force an unsigned `ands` instruction; a signed field type may produce different condition codes or instruction shape.
- For the byte store after the loop, write `*(unsigned char *)((char *)func_020498f0() + 0xe) = (unsigned char)count` rather than a struct field, to keep the same byte-store addressing pattern the asm uses.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020498f0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

