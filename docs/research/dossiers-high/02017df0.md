---
# func_02017df0 (main, HIGH class A, 132B)

**Recipe:** GetSystemWork bitfield-guard + cmp/beq state chain (1,2), guard-chains + RMW
**Risk:** 0x8d8&1 guard -> `and #1` wall; literal-pool message loads and call ordering may diverge
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and #1` wall and call/RMW ordering within state arms

**Why this recipe hits this mode:** The `(v<<31)>>31` entry guard is the canonical `and #1` wall visible immediately in verify.py. Within state 1 the ordering of the message-id store, `func_0201aabc(0x7b)`, and `|= 0x1000000` RMW must match the .s sequence; within state 2 the store then `func_0201aaf4`, then `func_0201b7b4`, then `&= ~0x1000000` ordering is similarly fixed.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02017df0.c func_02017df0 [--cc all] — instruction bytes (and #1 wall + call order visible)
2. ninja sha1 — final gate

## Reshape hints

- State 1 ordering must be: message-id store (`0x00280100`), `func_0201aabc(0x7b)`, `*(int *)(w+0x900) |= 0x1000000` — verify .s instruction sequence for any swap
- State 2 ordering must be: message-id store (`0x00280200`), `func_0201aaf4(0x7b)`, `func_0201b7b4()`, `*(int *)(w+0x900) &= ~0x1000000` — the `&=` after the two calls is critical
- Keep state chain as two `if (*(int *)(w+0x920) == N)` blocks rather than a switch — two arms are below the jump-table threshold and must stay as cmp/beq pairs
- The `func_0201b7e0(o) != 3` guard in state 2 (passing the task pointer) must remain as the first guard in that arm, before the `func_02019664` check
- The `w+0x8d8` bit0 guard uses the `<<0x1f; >>0x1f` shift idiom — do not convert to `& 1`; if verify shows a single `and` instruction this candidate is .s-bound at that line
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00280100` | MMIO/const |
| `_LIT1` | `0x00280200` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190ec` | main TU call |
| `bl` | `func_020139c4` | main TU call |
| `bl` | `func_0201aabc` | main TU call |
| `bl` | `func_0201b7e0` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_0201aaf4` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

