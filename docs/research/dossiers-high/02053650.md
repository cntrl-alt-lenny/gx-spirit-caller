---
# func_02053650 (main, HIGH class A, 56B)

**Recipe:** GUARD early-return + AND-mask bit-test bool
**Risk:** (x&4)==4 may compile via tst/movne differently than cmp #4; otherwise clean leaf — low risk
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; `tst #4`/`movne` vs `and`/`cmp #4`/`moveq` emit different bytes, caught directly by verify.py

**Why this recipe hits this mode:** the bit-test `(func_020536a0(self) & 4) == 4` can lower as `and r0, r0, #4 / cmp r0, #4 / moveq r0, #1 / movne r0, #0` or as `tst r0, #4 / movne r0, #1 / moveq r0, #0`; the two sequences are different bytes and verify.py catches the mismatch immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (`#3`, `#4`) are compared directly by verify.py.

**Verify gate sequence:**
1. verify.py src/main/func_02053650.c func_02053650 [--cc all] — instruction diff visible directly (tst vs and+cmp, movne/moveq order)
2. ninja sha1 — final gate

## Reshape hints

- If verify.py shows `tst r0, #4 / movne` but the recipe emits `and / cmp #4 / moveq`, try the explicit `!= 0` form: `return (func_020536a0(self) & 4) != 0` — this is more likely to emit a `tst` + `movne` pair than the `== 4` comparison
- Conversely if the orig .s uses `and / cmp #4`, keep `== 4` exactly as written in the prep
- The guard `func_02053688(self) != 3` should emit `cmp r0, #3 / moveq r0, #0 / bxeq lr` (or `beq` to a return); confirm the early-return branch is a conditional `bx lr` not a branch to a tail block
- r4 holds `self` across both calls; ensure no intermediate variable shadows it and forces a reload
- This is a high-confidence leaf — if verify.py shows 0 diffs, go straight to `ninja sha1`
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02053688` | main TU call |
| `bl` | `func_020536a0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

