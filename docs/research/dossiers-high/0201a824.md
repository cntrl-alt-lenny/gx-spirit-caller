---
# func_0201a824 (main, HIGH class A, ?B)

**Recipe:** Accessor + byte-extract eq/ne to bool (== 0)
**Risk:** field-at-+8 offset and func_02018bc0 ptr-return assumed; bool codegen should match movs/moveq.
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible diff if the `(u8)x == 0` bool codegen emits `cmp`/`moveq`/`movne` rather than `movs`/`moveq`

**Why this recipe hits this mode:** The byte-extract `(u8)*(int *)(p+8)` must flag via `movs` (setting the zero flag on the low byte) so `moveq r0,#1` / `movne r0,#0` encode correctly; if mwcc emits a separate `cmp r0,#0` the instruction bytes differ visibly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_0201a824.c func_0201a824 [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- Cast as `(u8)*(int *)(p + 8) == 0` where `u8` is `typedef unsigned char u8`; this nudges the compiler toward the `movs`-and-`moveq` form for the zero test.
- Keep `GetSystemWork()` as a leading discarded call; its result is unused but the BL must appear.
- If the bool codegen emits `cmp r0,#0; moveq/movne` instead of `movs; moveq/movne`, try `!(*(unsigned char *)(p + 8))` or `(*(unsigned char *)(p + 8)) == 0u` to force the flagging form.
- Field offset +8 is assumed — cross-check the `ldr`/`ldrsb` displacement in the orig .s if confidence remains medium after first compile.
- High-confidence recipe: attempt as-is first; only reshape if the initial objdiff shows a specific instruction pattern mismatch.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_02018bc0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

