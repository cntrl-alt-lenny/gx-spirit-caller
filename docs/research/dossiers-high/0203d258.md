# func_0203d258 (main, HIGH class A, 144B)

**Recipe:** Guarded lookup loop, stride 0x24; byte-key match + helper; indexed byte result
**Risk:** Stride 0x24, offsets 0x3,0x1,0x4,0xa,0xd12 guessed; r2==0x20 early-exit; ldreqb signed result

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203d258.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0203d258.c func_0203d258 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0203d258.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
