---
# func_02020398 (main, HIGH class A, 44B)

**Recipe:** guard, switch-on-field, struct callback blx with reordered args
**Risk:** callback at 0x1c held in r3 across; arg marshalling r0=m12,r1=mc,r2=m10 ordering vs source; offsets 0xc/0x10/0x12 guessed
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — named global data_02191f40 used as pool base for the callback pointer at field +0x1c; wildcarded by verify.py, only ninja report catches mismatches
**Secondary mode:** Mode E — visible instruction byte diff (callback held in r3 across the switch; arg order m12/mc/m10 may differ from source order)

**Why this recipe hits this mode:** The callback pointer at data_02191f40.f1c must be loaded once and held in a register across the switch's case arms; the specific register chosen (r3 per the risk note) and the argument marshalling order (r0=m12, r1=mc, r2=m10, which is not the struct-field declaration order) both affect instruction bytes in ways verify.py will catch.

**Pool words to verify against orig .s:**
- `data_02191f40` — struct base; field +0x1c loaded as callback pointer and held across cases

**Verify gate sequence:**
1. verify.py src/main/func_02020398.c func_02020398 [--cc all] — instruction bytes
2. ninja report — resolves pool word data_02191f40; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Load the callback into a local before the switch so the compiler holds it in a register across all cases: `void (*cb)(...) = (void(*)(...))data_02191f40.f1c;` — this mirrors the r3-hold pattern noted in the risk
- Match the exact arg order for each call: case 0x15 passes (p->m12, p->m10, p->mc) in that order (r0=m12, r1=m10, r2=mc per the risk note, not the source order declared in the struct); verify against the .s BLX arg registers
- Field offsets 0xc/0x10/0x12 are guessed; if verify.py shows different offset values in the loads, adjust the Msg struct padding (the pad6 array between m4 and mc)
- The two case values 0x15 and 0x9 are not contiguous, so MWCC should emit a cmp-chain rather than a jump table — confirm this in the .s; if a table appears, the constants may be different
- The `p->m2 != 0` entry guard returns void (calls func_0201f19c then returns); keep it as the first check before the callback load so the compiler does not hoist the pool load above the guard
---
