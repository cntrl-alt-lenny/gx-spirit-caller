# func_02060b84 (main, HIGH class A, 140B)

**Recipe:** global-guard + counted linear search over indexed array, -1 sentinel
**Risk:** global reloaded (r8) each loop iter matches asm; element field offsets 0/4/8 fixed; count-recompute placement may shift
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_02060b84.c func_02060b84 --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219e518` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02054140` | main TU call |
| `bl` | `func_020540d0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

