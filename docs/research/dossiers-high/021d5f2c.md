# func_021d5f2c (ov002, HIGH class B, 136B)

**Recipe:** byte-unpack arg0 (signed >>8), sentinel branch on 0xffff, chained sink calls
**Risk:** heavy callee-saved holder allocation (r5-r9) for the unpacked bytes; this is the reg-mirror/reg-alloc class flagged as a wall — likely 1-2 regs off
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay002/func_ov002_021d5f2c.c func_ov002_021d5f2c --cc all --module ov002` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode B mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x0000ffff` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021b3618` | overlay call (ov002) |
| `bl` | `func_ov002_021b34f4` | overlay call (ov002) |
| `bl` | `func_ov002_021d5ab4` | overlay call (ov002) |
| `bl` | `func_ov002_021d5d58` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Cross-check every BL/BLX target address in the .s against your `extern func_XXXX` declarations
- BL relocs are wildcarded by verify.py; only `ninja report` resolves them
- Wrong callee address passes verify.py at 100% but fails sha1

## Reference

- Prep: `docs/research/c-match-prep/021d5f2c.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
