# func_02045c7c (main, HIGH class A, 60B)

**Recipe:** stack-arg spill (5th-7th) + cached-arg sequential calls
**Risk:** 7-arg shape inferred from spills; mwcc arg-evaluation/spill order for the stack args may reorder; a3 passed twice

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02045c7c.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02045c7c.c func_02045c7c --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02045c7c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
