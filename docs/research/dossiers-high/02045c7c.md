---
# func_02045c7c (main, HIGH class A, 60B)

**Recipe:** stack-arg spill (5th-7th) + cached-arg sequential calls
**Risk:** 7-arg shape inferred from spills; mwcc arg-evaluation/spill order for the stack args may reorder; a3 passed twice
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible spill-order instruction diff

**Why this recipe hits this mode:** `data_020ff8fc` is named and used as the 3rd register argument, so it is loaded from the literal pool; verify.py wildcards that pool load, meaning a wrong value silently passes verify.py but fails `ninja report` / `ninja sha1`. Additionally, the order mwcc emits STR/PUSH instructions for the three stack-spill args (a0, a3, a1) may differ from the original, producing a visible byte diff.

**Pool words to verify against orig .s:**
- `data_020ff8fc` — 3rd register arg to func_0209150c; its pool .word must match the original .s literal

**Verify gate sequence:**
1. verify.py src/main/func_02045c7c.c func_02045c7c [--cc all] [--module main] — instruction bytes; spill-order divergence shows here
2. ninja report — resolves pool word for data_020ff8fc; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write stack args in the exact left-to-right order the .s spills them: 5th = a0, 6th = a3, 7th = a1 — match that position precisely in the call expression.
- Cross-check `data_020ff8fc` address in the .s pool `.word` against its entry in config/eur/arm9/symbols.txt before running ninja.
- The `a3` duplicate (4th register arg AND 6th stack arg) — if mwcc re-evaluates it for the stack slot, try binding `int saved_a3 = a3;` before the call so the compiler reuses one value.
- If the `func_020aaddc(a2)` epilogue emits a reload instead of reuse, cache a2 explicitly as a callee-saved local before the 7-arg call.
- For persistent spill-order divergence, try moving the 0x1000 constant to a named `int` local so mwcc evaluates it earlier and spills variables from right to left.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ff8fc` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0209150c` | main TU call |
| `bl` | `func_020aaddc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

