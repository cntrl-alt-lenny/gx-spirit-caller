# func_02063548 (main, HIGH class A, 216B)

**Recipe:** sparse switch (cmp-chain) on opcode, per-case bool-return dispatch
**Risk:** hoisted a=x+3/b=y-3 before switch; per-case `if(f)return 1;return 0` vs ternary; default=1 placement; sp3 frame
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/main/func_02063548.c func_02063548 --cc all` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02063808` | main TU call |
| `bl` | `func_0206371c` | main TU call |
| `bl` | `func_02063710` | main TU call |
| `bl` | `func_02063664` | main TU call |
| `bl` | `func_02063620` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

