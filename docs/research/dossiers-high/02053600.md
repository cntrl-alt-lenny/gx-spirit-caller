# func_02053600 (main, HIGH class B, 44B)

**Recipe:** bic+orr bitfield merge (mask-clear low2, or-in arg)
**Risk:** frame reserves 0x4 with no apparent stack use — likely an alignment pad mwcc emits; orr operand order (v&~3)|r4 matches bic-then-orr.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02053600.c)

**Verify gate:**
1. python tools/verify.py src/main/func_02053600.c func_02053600 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/02053600.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
