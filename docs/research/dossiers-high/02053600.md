---
# func_02053600 (main, HIGH class B, 44B)

**Recipe:** bic+orr bitfield merge (mask-clear low2, or-in arg)
**Risk:** frame reserves 0x4 with no apparent stack use — likely an alignment pad mwcc emits; orr operand order (v&~3)|r4 matches bic-then-orr.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; `bic`/`orr` operand order and the alignment-pad stack frame are compared directly by verify.py

**Why this recipe hits this mode:** the `(v & ~3) | r4` expression emits `bic r_v, r_v, #3 / orr r_out, r_v, r4`; if the operand order in the `orr` is reversed (i.e. `r4` first) or the `~3` mask is encoded differently the bytes differ. The mysterious `sub sp, #4` alignment pad also changes the frame bytes if missing.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (`~3` encoded as `#3` in `bic`) are compared directly by verify.py.

**Verify gate sequence:**
1. verify.py src/main/func_02053600.c func_02053600 [--cc all] — instruction diff visible directly (bic/orr order, stack pad)
2. ninja sha1 — final gate

## Reshape hints

- If verify.py shows the `orr` operands in wrong order, swap: `(r4 | (v & ~3))` — ARM assembler allows either but mwcc picks a fixed order based on source order
- The `sub sp, #4` alignment pad is not driven by any local variable in the C; if it appears in the orig .s, adding a dummy `volatile int _pad` or a redundant stack-addressed local may force mwcc to emit it
- The mask `~3` must be written as `& ~3` (not `& 0xFFFFFFFC` or `& -4`) so mwcc emits a `bic #3` rather than loading a large immediate
- Verify that `func_020536a0` (getter) is called first and its result `v` goes into the bic, then the orr mixes in the original `r4` argument — do not swap the call argument and the mask operand
- This function is called by `func_020532f4` with `r4=0`; test that case too to confirm the `|r4` does not produce a spurious non-zero or-in
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020536a0` | main TU call |
| `bl` | `func_0205362c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

