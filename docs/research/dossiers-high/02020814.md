---
# func_02020814 (main, HIGH class A, 43B)

**Recipe:** store-order field clears, moveq/movne boolean tail
**Risk:** stores must emit in exact asm order (f10,f3c,f1c,f4,fa=1,f2c,fa0,fa4,f20); the shared r2=0 const reuse; offsets guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (store scheduling out of exact asm order; r2=0 reuse pattern; moveq/movne boolean tail)
**Secondary mode:** Mode A — named global data_02191f40 used as pool base for all nine stores; wildcarded by verify.py, only ninja report catches value mismatches

**Why this recipe hits this mode:** Nine field stores into data_02191f40 at scattered offsets must be emitted in a specific non-ascending order (f10, f3c, f1c, f4, fa=1, f2c, fa0, fa4, f20); MWCC may reorder stores to the same base for scheduling, and the compiler's choice of when to reload the r2=0 constant versus reuse it in a register produces visible byte differences.

**Pool words to verify against orig .s:**
- `data_02191f40` — global struct base for all nine field stores

**Verify gate sequence:**
1. verify.py src/main/func_02020814.c func_02020814 [--cc all] — store order and moveq/movne visible directly
2. ninja report — resolves pool word data_02191f40; catches Mode A mismatches
3. ninja sha1 — final gate

## Reshape hints

- Write the nine stores in exactly the order specified in the risk note: f10=0, f3c=0, f1c=0, f4=0, fa=1, f2c=0, fa0=0, fa4=0, f20=0 — this non-ascending offset order is what the .s mandates; any reorder changes the STR sequence
- The `fa = 1` store is the only non-zero value and breaks the zero run; it must appear exactly between f4=0 and f2c=0 in source order to force the compiler to load the constant 1 at that point
- The shared r2=0 reuse means MWCC loads zero into r2 once and reuses it for multiple strh/str instructions; avoid any intervening call or complex expression between the zero stores that would force a register save
- For the boolean tail, express as `return func_02020880() != 0` to emit moveq r0,#0/movne r0,#1; if the compiler emits a plain pass-through, try binding to a local: `int r = func_02020880(); return r != 0;`
- Field offsets are guessed; verify fa (+0xa), f10 (+0x10), f1c (+0x1c), f2c (+0x2c), f3c (+0x3c), fa0 (+0xa0), fa4 (+0xa4) against actual .s STR offsets; a wrong pad collapses all subsequent stores
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02191f40` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02020880` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

