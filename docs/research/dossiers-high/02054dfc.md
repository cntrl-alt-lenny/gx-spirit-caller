---
# func_02054dfc (main, HIGH class A, 88B)

**Recipe:** u16 early-return guard + unrolled byte copy + tail call
**Risk:** the ldrh-guard fold (addeq sp/moveq r0/ldmeq) and loop unroll shape may not match; src struct layout guessed
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (ldrh-guard early-return fold and loop unroll shape)

**Why this recipe hits this mode:** The early-return on `src->n == 0` must fold into an `ldrh` + `cmp` + conditional `addeq sp` / `moveq r0, #0` / `ldmeq` sequence; if mwcc emits a branch-over instead of a conditional-epilogue fold, the instruction pattern differs visibly from the orig.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054dfc.c func_02054dfc [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The `Src` struct layout (b0, b1, n at offset +2) is guessed; cross-check against the orig `.s` for the `ldrh r?, [r?, #2]` instruction to confirm the guard field offset before anything else.
- The early-return `if (src->n == 0) return 0;` must produce a conditional epilogue (`addeq sp`/`ldmeq`) not a branch; if objdiff shows a `beq` instead, try `if (src->n != 0) { ... } return 0;` to invert the branch sense.
- The byte-copy loop and `buf[0]` override are structurally identical to `02054db8`; if that function matches, use the same loop form here.
- The cast `(unsigned char *)src` to alias the struct as a byte array is required so that `s[i*2]` and `s[i*2+1]` produce `ldrb` instructions rather than struct-field accesses.
- If the guard instruction is `ldrsh` (signed halfword) instead of `ldrh`, change `unsigned short n` to `short n` in the `Src` struct definition.
---
