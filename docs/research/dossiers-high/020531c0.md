---
# func_020531c0 (main, HIGH class A, 112B)

**Recipe:** two-arm cmpeq-flag; branch-tail duplication; long-long callee
**Risk:** struct offsets guessed; cmp/cmpeq operand order (hi-then-lo) may swap; moveq/movne block-order coin-flip
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; ARM `cmp`/`cmpeq` hi/lo operand order and moveq/movne branch-order are compared directly by verify.py

**Why this recipe hits this mode:** the `&&`-chain equality `s.f_4 == (v >> 32) && s.f_0 == v` lowers to `cmp r_hi, field_hi / cmpeq r_lo, field_lo`; swapping hi/lo ordering or the true/false moveq/movne assignment produces different instruction bytes that verify.py catches immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020531c0.c func_020531c0 [--cc all] — instruction diff visible directly (cmp/cmpeq order, moveq/movne)
2. ninja sha1 — final gate

## Reshape hints

- If verify.py shows `cmp r_lo` before `cmp r_hi`, swap to lo-first order: `s.f_0 == (unsigned int)v && s.f_4 == (unsigned int)(v >> 32)` (and the same transposition in the else branch)
- If the moveq/movne true/false is inverted, negate the entire condition: `return !(s.f_4 != (unsigned int)(v >> 32) || s.f_0 != (unsigned int)v)` forces the opposite condition code
- Duplicate `func_02053728(self)` in each arm as written — do not hoist it above the `if`; mwcc must not merge the two calls or the branch structure changes
- Keep the two arms self-contained (no shared tail block) so mwcc does not emit a cross-jump; the comment explicitly flags this requirement
- If struct offsets f_0/f_4/f_8/f_c are wrong, locate other callers of `func_02044c60` that access the returned `pair_t` — the field order must match whatever offset `func_02044c60` writes
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02044c60` | main TU call |
| `bl` | `func_02053728` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

