---
# func_0204b3d0 (main, HIGH class A, 64B)

**Recipe:** leaf scan loop, mul stride, masked early return
**Risk:** i*0xc stride may strength-reduce to pointer-bump; (i+0x64)&0xff masking
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — Wrong pool word value (hidden from verify.py; only ninja report catches it)

**Why this recipe hits this mode:** `data_0219dcb4` is the array base loaded from the pool for every byte access `data_0219dcb4[i * 0xc]`; the pool .word is wildcarded by verify.py, so a wrong base address passes instruction-byte comparison silently.

**Pool words to verify against orig .s:**
- `data_0219dcb4` — byte-array base for all stride-indexed reads; the pool .word must match the orig .s entry

**Verify gate sequence:**
1. verify.py src/main/func_0204b3d0.c func_0204b3d0 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep `i * 0xc` as a multiply expression rather than a pointer increment; if MWCC strength-reduces it to a pointer bump (`r2 += 0xc`), verify.py will show a visible diff — try using the array subscript form `data_0219dcb4[i * 0xc]` to force the mul
- The `(i + 0x64) & 0xff` return expression must produce an `and r0,#0xff` instruction; if the compiler omits the mask, cast the result explicitly to `unsigned char` before returning
- The `i = (i + 1) & 0xff` loop increment must emit `and r_i,#0xff` each iteration matching the orig .s `and r3,#0xff`; if the compiler drops the mask (treating i as always in range), keep the explicit `& 0xff` in the source
- The do-while structure enters the loop body before checking `i < 0x9a`; a while-first structure changes the branch layout and will not match
- Return 0 on fall-through (no matching slot found) is a plain `mov r0,#0; bx lr` in the .s; ensure no extra cleanup code is inserted after the loop
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dcb4` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

