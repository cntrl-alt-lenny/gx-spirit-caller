---
# func_020071c4 (main, HIGH class A, unknown B)

**Recipe:** bitfield-test guard (lsl#1e;lsr#1f = bit1) + branch-tail clear; bind base after guard
**Risk:** Orig uses TWO pool words for same addr (patcher trim bypass); clean C dedupes to one — likely 1-word diff.
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode D — .s-only candidate; the orig emits TWO pool words for `data_02104f1c` (one via the extern symbol for the guard, one via a raw `0x02104f1c` literal that bypasses the patcher trim); clean C collapses these to one pool word, producing a structural pool difference that cannot be reproduced in clean C.

WARNING: .s-only candidate — do NOT attempt .c replacement; the orig has duplicate pool words (patcher bypass) that clean C cannot reproduce.

**Why this recipe hits this mode:** The prep comment explicitly states the orig emits two literal-pool words for `data_02104f1c` — one through the named `.extern` for the guard read and one through a raw `0x02104f1c` literal that bypasses patcher trimming. mwcc will always deduplicate these into a single pool entry for a clean-C reference, creating an irreconcilable 1-word pool diff at the binary level.

**Pool words to verify against orig .s:**
- `data_02104f1c` — struct base used both for the guard flag read and for the post-guard field clears (+0x10 flags, +0x14 ctx); orig has TWO pool words for this address (structural mismatch for clean C)

**Verify gate sequence:**
1. verify.py src/main/func_020071c4.c func_020071c4 [--cc all] — instruction bytes (pool wildcarded, but pool SIZE will differ)
2. ninja report — resolves pool words; the duplicate-pool structural diff will surface here
3. ninja sha1 — final gate

## Reshape hints

- This function is flagged Mode D; a clean .c is expected to fail at the pool-word count — the only known fix is a hand-written .s that emits the second raw literal
- If a .c attempt is made despite Mode D: the `lsl #0x1e; lsr #0x1f` bit-1 extract maps to `(flags >> 1) & 1` or `(flags & 0x2) != 0` — use the `& 0x2` form to match the orig cmp sequence
- Bind the post-guard pointer as `p = &data_02104f1c` only after the guard passes; the orig materializes the callee-saved base only after the early-return branch, so binding before the guard will shift the pool layout further
- The `p->flags &= ~0x2u` clear must use `unsigned int` cast to keep the `bic` encoding; signed `int` may produce different instruction encoding
- Cross-check the orig .s for the exact pool layout (two `.word 0x02104f1c` entries vs one) before investing time on a .c reshape — Mode D means the structural mismatch is deterministic
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_02007210` | `data_02104f1c` | data symbol |
| `.L_02007214` | `0x02104f1c` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02006918` | main TU call |
| `bl` | `func_02006a38` | main TU call |
| `bl` | `func_020057dc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

