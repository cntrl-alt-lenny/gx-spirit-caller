---
# func_020536d0 (main, HIGH class A, 52B)

**Recipe:** arg-shuffle + call + single SETTER store
**Risk:** reg-alloc-walled per .s header: r4/r5 save-reg choice for a1/self may flip; 0x7ff pool placement; offset guessed
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; `0x7ff` literal pool placement and STR offset are compared directly by verify.py. Secondary: reg-alloc save-reg flip (r4 vs r5 assignment) also produces different bytes.

**Why this recipe hits this mode:** `0x7ff` is too large to encode as an ARM immediate and must be loaded from the literal pool; the pool-word placement relative to the function body is compared directly by verify.py. The reg-alloc wall means r4/r5 may be assigned to `a1`/`self` in opposite order, changing which callee-saved register holds which value across the BL.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constant `0x7ff` is loaded from a literal pool slot compared directly by verify.py.

**Verify gate sequence:**
1. verify.py src/main/func_020536d0.c func_020536d0 [--cc all] — instruction diff visible directly (pool word placement, STR offset, reg assignment)
2. ninja sha1 — final gate

## Reshape hints

- If verify.py shows the save-reg assignment is flipped (r4=self, r5=a1 instead of r4=a1, r5=self), try declaring `a1` as the first parameter and `self` as the second in the function signature — mwcc assigns r4 to the live-across-call value it sees first in the source
- The `0x7ff` literal must appear as the fourth argument to `func_02053748` exactly as written; do not fold it into a `#define` or cast that changes how it is encoded
- Confirm the field offset of `f_4` against the sibling `func_020536bc` which also writes `self->f_4`; if both are in the same module, they must agree on offset `0x4`
- The store `self->f_4 = a1` must come AFTER the call; do not hoist it before `func_02053748` or mwcc may not need to save `a1` across the call and the reg-alloc changes
- This is flagged as reg-alloc-walled and low confidence; if verify.py shows >50% diff after reshaping, consider shipping as .s instead
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x000007ff` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02053748` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

