# func_02093848 (main, HIGH class A, 128B)

**Recipe:** linear call sequence + u16-cast (lsl16;lsr16) + arg held in callee-saved
**Risk:** Literal-pool order (_LIT0 before _LIT1) and scheduling of the 0x027ffc20 store vs mov r0,#0x10 may differ.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_02093848.c func_02093848 --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode B mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x027ffc40` | MMIO/const |
| `_LIT1` | `0x027ffc20` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02093bfc` | main TU call |
| `bl` | `func_02090868` | main TU call |
| `bl` | `func_0209c014` | main TU call |
| `bl` | `func_02093d44` | main TU call |
| `bl` | `func_0209065c` | main TU call |
| `bl` | `func_020905a8` | main TU call |
| `bl` | `func_020938c8` | main TU call |
| `bl` | `func_01ff8624` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Cross-check every BL/BLX target address in the .s against your `extern func_XXXX` declarations
- BL relocs are wildcarded by verify.py; only `ninja report` resolves them
- Wrong callee address passes verify.py at 100% but fails sha1

## Reference

- Prep: `docs/research/c-match-prep/02093848.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
