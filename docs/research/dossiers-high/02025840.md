---
# func_02025840 (main, HIGH class B, 64B)

**Recipe:** Two single-bit tests (bind h) + invert (== 0) result
**Risk:** bit extracts may compile to and-mask form not lsl;lsr without :1 bitfields; the two ifs might merge into one && altering branch layout
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; lsl/lsr vs and-mask encoding and branch layout changes are directly visible to verify.py

**Why this recipe hits this mode:** The `(h << 31) >> 31` and `(h << 30) >> 31` shift idioms must produce `lsl; lsr` pairs rather than `and #1` / `and #2` mask forms. Any divergence in bit-extract encoding or basic-block structure (nested ifs vs merged `&&`) is an instruction-level difference that verify.py reports immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02025840.c func_02025840 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Use `(unsigned)(h << 31) >> 31` and `(unsigned)(h << 30) >> 31` (as written) to force lsl+lsr pairs; a `:1` bitfield or `& 1` / `& 2` mask may produce `and #1` instead of the shift pair
- Keep the two bit tests as separate nested `if` statements rather than `if (bit0 && bit1)` to preserve the two-branch basic-block layout; merging them into one `&&` changes block structure
- The result inversion `return func_02025880(p) == 0` must use `== 0` (not `!`) to match the `cmp+moveq/movne` pattern in the orig .s
- `h` must be loaded once and shifted twice from the same register; ensure no reload occurs between the two bit tests by keeping `h` as an `unsigned short` local
- The return type of `func_02025880` must be `int` so `== 0` compiles to `cmp+conditional move` rather than a branch
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02025880` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

