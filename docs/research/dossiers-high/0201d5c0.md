---
# func_0201d5c0 (main, HIGH class A, ?B)

**Recipe:** SETTER sparse switch -> if/else-if predicated stores
**Risk:** mwcc may emit a jump table or flip beq/streq predication vs the orig branch+predicate mix
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — named pool global data_02191e30 is a pool-word candidate; wrong address wildcarded by verify.py

**Secondary mode:** Mode E — visible diff if mwcc emits a jump table instead of branch+predicate stores, or flips the beq/streq encoding

**Why this recipe hits this mode:** `data_02191e30` is loaded as a pool literal base for the two field stores; verify.py wildcards the pool .word so a wrong address is invisible until `ninja report`. Additionally, the sparse two-case if/else-if may compile to a jump table (visible byte diff) or emit `streq` predicated stores rather than branch+str.

**Pool words to verify against orig .s:**
- `data_02191e30` — base for the two-field struct; `f_0` at offset +0 (case 4 store) and `f_4` at offset +4 (case 9 store)

**Verify gate sequence:**
1. `verify.py src/main/func_0201d5c0.c func_0201d5c0 [--cc all]` — instruction bytes
2. `ninja report` — resolves data_02191e30 pool word; catches Mode A mismatch before sha1
3. `ninja sha1` — final gate

## Reshape hints

- Write as `if (sel == 4) { data_02191e30.f_0 = v; } else if (sel == 9) { data_02191e30.f_4 = v; }` — the two-branch if/else-if form is least likely to trigger jump-table emission for only two sparse cases.
- If mwcc still emits a jump table, add a `__attribute__` or rewrite as two independent `if` statements without `else` to break the switch-like shape.
- Cross-check the exact address of `data_02191e30` in the orig .s pool — the struct is described as 8 bytes (two ints), so offset +0 and +4 should match the orig `str` displacements.
- If case-9 emits a `streq` predicated store instead of a `beq`-guarded `str`, try wrapping it in an explicit `if (sel == 9)` without else to separate the two branches.
- The pool literal order in the .s (data_02191e30 appears once for both fields) means both stores share the same loaded base register — keep both accesses through the same struct variable.
---
