---
# func_02045744 (main, HIGH class A, 148B)

**Recipe:** GUARD chain, void; s->f4 CSE'd; RELOAD singleton post-call/between stores
**Risk:** second s->f4 may reload instead of CSE; final two stores' reload scheduling may differ; cluster is reg-alloc-walled
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word mismatch; wrong singleton address wildcarded by verify.py, only ninja report catches it
**Secondary mode:** Mode E — visible reg-alloc / CSE scheduling wall; verify.py catches instruction diffs directly

**Why this recipe hits this mode:** `data_0219dad0` is a named BSS pointer whose pool .word is wildcarded by verify.py; the CSE vs reload decision for the second `s->f4` read and the post-call store scheduling are reg-alloc sensitive and produce visible instruction diffs that verify.py surfaces directly.

**Pool words to verify against orig .s:**
- `data_0219dad0` — BSS singleton pointer (S*); load base for all f0/f4/f6 field accesses and post-call stores

**Verify gate sequence:**
1. verify.py src/main/func_02045744.c func_02045744 [--cc all] — instruction bytes; Mode E diffs (CSE vs reload, store order) visible directly
2. ninja report — resolves pool words; catches Mode A mismatch (wrong data_0219dad0 address) before sha1
3. ninja sha1 — final gate

## Reshape hints

- The first `s->f4 == 2` and second `s->f4 != 4` reads are expected to CSE (single ldrsh used for both comparisons); if verify.py shows two separate ldrsh instructions, bind the first read to a local `unsigned short f4 = s->f4` and use it for both guards
- Post-call stores (`data_0219dad0->f6 = 0; data_0219dad0->f4 = 6`) must reload the singleton each time since no callee-saved register holds it; write as `data_0219dad0->` (not `s->`) to force reloads
- If the compiler emits the f6=0 store and f4=6 store in the wrong order, swap the two store statements in the C source
- The `func_0207b5f8() == 9` early-return must produce a single cmp+beq; if the compiler emits a subtraction before the compare, try `if (func_0207b5f8() != 9)` with a fall-through body instead
- This function is flagged as a known reg-alloc wall; if CSE reshaping does not converge, the permuter may be needed
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dad0` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203cc58` | main TU call |
| `bl` | `func_0207b5f8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

