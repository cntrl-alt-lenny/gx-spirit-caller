---
# func_0203f6cc (main, HIGH class A, unknown B)

**Recipe:** stack-buffer; compare guard early-return; tail copy
**Risk:** the 0x18 frame and buf[0]/buf[8] split may not match mwcc's stack layout exactly; arg0 pass-through
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named pool global `data_020fe550` used as load base; wrong value wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** `data_020fe550` is passed directly as the comparison base to `func_020a7440`; the compiler emits an LDR pool word for this address, which verify.py BL-wildcards and cannot validate — only the linked ROM report resolves it.

**Pool words to verify against orig .s:**
- `data_020fe550` — comparison base passed as second arg to `func_020a7440` (8-byte compare target)

**Verify gate sequence:**
1. verify.py src/main/func_0203f6cc.c func_0203f6cc [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Frame size 0x18 is shared with siblings; if mwcc pads to 0x20 check that buf is declared as `u8 buf[0x18]` not a larger type
- The buf[0]/buf[8] split relies on `func_0203f590` writing to `&buf[0]` and `func_02094688` copying from `&buf[8]`; keep these pointer offsets literal, not pointer-arithmetic variables
- `arg0 pass-through` means arg0 must not be reloaded from stack between the two call sites; keep it as a parameter without any intermediate binding
- Verify `data_020fe550` pool word address against the `.s` LDR pool entry before declaring a match
- If verify.py shows 100% but sha1 fails, run ninja report first to confirm the pool-word relocation resolves correctly
---
