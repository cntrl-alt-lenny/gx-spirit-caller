# func_02052bd4 (main, HIGH class B, 100B)

**Recipe:** flag-track + sign-test, conditional-move ladder to 0/1/2/3
**Risk:** movne-before-moveq order: orig tests flag!=0 (movne first); ternary may emit moveq/movne swapped vs orig, a coin-flip diff.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02052bd4.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02052bd4.c func_02052bd4 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02052bd4.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
