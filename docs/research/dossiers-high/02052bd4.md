---
# func_02052bd4 (main, HIGH class B, 100B)

**Recipe:** flag-track + sign-test, conditional-move ladder to 0/1/2/3
**Risk:** movne-before-moveq order: orig tests flag!=0 (movne first); ternary may emit moveq/movne swapped vs orig, a coin-flip diff.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from movne/moveq swap in the conditional-move ladder

**Why this recipe hits this mode:** The risk explicitly names "movne-before-moveq order" — the orig emits `movne` before `moveq` when materialising the 0/1/2/3 result; a ternary written in the wrong polarity flips the two, producing a visible instruction byte diff that verify.py catches directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02052bd4.c func_02052bd4 [--cc all] — instruction diff visible (movne/moveq order)
2. ninja sha1 — final gate

## Reshape hints

- The ternaries `flag ? 2 : 3` and `flag ? 1 : 0` must test the non-zero (true) case first to emit `movne` before `moveq`; if objdiff shows the pair swapped, try `!flag ? 3 : 2` and `!flag ? 0 : 1`
- The sign test `r5 < 0` must come after the flag-set block — do not hoist the `func_020448d8` result check above the conditional flag block
- `func_02000b60(0x02000b8c)` takes a raw hex address literal — this is compared directly by verify.py; no pool reloc concern
- If both ternaries need inversion simultaneously, flip the outer if to `r5 >= 0` and swap the two return blocks
- Keep `flag` initialised to 0 before `func_02000b60` — the compiler may use the zero-init to elide a mov; reordering changes the preamble
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x02000b8c` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02000b60` | main TU call |
| `bl` | `func_020448d8` | main TU call |
| `bl` | `func_02044c10` | main TU call |
| `bl` | `func_02044ea8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

