---
# func_02007f38 (main, HIGH class A, unknown B)

**Recipe:** large constant synthesis (mov+orr chain) single field store
**Risk:** mwcc may decompose 0x52008421 in a different orr order than orig (0x21,0x8400,0x52000000).
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff if mwcc chooses a different orr-operand decomposition order for 0x52008421

**Why this recipe hits this mode:** The constant 0x52008421 requires multiple ARM immediate rotations and mwcc synthesizes it as a `mov+orr` chain; the exact sequence (`mov #0; orr #0x21; orr #0x8400; orr #0x52000000`) may differ in order from what the compiler emits for the unsigned literal, producing a visible byte diff that `verify.py` catches directly.

**Pool words to verify against orig .s:**
- `data_02104f3c` — store base; field offset +0xa74 for the composite constant write

**Verify gate sequence:**
1. verify.py src/main/func_02007f38.c func_02007f38 [--cc all] — instruction bytes (pool wildcarded for data_02104f3c)
2. ninja report — resolves pool word value for data_02104f3c; catches Mode A mismatch
3. ninja sha1 — final gate

## Reshape hints

- The constant must be written as the exact unsigned hex literal `0x52008421u` — do not use decimal or split the constant into sub-expressions, as mwcc's immediate decomposer may choose a different rotation path for non-literal forms
- If mwcc reorders the orr chain: try expressing the constant as a series of explicit bitwise ORs (`(0x52000000u | 0x8400u | 0x21u)`) to steer the decomposition to match the orig's `mov #0; orr` sequence
- The write must be to `*(unsigned int *)(data_02104f3c + 0xa74)` — verify the field offset +0xa74 in the orig .s `str` immediate; a nibble error here is Mode A (pool-wildcarded) but will be caught by ninja report
- Keep the function body to a single store statement; any extra local variable would shift register allocation and change the orr-chain register assignments
- If the orr order still mismatches after the above, use `__asm` volatile to pin the constant synthesis — but exhaust C reshapes first
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_02007f54` | `data_02104f3c` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

