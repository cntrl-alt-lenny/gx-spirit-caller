# func_0206ddc0 (main, HIGH class A, 168B)

**Recipe:** field-load + OR-guard flag, call block, IRQ-bracket, bic/orr bit ops
**Risk:** struct offsets (0x4,0x73,0x70,0x68,0x64) guessed; flag if-chain order may flip movne
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/main/func_0206ddc0.c func_0206ddc0 --cc all` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02091af4` | main TU call |
| `bl` | `func_02070c40` | main TU call |
| `bl` | `func_02070bac` | main TU call |
| `bl` | `func_02070f24` | main TU call |
| `bl` | `func_02071010` | main TU call |
| `bl` | `func_0206bef4` | main TU call |
| `bl` | `OS_DisableIrq` |  |
| `bl` | `func_0206e550` | main TU call |
| `bl` | `func_0206e568` | main TU call |
| `bl` | `OS_RestoreIrq` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Run `verify.py --cc all`; instruction byte diffs tell you what to reshape
- movne/moveq: try inverting the condition or swapping then/else
- `and #1` wall: use `(x << 31u) >> 31` to force lsl;lsr pair
- jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: `docs/research/c-match-prep/0206ddc0.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
