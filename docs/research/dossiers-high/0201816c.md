---
# func_0201816c (main, HIGH class A, 72B)

**Recipe:** GetSystemWork bitfield-guard + linear guard-chain (+3 bound), cond tail store
**Risk:** 0x8d8&1 guard -> `and #1` wall; `ldmcc` early-return on unsigned compare + `streq` cond store may diverge; r4/r5 order
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and #1` wall and conditional-store / unsigned-compare shape

**Why this recipe hits this mode:** The `(v<<31)>>31` entry guard is the canonical `and #1` wall. The unsigned lower-bound compare `(unsigned int)func_02019034() < (unsigned int)(*(int *)(w+0x8dc) + 3)` maps to a `bcc` early-return in .s; mwcc's lowering of the unsigned compare must emit the same `ldmcc` / `bcc` shape. The final conditional store is gated on `func_020139c4() == 5` without an else — must emit `streq` or equivalent conditional store, not an unconditional branch around a store.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0201816c.c func_0201816c [--cc all] — instruction bytes (and #1 wall + bcc shape visible)
2. ninja sha1 — final gate

## Reshape hints

- Keep local variable order as `o` (-> r4) then `w` (-> r5) — the .s comment confirms `mov r4,r0 / mov r5,r0`; swapping the local declarations may reverse register assignment
- The unsigned bound check `(unsigned int)func_02019034() < (unsigned int)(*(int *)(w+0x8dc) + 3)` must stay as an unsigned cast on both sides — signed comparison changes `bcc` to `blt` in .s
- The threshold is +3 (not +1 as in func_02017a98) — confirm against the .s that the literal is 3
- Final tail: `if (func_020139c4() == 5) *(int *)((char *)o+8) = 0x002a0100;` — if mwcc emits an unconditional branch instead of `streq`, try rewriting as `if (func_020139c4() != 5) return; *(int *)((char*)o+8) = 0x002a0100;`
- This is one of the shortest functions in the group (72B); a single verify.py pass will show all divergent instructions at once
---
