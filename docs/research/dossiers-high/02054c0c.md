---
# func_02054c0c (main, HIGH class A, 36B)

**Recipe:** out-param call + conditional select on result (mvneq/ldrne)
**Risk:** minimal; out local must stay at sp+0 (single 4-byte frame)
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (mvneq/ldrne conditional pair)

**Why this recipe hits this mode:** The return path emits `mvneq r0, #0` and `ldrne r0, [sp, #0]` as conditional alternatives; if mwcc selects different condition codes or flips the branch polarity, verify.py catches the byte diff immediately on the instruction pair.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054c0c.c func_02054c0c [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The frame must be exactly 4 bytes with `out` at sp+0; do not add any other locals or the stack layout shifts.
- The conditional return pair is `mvneq` / `ldrne`; if objdiff shows a polarity flip, try negating the `== 0` to `!= 0` and swapping the two return branches.
- Keep `func_0206e720` declared as returning `int` (not `void`); a void return type may suppress the branch-on-result pattern.
- Do not assign the return value of `func_0206e720` to a named variable; the direct comparison `== 0` is needed to emit the cmp-then-conditional-mov sequence rather than a load+cmp.
- If the `mvneq` emits as `mvn r0, #0` unconditionally, try writing `return (func_0206e720(a0, &out) != 0) ? out : -1;` as an alternative form.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206e720` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

