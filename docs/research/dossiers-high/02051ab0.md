---
# func_02051ab0 (main, HIGH class A, 96B)

**Recipe:** guard chain + tail returns, RELOAD global; reuse saved args
**Risk:** field 0xe4 word(ptr); func_02068cc8(p,r6)/func_02068a20(_,r5,r4) arg slots; default-arg passthrough r4
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — indirect callees func_02068cc8 / func_02068a20 wildcarded by verify.py BL reloc

**Why this recipe hits this mode:** Both inner calls (`func_02068cc8` and `func_02068a20`) are extern BL targets; verify.py wildcards all BL relocations, so a wrong callee address at either site passes instruction-byte checks silently and only ninja report resolves the actual callee bindings. The `fe4` field offset at +0xe4 is visible (an offset mismatch changes the ldr instruction), but both callees remain hidden.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02051ab0.c func_02051ab0 [--cc all] — instruction bytes
2. ninja report — resolves BL targets for func_02068cc8 and func_02068a20; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write the outer guards as a single compound `&&` condition (`if (func_020498f0() != 0 && func_02045280() != 0)`) to keep both guards in one branch sequence; splitting them into nested ifs may add an extra branch instruction.
- The default return value `r = c` must be assigned at the top before any guards — the asm saves the `c` argument in r4 and returns it on the fast path, so the variable must be initialised unconditionally.
- The argument order for `func_02068cc8` is `(func_020498f0()->fe4, a)` — struct word first, then the `a` parameter; the asm holds `fe4` in r6 and `a` in the next slot.
- The argument order for `func_02068a20` is `(func_020498f0()->fe4, b, c)` — struct word, then `b`, then `c`; the asm reuses the r5/r4 saved registers for `b`/`c`.
- Cross-check both extern function addresses against their respective .s BL instructions before finalising.
---
