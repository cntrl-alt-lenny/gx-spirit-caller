---
# func_02046a5c (main, HIGH class A, 104B)

**Recipe:** guard + ptr-arith out-param + byte dispatch + tail calls; sp3
**Risk:** b369 double-read needs separate || operands (not a cached temp); 0x624 materialized as pool word — magnitude may inline as imm
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible instruction diff (double-read vs cached ldrb, pool word vs imm for 0x624)

**Why this recipe hits this mode:** `data_0219daec` is a named global loaded from the pool twice (once for the guard, once for the byte reloads); verify.py wildcards those pool loads, so a wrong symbol address passes silently and only `ninja report` catches it. Additionally, the 0x624 offset may or may not be materialized as a pool literal depending on mwcc's immediate-range decision, and the b369 byte must be re-read with two separate LDRB instructions (once per compare) — caching it in a local emits only one LDRB and produces a visible byte diff.

**Pool words to verify against orig .s:**
- `data_0219daec` — global struct pointer; loaded from pool for the null guard and again for the b_369 double-read; pool .word must match orig .s

**Verify gate sequence:**
1. verify.py src/main/func_02046a5c.c func_02046a5c [--cc all] [--module main] — instruction bytes; ldrb-count and 0x624 encoding visible here
2. ninja report — resolves pool word for data_0219daec; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Do NOT cache `data_0219daec->b_369` in a local variable; write the two comparisons as separate member accesses (`data_0219daec->b_369 == 2 || data_0219daec->b_369 == 3`) so mwcc emits two LDRB instructions.
- Use the `||` operator (short-circuit) not a bitwise `|` for the condition — the .s shows a beq after the first compare and a bne after the second, matching short-circuit evaluation.
- The 0x624 pointer-arithmetic `(char *)data_0219daec + 0x624`: if the .s pools it as a `.word 0x624`, keep it as a literal constant; if the .s encodes it as an immediate ADD, try casting to `(char *)` + integer constant so mwcc can use ADD r1, r3, #0x624 directly.
- The global is reloaded from pool for the b_369 reads (ldr r2,[r2] in the .s comment): ensure the C accesses `data_0219daec->b_369` twice from the global, not from a cached pointer local.
- Cross-check `data_0219daec` pool .word in the .s against config/eur/arm9/symbols.txt before running ninja report.
---
