# func_02046a5c (main, HIGH class A, 104B)

**Recipe:** guard + ptr-arith out-param + byte dispatch + tail calls; sp3
**Risk:** b369 double-read needs separate || operands (not a cached temp); 0x624 materialized as pool word — magnitude may inline as imm.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02046a5c.c)

**Verify gate:**
1. python tools/verify.py src/main/func_02046a5c.c func_02046a5c --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/02046a5c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
