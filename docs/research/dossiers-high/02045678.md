---
# func_02045678 (main, HIGH class A, 204B)

**Recipe:** GUARD/branch-tail chain + RELOAD singleton per field (no callee-saved)
**Risk:** reload-vs-bind of the singleton after the call and between the two f4/f6 stores; return-constant reuse may emit moveq; known reg-alloc wall
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word mismatch; wrong singleton address wildcarded by verify.py, only ninja report catches it
**Secondary mode:** Mode E — visible reg-alloc / moveq wall; verify.py catches instruction diffs directly

**Why this recipe hits this mode:** `data_0219dad0` is a named BSS pointer whose pool .word is wildcarded by verify.py; additionally the singleton is reloaded after the call (no callee-saved regs) and the return-constant reuse may produce `moveq` instructions that differ if the branch structure changes — both issues are confirmed by verify.py for Mode E but the pool address itself requires ninja report.

**Pool words to verify against orig .s:**
- `data_0219dad0` — BSS singleton pointer (S*); load base for all post-guard field accesses (f4 at +0x04, f6 at +0x06) and post-call stores

**Verify gate sequence:**
1. verify.py src/main/func_02045678.c func_02045678 [--cc all] — instruction bytes; Mode E diffs (moveq, reg-alloc) visible directly
2. ninja report — resolves pool words; catches Mode A mismatch (wrong data_0219dad0 address) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Post-call stores must reload the singleton via `data_0219dad0->f4 = ...` (not a cached local `s`), because no callee-saved register holds the pointer across the call; using `s->f4` post-call will bind to a stale local
- For return-constant reuse (moveq): each early-return path should return the constant directly (e.g. `return 4;`) rather than going through a shared register; if verify.py shows a moveq where a mov is expected, invert the condition on the preceding branch
- The r >= 0 early-return (return 2) must appear as a plain `bge` + `return 2`; if the compiler merges it with the r==5 branch, add a dummy intermediate to split the codegen
- For the two func_0204525c call sites (r < -10 and else), ensure the second argument expressions (`r` and `r - 0x2bc`) are computed inline at the call rather than bound to a temp; pre-binding can shift register allocation
- This function is flagged as a known reg-alloc wall; if no C reshaping achieves 100%, the permuter or a `.s` fallback may be required
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dad0` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203cbc0` | main TU call |
| `bl` | `func_0204525c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

