---
# func_02011178 (main, HIGH class A, unknown B)

**Recipe:** struct value-copy (*dst=*src) -> auto self-assign guard + 12-word unroll
**Risk:** mwcc might emit ldmia/stmia block instead of the per-word ldr/str unroll; arg order src=r0,dst=r1
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches ldmia/stmia block vs per-word ldr/str unroll

**Why this recipe hits this mode:** mwcc's choice between an `ldmia`/`stmia` block and a per-word `ldr`/`str` unroll for a 12-word struct copy depends on the exact struct size and compiler heuristics; if the emitted form differs from the baserom's sequence, verify.py will show a byte difference immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02011178.c func_02011178 — ldm/stm vs ldr/str unroll difference is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The argument order is `(Rec12 *src, Rec12 *dst)` (src=r0, dst=r1); do not swap — the self-assign guard `cmp src,dst` / `bxeq lr` depends on this
- Write the body as a plain struct assign `*dst = *src;` and let mwcc decide the copy form; do NOT manually unroll to 12 ldr/str pairs as that may prevent the compiler from choosing its natural pattern
- If mwcc emits `ldmia`/`stmia` but the baserom has per-word `ldr`/`str`, try splitting the struct into two sub-structs of 6 words each or using a `volatile` intermediate to break the block-copy heuristic
- If mwcc emits per-word copies but the baserom has a block, try declaring the struct at the call site differently (e.g., with `__attribute__((packed))` or by increasing the word count past the threshold)
- The self-assign guard (`cmp r0,r1` / `bxeq lr`) is auto-inserted by mwcc for struct copies of this size — do not manually add it or replicate its logic in C
---
