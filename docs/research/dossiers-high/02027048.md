---
# func_02027048 (main, HIGH class A, 136B)

**Recipe:** counted-loop + null-guard tail-call + STORE-ORDER MMIO reset
**Risk:** hand-.s has TWO literals (reloc + bare 0x0219a924 anti-trim); C emits one -> literal-pool/reloc differs
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode D — .s-ONLY candidate; orig hand-.s uses TWO literals (a reloc pool word plus a bare `0x0219a924` anti-trim literal) that clean C cannot reproduce

WARNING: .s-only candidate — do NOT attempt .c replacement; the orig has duplicate pool words (patcher bypass) that clean C cannot reproduce.

**Why this recipe hits this mode:** The risk explicitly states "hand-.s has TWO literals (reloc + bare 0x0219a924 anti-trim); C emits one -> literal-pool/reloc differs." The patcher anti-trim pattern requires a second bare literal of the same address alongside the reloc-bearing pool word; mwcc will never emit this duplication from clean C source, making byte-identical output impossible without a hand-written .s.

**Pool words to verify against orig .s:**
- `data_0219a924` — struct base for `.entries` (offset +0x00) and `.count` (offset +0x04); expected TWICE in orig .s pool (one reloc `.word data_0219a924` plus one bare `.word 0x0219a924` anti-trim literal)

**Verify gate sequence:**
Not applicable — this is a .s-only candidate. Do not attempt clean C replacement. For the hand .s:
1. Write `src/main/func_02027048.s` with two pool literals manually
2. ninja sha1 — final gate

## Reshape hints

- Do NOT write a .c for this function; the two-literal pool pattern cannot be reproduced by mwcc
- In the hand .s: emit `.word data_0219a924` (reloc) followed by `.word 0x0219a924` (bare) to satisfy both the linker relocation and the anti-trim guard
- The MMIO stores `*(volatile u16*)0x05000000 = 0` and `*(volatile u16*)0x05000400 = 0` use raw hex addresses (no named symbols) — these are compared directly, no pool-word concern
- The store order (0x05000000 before 0x05000400) must match the orig .s `str`/`strh` sequence exactly
- The null-guard block (`if (entries) { invoke; clear; }`) must precede the MMIO stores in basic-block layout; preserve this order in the .s
---
