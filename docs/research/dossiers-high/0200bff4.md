---
# func_0200bff4 (main, HIGH class A, unknown B)

**Recipe:** find-slot scan (break on <0 or ==key) + struct-assign (ldm/stm) + field overwrite
**Risk:** scan induction may start at base+0xbc(off 0x20) not base(off 0xdc); slot layout 0x24/key@0x20 guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches struct-layout offset divergence and ldm/stm block shape

**Why this recipe hits this mode:** The slot layout (stride 0x24, key at +0x20) is guessed; if the actual key offset is wrong the pointer arithmetic changes and objdiff will show offset differences. The `struct Payload` assign emits an `ldmia`/`stmia` block whose shape is sensitive to the struct size being exactly 8 words (0x20 bytes).

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0200bff4.c func_0200bff4 — offset differences and ldm/stm shape are visible directly
2. ninja sha1 — final gate

## Reshape hints

- Confirm the scan starts at `base + 0xbc` (pointer to `struct Slot`), not `base + 0xdc`; the guessed offset is the main risk
- The `struct Slot` key field must be at `+0x20` (after the 8-word payload) for the stride-0x24 addressing to match
- Write the struct-assign as `slots[i].data = *src` to get a single `ldmia`/`stmia` block; splitting into per-field copies may change the instruction sequence
- The field overwrite `slots[i].data.w[6] = a2` must come after the struct-assign and before the key write, in exactly that order
- If objdiff shows the key offset is off, try `base + 0xdc` as the scan start (equivalent to `base + 0xbc + 0x20`) and adjust the struct definition accordingly
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

