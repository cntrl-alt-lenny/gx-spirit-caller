---
# func_020224c0 (main, HIGH class A, 76B)

**Recipe:** DISPATCH switch + nested guard + two tail-ish calls
**Risk:** branch ordering of the x<0x20 split (blt vs bge first) may flip; the unused base arg in case 0/default could be optimized differently
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; blt/bge ordering flip is directly visible to verify.py
**Secondary mode:** Mode B — BL targets `func_0202224c` and `func_02022270` are extern guessed addresses; wrong callees wildcarded by BL reloc, only ninja report catches

**Why this recipe hits this mode:** The blt/bge ordering for the `x < 0x20` branch in case 1 is an instruction-level difference that verify.py reports directly. Additionally, both callees are extern func_XXXX symbols whose addresses are resolved by the linker — a wrong address is wildcarded by verify.py's BL reloc wildcard and only surfaces at `ninja report`.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020224c0.c func_020224c0 [--cc all] — instruction bytes (branch order visible)
2. ninja report — resolves BL targets; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- If objdiff shows `bge` before `blt` (inverted from orig), negate the condition: use `if (x >= 0x20)` with the `func_02022270` call first, then `else` for `func_0202224c`, to flip branch order
- Initialize `result = 0` before the switch (as written) to match the `mov r3,#0` pre-switch init in the orig .s — do not use a post-switch assignment or ternary
- The default arm returning 0 relies on the pre-initialized `result`; adding an explicit `default: result = 0; break;` may alter codegen — keep it implicit (fall-off)
- Cross-check `func_0202224c` and `func_02022270` addresses against orig .s BLX instructions via `ninja report` before claiming a sha1 match
- The `base` parameter is unused in case 0 and default; do not force a use of it — the compiler correctly optimizes it away
---
