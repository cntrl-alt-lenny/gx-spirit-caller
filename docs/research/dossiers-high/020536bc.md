---
# func_020536bc (main, HIGH class A, 12B)

**Recipe:** SETTER two-store, preserve arg order + store order
**Risk:** offsets 0x4/0x8 guessed; trivial leaf — should byte-match (confirmed by sibling func_02052e90.c caller)
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; field offsets `0x4`/`0x8` in STR instructions are compared directly by verify.py; a wrong offset is immediately visible

**Why this recipe hits this mode:** the two `str` instructions encode the field offsets `+0x4` and `+0x8` as immediate operands; if either offset is wrong by even one byte the encoded instruction differs and verify.py catches it. Store order also matters if mwcc reorders them.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (field offsets `0x4`, `0x8` encoded in STR immediates) are compared directly by verify.py.

**Verify gate sequence:**
1. verify.py src/main/func_020536bc.c func_020536bc [--cc all] — instruction diff visible directly (STR offsets, store order)
2. ninja sha1 — final gate

## Reshape hints

- Write stores in exactly the prep order: `self->f_4 = a1` then `self->f_8 = a2` — do not swap; mwcc may or may not reorder sequential stores and the orig .s fixes the order
- If verify.py flags wrong offsets, check the sibling caller `func_02052e90.c` which is stated to confirm these offsets — the caller's load or store of the same struct fields pins the correct values
- This is a 12-byte leaf (two STR + BX LR); there are almost no knobs to turn — if the offsets are right, it will byte-match immediately
- If mwcc emits `stmia` instead of two separate `str` instructions (possible for adjacent fields), and that does not match the orig .s, add a volatile barrier between the two stores to prevent merging
- The struct `_pad[0x4]` at offset 0 must remain to push `f_4` to the correct address; do not remove it even though it is unused
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

