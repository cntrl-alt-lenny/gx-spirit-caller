---
# func_0201fbe0 (main, HIGH class A, 46B)

**Recipe:** guard chain, nested guards, struct callback blx
**Risk:** first ldrh is from r0 arg (p->f2) not the global; rest reload data_02191f40; offsets 0x18/0x38 guessed; callback reloads base into r0 for arg
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — named global data_02191f40 used repeatedly as pool base for field loads at +0x0, +0x18, +0x38; pool-word materialisation order is wildcarded by verify.py; only ninja report catches mismatches
**Secondary mode:** Mode E — visible instruction byte diff (first ldrh from arg register vs global, callback arg-reload sequencing)

**Why this recipe hits this mode:** The function begins with `p->f2` loaded from the r0 argument (not the global), then switches to data_02191f40 for subsequent field loads; the compiler must juggle the arg pointer and the global base pointer, and the callback blx reloads the global base as r0 for the argument — this two-source pattern may produce a different pool-word sequence than expected.

**Pool words to verify against orig .s:**
- `data_02191f40` — struct base for fields +0x0 (f0), +0x18 (f18), +0x38 (f38 callback)
- `data_020c6670_obj` — object address passed as argument to the callback blx at field +0x38

**Verify gate sequence:**
1. verify.py src/main/func_0201fbe0.c func_0201fbe0 [--cc all] — instruction bytes
2. ninja report — resolves pool words data_02191f40 and data_020c6670_obj; catches Mode A ordering mismatches
3. ninja sha1 — final gate

## Reshape hints

- Keep the first guard on `p->f2` (from the function argument, not the global) — this is the only ldrh that does NOT go through data_02191f40, so the compiler must keep p in r0 for that load before loading the global base
- The guard chain order matters: p->f2 check → f138(1) call → f0==0 check → f18!=0 branch → nested fc6c/fd24 checks → f38 callback; any reorder shifts which pool literal is alive at each point
- Field offsets 0x18 and 0x38 are guessed; if verify.py shows ldrh/ldr from different offsets, adjust the struct pad between f2 and f18, and between f18 and f38
- The callback blx passes &data_020c6670_obj as r0; the compiler must reload data_02191f40 base into r0 for this arg — if it uses the stale r0 from the earlier p pointer, the arg will be wrong; keep the arg expression explicit as `&data_020c6670_obj`
- If pool ordering is wrong, try declaring a local pointer to data_02191f40 at function entry to force a single pool materialisation that the compiler reuses for all field accesses
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_0201fc64` | `data_02191f40` | data symbol |
| `.L_0201fc68` | `data_020c6670` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0201f19c` | main TU call |
| `bl` | `func_0201f138` | main TU call |
| `bl` | `func_0201fc6c` | main TU call |
| `bl` | `func_0201fd24` | main TU call |
| `blx` | `r1` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

