# func_0202bb88 (main, HIGH class A, 64B)

**Recipe:** counted byte-clear loop, guard, no calls
**Risk:** loop may index [base+0xf6c, i] vs the asm's pointer-walk (add r3,#1); ldr [r0] needs int-typed count

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0202bb88.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0202bb88.c func_0202bb88 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0202bb88.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
