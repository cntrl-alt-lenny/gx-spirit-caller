---
# func_02044724 (main, HIGH class A, 52B)

**Recipe:** memset/fill wrapper + recomputed-base byte SETTER (lsl #8)
**Risk:** post-call address may be cached differently; func_0209448c arg order (val,ptr,len) assumed from regs
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode B — hidden callee arg-order mismatch; verify.py wildcards the BL reloc, only ninja report catches it

**Why this recipe hits this mode:** `func_0209448c`'s parameter order `(val, ptr, len)` was inferred from register usage in the asm; if the actual signature is `(ptr, len, val)` or another order, the wrong arguments land in r0/r1/r2 but the BL reloc is wildcarded by verify.py, so the mismatch only surfaces at ninja report.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py). The slot address `base + (idx << 8)` is computed from parameters; no pooled global addresses.

**Verify gate sequence:**
1. verify.py src/main/func_02044724.c func_02044724 [--cc all] — instruction bytes
2. ninja report — resolves BL targets; catches Mode B mismatch (wrong arg order to func_0209448c) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Cross-check the orig .s BL/BLX instruction at the func_0209448c call site: read off which register holds val (0), which holds p, and which holds len (0x100) to confirm the `(int a, void *p, int n)` signature order
- If ninja report shows a wrong-register argument, swap the parameter order in the extern declaration and the call site (try `(void *p, int n, int a)` or `(int n, void *p, int a)`)
- Post-call, the slot address `p = base + (idx << 8)` must be rematerialized (not cached in a callee-saved register); write `p[0xe7] = 0xff` as `base[(idx << 8) + 0xe7] = 0xff` if the compiler reuses a stale cached address
- The prologue saves r4, r5 with a sub sp,#4 pad (odd push count); if the compiler saves fewer registers and omits the pad, try adding a local variable to force an extra spill
- Confirm the lsl #8 encoding: `idx << 8` must not be strength-reduced to a multiply; if it is, use `(unsigned int)idx << 8` to keep the shift form
---
