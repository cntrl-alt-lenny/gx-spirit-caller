---
# func_020458d8 (main, HIGH class A, 100B)

**Recipe:** DISPATCH switch -> cmp/beq chain of pointer-arg tail calls
**Risk:** low; mwcc could pick a jump table, but 3 dense cases normally stay a cmp chain
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word mismatch; wrong global array addresses wildcarded by verify.py, only ninja report catches it
**Secondary mode:** Mode C — jump table risk; if mwcc emits a branch table instead of cmp/beq chain, extra .rodata section breaks link silently

**Why this recipe hits this mode:** `data_020ff890`, `data_020ff8b8`, and `data_020ff8dc` are named global arrays whose addresses are emitted as pool .words and passed as pointer arguments; verify.py wildcards these pool loads, so wrong addresses pass silently until ninja report. Additionally, mwcc may choose a jump table for 3 dense cases (cases 0,1,2), which would emit an extra .rodata section and break the link silently (Mode C).

**Pool words to verify against orig .s:**
- `data_020ff890` — pointer arg for case 0 call to func_0204067c
- `data_020ff8b8` — pointer arg for case 1 call to func_0204067c
- `data_020ff8dc` — pointer arg for case 2 call to func_0204067c

**Verify gate sequence:**
1. verify.py src/main/func_020458d8.c func_020458d8 [--cc all] — instruction bytes; Mode C jump-table presence visible as section diff
2. ninja report — resolves pool words; catches Mode A mismatches (wrong data_020ff8xx addresses) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep cases in source order 0, 1, 2 with explicit break statements; mwcc lays case branches in source order for a cmp/beq chain, so any reordering changes the branch sequence
- If verify.py or ninja report shows a jump table (extra .rodata section), add a `default: break;` or restructure as an if/else-if chain to prevent mwcc from generating a branch table
- Confirm each of the three data_020ffXXX addresses via ninja report; all three are pool .words wildcarded by verify.py and a single wrong address passes silently
- Verify the func_0204067c BL encoding is consistent for all three cases; if one case emits a BLX instead of BL (or vice versa), the callee declaration may need a `void` return vs a pointer return distinction
- The switch has no default path and three non-returning break targets; if the compiler emits an unconditional branch past the last case, ensure the source has no statements after the closing brace
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ff890` | data symbol |
| `_LIT1` | `data_020ff8b8` | data symbol |
| `_LIT2` | `data_020ff8dc` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0204067c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

