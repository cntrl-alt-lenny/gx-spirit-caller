---
# func_02040de8 (main, HIGH class A, unknown B)

**Recipe:** counted loop over 0x20 stride-8 table; +0x1000 field; recompute base each iter
**Risk:** i<<3 must lower to add r0,base,r4,lsl#3 (not strength-reduced to a pointer bump) — risk it bumps; signed blt
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E (VISIBLE) — strength-reduction of `i<<3` to a pointer bump changes the address-calculation instructions; visible instruction-byte diff caught by verify.py

**Why this recipe hits this mode:** if mwcc strength-reduces the `i << 3` index expression to an incrementing pointer (adding 8 each iteration), it emits `add rX, #8` instead of `add rX, base, r4, lsl #3`; the two sequences differ in byte layout and verify.py catches the divergence immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02040de8.c func_02040de8 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Use `(char *)base + (i << 3) + 0x1000 + 0xa34` spelled out explicitly each iteration so mwcc cannot strength-reduce to a pointer bump; do not introduce a running pointer variable
- Keep `i` as a signed `int` loop counter with `i < 0x20` so mwcc emits `blt` (signed branch-less-than), not `blo` (unsigned)
- The two field accesses `+0xa34` (int check) and `+0xa38` (pointer return) differ by 4; write them as separate casts from `(char *)base` rather than as struct members to preserve the lsl-based addressing
- The combined constants `0x1000 + 0xa34 = 0x1a34` and `0x1000 + 0xa38 = 0x1a38` may require pool loads if they cannot encode as ARM immediates; verify which form the orig .s uses before tweaking
- If strength-reduction still occurs with the explicit `i << 3` form, try `volatile int vi = i; ... + (vi << 3) + ...` as a last resort to block the optimisation
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020aaf40` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

