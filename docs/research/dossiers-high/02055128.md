# func_02055128 (main, HIGH class A, 72B)

**Recipe:** stack marshalling, literal args, !=-1 guard, return out
**Risk:** the unconditional ldrne/mov r0 (out left uninitialised on -1 path) and &len passed both stacked+r3 are hard to force; low

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02055128.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02055128.c func_02055128 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02055128.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
