---
# func_02055128 (main, HIGH class A, 72B)

**Recipe:** stack marshalling, literal args, !=-1 guard, return out
**Risk:** the unconditional ldrne/mov r0 (out left uninitialised on -1 path) and &len passed both stacked+r3 are hard to force; low
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (ldrne on success path, uninitialised-out on failure path, dual &len passing)

**Why this recipe hits this mode:** The orig emits `ldrne r0, [sp, #out_offset]` on the not-equal path and falls through unconditionally, meaning `out` is unread on the `-1` path; the C draft's dual `return out` attempts to match this but mwcc may still emit a mov or branch that differs from the orig conditional-load pattern.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02055128.c func_02055128 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The dual `return out` (one inside the if, one after) is the mechanism to produce a single `ldrne` without a branch; do not collapse them into one `return` or add an `else`.
- `len` must be stack-allocated at a specific offset (likely `sp+8` per the comment) so that `&len` passed as the 4th arg produces the correct `add r3, sp, #8` instruction.
- The literals `0xffff` and `0x1001` must load into `r0` and `r1` respectively; if the compiler emits `mvn r0, #0` instead of a literal load for `0xffff`, try `(int)0xffff` or `0x0000ffff` as the literal.
- If the `&len` dual-pass (stacked and in `r3`) does not reproduce, confirm `func_02054c78`'s 4th parameter is declared as `int *plen` and that `&len` is passed directly rather than via a pointer copy.
- This function should only match if `02055170` also matches (they are clones differing only in the second literal); coordinate testing of both together.
---
