---
# func_0200efe0 (main, HIGH class A, unknown B)

**Recipe:** wrapper call(func_0200ef60) + flag-guard + conditional stores + setter tail
**Risk:** a1/a2 pass-through to ef60; r0 from ef60 then re-fetched via edb8; &&-store; offsets 0x98/0x9c guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode B (HIDDEN) — callee addresses func_0200ef60 and func_0200edb8 are BL targets wildcarded by verify.py BL reloc; only ninja report catches a wrong callee address

**Why this recipe hits this mode:** Both `func_0200ef60` and `func_0200edb8` are extern BL calls whose addresses are resolved by the linker via BL relocations; verify.py wildcards these so a misspelled extern target passes instruction-byte checks but breaks `ninja report`/`ninja sha1`.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0200efe0.c func_0200efe0 [--cc all] — instruction bytes (BL targets wildcarded)
2. ninja report — resolves BL relocations; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Cross-check both extern addresses (`func_0200ef60` at 0x0200ef60, `func_0200edb8` at 0x0200edb8) against the .s BL instructions — misspelled externs are invisible to verify.py
- The `&&`-store (`r != 0 && payload != 0`) must be written as a single `if (r != 0 && payload != 0)` compound guard to match the mwcc short-circuit emission; splitting into two nested ifs may change the branch shape
- Keep the two stores in pool order: `r[0x98/4] = (int)payload` before `r[0x9c/4] = flag` to match the .s str sequence
- Offsets 0x98 and 0x9c are guessed; if objdiff shows mismatched immediates, verify against the .s ldr/str offset fields
- The final `return (int)r` (returning the fetched record pointer as int) must not be converted to `return r != 0` — the original returns the pointer value, not a boolean
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0200ef60` | main TU call |
| `bl` | `func_0200edb8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

