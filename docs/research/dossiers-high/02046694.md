---
# func_02046694 (main, HIGH class A, 96B)

**Recipe:** counted byte-scan loop + tail-call(index); sp3 tier
**Risk:** loop induction (p++ vs base[i]) may schedule ldrb/add differently; ble-vs-blt guard ordering could flip
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (scheduling / induction form)

**Why this recipe hits this mode:** The loop induction style (pointer-increment `p++` vs array-index `base[i]`) changes how mwcc emits the LDRB/ADD pair inside the loop body; the wrong form produces a byte-different instruction sequence that verify.py will catch immediately. Also, the pre-loop BLE vs BLT guard for the zero-count case can flip depending on how the comparison is written.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02046694.c func_02046694 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Use the pointer-increment form `p++` inside the loop body (not `base[i]`) to match the LDRB/ADD scheduling seen in the .s: `ldrb r2,[r3]; ... add r3,#1`.
- Keep the loop counter `i` as a separate integer that is incremented after the LDRB: mirroring `add r0,#1; cmp r0,r1; add r3,#1` with `i++; if (i < n) continue;`.
- For the pre-loop zero-count guard: write `if (n <= 0) ...` (BLE) not `if (n == 0) ...` — the .s shows `ble .L_2ac`.
- After the loop, test `i == n` (not `base+n == p`) to trigger the exhausted-path return 0 — keeps the compare in the right register.
- Verify the tail call `func_0204fa7c(i)` passes the loop counter directly in r0 — no additional adjustment before the call.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0204f998` | main TU call |
| `bl` | `func_0204fa7c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

