# func_02230a30 (ov002, HIGH class A, 192B)

**Recipe:** global-load hoist, guard chain, byte-unpack, 6-arg call w/ stack args
**Risk:** cd3f4[1]/[8] loads must hoist before the first call (callee-saved r4/r5); 02257594 called 1-arg vs its 2-arg .c.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay002/func_ov002_02230a30.c func_ov002_02230a30 --cc all --module ov002` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode B mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov002_022cd3f4` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_02257594` | overlay call (ov002) |
| `bl` | `func_ov002_0223df38` | overlay call (ov002) |
| `bl` | `func_ov002_0223de94` | overlay call (ov002) |
| `bl` | `func_ov002_0225764c` | overlay call (ov002) |
| `bl` | `func_ov002_021c8470` | overlay call (ov002) |
| `bl` | `func_ov002_021d5a08` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Cross-check every BL/BLX target address in the .s against your `extern func_XXXX` declarations
- BL relocs are wildcarded by verify.py; only `ninja report` resolves them
- Wrong callee address passes verify.py at 100% but fails sha1

## Reference

- Prep: `docs/research/c-match-prep/02230a30.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
