---
# func_0203db24 (main, HIGH class B, 76B)

**Recipe:** two-call guard, mvn-compare (==-0x27), predicated returns
**Risk:** -0x27 must form via mvn #0x26 (signed int compare); the two return-1 paths (e==0, e==-0x27) must merge as beq .L478 / fallthrough; reg-alloc on e.
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (reg-alloc / branch-merge shape), verify.py catches directly

**Why this recipe hits this mode:** The compiler must emit `mvn r1,#0x26` for the constant -0x27 and must merge the two return-1 paths into a single `beq` forward branch; any change to `e`'s register assignment or condition-code usage breaks instruction bytes in a way verify.py shows immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203db24.c func_0203db24 [--cc all] — instruction bytes visible directly
2. ninja sha1 — final gate

## Reshape hints

- Keep `e` as a plain `int` local so the compiler can allocate it to a single register without spilling across the two comparisons
- For the mvn #0x26 form: write the constant as `-0x27` (not `~0x26`) so the signed int path naturally produces `mvn`; do not cast to unsigned
- The two return-1 paths (e==0 and e==-0x27) must produce a `beq` forward-branch to a shared return-1 block; if they instead form two separate `mov r0,#1 / bx lr` sequences, split into two explicit `if (e == 0) return 1; if (e != -0x27) return 0; return 1;` statements (already done in prep)
- Verify `func_0206df90` guard produces the `cmp r0,#0 / bne` at the top without an extra load
- Run `verify.py --cc all` first; any remaining diff will be a reg-alloc or branch-order issue visible in the instruction trace
---
