# func_0203db24 (main, HIGH class B, 76B)

**Recipe:** two-call guard, mvn-compare (==-0x27), predicated returns
**Risk:** -0x27 must form via mvn #0x26 (signed int compare); the two return-1 paths (e==0, e==-0x27) must merge as beq .L478 / fallthrough; reg-alloc on e.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203db24.c)

**Verify gate:**
1. python tools/verify.py src/main/func_0203db24.c func_0203db24 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/0203db24.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
