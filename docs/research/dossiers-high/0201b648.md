---
# func_0201b648 (main, HIGH class A, ?B)

**Recipe:** Clear loop (split-bic via ~0x1ff) + single-field clear + tail call
**Risk:** mwcc must split ~0x1ff as bic 0xff then 0x100 in that order; struct stride 0x18 guessed.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible diff if mwcc encodes `~0x1ff` as a different immediate sequence or reorders the two `bic` instructions

**Why this recipe hits this mode:** `~0x1ff` (= 0xFFFFFE00) is not a single ARM immediate, so mwcc must emit `bic reg,#0xff` followed by `bic reg,#0x100` in that exact order; if the compiler reorders or combines them differently the instruction bytes change visibly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_0201b648.c func_0201b648 [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- Write `e->f8 &= ~0x1ff;` exactly as shown; do not expand to `e->f8 &= 0xFFFFFE00;` or `e->f8 = e->f8 & ~0x1ff;` — the `~0x1ff` literal form is what drives the two-bic split.
- Verify in objdiff that the two `bic` immediates appear as `bic #0xff` then `bic #0x100` (in that order); if reversed, try an explicit two-step: `e->f8 &= ~0xff; e->f8 &= ~0x100;`.
- Struct stride 0x18: the `Ent18` layout uses six ints (6 × 4 = 24 = 0x18); field `f8` is at offset +8 (third int); verify the `ldr`/`str` displacement in the orig .s is +8.
- Loop count 0x56: keep as `i < 0x56` (not `<= 0x55`) to match the orig `cmp`/`blt` encoding.
- The tail call `func_0201b60c(0)` should be a plain call at the end, not inlined or reordered before the `base[0x8f8] &= ~0xff` store.
---
