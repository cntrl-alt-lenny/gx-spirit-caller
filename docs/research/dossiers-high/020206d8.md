---
# func_020206d8 (main, HIGH class A, 33B)

**Recipe:** guard, store halfword, struct callback blx, reload field
**Risk:** struct lacks f30 member referenced; base reloaded each ldr r0,_LIT0 (RELOAD recipe); callback arg order r0=data,r1=fc; offsets guessed
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — named globals data_02191f40 (multiple field accesses, reloaded each time) and data_020c67f8 (callback arg) used as pool words; the RELOAD pattern means the base is materialised multiple times from the pool; verify.py wildcards each occurrence, only ninja report catches value mismatches
**Secondary mode:** Mode E — visible instruction byte diff (each reload of the base may use a different pool slot index; callback arg order r0=data,r1=fc may not match source expression order)

**Why this recipe hits this mode:** The RELOAD recipe explicitly reloads the data_02191f40 base from the literal pool before each field access rather than keeping it in a register; this produces multiple pool entries for the same symbol, and the compiler's choice of when to re-materialise versus reuse affects instruction bytes that verify.py will catch on the non-pool bytes while missing the pool values.

**Pool words to verify against orig .s:**
- `data_02191f40` — global struct base; reloaded for each of: f30 guard, f138 call context, f6 read, fc write, f38 callback load
- `data_020c67f8` — byte object address passed as r0 to the callback blx

**Verify gate sequence:**
1. verify.py src/main/func_020206d8.c func_020206d8 [--cc all] — instruction bytes
2. ninja report — resolves all occurrences of pool words data_02191f40 and data_020c67f8; catches Mode A mismatches
3. ninja sha1 — final gate

## Reshape hints

- Do NOT cache data_02191f40 in a local pointer — the RELOAD pattern requires the base to be reloaded from the literal pool before each use; introducing a local would collapse the reloads into a single load and change the instruction sequence
- The guard is `data_02191f40.f30 != 7` calling func_02093bfc (not a return — the function continues); confirm the .s does a BL then falls through rather than branching past the body
- Callback arg order is r0=&data_020c67f8, r1=data_02191f40.fc; write the call as `data_02191f40.f38(&data_020c67f8, data_02191f40.fc)` with fc read from the global (post-store) so the compiler reloads it as r1
- The struct definition references f30 in the source but the struct definition only has f0/f6/fc/f38; add a `char pad[0x30-0xe]; int f30; char pad2[...];` member to cover the guard field, or the compiler will refuse to compile
- Field offsets are guessed; if verify.py shows unexpected load offsets, check f6 (halfword read, +0x6), fc (halfword write, +0xc), and f38 (callback pointer, +0x38) against the actual .s LDR offsets
---
