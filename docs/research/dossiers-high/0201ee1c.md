---
# func_0201ee1c (main, HIGH class A, 72B)

**Recipe:** DISPATCH switch (cmp-chain) + struct copy + TAIL CALL
**Risk:** 3 contiguous cases may lower to a jump table not the cmp #0/#1/#2 chain; field offsets guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (switch lowering to jump table vs cmp-chain)
**Secondary mode:** Mode A — field offset assumptions; named `data_XXXX` not used as pool globals but wrong struct layout would shift loads

**Why this recipe hits this mode:** MWCC may lower a switch over three contiguous cases (0/1/2) to a jump table, producing a completely different branch pattern from the expected cmp-chain; a jump table also inserts a .rodata section which would be caught by verify.py as a layout difference.

**Pool words to verify against orig .s:**
No named-symbol pool words — all callee addresses are BL targets (Mode B), not pool-address loads.

**Verify gate sequence:**
1. verify.py src/main/func_0201ee1c.c func_0201ee1c [--cc all] — instruction diff is visible directly (jump table vs cmp-chain)
2. ninja sha1 — final gate

## Reshape hints

- If the compiler emits a jump table, force cmp-chain by converting the switch to an if-else-if chain: `if (sel == 0) { } else if (sel == 1) { } else if (sel == 2) { }` — MWCC avoids the table for non-contiguous or explicit if-chains
- Verify field offsets in the struct: f_0 at +0x00, f_4 at +0x04, pad at +0x08 (4 bytes), f_c at +0x0c — wrong padding shifts every load and produces visible byte diffs
- The epilogue struct copy `out->g_0 = p->f_0; out->g_4 = p->f_4;` must precede the tail call; MWCC may merge these stores if they are reordered
- The tail call `return func_0207f850(out, sel, a1)` is the only BL target used as a return value; make sure it is expressed as a return so MWCC can emit a tail BL
- Cross-check each case's call sequence against the .s: case 0 calls func_02090114 then func_020900a0 then func_02090048; cases 1/2 differ by the p->f_4 branch guard
---
