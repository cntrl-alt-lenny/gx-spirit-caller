# func_02064158 (main, HIGH class A, 176B)

**Recipe:** state==5||6 dispatch; 5-arg call w/ stacked arg; bool-normalize; 1-arg thunk
**Risk:** 5th stacked arg + outgoing-arg sub sp,#8 should match; f9c/state offsets guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_02064158.c func_02064158 --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020643d8` | main TU call |
| `bl` | `func_02054140` | main TU call |
| `bl` | `func_02061798` | main TU call |
| `bl` | `func_02061b60` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

