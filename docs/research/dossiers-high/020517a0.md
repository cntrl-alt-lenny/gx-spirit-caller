# func_020517a0 (main, HIGH class A, 364B)

**Recipe:** linear init: repeated RELOAD + zero-stores in store-order; Fill helper; stacked args
**Risk:** ASM IS TRUNCATED in batch (76 lines, ends mid-body) — tail stores+epilogue/routing UNKNOWN; offsets/Fill32 sig guessed

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/020517a0.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_020517a0.c func_020517a0 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/020517a0.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
