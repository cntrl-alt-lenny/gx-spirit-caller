---
# func_020517a0 (main, HIGH class A, 364B)

**Recipe:** linear init: repeated RELOAD + zero-stores in store-order; Fill helper; stacked args
**Risk:** ASM IS TRUNCATED in batch (76 lines, ends mid-body) — tail stores+epilogue/routing UNKNOWN; offsets/Fill32 sig guessed
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; store order and offset values caught by verify.py (but tail is unknown due to truncation)

**Secondary mode:** Mode B — BL targets for func_02049900 / func_020498dc / func_020945f4 wildcarded by verify.py reloc

**Why this recipe hits this mode:** The prep explicitly notes the asm source was truncated mid-body; the tail stores, final epilogue instructions, and compiler routing (sp1p5 vs sp3) are all unknown and guessed. The visible portion has a long ordered store sequence whose relative order must be exact and verify.py catches any reorder, but the hidden tail makes this a high-risk incomplete candidate that cannot sha1 as written.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020517a0.c func_020517a0 [--cc all] — instruction bytes for the visible portion; any store-order diff is immediately visible
2. ninja report — resolves BL targets for func_02049900, func_020498dc, func_020945f4; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- IMPORTANT: obtain the complete .s disassembly for 0x020517a0 from the orig before attempting a final match; the prep draft is cut off mid-body and the incomplete C cannot sha1.
- The visible store sequence must follow this exact offset order: 0x0 (word), 0x4 (word), 0x8 (word), 0x10 (word=0), 0x1c (word=0), 0x1a (short=0), 0xe4 (word=0), then func_020498dc(0), then 0xf (byte=0), 0x19 (byte=0), 0x1af (byte=0), 0x1b6 (short=0 via add+strh), 0x1dc (word=0), 0x200 (word=0), 0x2f4–0x300 (stacked args), func_020945f4 Fill call — any reorder produces a visible diff.
- The short store at +0x1b6 must use the two-instruction pattern `add r0, base, #0x100; strh [r0, #0xb6]` — write as `*(short*)((char*)func_020498f0() + 0x100 + 0xb6) = 0` to reproduce the split-add addressing.
- The accessor `func_020498f0()` must be reloaded before each store group; do not hoist the pointer.
- Because epilogue routing is unknown, try `func_020517a0.c` (sp1p5 default) first; if verify.py shows a prologue mismatch, also try `func_020517a0.legacy_sp3.c`.
---
