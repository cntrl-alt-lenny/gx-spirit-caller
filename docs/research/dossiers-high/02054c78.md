---
# func_02054c78 (main, HIGH class A, 48B)

**Recipe:** drop dead leading args + helper call + tail call constants
**Risk:** func_02054ea8 return assumed int; *a4 deref is word; dead args must emit no code
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (dead-arg register emission and tail-call constant load)

**Why this recipe hits this mode:** The three dead leading args (a0..a2) must vanish entirely from the emitted code; if mwcc spills or references any of them the instruction count changes visibly. The tail call passes literal `0` and `-1` whose load sequences are also byte-sensitive.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054c78.c func_02054c78 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The dead args `a0`, `a1`, `a2` must be declared in the signature to hold their register slots but must generate zero instructions; if the compiler references them, add `(void)a0; (void)a1; (void)a2;` casts to suppress accidental spills.
- `a4` is a pointer to `int`; keep it as `int *` and dereference with `*a4` so mwcc emits a word load (`ldr`) not a byte or halfword load.
- The tail call `func_02054ea8(0, -1)` must resolve to a `b` branch not `bl`+`pop`; return the result directly without an intervening local assignment.
- If objdiff shows an extra `mov` or register shuffle before the call to `func_020945f4`, try removing the `(void)` casts and relying on the optimizer to elide dead reads.
- Keep `func_020945f4` declared as `void` return; a non-void return type might cause the compiler to emit a result-move before the tail call.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020945f4` | main TU call |
| `bl` | `func_02054ea8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

