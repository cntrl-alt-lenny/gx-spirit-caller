---
# func_0201f6c0 (main, HIGH class A, 33B)

**Recipe:** guard chain, struct callback blx, branch tails
**Risk:** cb is loaded into r0 base then reloaded as arg via _LIT2; the ldr r1,[r0,#0x38] then ldr r0,_LIT2 ordering may differ; field offset 0x38 assumed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — pool-word order; named globals data_02191f40, data_021922e0, data_020c6578_obj used as pool addresses; wrong order is wildcarded by verify.py, only ninja report catches it
**Secondary mode:** Mode E — visible instruction byte diff (ldr ordering around the callback field load)

**Why this recipe hits this mode:** The function loads data_02191f40.cb (field +0x38) into a temp register, then separately loads &data_020c6578_obj as the call argument — the compiler must materialize two separate pool words for these, and their relative ordering in the literal pool may differ from what verify.py wildcards out.

**Pool words to verify against orig .s:**
- `data_02191f40` — struct base; field +0x38 loaded as callback pointer
- `data_021922e0` — pointer passed to func_020a05f8 as guard arg
- `data_020c6578_obj` — object address passed as argument to the callback blx

**Verify gate sequence:**
1. verify.py src/main/func_0201f6c0.c func_0201f6c0 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A ordering mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Match the exact load order in the .s: load data_021922e0 for the first guard, then load data_02191f40 for func_0201f724, then load data_02191f40.cb (field +0x38), then load &data_020c6578_obj — any reorder shifts the pool
- If the compiler merges the two data_02191f40 pool literals into one, try declaring the cb local before the second guard so the base stays live in r0 when the field is read
- The callback blx uses a two-register sequence: r1 = [r0, #0x38] then r0 = &data_020c6578_obj; if the compiler emits these swapped, bind cb via a separate local so the base reload is explicit
- Field offset 0x38 is assumed — if verify.py shows a load from a different offset, adjust the struct pad to match
- The `if (cb != 0)` null-check before the blx must not be hoisted; keep the check and call in strict source order
---
