# func_0203dae0 (main, HIGH class A, 68B)

**Recipe:** Two-helper-preamble then switch(==7/==8) dispatch; result in callee-saved
**Risk:** func_0203c888 return cached in r4 then overwritten only in cases; default returns the c900 setup value

## SHA1 pitfall

**Primary mode:** Mode B - Wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203dae0.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0203dae0.c func_0203dae0 --cc all (instruction bytes)
2. ninja report (resolves pool/BL words; catches Mode B mismatch)
3. ninja sha1 (final gate)

## Reshape hints

- Cross-check every BL/BLX target address in the .s against your extern func_XXXX declarations
- BL relocs are wildcarded by verify.py; only ninja report resolves them
- Wrong callee address passes verify.py at 100% but fails sha1

## Reference

- Prep: docs/research/c-match-prep/0203dae0.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203c888` | main TU call |
| `bl` | `func_0203c900` | main TU call |
| `bl` | `func_0203d9c4` | main TU call |
| `bl` | `func_0203d888` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

