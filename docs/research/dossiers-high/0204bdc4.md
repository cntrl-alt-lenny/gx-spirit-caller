---
# func_0204bdc4 (main, HIGH class A, 124B)

**Recipe:** SETTER f20c + call(byte fd+1) + branch: flag-bracketed call vs else call
**Risk:** fd/f20c/f1a8 offsets; movs r4 zero-test selects branch; arg fd reload
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — Wrong callee address (hidden from verify.py; BL reloc wildcarded, only ninja report catches it)

**Why this recipe hits this mode:** `func_020498f0` is called multiple times via BL to get the accessor pointer, and `func_02046770`, `func_0206255c`, and `func_0204fc38` are all BL targets; verify.py wildcards all BL relocs so any wrong callee address is silent until `ninja report`.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0204bdc4.c func_0204bdc4 [--cc all] — instruction bytes
2. ninja report — resolves BL relocs; catches Mode B callee-address mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Reload `func_020498f0()->fd` for the second use (in the else branch `func_0204fc38(a0, acc()->fd + 1)`) rather than caching `fd` in a local; the .s reloads the byte field from the accessor each time it is needed
- The `movs r4, r0` zero-test pattern means the return value of `func_02046770` is stored into r4 and tested; express this as `r = func_02046770(...)` and then `if (r)` so MWCC can emit the movs/beq sequence
- Field offsets fd (0x0d), f1a8 (0x1a8), and f20c (0x20c) are guessed; cross-check the `strb`/`ldrb`/`str` offsets in the orig .s against these values and adjust pad sizes if they differ
- In the true branch, the f1a8 flag-set (2), call to `func_0206255c(*r)`, and flag-clear (0) must appear in that exact order, bracketing the call; misplacing either store changes the visible instruction sequence
- Cross-check every BL target address (`func_020498f0`, `func_02046770`, `func_0206255c`, `func_0204fc38`) in the orig .s before running `ninja sha1`
---
