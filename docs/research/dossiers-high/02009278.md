---
# func_02009278 (main, HIGH class A, unknown B)

**Recipe:** GUARD/DISPATCH switch + TAIL CALL (case0 returns callee)
**Risk:** callee-saved reg slot for saved args may differ; func arg types guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from callee-saved register slot choice for saved arguments across the call to `func_02008c10`

**Why this recipe hits this mode:** `func_02008c10()` is a void call that clobbers argument registers; mwcc must save `sel` and `a` across it in callee-saved registers. The exact register slot chosen (e.g. `r4`/`r5` vs `r4`/`r6`) depends on the function signature and local variable layout, producing a visible byte difference in the prologue/epilogue push/pop that `verify.py` catches directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02009278.c func_02009278 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- If the callee-saved register slot mismatches: try adjusting the function signature — changing `int a` to a typed parameter (e.g. `unsigned int`) or reordering declarations can shift register allocation and change which callee-saved slot mwcc picks
- The `func_0200924c(a)` in case 0 is a direct return; keep it as `return func_0200924c(a)` so mwcc can tail-call-optimize if the epilogue matches
- Verify the `func_02008c84(1, a, 0)` call's third argument is `0` (not a register copy) against the orig .s; if orig emits `mov r2,r0` using a cached zero, try `func_02008c84(1, a, sel-1)` when sel==1 to steer register reuse
- Compared to `func_020091f4`, this function has one fewer parameter (`int b` is absent); the narrower signature means fewer arguments to save across `func_02008c10`, which may put both saved values in `r4`/`r5` — verify the orig push list
- The `return 1` in the case-1 branch should produce `mov r0,#1` after the `func_02008c84` call returns; if objdiff shows `mov r0,r4` instead, the compiler reused the callee-saved copy of `sel` — adjust the source to steer toward the expected form
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02008c10` | main TU call |
| `bl` | `func_0200924c` | main TU call |
| `bl` | `func_02008c84` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

