---
# func_02001e5c (main, HIGH class A, unknown B)

**Recipe:** ACCESSOR (signed-byte) + double-negation bool (!(p&&*p))
**Risk:** movne/moveq order for the !(...) may flip; field offset 0x28 assumed pointer.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff in movne/moveq conditional move order

**Why this recipe hits this mode:** The `!(q && *q)` double-negation forces a `cmp/movne/moveq` chain; mwcc may emit `movne r0,#0; moveq r0,#1` or the opposite polarity depending on how it lowers the negated `&&`, producing a visible byte diff that `verify.py` will catch directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02001e5c.c func_02001e5c [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- If movne/moveq polarity flips: try inverting the return expression — `return (q == 0 || *q == 0)` vs the `!` form to steer which branch carries zero vs one
- The `cmpne r0,#0` after the `ldrnesb` is the canonical mwcc double-cmp for `&&`; keep both operands on the right side of the `&&` (no short-circuit rewrite)
- Field offset 0x28: if objdiff shows a wrong immediate, verify the struct padding — count each `_pad` byte against the disassembly's `ldr r0,[r0,#0x28]`
- The `signed char *` at +0x28 must stay a pointer-to-signed-char (not `int *` or `char *`) so mwcc emits `ldrnesb` (signed-byte extend) rather than `ldrne` or `ldrneb`
- If the match fails at the `cmpne` stage, try a two-step local: `int nz = (q != 0) & (*q != 0); return !nz;` to separate the two comparisons and force a particular movXX assignment
---
