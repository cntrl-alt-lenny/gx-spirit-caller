---
# func_02053bfc (main, HIGH class A, 56B)

**Recipe:** decrementing for-loop calling helper; signed >=0 exit
**Risk:** reg-alloc-walled per .s header: r4/r5 counter-vs-self assignment may rotate; loop epilogue dup may differ
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; reg-alloc rotation produces different register assignments that verify.py sees directly

**Why this recipe hits this mode:** The loop holds `i` in r4 and `self` in r5, but MWCC may swap the counter/self register assignment (r4/r5 rotation) yielding a different register encoding in the loop body; the epilogue duplication at early-exit (addmi+ldmmiia path) may also differ if MWCC elides or reorders it.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02053bfc.c func_02053bfc [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- If r4/r5 are rotated in the diff, introduce a local `self2 = self` binding before the loop to force MWCC to commit the preferred allocation before the first call
- The `subs r4,r0,#1` / early-exit path requires the initial `n-1` expression to be the loop initializer directly — do not factor out `n` as a separate local before subtracting
- If the epilogue (addmi+ldmmiia) does not appear, try writing the `i >= 0` test as the for-condition rather than a do-while to match the signed-bpl loop shape
- Use `void *self` (pointer, not typed struct) so MWCC does not insert type-cast load sequences that perturb r5 before the first call
- Run verify.py --cc all to identify which SP variant places r4/r5 correctly before iterating on reshape
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02054140` | main TU call |
| `bl` | `func_02053e58` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

