# func_02053bfc (main, HIGH class A, 56B)

**Recipe:** decrementing for-loop calling helper; signed >=0 exit
**Risk:** reg-alloc-walled per .s header: r4/r5 counter-vs-self assignment may rotate; loop epilogue dup may differ

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02053bfc.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02053bfc.c func_02053bfc --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02053bfc.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
