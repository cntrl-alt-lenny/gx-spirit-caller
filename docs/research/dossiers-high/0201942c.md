---
# func_0201942c (main, HIGH class A, 96B)

**Recipe:** dense range switch -> addls jump-table, case0 returns 0 else 1
**Risk:** case0-and-default both return 0 may collapse to a range compare instead of the 18-arm jump table; table arm layout must match exactly
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff if mwcc collapses the dense switch to a `sub;cmp;movls` range compare instead of emitting an 18-entry `addls pc` jump table

**Why this recipe hits this mode:** The switch has 18 contiguous arms (0x4c..0x5d) with case 0x4c returning 0 and all others returning 1. mwcc may detect that `case 0x4c` and `default` share the same return value (0) and collapse the entire dispatch to an `rsb;cmp;movhi #1; bx lr` range compare, eliminating the jump table. The .s uses an explicit 18-entry `addls pc` table; if the C output is a range compare, the byte sequence differs completely and verify.py will catch it.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0201942c.c func_0201942c [--cc all] — jump-table presence vs range-compare is immediately visible
2. ninja sha1 — final gate

## Reshape hints

- If mwcc collapses to a range compare, make case 0x4c's return value distinct from the default: confirm in the .s that case 0x4c truly returns 0 and that the jump table index-0 arm branches to the `mov r0,#0; bx lr` epilogue — if so the switch-written form is correct and the optimizer is the problem
- Try adding a dummy `case 0x4c: { volatile int _ = 0; return _; }` to defeat constant-folding; or add `__attribute__((noinline))` if supported — these are last-resort; the goal is the plain switch first
- Confirm the base is `0x4c` (not `0x4d`): the .s subtracts `0x4c` and range-checks `<= 0x11` (17 = 0x11), covering 18 entries (0x4c..0x5d); if case 0x4c is the zero return and the table has 18 arms, the switch as written is correct
- The `addls pc` table arm for index 0 points to the 0-return epilogue; all other 17 arms point to the 1-return epilogue — this means 17 identical table entries; some compilers deduplicate these into a range check; verify the .s table contains 18 distinct entries or a shared epilogue
- If verify shows the correct jump-table shape but wrong table size, adjust the switch range by adding or removing explicit cases at the boundary
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

