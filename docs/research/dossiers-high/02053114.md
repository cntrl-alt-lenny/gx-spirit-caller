---
# func_02053114 (main, HIGH class A, 68B)

**Recipe:** clean compute + cmp-returning-bool (moveq/movne)
**Risk:** f_3c offset guessed; 0x400 buffer must stay one array (not split); poly literal pool placement
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; moveq/movne bool order and `0xedb88320` literal pool placement are compared directly by verify.py

**Why this recipe hits this mode:** the `return crc == self->f_3c` expression compiles to `cmp` + `moveq`/`movne`; if the true/false assignment order is inverted the instruction bytes differ immediately. The large poly constant `0xedb88320` lands in the literal pool and is compared byte-for-byte by verify.py.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (`0xedb88320`, `0x3c`) are compared directly by verify.py.

**Verify gate sequence:**
1. verify.py src/main/func_02053114.c func_02053114 [--cc all] — instruction diff visible directly (moveq/movne order, poly pool word)
2. ninja sha1 — final gate

## Reshape hints

- If the bool emits `movne r0, #0 / moveq r0, #1` instead of `moveq r0, #1 / movne r0, #0`, flip the comparison operands: `return self->f_3c == func_020a66e8(buf, self, 0x3c)` — the reversed operand order may change which condition code fires
- Keep `buf[0x400]` as a single contiguous stack array; splitting it changes the frame size and breaks `sub sp, #0x400` in the prologue
- The `0x3c` passed to `func_020a66e8` is the byte-count of the CRC region — verify against the orig .s that the load reads exactly `0x3c` bytes and not a different field boundary
- If field `f_3c` offset is wrong, cross-reference `func_020532f4` which stores `self->f_3c = crc` at the same offset — the store offset there pins the correct value
- Do not add any extra local variables; the only stack slot needed beyond `buf` is the implicit saved register for `self` (r4)
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0xedb88320` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020a67cc` | main TU call |
| `bl` | `func_020a66e8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

