---
# func_02052704 (main, HIGH class A, 40B)

**Recipe:** cmp chain returning constant (DISPATCH); leaf bx lr
**Risk:** pure leaf; (r0 in {2,3,4})->0xc else 0x8; compare-order matches source 2,3,4
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff if compare order deviates from source order 2, 3, 4

**Why this recipe hits this mode:** The risk note says "compare-order matches source 2,3,4" — if the compiler reorders the cmp chain (e.g. tests 4 before 2), the instruction bytes differ and verify.py catches it directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02052704.c func_02052704 [--cc all] — instruction diff visible (cmp order)
2. ninja sha1 — final gate

## Reshape hints

- Keep conditions in source order `x == 2 || x == 3 || x == 4` — do not reorder or merge into a range check
- This is a pure leaf (bx lr epilogue) — do not add any local variables or function calls that would force a frame
- Return values are `0xc` (match) and `0x8` (no-match); if the compiler emits them swapped, invert the condition to `x != 2 && x != 3 && x != 4`
- Confidence is high: if the first compile attempt misses, the only knob is cmp order; try reversing to `x == 4 || x == 3 || x == 2` if the orig .s shows checks in that order
- Verify the file does NOT use the legacy_sp3 suffix — the comment specifies `mwcc 2.0/sp1p5 default` with `bx lr`
---
