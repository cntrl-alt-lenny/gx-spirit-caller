# func_02042190 (main, HIGH class A, 72B)

**Recipe:** reload-per-use global (3 separate ldr); store arg0 at +0x1000; two calls
**Risk:** data_0219d9f0 must RELOAD each ref (do NOT bind) — written as 3 refs; 0x11dc is a reused pool word; arg0 store width

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
- data_0219d9f0

**Verify gate:**
1. python tools/verify.py src/main/func_02042190.c func_02042190 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/02042190.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
