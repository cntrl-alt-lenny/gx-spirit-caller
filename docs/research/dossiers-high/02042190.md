---
# func_02042190 (main, HIGH class A, 72B)

**Recipe:** reload-per-use global (3 separate ldr); store arg0 at +0x1000; two calls
**Risk:** data_0219d9f0 must RELOAD each ref (do NOT bind) — written as 3 refs; 0x11dc is a reused pool word; arg0 store width
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word mismatch; wrong global address wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** `data_0219d9f0` is a named BSS pointer whose address is emitted as a literal pool .word; verify.py wildcards LDR pool loads during reloc comparison, so a wrong address for this global passes byte-comparison silently until ninja report resolves pool symbols.

**Pool words to verify against orig .s:**
- `data_0219d9f0` — BSS singleton pointer; load base for +0x11dc (arg to both calls) and +0x1000 (void* store target)

**Verify gate sequence:**
1. verify.py src/main/func_02042190.c func_02042190 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatch (wrong data_0219d9f0 address) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Do NOT bind `data_0219d9f0` to a local `char *base`; write all three references as bare `data_0219d9f0 + offset` so the compiler emits three separate LDR instructions matching the original reload-per-use pattern
- Preserve call order exactly: func_020927b8(data_0219d9f0 + 0x11dc), then the store at +0x1000, then func_02092748(data_0219d9f0 + 0x11dc); reordering trips the reload schedule
- The store at +0x1000 must use `void **` cast (word-width store); a narrower cast produces strb/strh instead of str
- 0x11dc is a reused pool word (appears twice in the pool); both LDR args should resolve to the same offset — confirm there is only one .word for 0x11dc in the orig .s pool
- Confirm data_0219d9f0 resolves to its correct BSS address via ninja report; verify.py wildcards it silently
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219d9f0` | data symbol |
| `_LIT1` | `0x000011dc` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020927b8` | main TU call |
| `bl` | `func_02092748` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

