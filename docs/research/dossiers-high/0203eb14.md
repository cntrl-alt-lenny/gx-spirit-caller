---
# func_0203eb14 (main, HIGH class B, 52B)

**Recipe:** unsigned clamp (movhi), shift-build mask orr (value-first), strh
**Risk:** idx must be unsigned for movhi (else movgt); orr operand order needs existing value first; halfword field at base+0xd16; func_0203c900 retyped char*.
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (movhi vs movgt / orr operand order), verify.py catches directly

**Why this recipe hits this mode:** Two distinct instruction-level risks: (1) if `idx` is declared `signed int`, mwcc emits `movgt` instead of `movhi` for the clamp; (2) the `orr` must encode the existing field value as the first operand — operand order is part of the instruction encoding and verify.py compares it directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203eb14.c func_0203eb14 [--cc all] — movhi/movgt and orr operand order are visible directly
2. ninja sha1 — final gate

## Reshape hints

- Declare `idx` as `unsigned int` in the function signature so the clamp `if (idx > 0xd) idx = 0xd` emits `cmp r0,#0xd / movhi r0,#0xd` not `movgt`
- The `orr` must read the existing halfword first: `*(unsigned short *)(p + 0x16) | (1 << (idx - 1))`; the read-modify-write form already has the field as the left operand of `|`, which should produce the correct orr operand order
- `func_0203c900` must be typed `char *func_0203c900(int sel)` so that `+ 0xd00` is a byte-offset add; if typed `int *` or `void *` the pointer arithmetic scales incorrectly
- The halfword store is at `p + 0x16` (i.e., `func_0203c900(0x10) + 0xd00 + 0x16 = base + 0xd16`); verify the STRH immediate offset in the orig .s matches `0x16` relative to the `p` pointer
- If the shift `(1 << (idx - 1))` produces an extra `sub` instruction for `idx - 1`, try pre-decrementing: `unsigned int bit = idx - 1; ... | (1u << bit)` to let mwcc fold the subtract into the shift encoding
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203c900` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

