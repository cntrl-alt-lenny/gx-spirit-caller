# func_0203eb14 (main, HIGH class B, 52B)

**Recipe:** unsigned clamp (movhi), shift-build mask orr (value-first), strh
**Risk:** idx must be unsigned for movhi (else movgt); orr operand order needs existing value first; halfword field at base+0xd16; func_0203c900 retyped char*.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203eb14.c)

**Verify gate:**
1. python tools/verify.py src/main/func_0203eb14.c func_0203eb14 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/0203eb14.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
