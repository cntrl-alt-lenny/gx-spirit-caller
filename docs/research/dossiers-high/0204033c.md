---
# func_0204033c (main, HIGH class A, unknown B)

**Recipe:** bind global once (single ldr reused); +0x1000 guard; tail call
**Risk:** data_0219d9d4 must be a held base (one ldr, reused) — bound; field 0x384 type/sign; 0x1318 pool word should survive as literal
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named pool global `data_0219d9d4` used as struct base; wrong value wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** `data_0219d9d4` is loaded once into a local binding `b` and reused for both the field guard and the tail call argument; the LDR pool word for this address is wildcarded by verify.py's BL reloc pass, so a wrong address value passes instruction checks silently. Only ninja report resolves the pool relocation.

**Pool words to verify against orig .s:**
- `data_0219d9d4` — struct base held in a callee-saved register; used for the +0x1000+0x384 guard field load and the +0x1318 tail-call argument

**Verify gate sequence:**
1. verify.py src/main/func_0204033c.c func_0204033c [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Bind `data_0219d9d4` to a local `char *b` before the if-guard so mwcc emits a single LDR into a callee-saved register that is reused for both the guard and the tail call
- The offset `0x1000 + 0x384 = 0x1384` can be written as either a combined literal or split; match whichever form produces the same immediate encoding the orig .s uses (check for two-instruction add vs single ldr with offset)
- Field at +0x1384 is declared as `int`; if verify.py shows a sign-extension difference, try `u32` or `s32` to steer the cmp instruction form
- The tail-call target address `b + 0x1318` should be computed as a register add so that 0x1318 can encode as an ARM immediate; if mwcc emits an extra pool load for 0x1318, split the constant into `0x1000 + 0x318`
- Verify `data_0219d9d4` pool entry in the orig .s before declaring a match; the held-base pattern means it appears exactly once in the pool
---
