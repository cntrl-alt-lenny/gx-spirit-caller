---
# func_02017888 (main, HIGH class A, 328B)

**Recipe:** GetSystemWork bitfield-guard + cmp/beq chain + jump-table substate
**Risk:** 0x8d8&1 guard -> `and #1` wall; substate jump-table + repeated-call CSE + cond loads likely diverge
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and #1` wall and repeated-call / jump-table layout

**Why this recipe hits this mode:** The `(v<<31)>>31` bit0 guard is the canonical `and #1` wall — mwcc lowers it to a single `and` instruction while the baserom emits the two-instruction `lsl#31; lsr#31` pair, producing a visible byte diff that verify.py will catch immediately. Additionally, the three separate calls to `func_020190c0` in state 1 (intentionally not CSE'd) and the w[0x924] jump-table ordering must exactly mirror the .s layout.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02017888.c func_02017888 [--cc all] — instruction bytes (and #1 wall shows immediately)
2. ninja sha1 — final gate

## Reshape hints

- The `(v<<31)>>31` form for bit0 guard is correct; do NOT simplify to `& 1` — the swarm already chose the shift form to attempt to force `lsl;lsr` but mwcc still emits `and`. If verify shows `and #1` vs `lsl;lsr`, this candidate is .s-bound for the guard line
- Write the three `func_020190c0()` calls in state 1 as three distinct call expressions with no temp binding — any temp variable may trigger CSE and collapse them into one call
- For the jump-table: emit cases in switch-source order (0, 5, 3, 2, 1 with case 0 falling through to 5) — reordering cases changes the table layout
- For `w[0x8e0] bit0` inner guard in case 2: use `((*(unsigned int *)(w+0x8e0) << 0x1f) >> 0x1f)` matching the outer pattern exactly
- For state 3 case 4 fallthrough (0->5): the `*(int *)(w+0x924) = 5` assignment must precede the fallthrough — place it as the first statement in case 0
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00230100` | MMIO/const |
| `_LIT1` | `0x00230200` | MMIO/const |
| `_LIT2` | `0x00230300` | MMIO/const |
| `_LIT3` | `0x00230301` | MMIO/const |
| `_LIT4` | `0x00230302` | MMIO/const |
| `_LIT5` | `0x00230303` | MMIO/const |
| `_LIT6` | `0x00230304` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190c0` | main TU call |
| `bl` | `func_020139c4` | main TU call |
| `bl` | `func_02019034` | main TU call |
| `bl` | `func_0201b7e0` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |
| `bl` | `func_020190ec` | main TU call |
| `bl` | `func_0201aabc` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_ov000_021ae4ac` | overlay call (ov000) |
| `bl` | `func_02019640` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201aaf4` | main TU call |
| `bl` | `func_0201ab5c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

