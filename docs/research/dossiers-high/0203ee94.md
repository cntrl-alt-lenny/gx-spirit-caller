---
# func_0203ee94 (main, HIGH class A, 76B)

**Recipe:** Two-call guard chain returning constants; sub sp #4 stub frame
**Risk:** mvn r1,#0x26 => compare against -0x27; constants 0xb/0x9 mapping; conditional-return epilogues
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (mvn constant form / conditional-return epilogue layout), verify.py catches directly

**Why this recipe hits this mode:** The risk centres on two instruction-level details: `mvn r1,#0x26` must be the literal encoding for -0x27 (signed int comparison), and the sub-sp-#4 stub frame must produce the correct conditional-return epilogue sequence; both produce specific byte patterns that verify.py compares directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203ee94.c func_0203ee94 [--cc all] — instruction bytes visible directly
2. ninja sha1 — final gate

## Reshape hints

- Write the -0x27 comparison as `r == -0x27` (signed int, not unsigned); the compiler must emit `mvn r1,#0x26` for the constant; do not use `r == (int)0xFFFFFFD9` or any unsigned form
- The two conditions `r == 0 || r == -0x27` returning 0x9 can be written as a single `if` with `||`; if mwcc emits two separate `beq` blocks instead of combining, try splitting into two explicit `if` statements (test r==0 first, then r==-0x27)
- Constants must map: guard-failure -> `0xb`, success-path -> `0x9`, fallthrough-failure -> `0xb`; verify the mov immediates in the orig .s match exactly
- The sub-sp-#4 stub frame implies this function is compiled with a minimal frame setup/teardown; do not add local variables that would inflate the frame
- Conditional-return epilogues: ensure the `return 0xb` at the top guard and the `return 0xb` fallthrough are not merged into one block if the .s shows them as separate epilogues; use two explicit return sites
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206df90` | main TU call |
| `bl` | `func_0206e7b0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

