---
# func_0205272c (main, HIGH class B, 60B)

**Recipe:** stack-buffer init+memcmp, eq-guard returns u16, else 0
**Risk:** func_02094688 sig (memset vs init); buf size 8 vs frame 0xc and the +4/+6 offsets are reconstructed from the loads.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named array `data_020ffa98` is a pool word passed as pointer arg; wrong value wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** `data_020ffa98` is loaded via an LDR pool literal and passed as the second argument to `func_020a7440`; if the pool word value is wrong, verify.py wildcards the reloc and the mismatch is invisible until `ninja report`.

**Pool words to verify against orig .s:**
- `data_020ffa98` — comparison base pointer: passed as second arg to func_020a7440 (memcmp equivalent)

**Verify gate sequence:**
1. verify.py src/main/func_0205272c.c func_0205272c [--cc all] [--module main] — instruction bytes
2. ninja report — resolves data_020ffa98 pool word; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- The buf offsets `+6` (for memcmp) and `+4` (for the u16 return) are reconstructed from loads — cross-check `[sp, #6]` and `[sp, #4]` ldr/ldrh offsets in the orig .s before committing
- Frame size is 0xc despite buf being 8 bytes — the extra 4 bytes are alignment/spill; do not try to resize the stack buffer
- `func_02094688` is memset-like with sig `(void *, int, int)` and args `(buf, 0, 8)` — keep this exact signature
- If `func_020a7440` returns non-zero meaning no-match, the guard `== 0` is correct for the eq path; if objdiff shows the branch inverted, flip to `!= 0` returning 0 early
- Cross-check `data_020ffa98` pool literal address in the orig .s to confirm the symbol value
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ffa98` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02094688` | main TU call |
| `bl` | `func_020a7440` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

