# func_0205272c (main, HIGH class B, 60B)

**Recipe:** stack-buffer init+memcmp, eq-guard returns u16, else 0
**Risk:** func_02094688 sig (memset vs init); buf size 8 vs frame 0xc and the +4/+6 offsets are reconstructed from the loads.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0205272c.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0205272c.c func_0205272c --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0205272c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
