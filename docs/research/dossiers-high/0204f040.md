---
# func_0204f040 (main, HIGH class A, 104B)

**Recipe:** stack-arg pack (10 words) + single call, r0 passthrough
**Risk:** many stack slots — any reorder fails; data_ symbols passed by address
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — named data_ pool globals wildcarded by verify.py, only ninja report catches mismatches

**Secondary mode:** Mode B — BL to func_0209150c wildcarded by verify.py reloc

**Why this recipe hits this mode:** Five named `data_020ffXXX` arrays are passed by pointer as arguments; their addresses materialise in the literal pool and verify.py wildcards all pool-word relocations, so a wrong symbol address is invisible in the instruction-byte diff and only ninja report resolves each address against the orig .s pool.

**Pool words to verify against orig .s:**
- `data_020ff980` — passed as stack arg s1 (5th argument position, pointer to data block)
- `data_020ff9a0` — passed as stack arg s5 (9th argument position, pointer to data block)
- `data_020ff9ac` — passed as stack arg s7 (11th argument position, pointer to data block)
- `data_020ff9b8` — passed as register arg r3 (4th register argument)
- `data_020ff9ec` — passed as register arg r2 (3rd register argument)

**Verify gate sequence:**
1. verify.py src/main/func_0204f040.c func_0204f040 [--cc all] — instruction bytes
2. ninja report — resolves pool words for all five data_ symbols and the func_0209150c BL target; catches Mode A/B mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The full argument list order to `func_0209150c` is fixed and must match exactly: r0=a0, r1=0x100, r2=data_020ff9ec, r3=data_020ff9b8, then stack in ascending order: 3, data_020ff980, a1, a2, a2, data_020ff9a0, a3, data_020ff9ac, 3.
- Note that `a2` appears twice on the stack (positions 7 and 8 of the 13-argument list) — this is intentional, not a copy-paste error.
- Cross-check all five data_ symbol addresses against the .s literal pool words in the order the compiler materialises each pointer; any address mismatch must be corrected in config/eur, not in the C code.
- Cross-check the address of `func_0209150c` against the .s BL instruction before finalising.
- If verify.py shows an instruction diff in the argument-setup sequence, the most likely cause is a stack-slot reorder — match the prep's annotated order precisely.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ff980` | data symbol |
| `_LIT1` | `data_020ff9a0` | data symbol |
| `_LIT2` | `data_020ff9ac` | data symbol |
| `_LIT3` | `data_020ff9ec` | data symbol |
| `_LIT4` | `data_020ff9b8` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0209150c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

