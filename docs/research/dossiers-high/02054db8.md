---
# func_02054db8 (main, HIGH class A, 68B)

**Recipe:** unrolled byte-copy loop + buffer + tail call
**Risk:** mwcc may not 2x-unroll the loop identically (lr counter, post-inc ldrb/strb pairs); buffer size/scheduling may differ
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (loop unroll factor and ldrb/strb pair scheduling)

**Why this recipe hits this mode:** The 4-iteration 2x-unrolled loop produces a specific `ldrb`/`strb` interleave that depends on mwcc's scheduler; any deviation in unroll factor, post-increment addressing, or instruction ordering produces a byte diff that verify.py catches.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054db8.c func_02054db8 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The explicit `for (i = 0; i < 4; i++) { buf[i*2] = src[i*2]; buf[i*2+1] = src[i*2+1]; }` form is written to encourage a 2x-unrolled 4-iteration loop; if the compiler does not unroll it, try fully unrolling manually (8 individual assignments).
- `buf[0] = (unsigned char)b` must come after the loop completes, not interleaved; the comment confirms `strb b at sp AFTER the copy`.
- The `buf` array must be stack-resident (`unsigned char buf[8]`) so the compiler does not optimize the copy into registers and skip the stack writes.
- If objdiff shows `lr` used as a loop counter in the orig but a GPR in the C output, this function may require the legacy compiler (`*.legacy.c` suffix) — check the epilogue style in the orig `.s`.
- Verify that `func_0206ee40` takes `(int, unsigned char *)` so the `buf` address is passed as a pointer from `add rN, sp, #offset`.
---
