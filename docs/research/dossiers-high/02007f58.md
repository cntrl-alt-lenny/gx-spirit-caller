---
# func_02007f58 (main, HIGH class A, unknown B)

**Recipe:** straight-line init: calls + halfword/word field stores + masked-field-first orr
**Risk:** Many bases/offsets guessed; scheduling of interleaved literal loads; strh sharing r2=1 across two stores.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — multiple named data_XXXX globals (`data_02104f4c`, `data_0210593c`, `data_02104f3c`, `data_02105f3c`) whose pool-word values are wildcarded by `verify.py`; wrong offsets or values only caught by `ninja report`

**Secondary mode:** Mode E — visible scheduling diff from interleaved literal loads and the `orr` operand order for the masked-field write

**Why this recipe hits this mode:** Four distinct named globals appear as pool literals; their addresses are wildcarded by `verify.py`'s reloc pass so field-offset errors and wrong base addresses pass the instruction-bytes check but fail at `ninja report`. Additionally, the `strh` sharing of `r2=1` across two halfword stores and the `orr` operand order (`masked field FIRST`) may produce visible instruction diffs.

**Pool words to verify against orig .s:**
- `data_02104f4c` — base for `func_020945f4` first argument and destination for the +0x1aa8 (0x6aa*4) copy (also referenced by func_0200a454)
- `data_0210593c` — base for halfword stores at +0x62 and +0x68 (shared `r2=1`)
- `data_02104f3c` — base for word store at +0xa70 (value 0x1f4=500) and RMW at +0x14
- `data_02105f3c` — base for word store at +0x858 (value -1)

**Verify gate sequence:**
1. verify.py src/main/func_02007f58.c func_02007f58 [--cc all] — instruction bytes (all four pool globals wildcarded)
2. ninja report — resolves all four pool-word values and offsets; catches Mode A base/offset mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write the `orr` as `(*(int *)(data_02104f3c + 0x14) & ~7) | (r & 7)` with masked-field FIRST (left operand) so mwcc emits `orr r0,r2,r0` with the masked value in the destination register — swapping operands will produce `orr r0,r0,r2` and a visible byte diff
- The two `strh` stores at +0x62 and +0x68 should share the constant `1` in the same register (no intermediate store to a different value between them); verify the orig .s to confirm they share `r2` with no intervening reload
- Verify all four pool-base offsets (+0x62, +0x68, +0xa70, +0x14, +0x858) against the orig .s `str`/`strh` immediates — these are flagged as guessed; a wrong offset passes `verify.py` (wildcarded) but fails `ninja report`
- The call order in the teardown chain (`func_020138b8`, `func_0200a454`, `func_0200a26c(2)`, `func_0200a26c(1)`, `func_0200a26c(0)`, `func_02007f38`) must match the orig .s BL sequence exactly; any reordering produces a visible byte diff
- The `func_020945f4(data_02104f4c, 0, 0x1b0c)` call passes the base pointer as the first arg; if the field size 0x1b0c is wrong, `ninja report` will flag the mismatch when comparing against the orig module's data layout
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_02104f4c` | data symbol |
| `_LIT1` | `0x00001b0c` | MMIO/const |
| `_LIT2` | `data_0210593c` | data symbol |
| `_LIT3` | `data_02104f3c` | data symbol |
| `_LIT4` | `data_02105f3c` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020945f4` | main TU call |
| `bl` | `func_02018be8` | main TU call |
| `bl` | `func_02007e8c` | main TU call |
| `bl` | `func_02007278` | main TU call |
| `bl` | `func_020138b8` | main TU call |
| `bl` | `func_0200a454` | main TU call |
| `bl` | `func_0200a26c` | main TU call |
| `bl` | `func_02007f38` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

