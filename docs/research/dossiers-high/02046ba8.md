---
# func_02046ba8 (main, HIGH class A, 184B)

**Recipe:** guard chain + byte branch + field stores + calls; sp3 tier
**Risk:** struct holds *daec base across guard into branch (r2); C may reload. b_2d reload pattern + first-field double-deref must match
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible instruction diff (reload vs cached struct base, b_2d reload pattern)

**Why this recipe hits this mode:** Both `data_0219daec` and `data_020ff908` are named globals loaded from the literal pool; verify.py wildcards those loads, so wrong symbol addresses pass silently and only `ninja report` catches them. Additionally, the .s holds the `*data_0219daec` struct pointer in r2 across the guard chain into the branch — if C reloads the global pointer instead of reusing the cached struct base, extra LDR instructions appear and verify.py catches the diff.

**Pool words to verify against orig .s:**
- `data_0219daec` — struct pointer; loaded from pool for null guard and held across guards into branch; pool .word must match orig .s
- `data_020ff908` — array base passed as 2nd arg to func_02049038 in the b_361==0 branch; pool .word must match orig .s

**Verify gate sequence:**
1. verify.py src/main/func_02046ba8.c func_02046ba8 [--cc all] [--module main] — instruction bytes; reload vs cached base and b_2d pattern visible here
2. ninja report — resolves pool words for data_0219daec and data_020ff908; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The .s holds the struct base in r2 through all three guard checks: if C reloads via `data_0219daec->` each time, mwcc may not cache r2; try storing to a local `state_02046ba8_t *p = data_0219daec;` and using `p->` throughout to force the pointer into a callee-saved register.
- For the b_2d pattern: write `data_0219daec->b_2d = 1;` then the call then `data_0219daec->b_2d = 0;` as two separate global loads (the .s reloads `ldr r1,=data_0219daec` before the second strb) — using the local `p->b_2d` both times may or may not match depending on register pressure.
- The first-field deref `data_0219daec->first` must be read fresh before calling `func_02062530`: `func_02062530(data_0219daec->first)` not a cached pointer — matches the .s `ldr r0,=data_0219daec; ldr r0,[r0]; bl func_02062530`.
- Keep `f_24 != 5 && f_24 != 6` as two separate cmps (same pattern as func_02046b3c) — avoid range-form.
- Cross-check both pool .word entries in the .s against config/eur/arm9/symbols.txt before running ninja report.
---
