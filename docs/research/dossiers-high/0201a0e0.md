---
# func_0201a0e0 (main, HIGH class A, ?B)

**Recipe:** Accessor (ldrsb signed-byte field) + commutative add operand order
**Risk:** 7-byte struct stride/+2 offset guessed; >>1 must stay arithmetic (signed v) to emit asr.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — named pool global data_020b5b80 is a pool-word candidate; wrong base address wildcarded by verify.py

**Secondary mode:** Mode E — commutative add operand order (`add r0, r1, r0, asr#1` vs `add r0, r0, r1, asr#1`) is a visible diff

**Why this recipe hits this mode:** `data_020b5b80` is loaded as a pool literal base for the struct array; verify.py wildcards the pool .word so only `ninja report` catches a wrong address. The `add r0, r1, r0, asr#1` commutative operand form is also a visible instruction diff risk if mwcc picks the other operand order.

**Pool words to verify against orig .s:**
- `data_020b5b80` — base for `Entry7` array; the `ldrsb [base + 7*a + 2]` field access at stride 7, offset +2

**Verify gate sequence:**
1. `verify.py src/main/func_0201a0e0.c func_0201a0e0 [--cc all]` — instruction bytes
2. `ninja report` — resolves data_020b5b80 pool word; catches Mode A mismatch before sha1
3. `ninja sha1` — final gate

## Reshape hints

- Declare `v` as `int` (signed) so `>> 1` lowers to `asr#1` not `lsr#1`; if the result is declared `unsigned`, the shift is logical and the bytes differ.
- The add commutative order `tableByte + (v >> 1)` vs `(v >> 1) + tableByte` controls which register feeds the `asr#1` shift slot; if objdiff shows swapped operands, reverse the addition order.
- Use the `Entry7` struct with field `b2` (offset +2) rather than a raw pointer + 2 to let the compiler compute stride via `rsb r2,r4,r4,lsl#3`.
- Cross-check `data_020b5b80`'s exact address in the orig .s pool before claiming a sha1 match.
- Keep `GetSystemWork()` as a leading discarded call; removing it changes the frame and BL sequence.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020b5b80` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_02019604` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

