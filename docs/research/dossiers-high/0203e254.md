---
# func_0203e254 (main, HIGH class B, 156B)

**Recipe:** small switch (cmp;beq) to Fill32/calls, store-order strb, ldrb return
**Risk:** 3-case switch must stay cmp;beq not jump table; store order (0xd10 before 0xd0f in case1) must hold; final ldrb => base typed char*; Fill32 arg order.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (switch form / store ordering / ldrb return), verify.py catches directly

**Why this recipe hits this mode:** Three risks are instruction-level: (1) if mwcc promotes the 3-case switch to an `addls pc` jump table the binary layout changes completely; (2) the strb store order within case-1 must be `base[0xd10]` before `base[0xd0f]`; (3) the final `ldrb` must come from a `char *`-typed base, not a narrowing cast. All are visible in the instruction byte stream.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203e254.c func_0203e254 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Keep the switch sparse (3 cases: 0, 1, 2) so mwcc emits a `cmp;beq` chain rather than a jump table; if a table appears, add `default: break;` or restructure as explicit `if/else if`
- Case-1 store order must be `base[0xd10] = func_0203def0()` first, then `base[0xd0f] = func_0203deac(base)` — matches the strb sequence in the .s; do not swap
- Declare `base` as `char *` (not `unsigned char *` or `void *`) so the final `return (unsigned char)base[0xd10]` emits a single `ldrb` with the correct sign-extension cast
- `Fill32` argument order is `(value, dst, n)` — ensure `Fill32(0, base + 0x300, 0x144)` is not reordered; both case-0 and case-2 use the same Fill32 call so verify both emit identical instruction sequences
- If case-2 differs from case-0 in the Fill32 + store sequence, compare the orig .s carefully to determine which order the two strb instructions appear in case-2
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203c900` | main TU call |
| `bl` | `Fill32` |  |
| `bl` | `func_0203e198` | main TU call |
| `bl` | `func_0203def0` | main TU call |
| `bl` | `func_0203deac` | main TU call |
| `bl` | `func_0203e0a0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

