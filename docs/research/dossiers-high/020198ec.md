---
# func_020198ec (main, HIGH class A, ?B)

**Recipe:** GetSystemWork (discarded) + signed-byte table[idx*7+5], clamp-if-assign, asr#1, double call
**Risk:** func_02019604 called TWICE (no CSE) — must stay two call sites; `rsb idx*7` index, `asr#1` halving, and movgt clamp-as-if may reorder
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — named pool global data_020b5b80 is a pool-word candidate; wrong base address wildcarded by verify.py

**Secondary mode:** Mode E — visible diff risk from movgt clamp codegen and asr#1 halving operand order

**Why this recipe hits this mode:** `data_020b5b80` is loaded as a pool literal to compute the indexed access; verify.py wildcards the pool .word so a wrong address won't show as an instruction diff — only `ninja report` resolves it. Additionally, the `movgt` clamp-as-if shape and the double-call inhibition of CSE are both visible diff risks.

**Pool words to verify against orig .s:**
- `data_020b5b80` — load base for the `ldrsb [base + idx*7 + 5]` signed-byte table access

**Verify gate sequence:**
1. `verify.py src/main/func_020198ec.c func_020198ec [--cc all]` — instruction bytes
2. `ninja report` — resolves data_020b5b80 pool word; catches Mode A mismatch before sha1
3. `ninja sha1` — final gate

## Reshape hints

- Write `func_02019604(idx)` as two explicit separate call sites with no intermediate variable shared between them; a CSE-breaking temp binding can cause mwcc to eliminate the second call.
- Index as `data_020b5b80[idx * 7 + 5]` with a plain array access — do NOT use a pointer walk — so the `rsb r1,idx,idx,lsl#3` stride form is emitted.
- Write the clamp as `if (t + (func_02019604(idx) >> 1) > 0x1e) t = 0x1e; else t = t + (func_02019604(idx) >> 1);` to force `movgt`-as-if-assign rather than a min() intrinsic.
- Declare `t` as plain `int` so `asr#1` (arithmetic shift) is emitted; `unsigned int` would emit `lsr#1` instead.
- Cross-check the exact address of data_020b5b80 against the pool word in the orig .s before claiming a sha1 match.
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020b5b80` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_02019604` | main TU call |
| `bl` | `func_0201a170` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

