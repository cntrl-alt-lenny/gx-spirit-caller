---
# func_0200f01c (main, HIGH class B, unknown B)

**Recipe:** Null-guard (moveq/bxeq) + bic/orr/bic flag RMW in source order
**Risk:** field offset 0x60 guessed; the three bic/orr stay split only if written as three statements, else mwcc may fuse masks.
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches fused vs split mask operations and offset mismatch

**Why this recipe hits this mode:** mwcc will fold `(v & ~4) | 8` and `v &= ~0x1e00000` into fewer instructions if written in a compound expression; only keeping them as three separate assignment statements prevents mask fusion and preserves the bic/orr/bic sequence.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0200f01c.c func_0200f01c — bic/orr/bic split vs fused is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Write the three mask operations as three separate C statements (`v = (v & ~4u) | 8u;` then `v &= ~0x1e00000u;`) — do NOT combine them into one expression or mwcc will fuse the bic/orr pair
- Use `unsigned int` for the flags field to ensure bic/orr emit unsigned-mode instructions, not signed
- The null-guard `if (p == 0) return 0;` must use `== 0` not `!p` to match the `moveq`/`bxeq` pattern; verify the emit matches `cmp r0, #0` / `moveq r0, #0` / `bxeq lr`
- Field offset `+0x60` is guessed; if objdiff shows the ldr/str immediate is wrong, read the .s to confirm the actual `[rx, #0x60]` offset
- The mask constants `~4u`, `8u`, `~0x1e00000u` must be written as unsigned literals so the compiler does not sign-extend them in the bic encoding
---
