---
# func_02021158 (main, HIGH class B, 56B)

**Recipe:** DISPATCH switch, shared case body (cmpne fusion)
**Risk:** case-0 empty arm may be dropped (losing the leading cmp #0); 1||2 might not fuse to cmpne
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — pool-word order; two pool entries for `data_02194340` wildcarded by verify.py, only ninja report catches a count collapse

**Why this recipe hits this mode:** The function uses `data_02194340` twice — once dereferenced as `*(int*)` for the switch key, and once as a pointer arg to `func_02021934`. Both generate pool-word loads. Verify.py wildcards both relocations; if mwcc CSE's them to one register (one pool word instead of two) the pool count changes silently and only `ninja report` exposes it.

**Pool words to verify against orig .s:**
- `data_02194340` — dereferenced as `*(int*)` for the switch guard (pool word 1) and passed as char* arg to `func_02021934` (pool word 2; same symbol, two entries expected)

**Verify gate sequence:**
1. verify.py src/main/func_02021158.c func_02021158 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A two-vs-one pool collapse before sha1
3. ninja sha1 — final gate

## Reshape hints

- If case-0 is silently dropped, add a dummy `(void)0;` inside the `case 0:` arm to force the empty branch to emit a `cmp #0`
- For cmpne fusion: write cases 1 and 2 as fall-through (no break between them) so mwcc sees the shared body as one unit and emits `cmp #1; cmpne #2; bne`
- If `1||2` does not fuse, try restructuring as `if (x == 1 || x == 2)` to coax the same cmp pair without a switch
- The two pool words for `data_02194340` must appear in order: guard deref first, pointer-arg second — ensure the deref is textually before the call site
- If mwcc collapses to one pool word, bind a local pointer: `char *p = data_02194340; switch (*(int*)p) { ... func_02021934(p); }` to keep the base live across the call
---
