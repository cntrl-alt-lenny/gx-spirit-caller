# func_0202111c (main, HIGH class B, 60B)

**Recipe:** DISPATCH switch returning const + dual global reference
**Risk:** switch may emit a jump-table instead of the cmp #0/#1/#2 chain; case-2 empty arm may be pruned

## SHA1 pitfall

**Primary mode:** Mode A - Wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0202111c.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0202111c.c func_0202111c --cc all (instruction bytes)
2. ninja report (resolves pool/BL words; catches Mode A mismatch)
3. ninja sha1 (final gate)

## Reshape hints

- Read the orig .s pool section: list each .word in order; C must reference the same globals in that exact order
- Named data_XXXX globals are pool-word candidates; wrong address silently passes verify.py
- Use ninja report (not just ninja objdiff) before sha1

## Reference

- Prep: docs/research/c-match-prep/0202111c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_02021150` | `data_02194340` | data symbol |
| `.L_02021154` | `data_02194340` | data symbol |

> **Mode D warning:** duplicate pool value(s) ['data_02194340'] — `clean C` will deduplicate → 4-byte size diff → sha1 fail. This is a `.s`-only candidate.

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020216c8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

