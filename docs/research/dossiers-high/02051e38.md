---
# func_02051e38 (main, HIGH class A, 92B)

**Recipe:** guard chain returning bool, RELOAD global; sp3 routing
**Risk:** field 0x1a9 u8; func_020469b4()==5 guard; sp3 (push{lr}+sub sp,#4+pop pc); return-block order
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; sp3 prologue/epilogue shape and return-block order caught by verify.py

**Secondary mode:** Mode B — BL targets for func_02045280 / func_020498f0 / func_020469b4 wildcarded by verify.py reloc

**Why this recipe hits this mode:** This function uses the mwcc 1.2/sp3 compiler (legacy_sp3 routing: `push{lr}+sub sp,#4+pop{pc}`); the wrong compiler produces an immediately visible prologue mismatch. Additionally, the ordering of the three guard conditions and the placement of each return block control which branch instructions are emitted — any reorder is visible in verify.py. All BL relocs are wildcarded and need ninja report confirmation.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02051e38.legacy_sp3.c func_02051e38 [--cc all] — instruction diff (prologue shape and return-block order) is visible directly
2. ninja report — resolves BL targets for func_02045280, func_020498f0, func_020469b4; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- File must be named `func_02051e38.legacy_sp3.c` (not `.c`) to select the mwcc 1.2/sp3 compiler; the default sp1p5 produces a different frame shape immediately visible in verify.py.
- The guard order must be exactly: (1) `if (func_02045280() != 0) return 0;` first; (2) `if (func_020498f0() != 0 && func_020469b4() == 5 && func_020498f0()->f1a9 == 0) return 1;` second; (3) `return 0` fall-through — any reordering changes the branch structure visibly.
- The byte field f1a9 at +0x1a9 must be typed `unsigned char`; a signed or word-width comparison may emit a different condition test and change the cmp/tst instruction.
- The `func_020469b4() == 5` comparison must appear as the second sub-condition inside the compound guard, between the null-pointer check and the field check; the asm evaluates them in this order.
- Cross-check addresses of all three extern functions against their respective .s BL instructions to confirm no callee swap before finalising.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02045280` | main TU call |
| `bl` | `func_020498f0` | main TU call |
| `bl` | `func_020469b4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

