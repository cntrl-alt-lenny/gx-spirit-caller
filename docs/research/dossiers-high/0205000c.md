---
# func_0205000c (main, HIGH class A, 72B)

**Recipe:** cmp early-return (byte guard) + 2 byte clears + tail call, RELOAD
**Risk:** fields 0x15/0x14/0x16 u8, 0x10 word(ptr); func_020673dc takes that word; sp3 routing
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; sp3 prologue/epilogue shape and compiler routing caught by verify.py

**Secondary mode:** Mode B — BL to func_020673dc wildcarded by verify.py reloc

**Why this recipe hits this mode:** This function uses the mwcc 1.2/sp3 compiler (legacy_sp3 routing: `push{lr}+sub sp,#4+pop{pc}` prologue/epilogue); using the default 2.0/sp1p5 compiler produces a completely different frame shape that verify.py catches on the very first instruction. The BL reloc for func_020673dc is wildcarded and needs ninja report confirmation.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0205000c.legacy_sp3.c func_0205000c [--cc all] — instruction diff (prologue shape) is visible directly
2. ninja report — resolves BL target for func_020673dc; catches Mode B callee mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- File must be named `func_0205000c.legacy_sp3.c` (not `.c`) to select the mwcc 1.2/sp3 compiler; the default sp1p5 produces a `push{r4,lr}`/`pop{r4,pc}` frame that differs immediately from the sp3 `push{lr}+sub sp,#4+pop{pc}` shape.
- The guard must compare byte field f15 at +0x15 (`unsigned char`) against the literal 2 using the accessor — using a word-load or signed field type changes the load+cmp instruction pair.
- Both byte clears (`f14 = 0` at +0x14, `f16 = 0` at +0x16) must appear after the early return and in that order; each must reload the accessor via BL rather than reusing a cached pointer.
- The word at +0x10 is loaded by `func_020498f0()->f10` and passed directly to `func_020673dc`; avoid storing it in a local variable — the asm reloads the accessor immediately before the BL.
- Cross-check the address of `func_020673dc` against the .s BL instruction to confirm the callee is correct before finalising.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020498f0` | main TU call |
| `bl` | `func_020673dc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

