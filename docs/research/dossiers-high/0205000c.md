# func_0205000c (main, HIGH class A, 72B)

**Recipe:** cmp early-return (byte guard) + 2 byte clears + tail call, RELOAD
**Risk:** fields 0x15/0x14/0x16 u8, 0x10 word(ptr); func_020673dc takes that word; sp3 routing

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0205000c.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0205000c.c func_0205000c --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0205000c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
