---
# func_02017a98 (main, HIGH class A, 88B)

**Recipe:** GetSystemWork bitfield-guard + linear guard-chain, conditional tail store
**Risk:** 0x8d8&1 guard -> `and #1` wall; `ldrcs r0,_LIT; strcs` conditional load may emit full materialize; r4/r5 order
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and #1` wall and conditional-load materialization

**Why this recipe hits this mode:** The `(v<<31)>>31` guard is the `and #1` wall — mwcc emits `and r0,#1` while the baserom has `lsl#31; lsr#31`, a two-instruction sequence verify.py will flag as a byte diff. The conditional tail store (`strcs`) may also diverge if mwcc materializes the constant through a full load+store sequence rather than the predicted `ldrcs/strcs` pair.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02017a98.c func_02017a98 [--cc all] — instruction bytes (and #1 wall shows immediately)
2. ninja sha1 — final gate

## Reshape hints

- Keep local variable declarations in order `o` then `w` (matching `mov r4,r0 / mov r5,r0` in the .s) — swap order may change which callee-saved register gets each pointer
- The `(unsigned int)func_02019034() >= (unsigned int)(*(int *)(w + 0x8dc) + 1)` condition must stay as an unsigned compare; do not cast to signed or the `bcs`/`strcs` pattern breaks
- If verify shows the conditional store materializes into an unconditional ldr+str pair, try hoisting the condition: `if (cond) *(int*)((char*)o+8) = 0x00240100;` (already written this way — confirm no extra temp)
- For the guard: `((*(unsigned int *)(w + 0x8d8) << 0x1f) >> 0x1f)` is the correct shift idiom; any simplification to `& 1` will force the `and` instruction and guarantee mismatch on that line
- This function is short enough that a single verify pass should pinpoint all divergent instructions before any reshape attempt
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00240100` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190ec` | main TU call |
| `bl` | `func_020139c4` | main TU call |
| `bl` | `func_02019034` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

