# func_02038dac (main, HIGH class A, 48B)

**Recipe:** backward table scan, stride 0x5c, 32 entries, match f4==key
**Risk:** struct size 0x5c / f4 offset guessed; bpl loop (i>=0) should match the subs;bpl

## SHA1 pitfall

**Primary mode:** Mode A - Wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02038dac.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02038dac.c func_02038dac --cc all (instruction bytes)
2. ninja report (resolves pool/BL words; catches Mode A mismatch)
3. ninja sha1 (final gate)

## Reshape hints

- Read the orig .s pool section: list each .word in order; C must reference the same globals in that exact order
- Named data_XXXX globals are pool-word candidates; wrong address silently passes verify.py
- Use ninja report (not just ninja objdiff) before sha1

## Reference

- Prep: docs/research/c-match-prep/02038dac.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
