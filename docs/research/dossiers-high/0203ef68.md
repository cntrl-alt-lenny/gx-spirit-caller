---
# func_0203ef68 (main, HIGH class A, 56B)

**Recipe:** Two-call guard returning constants; void setup call on success path
**Risk:** func_020433d0==0 -> 0xe; else func_0203c85c(3) then return 0x11; sub sp #4 stub frame
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee address for func_020433d0 wildcarded by verify.py BL reloc; only ninja report catches it

**Secondary mode:** Mode E — visible diff if stub frame or constant encoding (0xe / 0x11) mismatches; verify.py catches directly

**Why this recipe hits this mode:** The risk note flags `func_020433d0` as a guessed callee address; verify.py wildcards all BL relocations, so if the address is wrong the instruction bytes appear to match but the linked binary diverges. The sub-sp-#4 stub frame and the specific return constants 0xe/0x11 are also instruction-visible risks.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203ef68.c func_0203ef68 [--cc all] — instruction bytes (BL offsets wildcarded)
2. ninja report — resolves BL target for func_020433d0; catches Mode B mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Cross-check the address `0x020433d0` in the orig .s BL instruction; a one-nibble typo in the guessed address passes verify.py but fails at ninja report
- `func_0203c900(8)` is a setup call declared `void` return here; verify its signature in symbols.txt (if it returns a value in the orig, declaring it void is fine as long as the BL appears at the right position)
- The sub-sp-#4 stub frame implies no local variables; do not add any locals or the frame grows and the epilogue changes
- Constants must be `0xe` (guard-fail) and `0x11` (success path after `func_0203c85c(3)`); verify the mov immediates in the orig .s
- `func_0203c85c(3)` must be called with argument 3 before the `return 0x11`; if mwcc tail-calls it instead, ensure the return value is used or discard it explicitly
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0203c900` | main TU call |
| `bl` | `func_020433d0` | main TU call |
| `bl` | `func_0203c85c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

