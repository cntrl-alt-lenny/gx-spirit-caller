---
# func_02054d68 (main, HIGH class A, 52B)

**Recipe:** wrapper, mov-to-callee-save args, byte copy, tail call
**Risk:** r4/r5 assignment to p3/p2 and the sub sp,#4 scratch frame may not reproduce; clone-of-02054c30 shape
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (callee-save register allocation for p2/p3 and scratch frame size)

**Why this recipe hits this mode:** The comment says mwcc is expected to move `p2` and `p3` into callee-save registers `r4`/`r5`, and the prologue should emit `sub sp, #4` for a scratch frame; if the register allocator makes different choices the prologue/epilogue byte pattern changes visibly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054d68.c func_02054d68 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- This is noted as a clone of `02054c30`; if `02054c30` matches, try the identical structural form here with just the callee `func_0206ea00` substituted for `func_0206eb4c`.
- The `sub sp, #4` scratch frame suggests exactly one 4-byte stack slot is needed; do not add any local variables beyond `r` to avoid enlarging the frame.
- If `r4`/`r5` callee-saves do not appear in the output, try adding a use of `p2` or `p3` after the call to pin them into callee-save slots, or swap the order of `p2` and `p3` in the declaration.
- The byte copy pair `*p2 = (unsigned char)*p3` and `*p3 = (unsigned char)*p2` requires `ldrb`/`strb`; keep both casts explicit.
- Confirm `func_0206ea00` takes `(int, unsigned char *)` — if the second parameter type differs, the calling convention may shift register assignments for `p2`.
---
