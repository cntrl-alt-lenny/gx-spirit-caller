---
# func_02017afc (main, HIGH class A, 164B)

**Recipe:** GetSystemWork bitfield-guard + jump-table dispatch, RMW bit ops, cond stores
**Risk:** 0x8d8&1 guard -> `and #1` wall; jump-table layout + `bic;orr` idempotent set + dual cond loads likely diverge
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and #1` wall, `bic;orr` idiom, and jump-table case ordering

**Why this recipe hits this mode:** The `(v<<31)>>31` guard produces the canonical `and #1` mismatch visible immediately in verify.py output. The idempotent `(x & ~1) | 1` write (`bic r0,#1; orr r0,#1` pair in .s) must be preserved exactly as written — any optimizer fold to a plain `orr` loses the `bic` instruction. The w[0x924] jump-table case ordering (0, 3, 2, 1 with case 0 falling through to 3) must mirror the .s table exactly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02017afc.c func_02017afc [--cc all] — instruction bytes (and #1 wall + bic;orr pair visible immediately)
2. ninja sha1 — final gate

## Reshape hints

- Write `*(int *)(w + 0x8e0) = (*(int *)(w + 0x8e0) & ~1) | 1;` exactly as shown — do not split into separate read-modify-write variables or mwcc may fold the bic+orr into a single orr
- For case 0 fallthrough to case 3: write `case 0: *(int *)(w+0x924) = 4; /* fallthrough */ case 3:` — the assignment and fallthrough must appear together with no intervening return
- Keep the w[0x8e0] bit0 inner guard in case 2 as `((*(unsigned int *)(w+0x8e0) << 0x1f) >> 0x1f)` — do not rewrite as `& 1`
- The two cond stores in case 2 (0x00250101 vs 0x00250102) must remain as a simple if/else on `func_020195ec() == 0` — do not bind the return value to a temp, keep the direct compare in the branch
- Verify the jump-table arm count and range in the .s: the switch covers cases 0..3; any gap or extra case changes the table size
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00250100` | MMIO/const |
| `_LIT1` | `0x00250101` | MMIO/const |
| `_LIT2` | `0x00250102` | MMIO/const |
| `_LIT3` | `0x00250103` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_0201b7e0` | main TU call |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |
| `bl` | `func_0201abb0` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

