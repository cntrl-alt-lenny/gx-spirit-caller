---
# func_02054ca8 (main, HIGH class A, 80B)

**Recipe:** unrolled byte copy + buf[0] override + forward + tail call
**Risk:** unroller may not emit 4x2; DSE could drop buf[0] copy; frame/buf placement (sp+4) may differ
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (loop unroll shape, frame layout, buf placement)

**Why this recipe hits this mode:** mwcc's loop unroller must emit exactly 4 pairs of `ldrb`/`strb` for the 8-byte copy; any deviation in unroll factor or loop residue produces a visible instruction count difference that verify.py catches immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054ca8.c func_02054ca8 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Force the 4x2 unroll by writing the loop as `for (i = 0; i < 4; i++) { buf[i*2] = src[i*2]; buf[i*2+1] = src[i*2+1]; }` — this matches the prep body and is the most likely form to unroll identically.
- The frame comment says `buf at sp+4, outgoing ptr at sp+0`; a `0x10` frame with this layout requires at least one other stack slot below `buf`. If the frame comes out wrong, try adding a dummy `int` local before `buf` or reordering declarations.
- Dead-store elimination may remove the `buf[0] = (unsigned char)extra` override if the compiler proves it is overwritten by `func_0206eccc`; keeping `buf` as `unsigned char [8]` (stack-resident) prevents DSE.
- If the tail call to `func_02054ea8` emits `bl`+`pop` instead of `b`, split: `int tmp = func_0206eccc(a0, a1, a2, a3, buf); return func_02054ea8(tmp, -1);`.
- Cross-check that `func_0206eccc` takes `void *buf` as its fifth arg; the compiler must emit `add rN, sp, #4` (address-of buf) before the call.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206eccc` | main TU call |
| `bl` | `func_02054ea8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

