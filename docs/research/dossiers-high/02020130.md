---
# func_02020130 (main, HIGH class A, 27B)

**Recipe:** guard, call, moveq/movne boolean tail
**Risk:** final moveq r0,#0/movne r0,#1 expects !=0 boolean from func_02020188; offset 0x30 guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (moveq/movne boolean tail)
**Secondary mode:** Mode A — named globals data_02191f40 (field +0x30 guard) and data_021922e0 (guard arg) used as pool words; wildcarded by verify.py, only ninja report catches mismatches

**Why this recipe hits this mode:** The `return func_02020188() != 0` expression must emit as a moveq/movne pair to convert the function's return to a 0/1 boolean; MWCC may instead emit a cmp+movne or a straight pass-through, producing a visible byte difference that verify.py will catch directly.

**Pool words to verify against orig .s:**
- `data_02191f40` — struct base; field +0x30 loaded for the `f30 != 6` entry guard
- `data_021922e0` — pointer passed as arg to func_020a05f8 for the second guard

**Verify gate sequence:**
1. verify.py src/main/func_02020130.c func_02020130 [--cc all] — instruction bytes (moveq/movne visible directly)
2. ninja report — resolves pool words data_02191f40 and data_021922e0; catches Mode A mismatches
3. ninja sha1 — final gate

## Reshape hints

- Express the boolean tail as `return func_02020188() != 0` — this is the canonical form MWCC lowers to moveq r0,#0/movne r0,#1; do not bind the result to a local or cast it before returning
- If the compiler emits a plain pass-through (no moveq/movne), try `return (func_02020188() != 0) ? 1 : 0` or `int r = func_02020188(); return r != 0;` to force the materialisation
- Field offset 0x30 is guessed; if verify.py shows a ldr from a different offset, adjust the struct pad between the start and f30
- The two early returns (`return 0` on f30!=6 and on func_020a05f8 success) must not be folded by the compiler into a single branch; keep them as separate if-checks in source order
- Confirm pool word ordering: data_02191f40 should appear first (first use), then data_021922e0; if ninja report shows a mismatch, reorder the guards or introduce an explicit temp pointer for one of them
---
