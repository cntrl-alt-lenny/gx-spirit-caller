---
# func_02029204 (main, HIGH class A, 136B)

**Recipe:** single-load bitfield && guard chain; reload-after-call for b4; if(!b2)/else-if
**Risk:** BB ordering of func_02029a88 (out-of-line a58) and reload of flags at a5c sensitive; offsets guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; basic-block ordering of `!b2` branch and b4/field188 guard are directly visible to verify.py
**Secondary mode:** Mode B — BL targets `func_02029a88`, `func_020294c0`, `func_02029f74` are extern guessed addresses; wrong callees wildcarded by BL reloc, only ninja report catches

**Why this recipe hits this mode:** The `if(!p->b2) / else if(!(b5&&b6))` branch ordering and the reload of flags after `func_02029a88()` are instruction-level differences visible to verify.py. All three callee addresses are guessed extern symbols whose BL relocations are wildcarded by verify.py — only `ninja report` validates the actual call targets.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02029204.c func_02029204 [--cc all] — instruction bytes (BB order and bitfield extract visible)
2. ninja report — resolves BL targets; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The flags word at `0x19a` must be loaded once (bound) before the `b0 && b1` outer guard; b4 likely requires a fresh re-read from memory after `func_02029a88()` — do not cache `b4` in a local before the call
- If objdiff shows the `!b2` and `b5&&b6` branches in wrong order, try: `if (p->b2) { if (!(p->b5 && p->b6)) func_020294c0(p); } else { func_02029a88(); }` — this swaps which path is fall-through
- The `b4 && field188` guard must be a separate `if` after the b2/b5/b6 dispatch block, not chained as `else if`
- Struct offsets (0x188 for `field188`, 0x19a for flags bitfield) are guessed — cross-check against orig .s ldrsh/ldr immediate values if objdiff shows layout errors
- Cross-check `func_02029a88` (described as "out-of-line a58"), `func_020294c0`, and `func_02029f74` against orig .s BLX instructions via `ninja report`
---
