# func_0202a240 (main, HIGH class A, 60B)

**Recipe:** guard+Task_InvokeLocked+clear, dual-pool alias, tail call
**Risk:** if mwcc dedupes the two pool words the second .word collapses; volatile may add an extra reload

## SHA1 pitfall

**Primary mode:** Mode D - .s-ONLY candidate (pool dedup / patcher trim bypass)

WARNING: .s-only candidate. Do NOT write a .c.

**Why this recipe hits this mode:** The recipe aliases `data_0219a934` and `data_0219a934_alias` to the same raw address to force mwcc to emit two separate `.word` pool entries; if mwcc deduplicates them the second entry collapses, changing pool layout and producing a byte mismatch that clean C cannot recover from.

**Pool words to verify against orig .s:**
- `data_0219a934` — test-read base; first `.word` pool entry for the null-check LDR
- `data_0219a934_alias` — store base; second `.word` pool entry (must be distinct, same raw addr) for the Task_InvokeLocked call and clear store
- `func_0202a998` — tail-call BL target; Mode B risk (wildcarded by verify.py, only ninja report catches wrong value)

**Verify gate:**
N/A - .s-only; the existing .s ship is correct. Do not write a .c.

## Reshape hints

- Do not attempt a .c replacement — the two-pool-word trick cannot be reproduced reliably in clean C without compiler-specific hacks
- If a .c attempt is forced, use two separate `extern` declarations for the same address and `volatile` pointer casts, then verify the raw .s output shows two distinct `.word` entries
- Expect a 4-byte size diff if mwcc deduplicates the pool words
- Cross-check `func_0202a998` BL offset in orig .s against its expected address via ninja report before any sha1 attempt
- For reference: the dual-pool-word technique is documented in docs/research/c-match-prep/0202a240.c header comment

## Reference

- Prep: docs/research/c-match-prep/0202a240.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_0202a274` | `data_0219a934` | data symbol |
| `.L_0202a278` | `data_0219a934` | data symbol |

> **Mode D warning:** duplicate pool value(s) ['data_0219a934'] — `clean C` will deduplicate → 4-byte size diff → sha1 fail. This is a `.s`-only candidate.

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Task_InvokeLocked` |  |
| `bl` | `func_0202a998` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

