---
# func_02019820 (main, HIGH class A, ?B)

**Recipe:** GetSystemWork (discarded) + guard `>=5` then `>0 ? 1 : 0` flag return
**Risk:** GetSystemWork return is computed but unused (must stay sequenced); `movgt r0,#1` + fallthrough `mov r0,#0` flag-clamp may emit differently
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; movgt/fallthrough flag-clamp may not match

**Why this recipe hits this mode:** The `if (x>0) return 1; return 0;` shape must lower to `cmp r0,#0; movgt r0,#1; ldmgt ...` then fall-through `mov r0,#0`; if mwcc folds the ternary into a `movgt`/`movle` pair or reorders the comparison, the instruction bytes differ visibly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_02019820.c func_02019820 [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- Keep `GetSystemWork()` as a standalone discarded call at the top so the compiler emits the BL even though the result is unused.
- Write the `>0` guard as `if (func_020197ac(a) > 0) return 1;` followed by a plain `return 0;` — do NOT collapse into a ternary, to force the `movgt`/fallthrough shape rather than `movgt r0,#1; movle r0,#0`.
- Ensure the `< 5` guard is written as `if (func_02019604(a) < 5) return 0;` so the early-exit branch appears before the `>0` test.
- If the flag-clamp emits `movgt`/`movle` pair instead of fallthrough, try an explicit `else return 0;` or restructure so both paths hit the same tail return.
- Single callee-saved register r4 frame: keep the function body simple enough that only one callee-saved reg is needed; adding any local requiring r5 changes the frame.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_02019604` | main TU call |
| `bl` | `func_020197ac` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

