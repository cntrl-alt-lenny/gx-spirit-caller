---
# func_0201e5b8 (main, HIGH class A, 44B)

**Recipe:** GUARD/scan loop; FourCC literals reloaded into held regs; if-else-if order
**Risk:** loop reg-alloc: orig pins 3 FourCC consts + base in callee regs; literal-pool order/holding may flip
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (reg-alloc / literal-pool holding mismatch)

**Why this recipe hits this mode:** The loop holds three FourCC constants in callee-saved registers simultaneously; MWCC's register allocator may pin them in a different order or spill one to the literal pool, producing visible instruction differences that verify.py will catch directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0201e5b8.c func_0201e5b8 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Write the three FourCC compares in the exact order they appear in the .s: OBJD (0x444a424f), BGDT (0x54444742), PALT (0x544c4150) — changing the if-else-if order shifts which register holds which constant
- If the literal pool reorders, try caching only the tag comparison result in a local rather than hoisting the constants into named locals; this lets the compiler reload from pool each iteration and may avoid the multi-reg pin
- Keep `e += *(int *)(e + 4)` at the loop tail — moving the advance to the top changes when the +4 load is issued relative to the tag load
- Check whether the orig uses a pre-decrement counter or a less-than count; the `for (i = 0; i < count; i++)` pattern should be correct but a `do { } while (--count)` may emit tighter reg usage
- If callee-saved spilling is the issue, try breaking the loop into two passes (outer dispatch, inner advance) to reduce simultaneous live regs
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x544c4150` | MMIO/const |
| `_LIT1` | `0x54444742` | MMIO/const |
| `_LIT2` | `0x444a424f` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0201d778` | main TU call |
| `bl` | `func_0201de34` | main TU call |
| `bl` | `func_0201e4cc` | main TU call |
| `bl` | `func_0209286c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

