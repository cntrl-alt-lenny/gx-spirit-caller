# func_020552dc (main, HIGH class A, 64B)

**Recipe:** null-guard + alloc(size+1) + cond init call + return ptr
**Risk:** movs r5/r4 callee-save assignment and the movs-based null tests may flip reg-alloc; otherwise clean
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_020552dc.c func_020552dc --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode B mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020aaddc` | main TU call |
| `bl` | `func_020453e8` | main TU call |
| `bl` | `func_020aadf8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

