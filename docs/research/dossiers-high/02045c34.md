---
# func_02045c34 (main, HIGH class A, 72B)

**Recipe:** arg-caching across calls + sequential call/shuffle
**Risk:** exact callee-saved register numbers (r4..r7) assigned to a0..a3 may permute vs the asm
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (register-allocation permutation)

**Why this recipe hits this mode:** The function caches four incoming args across three calls using r4..r7; mwcc's register allocator may assign those callee-saved registers in a different order than the original, producing a detectable byte diff that verify.py will surface immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02045c34.c func_02045c34 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Reorder local variable declarations so a2 (reused across call 1 and call 3) is bound to r4, ensuring it stays live in the lowest callee-saved register.
- If r4/r5 are swapped: introduce a named temp `int saved_a2 = a2;` before the first call so mwcc pins it first.
- Check that `func_020ab088(a2, 0)` result is bound to a temp `x` immediately — binding prevents mwcc from using a caller-saved register and forcing a reload.
- If arg shuffle still diverges, try reversing the order of the extern declarations so the callee that uses a2 first is declared first (declaration order influences callee-saved numbering in mwcc).
- Verify the three-call sequence in the .s (020ab088 → 02045c7c → 020aaddc) matches the C order exactly — no reordering by the optimizer.
---
