---
# func_0205407c (main, HIGH class B, 84B)

**Recipe:** assert-on-null guard then null-early-return + forwarding tail call
**Risk:** movs/bne uses the same r5==0 test twice; mwcc may fold the two null checks or reorder. func_020a6d54 arg count (4) inferred from r0-r3 setup.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — hidden pool-word order mismatch; data_020ffb5c and data_020ffb04 are named globals whose pool-slot order may differ from the orig .s, wildcarded by verify.py, only ninja report catches it

**Secondary mode:** Mode E — visible instruction diff; MWCC may fold the duplicate `r5==0` test into a single branch, changing the movs/bne sequence

**Why this recipe hits this mode:** Two named data_ globals (data_020ffb5c as file string, data_020ffb04 as expr string) are loaded into r0/r1 for the assert call. MWCC assigns pool slots by first-use order; if the recipe places them in the wrong order the pool literals differ from orig — verify.py wildcards BL relocs but not pool-word values, so only ninja report will catch this mismatch.

**Pool words to verify against orig .s:**
- `data_020ffb5c` — assert file-string pointer, loaded into r0 (first arg to func_020a6d54)
- `data_020ffb04` — assert expr-string pointer, loaded into r1 (second arg to func_020a6d54)

**Verify gate sequence:**
1. verify.py src/main/func_0205407c.c func_0205407c [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches (wrong data_ slot) before sha1
3. ninja sha1 — final gate

## Reshape hints

- Keep the two null checks structurally separate: first `if (!r5) func_020a6d54(...)` then `if (r5 == 0) return 0` — merging them may eliminate the second movs/bne that the orig emits
- Write the assert args in orig order: data_020ffb5c -> r0, data_020ffb04 -> r1, 0 -> r2, 0xa0 -> r3; cross-check pool slot order in the compiled .o if ninja report shows a mismatch
- If MWCC folds both null checks, convert the second to `if (!r5)` to match the first and let MWCC emit a paired-movs pattern
- The forwarding tail call passes (int)r5, r4, r5[0] — use explicit casts to suppress promotions that may add extra load/store instructions
- Verify func_020a6d54 prototype has exactly 4 args (void*, void*, int, int) to prevent stack-spill on the assert call
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_020ffb5c` | data symbol |
| `_LIT1` | `data_020ffb04` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020a6d54` | main TU call |
| `bl` | `func_02053fc4` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

