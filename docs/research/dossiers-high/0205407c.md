# func_0205407c (main, HIGH class B, 84B)

**Recipe:** assert-on-null guard then null-early-return + forwarding tail call
**Risk:** movs/bne uses the same r5==0 test twice; mwcc may fold the two null checks or reorder. func_020a6d54 arg count (4) inferred from r0-r3 setup.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0205407c.c)

**Verify gate:**
1. python tools/verify.py src/main/func_0205407c.c func_0205407c --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/0205407c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
