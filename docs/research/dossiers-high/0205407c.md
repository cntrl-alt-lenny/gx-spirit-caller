# func_0205407c (main, HIGH class B, 84B)

**Recipe:** assert-on-null guard then null-early-return + forwarding tail call
**Risk:** movs/bne uses the same r5==0 test twice; mwcc may fold the two null checks or reorder. func_020a6d54 arg count (4) inferred from r0-r3 setup.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0205407c.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0205407c.c func_0205407c --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0205407c.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
