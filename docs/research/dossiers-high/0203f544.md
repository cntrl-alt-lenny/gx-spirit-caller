---
# func_0203f544 (main, HIGH class A, 76B)

**Recipe:** linear stack-buffer call seq; address-of locals in asm order
**Risk:** stack frame is 0x6c; my struct split (0x14/0x58) may shift sp offsets if mwcc lays locals differently
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (sp offset encoding for address-of-local instructions); verify.py catches directly

**Why this recipe hits this mode:** The `add rN, sp, #offset` instructions encoding the addresses of `s.a` and `s.b` embed the sp offsets as immediates; if mwcc chooses a different local layout than the guessed 0x14/0x58 split, those immediates change and the instruction bytes differ in a way verify.py shows immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203f544.c func_0203f544 [--cc all] — sp-offset immediates visible directly
2. ninja sha1 — final gate

## Reshape hints

- Cross-check the orig .s `add rN, sp, #offset` instructions: find the two distinct sp offsets used for `&s.a` (or `&s.a[3]`) and `&s.b`; if they are not `+0x00` and `+0x14`, adjust the struct field sizes accordingly
- If mwcc merges `s.a` and `s.b` into a flat `u8 buf[0x6c]` layout, the sp offsets may shift; try declaring a single flat array `u8 s[0x6c]` and passing `s+0x00`, `s+0x14`, `s+0x17` explicitly
- `func_02094688(&s.a[3], arg1, 0xd)` uses `s.a[3]` which is `sp + 3` relative to `s.a`; verify the orig .s `add` for this arg encodes `+0x03` (or equivalent combined offset from sp)
- The call order (`func_02099298`, `func_020991a4`, `func_020990e0`, `func_02094688`) must match the .s BL sequence exactly; do not reorder
- Confirm total frame is 0x6c by checking the orig .s `sub sp, #0x6c` prologue; if the frame is different (e.g. 0x70 due to alignment), adjust the struct to pad to match
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02099298` | main TU call |
| `bl` | `func_020991a4` | main TU call |
| `bl` | `func_020990e0` | main TU call |
| `bl` | `func_02094688` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

