---
# func_02017be0 (main, HIGH class A, 204B)

**Recipe:** GetSystemWork bitfield-guard + jump-table + nested bit2 tests, cond substate dec
**Risk:** 0x8d8&1 / 0x8e0 bit-tests -> `and` wall; jump-table + `subeq` conditional substate decrement likely diverge
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from `and` wall on two separate bitfield guards and `subeq`-style conditional decrement

**Why this recipe hits this mode:** Both the `(w+0x8d8)<<0x1f)>>0x1f` entry guard and the `(w+0x8e0)<<0x1f)>>0x1f` / `(w+0x8e0)<<0x1e)>>0x1f` inner guards are susceptible to the `and #1` / `and #3` wall. The conditional substate decrement `*(int *)(w+0x924) = *(int *)(w+0x924) - 1` must map to a `subeq` in the .s — any binding of the load to a temp may instead generate an unconditional `sub`.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02017be0.c func_02017be0 [--cc all] — instruction bytes (all three bit guards + subeq decrement visible)
2. ninja sha1 — final gate

## Reshape hints

- Keep all three bit-extract guards in the `(v << k) >> k` shift form — the two bit0 guards use `<<0x1f / >>0x1f` and the bit1 guard uses `<<0x1e / >>0x1f`; do not simplify any to `& 1` or `& 2`
- The conditional decrement in case 2 must be written as `*(int *)(w+0x924) = *(int *)(w+0x924) - 1` (inlined load+sub+store) rather than via a temp variable, to keep mwcc from scheduling a separate `ldr` before the `sub`
- In case 1, the `*(int *)(w+0x920) = 1` and `*(int *)(w+0x924) = 5` pair must execute unconditionally before the bit1 branch — they gate the `func_02019640(0)` call via the second `if`
- Jump-table cases 0..4 (with 0 falling through to 3, and case 4 entirely absent from the switch body) — verify the .s table width; if case 4 is an out-of-range default, the switch must not emit a case 4 label
- The duplicate bit1 test in case 1 (checked twice: once to skip the state-reset, once to skip the call) maps to two separate `tst`/`bne` pairs in .s — do not fold them into a single check with a local bool
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `0x00260100` | MMIO/const |
| `_LIT1` | `0x00260104` | MMIO/const |
| `_LIT2` | `0x00260102` | MMIO/const |
| `_LIT3` | `0x00260103` | MMIO/const |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_02019664` | main TU call |
| `bl` | `func_0201969c` | main TU call |
| `bl` | `func_020195ec` | main TU call |
| `bl` | `func_0201b7b4` | main TU call |
| `bl` | `func_0201abd4` | main TU call |
| `bl` | `func_02019640` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

