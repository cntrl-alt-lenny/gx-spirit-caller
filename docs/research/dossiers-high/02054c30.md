---
# func_02054c30 (main, HIGH class A, 52B)

**Recipe:** byte marshal around call + reload + tail call
**Risk:** func_02054ea8/func_0206eb4c return types assumed int; *a2 store width (str) vs strb
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (store-width str vs strb and tail-call register scheduling)

**Why this recipe hits this mode:** The store of the reloaded byte back into `*a2` may emit `str` (word) instead of `strb` (byte) depending on the cast, and the tail call to `func_02054ea8` with `-1` may schedule registers differently — both are byte-visible and caught by verify.py.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054c30.c func_02054c30 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The store `*a2 = *a1` after the call must use byte width; keep `a2` typed as `int *` and the right-hand side cast as `(unsigned char)*a1` so mwcc emits `strb`, not a full-width `str`.
- The reload `*a1` must happen after `func_0206eb4c` returns, not before — do not hoist the load across the call boundary.
- If objdiff shows the tail call emitting a `bl` + `pop` instead of a `b` (branch tail), try returning the value of `func_02054ea8` directly without an intervening store to a local.
- Both callee return types must stay `int`; changing either to `void` will alter the calling convention and break the tail-call pattern.
- If the str/strb mismatch persists, try `*(unsigned char *)a2 = *a1` to make the byte-narrowing explicit at the pointer level.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206eb4c` | main TU call |
| `bl` | `func_02054ea8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

