---
# func_02040c98 (main, HIGH class A, unknown B)

**Recipe:** trivial stack-buffer two-call wrapper; arg0 held in r4
**Risk:** frame 0x18 fixed; arg0 saved to callee-saved before first call — natural; buf size guessed
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E (VISIBLE) — frame size rounding or call order mismatch shifts instruction bytes; visible in verify.py

**Why this recipe hits this mode:** the only structural risks are the frame size (0x18 buf, possibly padded to 0x20 by mwcc) and the call order (`func_02044610` is called first to initialise the buffer, then `func_020409d8` uses it); any deviation produces a visible instruction-byte diff.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02040c98.c func_02040c98 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Call `func_02044610(&buf[0])` first (init), then `func_020409d8(arg0, &buf[0])` (use); reversing the order is a common mistake on two-call wrappers
- arg0 must be preserved across the first call in a callee-saved register (r4); declare it as a function parameter without rebinding so mwcc naturally saves it
- Frame 0x18 is expected; if mwcc emits `sub sp, #0x20`, try a smaller buf type or adjust alignment
- No data globals are referenced so there are no pool-word hazards; all operands are stack-relative or register
- If call order and frame match but bytes still differ, check the prologue push list — the second call needs arg0 in a callee-saved reg, so r4 must appear in the push
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02044610` | main TU call |
| `bl` | `func_020409d8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

