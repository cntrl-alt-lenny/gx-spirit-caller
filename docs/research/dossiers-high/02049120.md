---
# func_02049120 (main, HIGH class A, 108B)

**Recipe:** GUARD + find-loop + bound-global reload + accessor f14
**Risk:** g->f14 reload placement / r5-vs-r6 reg-alloc on i,arg may flip
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — Wrong pool word value (hidden from verify.py; only ninja report catches it)

**Why this recipe hits this mode:** `data_0219dc80` is used as the guard-load base and also reloaded inside the loop (`[r4]` pattern); the two LDR-pool slots for this global must match the orig .s .word entries exactly, and verify.py wildcards these relocatable pool slots.

**Pool words to verify against orig .s:**
- `data_0219dc80` — guard base and within-loop reload base for struct ptr; all pool-word slots referencing this global must match the orig .s

**Verify gate sequence:**
1. verify.py src/main/func_02049120.c func_02049120 [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Reload `data_0219dc80->count` (f14) inside the do-while condition rather than caching it in a local; the .s shows `ldr r3,[r4]` for the bound check each iteration
- If reg-alloc flips i and key between r5 and r6, try reordering the local declarations or changing the loop variable name to influence MWCC's allocation order
- The do-while form is important: the .s enters the loop body before the bound check, so avoid converting to a while-first structure
- Ensure the null guard on `key` comes before the loop, not after the `count` guard; the .s has three sequential beq-to-return checks in order: ptr, key, count
- Cross-check `func_0204918c` BL target address in the .s against the extern declaration to catch any Mode B mismatch early
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219dc80` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0204918c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

