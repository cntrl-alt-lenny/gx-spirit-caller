# func_022a8ca8 (ov002, HIGH class A, 72B)

**Recipe:** Sequential-call dispatch, arg held in r4 across all calls
**Risk:** Callee signatures guessed void(void*); if any returns/varies args r0 setup unchanged so still safe. Order is fixed.
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee/BL target (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay002/func_ov002_022a8ca8.c func_ov002_022a8ca8 --cc all --module ov002` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode B mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_0229d258` | overlay call (ov002) |
| `bl` | `func_ov002_022a8cf0` | overlay call (ov002) |
| `bl` | `func_ov002_022a96a8` | overlay call (ov002) |
| `bl` | `func_ov002_022a8e40` | overlay call (ov002) |
| `bl` | `func_ov002_022a9144` | overlay call (ov002) |
| `bl` | `func_ov002_022a9b6c` | overlay call (ov002) |
| `bl` | `func_ov002_022a9cac` | overlay call (ov002) |
| `bl` | `func_ov002_022a9d9c` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Cross-check every BL/BLX target address in the .s against your `extern func_XXXX` declarations
- BL relocs are wildcarded by verify.py; only `ninja report` resolves them
- Wrong callee address passes verify.py at 100% but fails sha1

## Reference

- Prep: `docs/research/c-match-prep/022a8ca8.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
