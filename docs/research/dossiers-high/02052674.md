---
# func_02052674 (main, HIGH class A, 68B)

**Recipe:** store-global + Fill call + offset halfword store (constant); sp3
**Risk:** data_0219e3ec is ptr (str r0 then later ldr+deref); func_020945f4(_,0,0x614); +0x600+0x10 strh 0x5b9; sp3
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named global `data_0219e3ec` is a pool word; wrong value wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** `data_0219e3ec` appears as both a store target (STR) and a subsequent load base (LDR+deref) for the halfword store; if its pool literal address is wrong, verify.py wildcards the reloc and the mismatch is invisible until `ninja report`.

**Pool words to verify against orig .s:**
- `data_0219e3ec` — global pointer: first written (str r0) to initialise it, then re-loaded for the strh at +0x610

**Verify gate sequence:**
1. verify.py src/main/func_02052674.c func_02052674 [--cc all] [--module main] — instruction bytes
2. ninja report — resolves data_0219e3ec pool word; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- sp3 routing: file must be named `.legacy_sp3.c`; frame shape is push{lr}+sub sp,#4+pop pc
- The store `data_0219e3ec = p` must come before the Fill call so that r0 is still the parameter and STR fires first
- The halfword constant is `0x5b9` at offset `0x610` (= 0x600+0x10) — write as `*(unsigned short *)((char *)data_0219e3ec + 0x600 + 0x10) = 0x5b9;` to force the same base+offset addressing
- Fill args are `(p, 0, 0x614)` — verify these three constants against the orig BL sequence; wrong size changes the pool
- Cross-check the `data_0219e3ec` pool literal in the orig .s to confirm the assembled address matches the global symbol
---
