---
# func_02023fec (main, HIGH class A, 56B)

**Recipe:** GUARD + invoke + clear-global (bind base across call)
**Risk:** mwcc may CSE all three global refs into one register instead of the guard/r4 split; value could be int not pointer
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — pool-word order; `data_0219a8e4` is the load base for both the guard load and the body base (r4 bind); two pool entries expected; wildcarded by verify.py, only ninja report catches a CSE collapse

**Why this recipe hits this mode:** `data_0219a8e4` is referenced three times: the guard `!= 0` test (r0 load), the call arg (r4 bind), and the clear store. Mwcc must hold the base in r4 across the call, generating two pool entries for the same symbol. If CSE collapses all three to one register/pool entry the pool count changes silently — only `ninja report` exposes this.

**Pool words to verify against orig .s:**
- `data_0219a8e4` — guard load base (pool word 1, r0 deref for `!= 0` test) and body base held in r4 for `Task_InvokeLocked` arg and the subsequent null store (pool word 2 expected)

**Verify gate sequence:**
1. verify.py src/main/func_02023fec.c func_02023fec [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A CSE collapse before sha1
3. ninja sha1 — final gate

## Reshape hints

- The double-deref `*(void **)data_0219a8e4` for both the guard and the call arg is the key to forcing two pool loads; avoid a local variable that lets mwcc CSE both into one register prematurely
- If mwcc still collapses guard + call arg to one pool word, try `void *v = *(void**)data_0219a8e4; if (v) { Task_InvokeLocked(v); *(void**)data_0219a8e4 = 0; }` — binding `v` forces r4 and the clear still uses the base address
- The clear `*(void**)data_0219a8e4 = 0` must use the global base address directly (not a local copy) to preserve the store's pool word
- If the type proves to be `int` rather than pointer, replace `*(void**)` with `*(int*)` — the guard and clear are type-neutral but the callee signature must match
- Confidence is high for this pattern; the guard/invoke/clear recipe is well-understood
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `.L_0202401c` | `data_0219a8e4` | data symbol |
| `.L_02024020` | `data_0219a8e4` | data symbol |

> **Mode D warning:** duplicate pool value(s) ['data_0219a8e4'] — `clean C` will deduplicate → 4-byte size diff → sha1 fail. This is a `.s`-only candidate.

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `Task_InvokeLocked` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

