---
# func_02045cb8 (main, HIGH class A, 72B)

**Recipe:** sequential calls + global stores + pooled-immediate arg
**Risk:** low; preload order of the 0xfffe8515 pool word vs the daf0 store is scheduling-only
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool-word value (HIDDEN; only ninja report catches)
**Secondary mode:** Mode E — visible scheduling diff (pool-word preload vs store order)

**Why this recipe hits this mode:** Both `data_0219daf0` and `data_0219daec` are named globals used as store targets, so they are loaded from the literal pool; verify.py wildcards those loads. A wrong address for either global passes verify.py silently. Additionally, the constant 0xfffe8515 must also come from the pool, and mwcc may preload it either before or after the `data_0219daf0` store — a scheduling-only byte diff that verify.py does surface.

**Pool words to verify against orig .s:**
- `data_0219daf0` — store target for func_02054bfc() result; pool .word must match orig .s
- `data_0219daec` — pointer global; dereferenced and zeroed via `*data_0219daec = 0`; pool .word must match orig .s

**Verify gate sequence:**
1. verify.py src/main/func_02045cb8.c func_02045cb8 [--cc all] [--module main] — instruction bytes; scheduling diff visible here
2. ninja report — resolves pool words for data_0219daf0 and data_0219daec; catches Mode A mismatches
3. ninja sha1 — final gate

## Reshape hints

- Write the statements in exactly the order the .s executes them: func_02062510(), then assign data_0219daf0, then func_0204525c(9, 0xfffe8515), then *data_0219daec = 0.
- If mwcc preloads the 0xfffe8515 pool word too early (before the daf0 store), introduce a temporary `int imm = 0xfffe8515;` to pin when it materialises.
- Ensure `data_0219daf0` is typed `int` (not `unsigned int`) and `data_0219daec` is `int *` — type mismatch shifts the store width/instruction.
- The pool preload vs store scheduling is the only expected diff; if verify.py is clean, proceed directly to ninja sha1.
- Cross-check both pool .word entries in the .s against config/eur/arm9/symbols.txt before linking.
---
