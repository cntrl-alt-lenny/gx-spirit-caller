# func_021b2760 (ov006, HIGH class A, 164B)

**Recipe:** capture field, 7-sink step + enter, guarded state-set (store-order), return 1
**Risk:** guard codegen (sel!=0 && sel==1 -> cmp#0;beq;cmp#1;bne) and per-branch reload of the state global must match; field held in r4 across calls
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/overlay006/func_ov006_021b2760.c func_ov006_021b2760 --cc all --module ov006` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov006_0225c4dc` | raw |
| `_LIT1` | `data_ov006_0225e138` | raw |
| `_LIT2` | `data_ov006_0225e100` | raw |
| `_LIT3` | `data_ov006_0225e068` | raw |
| `_LIT4` | `data_ov006_0225df3c` | raw |
| `_LIT5` | `data_ov006_0225de70` | raw |
| `_LIT6` | `data_ov006_0225cb5c` | raw |
| `_LIT7` | `data_ov006_021cf140` | raw |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov006_021cb0a0` | overlay call (ov006) |
| `bl` | `func_ov006_021cac30` | overlay call (ov006) |
| `bl` | `func_ov006_021c9ef4` | overlay call (ov006) |
| `bl` | `func_ov006_021c757c` | overlay call (ov006) |
| `bl` | `func_ov006_021c6990` | overlay call (ov006) |
| `bl` | `func_ov006_021c159c` | overlay call (ov006) |
| `bl` | `func_ov006_021ba090` | overlay call (ov006) |
| `bl` | `func_ov006_021b2620` | overlay call (ov006) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Run `verify.py --cc all`; instruction byte diffs tell you what to reshape
- movne/moveq: try inverting the condition or swapping then/else
- `and #1` wall: use `(x << 31u) >> 31` to force lsl;lsr pair
- jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: `docs/research/c-match-prep/021b2760.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
