---
# func_02041dbc (main, HIGH class A, unknown B)

**Recipe:** memset call; bind base+0x1000 once (held in r1); store-order fields; reload 0x1134 twice
**Risk:** 0xb68/0x5ea ride pool words (LIT1/LIT2) — must stay literal; base+0x1134 reloaded not bound; store order exact
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E (VISIBLE) — store order for the four `b[0x170/4]`, `b[0x174/4]`, `b[0x17c/4]`, `b[0x180/4]` assignments must match the orig .s str/ldr sequence exactly; any reordering is a visible instruction-byte diff

**Why this recipe hits this mode:** the recipe has a specific store order interleaved with two reloads from `b[0x9c8/4]` and `b[0x9cc/4]`; mwcc's instruction scheduling is sensitive to the order fields are written. Additionally the literal constants 0xb68 and 0x5ea must survive as pool words (LIT1/LIT2) rather than being folded into immediates; if mwcc encodes them differently the byte sequence changes.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py). Note: 0xb68 and 0x5ea are literal pool words in the orig .s but are raw integer constants in the C; they are compared directly by verify.py as long as their pool placement matches.

**Verify gate sequence:**
1. verify.py src/main/func_02041dbc.c func_02041dbc [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Write the four stores in this exact order: `b[0x170/4] = 0xb68`, then `b[0x174/4] = b[0x9c8/4]` (reload1), then `b[0x17c/4] = 0x5ea`, then `b[0x180/4] = b[0x9cc/4]` (reload2); the interleaved literal-then-reload pattern must match the orig .s
- Bind `b = (int *)(base + 0x1000)` as a single local so mwcc holds it in r1 across all stores; do not recompute `base + 0x1000` inline each time
- The `base + 0x1134` argument to `func_020945f4` (memset) and `func_02071028` must be computed as `base + 0x1134` each time — not bound to a separate local — to reproduce the two-reload pattern the risk note describes
- The constants 0xb68 and 0x5ea cannot be encoded as 8-bit rotated ARM immediates; they should appear as LDR pool loads in the orig .s — verify their pool-word encoding against the .s before declaring a match
- The memset call `func_020945f4(base + 0x1134, 0, 0x64)` must come first; then the b-pointer bind; then the four stores; then the final `func_02071028(base + 0x1134)` — any reordering changes the register schedule
---
