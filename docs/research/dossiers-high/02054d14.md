---
# func_02054d14 (main, HIGH class A, 56B)

**Recipe:** arg-forward wrapper + stacked-arg + tail call
**Risk:** reg-alloc of forwarded r0-r3 / stack-slot ordering of p5 store may differ; arg5/arg6 split a guess
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (reg-alloc shuffle for stacked args and byte-store width)

**Why this recipe hits this mode:** Forwarding `a1..a4` through `r0-r3` to `func_0206ed7c` while also placing `p5` on the stack can cause mwcc to emit extra move instructions to resolve the register overlap; any scheduling difference is visible to verify.py.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054d14.c func_02054d14 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- The parameter naming convention (a1..a4 for r0-r3) means the first register arg is named `a1` not `a0`; keep that naming to avoid accidental register reassignment by the compiler.
- The byte stores `*p5 = (unsigned char)*p6` and `*p6 = (unsigned char)*p5` must use `strb`; keep the explicit `(unsigned char)` cast on the right-hand side.
- `p5` is a stacked argument (5th arg) already in memory; passing it to `func_0206ed7c` as the 5th arg (also stacked) means the compiler must emit a stack-to-stack `ldr`/`str` pair — confirm that pair appears in the diff.
- If objdiff shows `r4`/`r5` callee-saves being used for `p5`/`p6`, try swapping the order of their declarations to see if that alters which callee-save registers get allocated.
- The tail call `return func_02054ea8(r, -1)` must produce a `b` branch; if it emits `bl`+`bx lr`, split: `int ret = func_02054ea8(r, -1); return ret;`.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0206ed7c` | main TU call |
| `bl` | `func_02054ea8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

