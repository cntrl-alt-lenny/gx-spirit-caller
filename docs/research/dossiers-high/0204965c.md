---
# func_0204965c (main, HIGH class A, 40B)

**Recipe:** GUARD + dual SETTER (f44,f48) + bound-global reload, return 0/1
**Risk:** field offsets guessed; arg-store order f44 then f48 must match
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A — Wrong pool word value (hidden from verify.py; only ninja report catches it)

**Why this recipe hits this mode:** `data_0219dc80` is loaded from the pool for the null guard and then reloaded between the two stores (`ldrne r2,[r2]` pattern); both pool-word slots are wildcarded by verify.py, so a wrong global address passes instruction comparison silently.

**Pool words to verify against orig .s:**
- `data_0219dc80` — guard load base and inter-store reload base; the .s has two pool .word entries for this global that must both match

**Verify gate sequence:**
1. verify.py src/main/func_0204965c.c func_0204965c [--cc all] — instruction bytes
2. ninja report — resolves pool words; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write f44 store before f48 store and do not cache the reloaded pointer: the .s reloads `data_0219dc80` from its own pool slot between the two `str` instructions (`ldrne r2,[r2]`), so the C must produce a second load of the global before accessing f48
- The field offsets 0x44 and 0x48 are guessed; if verify.py shows a wrong LDR/STR offset, adjust the pad array size in the struct typedef
- The return values 0 (on null guard) and 1 (success) are unconditional; make sure neither path falls through to the other's return
- The confidence is high, so prioritize checking the pool-word address and field offsets in the .s before any instruction-level reshaping
- Use `ninja report` immediately after the first green `verify.py` run to catch the Mode A pool-word risk before committing the match
---
