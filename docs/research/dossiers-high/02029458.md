---
# func_02029458 (main, HIGH class A, 104B)

**Recipe:** signed s16 accessor guard + mvn(-1) store + byte/halfword bic RMW + invoke-clear
**Risk:** compiler may not hoist byte187 base (r4+0x87) above the blt; store scheduling; offsets guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; store scheduling, byte187 hoist, and `mvn`/`bic` instruction forms are directly visible to verify.py
**Secondary mode:** Mode B — BL targets `func_02022580` and `Task_InvokeLocked` are extern guessed addresses; wrong callees wildcarded by BL reloc, only ninja report catches

**Why this recipe hits this mode:** The instruction ordering around the `h184 >= 0` guard — specifically whether `r4+0x87` (byte187 base) is hoisted before the `blt` — is a scheduling difference that verify.py reports immediately. The `mvn` for `-1` and the `bic` RMW forms are instruction-visible. Callee addresses are guessed and BL-wildcarded.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02029458.c func_02029458 [--cc all] — instruction bytes (scheduling and bic form visible)
2. ninja report — resolves BL targets; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- The `p->h184 = -1` assignment must use `s16` type so mwcc emits `mvn r,#0` (or `mov r,#-1`) + `strh`; do not widen to int before storing
- The `p->byte187 &= ~1` RMW requires `ldrb`/`bic #1`/`strb` — keep `byte187` typed as `u8` to prevent halfword or word RMW
- If objdiff shows the byte187 base computation appearing after the `blt`, try materializing a pointer before the guard: `u8 *pb = &p->byte187;` to coax the hoist
- The `p->flags19a &= ~1` halfword RMW must use `ldrh`/`bic`/`strh` — keep `flags19a` typed as `u16`
- Struct offsets 0x184 (h184), 0x187 (byte187), 0x188 (field188), 0x19a (flags19a) are guessed — cross-check against orig .s ldrsh/ldrb/ldr/ldrh immediate values if objdiff shows layout errors
---
