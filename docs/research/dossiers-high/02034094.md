# func_02034094 (main, HIGH class A, 36B)

**Recipe:** range guard (blt->0) + (v<=K)->movle/mov bool; K from pool
**Risk:** first param assumed unused so value lands in r1; minor: movle/mov vs movle/movgt ordering

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02034094.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02034094.c func_02034094 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02034094.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
