# func_020442f8 (main, HIGH class A, 48B)

**Recipe:** COMMUTATIVE eor operand-order + parallel induction-var loop
**Risk:** strength reduction: mwcc may emit i<<3 instead of the separate sh accumulator the asm keeps

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/020442f8.c)

**Verify gate:**
1. python tools/verify.py src/main/func_020442f8.c func_020442f8 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/020442f8.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
