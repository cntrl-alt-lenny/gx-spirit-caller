---
# func_020442f8 (main, HIGH class A, 48B)

**Recipe:** COMMUTATIVE eor operand-order + parallel induction-var loop
**Risk:** strength reduction: mwcc may emit i<<3 instead of the separate sh accumulator the asm keeps
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches it directly

**Why this recipe hits this mode:** The parallel induction variable `sh` (0, 8, 16, 24) may be strength-reduced by mwcc to `i << 3`, collapsing the two-variable loop to one and changing the instruction sequence; the eor operand order for `~0u ^ (~0u >> n)` is also commutative but the asm encodes a specific operand ordering that verify.py surfaces.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020442f8.c func_020442f8 [--cc all] — instruction bytes; diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Keep the two-variable loop `for (i = 0, sh = 0; i < 4; i++, sh += 8)` exactly as written; do not simplify to `i << 3` — the parallel `sh` accumulator matches the asm's separate shift register (rsb r0,r2,#0x18 pattern)
- Write the eor as `~0u ^ (~0u >> n)` (unshifted operand first); if verify.py shows reversed operand order, swap to `(~0u >> n) ^ ~0u` to flip the asm encoding
- Use `(unsigned char)(mask >> (0x18 - sh))` for the byte extraction; a signed intermediate may trigger an asr instead of lsr for the mask shift
- If mwcc strength-reduces the loop anyway, try `volatile int sh = 0` or split the increment into two statements to prevent the optimizer from folding sh into i<<3
- Run verify.py --cc all; the operand-order diff will appear as a single-instruction swap that is immediately identifiable
---
