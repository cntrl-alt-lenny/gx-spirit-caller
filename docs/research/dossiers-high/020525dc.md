---
# func_020525dc (main, HIGH class A, 64B)

**Recipe:** double-deref global (RELOAD) + null-guard + blx callback dispatch
**Risk:** global is ptr-to-struct, +0x60c is fn-ptr; callback(arg1, func_02046870()); r0(arg0) ignored at entry
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — named global `data_0219e3ec` is a pool word; wrong value is wildcarded by verify.py reloc, only ninja report catches it

**Why this recipe hits this mode:** `data_0219e3ec` is loaded via an LDR pool literal — if the pool word value is wrong (wrong address assembled), verify.py wildcards the reloc bytes and the mismatch is invisible until `ninja report` resolves the symbol.

**Pool words to verify against orig .s:**
- `data_0219e3ec` — double-deref base: loaded as pointer, then dereffed again at +0x60c to get the callback fn-ptr; appears twice (RELOAD pattern)

**Verify gate sequence:**
1. verify.py src/main/func_020525dc.c func_020525dc [--cc all] [--module main] — instruction bytes
2. ninja report — resolves data_0219e3ec pool word; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- The RELOAD pattern for `data_0219e3ec` must appear twice: once for the null-guard `cb == 0` check, and once for the dispatch call — do not cache the pointer into a local variable
- The first parameter `unused` must be declared and cast to void; omitting it changes the prologue register layout
- `func_02046870()` result must be bound to a local `v` inside a nested block after the guard passes to match the compiler's register allocation
- Callback arg order is `(arg, v)` not `(v, arg)` — cross-check against the BLX operand order in the orig .s
- Cross-check the `data_0219e3ec` pool literal in the orig .s (`ldr r2,[pc,#N]; ldr r1,[r2]; ldr r1,[r1,#0x60c]`) to confirm the assembled address matches the symbol
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219e3ec` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02046870` | main TU call |
| `blx` | `r2` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

