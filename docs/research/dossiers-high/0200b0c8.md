---
# func_0200b0c8 (main, HIGH class A, unknown B)

**Recipe:** null-guard + counted loop (RELOAD arr base each iter) + tail invokes
**Risk:** count read once before loop vs re-read; relies on p->arr[i] reloading f4 each iteration
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches loop-reload vs hoist difference

**Why this recipe hits this mode:** The critical constraint is that `p->arr[i]` must reload the `f4` base register each iteration rather than hoisting it; if mwcc decides to hoist the pointer, the emitted LDR sequence changes and byte-match fails.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0200b0c8.c func_0200b0c8 — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Do NOT hoist `p->arr` before the loop; keep `p->arr[i]` inline so `f4` (arr base) reloads every iteration
- Keep `p->count` read before the loop in a local `int i; if (!p) return 0;` guard to match the count-read-once pattern
- The two tail calls `Task_InvokeLocked((void *)p->arr)` then `Task_InvokeLocked((void *)p)` must remain in this exact order after the loop body
- If objdiff shows the loop reloading `arr` incorrectly, try `volatile void **arr = p->arr;` inside the loop or restructure to force per-iteration LDR
- Check that the null-guard for `p->arr[i]` is emitted as a standalone conditional BL, not fused into the loop increment
---
