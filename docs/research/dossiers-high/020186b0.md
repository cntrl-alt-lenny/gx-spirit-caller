---
# func_020186b0 (main, HIGH class A, 24B)

**Recipe:** GetSystemWork leaf bit0-extract accessor (the documented `and #1` wall)
**Risk:** GUARANTEED mismatch: `(v<<31)>>31` lowers to `and r0,#1` (1 insn), baserom is `lsl#31; movs lsr#31` (2 insn) — in-tree .s for this exact reason
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — GUARANTEED visible instruction byte diff; this function IS the canonical `and #1` wall; no C phrasing recovers the two-instruction form

WARNING: This candidate cannot be matched as clean C. The in-tree .s for func_020186b0 exists specifically because mwcc always lowers any C bit0-extract to a single `and r0,r0,#1` instruction, whereas the baserom emits the two-instruction `mov r0,r0,lsl#31; movs r0,r0,lsr#31` sequence. There is no known C spelling that forces the two-instruction form from mwcc 2.0/sp1p5. Do NOT attempt to replace the .s file.

**Why this recipe hits this mode:** The function body is a single expression `(*(unsigned int *)(GetSystemWork() + 0x8d8) << 0x1f) >> 0x1f`. mwcc unconditionally folds `(x << 31) >> 31` into `and r0,r0,#1` regardless of phrasing — the two-instruction baserom sequence is not reachable from C with this compiler.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_020186b0.c func_020186b0 [--cc all] — will show `and #1` (C) vs `lsl#31; lsr#31` (orig); confirms mismatch
2. ninja sha1 — will fail; .s must remain

## Reshape hints

- Do NOT attempt this match — the mismatch is guaranteed by the compiler's algebraic simplification of `(x<<31)>>31`
- All other functions in this cluster that use the same guard idiom inline it as `if (!((*(unsigned int *)(w + 0x8d8) << 0x1f) >> 0x1f)) return;` — they do not call func_020186b0, so this function stands alone as a .s-only symbol
- If the project ever migrates to a compiler version that does not simplify the shift pair, revisit; otherwise treat this address as permanently .s
- The prep file is included to document the attempt and the reason for the .s — leave the .s in place and mark the address accordingly in the campaign worklist
- No reshape is possible: the problem is in the compiler's pattern-matching pass, not in the C structure
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

