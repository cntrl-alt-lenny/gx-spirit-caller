# func_021c4348 (ov006, HIGH class A, 116B)

**Recipe:** INIT store-order + min/max-style if-guards, counted scan loop
**Risk:** struct field offsets/types guessed; r3-as-4th-param ABI may force a different reg-alloc
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay006/func_ov006_021c4348.c func_ov006_021c4348 --cc all --module ov006` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_ov006_021cde48` | raw |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Read the orig .s pool section; C must reference the same globals in the same ORDER
- Named `data_XXXX` globals are pool-word risks; wrong address silently passes verify.py
- Use `ninja report` (not just `ninja objdiff`) before sha1

## Reference

- Prep: `docs/research/c-match-prep/021c4348.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
