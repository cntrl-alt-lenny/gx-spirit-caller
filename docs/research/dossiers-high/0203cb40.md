# func_0203cb40 (main, HIGH class A, 80B)

**Recipe:** Linear state dispatch; sub sp #4 byte temp on stack; tail calls
**Risk:** Stack byte-slot (strb/ldrb [sp]) needs char local addr-taken; func_0203dc10 takes &temp

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203cb40.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_0203cb40.c func_0203cb40 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/0203cb40.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
