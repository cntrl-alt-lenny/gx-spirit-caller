# func_02052f04 (main, HIGH class B, 132B)

**Recipe:** discriminant switch, tail calls, two-word call-result reuse
**Risk:** func_0205370c returns pair (r0,r1) into a,b; modeling its second return (r1=b) as a separate int is fragile — may need long long split.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/02052f04.c externs)

**Verify gate:**
1. python tools/verify.py src/main/func_02052f04.c func_02052f04 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/02052f04.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
