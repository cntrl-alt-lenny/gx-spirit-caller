---
# func_02052f04 (main, HIGH class B, 132B)

**Recipe:** discriminant switch, tail calls, two-word call-result reuse
**Risk:** func_0205370c returns pair (r0,r1) into a,b; modeling its second return (r1=b) as a separate int is fragile — may need long long split.
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode B — wrong callee modelling; BL reloc wildcards the second return register, hiding a mismatch until ninja report

**Why this recipe hits this mode:** `func_0205370c` is declared returning `int` but is suspected to return a two-register pair (r0=a, r1=b). verify.py wildcards BL relocations, so if the calling convention is wrong (e.g. callee should be `long long`) the r1 discrepancy is invisible until `ninja report` resolves the BL and compares the full call sequence.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02052f04.c func_02052f04 [--cc all] — instruction bytes (BL wildcarded)
2. ninja report — resolves BL targets; catches Mode B two-register-return mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- If `b` (r1) is actually non-zero from the callee, change `func_0205370c` to return `long long` and split: `a = (int)v; b = (int)(v >> 32)` — or reverse if the orig .s loads r1 into the lo word
- The prep hardcodes `b = 0` in case 2; delete that assignment if the orig .s shows r1 is live from the BL rather than zeroed separately
- Keep the `switch` using the `func_02053688(r4)` call exactly once — any hoist changes the case-dispatch code path and the literal pool
- For case 2 tail: confirm `func_020534b4(a, b, r6[9])` is a genuine tail call (B instruction) in the orig .s; if it is BL+pop then it is not a tail call and the frame must be preserved
- For case 3: likewise verify `func_02053704(r4)` is B not BL in the orig .s before writing it as a tail-call `return`
---
