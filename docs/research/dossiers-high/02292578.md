# func_02292578 (ov002, HIGH class A, 108B)

**Recipe:** :2 bitfield guard, !bit0 rsb, 4-arg call, bool tails
**Risk:** tag2 :2 field at bit12 offset guessed; tail func_02291160(self) arg assumed; equality polarity
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/overlay002/func_ov002_02292578.c func_ov002_02292578 --cc all --module ov002` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_ov002_021bcf18` | overlay call (ov002) |
| `bl` | `func_ov002_021bbf50` | overlay call (ov002) |
| `bl` | `func_ov002_02291160` | overlay call (ov002) |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Read the orig .s pool section; C must reference the same globals in the same ORDER
- Named `data_XXXX` globals are pool-word risks; wrong address silently passes verify.py
- Use `ninja report` (not just `ninja objdiff`) before sha1

## Reference

- Prep: `docs/research/c-match-prep/02292578.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
