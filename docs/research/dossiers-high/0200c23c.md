---
# func_0200c23c (main, HIGH class A, unknown B)

**Recipe:** indexed find-slot loop + if-guards; pointer-arith addressing
**Risk:** ip-counter reg choice or the lsl#4 base-add vs struct-index addressing may differ; full=8 sentinel must stay int
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff; verify.py catches lsl#4 pointer-arith vs struct-index encoding difference

**Why this recipe hits this mode:** The compiler's register choice for the induction counter (`ip` vs another register) and whether it emits `add r,base,i,lsl#4` or a struct-indexed load changes the instruction bytes. Using raw pointer arithmetic `o + (i << 4) + offset` must lower to exactly `lsl #4` scaling.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0200c23c.c func_0200c23c — lsl#4 encoding and register choice are visible directly
2. ninja sha1 — final gate

## Reshape hints

- Keep the induction variable `i` as a plain `int` declared before the `do`-loop so the compiler uses a stable register for it across the loop body
- Write the key read as `*(int *)(o + (i << 4) + 0x1e8)` (explicit pointer arith with `i << 4`) to force `lsl #4` in the add; struct indexing may emit different scaling
- The sentinel check `if (i == 8) return 0;` must come after the loop exit and use `int` comparison (`i == 8`), not unsigned, to match the signed cmp
- Write the two stores (`val` then `id`) as separate `*(int *)` pointer-arith expressions in the same order as the .s str sequence
- If the register choice for `ip` differs, try hoisting the `o + (i << 4)` sub-expression into a local pointer to influence which register the compiler picks for the accumulator
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

