# func_02053a64 (main, HIGH class A, 80B)

**Recipe:** stacked-arg call + result-store-to-global; reload global
**Risk:** data_0219e474 offsets 0x0/0x4c/0x50 guessed; the two global reloads (LIT1 used twice) must each reload, not bind

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
- data_0219e474

**Verify gate:**
1. python tools/verify.py src/main/func_02053a64.c func_02053a64 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/02053a64.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
