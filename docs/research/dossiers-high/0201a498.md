---
# func_0201a498 (main, HIGH class B, ?B)

**Recipe:** Bind-base GetSystemWork + reload field, lsl/lsrs#31 bit-test guard, orr-set+store
**Risk:** same as a3ec: empty-if/else fall-through may invert, lsr operand-reg choice may differ. Bit shift 0x15 -> bit 10, mask 0x400.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible diff from inverted empty-if/else branch direction and lsl/lsr operand-reg selection

**Why this recipe hits this mode:** Sibling of func_0201a3ec with bit 10 instead of bit 9; the same empty-if/else fall-through inversion hazard applies. The `(v << 0x15) >> 0x1f` shift pair must select the same registers as the orig .s, and the beq-fall-through direction must be preserved — both are visible byte differences.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_0201a498.c func_0201a498 [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- This is the bit-10 sibling of func_0201a3ec: use shift `<< 0x15` (not `<< 0x16`) and mask `0x400` (not `0x200`), and call `func_0200a26c(5)` (not `4`).
- Write the empty-if/else in the same orientation as a3ec: `if (bitSet) { } else { store; notify; }` — if mwcc inverts the branch, try the negated condition with no empty body.
- Keep `sys = GetSystemWork()` bound across all three calls (func_020190c0, func_0201a454, and the conditional store) just as in a3ec.
- Cross-check field offset 0x8f8 in the orig .s — it is the same across the sibling family.
- If mwcc emits `movgt`/`movle` instead of `movs`/`lsr` for the bit-test, the shift pair must be rewritten as a `movs`-flagging form rather than a plain arithmetic expression.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `GetSystemWork` |  |
| `bl` | `func_020190c0` | main TU call |
| `bl` | `func_0201a454` | main TU call |
| `bl` | `func_0200a26c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

