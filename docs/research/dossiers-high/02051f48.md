---
# func_02051f48 (main, HIGH class A, 80B)

**Recipe:** byte field copy + switch-on-halfword tail call; reuse r1 sw value; sp3
**Risk:** fields 0x1d/0x1e u8, 0x22 u16; func_0204fe54(a, p->f22, b) middle arg = leftover r1; sp3 routing
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from r1 register reuse vs reload for the middle argument

**Why this recipe hits this mode:** The middle argument to `func_0204fe54` must be the halfword value already held in r1 after the ldrh; if the compiler emits an extra ldrh reload instead of reusing r1, the instruction sequence differs visibly and verify.py catches it directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02051f48.c func_02051f48 [--cc all] — instruction diff visible (r1 reuse vs reload)
2. ninja sha1 — final gate

## Reshape hints

- The `sw` local variable binding is critical: load `p->f22` into `sw` before the if-chain so the compiler holds it in r1 and reuses it as the middle arg to `func_0204fe54` without a reload
- If objdiff shows an extra ldrh before the BL, try removing the `sw` local and passing `p->f22` directly as the middle arg — or vice versa — to alter register allocation
- sp3 routing: file must be named `.legacy_sp3.c`; frame shape is push{r4,r5,lr}+sub sp,#4+pop pc
- The byte copy `p->f1d = p->f1e` must come before the halfword load; reordering changes instruction scheduling
- Confidence is low: the r1-leftover trick is fragile; if no reshape recovers it, escalate to permuter
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0205236c` | main TU call |
| `bl` | `func_0204fe54` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

