---
# func_0200fdc0 (main, HIGH class A, unknown B)

**Recipe:** single list search; compare arg[8]==(node->fc>>12u); leaf
**Risk:** unsigned lsr#0xc -> must stay unsigned shift; node field @0xc, next@0; head@+0x910
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode A (HIDDEN) — `data_0218dbc8` is a named pool global; its value is wildcarded by verify.py and only `ninja report` catches a wrong address

**Why this recipe hits this mode:** `data_0218dbc8` is used as a load base for the list head at `+0x910`; verify.py wildcards the pool LDR for this symbol, so a wrong address passes instruction-byte checks but breaks `ninja report`/`ninja sha1`.

**Pool words to verify against orig .s:**
- `data_0218dbc8` — singleton base for the node list; loaded to reach the head pointer at `&data_0218dbc8 + 0x910`

**Verify gate sequence:**
1. verify.py src/main/func_0200fdc0.c func_0200fdc0 [--cc all] — instruction bytes (pool LDR wildcarded)
2. ninja report — resolves pool word for data_0218dbc8; catches Mode A address mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Confirm `data_0218dbc8` is at address `0x0218dbc8` against the .s pool word; note this appears to be `data_0218cbc8 + 0x1000` from the sibling function `0200fbd4`
- The packed field shift `n->packed >> 12` must use an `unsigned int` field and unsigned right-shift (`>> 12u` or declare the field `unsigned int`); a signed shift would emit `asr #12` instead of `lsr #12`
- Write the key load as `(unsigned int)arg[2]` (word at index 2 = byte offset 8) to match the `ldr` at `[arg, #8]` without sign extension
- Keep the inner loop as `do { ... } while (n != 0)` to match the .s branch structure; a `while`-headed loop may add an extra branch before the first iteration
- The node layout must be `{next@0, pad@4, packed@8}` — if the field at `@0xc` in the comment is the right offset, adjust the struct to `{next@0, pad@4, pad2@8, packed@0xc}` and re-verify with objdiff
---
