---
# func_0201cfa0 (main, HIGH class A, ?B)

**Recipe:** Guard chain + compare-to-bool (>=1 via movge/movlt)
**Risk:** The >=1 must lower to cmp#1/movge/movlt not a branch; structurally clean otherwise.
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible diff if `>= 1` does not lower to `cmp r0,#1; movge r0,#1; movlt r0,#0` conditional-move pair

**Why this recipe hits this mode:** The tail expression `func_0201cff8() >= 1` must emit a conditional-move pair (`movge`/`movlt`) rather than a branch-and-store; if mwcc emits a branch it produces extra instructions and visible byte differences.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. `verify.py src/main/func_0201cfa0.c func_0201cfa0 [--cc all]` — instruction diff is visible directly
2. `ninja sha1` — final gate

## Reshape hints

- Write the tail as `return func_0201cff8() >= 1;` — not `return func_0201cff8() > 0;` — so mwcc emits `cmp r0,#1` (not `cmp r0,#0`) followed by `movge`/`movlt`.
- Keep the two early-exit guards as separate `if` statements with explicit `return 0;` and `return 1;` so the compiler does not merge them into a single branch tree.
- High-confidence recipe: try as-is first; the only known risk is the `>=1` lowering form, which is directly testable in objdiff.
- If mwcc emits a branch instead of the movge/movlt pair for `>= 1`, add an explicit `int r = func_0201cff8(); return r >= 1;` to bind the result and force the compare-to-bool form.
- No callee-saved registers or locals needed — this should be a leaf-like three-call chain with no frame beyond lr.
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0202046c` | main TU call |
| `bl` | `func_0201cfe0` | main TU call |
| `bl` | `func_0201cff8` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

