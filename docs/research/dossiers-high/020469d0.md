# func_020469d0 (main, HIGH class B, 76B)

**Recipe:** global-ptr null guard, 1<<key bit-test (field-first ands), tail call
**Risk:** g bound once (r2) then reloaded as +0x644 base; field & (1<<key) needs field as first ands operand; data_0219daec typed void* (struct ptr); 0x644 offset.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
- data_0219daec

**Verify gate:**
1. python tools/verify.py src/main/func_020469d0.c func_020469d0 --cc all (catches diff directly)
2. ninja sha1 (final gate)

## Reshape hints

- Run verify.py --cc all; instruction byte diffs tell you what to reshape
- For movne/moveq: try inverting the condition or swapping then/else
- For and #1 wall: use (x << 31u) >> 31 to force lsl;lsr pair
- For jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: docs/research/c-match-prep/020469d0.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
