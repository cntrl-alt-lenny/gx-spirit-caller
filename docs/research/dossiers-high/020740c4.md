# func_020740c4 (main, HIGH class A, 96B)

**Recipe:** init store-order; calls in source order; addr-passed globals
**Risk:** ef24/ef28 share the zero value; their two adjacent stores could reorder
**Confidence:** high

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (verify.py catches directly)

**Verify gate:**
1. `python tools/verify.py src/main/func_020740c4.c func_020740c4 --cc all` (diffs visible directly)
2. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219f0b8` | data symbol |
| `_LIT1` | `data_0219f178` | data symbol |
| `_LIT2` | `data_0219ef20` | data symbol |
| `_LIT3` | `data_0219ef24` | data symbol |
| `_LIT4` | `data_0219ef28` | data symbol |

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02074134` | main TU call |
| `bl` | `func_02091af4` | main TU call |
| `bl` | `func_02091b5c` | main TU call |
| `bl` | `func_02074498` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

## Reshape hints

- Run `verify.py --cc all`; instruction byte diffs tell you what to reshape
- movne/moveq: try inverting the condition or swapping then/else
- `and #1` wall: use `(x << 31u) >> 31` to force lsl;lsr pair
- jump-table: match case ordering to the orig .s branch table

## Reference

- Prep: `docs/research/c-match-prep/020740c4.c`
- Gap taxonomy: `docs/research/objdiff-sha1-gap/gap-taxonomy.md`
