# func_0203db70 (main, HIGH class B, 160B)

**Recipe:** dense range switch -> addls pc jump table; calls per case; tail return 0
**Risk:** May collapse missing cases differently; case-11 vs case-12 body order (early-return) may still swap; -1 via mvn r0,#0; jump-table arm layout must match.

## SHA1 pitfall

**Primary mode:** Mode E - Visible instruction byte diff (verify.py catches directly)

**Pool words to verify:**
(none in metadata — check docs/research/c-match-prep/0203db70.c)

**Verify gate:**
1. python tools/verify.py src/main/func_0203db70.c func_0203db70 --cc all
2. ninja sha1 (final)

## Reshape hints

- Run verify.py --cc all; diffs tell you what to reshape
- movne/moveq: try inverting condition or swapping then/else
- and #1 wall: use (x << 31u) >> 31
- jump-table: match case ordering to orig .s

## Reference

- Prep: docs/research/c-match-prep/0203db70.c
- Gap taxonomy: docs/research/objdiff-sha1-gap/gap-taxonomy.md
