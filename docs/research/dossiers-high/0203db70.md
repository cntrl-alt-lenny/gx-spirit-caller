---
# func_0203db70 (main, HIGH class B, 160B)

**Recipe:** dense range switch -> addls pc jump table; calls per case; tail return 0
**Risk:** May collapse missing cases differently; case-11 vs case-12 body order (early-return) may still swap; -1 via mvn r0,#0; jump-table arm layout must match.
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (jump-table arm layout / case-body ordering), verify.py catches directly

**Why this recipe hits this mode:** The ARM `addls pc, pc, r0, lsl #2` jump table encodes each case arm as a literal PC-relative word; the arm order is determined by source case ordering, so any swap of case-11 and case-12 produces different bytes that verify.py flags immediately.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_0203db70.c func_0203db70 [--cc all] — instruction diff is visible directly
2. ninja sha1 — final gate

## Reshape hints

- Source case order must be `0, 1, 3, 6, 9, 12, 11` to match the `.text` layout; case-12 body appears before case-11's early-return body in the original — list case-12 before case-11 in source
- The -1 return in case-11 must produce `mvn r0,#0`; use `return -1` as a plain int literal, not a cast or unsigned form
- Missing cases 2, 4, 5, 7, 8, 10 must fall through to `return 0` — ensure they share the default return arm, not individual stubs
- Verify the dense jump table spans exactly 13 arms (range 0..12) via `addls`; if mwcc emits a chain of `cmp;beq` instead, the switch range or case density needs adjustment
- All callees are direct BL (no BLX, no indirect pointer); no pool-word risk on targets
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_0207b5f8` | main TU call |
| `bl` | `func_0207c20c` | main TU call |
| `bl` | `func_0207bec8` | main TU call |
| `bl` | `func_0207bc20` | main TU call |
| `bl` | `func_0207b888` | main TU call |
| `bl` | `func_0207b62c` | main TU call |
| `bl` | `func_0203c85c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

