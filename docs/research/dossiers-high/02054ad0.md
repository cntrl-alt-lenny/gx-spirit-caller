---
# func_02054ad0 (main, HIGH class B, 48B)

**Recipe:** ptr-diff early-eq-return then divmod-by-name, add base back
**Risk:** BUG: r0 into func_020b3870 comes from func_02054b1c's return (r0 preserved), not a dummy — should be func_020b3870(func_02054b1c(), d)+r5; placeholder name will not compile, fix to single call.
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode B — hidden callee mismatch; func_020b3870 receives its first arg from the return value of func_02054b1c (r0 preserved across the call), so the recipe's placeholder `func_02054b1c_dummy()` does not compile and the correct single-call pattern must be used; BL relocs are wildcarded by verify.py, so only ninja report catches a wrong call target

**Secondary mode:** Mode E — visible instruction diff; the ptr-diff and pointer-add arithmetic around `(int)r5` may emit different width casts depending on pointer vs int promotion rules

**Why this recipe hits this mode:** The prep file contains a known bug: `func_02054b1c_dummy()` is a placeholder that will not compile. The correct form is `func_020b3870(func_02054b1c(), d) + (int)r5` — a single chained call where r0 from func_02054b1c flows directly into r0 of func_020b3870. Because verify.py wildcards BL reloc targets, a wrong callee address would pass instruction-byte checking but fail at ninja report/sha1.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02054ad0.c func_02054ad0 [--cc all] — instruction bytes
2. ninja report — resolves BL targets; catches Mode B callee mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Fix the prep bug first: replace the two-call pattern with `return func_020b3870(func_02054b1c(), d) + (int)r5;` — this keeps r0 from func_02054b1c alive into the second BL without an intervening move
- The early-return path `if (d == 0) return (int)r5` must use an explicit `(int)` cast on the char pointer to avoid a sign-extension instruction that the orig may not emit
- `d = r1 - r5` is a pointer difference of two char pointers; declare both params as `char *` so MWCC emits a straight sub without a shift
- Cross-check the BL addresses for func_02054b1c and func_020b3870 against the orig .s BLX instructions — run ninja report after first compile to verify both callee slots resolve correctly
- If the addition `+ (int)r5` produces an extra zero-extend or sign-extend instruction, try `+ (int)(unsigned int)r5` or reorder the expression to match the orig add instruction's operand order
---
