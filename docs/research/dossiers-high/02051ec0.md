---
# func_02051ec0 (main, HIGH class A, 92B)

**Recipe:** guard chain with deref + call, RELOAD; bool-invert movs; sp3 routing
**Risk:** field 0x200 word(ptr); func_02051618 result inverted to bool (eq->1); sp3 routing; guard order
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff from movne/moveq swap on bool-invert result

**Why this recipe hits this mode:** The final `== 0` bool-invert on `func_02051618`'s return value produces a `moveq r0,#1` / `movne r0,#0` pair; if the ternary polarity is wrong, the compiler emits the pair swapped — a visible byte diff that verify.py catches directly.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02051ec0.c func_02051ec0 [--cc all] — instruction bytes (movne/moveq order visible)
2. ninja sha1 — final gate

## Reshape hints

- The bool-invert `== 0` must emit `moveq` (true branch) first; if objdiff shows them swapped, try `func_02051618(func_020498f0()->f200) != 0 ? 0 : 1` to flip the cmp polarity
- The guard chain calls `func_020498f0()` three times (RELOAD pattern) — do not cache into a local variable or the reload sequence changes
- Guard order must be: null-ptr check first, then field f200 zero check, then `func_02045280()` — do not reorder
- sp3 routing: file must be named `.legacy_sp3.c` to pick the `1.2/sp3` compiler tier (sub sp,#4 prologue + pop pc epilogue)
- If a near-miss persists on the eq/ne condition, swap `== 0` to `!= 0` on the final return and adjust the returned integer accordingly
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020498f0` | main TU call |
| `bl` | `func_02045280` | main TU call |
| `bl` | `func_02051618` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

