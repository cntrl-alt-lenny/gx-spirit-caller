---
# func_02052c58 (main, HIGH class B, 132B)

**Recipe:** discriminant if-chain re-calling probe, three tail calls
**Risk:** func_02053488 declared returning int but asm shows void tail; r5[9] is [r5,#0x24]. buf offsets 0x0/0xd from stack frame 0x24.
**Confidence:** low

## SHA1 pitfall

**Primary mode:** Mode B (HIDDEN) — BL/BLX targets to extern callees wildcarded by verify.py reloc, only ninja report catches mismatches

**Why this recipe hits this mode:** All three tail-call targets (`func_02053650`, `func_02053488`, `func_0205337c`) are BL relocations — if any callee address is wrong, verify.py wildcards the BL bytes and the mismatch is invisible until `ninja report` resolves them.

**Pool words to verify against orig .s:**
No named-symbol pool words — raw hex constants only (compared directly by verify.py).

**Verify gate sequence:**
1. verify.py src/main/func_02052c58.c func_02052c58 [--cc all] [--module main] — instruction bytes
2. ninja report — resolves BL reloc targets for func_02053650, func_02053488, func_0205337c; catches Mode B mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- `func_02053488` shows a void tail in the asm but is declared `void` in the prep — if the return path shape is wrong, change `return func_02053488(...)` to `func_02053488(...); return 0;` so the compiler does not expect a return value register
- `r5[9]` corresponds to `[r5, #0x24]` — the first parameter must be `int *r5` so that `r5[9]` compiles to a word-offset of 0x24 (9*4)
- Stack frame is 0x24 bytes; buf offsets must be `buf + 0` and `buf + 0xd` exactly as written — do not adjust
- `func_02053688(r4)` is called three times (RELOAD pattern) — do not cache its result into a local variable
- Confidence is low; if discriminant check order or buf offset layout does not match after the first compile, cross-check each branch against the orig .s arm-by-arm
---

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_02053688` | main TU call |
| `bl` | `func_02053650` | main TU call |
| `bl` | `func_0205370c` | main TU call |
| `bl` | `func_02053488` | main TU call |
| `bl` | `func_0205337c` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

