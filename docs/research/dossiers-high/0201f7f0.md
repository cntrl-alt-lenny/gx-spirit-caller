---
# func_0201f7f0 (main, HIGH class A, 46B)

**Recipe:** store-order field init, guard, branch tail
**Risk:** arg order (r0=a,r1=b,r2=c) and movs vs mov on the f30!=1 path; many struct offsets guessed; store scheduling must stay in asm order
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode E — visible instruction byte diff (store scheduling, movs vs mov, arg order)
**Secondary mode:** Mode A — named global data_02191f40 used as pool base for all stores; wrong pool-word value is wildcarded by verify.py, only ninja report catches it

**Why this recipe hits this mode:** Nine separate stores into data_02191f40 fields at scattered offsets must be emitted in precisely the asm-mandated order; MWCC may reorder adjacent stores to the same base, and the movs vs mov distinction on the f30!=1 path produces a visible byte difference that verify.py will catch.

**Pool words to verify against orig .s:**
- `data_02191f40` — global struct base; all nine field stores target this base at offsets +0x28, +0x2, +0x84, +0x0, +0x8c, +0x8a, +0x88, +0x30(?), +others

**Verify gate sequence:**
1. verify.py src/main/func_0201f7f0.c func_0201f7f0 [--cc all] — instruction bytes (store order and movs/mov visible)
2. ninja report — resolves pool word data_02191f40; catches Mode A mismatches before sha1
3. ninja sha1 — final gate

## Reshape hints

- Write the nine stores in exactly the order listed in the .s: f10 first, then f3c, f1c, f4, fa=1, f2c, fa0, fa4, f20 — the recipe's risk note gives this exact sequence; do not let the compiler reorder
- To lock store order, introduce a volatile pointer to the struct or write each store through a separate explicit assignment with no intervening reads that could be folded
- The `if (data_02191f40.f30 != 1) func_02093bfc()` guard at the top has no else-return; confirm the .s falls through (call then continues) rather than returning early — this affects whether MWCC emits movs or mov for the comparison result
- Check each struct offset against the .s LDR/STR offsets; field pad calculations for f28 (0x28), f2 (0x2), f84 (0x84), f8c (0x8c), f8a (0x8a), f88 (0x88) are guessed and a single wrong size collapses all subsequent offsets
- Confirm arg registers: a=r0, b=r1, c=r2 at entry; if the .s shows a different mapping for the stacked arg, adjust the function signature accordingly
---
