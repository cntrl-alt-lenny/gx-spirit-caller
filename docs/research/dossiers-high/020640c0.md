# func_020640c0 (main, HIGH class A, 152B)

**Recipe:** guard-chain; 1-arg thunk; two stack buffers; state=3 on success
**Risk:** loc0/loc2 stack ordering (sp+0 vs sp+0x20) may flip; func_02061150 arity guessed 1-arg; offsets guessed
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — wrong pool word value (HIDDEN from verify.py; only ninja report catches)

**Verify gate:**
1. `python tools/verify.py src/main/func_020640c0.c func_020640c0 --cc all` (instruction bytes)
2. `ninja report` (resolves pool/BL words; catches Mode A mismatch)
3. `ninja sha1` (final gate)

## GROUND TRUTH (from .s)

**Pool words:** none (no literal pool in this function).

**BL/BLX callees (in call order, unique):**

| instr | target | notes |
|-------|--------|-------|
| `bl` | `func_020643d8` | main TU call |
| `bl` | `func_02061018` | main TU call |
| `bl` | `func_02061150` | main TU call |
| `bl` | `func_02062cfc` | main TU call |

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

