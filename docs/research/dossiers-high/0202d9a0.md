---
# func_0202d9a0 (main, HIGH class A, 88B)

**Recipe:** bind-once counter increment + wrap if-assign
**Risk:** movge/strge wrap may emit as ternary store; field offsets guessed; +0x60 limit add may CSE differently
**Confidence:** med

## SHA1 pitfall

**Primary mode:** Mode A — named pool global `data_0219ad48`; address wildcarded by verify.py, only ninja report catches a wrong value; field offsets are guessed and a wrong offset in the struct would silently pass verify.py

**Secondary mode:** Mode E — visible instruction byte diff if `movge r1,#0; strge r1,[r0,#0x10]` wraps via a branch instead of conditional-move/store pair; verify.py catches it

**Why this recipe hits this mode:** `data_0219ad48` is accessed across multiple fields (`+0x00`, `+0x08`, `+0x10`, `+0x24`, `+0x64`) via a single pool `.word` whose address verify.py wildcards; a wrong symbol address passes instruction-byte verification but breaks ninja report and sha1. Additionally, the wrap guard `if (t >= p->field_08 + 0x60)` should compile to a `cmp/movge/strge` pair, not a taken branch.

**Pool words to verify against orig .s:**
- `data_0219ad48` — struct base; `.word` pool entry held in r0 across all field reads and writes (bind-once leaf); wrong address is hidden from verify.py

**Verify gate sequence:**
1. verify.py src/main/func_0202d9a0.c func_0202d9a0 [--cc all] — instruction bytes (catches movge/strge vs branch and CSE form)
2. ninja report — resolves pool word for data_0219ad48; catches Mode A mismatch before sha1
3. ninja sha1 — final gate

## Reshape hints

- Bind the global to a single local `C_0202d9a0 *p = &data_0219ad48` and access all fields through `p` to reproduce the bind-once register pattern (single pool reload, all accesses via r0)
- For the wrap guard, use a post-increment temp to match `movge/strge`: `int t = p->field_10 + 1; p->field_10 = t; if (t >= p->field_08 + 0x60) p->field_10 = 0;` — this lets mwcc emit `movge` + `strge` rather than a branch
- Do not CSE `p->field_08 + 0x60` separately; let mwcc compute it inline from the loaded `field_08` register to match the `.s` `add r1,r1,#0x60; cmp r2,r1`
- Verify each field offset (`+0x08`, `+0x10`, `+0x24`, `+0x64`) against the .s LDR/STR immediates — they are guessed in the prep, so a mismatch here causes a silent Mode A failure
- Cross-check `data_0219ad48` pool `.word` value against `symbols.txt` before ninja report to rule out Mode A early
---

## GROUND TRUTH (from .s)

**Pool words (literal pool, in order):**

| label | value | type |
|-------|-------|------|
| `_LIT0` | `data_0219ad48` | data symbol |

**BL/BLX callees:** none (leaf function).

> **Mode-C reminder:** never emit `static const` arrays or struct literals — they generate a `.rodata` section that breaks the link silently. Use `extern data_XXXX` references instead.

