[//]: # (markdownlint-disable MD013 MD041)

# Dispatch log — which machine ran which round

One row per brain round, appended **in the round's seeding/doc PR** at dispatch
time. Exists because machine-locality has been the campaign's most persistent
unfixed failure class: worktrees misread as evidence about a lane (the
PR #1504 correction), kickoffs pasted with the wrong machine's paths,
transcripts unreadable
from the other machine (round 0822's review), a machine-local git object making
a test suite falsely green (#1520), and a location guard asserting a directory
the session never navigated to (round 0822's Codex re-dispatch). A future brain
on either machine reads this table and knows where round N ran, where its
transcripts live, and which worktrees are evidence — instead of rediscovering it
painfully.

Columns: **Host** is where the brain session ran AND dispatched from (if those
ever differ, say so in Notes). **Transcripts** is where AGENTS.md control 12's
audit can actually be performed. Worktree sets: Windows =
`C:/Users/leona/Dev/gx-spirit-caller/{decomper, scaffolder, kb-map, kb-types}`;
Mac = `~/Dev/spirit-caller/{decomper, scaffolder, codex-decomper-queue,
codex-scaffolder-queue}`.

| Round | Date | Host | Brain model | Lanes dispatched (lane → item) | Transcripts | Notes |
|---|---|---|---|---|---|---|
| 0817 | 2026-08-17 | Windows PC | Opus 5 | CC-D → `cm-main-wall-filtered-sweep-1` · CC-S → `cm-restock-carve-9` · CX-D → `q-park-class-normalisation` · CX-S → `q-ledger-ship-coverage` | Windows | Merged #1499–#1502, #1503. |
| 0818 | 2026-08-18 | Mac M1 | Fable 5 | CC-D → `cm-main-exploit-drain-1` (others mid-flight or per 0818b) | Mac | Merged #1506–#1508; held #1505. |
| 0818b | 2026-08-18 | Mac M1 | Fable 5 | CX-D / CX-S queues deepened to 3 items each | Mac | Merged #1511, #1512; partial round, CC-D still draining. |
| 0819 | 2026-08-19 | Mac M1 | Fable 5 | CC-S → `cm-toolchain-adopt-2` · CX-S → 3 items (`q-kickoff-lint-canary-check`, `q-pool-freshness-tool`, `q-unittest-required-evidence`) | Mac | Merged #1513–#1519, #1521, #1523. Seed commit `ec8148a9b`. |
| 0822 | 2026-08-22 | Windows PC | Opus 5 | CC-D → `cm-main-exploit-drain-2` · CC-S → `cm-restock-carve-10` · CX-D → 3 items (`q-wall-citation-backfill`, `q-batch-sha1-stale-s`, `q-fastmatch-error-masking`) · CX-S → 3 items (`q-kickoff-lint-sha-brittleness`, `q-pool-freshness-tool`, `q-unittest-required-evidence`) | Windows | Merged #1522, #1524, #1525, #1527; held #1520. **Incident:** first Codex kickoffs asserted the worktree without navigating to it — both lanes correctly STOPped `WRONG WORKTREE`; re-dispatched with `Set-Location` first. Transcript audit of round 0819 was impossible from this host (recorded in state.md). |
| 0822b | 2026-08-22 | Windows PC | Fable 5 | — (no lane dispatch; all four mid-flight) | Windows | Owner-approved hardening package: `unittest` made a required check (#1531 + ruleset), this log created, post-small-pool strategy memo, 3 queue seeds, `q-unittest-required-evidence` parked as superseded. |
| 0822c | 2026-08-22 | Windows PC | Opus 5 | CC-D → `cm-main-boundary-rerun` · CC-S → `cm-restock-carve-11`, `cm-progress-dashboard` · CX-D → `q-worktree-gc` · CX-S → `q-pool-freshness-tool` (re-take), `q-make-kickoff-generator` | Windows | Merged #1526, #1528, #1529, #1530, #1532, #1535, #1536; held #1534; closed #1520 as superseded. **First round since 0817 where control 12's transcript audit was fully executable** — all four lanes ran on this host and all four were read. #1536's 0/40 boundary result disclosed as effort-confounded by the lane itself; re-run seeded rather than acted on. |
| 0824 | 2026-08-24 | Windows PC | Opus 5 | CC-D → `cm-main-band-followthrough` · CC-S → `cm-restock-carve-12` (rewritten as the group verifier) · CX-D → `q-ledger-chronology`, `q-remaining-opportunity-census` · CX-S → `q-make-kickoff-generator` (commit the paused work FIRST) | Windows | Merged #1542, #1544-#1553 (11 PRs); closed #1534 as superseded. **The 193-256 B band returned 0/20 at matched effort — the small-code frontier is closed on clean evidence and Outcome B fires.** Codex Decomper cleared 7/7. Two brain findings against merged work: #1546's contradiction audit reads row order as chronology (2 of 3 false positives), and the Codex Scaffolder's paused generator sat uncommitted in kb-types, one `reset --hard` from loss. |
| 0824c | 2026-08-24 | Windows PC | Opus 5 | CC-D → `cm-main-band-finish` · CC-S → next data item · CX-D → re-seed · CX-S → `q-wine-link-concurrency` (deferred, retry on an idle machine) | Windows | Merged #1557-#1565 (nine PRs). **Composition into the data pool is DEAD (0/575)**; 257-320 B is NOT closed — 2/9 partial with parked median 55.2% vs 11.2% at 193-256 B and zero dead drafts, so the sample gets finished rather than spent. First round with the `attempts` column populated. Brain fixes at merge: the census's stale `bl`-tool caveat, and #1561's red ruff check. |
| 0825 | 2026-08-25 | Windows PC | Opus 5 | all four QUEUE-EMPTY; re-seeded pending an owner ROI call on the 257-320 B band | Windows | Merged #1569-#1574 (six PRs). **Code frontier now fully mapped**: 257-320 B is MARGINAL at 4/20 = 20.0% on a complete sample (263 candidates / 75,980 B behind it); 513-1023 B returned 0/15; every other band closed or exhausted. bl/blx dispatch-pool counting consolidated into `scan()` and brain-verified agreeing with `pool_freshness.py` at three caps. Wine-link concurrency measured cleanly (4-way deterministic, ~3.1x throughput) but the lock was never active on Windows — brain corrected its own earlier framing. |
| 0825b | 2026-08-25 | Windows PC | Opus 5 | all four QUEUE-EMPTY; re-seed pending the owner's direction call | Windows | Merged #1577-#1580. **321-376 B CLOSED at 1/15**, leaving 257-320 B's 20.0% as the only live code pocket. **#1580 found a Windows-only `load_module_sections` bug** that hid every overlay/ITCM/DTCM data symbol since carve-10 — reachable pool 187,760 B → 407,506 B once fixed; brain reproduced it (26 of 27 map keys were raw backslash paths). Brain supplied the census's missing byte-sim half from a worktree with `extract/`: **~198,232 B of the port backlog is byte-identical**. |
| 0827 | 2026-08-27 | Windows PC | Opus 5 | Decomper -> `cm-port-drain-usa` (shipped) - Scaffolder -> `cm-port-drain-jpn` (produced nothing) | **NONE - see Notes** | Merged #1584: **283 USA ports, natural-C 11.84% -> 13.43%**, the largest regional jump the ports have produced; brain re-derived 283/283/283 from the diff rather than accepting the body. **Control 12 was NOT executable** - both lanes ran outside Claude Code and Codex (owner trialling other models), so no transcript exists on any host; findings are git-derived only. **Incident:** the brain scheduled two toolchain-bound lanes onto one serialising machine. The USA lane held mwcc 18:08-20:08; the JPN lane's kickoff required a full 3-region gate as its next step and forbade concurrent builds, so it correctly stalled after one accepted port. `port_harvest.py` also has no region flag (`REGIONS = ("usa", "jpn")`), so the split was wrong at the root. New standing rule: one toolchain-bound lane per machine per round. |
| 0828 | 2026-08-28 | Windows PC | Opus 5 | Decomper -> `q-port-refusal-taxonomy` - Scaffolder -> `cm-port-drain-jpn` | **NONE - see Notes** | Merged #1586, #1587. **283 JPN ports at 43.3% (USA was 43.5%), JPN natural-C 11.82% -> 13.41%** - USA and JPN now level. The one-toolchain-lane rule from 0827 worked: both lanes delivered, and the build-free lane produced the better PR. **#1586 found a ~15 KB unlock across both regions** - `resolve_symbol` never checks the target region's own `symbols.txt` for an identical committed name before fingerprinting (brain verified `OS_DisableIrq` present in all three regions at different addresses). Brain mutation-tested the new suite per control 7; one test went red correctly. **Control 12 NOT executable** for the second consecutive round - both lanes ran outside Claude Code and Codex. **Incident:** the brain's own instruction to preserve a stranded `.c` beside its still-routed `.s` collided in ninja and silently blocked the entire JPN harvest region on the first run. Lanes move to Claude Sonnet 5; kickoffs re-tuned to `xhigh` effort with swarm permitted in the build-free lane only. |
| 0828b | 2026-08-28 | Windows PC | Opus 5 | Decomper -> `q-fingerprint-promotion-evidence` - Scaffolder -> `cm-port-exact-name-unlock` | **NONE - see Notes** | Merged PR #1589, PR #1590. **119 ports banked; USA and JPN both 13.43/13.41% -> 13.79%** (identical port sets, 328,816 B each). **The two lanes independently corroborated each other**: PR #1589 measured that the fingerprinter's `Copy32` pick is wrong, PR #1590 fixed it by reading the authoritative source; brain verified `Copy32` at 0x020943e0 vs the picked unnamed placeholder `func_020943f8` of identical size. **PR #1589's `verified_neighbor` covers 98.7% of the ambiguous population at 100% accuracy (2,960/2,960)** and predicts all 4 known-wrong rows - the 95.3% MEDIUM wall may not be a wall. Brain ran control 7 on both new suites (2/26 and 2/17 red on mutation). **Control 12 NOT executable, third consecutive round.** **Process:** the derived-dashboard self-reference cost two lane gate runs plus a cross-lane merge conflict - fix seeded. First recon caught the scaffolder mid-flight; check for live toolchain processes before concluding a lane produced nothing. |
| 0829 | 2026-08-29 | Windows PC | Opus 5 | Decomper -> `q-collision-pair-audit` + `q-derived-artifact-selfreference` - Scaffolder -> `cm-verified-neighbor-tranche` | **NONE - see Notes** | Merged PR #1592, PR #1593, PR #1594. **The falsification test came back negative: `verified_neighbor` survived 95 ports across 9 consecutive gated batches, zero failures.** USA 13.79% -> **14.15%**, JPN -> **14.07%**. PR #1594 re-derived PR #1589's measurement against the current larger tree before running, and made the prediction arithmetic-only with an existence check at the call site so it cannot fabricate a resolution. **PR #1592: all 7 unchecked collision pairs CORRECT** - the 1-in-8 known-wrong rate does not generalise; `reloc_sig` drops the relocation target address by design, which is why same-size siblings collide at all. Bonus: brief-435's prose has two symbol names swapped (brain confirmed; sizes 0x50 vs 0x54, not even same-size siblings). **PR #1593 RESOLVED the dashboard SHA self-reference** that had cost three rounds, demonstrated pre-fix FAIL / post-fix PASS. **Control 12 NOT executable, fourth consecutive round.** **Watch items:** PR #1594's prose miscounted its own batches (8 vs the actual 9) - verify counts not just totals; and it kept two copies of `verified_neighbor_signal()`, the same shape as the `FLOOR_RANK` drift PR #1590 had to fix. |
| 0830 | 2026-08-30 | Windows PC | Opus 5 | Decomper -> `q-eur-next-frontier` - Scaffolder -> `cm-verified-neighbor-drain` | **NONE - see Notes** | Merged PR #1596, PR #1597. **THE PORT CAMPAIGN IS DONE**: 489 final ports (USA 238, JPN 251) across 26 gated batches, backlog 524 -> 35 rows, USA and JPN both **15.85%**. **PR #1596 corrected the brain twice, both load-bearing.** (1) "EUR stuck at 17.27%" is a `.text`-only claim - `progress.py`'s natural-C metric never scans `.data`/`.bss`/`.rodata` (brain confirmed in source), so the 407,506 B data pool moves a separate metric and carve work cannot move the headline. (2) The "closed" code bands were ATTEMPT counts, not populations - brain re-derived from `attempts.tsv`: 193-256 B has 267 attempts, 321-376 B has 70, and **the three largest bands hold 1,211,260 B (87% of the code pool) against 83 attempts total, with `>=1024 B` attempted exactly ONCE**. 377-512 B appears in no prior band recap. PR #1596 left those ship rates blank rather than extrapolate, and also corrected ITCM (zero data symbols; the hidden pool was 100% overlay data). **Control 12 NOT executable, fifth consecutive round.** Next round pivots to the large bands: a pre-registered n=20 probe at 377-512 B, plus a build-free test of whether `m2c_feed.find_object` (globs only `_dsd_gap@*.o`) is structurally blind to the ~10,000 per-source delink objects. |
| 0831 | 2026-08-31 | Windows PC | Opus 5 | Decomper -> `q-large-band-reachability` - Scaffolder -> `cm-377-512-probe` | **NONE - see Notes** | Merged PR #1599, PR #1600. **Both results NEGATIVE and both reported honestly.** 377-512 B **CLOSED at 1/20 = 5.0%** on a pre-registered threshold nobody moved (recorded as BR-7; corrects BR-6's "fully characterised" claim). The frontier is now monotone: larger is harder, and three 80%+ near-misses were diagnosed as compiler-internal register-allocation residuals, not under-effort. **Two new levers:** `volatile` on a raw MMIO pointer forces mwcc's two-load RMW (20.8% -> 81.1% in one change), and shift-pair over mask simplification closed a 6.7% attempt to 100%. **The brain's tooling hypothesis is DEAD** - coverage RISES with size, and zero of the 83 large-band attempts used gap objects, so the low count is a methodology choice. Brain verified the canary (one row >=1024 B, `0x02246a50`/ov002/1036 B/brief-650, produced by hand). **Brain finding:** PR #1599's coverage figure is BUILD-STATE-DEPENDENT - the same tool returned ~0% on the integration tree, whose gap objects held only 55 distinct functions; the conclusion survives (it rests on the ledger cross-check) but the number needs dating. **Gate caught real drift:** three SHA1 PASS but 2 dashboard tests failed on merge content drift; regenerated, then 3,540 passed - and the `tee` wrapper reported exit 0 while the gate had FAILED. **Control 12 NOT executable, sixth consecutive round.** |
| 0901 | 2026-09-01 | Windows PC | Opus 5 | Decomper -> `q-find-object-persource` - Scaffolder -> `cm-257-320-drain` | **NONE - see Notes** | Merged PR #1602, PR #1603. EUR 17.29% -> **17.32%**. **3/17 = 17.6%** at 257-320 B, inside the noise of the 20.0% baseline; 259 candidates remain (BR-8). **Shift-pair lever CONFIRMED transferring on a direct A/B** - literal `(u32)(x << 0x1F) >> 0x1F` 71.2% vs `x & 1` 7.7% on the same candidate; the `volatile` lever was honestly reported as **not exercised**. ⚠️ **Denominator now structural:** 6 of 23 candidates were screened out on sight as P-20 wall members, giving 17.6% attempted-rate vs 13.0% pool-rate - **wall membership now gates which candidates are attempted at all, and a screened-out candidate leaves no attempt row**, so it is invisible to every computed rate. PR #1602 removed the `find_object()` tax (per-source fallback that disasm-verifies the header, not just the path) and **confirmed the brain's build-state finding directly**: lane tree 2,083 gap objects / 2,881 functions -> 45-68%, brain tree 754 / 55 -> ~0%; the tool now prints its own build state. **Brain miss caught by the lane:** `dashboard.md` was stale on main with `line count differs` - PR #1593's tolerance covers a rewritten trailing SHA but not a NEW ROW, and the round-0831 brain PR is what added one. **Control 12 NOT executable, seventh consecutive round.** |
| 0902 | 2026-09-02 | Windows PC | Opus 5 | Decomper -> `q-wall-overblock-audit` - Scaffolder -> `cm-257-320-drain-2` | **NONE - see Notes** | Merged PR #1605, PR #1606. EUR 17.32% -> **17.34%**. **The ledger blind spot is TOTAL:** of 1,822 rows, **zero** `not-attempted` rows are tagged to any formal P-NN wall (brain verified: the 31 that exist carry `unknown`/`C-31`/`permanent-header`/`C-23-C-36`/`complexity`), so on-sight screening leaves no trace for ANY wall. **P-20 is the only wall with independent 3+-round confirmation** (55 members, 6 convergent rounds, fresh re-scan found zero further matches) - so the screening that prompted this audit was justified. **Two defects found, neither acted on:** `generate_walls_index.py`'s heading regex bleeds brackets between walls (brain confirmed P-20 reports 30 vs re-count 55; P-49 reports 9 vs 1), and 8 walls carry stale members - **P-11 has 10 of 16 (62.5%) already shipped**. Drain: **1/14 attempted, 1/27 pool**, both reported; **zero of 27 needed `--obj`** (was 20/20), confirming PR #1602 in the field; corrected the brain's stale 259 to 246. All-time band rate is **8/51 = 15.7% attempted** - still marginal; the lower pool-rate reflects bounded P-20 membership, not band difficulty. Next round the drain **writes a `not-attempted` ledger row when it screens**, closing the blind spot. **Control 12 NOT executable, eighth consecutive round.** |

| 0903 | 2026-09-03 | Windows PC | Opus 5 | Decomper -> `q-wall-catalog-repair` - Scaffolder -> `cm-257-320-drain-3` | **NONE - see Notes** | Merged PR #1608, PR #1609, plus brain PRs #1610-#1614. EUR 17.34% -> **17.36%**. **The ledger screen-row change landed and worked**: `attempts.tsv` now carries a `result=not-attempted` row when a candidate is screened (1,853 rows, 0 errors), and **P-20 screens fell 12 -> 0**, giving the first tranche where attempted-rate (12.5%) and pool-rate (11.8%) nearly converge. PR #1608 fixed the wall-index heading-boundary regex, surfaced a wall nobody had asked about (C-39), and corrected seven stale counts - **P-11 is 13/16 shipped, not the audit's own 10/16**; P-50 went 4 -> 21. **Round written up retroactively during 0904.** ⚠️ **Process defect:** this round's own bookkeeping never happened - no Round 0903 heading, no `main-sha` bump, no dispatch row - and **both guards that exist to catch it are inert**: `queue_state_drift.py` counts `--merges` under a repo that has squash-merged exclusively since 2026-08-25, and `check_dispatch_log.py` keys off the `**Last updated:**` paragraph alone. Neither is wired into CI. Seeded as `q-handoff-guard-repair`. **Control 12 NOT executable, ninth consecutive round.** |
| 0904 | 2026-09-04 | Windows PC | Opus 5 | Decomper -> `q-ci-test-visibility` - Scaffolder -> `cm-257-320-drain-4` | Claude Code Decomper - Claude Code Scaffolder | Merged PR #1616; **PR #1615 HELD open on a red required check.** EUR 17.36% -> **17.37%**. ⚠️ **THE BAND IS TWO POOLS, and this decides the next dispatch.** The drain returned 1/17 = 5.9% and honestly reported it as a divergence, naming the cause in prose; the brain quantified it from the ledger across all six 257-320 B briefs: **ov002 ships 6/30 = 20.0% with 132 candidates left, `main` ships 1/34 = 2.9% with 56 left**. Drain-4 spent 16 of 17 attempts on the 2.9% sub-pool. Per-round rate tracks module mix almost exactly across all four drains - **selection artefact, not band exhaustion**. Remaining pool 198 / 57,976 B via `pool_freshness.py`, reconciling exactly with the lane's 215 minus 17. **Cumulative pinned at 11/84 = 13.1%**, superseding three competing figures; `codegen-walls.md`'s BR ledger stops at BR-9 and needs BR-10/BR-11 backfilled. **PR #1615 held:** its `unittest` check is red on two real pre-existing defects the new guard exposed - a shallow `actions/checkout` without `fetch-depth: 0`, and `make_kickoff._run_pool` executing the hard `python3.13` pin on a 3.11 Linux runner. Brain declined to fix the second, which round 0903 reserved as unverifiable without the Mac. **Brain findings:** `m2ctx.py` hardcodes `gcc` (absent here, reproduced), so `build_context` raises for every module with a `*_core.h` - i.e. exactly `ov002` - and PR #1616's new `context_error` field is written at `cmatch_loop.py:361` and **read nowhere**. **Gate caught real drift again:** three SHA1 PASS then a red research-index test, with `[exited with code 0]` printed directly under `GATE FAIL`; fixed on the lane's branch and re-gated to PASS. ✅ **Control 12 EXECUTABLE for the first time in nine rounds** - both lanes ran in Claude Code, both transcripts read, neither report contradicted the artefacts. |

Rounds before 0817 predate this log and are deliberately not reconstructed —
the per-round research docs and `docs/research/brain-rounds-*.md` archives are
the record there. Backfilling from memory would violate the log's own premise
(state is captured, never recalled).
