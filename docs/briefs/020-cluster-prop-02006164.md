### decomper/cluster-prop-02006164

**Goal:** Cluster-propagation pilot round 4 against the new top-HIGH
cluster after wave 17 drained the previous one. Anchor:
`main / func_02006164` — size 0x18, sig 1, **28 matched / 23 unmatched
siblings**, predicted yield ≈74% [HIGH] per
`tools/find_pattern_clusters.py --version eur --top 8`.

**Context:**

- Wave 17 (PR #293, 66 matches) drained `func_0209de5c` (the
  previous post-#278 top-HIGH anchor) — that cluster fell out of
  the top 8.
- The new top-HIGH `func_02006164` is itself one of wave 17's
  matches (landed via #288), which is convenient: the anchor is
  fresh in src/, the shape is well-understood, and there are
  already 28 byte-identical siblings in the cluster (high template
  confidence relative to brief 015's 38 matched / brief 016's 11
  matched).
- The anchor reads bit 1 of `data_02103d74[0x28]` via a 32-bit
  load + lsl 30 + lsr 31 idiom encoded as
  `unsigned int bit_1 : 1;` in a packed bitfield struct
  (`src/main/func_02006164.c`, 20 lines).
- Predicted yield 74% × 23 unmatched ≈ **17 expected matches**.
  Target band: 12–18 byte-identical siblings, in line with #264's
  94.7% pilot and ahead of brief 015's 12.5%.

**Scope:**

- Match the unmatched siblings of `func_02006164`. Each sibling
  reads a different bit of `data_02103d74[0x28]` (or a sibling
  bitfield struct at a different address) using the same shape.
  The `propagate_template` flow ships per-sibling files in
  `src/main/`.
- Rename matched symbols in `config/eur/arm9/symbols.txt` per
  AGENTS.md convention.
- A few siblings may differ on the immediate (the `lsl #N` shift
  amount = 31 - bit_position). `propagate_template --substitute-imm`
  is **not** available (brief 018's second half was superseded —
  see `docs/state.md`), so for siblings whose only difference is
  the bit position, hand-patch the bitfield-name and shift constants
  per the per-sibling disassembly. Cap at 3 hand-patches per PR;
  beyond that the cluster fingerprint is too coarse and the brief
  closes.
- Standard verification gate per AGENTS.md: `ninja rom`, `dsd
  check modules` (must remain 24/27), `check_match_invariants` (no
  new warnings), `next_targets.py` (easy + hard tier delta matches
  PR claim).

**Non-scope:**

- ❌ Anything outside the `func_02006164` cluster's 23 unmatched
  siblings.
- ❌ Renaming `data_02103d74` itself or restructuring the
  `state_28_t` struct (cluster siblings might use entirely
  different addresses; only the *shape* propagates).
- ❌ Touching `tools/`, `libs/`, `include/`, `AGENTS.md`,
  `docs/state.md`.
- ❌ Pursuing the LOW-yield ov006 anchors (rank #1 + #6 in the
  current cluster ranking) — they'll need cluster-tooling work
  before they're worth a pilot.

**Success:**

- ≥ 12 byte-identical matches in `src/main/` from the cluster's 23
  unmatched siblings (12/23 = 52%, comfortable lower bound for the
  74% predicted yield).
- All matches verified locally + `pr-tier-delta` green.
- PR body documents:
  - Sibling list with per-sibling bit position / address / size.
  - Any siblings dropped (with one-line reason — codegen quirk,
    different shape, etc.).
  - Confirmation the 24/27 dsd-check baseline holds.

**Branch:** `decomper/cluster-prop-02006164`

**After this brief lands:**

1. Brain queues the next pilot anchor from the post-PR cluster
   ranking. Likely candidates: the rank-#4 `func_0202b0e0` HIGH
   cluster (10 matched / 31 unmatched, 74% yield) or the rank-#3
   `func_02033f10` MED cluster (16 matched / 31 unmatched, 37%
   yield).
2. If the LOW-yield ov006 clusters (`func_ov006_021c81a4`,
   `func_ov006_021b7ce0`) stay stuck at the top of the unmatched
   pool, brain queues a research / cluster-tooling brief for cloud
   to investigate why they're LOW.
