"""Shared tooling package.

The command-line entry points under this directory remain runnable as
``python tools/<script>.py``; the package surface is for shared imports and
tests.  Build.ninja continues to invoke the scripts directly.
"""

