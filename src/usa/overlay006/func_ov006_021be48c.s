; func_ov006_021be48c — whole-function ship-as-.s (GLOBAL_ASM endgame, brief 302):
; the original disassembly emitted verbatim as a byte-exact mwasm TU.
; For reg-alloc-walled functions with no C match (brief 294 endgame).

        .text
        .global func_ov006_021be48c
        .arm
func_ov006_021be48c:
    mov r1, #0x6
    str r1, [r0, #0x88]
    mov r1, #0x15
    str r1, [r0, #0x8c]
    mov r1, #0x0
    str r1, [r0, #0x90]
    mov r0, #0x1
    bx lr
